# Gemini Bootstrap

Read `README.md`, `MEMORY_ARCHITECTURE.md`, `KNOWLEDGE_MAP.md`, `knowledge-map/KNOWLEDGE_MAP_ARCHITECTURE.md`, `DOCTRINE_ENFORCEMENT.md`, and `TOOLBOX.md`; then resume from `MEMORY.md`. Track work in `docs/tasks/` (no code change without a task-tree leaf owning it first), durable decisions in `docs/decisions/`, durable retrieval facts through the Knowledge Map, and commit per `COMMIT.md`. Every doctrine is mechanically gated by `scripts/check_doctrines.sh` (the git hook and CI run it).
