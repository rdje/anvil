/target
/book/book-out
Cargo.lock
*.swp

# Untracked commit-message scratchpad. See COMMIT.md.
git_message_brief.txt

# Claude Code harness runtime artifacts. The committed, team-wide config
# is .claude/settings.json (tracked); everything below is per-machine /
# per-session runtime or personal overrides and must never be committed.
.claude/scheduled_tasks.lock
.claude/worktrees/
.claude/settings.local.json

# Per-machine local reference cache (e.g. the SystemVerilog LRM in Markdown
# under .cache/local-references/sv/). Untracked grounding material for agents;
# never part of the repo's signoff surface.
.cache/

# Python bytecode from scripts/*.py. Created whenever one of the book-proof
# instruments is imported as a module (rather than run as a program) — which is
# how a leaf reuses another proof's normalizer instead of forking it.
__pycache__/
