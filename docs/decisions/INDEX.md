# Decision Records (Layer C of `MEMORY_ARCHITECTURE.md`)

Durable, cross-cutting facts and decisions that must survive across
sessions, AI models, and harnesses. Use one record per file, dated, with
`Context -> Decision -> Consequences`. Append, dedupe, and supersede; do
not silently rewrite history.

This is memory layer C. Facts that outlive one task-tree but should not
live in the bounded resume pointer (`MEMORY.md`) belong here. Work-state
memory stays in task trees under `docs/tasks/`; history of what changed
lives in git.

Decision records may also carry Knowledge Map front matter. A non-empty
`answers:` list makes the record discoverable in the generated
`KNOWLEDGE_MAP.md` retrieval index.

| # | Title | Date | Status | Tags |
| --- | --- | --- | --- | --- |
| [0001](0001-task-tree-and-commit-doctrine.md) | Task-tree ownership before work; strict commit workflow | 2026-06-04 | accepted | process, doctrine |
| [0002](0002-live-doc-path-portability.md) | Live docs and book use repo-root-relative project paths | 2026-06-04 | accepted | docs, portability |
| [0003](0003-resource-safe-validation.md) | Full-suite validation is resource-monitored and not mandatory for workflow-doc memory and retrieval leaves | 2026-06-04 | accepted | validation, environment, safety |
| [0004](0004-agent-introspection-mcp-lane.md) | Agent control + deep introspection exposed as a default-off MCP adapter beside the generator core | 2026-06-14 | accepted | mcp, agent, api, architecture, introspection |
| [0005](0005-agent-mcp-expansion-surface.md) | Broaden the read-mostly agent/MCP surface by projecting recorded facts, routing non-DUT lanes, and adding an optional HTTP transport | 2026-06-15 | accepted | mcp, agent, coverage, transport, architecture, introspection |
| [0006](0006-signoff-automation-first-increment.md) | The first SIGNOFF-AUTOMATION-EXPANSION increment promotes unswept generator knobs into explicit matrix axes + coverage facts | 2026-06-15 | accepted | signoff, tool-matrix, coverage, adversarial, sweep, quality |
| [0007](0007-identity-deepening-first-extension.md) | The first IDENTITY-DEEPENING extension is bounded bisimulation-based sequential flop equivalence (default-off, reusing the bounded combinational endpoint proof) | 2026-06-15 | accepted | identity, sequential, factorization, bisimulation, coinduction, flop-merge |
| [0008](0008-identity-deepening-whole-module-sequential-equivalence.md) | The second IDENTITY-DEEPENING extension is bounded whole-leaf-module sequential equivalence via cross-module bisimulation (default-off, beside dedup_semantic_modules) | 2026-06-15 | accepted | identity, sequential, factorization, bisimulation, coinduction, module-dedup, hierarchy |
| [0009](0009-sv-version-targeting.md) | ANVIL gains a --sv-version capability gate that targets a chosen IEEE 1800 standard valid-by-construction (default byte-identical) | 2026-06-15 | accepted | capability, sv-version, emission, downstream, valid-by-construction, north-star, breadth |
| [0010](0010-sv-version-first-upopt-soft-packed-union.md) | ANVIL's first version-distinctive up-opt is a heterogeneous-width packed `union soft` (IEEE 1800-2023 §7.3.1), default-off / byte-identical | 2026-06-16 | accepted | capability, sv-version, up-opt, emission, downstream, soft-union, 2023, valid-by-construction, north-star |
| [0011](0011-semantic-introspection-derived-query-surface.md) | ANVIL gains a first-class, MCP-queryable, SCHEMA-DERIVED derived-relation introspection API; the first query is the transitive support cone of an output | 2026-06-16 | accepted | introspection, mcp, semantic, derived-query, schema-derived, agent, api, north-star |
| [0012](0012-structured-emission-first-surface-combinational-function.md) | ANVIL's first richer-structured SV surface is a default-off, valid-by-construction combinational `function automatic` emit-projection of an existing cone | 2026-06-16 | accepted | capability, structured-emission, function, emission, downstream, valid-by-construction, rules-first, breadth, north-star |
| [0013](0013-structured-emission-second-surface-generate-loop.md) | ANVIL's second richer-structured SV surface is a default-off, valid-by-construction `generate for` loop emit-projection of an existing replicated construction | 2026-06-16 | accepted | capability, structured-emission, generate, genvar, emission, downstream, valid-by-construction, rules-first, breadth, north-star |
| [0014](0014-structured-emission-third-surface-combinational-task.md) | ANVIL's third richer-structured SV surface is a default-off, valid-by-construction combinational `task automatic` emit-projection of an existing combinational gate | 2026-06-16 | accepted | capability, structured-emission, task, emission, downstream, valid-by-construction, rules-first, breadth, north-star |
| [0015](0015-structured-emission-fourth-surface-wide-lane-generate-loop.md) | ANVIL's fourth richer-structured SV surface is a default-off, valid-by-construction wider-lane `generate for` part-select emit-projection of an existing `{N{x}}` replication | 2026-06-17 | accepted | capability, structured-emission, generate, genvar, part-select, emission, downstream, valid-by-construction, rules-first, breadth, north-star |
| [0016](0016-structured-emission-fifth-surface-cone-function.md) | ANVIL's fifth richer-structured SV surface is a default-off, valid-by-construction multi-gate-cone `function automatic` emit-projection of an existing combinational cone | 2026-06-17 | accepted | capability, structured-emission, function, cone, emission, downstream, valid-by-construction, rules-first, breadth, north-star |
| [0017](0017-api-first-everything-mcp-accessible.md) | API-first mandate — every meaningful ANVIL feature must be MCP-accessible, controllable, steerable, and queryable; deep semantic introspection is first-class | 2026-06-17 | accepted | mcp, api, agent, introspection, steerable, queryable, doctrine, north-star, architecture, automation |
| [0018](0018-bug-hunt-orchestration-loop.md) | The turnkey downstream bug-hunt loop is a thin, MCP-driveable orchestrator (fuzz → detect → minimize → reproducer bundle) composing the existing validate/minimize/diff-sim/introspect surfaces | 2026-06-17 | accepted | usability, bug-hunt, mcp, api, downstream, minimize, reproducer, diff-sim, north-star, orchestration, agent |
| [0019](0019-acceptance-divergence-hunting.md) | Acceptance divergence is a first-class, default-off, SCHEMA-DERIVED detector — per-tool accept/warn/reject verdicts + a divergence classifier over the existing run_verilator/run_yosys/run_iverilog primitives — shared by the hunt loop, a tool_matrix column, and an MCP controlled tool; tool-version-vs-version is a later increment | 2026-06-17 | accepted | usability, divergence, downstream, acceptance, mcp, api, minimize, reproducer, diff-sim, north-star, orchestration, agent |
| [0020](0020-downstream-adapter-interface.md) | ANVIL's downstream reach becomes pluggable through a closed, compile-time Adapter registry over the one run_tool runner + the one tool_verdict classifier — new tools (sv2v first, slang second) plug in as opt-in acceptance columns selectable and queryable over the same API, with the allow-list/sandbox/RAM-guard discipline preserved | 2026-06-17 | accepted | usability, downstream, adapter, registry, mcp, api, acceptance, sv2v, slang, surelog, north-star, breadth, architecture, agent |
| [0021](0021-knob-ergonomics-presets-and-queryable-catalog.md) | Knob ergonomics — promote 16 of 19 config-only knobs to CLI flags, add a declarative `--profile` preset registry (4 curated presets), pin the default→config→profile→explicit resolution order (byte-stable), and expose an API-queryable SCHEMA-DERIVED knob catalog + preset registry; default-off / DUT byte-identical | 2026-06-18 | accepted | usability, knobs, presets, profile, cli, mcp, api, catalog, schema-derived, byte-identical, reproducible, north-star |
| [0022](0022-ci-packaging-prebuilt-binaries-and-github-action.md) | Drop-in CI packaging — a hand-rolled tag-triggered release workflow (prebuilt `anvil`/`anvil-mcp` binaries) + a composite GitHub Action wrapping `anvil hunt`/`divergence` through the same CLI-shim-over-API surface; user-installed tools (no vendoring); version-pinned + reproducible; default-off / DUT byte-identical | 2026-06-18 | accepted | usability, ci, packaging, release, github-action, bug-hunt, divergence, mcp, api, reproducible, north-star, distribution |
| [0023](0023-coverage-steered-generation.md) | Coverage-steered generation — a deterministic per-category probability-prior multiplier at the `roll_knob` site (rules-first, never generate-then-filter; one RNG draw per roll preserved ⇒ byte-stable per (seed, knobs, steering-config)), a SCHEMA-DERIVED achieved-coverage readout, and an OUTER measure→derive→re-steer feedback loop; API-settable target + API-queryable coverage; default-off / DUT byte-identical | 2026-06-21 | accepted | coverage, steering, generation, rules-first, reproducible, byte-identical, mcp, api, knobs, north-star, effectiveness |
| [0024](0024-mealy-fsm-outputs.md) | Mealy FSM outputs — a default-off combinational output decode over `(state_q, sel)` (a per-`(state, sel_value)` table mirroring `transitions`), extending the Phase-6 Moore-only `Fsm`; the `FsmOut` leaf stays opaque, only its decode reads the input-dependent `sel` cone; new `fsm_mealy_prob` knob + `num_mealy_fsm_modules` metric (schema `1.13`) + `saw_mealy_fsm_design` gate + MCP selectability; all-tool-clean, default-off / DUT byte-identical | 2026-06-22 | accepted | capability, fsm, mealy, moore, sequential, emission, downstream, valid-by-construction, rules-first, breadth, north-star |
| [0025](0025-structured-emission-sixth-surface-multi-output-task.md) | Sixth structured-emission surface — a default-off, valid-by-construction multi-output combinational `task automatic` co-emitting a mutually-fan-in-independent **pair** of co-supported gates (sharing a non-constant operand) into one task with deduplicated input formals + per-member output args, called once from `always_comb`; generalizes the single-gate `task` (decision `0014`) and reuses the cone-function dedup render primitives (decision `0016`); soundness rule = mutual fan-in independence (no cycle through the shared task); own `multi_output_task_emit_prob` knob + `num_emitted_multi_output_tasks` metric (schema `1.13 → 1.14`) + `--multi-output-task-gate` / `saw_multi_output_task_emit`; default-off / DUT byte-identical | 2026-06-22 | accepted | capability, structured-emission, task, multi-output, emission, downstream, valid-by-construction, rules-first, breadth, north-star |
| [0026](0026-doctrine-enforcement-adoption.md) | Adopt the portable doctrine-enforcement architecture (#4 of 4) — pair every load-bearing doctrine with a deterministic check run from one registry+driver (`scripts/check_doctrines.sh`, with a dangling-entry meta-check) gated by the git hook (E3) + CI (E4); first registry = `MEMORY-ARCH` + `KNOWLEDGE-MAP` (existing structural checks) + new scope-aware `CODE-CHANGE-EVIDENCE` + `TASK-TREE-OWNERSHIP`; add `TOOLBOX.md` cataloguing ANVIL's own diagnostic tools + the acceptance-checklist; workflow/docs only / DUT byte-identical | 2026-06-22 | accepted | process, doctrine, enforcement, ci, hooks, task-tree, memory-architecture, knowledge-map, toolbox, portability, north-star |
| [0027](0027-structured-emission-seventh-surface-procedural-if-else.md) | Seventh structured-emission surface — a default-off, valid-by-construction procedural `always_comb` `if`/`else` emit-projection of a `Mux` gate (the 2:1 selection it renders today as a `(sel)?(a):(b)` ternary), written into a per-gate `<wire>__cv` output var with the existing net driven by a passthrough `assign`; the first procedural-conditional construct in the lane (none of the six prior surfaces emits a procedural `if`/`else`); reuses the decision-`0014` single-gate-task output-var + passthrough mechanism; own `mux_if_emit_prob` knob + `num_emitted_mux_if_blocks` metric (schema `1.14 → 1.15`) + `--mux-if-gate` / `saw_mux_if_emit` (`__cv` detection); N-way `CaseMux` → `if`/`else if` chain recorded as the follow-up; default-off / DUT byte-identical | 2026-06-22 | accepted | capability, structured-emission, if-else, procedural, mux, always-comb, emission, downstream, valid-by-construction, rules-first, breadth, north-star |
| [0028](0028-structured-emission-eighth-surface-case-mux-priority-chain.md) | Eighth structured-emission surface — a default-off, valid-by-construction procedural `always_comb` `if`/`else if` **priority-chain** emit-projection of a `CaseMux` gate (the N-way selection it renders today as a parallel `case (sel) … default` statement), writing the gate's existing `always_comb`-written `logic` var (no `__cv` passthrough needed — unlike the seventh surface's 2:1 `Mux`); the recorded decision-`0027` follow-up; distinct from the seventh surface (N-way chain vs single 2:1 conditional) and from the `case` render (sequential-priority vs parallel-match); own `case_mux_if_emit_prob` knob + `num_emitted_case_mux_if_chains` metric (schema `1.15 → 1.16`) + `--case-mux-if-gate` / `saw_case_mux_if_emit` (metric-keyed detection); plain `CaseMux` only (the `CasezMux` masked chain recorded as the follow-up); empirically probed clean (Verilator -Wall 2012/2017/2023 + both Yosys + Icarus + iverilog sim-equiv over 20000 vectors + exhaustive sweep); default-off / DUT byte-identical | 2026-06-22 | accepted | capability, structured-emission, if-else-if, priority-chain, case-mux, procedural, always-comb, emission, downstream, valid-by-construction, rules-first, breadth, north-star |
| [0031](0031-ssd-volume-exclusivity.md) | **Standing owner directive** — the SSD is the only volume this project stores on *or points at*: no writes and no references to `/tmp`, `/private/tmp`, `$TMPDIR`, `$HOME`, or any boot-volume path, prose included; `CARGO_HOME`/`RUSTUP_HOME`/`ANVIL_SANDBOX_ROOT` repointed onto the repo volume; sole exception is the installed toolchain (execute-only, documented); amends `0002` (withdraws its `/tmp` evidence-path allowance) and `0030` (its option (iii) is now partly mandated); shared global caches are populated, never deleted | 2026-07-29 | accepted | doctrine, data-locality, volume, paths, owner-directive |
| [0030](0030-durable-closure-evidence-citations.md) | Closure evidence is cited through committed per-bank digests under `docs/evidence/` (each carrying the exact re-runnable command + report SHA-256); every pre-0030 `anvil-*` citation is a labelled historical breadcrumb re-verified only by oracle re-run; contract mechanically checkable via a `check_evidence_citations.sh` registry entry | 2026-07-29 | accepted | evidence, durability, docs, doctrine, process |
| [0029](0029-structured-emission-ninth-surface-casez-mux-masked-priority-chain.md) | Ninth structured-emission surface — a default-off, valid-by-construction procedural `always_comb` `if`/`else if` **masked** priority-chain emit-projection of a `CasezMux` gate (the wildcard N-way selection it renders today as a parallel `casez` statement), writing the gate's existing `always_comb`-written `logic` var (no `__cv` passthrough, like the eighth surface); generalizes the eighth surface's bare-equality `CaseMux` chain to the wildcard `CasezMux` via a **masked** compare `(sel & care_mask) == value_masked` (the existing `metrics.rs`/`compact.rs` `care_mask = (~wildcard_mask) & sel_mask` idiom); the concise `sel ==? pattern` form is **empirically disqualified** (Yosys 0.64 rejects `==?` in both repo modes), so the lowered masked-AND form ships; own `casez_mux_if_emit_prob` knob + `num_emitted_casez_mux_if_chains` metric (schema `1.16 → 1.17`) + `--casez-mux-if-gate` / `saw_casez_mux_if_emit` (metric-keyed detection); empirically probed clean (Verilator -Wall 2012/2017/2023 + both Yosys + Icarus + iverilog exhaustive sim-equiv incl. an overlapping-priority probe); default-off / DUT byte-identical | 2026-06-23 | accepted | capability, structured-emission, if-else-if, priority-chain, masked-comparison, casez-mux, wildcard, procedural, always-comb, emission, downstream, valid-by-construction, rules-first, breadth, north-star |
| [0033](0033-shadow-enumeration-classification.md) | A hand-maintained list is a **shadow enumeration** only if it is (1) *derivable* — another artifact in the repo already enumerates the same membership by ordinary program/script means — **and** (2) *growth-coupled* — that set grows and every growth requires a matching entry — **and** (3) *silent* — the omission produces no compile error, no failing test, no runtime error; the middle test is load-bearing because it is what keeps `check_no_boot_volume_refs.sh`'s allow-list and the frozen `EVIDENCE-CITATIONS` §1 pin authoritative (they satisfy (1) and are *supposed* to differ), and the first keeps `presets()` / `DOCTRINES` authoritative (no such set exists); four standard repairs in strict preference order — **R1** derive (the list stops existing) → **R2** make it loud (a compile error) → **R3** guard with a test that *derives* the expectation (models: `knob_catalog_classifies_every_field`, the decision-`0032` preset drift test) → **R4** a registered doctrine check, for sites with neither a compiler nor `cargo test` — under two binding constraints (a repair may not introduce a new hand-maintained list; every guard is negative-controlled both ways); the class is honestly declared **not mechanizable as discovery** (test (1) is a semantic relation between two sets, so a detector must already know the pairing, and its only failure modes are *miss* → false confidence and *cry wolf* → the gate gets deleted), only classified *pairs* are held — Rust pairs by an in-crate `#[test]` already gated by `cargo test`, docs/script pairs by **one** new `ENUMERATION-PARITY` check over a declared, itself-authoritative pairs table; severity tiers **S3** (a gate reports success it did not earn — a false green, banked as `0030` evidence) / **S2** (a user-requested behaviour silently does not happen) / **S1** (a contract understates reality — fail-safe in direction); a measured 20-site audit corrects `.1`'s ranking — `merge_coverage` drops to S1 (all 149 merges monotone ⇒ under-report only; 134 of 149 fields gated ⇒ a forgotten merge yields a spurious gap ⇒ the gate bails **loudly**; genuinely silent surface = 15 fields) and the previously-unaudited `Config::apply_cli_overrides` (87 fields, unguarded, an omission making a CLI flag *and every preset that sets it* inert) takes second place; docs-only ⇒ DUT byte-identical | 2026-07-30 | accepted | quality, doctrine, enumeration, shadow-list, derivation, drift, tool-matrix, config, coverage, mcp, adapter, book, enforcement, north-star |
| [0032](0032-emit-surface-interaction-gate.md) | The nine emit-projections are gated **in combination**, not only in isolation — a 3 + 1 `tool_matrix` sweep (three comb-only scenarios, one per construction strategy, with all eight non-version-gated `*_emit_prob` at `0.25`, plus one all-at-`1.0` saturation scenario) with `soft_union` split into its own Verilator-only 2023 scenario; co-occurrence proven **metric-keyed** from a derived `distinct_emit_surfaces` over the nine existing `ModuleReport.emitted_*` booleans (no new identifier token, the `0028` precedent); pins that "all knobs at `1.0`" is *not* "all surfaces emitted" — mutual exclusion plus the fixed pass order makes the current `structured-emission-max` preset emit **1 of 9** surfaces (measured) — so the preset is redefined as maximal surface **diversity** at `0.25`; names five interaction risks reasoned from source, four measured clean, one (`cone_function` absorption vs the uncounted `Memory`/`Fsm` consumers in `compute_use_counts`) a latent trap split out for hardening; no surface semantics or default changed ⇒ DUT byte-identical | 2026-07-30 | accepted | quality, structured-emission, interaction, mutual-exclusion, tool-matrix, gate, coverage, preset, profile, cone-function, absorption, downstream, rules-first, north-star |
| [0034](0034-one-steering-aware-knob-roll-primitive.md) | There is exactly **one** knob-roll primitive and it is steering-aware: `COVERAGE-STEERED-GENERATION.2a`'s premise that *"all steerable rolls funnel through one function"* was **false when written** — `src/gen/hierarchy.rs` had defined seven more roll primitives two months earlier that record the same `knob_rolls` telemetry while omitting `SteeringConfig::effective_prob`, so **6 of the 22 `KnobId`s were never steered** and the whole documented `hierarchy` steering category was a measured **silent no-op** (a 9x weight leaves the recorded fire counts bit-identical — `0/5` where `5/5` was requested — and an 800x spread emits byte-identical SV, while `--steer` accepts, validates, stores and echoes the weight); root cause is decision `0033` rule (2) recurring — the survey searched by the *shape* it knew (`roll_knob(` call sites) instead of from the **authoritative set** (`knob_rolls.record(`), which returns 8 sites in 2 files; fix = extract one `roll_knob_into(&mut KnobRollCounters, &SteeringConfig, rng, knob, prob)` primitive, delete the seven hand-rolled helpers (seven one-line `KnobId` wrappers are themselves a shadow), and make a second primitive a **compile error** by privatizing `KnobRollCounters::record` into the primitive's module (repair rung **R2**, not a new registered doctrine); unsteered emission byte-identical, steered hierarchy emission changes (that is the fix); scope boundary pinned — the 16 further Bernoulli knobs with **no** `KnobId` (7 motif + 9 emit-projection) fail *loudly* (`unknown steer key`) and are a feature gap owned by `.4`, while `operand_duplication_rate`/`mux_arm_duplication_rate` (dedup thresholds, not rolls) and the reader-less `library_prob` are excluded by kind | 2026-07-30 | accepted | steering, coverage, knob-roll, hierarchy, full-factorization, silent-no-op, defect, rules-first, api-completeness, north-star |
| [0035](0035-steering-width-motif-and-emission-knobs.md) | Steering's **width**: after `0034` fixed its *reach*, the remaining gap is the **16** `Config` Bernoulli knobs with no `KnobId` at all (7 module-level **motif** rolls + 9 per-gate **emit-projection** rolls, out of 41 validated probability knobs, 22 of which are already steerable) — a **feature gap, not a defect**, because their symptom is *loud* (`--steer memory_prob=2.0` ⇒ `unknown steer key`), which is why `0034` shipped first and alone; the real cost is that the outer measure→derive→re-steer loop is blind to **nine of ANVIL's most interesting surfaces** (the whole structured-emission family decision `0032` calls the DUT lane's best bug-bait has no `knob_fire_rates` entry); decision: **two new categories** `motifs` and `emission` (folding into the existing six rejected — `memory_prob` is not `state`, rendering is not selection; one merged `capabilities` category rejected because per-module and per-gate roll granularities differ by two orders of magnitude and a mixed average is meaningless — the measured `.3b` calibration failure is the evidence), the nine `annotate_*` passes gain a `&SteeringConfig` param (not `&Config`), and the existing six categories keep their exact membership so every working steering config keeps meaning the same thing; the `if cfg.<knob> > 0.0` guard is pinned **load-bearing for reproducibility** (removing it would consume 16+ extra RNG draws on the default path), with its honest consequence stated — a default-off knob records `attempts = 0`, so the readout cannot distinguish *off* from *never reached*; **no schema bump**, with the rule stated for future contributors: bump for a new payload key or query kind, not for more entries in an existing map; three knobs excluded **by kind** (`operand_duplication_rate`/`mux_arm_duplication_rate` are dedup thresholds with no roll to prior-multiply; `library_prob` has no reader in `src/`) since a paper category is a ROADMAP steering-gap-1 regression; flags an unexamined hard-coded `gen_bool(0.5)` at `src/gen/module.rs:143` and that `KnobId::all()` reaching 38 hand-maintained entries needs an R2 guard (proposed: an exhaustive private `index()` match + a derived length test); split `.4b.1` (7 motif knobs) / `.4b.2` (9 emit knobs, ~99 call sites) / `.4c` (docs); docs-only ⇒ DUT byte-identical | 2026-07-30 | accepted | steering, coverage, knob-roll, motifs, structured-emission, api-completeness, rules-first, telemetry, north-star |
| [0036](0036-readme-landing-page-restoration.md) | The README is restored to a landing page by **deletion, not relocation**. Measured at `fcb9e58`: **1771 lines / 122,767 bytes** vs the policy's illustrative `300`/`16,384`, with two sections at **92 %** of the file — but everything the policy's content contract asks for totals **141 lines**, so *the landing page already exists and already fits*. `## Current CLI truth` (64 %) is **not a CLI reference**: **zero** command lines, 50 design essays averaging ~23 lines, 33 citing a decision record — and a phrase probe proves it is a **lossy copy** (`__cv` appears 7x in README vs 15x in decision `0027` and 19x in the book; `care_mask` 2x vs 9x in `0029`), so the content needs **deleting with a link**, not the expensive and risky 1141-line relocation. `## Build and validation` splits the same way: a genuine quick start plus gate invocations around 78 `saw_*` fact lines and 15 banked tallies that decision `0030` already built `docs/evidence/` to hold. **A generated CLI reference is REJECTED** — the option this decision was asked to weigh, and the one initially favoured before measuring: there is nothing to generate (zero command lines), the SCHEMA-DERIVED reference **already exists** (decision `0021`'s queryable knob catalog + `--dump-config`), so a generated Markdown surface would be a **fourth** copy of solved truth and would re-import the drift it claims to prevent (template + build step + no-diff gate is justified against a hand-maintained list, not against *a link*). Caps **250 lines / 12,288 bytes**, derived from the measured survivors (141 + trimmed quick start + navigation ⇒ ~186) and deliberately **below** the policy's illustrative numbers, which would leave 60 % headroom to regrow into. Enforced as the **8th registered doctrine** `README-GROWTH` (hook + CI per the policy's own clause, reusing `DOCTRINE_ENFORCEMENT.md` rather than a standalone script) failing with a **routing hint** naming the canonical home per kind. `.2` must run the phrase probe per bullet before deleting — the only place information could be lost. Expected result: **1771 → ~186 lines, an 89 % reduction with no information lost**; docs-only ⇒ DUT byte-identical | 2026-07-30 | accepted | docs, readme, policy, shadow-list, de-duplication, doctrine, enforcement, workflow |

## How To Add A Record

1. Copy the shape of an existing record.
2. Use the next sequential number.
3. Add a row to this index.
4. Link the record from related task-tree files when relevant.
5. To change a fact, add a new record or mark the old one superseded; do
   not silently rewrite the old decision.
