# Decision Records (Layer C of `MEMORY_ARCHITECTURE.md`)

Durable, cross-cutting facts and decisions that must survive across
sessions, AI models, and harnesses. Use one record per file, dated, with
`Context -> Decision -> Consequences`. Append, dedupe, and supersede; do
not silently rewrite history.

This is memory layer C. Facts that outlive one task-tree but should not
live in the bounded resume pointer (`MEMORY.md`) belong here. Work-state
memory stays in task trees under `docs/tasks/`; history of what changed
lives in git.

Decision records may also carry Knowledge Map front matter. A non-empty
`answers:` list makes the record discoverable in the generated
`KNOWLEDGE_MAP.md` retrieval index.

| # | Title | Date | Status | Tags |
| --- | --- | --- | --- | --- |
| [0001](0001-task-tree-and-commit-doctrine.md) | Task-tree ownership before work; strict commit workflow | 2026-06-04 | accepted | process, doctrine |
| [0002](0002-live-doc-path-portability.md) | Live docs and book use repo-root-relative project paths | 2026-06-04 | accepted | docs, portability |
| [0003](0003-resource-safe-validation.md) | Full-suite validation is resource-monitored and not mandatory for workflow-doc memory and retrieval leaves | 2026-06-04 | accepted | validation, environment, safety |
| [0004](0004-agent-introspection-mcp-lane.md) | Agent control + deep introspection exposed as a default-off MCP adapter beside the generator core | 2026-06-14 | accepted | mcp, agent, api, architecture, introspection |
| [0005](0005-agent-mcp-expansion-surface.md) | Broaden the read-mostly agent/MCP surface by projecting recorded facts, routing non-DUT lanes, and adding an optional HTTP transport | 2026-06-15 | accepted | mcp, agent, coverage, transport, architecture, introspection |
| [0006](0006-signoff-automation-first-increment.md) | The first SIGNOFF-AUTOMATION-EXPANSION increment promotes unswept generator knobs into explicit matrix axes + coverage facts | 2026-06-15 | accepted | signoff, tool-matrix, coverage, adversarial, sweep, quality |
| [0007](0007-identity-deepening-first-extension.md) | The first IDENTITY-DEEPENING extension is bounded bisimulation-based sequential flop equivalence (default-off, reusing the bounded combinational endpoint proof) | 2026-06-15 | accepted | identity, sequential, factorization, bisimulation, coinduction, flop-merge |
| [0008](0008-identity-deepening-whole-module-sequential-equivalence.md) | The second IDENTITY-DEEPENING extension is bounded whole-leaf-module sequential equivalence via cross-module bisimulation (default-off, beside dedup_semantic_modules) | 2026-06-15 | accepted | identity, sequential, factorization, bisimulation, coinduction, module-dedup, hierarchy |
| [0009](0009-sv-version-targeting.md) | ANVIL gains a --sv-version capability gate that targets a chosen IEEE 1800 standard valid-by-construction (default byte-identical) | 2026-06-15 | accepted | capability, sv-version, emission, downstream, valid-by-construction, north-star, breadth |
| [0010](0010-sv-version-first-upopt-soft-packed-union.md) | ANVIL's first version-distinctive up-opt is a heterogeneous-width packed `union soft` (IEEE 1800-2023 §7.3.1), default-off / byte-identical | 2026-06-16 | accepted | capability, sv-version, up-opt, emission, downstream, soft-union, 2023, valid-by-construction, north-star |
| [0011](0011-semantic-introspection-derived-query-surface.md) | ANVIL gains a first-class, MCP-queryable, SCHEMA-DERIVED derived-relation introspection API; the first query is the transitive support cone of an output | 2026-06-16 | accepted | introspection, mcp, semantic, derived-query, schema-derived, agent, api, north-star |
| [0012](0012-structured-emission-first-surface-combinational-function.md) | ANVIL's first richer-structured SV surface is a default-off, valid-by-construction combinational `function automatic` emit-projection of an existing cone | 2026-06-16 | accepted | capability, structured-emission, function, emission, downstream, valid-by-construction, rules-first, breadth, north-star |
| [0013](0013-structured-emission-second-surface-generate-loop.md) | ANVIL's second richer-structured SV surface is a default-off, valid-by-construction `generate for` loop emit-projection of an existing replicated construction | 2026-06-16 | accepted | capability, structured-emission, generate, genvar, emission, downstream, valid-by-construction, rules-first, breadth, north-star |
| [0014](0014-structured-emission-third-surface-combinational-task.md) | ANVIL's third richer-structured SV surface is a default-off, valid-by-construction combinational `task automatic` emit-projection of an existing combinational gate | 2026-06-16 | accepted | capability, structured-emission, task, emission, downstream, valid-by-construction, rules-first, breadth, north-star |
| [0015](0015-structured-emission-fourth-surface-wide-lane-generate-loop.md) | ANVIL's fourth richer-structured SV surface is a default-off, valid-by-construction wider-lane `generate for` part-select emit-projection of an existing `{N{x}}` replication | 2026-06-17 | accepted | capability, structured-emission, generate, genvar, part-select, emission, downstream, valid-by-construction, rules-first, breadth, north-star |
| [0016](0016-structured-emission-fifth-surface-cone-function.md) | ANVIL's fifth richer-structured SV surface is a default-off, valid-by-construction multi-gate-cone `function automatic` emit-projection of an existing combinational cone | 2026-06-17 | accepted | capability, structured-emission, function, cone, emission, downstream, valid-by-construction, rules-first, breadth, north-star |
| [0017](0017-api-first-everything-mcp-accessible.md) | API-first mandate — every meaningful ANVIL feature must be MCP-accessible, controllable, steerable, and queryable; deep semantic introspection is first-class | 2026-06-17 | accepted | mcp, api, agent, introspection, steerable, queryable, doctrine, north-star, architecture, automation |
| [0018](0018-bug-hunt-orchestration-loop.md) | The turnkey downstream bug-hunt loop is a thin, MCP-driveable orchestrator (fuzz → detect → minimize → reproducer bundle) composing the existing validate/minimize/diff-sim/introspect surfaces | 2026-06-17 | accepted | usability, bug-hunt, mcp, api, downstream, minimize, reproducer, diff-sim, north-star, orchestration, agent |
| [0019](0019-acceptance-divergence-hunting.md) | Acceptance divergence is a first-class, default-off, SCHEMA-DERIVED detector — per-tool accept/warn/reject verdicts + a divergence classifier over the existing run_verilator/run_yosys/run_iverilog primitives — shared by the hunt loop, a tool_matrix column, and an MCP controlled tool; tool-version-vs-version is a later increment | 2026-06-17 | accepted | usability, divergence, downstream, acceptance, mcp, api, minimize, reproducer, diff-sim, north-star, orchestration, agent |
| [0020](0020-downstream-adapter-interface.md) | ANVIL's downstream reach becomes pluggable through a closed, compile-time Adapter registry over the one run_tool runner + the one tool_verdict classifier — new tools (sv2v first, slang second) plug in as opt-in acceptance columns selectable and queryable over the same API, with the allow-list/sandbox/RAM-guard discipline preserved | 2026-06-17 | accepted | usability, downstream, adapter, registry, mcp, api, acceptance, sv2v, slang, surelog, north-star, breadth, architecture, agent |
| [0021](0021-knob-ergonomics-presets-and-queryable-catalog.md) | Knob ergonomics — promote 16 of 19 config-only knobs to CLI flags, add a declarative `--profile` preset registry (4 curated presets), pin the default→config→profile→explicit resolution order (byte-stable), and expose an API-queryable SCHEMA-DERIVED knob catalog + preset registry; default-off / DUT byte-identical | 2026-06-18 | accepted | usability, knobs, presets, profile, cli, mcp, api, catalog, schema-derived, byte-identical, reproducible, north-star |
| [0022](0022-ci-packaging-prebuilt-binaries-and-github-action.md) | Drop-in CI packaging — a hand-rolled tag-triggered release workflow (prebuilt `anvil`/`anvil-mcp` binaries) + a composite GitHub Action wrapping `anvil hunt`/`divergence` through the same CLI-shim-over-API surface; user-installed tools (no vendoring); version-pinned + reproducible; default-off / DUT byte-identical | 2026-06-18 | accepted | usability, ci, packaging, release, github-action, bug-hunt, divergence, mcp, api, reproducible, north-star, distribution |
| [0023](0023-coverage-steered-generation.md) | Coverage-steered generation — a deterministic per-category probability-prior multiplier at the `roll_knob` site (rules-first, never generate-then-filter; one RNG draw per roll preserved ⇒ byte-stable per (seed, knobs, steering-config)), a SCHEMA-DERIVED achieved-coverage readout, and an OUTER measure→derive→re-steer feedback loop; API-settable target + API-queryable coverage; default-off / DUT byte-identical | 2026-06-21 | accepted | coverage, steering, generation, rules-first, reproducible, byte-identical, mcp, api, knobs, north-star, effectiveness |
| [0024](0024-mealy-fsm-outputs.md) | Mealy FSM outputs — a default-off combinational output decode over `(state_q, sel)` (a per-`(state, sel_value)` table mirroring `transitions`), extending the Phase-6 Moore-only `Fsm`; the `FsmOut` leaf stays opaque, only its decode reads the input-dependent `sel` cone; new `fsm_mealy_prob` knob + `num_mealy_fsm_modules` metric (schema `1.13`) + `saw_mealy_fsm_design` gate + MCP selectability; all-tool-clean, default-off / DUT byte-identical | 2026-06-22 | accepted | capability, fsm, mealy, moore, sequential, emission, downstream, valid-by-construction, rules-first, breadth, north-star |
| [0025](0025-structured-emission-sixth-surface-multi-output-task.md) | Sixth structured-emission surface — a default-off, valid-by-construction multi-output combinational `task automatic` co-emitting a mutually-fan-in-independent **pair** of co-supported gates (sharing a non-constant operand) into one task with deduplicated input formals + per-member output args, called once from `always_comb`; generalizes the single-gate `task` (decision `0014`) and reuses the cone-function dedup render primitives (decision `0016`); soundness rule = mutual fan-in independence (no cycle through the shared task); own `multi_output_task_emit_prob` knob + `num_emitted_multi_output_tasks` metric (schema `1.13 → 1.14`) + `--multi-output-task-gate` / `saw_multi_output_task_emit`; default-off / DUT byte-identical | 2026-06-22 | accepted | capability, structured-emission, task, multi-output, emission, downstream, valid-by-construction, rules-first, breadth, north-star |
| [0026](0026-doctrine-enforcement-adoption.md) | Adopt the portable doctrine-enforcement architecture (#4 of 4) — pair every load-bearing doctrine with a deterministic check run from one registry+driver (`scripts/check_doctrines.sh`, with a dangling-entry meta-check) gated by the git hook (E3) + CI (E4); first registry = `MEMORY-ARCH` + `KNOWLEDGE-MAP` (existing structural checks) + new scope-aware `CODE-CHANGE-EVIDENCE` + `TASK-TREE-OWNERSHIP`; add `TOOLBOX.md` cataloguing ANVIL's own diagnostic tools + the acceptance-checklist; workflow/docs only / DUT byte-identical | 2026-06-22 | accepted | process, doctrine, enforcement, ci, hooks, task-tree, memory-architecture, knowledge-map, toolbox, portability, north-star |
| [0027](0027-structured-emission-seventh-surface-procedural-if-else.md) | Seventh structured-emission surface — a default-off, valid-by-construction procedural `always_comb` `if`/`else` emit-projection of a `Mux` gate (the 2:1 selection it renders today as a `(sel)?(a):(b)` ternary), written into a per-gate `<wire>__cv` output var with the existing net driven by a passthrough `assign`; the first procedural-conditional construct in the lane (none of the six prior surfaces emits a procedural `if`/`else`); reuses the decision-`0014` single-gate-task output-var + passthrough mechanism; own `mux_if_emit_prob` knob + `num_emitted_mux_if_blocks` metric (schema `1.14 → 1.15`) + `--mux-if-gate` / `saw_mux_if_emit` (`__cv` detection); N-way `CaseMux` → `if`/`else if` chain recorded as the follow-up; default-off / DUT byte-identical | 2026-06-22 | accepted | capability, structured-emission, if-else, procedural, mux, always-comb, emission, downstream, valid-by-construction, rules-first, breadth, north-star |
| [0028](0028-structured-emission-eighth-surface-case-mux-priority-chain.md) | Eighth structured-emission surface — a default-off, valid-by-construction procedural `always_comb` `if`/`else if` **priority-chain** emit-projection of a `CaseMux` gate (the N-way selection it renders today as a parallel `case (sel) … default` statement), writing the gate's existing `always_comb`-written `logic` var (no `__cv` passthrough needed — unlike the seventh surface's 2:1 `Mux`); the recorded decision-`0027` follow-up; distinct from the seventh surface (N-way chain vs single 2:1 conditional) and from the `case` render (sequential-priority vs parallel-match); own `case_mux_if_emit_prob` knob + `num_emitted_case_mux_if_chains` metric (schema `1.15 → 1.16`) + `--case-mux-if-gate` / `saw_case_mux_if_emit` (metric-keyed detection); plain `CaseMux` only (the `CasezMux` masked chain recorded as the follow-up); empirically probed clean (Verilator -Wall 2012/2017/2023 + both Yosys + Icarus + iverilog sim-equiv over 20000 vectors + exhaustive sweep); default-off / DUT byte-identical | 2026-06-22 | accepted | capability, structured-emission, if-else-if, priority-chain, case-mux, procedural, always-comb, emission, downstream, valid-by-construction, rules-first, breadth, north-star |
| [0031](0031-ssd-volume-exclusivity.md) | **Standing owner directive** — the SSD is the only volume this project stores on *or points at*: no writes and no references to `/tmp`, `/private/tmp`, `$TMPDIR`, `$HOME`, or any boot-volume path, prose included; `CARGO_HOME`/`RUSTUP_HOME`/`ANVIL_SANDBOX_ROOT` repointed onto the repo volume; sole exception is the installed toolchain (execute-only, documented); amends `0002` (withdraws its `/tmp` evidence-path allowance) and `0030` (its option (iii) is now partly mandated); shared global caches are populated, never deleted | 2026-07-29 | accepted | doctrine, data-locality, volume, paths, owner-directive |
| [0030](0030-durable-closure-evidence-citations.md) | Closure evidence is cited through committed per-bank digests under `docs/evidence/` (each carrying the exact re-runnable command + report SHA-256); every pre-0030 `anvil-*` citation is a labelled historical breadcrumb re-verified only by oracle re-run; contract mechanically checkable via a `check_evidence_citations.sh` registry entry | 2026-07-29 | accepted | evidence, durability, docs, doctrine, process |
| [0029](0029-structured-emission-ninth-surface-casez-mux-masked-priority-chain.md) | Ninth structured-emission surface — a default-off, valid-by-construction procedural `always_comb` `if`/`else if` **masked** priority-chain emit-projection of a `CasezMux` gate (the wildcard N-way selection it renders today as a parallel `casez` statement), writing the gate's existing `always_comb`-written `logic` var (no `__cv` passthrough, like the eighth surface); generalizes the eighth surface's bare-equality `CaseMux` chain to the wildcard `CasezMux` via a **masked** compare `(sel & care_mask) == value_masked` (the existing `metrics.rs`/`compact.rs` `care_mask = (~wildcard_mask) & sel_mask` idiom); the concise `sel ==? pattern` form is **empirically disqualified** (Yosys 0.64 rejects `==?` in both repo modes), so the lowered masked-AND form ships; own `casez_mux_if_emit_prob` knob + `num_emitted_casez_mux_if_chains` metric (schema `1.16 → 1.17`) + `--casez-mux-if-gate` / `saw_casez_mux_if_emit` (metric-keyed detection); empirically probed clean (Verilator -Wall 2012/2017/2023 + both Yosys + Icarus + iverilog exhaustive sim-equiv incl. an overlapping-priority probe); default-off / DUT byte-identical | 2026-06-23 | accepted | capability, structured-emission, if-else-if, priority-chain, masked-comparison, casez-mux, wildcard, procedural, always-comb, emission, downstream, valid-by-construction, rules-first, breadth, north-star |
| [0033](0033-shadow-enumeration-classification.md) | A hand-maintained list is a **shadow enumeration** only if it is (1) *derivable* — another artifact in the repo already enumerates the same membership by ordinary program/script means — **and** (2) *growth-coupled* — that set grows and every growth requires a matching entry — **and** (3) *silent* — the omission produces no compile error, no failing test, no runtime error; the middle test is load-bearing because it is what keeps `check_no_boot_volume_refs.sh`'s allow-list and the frozen `EVIDENCE-CITATIONS` §1 pin authoritative (they satisfy (1) and are *supposed* to differ), and the first keeps `presets()` / `DOCTRINES` authoritative (no such set exists); four standard repairs in strict preference order — **R1** derive (the list stops existing) → **R2** make it loud (a compile error) → **R3** guard with a test that *derives* the expectation (models: `knob_catalog_classifies_every_field`, the decision-`0032` preset drift test) → **R4** a registered doctrine check, for sites with neither a compiler nor `cargo test` — under two binding constraints (a repair may not introduce a new hand-maintained list; every guard is negative-controlled both ways); the class is honestly declared **not mechanizable as discovery** (test (1) is a semantic relation between two sets, so a detector must already know the pairing, and its only failure modes are *miss* → false confidence and *cry wolf* → the gate gets deleted), only classified *pairs* are held — Rust pairs by an in-crate `#[test]` already gated by `cargo test`, docs/script pairs by **one** new `ENUMERATION-PARITY` check over a declared, itself-authoritative pairs table; severity tiers **S3** (a gate reports success it did not earn — a false green, banked as `0030` evidence) / **S2** (a user-requested behaviour silently does not happen) / **S1** (a contract understates reality — fail-safe in direction); a measured 20-site audit corrects `.1`'s ranking — `merge_coverage` drops to S1 (all 149 merges monotone ⇒ under-report only; 134 of 149 fields gated ⇒ a forgotten merge yields a spurious gap ⇒ the gate bails **loudly**; genuinely silent surface = 15 fields) and the previously-unaudited `Config::apply_cli_overrides` (87 fields, unguarded, an omission making a CLI flag *and every preset that sets it* inert) takes second place; docs-only ⇒ DUT byte-identical | 2026-07-30 | accepted | quality, doctrine, enumeration, shadow-list, derivation, drift, tool-matrix, config, coverage, mcp, adapter, book, enforcement, north-star |
| [0032](0032-emit-surface-interaction-gate.md) | The nine emit-projections are gated **in combination**, not only in isolation — a 3 + 1 `tool_matrix` sweep (three comb-only scenarios, one per construction strategy, with all eight non-version-gated `*_emit_prob` at `0.25`, plus one all-at-`1.0` saturation scenario) with `soft_union` split into its own Verilator-only 2023 scenario; co-occurrence proven **metric-keyed** from a derived `distinct_emit_surfaces` over the nine existing `ModuleReport.emitted_*` booleans (no new identifier token, the `0028` precedent); pins that "all knobs at `1.0`" is *not* "all surfaces emitted" — mutual exclusion plus the fixed pass order makes the current `structured-emission-max` preset emit **1 of 9** surfaces (measured) — so the preset is redefined as maximal surface **diversity** at `0.25`; names five interaction risks reasoned from source, four measured clean, one (`cone_function` absorption vs the uncounted `Memory`/`Fsm` consumers in `compute_use_counts`) a latent trap split out for hardening; no surface semantics or default changed ⇒ DUT byte-identical | 2026-07-30 | accepted | quality, structured-emission, interaction, mutual-exclusion, tool-matrix, gate, coverage, preset, profile, cone-function, absorption, downstream, rules-first, north-star |
| [0034](0034-one-steering-aware-knob-roll-primitive.md) | There is exactly **one** knob-roll primitive and it is steering-aware: `COVERAGE-STEERED-GENERATION.2a`'s premise that *"all steerable rolls funnel through one function"* was **false when written** — `src/gen/hierarchy.rs` had defined seven more roll primitives two months earlier that record the same `knob_rolls` telemetry while omitting `SteeringConfig::effective_prob`, so **6 of the 22 `KnobId`s were never steered** and the whole documented `hierarchy` steering category was a measured **silent no-op** (a 9x weight leaves the recorded fire counts bit-identical — `0/5` where `5/5` was requested — and an 800x spread emits byte-identical SV, while `--steer` accepts, validates, stores and echoes the weight); root cause is decision `0033` rule (2) recurring — the survey searched by the *shape* it knew (`roll_knob(` call sites) instead of from the **authoritative set** (`knob_rolls.record(`), which returns 8 sites in 2 files; fix = extract one `roll_knob_into(&mut KnobRollCounters, &SteeringConfig, rng, knob, prob)` primitive, delete the seven hand-rolled helpers (seven one-line `KnobId` wrappers are themselves a shadow), and make a second primitive a **compile error** by privatizing `KnobRollCounters::record` into the primitive's module (repair rung **R2**, not a new registered doctrine); unsteered emission byte-identical, steered hierarchy emission changes (that is the fix); scope boundary pinned — the 16 further Bernoulli knobs with **no** `KnobId` (7 motif + 9 emit-projection) fail *loudly* (`unknown steer key`) and are a feature gap owned by `.4`, while `operand_duplication_rate`/`mux_arm_duplication_rate` (dedup thresholds, not rolls) and the reader-less `library_prob` are excluded by kind | 2026-07-30 | accepted | steering, coverage, knob-roll, hierarchy, full-factorization, silent-no-op, defect, rules-first, api-completeness, north-star |
| [0035](0035-steering-width-motif-and-emission-knobs.md) | Steering's **width**: after `0034` fixed its *reach*, the remaining gap is the **16** `Config` Bernoulli knobs with no `KnobId` at all (7 module-level **motif** rolls + 9 per-gate **emit-projection** rolls, out of 41 validated probability knobs, 22 of which are already steerable) — a **feature gap, not a defect**, because their symptom is *loud* (`--steer memory_prob=2.0` ⇒ `unknown steer key`), which is why `0034` shipped first and alone; the real cost is that the outer measure→derive→re-steer loop is blind to **nine of ANVIL's most interesting surfaces** (the whole structured-emission family decision `0032` calls the DUT lane's best bug-bait has no `knob_fire_rates` entry); decision: **two new categories** `motifs` and `emission` (folding into the existing six rejected — `memory_prob` is not `state`, rendering is not selection; one merged `capabilities` category rejected because per-module and per-gate roll granularities differ by two orders of magnitude and a mixed average is meaningless — the measured `.3b` calibration failure is the evidence), the nine `annotate_*` passes gain a `&SteeringConfig` param (not `&Config`), and the existing six categories keep their exact membership so every working steering config keeps meaning the same thing; the `if cfg.<knob> > 0.0` guard is pinned **load-bearing for reproducibility** (removing it would consume 16+ extra RNG draws on the default path), with its honest consequence stated — a default-off knob records `attempts = 0`, so the readout cannot distinguish *off* from *never reached*; **no schema bump**, with the rule stated for future contributors: bump for a new payload key or query kind, not for more entries in an existing map; three knobs excluded **by kind** (`operand_duplication_rate`/`mux_arm_duplication_rate` are dedup thresholds with no roll to prior-multiply; `library_prob` has no reader in `src/`) since a paper category is a ROADMAP steering-gap-1 regression; flags an unexamined hard-coded `gen_bool(0.5)` at `src/gen/module.rs:143` and that `KnobId::all()` reaching 38 hand-maintained entries needs an R2 guard (proposed: an exhaustive private `index()` match + a derived length test); split `.4b.1` (7 motif knobs) / `.4b.2` (9 emit knobs, ~99 call sites) / `.4c` (docs); docs-only ⇒ DUT byte-identical | 2026-07-30 | accepted | steering, coverage, knob-roll, motifs, structured-emission, api-completeness, rules-first, telemetry, north-star |
| [0036](0036-readme-landing-page-restoration.md) | The README is restored to a landing page by **deletion, not relocation**. Measured at `fcb9e58`: **1771 lines / 122,767 bytes** vs the policy's illustrative `300`/`16,384`, with two sections at **92 %** of the file — but everything the policy's content contract asks for totals **141 lines**, so *the landing page already exists and already fits*. `## Current CLI truth` (64 %) is **not a CLI reference**: **zero** command lines, 50 design essays averaging ~23 lines, 33 citing a decision record — and a phrase probe proves it is a **lossy copy** (`__cv` appears 7x in README vs 15x in decision `0027` and 19x in the book; `care_mask` 2x vs 9x in `0029`), so the content needs **deleting with a link**, not the expensive and risky 1141-line relocation. `## Build and validation` splits the same way: a genuine quick start plus gate invocations around 78 `saw_*` fact lines and 15 banked tallies that decision `0030` already built `docs/evidence/` to hold. **A generated CLI reference is REJECTED** — the option this decision was asked to weigh, and the one initially favoured before measuring: there is nothing to generate (zero command lines), the SCHEMA-DERIVED reference **already exists** (decision `0021`'s queryable knob catalog + `--dump-config`), so a generated Markdown surface would be a **fourth** copy of solved truth and would re-import the drift it claims to prevent (template + build step + no-diff gate is justified against a hand-maintained list, not against *a link*). Caps **250 lines / 12,288 bytes**, derived from the measured survivors (141 + trimmed quick start + navigation ⇒ ~186) and deliberately **below** the policy's illustrative numbers, which would leave 60 % headroom to regrow into. Enforced as the **8th registered doctrine** `README-GROWTH` (hook + CI per the policy's own clause, reusing `DOCTRINE_ENFORCEMENT.md` rather than a standalone script) failing with a **routing hint** naming the canonical home per kind. `.2` must run the phrase probe per bullet before deleting — the only place information could be lost. Expected result: **1771 → ~186 lines, an 89 % reduction with no information lost**; docs-only ⇒ DUT byte-identical | 2026-07-30 | accepted | docs, readme, policy, shadow-list, de-duplication, doctrine, enforcement, workflow |
| [0037](0037-enumeration-parity-declared-sites-and-list-scoped-coverage.md) | `ENUMERATION-PARITY`'s **declared-site list stays hand-written and is authoritative**, and its `covers_set` predicate is **measured vacuous at 3 of its 10 sites** and must be scoped to the enumeration rather than the file. **(i)** The candidate derivation (*every tracked `*.md` naming ≥ T of the ids on one line*) fails in **both directions and the two failures share no threshold**: measured over `git ls-files '*.md'` it selects 56/20/16/14/13/6/5 files at T = 2…8, and at **every** T ≤ 6 it pulls in `CHANGES.md:8400`, `DEVELOPMENT_NOTES.md:3999` and `docs/tasks/*` — which hold **the literal six-name list, correct when written** — so the gate would be satisfiable only by retro-editing append-only history (absolutely forbidden, decision `0031`) or by a second hand-maintained exclusion list (the same list, sign-flipped, now growth-coupled to a file that grows every commit); while at T ≥ 7 it **drops 2 of the 4 declared sites** (`book/src/algorithm.md`, `book/src/knobs.md`) purely because their lists wrap across two lines — a reflow accident silently exempting a site, the `PARITY-EXTRACTOR-ARM-SHAPE-GAP` hazard re-imported on the doc side. Under decision `0033`'s own rule the site list therefore **fails test (2)**: it is *supposed* to differ from "every file naming the ids", because **history must stay raw** — the fifth hard case, and the first at the enforcement layer rather than the content layer. The selector is nonetheless **adopted as a review-time discovery instrument** (it correctly surfaced `CODEBASE_ANALYSIS.md:2349` as an undeclared live site), giving `0033` (c)'s *discovered by review, held by derivation* a written procedure: sweep the whole tree from the authoritative set and classify **every** candidate — never add a site because it turned up in a bug report. **(ii)** Sweeping from the authoritative set = **every use of the predicate** (10 sites, not the one `.1` reported) and probing each by *deleting the enumeration and re-running the check*: **both pair-3 sites** (`book/src/api-tools.md`, `book/src/agent-mcp.md`) and **`book/src/knobs.md`** still pass — pair 3 provides **zero** protection at either site — and `USER_GUIDE.md` is one word from vacuous. The predictor is measurable (ids occurring *outside* their list: `yosys` 18, `verilator` 12 vs `MEMORY-ARCH` 0, `README-GROWTH` 0): **a coverage check's strength is inversely proportional to how ordinary its ids are in the checked document, and they are most ordinary in the document that documents them.** A **proximity window is rejected on measurement** — it leaves both pair-3 sites vacuous (window still 2 lines with the allow-list deleted) and misses a single-id omission at `algorithm.md` (6 lines) and `agent-mcp.md` (4) because `.1`'s own explanatory prose sits beside the list, i.e. its miss cases correlate with documentation *quality*. Repair: an **HTML-comment fence** delimiting each enumeration, checked for **exact bidirectional parity** inside the fence, with a missing fence a hard failure; the fence **names no members**, so it fails `0033` test (2) and cannot become the next shadow. Adopts *delete the subject and re-run the check* as the standard acceptance test for any coverage-shaped doctrine check. `.3` implements (fences, list-scoped predicate, pair 4 from 4 → 7 reviewed sites); docs-only ⇒ DUT byte-identical | 2026-07-31 | accepted | doctrine, enumeration, enforcement, shadow-list, derivation, vacuity, gate-quality, history, book, north-star |
| [0038](0038-changes-md-position-repair-by-pointer.md) | A misplaced-but-landed `CHANGES.md` entry is repaired by **appending a dated pointer stub at the position it should have occupied** — never by moving it. `0031`'s letter forbids retro-editing *content*; its **reason** is evidentiary (*"keep it raw, keep honest, so people can follow the whole history"*), and **position is itself a record** — that these two entries sit at the file's bottom is the evidence their author appended them there, so a silent relocation would leave a file in which the mistake never happened, which is decision `0030`'s `reverify` accident repeated: mechanically rewriting the one document whose *subject* is the thing being rewritten. Re-publishing the full bodies at the top is rejected (a second copy of a 65- and a 74-line entry is a `0033` shadow, silent on divergence); an in-place forward pointer inside the originals is rejected (that *is* a retro-edit, and never reaches the top-down reader). **Re-measured from the authoritative set, the finding narrows and sharpens:** against **git** — the real ordering oracle — **388** of 646 entries carry a resolvable hash and their commit indices descend with **zero** violations, so the file has **exactly ONE** ordering defect, not an unknown number. A **date-keyed** scan reports **3**, of which **2 are false** (mis-dated *headings* over correctly-ordered entries: lines 9428/9477 headed `2026-06-18` are successors of a `2026-06-21T13:28` commit the entry itself names as `previous:`; line 26652 headed `2026-05-13` committed `2026-05-14T23:38`, rev numbers descending 274→272→270→**267**→265→264→262) — so the obvious mechanism **cries wolf on 2 of 3**, and a gate that cries wolf gets deleted. A **hash-keyed** scan is **vacuous for this exact defect**: the two misplaced entries carry **no `Landed as:` line at all**, so the check's horizon stops at line 39567, **4,516 lines above** them — decision `0037`'s *delete-the-subject* test firing **without deleting anything**. The entries deviate in **three** ways, not two (placement, retired heading convention, **and** the missing provenance line — measured present in 571 entries, absent in 75 = the 73 oldest contiguous **plus these two**, making them the only post-adoption entries lacking it), so the root cause is a **stale template**, not an ordering slip — which reframes `.4` toward the authoring path rather than a post-hoc gate. Also corrects `.1`'s retired-convention count (**245** is one of two sub-forms; the true partition is 253 current + 393 retired = 646, the retired region being 245 word-slug + 148 numeric-slug) — `0033` rule (2) recurring. Six explicit non-licenses so it cannot be cited for a sweep; `0031` is **applied, not amended**; docs-only ⇒ DUT byte-identical | 2026-07-31 | accepted | docs, changes-log, append-only, history, position, evidence, findability, vacuity, gate-quality, doctrine, north-star |
| [0039](0039-sweep-exemption-past-vs-present-and-recorded-recall.md) | **Sweep conduct**, supplying the *review* half decision `0033` §(c) left unspecified when it closed on *"discovered by review and held by derivation"* — the half where both subsequent misses landed. **(a)** A sweep **exempts a hit when the enclosing record is a statement about the PAST, never for carrying a DATE**: a standing claim with a date on it is still a standing claim, and the date makes it *look* like history precisely to the sweep built to catch it. `BOOK-TEST-COUNT-SHADOWS.1` exempted every dated count and deleted every undated one; the two survivors — `book/src/architecture.md:612` (**294** claimed vs **946**, 31 % of reality) and `CODEBASE_ANALYSIS.md:2425` (**307** vs **946**, 32 %) — both call themselves ***current*** in their own text while carrying a two- and three-month-old date, i.e. self-refuting on their own terms. The test is asked of the **enclosing record**, not the numeral (*"does this describe what was true at a named moment, or what is true now?"*), which is also what makes the by-kind exclusion list **derivable from the rule** instead of carried by habit — and it gains `docs/TASK_TREE.md`. **(b)** A sweep **records its MATCH COUNT, not only its finds**: the authoritative set and its size, the key verbatim, the number of candidates matched, and every candidate's disposition. `BOOK-TEST-COUNT-SHADOWS.1` logged *"found 2 more live shadows and three false positives"* — **precision reported, recall never** — so a reader cannot tell whether it examined 5 candidates or 500; enumerating `CODEBASE_ANALYSIS.md` against that sweep's **own key** returns **14** matches where **3** were registered, of which **9 of 13** per-file claims were stale and every error an under-count (worst `tool_matrix.rs` **26 → 114**). The clincher is not an argument: for `src/metrics.rs` the book said **18**, `CODEBASE_ANALYSIS.md` said **20**, the truth is **31** — one derivable number, two copies, two *different* wrong values — while **4 of 13** were **accidentally correct**, the coincidence `0033` predicts. Corollary earned in-leaf: **print the bucket you could not classify** (a greedy-`.*` status extractor returned `surelog` as a tree status and was caught only because the unclassified bucket was printed). **No gate, deliberately** (`0033` §(c): the past-vs-present test is semantic, and a gate that cries wolf gets deleted; a gate on rule (b) would check a line's presence, never its truth). **Coordination with `OVERFLOW-DESTINATION-INSTRUMENTATION`'s candidate instrument (b) measured rather than argued:** a *self-declared freshness field* has **0** subjects in the 108-file live-doc set and **0** in either survivor (its dates are inline parentheticals, for which no "newest content" baseline exists), so it **could not have caught this class**; its **75** real subjects are all `docs/tasks/*.md`, **60** of them `done`/`closed` where the field is correctly frozen — so rule (a) is a **precondition** for that instrument, not a duplicate of it, and without it a staleness gate would cry wolf on **60 of 74** (`0033` test (2) exactly). Negative-controlled both ways (75 files over the excluded set; 1 on a synthetic insert). Canonical home is this record, **not** a `docs/knowledge/` card — `docs/decisions/` is a Knowledge Map scan dir, so a card would be a second copy of a rule whose subject is that second copies rot (`0033` R1 on its own output); `0033` gains a **dated pointer Amendment**, not a restatement. Five explicit non-licenses, incl. that it grants **no** new permission over history (`0031`) and does **not** amend `0033`; docs-only ⇒ DUT byte-identical | 2026-07-31 | accepted | doctrine, sweep, audit, recall, exemption, shadow-list, staleness, docs, gate-quality, method, north-star |
| [0040](0040-overflow-destination-classification-and-the-unmeasured-axis.md) | Overflow destinations are classified in **three** kinds, not two, and the pressure a cap redirects lands not only on a neighbouring **surface** but on the neighbouring **AXIS of the capped file itself**. `.1` posited *bounded surface* vs *append-only record*; measured, the bounded side **splits**, and `README_POLICY.md:32` had already said so without naming it (*"USER_GUIDE.md's length is its purpose"*): **B1** = bounded by a **stated contract**, so the cap is derivable from what the doc is *for* (`README.md`, `MEMORY.md`) ⇒ caps on **both** axes; **B2** = bounded in **kind** but unbounded in **size**, because length tracks the *product surface* (`USER_GUIDE.md`, `book/src/`, `ROADMAP.md`, `TOOLBOX.md`, `CODEBASE_ANALYSIS.md`) ⇒ **no** size cap, since one there is either raised per feature (decorative) or forces deleting content with nowhere to go (harmful); **A** = append-only record (`CHANGES.md`, `DEVELOPMENT_NOTES.md`, `docs/tasks/`, `docs/decisions/`, `docs/evidence/`) ⇒ **never** capped, `0031` + `0033` **test (2)**, stated **per entry with the doctrine named** so an exclusion can never later read as an oversight. `.1`'s binary would have forced five B2 surfaces into *"bounded ⇒ must be instrumented"*, which is exactly where *"cap every destination"* turns harmful. **The one real gap is `MEMORY.md`**, which carries a **line** cap (50) and **no byte cap** and now sits at **exactly 50 of 50 lines** — 100 % of its cap — at **20,311 bytes = 1.65× the byte cap `README.md` is held to**, longest line **2,874** chars, in a file whose own first line says *"keep <= 50 lines"* and whose standard says *"roughly one screen"*. The history makes the mechanism unambiguous: the cap landed at `2d01e8e` (`2026-06-05`) truncating **2,399 lines / 306,099 B → 19 / 1,227 / 64 B-per-line**, and since then **lines went 19 → 50 and STOPPED while bytes went 1,227 → 20,311 (16.6×) and density 64 → 406 B/line (6.3×)**, against README's measured **65** — the cap binding perfectly on the axis it measures while growth continued undisturbed on the axis it does not, i.e. decision `0036` §(c)'s *prose density is the hidden variable* as a live instance in the file every session reads first. And **65 % of it is in the wrong layer**: `## Operating gotchas` **10,491 B (51.6 %) in 14 lines = 749 B/line** + `## Standing directives` **2,696 B (13.3 %)** are `MEMORY_ARCHITECTURE.md` §3's layer-C definition verbatim, invisible to a line cap because they are written as 23 enormous lines; §6 prescribes the remedy exactly (*"move it down to B or C"*). Cap **derived, not fitted**: contract *one screen* ≤ 50 lines × the demonstrated-achievable **64 B/line** (what this file measured at `5043547`, the first commit after the architecture landed), budgeted generously to **~120 B/line ⇒ 6,144 bytes** — deliberately **half** README's cap, a resume pointer being a strictly smaller contract than a landing page. The file is **3.3×** that today, **which is the finding, not an argument for a bigger number**. **Both instruments `.1` proposed are retired on measurement:** the distinct-date status-view-vs-log test puts `ROADMAP.md` in the log band at **15** dates — all fifteen read, all phase/lane **closure** facts, i.e. correct past-tense statements inside a present-tense document (`0039` rule (a)) — so it **cries wolf on the repo's principal status surface**; and the self-declared-date instrument has **0** subjects in the live-doc set (`DATED-COUNT-SWEEP-EXEMPTION.3`). The **mixed-surface** category therefore keeps its rule (*separate before you cap*) and honestly gets **no detector** (`DOCTRINE_ENFORCEMENT.md` §9). **No new doctrine** — `MEMORY-ARCH` already owns that file's size, so the byte cap is a second assertion in `check_memory_architecture.sh`; **the tree grows to five leaves** with `.5` (demote `MEMORY.md`'s layer-C content behind pointers) ordered **before** `.3`, because switching the gate on at HEAD would block every commit. Two latent hazards recorded: the repo's **two** routing enumerations disagree by exactly `CHANGES.md` + `DEVELOPMENT_NOTES.md` and **must stay** different (the hint routes *new content*, which may never enter an append-only record; README's table routes a *reader*, and history does live there — "harmonising" them would instruct authors to append to history); and `CODEBASE_ANALYSIS.md` is named by **neither** yet is a mandatory bootstrap read with the worst byte profile of any B2 surface (**282,136 B**, one line of **24,990** chars = **2.03×** the entire README byte cap), its long line recorded as a **formatting** defect for a future leaf rather than swept here. Also corrects `.1`'s *"six canonical destinations"* — enumerated mechanically the hint names **seven**, a count beside a list inside the tree auditing where lists go stale. Six explicit non-licenses; docs-only ⇒ DUT byte-identical | 2026-07-31 | accepted | docs, policy, cap, growth, instrument, memory-architecture, classification, shadow-list, gate-quality, doctrine, north-star |
| [0041](0041-owner-standing-directives-recorded-in-layer-c.md) | **The owner's standing directives are RECORDED in layer C, not in the resume pointer.** Measured at `85e4b7f`: **13 citations across 9 decision records** pointed at them, and **4** named the location literally as *"(`MEMORY.md` standing directives, `2026-07-30`)"* — nine records citing, as durable provenance, a file `MEMORY_ARCHITECTURE.md` §3/§6 guarantees is **overwritten** on every update with a hard cap. The most-cited directive of the set is the one that says, in the owner's words, *"If you just record in `MEMORY.md` it will be lost there"* — so **the directive warning against parking things in the resume pointer was itself parked in the resume pointer.** Probing the seven entries separated **cited** from **recorded** (a coarse key reported *"has a home"* for entries whose only hits pointed **back** at `MEMORY.md`, so every hit was opened — `ODI.1`'s *a reference count is not a dependency measure*, applied to the probe itself): **four** are genuinely recorded in [`0031`](0031-ssd-volume-exclusivity.md) **with the owner's verbatim quotes** (never rewrite history · the SSD is the only project volume · shared means shared · harness runtime files belong to the harness), so `MEMORY.md`'s copies were **`0033` shadows** repaired at rung **R1 by pointer**, never restated; **three** had **no layer-C home at all** and are recorded here **verbatim** — *(a)* **a defect is only handled if a task-tree owns it** (surfacing is step one, opening the tree is step two, in the same turn), *(b)* **decide, don't ask** (escalate only where proceeding under any assumption would be unsafe or make the work useless if wrong; the directive removes the **question**, never the **surfacing**), *(c)* **`~/Documents/github` is owner-owned** — the pre-move checkouts, excluded from every audit, never deleted or migrated by an agent. **Relocation was authorised by the owner** on `2026-07-31` before anything moved, the content being owner-set; the wording is preserved verbatim and §(1) forbids rewording. **No citation needed editing — the reusable half:** all 13 cite by **owner + date**, stable across a move, which is exactly what `README-POLICY-PROVENANCE.1`'s provenance style bought; a citation keyed on a *location* would have required 13 edits across 9 landed records. Loss proved by sweeping **every** token in the removed 2,696-byte range rather than a phrase list (`0036` §(iii)): **29** backticked tokens, residue **0**; **162** words, residue **1** (`doc/code`, a composite of covered parts whose underlying fact is mechanically gated by `NO-BOOT-VOLUME-REFS`). `MEMORY.md` **19,885 → 18,157 B**; the **6,144** cap is **not** reached by this leaf and the **12,013 B** gap is **reported rather than met by raising the cap** (`0040` non-license 3). Five explicit non-licenses; docs-only ⇒ DUT byte-identical | 2026-07-31 | accepted | owner-directive, workflow, task-tree, memory-architecture, provenance, autonomy, audit, north-star **Amended `2026-08-01`** with directive **(d) the push cadence is every 200 commits** (owner-set, verbatim) — recorded here rather than in `COMMIT.md` because it is a *preference*, and it resolves a question the project left open in both directions: `MEMORY_ARCHITECTURE.md` §8 says *"push regularly"* with no number and `COMMIT.md`'s steps never mentioned pushing at all. `COMMIT.md` gains a **pointer** (new step 10), not a copy. Deliberately **not gated**, with the reason stated: a cadence is an owner risk appetite, not a repository invariant. |
| [0042](0042-task-tree-index-is-a-mixed-surface.md) | **The task-tree INDEX is a mixed surface — the first one measured in ANVIL, and it supersedes `0040` §(f)'s *"ANVIL has no mixed surface today"*.** `docs/TASK_TREE.md`'s *"Current frontier"* column — a field whose contract is **one value** — re-states **416 of the 508 leaves (81.9 %)** the owning `docs/tasks/<TREE>.md` files already hold in full, with **22 of 74** cells re-stating **≥5** of their own tree's leaves and the worst carrying **75 of 75** in a single 39,095-character cell. That is the `0036` lossy-copy signature inside the exact shape `0040` named and could not detect. **The classification was earned by READING, not by counting dates:** all 8 flagged cells were read, and of ~27 dates, 6 are registration facts and 4 are quoted evidence — both correct under `0039` rule (a) — while ~15 per-leaf completion entries are the actual journal. **The date count is not the signal; the accretion is.** The instrument this record contributes takes the leaf IDs the owning tree file declares as the authoritative set and counts how many the status cell re-states — it needs no baseline and cannot fire on `ROADMAP.md`, which has no leaf IDs; offered as a **finding, not a doctrine**, since one surface is not a class. Repair is **separation before instrumentation** (`.9`), never a cap. Routing gap closed: the index is now named in both enumerations — `0040` §(g)'s shape now has **two** instances, which makes it a pattern for `.4` to carry. |
| [0043](0043-reference-cache-stays-untracked-public-repo.md) | **The local IEEE 1800 LRM cache is NEVER tracked.** Measured `2026-08-01`: `rdje/anvil` is **PUBLIC**, the material is **6.7 MB / 118 files** carrying *"Copyright © 2024 IEEE. All rights reserved."*, and `.gitignore:19` already excludes `.cache/`. Reference-only intent does not change it — committing to a public remote **publishes** it. **The reason this is written down rather than left to judgement:** `.gitignore` stops the accident but not `git add -f`; repo visibility takes a `gh repo view` to learn; and decision `0031` forbids rewriting history, so the standard remedy for a bad commit is **prohibited by the project's own directive** — one of the few genuinely irreversible actions available here. **No new doctrine** (`.gitignore` already mechanises it; `feedback_full_factorization`), registry stays at **9**. Clause citations, a `sha256` manifest, and derived facts about ANVIL's own emission ARE trackable. Owner confirmed the reasoning and withdrew the request. |
| [0044](0044-capability-breadth-unique-priority-case-qualifiers.md) | **The next capability-breadth construct is a default-off `unique` / `priority` case-qualifier projection of `CaseMux` / `CasezMux` — registered as a THIRD strand `.3`, not as a reframing of the deferred `.1` up-opt strand.** A qualifier is an **assertion**, so the load-bearing question is not *do tools accept it* but *is it true for every block the generator can emit*. Both properties come free from the emitter: the always-present `default:` arm gives **FULL**, and the sequential arm indices give **PARALLEL** — no analysis pass, no generate-then-filter. Measured `2026-08-01`: **50,761** emitted `case`/`casez` blocks 100 % FULL + PARALLEL (0 nested, 0 unparsed), plus 130 nested FSM blocks; downstream ON-vs-OFF clean at the repo bar with `-Wall` **Δ=0** and **identical** synthesized cell counts 24/24; `verilator --assert` reports **zero** runtime violations on real `CaseMux` and `CasezMux` modules while the negative control **does** fire. **Two corrections:** the pre-`.1` *`CasezMux` arms can overlap* claim is **withdrawn** (it read the property off the IR type instead of the constructor), and `priority` — not `unique` — is the inert qualifier here, so choosing it first to dodge Icarus's `sorry: Case unique/unique0 qualities are ignored.` would have shipped no new tool code path. Per-qualifier tool plans are pinned so the gate is **earned**, not passed on `first_tool_warning`'s `warning:` lexical accident. |
| [0045](0045-changes-entry-placement-authoring-path-check.md) | **Entry placement DOES warrant a mechanism, and it keys on the AUTHORING PATH:** *if the staged `CHANGES.md` diff adds a `## ` heading, the file's first heading must be one of the added lines.* No date, no hash — which is the point. Measured over **766** commits touching `CHANGES.md`: 664 ok, 99 correctly skipped (no new entry), **3 fires, all true positives, zero false alarms**; negative-controlled both ways (same entry at the top ⇒ silent, appended at the bottom ⇒ fires, hash-backfill-only ⇒ skips). It found a **third** misplaced entry nobody knew about (`f9cf50a`, heading 6 of 379), taking the class from 2 members to 3. The load-bearing finding: **all three offenders are invisible to a hash-keyed scan** because the stale template that misplaces an entry is the same one that omits its `Landed as:` line — *a detector must not depend on a field the defect it detects also destroys*. Explicitly rules **against** back-filling the 181 `this commit` provenance lines (measured **182 → 181** across 26 new entries — frozen, not bleeding) and **against** ever requiring a hash, since the newest entry structurally cannot carry its own. Decision `0033`'s three-part test is recorded as **not applying** (there is no hand-maintained list here) rather than force-fitted. `.5` implements it. |
| [0046](0046-book-never-links-outside-book-src.md) | **A book chapter *names* a repo-root file in backticks — with any section named in parentheses as prose — and never *links* to one.** mdBook rewrites every `.md` target to `.html`, so `[USER_GUIDE.md](../../USER_GUIDE.md#…)` renders as `../../USER_GUIDE.html`, which does not exist: **alive on GitHub, dead in the rendered book**, and `mdbook build` exits `0` on it. **Decided by census, not taste** — measured `2026-08-01`: **33** repo-root live-doc filename occurrences in `book/src/*.md` = **31** plain-backtick + **2** from the single markdown link (its *text* and its *target* each name the file), i.e. **31 of 32** distinct references already used the chosen form. The rule was codified, not imposed. **The absolute-GitHub-URL candidate is refuted by the book's own configuration** — `book/book.toml` sets `git-repository-url = ""` and `edit-url-template = ""`, so the project had already decided the book is built host-agnostic. A book-visible **copy** is disqualified by [`0033`](0033-shadow-enumeration-classification.md) (it would re-create the very shadow `USER-GUIDE-CLI-TABLE-SHADOW.4` deleted). A fourth candidate, mdBook `{{#include}}`, is **recorded rather than dropped** because it is the only option that is both clickable *and* host-agnostic — rejected on four counts (163 KB injected as one chapter; anchor collisions, worst in `print.html`'s merged namespace; a build dependency outside `src` that `mdbook serve` watch misses; a table-of-contents restructure to repair one line). Accepted cost, stated: **not clickable** — and the parenthesised section name is itself a shadow that rots on a heading rename, but *no worse than the `#anchor` form*, which rots on the same event and lands the reader silently at the top of the page. **The durable prize is a portable predicate**: *no markdown link in `book/src` may escape `book/src`* — greppable, no `mdbook` required, and exact for this class, where the obvious check *"does the target exist?"* is **vacuous** (`../../USER_GUIDE.md` does exist). Registration as a doctrine is left to `BOOK-LINK-INTEGRITY.3`. |
| [0047](0047-negative-control-carrier-is-the-mutation.md) | **The carrier for *"prove the sabotage landed"* is the MUTATION, not the control — and a doctrine check is STRUCTURALLY disqualified.** `.1` measured the record: over **39** leaf-episodes, leg 3 is visibly reported in **2**, both on the day the rule was written; the other **37** are **unknowable**, not absent. **The failure belongs to one primitive** — `sed -i` and `perl -pi -e` exit **`0`** whether or not the pattern matched, and all three recorded leg-3 failures are textual substitutions, while in-language, compiler-checked, constructed and `git show HEAD:`-derived mutations have never failed this way because none has a *match* step. **The habit follows the gate:** the identical file-vs-baseline check appears **27** times proving the REVERT landed and **twice** proving the MUTATION did, because a failed revert leaves a dirty tree that three mechanisms punish while a failed mutation leaves a clean one they all read as success. So: **R1** prefer a mutation form that cannot silently no-op (the need disappears); **R2** where a textual substitution is unavoidable, one tracked helper whose `apply` refuses a zero-count substitution. A doctrine check is rejected on structure, not taste — the mutation is reverted before the commit **by construction**, leaving only the author's prose claim, so such a check would gate a §6.1 **self-tick**, cry wolf on every legitimately mutation-free control, and be satisfiable by typing the words. A `TOOLBOX.md` entry is **E1 discovery, not enforcement**, and "nothing" is refused because the only two episodes that ran leg 3 both ran it with a tool (**0 of 10** harness probes failed against **2 of 3** ad-hoc ones). Limits stated: use is not compelled, R1 is a preference rather than a check, and the 37 stay unknowable — a carrier that merely made the record look complete is candidate C. |
| [0048](0048-book-links-to-the-file-it-stopped-duplicating.md) | **When the book STOPS DUPLICATING a repo file it links to it with an absolute GitHub URL — a NARROW supersession of [`0046`](0046-book-never-links-outside-book-src.md) candidate B, not a reversal.** `0046` decided how a chapter makes an *incidental* reference, where the chapter stands on its own and the pointer is a courtesy; it measured form **A** (name in backticks, no link) unanimous at **31 of 32** sites. This decision covers a case `0046` never faced: a reference that is the **sole route** to content deliberately deleted from the page — and a reference that cannot be followed is, in that position, a deletion. **Triggered by `BOOK-PARAGRAPH-BLOBS.1`**, which measured four run-on enumerations (to **7,159** chars) that are a **lossy copy** of `ROADMAP.md`: of **111** distinct `saw_*` flags the book names, **0 are book-only**, and every sampled bank id appears **2–3×** in `ROADMAP.md` against **1×** in the book, with richer prose there — a [`0033`](0033-shadow-enumeration-classification.md) shadow whose repair is R1, delete the copy. **The owner's preferred "works on both" option was TESTED and does not exist:** `book/src` and `book/book-out` sit at the same depth, so a relative `../../ROADMAP.md` *would* resolve from either — but a probe chapter proved mdBook rewrites the target to `.html` in **both** the markdown form **and the raw-HTML `<a href>` form**, so the obvious escape hatch fails and no relative form survives into the rendered book. Settled on the owner's own fallback (*prioritize GitHub*): the absolute URL is the only form live in **both** surfaces. **Three constraints:** absolute never relative (mdBook rewrites `.md`→`.html`, so a relative escape is **dead in the rendered book** while `mdbook build` exits `0` — `BOOK-LINK-TARGETS` must keep blocking it); **sole-route only**, so the 31/32 convention is not reopened; and pin **`main`**, not a SHA, because the target is a live document. **`0046`'s two objections, answered rather than waved:** `book.toml`'s `git-repository-url = ""` governs mdBook **chrome** (repo icon, edit link), not what prose may name — and reversing that setting is deliberately out of scope; the fork/rename/**offline** breakage is **accepted as a stated cost**, because a local reader has the repository on disk and the link text names the path, whereas form A alone after a deletion leaves a networked reader **no route at all**. **`{{#include}}` stays rejected**, now on the owner's authority as well as `0046` D: it is the only clickable-*and*-offline-*and*-host-agnostic candidate, and it would inject the file as a chapter, bringing the **7,159-character** paragraph straight back into the rendered output. **Limits stated:** these links are **gated by nothing** (`check_book_link_targets.sh:149` skips `https?:`), so they rot silently; and the **6 absolute links already in the book are NOT precedent** — they predate `0046` (`9ad7385`) and are residue, a point `BOOK-PARAGRAPH-BLOBS.md` had recorded backwards at `a5645c1` and which is corrected there. |
| [0049](0049-the-bootstrap-read-is-scoped-to-the-work.md) | **The session-bootstrap read is SCOPED TO THE CHANGE IN HAND, and its sufficiency test is understanding the subject — never coverage of a corpus.** Encodes the owner directive of `2026-08-07` (*"read whatever is necessary … I find it dangerous to fix something you don't understand"*), which settles the tier axis: the replaced *"read every live doc, in full"* text was the wrong **shape**, not merely the wrong **length**, so the **3.03×**-a-context-window arithmetic dissolves rather than being negotiated down. Re-measured at `13c9cdf`: §1 live docs + book **5,507,558 B = 1.31×** a 4 MiB window on their own, `docs/tasks/` **3,252,947**, the §2 source walk **3,933,732**, total **12,694,237 B ≈ 3.03×** — with **68.6 %** of the §1 mass in two files that are append-only by owner direction, so the contract receded with every commit. Three tiers replace it: **fixed** (invariant *of the work* — measured **114,689 B, 2.73 %** of a window), **subject** (derived per leaf, deliberately *not* a list), **queried** (never loaded). **The budget is derived, not fitted**, the same way `0036` §(c) and `0040` §(c) took theirs: the demonstrated-sufficient fixed read doubled and rounded to the next binary round number ⇒ **256 KiB**, which that tier filled to **43.8 %** — a cap fitted to what exists sits at ~100 % of itself on the day it lands and can only be raised. **The circularity is reported, not absorbed:** `SESSION_BOOTSTRAP.md` is itself in Tier 1 and this decision rewrites it, so the derivation uses the **pre-change** state and then states the cost — **120,178 B / 45.8 %** of budget after landing, the file having grown **4,277 → 9,709 B**, and the record's `reverify` deliberately expects that post-landing figure rather than the flattering one. The number that matters is the complement: **≥ 93.75 %** of the window is left for the work, an axis the old contract had none of. **The `PostCompact` hook is what makes the tiering structural rather than stylistic** — `.claude/settings.json` re-injects `SESSION_BOOTSTRAP.md` verbatim and the protocol is *freshly in force*, so a session that compacts *N* times pays the fixed tier *N+1* times: sustainable at 2.73 %, unsurvivable at 1.31×. **Every demotion's loss is measured, not asserted:** the index's 91 rows re-state **416 of 508 leaves (81.9 %)** of the tree files (`0042`) so all but your own add ~0 unique fact; `CHANGES.md` is reachable by key (**632 of 709** carry `Landed as:`, **459** name a leaf id); `ROADMAP.md`'s per-leaf content is already in every tree file's own `- Roadmap lane:` field (**84 of 84**); `CODEBASE_ANALYSIS.md` names **55 of 55** `src/*.rs` files at **7.44 %** of the source mass, which makes it a complete **map** and — per `0039`'s **9 of 13** stale per-file claims — **never a substitute for reading the file you are changing**, so Tier 2 tightens where §2 only gestured. **The book question resolved from the file's own text:** §1 mandated all 30 chapters while the *"what not to do"* section four headings later already named the three that carry non-reconstructible design context (**20,391 B with `SUMMARY.md`, 2.7 %** of the book). **Reconciliation with `MEMORY_ARCHITECTURE.md` §5 is a MERGE by subordination, not a balance** — §5 owns the read path's shape, `SESSION_BOOTSTRAP.md` owns only the binding to this repository's filenames; deleting the file outright (candidate C) is rejected because §5 is project-agnostic and names no file, and would leave the `PostCompact` hook empty. **A second, unreconciled contract was measured and deliberately NOT absorbed:** the six harness bootstrap pointers mandate a *different* 7-item list (**801,434 B**) whose intersection with §1 is **exactly two files**, which routes to `SESSION_BOOTSTRAP.md` **not at all**, and which mandates the **694,469 B** derived `KNOWLEDGE_MAP.md` in full against its own standard's lookup contract — registered as `.3` with the residue stated rather than hidden. Six explicit non-licenses, incl. that it is **not** a licence to read less about the change (the subject tier got *stricter*), sets **no** cap on any file, and does **not** resume the paused `OVERFLOW-DESTINATION-INSTRUMENTATION`. Transferable rule: **a precondition nobody can satisfy is not a high standard, it is an unowned defect** — it makes every session a silent violator and yields no signal at all; docs-only ⇒ DUT byte-identical | 2026-08-07 | accepted | workflow, bootstrap, session-recovery, memory-architecture, owner-directive, budget, docs, north-star |
| [0050](0050-resume-pointer-holds-one-work-unit-not-a-roster.md) | **The resume pointer holds ONE work unit, not a roster.** `MEMORY.md`'s `active_work_unit` named **7** trees while **16** carried `Status: active` — a [`0033`](0033-shadow-enumeration-classification.md) shadow of `docs/TASK_TREE.md`'s own table, with test (3) *silence* **demonstrated rather than argued**: it had already drifted to 7-of-16 and nothing failed, because `MEMORY-ARCH` asserts the field *name* exists and has never had an opinion about its contents. So the file's growth was never prose bloat — it was **one term scaling with the number of task trees** inside a hard cap. Repaired by **R1 deletion** to `MEMORY_ARCHITECTURE.md` §6's own singular template. **Candidate A (generate the block) rejected** despite §6/§11.7 suggesting it: `docs/TASK_TREE.md` *already is* the derived roster, so a generator keeps the copy and automates its upkeep — R4 where R1 exists — and **a generated shadow is still a shadow, drifting on a schedule instead of by neglect**. **A precondition was found before deleting:** the removed text carried `OVERFLOW-DESTINATION-INSTRUMENTATION`'s pause, which had **no other durable home** (its own file said `Status: active`, zero occurrences of *paused*), so deletion would have silently un-paused a tree the owner stopped — `0041`'s *a directive parked in layer A is queued for deletion* recurring. Fixed first as `deferred`. Effect: **6,071 → 5,235 B**, headroom **73 → 909 B (12.5×)**, growth-per-tree **→ 0**. Limits stated: the commit-path coupling is untouched, `next_action` still accretes, and nothing checks the new invariant — deliberately, since re-accretion is visible where the drift was silent. Transferable rule: **before deleting a copy, check every claim it carries for an independent home — a copy that has quietly become the only original is not a copy** | 2026-08-07 | accepted | memory-architecture, shadow-list, resume-pointer, doctrine, task-tree, growth, gotcha, north-star |

## How To Add A Record

1. Copy the shape of an existing record.
2. Use the next sequential number.
3. Add a row to this index.
4. Link the record from related task-tree files when relevant.
5. To change a fact, add a new record or mark the old one superseded; do
   not silently rewrite the old decision.
