# Repo-Local Task Tree Workflow

This document defines the repo-local task-tree workflow used by ANVIL. It is
intentionally portable: the workflow was lifted from FSMGen's
`docs/TASK_TREE.md` and adapted to ANVIL's existing live-doc set.

For the portable, project-agnostic setup guide, read
[docs/TASK_TREE_README.md](TASK_TREE_README.md).

## Purpose

Use a task tree when a top-level task is too broad to finish safely as one
signoff-level slice, or when a task is expected to discover subtasks and
sub-subtasks over time.

The goal is not to create a second roadmap. `ROADMAP.md` states the high-level
phase direction. A task tree owns the recursive breakdown, current frontier,
acceptance criteria, blockers, decisions, validation, and completion evidence
for one top-level task.

## ANVIL Adoption Scope

**Doctrine (2026-05-17, non-negotiable, owner directive):** it is
**strictly forbidden to make any code change without it being
task-tree tracked or task-tree owned first.** Task-tree ownership
demonstrably improved code review and code quality over the earlier
ad-hoc/linear cadence, so it is now the mandatory mode of work for all
code — no compromise, no exceptions.

- **Code change ⇒ a task-tree leaf must own it, *before* the edit.**
  "Code" means anything that changes program/generator behaviour or
  generated RTL: `src/`, `tests/`, `examples/`, build/codegen logic,
  `Cargo` manifests that alter behaviour. If no tree/leaf covers the
  change, create or extend one (`docs/tasks/<TREE>.md` + a
  `docs/TASK_TREE.md` row) and name the owning leaf first. The leaf ID
  goes in the commit subject / first body line (`COMMIT.md` task-tree
  rules).
- **Exempt (no tree required):** pure-docs / live-doc / mdBook edits,
  workflow-config tweaks, and recording doctrine itself. These are not
  code changes. They still follow the standard `COMMIT.md` checklist.
- **`rN` is *not* retired** — it survives only as the optional
  within-leaf slice cadence *inside* a task tree (as the closed
  `HIERARCHY-AWARE-IDENTITY` leaves landed as r85/r86/r87). A bare
  `rN` slice that no task-tree leaf owns is no longer a legal way to
  land a code change.
- **Do not migrate finished work** retroactively. Closed `rN` slices
  stay where they are; the mandate is forward-going.

**Project-wide tracking directive (2026-05-16):** by explicit owner
directive, *every remaining roadmap phase* now has a registered
top-level task tree (`PHASE-4-HIERARCHY`, `PHASE-5-PARAMETERIZATION`,
`PHASE-5B-AGGREGATES`, `PHASE-6-ADVANCED-MOTIFS`,
`PHASE-7-ORACLE-MICRODESIGN`, `PHASE-8-FRONTEND-ACCEPT`,
`PHASE-9-MULTI-ARTIFACT-UMBRELLA`) so the whole roadmap is trackable
through task trees. This **does not retire `rN`**: `rN` remains the
within-leaf slice cadence. Each phase tree owns the sub-objective
decomposition, frontier, blockers, and completion evidence; individual
linear coverage slices inside a leaf still land under the `rN` naming +
`CHANGES.md` + `MEMORY.md` combination, exactly as the closed
`HIERARCHY-AWARE-IDENTITY` tree's leaves landed as r85/r86/r87. Closed
`rN` slices are still not migrated retroactively.

> **Evidence-bank citations in `docs/tasks/*.md`.** The verification logs below and in every
> tree file cite banks by a bare `anvil-<name>`. Those name the **run**, not a directory you
> can open: banks predating decision [`0030`](decisions/0030-durable-closure-evidence-citations.md)
> lived under the OS temp dir and are gone, and are listed as historical breadcrumbs in
> [`docs/evidence/INVENTORY.md`](evidence/INVENTORY.md) §1. Task files are **layer-B history**
> (`MEMORY_ARCHITECTURE.md` §3) and are not retro-edited, so this one note covers all of them;
> re-verify a claim by re-running its named gate command at the commit it was made.
> Banks recorded since `0030` carry a committed digest in [`docs/evidence/`](evidence/README.md).

## Active Task Trees

| Tree | Status | Roadmap lane | Current frontier | File |
| --- | --- | --- | --- | --- |
| `COMBINATIONAL-SEMANTIC-IDENTITY` | `done` | NodeId as identity / full-factorization mode | complete — `.1` gate-to-existing-endpoint / constant fold; `.2` shallow 12-bit semantic proof budget with fixed work envelope; `.3` closeout | [docs/tasks/COMBINATIONAL-SEMANTIC-IDENTITY.md](tasks/COMBINATIONAL-SEMANTIC-IDENTITY.md) |
| `SEQUENTIAL-COINDUCTIVE-IDENTITY` | `done` | NodeId as identity / full-factorization mode | complete — `.1` proof inventory; `.2.1` domain-aware flop identity; `.2.2` exact reset-defined self-hold merge; `.3` closeout | [docs/tasks/SEQUENTIAL-COINDUCTIVE-IDENTITY.md](tasks/SEQUENTIAL-COINDUCTIVE-IDENTITY.md) |
| `MEMORY-STATE-IDENTITY` | `done` | NodeId as identity / full-factorization mode | complete — `.1` reset-defined proof boundary; `.2` blocker record; `.3` closeout. Current reset-less memories remain state-by-instance; reset-defined memory sharing is blocked for the current warning-clean memory-inference lane. | [docs/tasks/MEMORY-STATE-IDENTITY.md](tasks/MEMORY-STATE-IDENTITY.md) |
| `HIERARCHY-SEMANTIC-IDENTITY` | `done` | NodeId as identity / hierarchical module identity | complete — `.1` pure-combinational leaves; `.2` bounded pure-combinational wrappers with recursively proven children; `.3` closeout and blockers | [docs/tasks/HIERARCHY-SEMANTIC-IDENTITY.md](tasks/HIERARCHY-SEMANTIC-IDENTITY.md) |
| `SIGNOFF-SURFACE-EXPANSION` | `done` | Quality / signoff-level downstream confidence | complete — `.1` N-flop CDC synchronizer; `.2` Verilator JSON frontend parity; `.3` Icarus compile axis; `.4` closeout and explicit deferred boundaries | [docs/tasks/SIGNOFF-SURFACE-EXPANSION.md](tasks/SIGNOFF-SURFACE-EXPANSION.md) |
| `ROADMAP-FOLLOWUP-OWNERSHIP` | `done` | Workflow / roadmap task-tree ownership | complete — `.1` registered the five post-phase follow-up trees before implementation resumed | [docs/tasks/ROADMAP-FOLLOWUP-OWNERSHIP.md](tasks/ROADMAP-FOLLOWUP-OWNERSHIP.md) |
| `DOCTRINE-ENFORCEMENT-ADOPTION` | `done` | Workflow / mechanical doctrine enforcement | complete — `.1` standard+decision `0026`; `.2` registry+driver `scripts/check_doctrines.sh` + hook/CI; `.3` `TOOLBOX.md` + `CODE-CHANGE-EVIDENCE`; `.4` `TASK-TREE-OWNERSHIP`; `.5` discovery (6 harness pointers); `.6` closeout. Portable architecture #4 adopted end-to-end | [docs/tasks/DOCTRINE-ENFORCEMENT-ADOPTION.md](tasks/DOCTRINE-ENFORCEMENT-ADOPTION.md) |
| `HIERARCHY-AWARE-IDENTITY` | `done` | Phase 4 — Hierarchy | (complete — all leaves done) | [docs/tasks/HIERARCHY-AWARE-IDENTITY.md](tasks/HIERARCHY-AWARE-IDENTITY.md) |
| `PHASE-4-HIERARCHY` | `done` | Phase 4 — Hierarchy | (complete — `.1` done, `.2` superseded, `.3` done; Phase 4 closed) | [docs/tasks/PHASE-4-HIERARCHY.md](tasks/PHASE-4-HIERARCHY.md) |
| `PHASE-5-PARAMETERIZATION` | `done` | Phase 5 — Parameterization | (complete — Phase 5 closed `2026-05-17`; `.2.4b` verified `anvil-tool-matrix-phase5-p1` clean → ROADMAP Phase 5 `done`) | [docs/tasks/PHASE-5-PARAMETERIZATION.md](tasks/PHASE-5-PARAMETERIZATION.md) |
| `PHASE-5B-AGGREGATES` | `done` | Phase 5b — Synthesizable aggregates | (complete — Phase 5b closed `2026-05-18`; `.2.4` verified `anvil-tool-matrix-phase5b-p1` clean → ROADMAP Phase 5b `done`) | [docs/tasks/PHASE-5B-AGGREGATES.md](tasks/PHASE-5B-AGGREGATES.md) |
| `PHASE-6-ADVANCED-MOTIFS` | `done` | Phase 6 — Advanced motifs | (complete — Phase 6 closed `2026-05-20`; **memory** verified `anvil-tool-matrix-phase6-p1` clean [`.2.4`, 219/876, `coverage_gaps=[]`, 876/0 Verilator+both-Yosys, `saw_inferrable_memory_design=true`] **and FSM** verified `anvil-tool-matrix-phase6-fsm-p1` clean [`.3.4b`, 222/888, `coverage_gaps=[]`, 888/0 Verilator+both-Yosys, `saw_fsm_design=true` AND `saw_inferrable_memory_design=true`, P4/P5/P5b regressions proven in the same banked report] → ROADMAP Phase 6 `done`; the separately-prioritised `MULTI-CLOCK-CDC` follow-up is now closed too) | [docs/tasks/PHASE-6-ADVANCED-MOTIFS.md](tasks/PHASE-6-ADVANCED-MOTIFS.md) |
| `PHASE-7-ORACLE-MICRODESIGN` | `done` | Phase 7 — Oracle-backed micro-design artifacts | (complete — Phase 7 closed `2026-05-20`; verified-clean banked artifact `anvil-microdesign-parity-phase7-yosys-p1/` — `cargo test -- --ignored parity_against_real_yosys_write_json` against yosys 0.64 exits 0 with "parity gate clean across 5 seeds"; per-seed fact agreement verified incl. seed 7 P4=-1 [bits=8 on both sides post-`.2c.2b.1` non-negative-modulo-idiom fix] and both generate branches exercised [seed 12345 takes `g_else`, others `g_taken`]; explicit yosys-supported-categories scope caveat — richer-AST coverage via a future microdesign-specific extractor is recorded as post-Phase-7 follow-up and does NOT retract closure since ANVIL's by-construction oracle already covers all 7 manifest categories) | [docs/tasks/PHASE-7-ORACLE-MICRODESIGN.md](tasks/PHASE-7-ORACLE-MICRODESIGN.md) |
| `PHASE-8-FRONTEND-ACCEPT` | `done` | Phase 8 — Frontend/elaboration accept corpora | (complete — Phase 8 closed `2026-05-20`; verified-clean banked artifact `anvil-frontend-parity-phase8-yosys-p1/` — `cargo test -- --ignored parity_against_real_yosys_hierarchy_write_json` against yosys 0.64 exits 0 with "parity gate clean across 5 seeds" on **first try**; per-seed fact agreement verified incl. both generate branches exercised AND the load-bearing hierarchy-aware Phase-8 axis (every seed has 2 instances × 4 per-instance per-binding values matched); yosys-supported-categories scope caveat explicit — yosys folds top localparams + package constants; `SIGNOFF-SURFACE-EXPANSION.2` now adds the optional Verilator JSON-AST gate `parity_against_real_verilator_json_frontend_ast`, clean across the same 5 seeds and enforcing all 7 Phase-8 manifest categories when local Verilator supports `--json-only`; cross-tree reuse of Phase 7's `expr_to_sv` carried `.2c.2b.1`'s non-negative-modulo-idiom fix forward at zero incremental cost — full-factorization doctrine vindicated) | [docs/tasks/PHASE-8-FRONTEND-ACCEPT.md](tasks/PHASE-8-FRONTEND-ACCEPT.md) |
| `PHASE-9-MULTI-ARTIFACT-UMBRELLA` | `done` | Phase 9 — Multi-artifact ANVIL umbrella | (complete — Phase 9 closed `2026-05-20`; `src/umbrella/` carries the `ArtifactLane` trait + all 3 lane impls + 8 cargo-portable proofs incl. per-lane byte-identical regression + cross-lane heterogeneous `dyn` dispatch; `src/main.rs` carries the `--artifact <lane>` CLI flag with default `dut`; load-bearing byte-identical default-`dut` contract verified by `tests/book_examples::every_runnable_book_bash_block_succeeds` passing 3/3 in 80s AFTER the CLI change. **All 9 numbered roadmap phases now delivered.** Post-phase follow-up trees `DIFFERENTIAL-SIMULATION` and `MULTI-CLOCK-CDC` are also closed as of `2026-05-24`.) | [docs/tasks/PHASE-9-MULTI-ARTIFACT-UMBRELLA.md](tasks/PHASE-9-MULTI-ARTIFACT-UMBRELLA.md) |
| `INSTA-SNAPSHOTS` | `done` | Quality — reproducibility regressions | (complete — closed `2026-05-18`; `.1` insta `=1.47.2` pin + baseline / `.2` 6 byte-stable shapes spanning every reachable axis incl. dedup-canonical-signatures / `.3` COMMIT.md non-negotiable snapshot-acceptance protocol + book "Snapshot guard-rails") | [docs/tasks/INSTA-SNAPSHOTS.md](tasks/INSTA-SNAPSHOTS.md) |
| `DIFFERENTIAL-SIMULATION` | `done` | Quality — signoff-level downstream consistency | (**complete — entire tree closed `2026-05-24`**; all four `.1`/`.2`/`.3`/`.4` leaves done) **`.4` landed `2026-05-24`** (docs-only): README + USER_GUIDE + `book/src/synthesizability.md` describe the `--diff-sim` opt-in cross-simulator semantic-agreement contract — per-axis K=5 subset, gated AFTER Verilator+Yosys clean, `DiffSimReport` with retained `mismatch_excerpt`, `saw_design_with_cross_simulator_agreement` coverage fact, friendly no-op when simulators absent. `mdbook build` clean; `cargo test --test book_examples` 3/3 still green (new bash block carries `<!-- book-test: skip -->` sentinel preserving byte-identical book-runnable contract). **`.3b.2` closed `.3b` + `.3` container `2026-05-24`**: `src/bin/tool_matrix.rs` (~600 lines added) gains the `--diff-sim` opt-in CLI flag + `DiffSimReport` per-module struct + `saw_design_with_cross_simulator_agreement` coverage fact + per-axis subset selector + per-module pipeline + `parse_dut_ports`/`emit_testbench_for_ports` matrix-side helpers + 8 cargo-portable proofs + 1 tool-gated `#[ignore]` end-to-end gate; real-tool gate clean: `DiffSimReport { ran: true, success: true, n_samples: 8 }` (24.15s wall against iverilog 13.0 + verilator 5.046); FOUND-AND-FIXED spec-vs-reality bug during e2e gate (ANVIL emits `"input  logic"` with TWO spaces — `src/emit/sv.rs:124`; replaced `strip_prefix` with `split_whitespace`). **`.3b.1` done `2026-05-24`** (pure refactor → `src/diff_sim/mod.rs`); **`.3a` design landed `2026-05-24`** (docs-only); **`.2b.2` closed `.2b` + `.2` container `2026-05-24`** — first gate to assert downstream-tool *semantic* agreement on ANVIL output (`project_anvil_north_star.md`). | [docs/tasks/DIFFERENTIAL-SIMULATION.md](tasks/DIFFERENTIAL-SIMULATION.md) |
| `COVERAGE-INSTRUMENTATION` | `done` | Quality — test-discipline visibility | (complete — closed `2026-05-18`; `.1` llvm-cov baseline / `.2` top-5 triage [no dead code] / `.3` cone retry-exhaustion focused proof + config orphan-knob audit [3 documented-reserved knobs] + baseline refresh) | [docs/tasks/COVERAGE-INSTRUMENTATION.md](tasks/COVERAGE-INSTRUMENTATION.md) |
| `BOOK-EXAMPLES-RUNNABLE` | `done` | Quality — user-facing book correctness | (complete — closed `2026-05-18`; `.1`/`.2.1`/`.2.2` done: 45+1 examples migrated to `cargo run --release --`, `tests/book_examples.rs` harness + `mdbook test` CI gate, pipe-deadlock root-caused & fixed, `cargo test --test book_examples` 3/3 green, 54 runnable exit-0) **REOPENED `2026-07-30`** with a new `.3` node (`pending`; the `.1`/`.2` scope stays closed): the `.2.2` **no-silent-skips guard is itself defeatable**. Measured against the real parse with a compiled probe — the reason extractor trims reason punctuation (`' '`/`'—'`/`'-'`/`':'`) off the **front** before stripping the closing `-->` off the back, so a reasonless `<!-- book-test: skip -->` yields the non-empty reason `">"` and passes both the inline `assert!` and `skip_sentinels_have_reasons`. A guard that exists to reject an unexplained skip cannot see one — the decision-`0033` **S3** tier (a gate that cannot fail), reached through trim ORDER rather than a missing enumeration entry. Found by accident during `COVERAGE-STEERED-GENERATION.3c`. **`.3` done `2026-07-30`, tree closed again:** the inline parse is extracted into a named `skip_sentinel_reason` with the order corrected (strip `-->` **first**), a missing delimiter now fails **safe** (`None` ⇒ the block runs, so a typo makes an example execute rather than vanish), a 14-shape table test pins the classification, and both negative controls hold — the pre-`.3` order fails the table test with exactly `left: Some(">") right: Some("")`, and a reasonless sentinel added to a live chapter now fails the suite where it previously passed silently. The 34 live sentinels audit clean. Test-code only ⇒ DUT byte-identical. | [docs/tasks/BOOK-EXAMPLES-RUNNABLE.md](tasks/BOOK-EXAMPLES-RUNNABLE.md) |
| `MULTI-CLOCK-CDC` | `done` | Capability / Quality — relax single-clock-domain invariant + emit by-construction CDC primitives | (**complete — entire tree closed `2026-05-24`**; all four `.1`/`.2`/`.3`/`.4` leaves done) **`.4` landed `2026-05-24`** (docs-only): `book/src/sequential.md` "Multi-clock and CDC" subsection describes the K=1 default + K=N multi-clock case + by-construction 2-flop synchronizer wrap + opt-in + 4-step pass flow + matrix scenario + coverage facts + first-cut MVP scope; README.md closure status updated. mdbook build clean; book_examples 3/3 byte-identical (no new bash blocks). **`.3b.2` closed `.3b` + `.3` container `2026-05-24`**: `Metrics.num_clock_domains` + `num_cdc_2_flop_synchronizers` in `src/metrics.rs` + `count_2flop_synchronizer_chains` helper (structural scan of `Module.flop_domains` for the synchronizer template `second.D == first.Q`); `CoverageSummary.saw_multi_clock_design` + `saw_cdc_2_flop_synchronizer` + merge + summarize updates; new `int_multi_clock_2flop_sync` default scenario; `Generator::generate_module` rewired to apply the promotion pass (single-module path matching tool_matrix's per-scenario flow); `promote_to_multi_clock` made idempotent (preserves byte-identical when both single-module + design-level paths fire). 4 new bin tool_matrix proofs. **End-to-end matrix gate clean**: Verilator 16/16 + Yosys 16/16; aggregate coverage facts both lit; multi-clock module recorded `num_clock_domains=2 num_cdc_2_flop_synchronizers=1`. **The first ANVIL multi-clock SV passed both downstream tools first try** — validates the entire `.2` + `.3a` + `.3b.1` + `.3b.2` chain end-to-end. K=1 default 0.0 byte-identical (snapshots + book_examples 3/3 in 84.10s preserved). **`.3b.1` done `2026-05-24`**: `Config.multi_clock_prob: f64` knob (default 0.0 backward-compatible); `multi_clock::promote_to_multi_clock` post-construction pass wired into `Generator::generate_design` per the `aggregate_prob` pattern (per-module Bernoulli roll on the seeded RNG; if eligible, adds clk_b/rst_n_b ports + 2 `ClockDomain` entries + wraps first 1-bit flop-driven output via `.3a` primitive + rewires output's drive to synced Q; declines cleanly on no-clock/no-output/wide-output modules). `PromotionOutcome { promoted, num_domains, num_synchronizers }` for `.3b.2` coverage. 8 new cargo-portable proofs incl. 2 end-to-end Generator integration tests. lib 256 → 264. K=1 default 0.0 byte-identical (snapshots + book_examples 3/3 in 84.28s preserved). `.3b` split per Phase-7 `.2c.2a`/`.2c.2b` discipline. **`.3a` done `2026-05-24`**: new `src/gen/multi_clock.rs` (~250 lines) carries `pub fn construct_2flop_synchronizer(module, src_q, dst_domain) -> Option<SynchronizerChain>` + `SynchronizerChain { first_flop, second_flop, synced_q }`. Both new flops land in `dst_domain` via `Module.flop_domains`; chain D=src_q → first → second → synced_q; width inherited via `Node::width()`. 5 cargo-portable proofs incl. end-to-end emit-shape integration. lib 251 → 256. K=1 byte-identical (snapshots + book_examples 3/3 in 72.71s preserved). `.3` split per Phase-7 `.2c.2a`/`.2c.2b` discipline. **`.2` done `2026-05-24`**: IR extension landed in `src/ir/types.rs` (new `ClockDomain { clk, rst_n, name }` struct + `Module.clock_domains: Vec<ClockDomain>` + `Module.flop_domains: BTreeMap<FlopId, u32>`, defaults empty for K=1 backward compat); emitter refactored in `src/emit/sv.rs` to per-domain `always_ff` loop via `Module::effective_clock_domains` + `Module::flop_domain` accessors. **K=1 byte-identical** verified by snapshots 6/6 + book_examples 3/3 in 85s (default-`dut` contract preserved across the IR + emit refactor). **K=2 proven** by hand-built `emits_one_always_ff_block_per_clock_domain_when_k_equals_two` lib unit proof. 4 new lib unit proofs (lib 247 → 251). All other suites unchanged. Minimum-blast-radius design (`Module.flop_domains` external BTreeMap, not `Flop.domain` field) kept 23 Flop construction sites at zero touches. **`.1` design landed `2026-05-24`** (docs-only): `DEVELOPMENT_NOTES.md` "Multi-clock + CDC primitives design" records 7-tier CDC primitive catalogue (Tier 1 = 2-flop synchronizer first cut; tiers 2-7 deferred to follow-up leaves or their own task trees); minimum-viable IR shape (`Module.clock_domains: Vec<ClockDomain>` + per-flop `Flop.domain: usize`; K=1 backward-compatible — existing tests + book-runnable contract stay byte-identical with `--multi-clock-prob` default 0.0); by-construction synchronizer rule (rules-first per `feedback_rules_first_generation.md` — never generate-then-filter); Verilator `--cdc=metastable` downstream gate (Yosys `-cdc` rejected — doesn't exist in stable 0.64; custom oracle deferred to `.4`); cross-simulator agreement via the just-landed `--diff-sim`; 6 rejected alternatives (single-flop synchronizer; clock-gating; latches; async-FIFO as min-viable; generate-then-filter; dynamic frequency); `.2`-`.4` leaf shape; `--multi-clock-prob: f64` knob. Opened as the only remaining named follow-up after `DIFFERENTIAL-SIMULATION` closed `2026-05-24`; the single-clock-domain invariant was the explicit Phase-6 deferral. | [docs/tasks/MULTI-CLOCK-CDC.md](tasks/MULTI-CLOCK-CDC.md) |
| `LIVE-DOC-PATH-HYGIENE` | `done` | Workflow / live-doc hygiene | (complete — `.1` rewrote local absolute repo paths to repo-root-relative references, aligned stale closed-tree status metadata, and passed full `COMMIT.md` validation) | [docs/tasks/LIVE-DOC-PATH-HYGIENE.md](tasks/LIVE-DOC-PATH-HYGIENE.md) |
| `README-POLICY-ADOPTION` | `closed` | Workflow / live-doc hygiene — owner-directed README policy | **Registered `2026-07-30`** (owner directive of that date: *"make sure README.md doesn't grow, grow and grow"*) — **not previously owned by any tree**. Measured `2026-07-30`: `README.md` is **1771 lines / 122,767 bytes** against the policy's illustrative `300` / `16,384` — ~**6×** the line cap, ~**7.5×** the byte cap — and no repo-root `README_POLICY.md` exists (the policy's own storage clause requires the project-owned copy). `## Current CLI truth` alone spans lines 620–1762 (~64 % of the file): an exhaustive CLI reference, banked evidence tallies, and per-knob design rationale — three categories the policy routes to `USER_GUIDE.md`, the release notes, and `docs/decisions/`. **`.1` done `2026-07-30`** (decision [`0036`](decisions/0036-readme-landing-page-restoration.md), on an explicit owner instruction to take a signoff decision): **the measurement reframed the task.** Everything the policy's content contract asks for totals **141 lines** — the compliant landing page already exists, with two appendices that are **92 %** of the file bolted on. `## Current CLI truth` (64 %) is **not a CLI reference**: **zero** command lines, 50 design essays averaging ~23 lines, 33 citing a decision record — and a phrase probe proves it a **lossy copy** (`__cv` 7× in README vs 15× in decision `0027` and 19× in the book; `care_mask` 2× vs 9× in `0029`). So the operation is **delete-and-link**, not the expensive 1141-line relocation. **A generated CLI reference is REJECTED** — nothing to generate, and the SCHEMA-DERIVED one already exists (decision `0021`'s knob catalog + `--dump-config`), so generating would add a *fourth* copy of solved truth. Caps **250 / 12,288**, derived from the survivors and deliberately below the policy's illustrative `300`/`16,384` (which leaves 60 % room to regrow); enforced as the **8th registered doctrine** `README-GROWTH` with a routing hint. Expected result: **1771 → ~186 lines, an 89 % reduction with no information lost.** **`.2` done `2026-07-30`**: executed — **`README.md` 1771 → 156 lines / 122,767 → 10,163 bytes (91 %)**, at 62 % / 83 % of the caps, and `README_POLICY.md` landed at the repo root with an ANVIL adoption note. The probe ran **three ways** before anything was cut — 57 identifying flag tokens, 79 rationale phrases, and an **exhaustive sweep of all 879 backticked tokens** in the deleted range — with the only 32 uncovered strings being composites of individually-covered parts, so **no atomic fact was unique to the README** (plus 78/78 `saw_*` Phase-4 facts covered). The audit's predicted residue turned out to be **one gap, not rationale**: `USER_GUIDE.md` documented every gate in prose but had runnable command lines for only **8 of 15**, and now carries all 15, derived from `tool_matrix.rs`'s own gate registry. `USER_GUIDE.md` owns gate invocations (the question `0036` left to `.2`); `TOOLBOX.md` keeps its one catalog row. Quick start re-run end-to-end (four commands + `verilator --lint-only` clean); 21/21 links resolve. **One doctrine site dropped deliberately**: `README.md` left `ENUMERATION-PARITY` pair 4 (`--steer` categories) rather than a category list being re-added to keep the gate green — decision `0033`'s R1 repair-by-deletion, since such a list grows by one line per future category; pair **1b** untouched (the README still names all seven registry ids, required by `DOCTRINE_ENFORCEMENT.md` E1). Cap-calibration finding for `.3`: the survivors measured **10,297 B for 141 lines**, so the projected ~186-line file would have been ~**13.4 KB — over the byte cap** while at 74 % of the line cap; the trim was deepened to fit, the cap **not** raised. **`.3` done `2026-07-30` — TREE CLOSED**: `scripts/check_readme_growth.sh` lands as the **8th registered doctrine** `README-GROWTH` (caps `250` / `12,288` + the policy's storage clause that `README_POLICY.md` must exist beside the file it governs), gated by the git hook (E3) **and** CI (E4) through the existing driver — `.github/workflows/ci.yml` needed no edit, which is precisely the argument decision `0036` §(d) made for the doctrine mechanism over a standalone script. **Deliberately NOT scope-aware**, the only registered check that isn't: landing-page size is a property of the **tree**, not of a change, so a commit that never touches `README.md` is still checked and an over-cap file cannot arrive via a revert or merge and sit behind a green gate. **Negative-controlled five ways** in an isolated fixture root — 251 lines / 502 B ⇒ FAIL (line cap), 100 lines / 20,100 B ⇒ FAIL (byte cap), `README_POLICY.md` absent ⇒ FAIL, exactly `250`/`12,288` ⇒ PASS, `12,289` ⇒ FAIL. The first two are the load-bearing pair: each is far *under* the other cap, turning "two caps catch different bloat" into a measured fact. A sixth control proved `ENUMERATION-PARITY` catches the new entry missing from a single live-registry site. Driver **8/8 PASS**; real README at 157 lines / 10,226 bytes (63 % / 83 %). The owner directive is now implemented **and** mechanically enforced; raising a cap requires a new decision record, not an edit to the check. Superseded plan: `.1` was registered as (audit + design: classify every section keep/relocate/dedupe, derive the caps from what SURVIVES the trim — the policy forbids choosing a cap to fit existing content — and decide whether the growth guard becomes an 8th registered doctrine), then `.2` (relocation, no information loss) and `.3` (the mechanical cap + negative control + close). Docs-only throughout ⇒ DUT byte-identical. | [docs/tasks/README-POLICY-ADOPTION.md](tasks/README-POLICY-ADOPTION.md) |
| `README-POLICY-PROVENANCE` | `done` | Live-doc hygiene / doctrine provenance | **Registered and CLOSED `2026-07-31`**, from an owner ruling that confirmed a status quo: *"let ANVIL keep and follow its own copy of the README policy."* So `README_POLICY.md` is **authoritative** and the file it was copied from is the **template of origin, not an upstream** — nothing syncs from it. Recorded explicitly because **the absence of a sync is a deliberate choice that looks exactly like an oversight**. Re-verified rather than assumed: **0 of 227** origin-template tokens are absent from ANVIL's copy, so nothing was lost in adoption and there is nothing to re-sync. Confirming the policy then raised *"where is that policy's authority written down?"*, and the measured answer was **a section number in a file that has no sections**: the tracked `CLAUDE.md` is **3 lines with one heading and zero numbered sections**, yet **8 live sites** cited `CLAUDE.md` §14 — including the `README-GROWTH` **registry line itself** and both check scripts. A sweep from the authoritative set (every harness-file section citation, not the instance that surfaced — decision `0033` rule (2)) widened it to **§13 and §3** as well, 23 citations across 13 files. **The citation is also wrong in kind, independently of the broken referent**, and that is the stronger objection (owner, same day: the policy *"shall be harness neutral too"*): ANVIL ships **five** harness bootstrap files with identical bodies, and `DOCTRINE_ENFORCEMENT.md` §7 makes enforcement **git-level** precisely so it binds *every* author — anchoring a repo-wide doctrine to one vendor's file implies the rule holds because that harness was told. Live docs now cite **owner + date**; **history keeps its citation raw** (decision `0031`) and that was re-greped and proven, since leaving history alone is an act that must be provable rather than an omission. Decision `0036` gained a **dated amendment**, not an edit. The standard's own group-C clause was corrected from *byte-identical* to **body-identical** (ANVIL's five differ only in their title line, measured) and now states the owner+date citation rule. Docs + two script comments ⇒ DUT byte-identical. | [docs/tasks/README-POLICY-PROVENANCE.md](tasks/README-POLICY-PROVENANCE.md) |
| `OVERFLOW-DESTINATION-INSTRUMENTATION` | `active` | Live-doc hygiene / doctrine-policy completeness | **Registered `2026-07-31`** on an **owner finding measured in PGEN** and sent back as a lesson: *"a cap that redirects overflow must also check where the overflow lands… naming four overflow destinations without requiring any of them to be bounded is a **policy-level hole, not a PGEN accident**."* PGEN capped `README.md` on two axes and routed the overflow into `LIVE_ACHIEVEMENT_STATUS.md`, which had no cap and no staleness check and reached **1,547,057 bytes, 94.7 % of it a dated changelog** — the cap moved the pressure to an uninstrumented neighbour. ANVIL adopted that policy (`README_POLICY.md`, decision [`0036`](decisions/0036-readme-landing-page-restoration.md), doctrine `README-GROWTH`), so it inherits the hole by construction. **`.1` done `2026-07-31` — measured at `43aad06`, and the measurement both confirms and qualifies the finding.** **Hole confirmed, identical in kind:** `scripts/check_readme_growth.sh` names six destinations at `:63-67` — **all inside `note` strings**, i.e. advice, never assertions — and repo-wide **exactly two** files carry a size instrument (`README.md` line+byte at `:89-90`; `MEMORY.md` line at `check_memory_architecture.sh:38`). Every named destination is unmeasured. **Severity is NOT PGEN’s, and that is stated rather than assumed:** run-log/banked-evidence density is `ROADMAP.md` **13.9 %**, `book/src/architecture.md` **13.8 %**, `USER_GUIDE.md` **8.8 %**, `CODEBASE_ANALYSIS.md` **7.8 %** — an order of magnitude cleaner than **94.7 %**; claiming parity would have been as wrong as missing the hole. **But line counts hide the exposure:** the longest single line in `CODEBASE_ANALYSIS.md` is **24,990 chars — 2.03× ANVIL’s entire README byte cap** (12,288), with a second at 13,472 (density **103 B/line** vs 64–65), so any instrument must be **byte-first** — decision `0036` §(c) at its limit. **The lesson needs one correction ANVIL can see and PGEN cannot:** *cap every destination* is **wrong** here — `CHANGES.md` (2.31 MB) and `DEVELOPMENT_NOTES.md` (988 KB) are **append-only by absolute owner directive** (`0031`), and `docs/tasks/` (2.68 MB) + `docs/decisions/` (647 KB) are layer-B/C records; capping any would pressure authors into the history rewrite `0031` forbids — decision [`0033`](decisions/0033-shadow-enumeration-classification.md) **test (2)** exactly. The rule is therefore **classify, then instrument only the bounded ones**. **A third category is named:** a **mixed** surface (bounded status view + unbounded dated changelog in one file) has **no valid cap because of the mixture**, and the mixture is what hides the growth — PGEN’s file is that category, and its repair is **separation before instrumentation**. `.2` classifies every destination and decides the instrument (byte-first, caps **derived** not fitted, stating what it does not license); `.3` applies it, negative-controlled both ways and **proven capable of failing** before being trusted; `.4` pushes the corrected rule back into the **portable** policy, since fixing only ANVIL leaves every other adopter exposed. Docs-only ⇒ DUT byte-identical. Frontier `.2`. **`.2` DONE `2026-07-31`** — decision [`0040`](decisions/0040-overflow-destination-classification-and-the-unmeasured-axis.md), and **measuring first changed both the classification and the target**. **The classes are THREE, not two:** `.1`'s binary would have forced five *bounded-in-kind-but-not-in-size* surfaces (`USER_GUIDE.md`, `book/src/`, `ROADMAP.md`, `TOOLBOX.md`, `CODEBASE_ANALYSIS.md`) into *"bounded ⇒ must be instrumented"*, which is exactly where *"cap every destination"* turns harmful — `README_POLICY.md:32` had already said it without naming it (*"USER_GUIDE.md's length is its purpose"*). **And the pressure went one category beyond what the tree opened on:** not to a neighbouring *surface* but to the **unmeasured AXIS of an already-capped file**. `MEMORY.md` has a line cap and **no byte cap**, and sits at **exactly 50 of 50 lines** at **20,311 B = 1.65× the byte cap `README.md` is held to**; since the line cap landed (`2d01e8e`, `2026-06-05`, truncating 2,399 lines/306,099 B → 19/1,227/**64 B-per-line**) **lines went 19 → 50 and stopped while bytes went ×16.6 and density 64 → 406** against README's 65. **65 %** of it is layer-C content (`## Operating gotchas` 10,491 B in **14** lines = **749 B/line**). **Both instruments `.1` proposed are retired on measurement** — the distinct-date test cries wolf on `ROADMAP.md` (15 dates, every one a correct phase-closure fact, `0039` rule (a)); the self-declared-date one has 0 live-doc subjects. Cap **derived** at **6,144 B** (half README's, a resume pointer being a smaller contract); the file is **3.3×** over, **which is the finding**. **Tree grows to five leaves**, `.5` (demote behind pointers) ordered **before** `.3`, since the gate cannot land at HEAD. **`.5a`/`.5b` done** — `MEMORY.md` **19,885 → 6,037 B** behind pointers to decision `0041` + 12 new Knowledge Map cards; **`.3` done** — the 6,144-byte cap live as a second assertion, negative-controlled on the byte axis alone and vacuity-probed. **`.6` done `2026-07-31` — the long-line question answered "no", and the sweep found something worse.** Reflow is **structurally unavailable**, not merely uneconomic: a table row cannot carry a newline, and table rows are **54 of the 95** in-scope long lines including **both** top offenders (26 of the 33 in-scope list-items are in auto-generated `KNOWLEDGE_MAP.md`); reflowable residue **15 lines**, none near the headline. The premise was wrong about *which* line is worst — `CODEBASE_ANALYSIS.md` is **third**, behind `docs/TASK_TREE.md` **39,679** and `SEMANTIC-INTROSPECTION-EXPANSION.md` 33,890. **And 36 malformed table rows silently drop 57,283 rendered characters** across 394 tables / 2,310 rows, including **5 links to detail files** — with **97.0 % of the headline 24,990-byte line never rendered at all**, because it is malformed, not merely long. Verified with `mdbook` + a negative control on `\|`. **Two leaves registered rather than parked in prose:** `.7` (repair + escape-aware gate) and `.8` (classify **this file** — `0040` never did, and neither routing enumeration names it; **648 B/line**, **79.0 %** of it inside the *Current frontier* column). **`.7` done `2026-07-31` — 36 → 0, and `TABLE-RENDER-FIDELITY` is the ninth doctrine.** The 36 were **three** classes, not one: 10 unescaped content pipes, 1 duplicated File cell, and **25 rows with a stray leading `— ` cell** dropping their entire Summary — the bulk of the loss, and a class `.6` never saw. `docs/tasks/` ruled **in scope**: *not retro-edited* protects a recorded claim, not a rendering defect, and `0031`'s own purpose (*"so that people can follow the whole history"*) is what an unrendered cell defeats. Proved against `git show HEAD:` with **0** unexplained differences, re-rendered through `mdbook`, and controlled five ways including a `0037` vacuity probe and a specificity control keeping it silent on short rows. **The gate caught its own registry row**, which contained the very defect it describes. **`.8` done `2026-07-31` — THIS FILE is the mixed surface**, decision [`0042`](decisions/0042-task-tree-index-is-a-mixed-surface.md), which **supersedes `0040` §(f)'s *"ANVIL has no mixed surface today"***: the *Current frontier* column, a field whose contract is **one value**, re-states **416 of the 508 leaves (81.9 %)** the owning tree files already hold, **22 of 74** cells re-stating ≥5 of their own and the worst **75 of 75** in a single 39,095-char cell. Earned by **reading** all 8 date-flagged cells, not by counting — 6 registration facts and 4 quoted-evidence dates are *correct* under `0039` rule (a). Repair is **separation before instrumentation** ⇒ **`.9`**. Routing gap closed in both enumerations. Frontier **`.9`**. | [docs/tasks/OVERFLOW-DESTINATION-INSTRUMENTATION.md](tasks/OVERFLOW-DESTINATION-INSTRUMENTATION.md) |
| `USER-GUIDE-CLI-TABLE-SHADOW` | `active` | Live-doc hygiene / shadow-enumeration residue | **Registered `2026-08-01`**, found while `CAPABILITY-BREADTH-EXPANSION.4b.3` added two rows to `USER_GUIDE.md`'s CLI flag table and the surrounding table turned out to be missing thirteen. `README.md` calls `USER_GUIDE.md` *"the live CLI reference: **every** flag"*, so that table is a hand-maintained list mirroring clap's registry — and it has **drifted in both directions**. Measured at `1df0071` against `anvil --help`: **108** clap flags, **75** in the table, **20** mentioned elsewhere in the file but not tabled, **13 absent entirely**, and **2 rows naming flags that no longer exist** (`--divergence`, `--no-minimize`). Eight of the 13 are hierarchy knobs — one lane's flags added without a docs pass, not thirteen independent slips — and two of them (`--operand-duplication-rate`, `--mux-arm-duplication-rate`) are the promoted unswept knobs `SIGNOFF-AUTOMATION-EXPANSION.2b` built a whole gate to prove fire by construction: gated, banked, and undocumented. **A genuine decision-`0033` silent shadow, all three tests passing**: derivable (clap), growth-coupled (`README`'s own promise), silent (13 absent, every gate green). **The instrument was corrected mid-audit and the first number was wrong** — a backtick-anchored matcher reported **22** absent because `` `--artifact dut` `` puts flag and value in one code span; the word-boundary matcher gives **13**. That is the fourth such correction in recent history (decision `0045` catalogues three), which is why the count is published with its matcher. Repair **deliberately deferred**: `.2` must first decide whether the table is **exhaustive or a curated core**, because 20 flags are documented in prose instead and writing rows first would answer that question by accident — and a *curated* table is authoritative under `0033` test (2) and must never be gated. **`.2` done `2026-08-01`** — and the contract it recorded is **neither** option: *exhaustive over the **knobs***, a **derived** set (every flag whose value is a `Config` field, i.e. equally settable in `--config knobs.json`, plus the three convenience flags that set several at once), with **mode** flags — run size and destination, lane, tracing, the introspection dumps — owned by their own sections. Written above the table **before any row**, naming **no members and no count** (`0033`: a list beside a set is one more copy of it). The partition was required **total**, not merely non-overlapping: **93 knob + 14 mode = 107 = `\|anvil --help\|`**, empty intersection, zero residue — `total_or_fail` applied to a docs audit. Table repaired **75 → 93**; the criterion earned **6 rows an "add the 13 absent" repair would have missed**, and retires `--version` on principle instead of as a footnote. **Two of `.1`'s five numbers were wrong, from one root cause**: the document tables **two** clap commands (`anvil`, `anvil hunt`) and `.1` measured both against one command's `--help`. `S` is **107**, not 108 — `--diff-sim` is quoted in prose inside the `hunt` **subcommand description**, not an option of `anvil`; and the **2 "stale rows" are 0** — `--divergence` / `--no-minimize` are live `anvil hunt` options in the `anvil hunt` table, which is itself **10/10** complete. Fifth instrument error in recent history and the second in this tree ⇒ filed as the fact card `cli-flag-audit-must-be-command-scoped`, and it is `.3`'s single most important input: a check here must be **command-scoped**. **`.4` registered**: `book/src/knobs.md` §*CLI coverage* is the same shadow of the same set, **missing 11 of 107**, under a sentence promising it is *"accurate as of this commit"*. **`.3` done `2026-08-01`** — the table is now **mechanically gated as `ENUMERATION-PARITY` pair 5**, *not* an eleventh doctrine: `feedback_full_factorization` was applied rather than waved through and points the opposite way from `TABLE-RENDER-FIDELITY`'s, which registered separately only because no doctrine owned markdown well-formedness. The set is derived from **`cli_overrides` in source**, never from `anvil --help` — a doctrine check reads the repository (§4(4)) and `--help` needs a built binary a fresh clone lacks, besides being the prose that produced `.1`'s wrong numbers. **Four negative controls fired**, and control 4 found the `PARITY-EXTRACTOR-ARM-SHAPE-GAP` defect **live** inside `cli_overrides`: `rustfmt` had split `cli` from `.hierarchy_registered_sibling_mixed_support_prob`, so a line-wise extractor reads **91 of 92** and the 92nd is *invisible*; the whitespace strip removes the hazard at its root and the tight floor catches its removal. `book/src/knobs.md` was deliberately **not** declared a second site — gating a copy freezes it, and `0033`'s rung R1 is to delete it. **`.4` done `2026-08-01`** — the book's copy is **deleted** by rung **R1** and §*CLI coverage* now points at the gated table. Two corrections, both from measuring: the registered **11 is 14** (`S` derived twice — the `Cli` struct and `anvil --help`'s `Options:` block — agreeing exactly at **107**, with `book/src/knobs.md` byte-identical `f2d282e..5ce2dd3`, so the *instrument* moved and not the file; the **sixth** such note in recent history); and the copy was **not merely behind but false** — its *"not yet exposed via CLI"* list named 12 knobs of which **9 have CLI flags**, **8 contradicted inside the same section** and all 9 by line 1128 of the same chapter. That settled R1: refreshing returns a copy to *behind*, gating *freezes* it, only deletion removes the mode. **Lossless proven before the cut** — 0 of 107 flags lost a home, and the 3 truthful bullets are strict subsets of richer entries already in the chapter; the 9 capability-knob paragraphs were **kept** (prose, failing `0033` test (2) — only the heading was wrong). **`.5` registered**: the §*`tool_matrix`* block names **22 of 37** — same class, **different** clap registry (`src/bin/tool_matrix.rs`), so a different extractor and its own contract decision. **Current frontier `.5`.** | [docs/tasks/USER-GUIDE-CLI-TABLE-SHADOW.md](tasks/USER-GUIDE-CLI-TABLE-SHADOW.md) |
| `CHANGES-ENTRY-PLACEMENT` | `closed` | Live-doc hygiene / evidence findability | **Registered `2026-07-31`**, found while writing `LIVE-DOC-REGISTRY-SHADOWS.2`'s own `CHANGES.md` entry: the top of the file did not name the two changes that had just landed. Measured at `087ca7b` — `CHANGES.md` states its own rule on line 2 (*"Newest entries at the top"*), `COMMIT.md` §2 mandates it, and the file is newest-first throughout; yet **the two most recent entries sit at the absolute BOTTOM**, at lines 43706 (`BOOK-TEST-COUNT-SHADOWS.2`, `715019b`) and 43780 (`LIVE-DOC-REGISTRY-SHADOWS.1`, `abf7090`), *below* line 43642's `2026-04-15-0001 — Initial scaffold`, the oldest entry in project history — and in the `## YYYY-MM-DD-slug` heading convention retired after `2026-06-14` (249 headings in the current convention, 245 in the retired one). Both commits **did** stage `CHANGES.md` (+74 and +65 lines), so the mandatory-amendment rule was followed and only the **placement** is wrong: the content is complete, which makes this a *findability* defect, not data loss. It matters because `CHANGES.md` is the evidence artifact `COMMIT.md`, `CODE-CHANGE-EVIDENCE` and `DOCTRINE_ENFORCEMENT.md` §6 all build on — a cold session reads it top-down and concludes the last change was `BOOK-TEST-COUNT-SHADOWS.1`, **two leaves stale, with no signal anything is missing**. No mechanism can see it: `scripts/check_diagnosis_evidence.sh:43` is `grep -qx 'CHANGES.md'` over the *staged file list* — presence, never position — and it is scope-aware, so a **docs-only** commit (which both of these were) is exempt outright. **That is decision [`0037`](decisions/0037-enumeration-parity-declared-sites-and-list-scoped-coverage.md)'s finding one layer up**: a check scoped to a *file* when the property it holds lives in a *region* of that file. The repair is deliberately gated behind a decision rather than treated as a tidy-up — `CHANGES.md` is **append-only and never retro-edited** (decision `0031`, owner: *"keep it raw, keep honest"*), so whether relocating a landed entry is a permitted correction of *position* or a prohibited sweep must be recorded **before** anything moves. **`.1` done** — audited and registered, no repair attempted. **`.2` done `2026-07-31`** (decision [`0038`](decisions/0038-changes-md-position-repair-by-pointer.md)), **nothing moved**: the ruling is **repair by pointer, never by relocation** — `0031`'s letter names *content*, but its reason is evidentiary and **position is itself a record**; that these entries sit at the bottom is the evidence their author appended them there, so moving them would leave a file in which the mistake never happened — decision `0030`'s `reverify` accident repeated, since this tree's own *subject* is the misplacement. The repair is therefore **additive**: a dated **pointer stub** at the position the entry should have occupied, carrying only join keys (date, leaf, title, hash) and the reason. Re-publishing the bodies at the top is rejected (a second copy of a 65- and a 74-line entry is a `0033` shadow); an in-place pointer inside the originals is rejected (that *is* a retro-edit). **Re-measuring the property rather than the instance changed the tree three ways.** It **narrowed** the scope: against **git** — the authoritative oracle — **388** of 646 entries carry a resolvable hash and descend with **zero** violations, so the file has **exactly ONE** ordering defect and `.3` is a bounded two-stub insertion at a placement `0038` already fixed (after line 244's `LIVE-DOC-REGISTRY-SHADOWS.2` `e873a6e`, before line 380's `BOOK-TEST-COUNT-SHADOWS.1` `1a6f276`; `LIVE-DOC-REGISTRY-SHADOWS.1` `abf7090` first). It **pre-empted `.4`**: both obvious mechanisms are dead on measurement — a **date**-keyed scan **cries wolf** (3 findings, 2 false: mis-dated *headings* over correctly-ordered entries at lines 9428/9477 and 26652), and a **hash**-keyed scan is **vacuous for this exact defect** (the offending entries carry no `Landed as:` line, so its horizon stops at line 39567, **4,516 lines above** them — decision `0037`'s delete-the-subject test firing *without deleting anything*); the open third candidate is to key on the **authoring path** (`git diff --cached` adds lines above the current first heading), which needs neither date nor hash and *would* have fired on both docs-only commits. And it **renamed the root cause**: the entries deviate in **three** ways, not two — the third being the missing `Landed as:` line (present in 571 entries, absent in 75 = the 73 oldest contiguous **plus these two**) — so the cause is a **stale authoring template**, pointing `.4` at `COMMIT.md` step 2 as much as at any gate. Also corrects `.1`'s retired-convention count (**245** is one of two sub-forms; true partition 253 current + 393 retired = 646, the retired region being 245 word-slug + 148 numeric-slug) — `0033` rule (2) recurring. `0031` **applied, not amended**; six explicit non-licenses recorded. **`.3` done `2026-07-31` — two pointer stubs inserted, NOTHING MOVED, nothing copied.** Ordered `LIVE-DOC-REGISTRY-SHADOWS.1` (`abf7090`) then `BOOK-TEST-COUNT-SHADOWS.2` (`715019b`), matching git. **Insertion-only proven three ways:** `head -n 625` byte-identical (`7197950b…ced29`), the **43,704**-line tail below the insertion point byte-identical (`3621b281…d5883`), and `git diff --numstat` = **`63  0`** with zero deletion lines and zero modification hunks. The oracle **still reports zero violations** at the authoritative width (269 canonical `**Landed as:** \`hash\`` citations, 268 resolved, 1 unresolved — the `cf3dc3c1…` `0038` §1(vi) already recorded, now at line 32598 = 32289 + 246 drift + 63 insertion). **`0038`'s own fixed line numbers had drifted 246 lines within the same day** (244/380 → 490/626, two `OVERFLOW-DESTINATION-INSTRUMENTATION.1` entries having landed above), so placement was **re-derived from the headings** and that evidence settled `0038`'s open question the stable way: **the stubs cite no line number**, only a hash and a `grep` recipe. **Two corrections to the record, both measured:** (a) `0038`'s *"388 entries at a wider extraction width"* is **not reproducible from its own description** — two candidate widenings give 449/19 and 360/14 apparent violations, **every one an `idx == prev` extractor artifact** (the pattern grabbing a *referenced* commit, or the same commit twice running, instead of the entry's own provenance); `.2`'s **finding** is independently confirmed at the strict width, its **instrument** is not reproducible; (b) **the provenance line is present far more often than it is complete — 574 `**Landed as:**` lines, only 269 carrying a hash, and 181 still saying `this commit`** — so the `COMMIT.md` §9 backfill that `0038`'s own same-day Amendment exists to license is **skipped for roughly a third of all entries**. That both sharpens and **enlarges** `.2`'s stale-template root cause: it is not two entries, it is a systematically skipped authoring step. Not repaired in `.3` (out of scope, and widening a repair beyond its proven defect is how the `/tmp` sweep damaged `0030`) — handed to `.4`, which must also rule explicitly on whether those 181 backfills are licensed, since `this commit` is literally a value the entry left explicitly pending. `.4` decides the mechanism, now with three pieces of evidence rather than one. Docs-only ⇒ DUT byte-identical. Frontier `.4`. **`.4` done `2026-08-01` — THE MECHANISM QUESTION IS ANSWERED: ship one**, recorded as decision [`0045`](decisions/0045-changes-entry-placement-authoring-path-check.md). The winning design is the **authoring-path** candidate `0038` named but could not evaluate — *if the staged `CHANGES.md` diff adds a `## ` heading, the file's first heading must be one of the added lines* — needing **neither date nor hash**, which is exactly why it works. Measured over **766** commits touching `CHANGES.md`: 664 ok, 99 correctly skipped (no new entry — hash backfills, the main false-alarm risk), **3 fires, ALL true positives, ZERO false alarms** (the bar the date-keyed candidate failed at 2-false-of-3). Negative-controlled both ways: same entry at the top ⇒ silent, appended at the bottom ⇒ fires, backfill-only ⇒ skips. **IT FOUND A THIRD OFFENDER** — `f9cf50a` (`RESOURCE-SAFE-TOOLING.2`), entry at heading **6 of 379** — so the class has **three** members, not two, and the reason `.1`/`.2`/`.3` all missed it is the load-bearing finding: **all three are invisible to a hash-keyed scan, because the stale template that misplaces an entry is the same one that omits its `Landed as:` line** (measured 0 of 3). The transferable rule: *a detector must not depend on a field the defect it detects also destroys.* Ruled **against** back-filling the 181 `this commit` lines (re-measured **182 → 181** across 26 new entries — frozen, not bleeding) and **against** ever requiring a hash (the newest entry structurally cannot carry its own). Decision `0033`'s three-part test is recorded as **not applying** rather than force-fitted. A **fourth** unreproducible count surfaced — the same corpus yields **181, 202 or 233** depending on the extractor — which is why `0045` ships its extractor as a runnable `reverify`. **Current frontier `.5`** (implement it as a registered doctrine; the doctrine-id list is `ENUMERATION-PARITY`-gated at four fenced sites, so the taxonomy change is priced immediately). **`.5` done `2026-08-01` — TREE CLOSED: `CHANGES-ENTRY-PLACEMENT` is live as the TENTH registered doctrine.** `scripts/check_changes_entry_placement.sh` + a `DOCTRINES` registry row + a `DOCTRINE_ENFORCEMENT.md` §10 row + the `ENUMERATION-PARITY`-gated doctrine-id list at **all four** fenced sites; driver **10/10**. **The evidence was REPRODUCED, not re-derived**, as the acceptance demanded: the historical replay uses the *same two extractions* the shipped script uses and reports 768 commits = 666 ok / 99 skipped / **3 fires** with the **identical** fire set (`f9cf50a`, `715019b`, `abf7090`) — `0045`'s 766 / 664 / 99 / 3 two commits earlier, with the delta accounted for line by line rather than waved at. **All three controls ran on the REAL staged path** in a throwaway git repo, a deliberate departure from the two scope-aware checks and their `DOCTRINE_STAGED_OVERRIDE` seam: the only things that can be wrong here *are* `git diff --cached -U0` and `git show :CHANGES.md`, so **a test seam that bypasses the extraction under test converts an oracle into a tautology**. Three further controls each close a real hole — a commit staging some *other* file is silent; a `0038` **mid-file pointer stub** landed with a top entry **passes** (otherwise this tree's own sanctioned repair mechanism would be unlandable); and an **untracked `CHANGES.md` fails loudly**, because every other path through this check is an exemption, so without a floor it would go green forever the moment its subject stopped existing. Vacuity-probed per `0037`, with the probe diffed against its backup before being believed. **Not scope-aware** — there is no code-path glob in the script at all, both original offenders being docs-only. Two limits written *into* the script: it governs the `## ` convention only (policing heading depth would cry wolf on all 86 in-body `###` subheadings), and the pointer-stub interaction passes only because `COMMIT.md` §2 mandates a top entry anyway. Docs/scripts only ⇒ DUT byte-identical. | [docs/tasks/CHANGES-ENTRY-PLACEMENT.md](tasks/CHANGES-ENTRY-PLACEMENT.md) |
| `DATED-COUNT-SWEEP-EXEMPTION` | `done` | Live-doc hygiene / shadow-enumeration residue | **Registered `2026-07-31`**, found during session bootstrap while reading `book/src/architecture.md` as `SESSION_BOOTSTRAP.md` step 1 requires. `BOOK-TEST-COUNT-SHADOWS` closed having deleted five per-file test counts from that chapter and replaced them with runnable derivations — **but the grand total survived 57 lines below the repair, in the same file**, and a second copy survived in `CODEBASE_ANALYSIS.md`. **Measured at `349eeb6`** with the three derivations the book itself publishes: `architecture.md:612` claims *"226 unit-target tests + 68 integration tests = 294 passing tests"* against an actual **751 + 195 = 946** — the claim is **31 % of reality**; `CODEBASE_ANALYSIS.md:2425` claims **307** — **32 %**; `:2423` claims `tests/pipeline.rs` has 79 (actual **133**); `:2424` claims `book_examples.rs` has 3 tests (actual **4**) and 9 skip-sentineled blocks (actual **39** sentinels). All under-counts — the monotone-decay signature. **THE FIRST ROOT-CAUSE HYPOTHESIS WAS TESTED AND REFUTED, and is recorded as refuted:** it is *not* that the sweep skipped the file it was editing — `BOOK-TEST-COUNT-SHADOWS.1` swept 94 files, and its own log proves it examined `CODEBASE_ANALYSIS.md` (rejecting that file's *"all 7 categories"* as a false positive, with a reason). Both files were in scope; both claims match its key. **The actual cause is the DATE.** `.1`/`.2` applied an exclusion **by kind** — *"dated measurements of a specific run are exempt by the same reasoning `.1` used for append-only history"* — and the discriminator is exact: the five counts **deleted** were undated bare numbers; the two that **survived** are parenthetically dated (*"(current HEAD, `cargo test` on 2026-04-30)"*, *"(`cargo test`, 2026-05-02)"*). **`.1` deleted every undated count in that file and left every dated one.** **Reusable finding: a date attached to a standing claim disguises it as history, and thereby exempts it from the sweep built to catch it** — the exemption must be keyed on *"is the enclosing record about the past or the present?"*, never on *"is it dated?"*. Both survivors answer **the present in their own words** (*"**Current** executed counts"*, *"**current** HEAD"*) over a 2–3-month-old date, i.e. **self-refuting on their own terms** — the same signature [`OVERFLOW-DESTINATION-INSTRUMENTATION.1`](tasks/OVERFLOW-DESTINATION-INSTRUMENTATION.md) independently measured in PGEN, whose candidate instrument (b) generalises here and should be **shared, not built twice**. The sharpest edge: `architecture.md` now carries **the lesson and a live violation of it 57 lines apart** — `:543-555` says these counts *"had decayed — one by 72 %"* and that deriving them *"is the repair that cannot rot"*, while `:612` is decayed by **69 %**. Class swept from the authoritative set (147 tracked `*.md`, `CHANGES`/`DEVELOPMENT_NOTES`/`docs/tasks`/`docs/evidence` excluded by kind): **exactly these two live-doc sites**; the `docs/decisions/*.md` hits are correctly exempt, because a decision record stating what was true when it was decided *is* a record about the past — which is precisely the distinction the corrected rule captures. Rung fixed **before** work starts: `0033` **R1, deletion**, never "update the numbers" (especially tempting here, since the correct numbers are now measured) and never a gate. Registered as a **new tree**, not a reopening — the precedent `BOOK-TEST-COUNT-SHADOWS` itself set over `SHADOW-ENUMERATION-SWEEP`. `.1` done (audit + register, no repair). **`.2` done `2026-07-31` — and `.1`'s scope was wrong by 6×, which measuring before editing is what caught.** `.1` registered three lines in `CODEBASE_ANALYSIS.md`; enumerating that file against `BOOK-TEST-COUNT-SHADOWS.1`'s **own key** returned **fourteen** — the file carries a **second, larger copy of the very per-file list** that tree deleted from the book, left untouched. Measured, all thirteen per-file claims: **nine stale**, every error an under-count, worst `tool_matrix.rs` **26 → 114** (4.4×, the worst in the repo); plus the dated total **307 → 946** and the `book_examples` sentinels **9 → 39**. **Root cause REFINED, not replaced, both halves kept:** **(A) the date** is exact *in the file `BTCS.1` actually repaired* — re-measured, **zero** undated counts survive in `architecture.md`; **(B)** it explains **nothing** in `CODEBASE_ANALYSIS.md`, where **undated** counts survived too, because that file was **judged, not enumerated** — it *was* swept (the log proves it, rejecting *"all 7 categories"* with a reason) but the sweep surfaced one candidate and stopped while fourteen matched its key. **(B) yields the more valuable rule: a sweep must record its MATCH COUNT, not only its finds.** `BTCS.1` recorded *"found 2 more live shadows and three false positives"* — precision reported, **recall never** — so its coverage claim was **unfalsifiable**. That is the exact sibling of [`CHANGES-ENTRY-PLACEMENT.3`](tasks/CHANGES-ENTRY-PLACEMENT.md)'s finding two commits earlier (an oracle's *count* recorded without its *pattern*): **an instrument is only as trustworthy as what it records about itself.** **Decisive evidence, better than any argument: the two copies DISAGREED WITH EACH OTHER** — for `src/metrics.rs` the book said **18**, this file said **20**, the truth is **31**; one derivable number, two copies, two *different* wrong values. And **four of thirteen are accidentally correct** (incl. `types.rs` 40), the very coincidence `BOOK-TEST-COUNT-SHADOWS` predicted — *a number that can become correct by accident carries no information*. Scope widened 3 → 15 lines **deliberately and said so**: one list, one shape, one rung, and every added line measured **before** it was touched (the opposite of the `/tmp` sweep's error). Repaired at **R1**: `architecture.md` needed no replacement (the derivations already sat 57 lines above) so its total became a paragraph naming *why this one survived*; `CODEBASE_ANALYSIS.md`'s bullets keep every descriptive word and lose only the numeral, its total replaced by a **pointer** plus the 18-vs-20-vs-31 divergence as recorded evidence — a pointer, never a second copy of the commands. **Acceptance check proven NON-VACUOUS** (`0037`): 0 hits after repair, 1 hit on a synthetic re-inserted count. Re-swept under the corrected rule: **no live-doc site asserts a test count**; every remaining hit is a past-tense record of a named leaf's completed action, each checked individually. `mdbook build` clean; `cargo test --test book_examples` green 4/4; 8/8 doctrines. **`.3` next — makes the corrected rules durable, and exists so `.2` cannot be mistaken for completion**; it now inherits **two** rules, not one, plus a note to **share** `OVERFLOW-DESTINATION-INSTRUMENTATION`'s self-declared-date instrument rather than build a second. Docs-only ⇒ DUT byte-identical. Frontier `.3`. **TREE CLOSED `2026-07-31` at `.3`.** `.3` landed the two corrected rules as decision [`0039`](decisions/0039-sweep-exemption-past-vs-present-and-recorded-recall.md) — **(i)** a sweep's exemption is keyed on whether the enclosing record speaks about the **past or the present**, never on whether it carries a **date**; **(ii)** a sweep **records its match count, not only its finds**, or its recall is unauditable. `0033` gains a **dated pointer Amendment**, not a restatement: `0033` §(c) closed on *"discovered by review and held by derivation"* and specified only the derivation half, and **both** of this tree's misses landed in the unspecified review half. **Placement was settled from the repo's own mechanism**, not from taste — `KM_SCAN_DIRS` is `docs/knowledge docs/decisions`, so `0039`'s twelve `answers:` keys index into `KNOWLEDGE_MAP.md` directly (91 → 92 facts / 920 → 932 keys) and a sweep author reaches the rule in **one lookup**, which makes a separate fact card a second copy of a rule whose subject is that second copies rot. **The coordination question with `OVERFLOW-DESTINATION-INSTRUMENTATION` was settled by measurement and the answer is "precondition", not "duplicate":** its candidate instrument (b) has **0** subjects in both survivor files and **0** across the 108-file live-doc set — its subject is a *file-level* freshness field with a derivable baseline, while this class's dates are *inline parentheticals* with none — so it **could not have caught this class**; and **60 of its 74** real subjects are `done`/`closed` trees whose date is correctly frozen, so without rule (i) it would cry wolf on 60 of 74 (`0033` test (2)). Negative-controlled both ways and the control proven capable of failing (75 files over the excluded set; 1 on a synthetic insert). **No gate added, deliberately.** | [docs/tasks/DATED-COUNT-SWEEP-EXEMPTION.md](tasks/DATED-COUNT-SWEEP-EXEMPTION.md) |
| `IR-TYPES-DECOMPOSITION` | `done` | Codebase hygiene / module ownership — owner-directed |  **Registered `2026-07-31`** on the owner directive *"why not break it down into smaller .rs files by ownership, I mean by who does what?"*. The framing is load-bearing and is written into the Non-Goals: this tree splits by **who answers which question** and explicitly refuses to split by line count. Measured at `bd7dba2`: `src/ir/types.rs` is **4069 lines**, the second-largest file in the crate, and it houses **two tenants foreign to a types file that are 55 % of it** — (a) the **canonicalization engine**, `flatten_associative` (539) + `intern_gate` (141) plus the ~38 `fold_*`/`peephole_*`/`flatten_associative_*`/`intern_gate_*` tests that are the bulk of the 1258-line test module (**~1830 lines together, 45 %**), and (b) the **steering taxonomy** `KnobId` + `impl` (378), which is not circuit IR at all — the giveaway being external: `scripts/check_enumeration_parity.sh` hard-codes `sed -n '/pub fn category(&self)/,/…/p' src/ir/types.rs`, i.e. **a doctrine check reaches into a file called `types.rs` to find a steering taxonomy**. The split is nearly free because `src/ir/mod.rs` already does `pub use types::*`: measured, only **2** sites repo-wide write `types::` explicitly, so **no call site changes** and a call-site change would prove the move was not pure. `KnobId` also carries **five parallel tables of the same 38 variants** (`enum`, `all()`, `index()`, `name()`, `category()`), which is exactly what `COVERAGE-STEERED-GENERATION.6` (rung **R1**) exists to collapse — hence the ordering decision: **`.1` (extract `KnobId`) runs BEFORE `CSG.6`** so that macro table lands in a file owning one thing and the ~340→~40-line diff is readable, and **`.3` (extract the interning engine) runs AFTER it** so a 1830-line move is never interleaved with a semantic change to a different type. `.4` re-measures and may legitimately conclude the remainder is one coherent data model and close without splitting further. Projected: `types.rs` → **~1750 lines** of pure data model. Every leaf is a pure move ⇒ `tests/snapshots.rs` untouched ⇒ DUT byte-identical. **`.1` done `2026-07-31`** (audit + registration; the measurement above *is* its work product). **`.2` done `2026-07-31`**: `types.rs` **4069 → 3607** (−462), new `src/ir/knob_id.rs` at 483 lines, `#[test]` count exactly conserved (42 → 40 + 2). Severability proven in **both** directions before cutting — nothing in `types.rs` referenced `KnobId`, and the moved block referenced **no** data-model type — so a file that shares no names with its host is a lodger, not a section. Blast radius exactly as `.1` predicted: **one** import line, and it is inside `src/ir/`; zero call sites elsewhere, which was both the acceptance and the review criterion. The doctrine extractor was repointed and negative-controlled (wrong path ⇒ 0 categories ⇒ floor trip). **The move surfaced two pre-existing defects**, each confirmed against `git show HEAD:` so neither was caused by it, and each now tree-owned: the `ENUMERATION-PARITY` steering extractor silently reads **7 of 8** categories ([`PARITY-EXTRACTOR-ARM-SHAPE-GAP`](tasks/PARITY-EXTRACTOR-ARM-SHAPE-GAP.md)), and four of five book test counts are stale ([`BOOK-TEST-COUNT-SHADOWS`](tasks/BOOK-TEST-COUNT-SHADOWS.md)). Frontier: **`COVERAGE-STEERED-GENERATION.6`** (different tree — `KnobId` now lives alone, so the rung-R1 macro table lands in a file owning one thing), then `.3`. **`.3` done `2026-08-01`** — the canonicalization engine (`intern_gate`, `intern_constant`, `fold_constants`, `flatten_associative`, `apply_peephole`) extracted to `src/ir/intern.rs`; **`types.rs` 3607 → 1485**. Widened from the goal's three functions to **five** on measurement: `fold_constants` and `apply_peephole` sit between them with **0** and **1** callers, so leaving them would have split one engine across two files. **Verbatim proven by whole-file line census** (residue **0**), not by reading the diff; `#[test]` **40 → 3 + 37**; fixture split measured per side so **nothing was duplicated**; blast radius exactly `src/ir/` with **zero** call-site changes. **1,058 tests pass**, `tests/snapshots.rs` 6/6 with **no `.snap` rewritten ⇒ DUT byte-identical**. **`.4` done `2026-08-01` — TREE CLOSED.** `.4` was written as *re-measure and decide*, and it decided **close**: `types.rs` is **1,473** lines (from **4,069**) and every region answers the one question a types file exists to answer — `Module` 29.7 %, nodes 24.6 %, tests 12.1 %, `impl Module` accessors 11.2 %, blocks 10.3 %, ports/params/aggregates/domains 9.3 %, `Design` 0.3 %. **The candidate third split evaporated under measurement**: `.1`'s *“~649 lines of port / clock-domain predicates”* is **302** today, and its `impl Module` half is **16 small accessors over a struct declared 170 lines above them** — a line-count boundary, not an ownership one, which is the split this tree's Goal refuses. Two numbers reproduce exactly (`impl Module` **1,327 lines / 21 methods** at `.1` → **165 / 16** now, the delta being precisely the five engine fns `.3` moved); the `~649` basis could **not** be reproduced and is recorded as unreproducible rather than rationalised. Net across three moves: **4,069 → 1,473**, two new single-purpose modules, **zero call sites changed**. Observed and deliberately **not** acted on: `src/ir/compact.rs` is now **5,453** lines and **unmeasured** — it would need its own tree and its own `.1` audit, never a silent widening of this one. | [docs/tasks/IR-TYPES-DECOMPOSITION.md](tasks/IR-TYPES-DECOMPOSITION.md) |
| `PARITY-EXTRACTOR-ARM-SHAPE-GAP` | `closed` | Doctrine enforcement — a gate that under-verifies | **Registered `2026-07-31`**, found while repointing the extractor during `IR-TYPES-DECOMPOSITION.2` and **confirmed pre-existing** against `git show HEAD:src/ir/types.rs`. `ENUMERATION-PARITY` pair 4 asserts four live docs each name every `--steer` category; its extractor has been returning **7 of 8** since it landed — `datapath` is invisible to it. **Root cause: the regex encodes a source *formatting* assumption, not a source fact.** `=> "[a-z]+"` assumes a one-line match arm; the `datapath` arm's three `\|`-joined patterns overflow the line width so `rustfmt` renders it as a block, putting the `=>` and the string on different lines. Nobody wrote it that way — **`rustfmt` did, because the pattern got long** — so the trigger is *"a category gains enough knobs to wrap"*, biasing the extractor against exactly the categories that grow. **Why it went undetected:** the count floor is `6` and the extractor returns `7`, and a floor catches *"matched nothing"*, not *"matched most"*; and `covers_set` is per-category, so a category the extractor never produces is verified at **no** doc site. Measured: all four sites *do* name `datapath`, so **the docs were fine and only the gate was blind** — a latent hole, not a live inconsistency, and worth stating precisely because overstating it would be as dishonest as missing it. Blast radius bounded by re-deriving **all six** declared extractors against their authoritative sets: five are exact; the five that are exact all parse structure a *tool* controls (a bash array, a Markdown column, a directory listing, a link syntax), and the only one parsing **`rustfmt` output** is the only one wrong. **`.1` done `2026-07-31` — TREE CLOSED**: fixed at the **class** level. `KnobId::category` returns `&'static str` and every arm's value is a bare string literal, so the extractor now takes **the set of string literals in the function body** — the taxonomy as a *fact*, not as a *layout* — with comment lines stripped first so a future `// "note"` cannot inject a phantom category (this gate must fail loud, never cry wolf). Explicitly **not** widening the regex to also accept the block form, which fixes the instance and leaves the class for the next arm `rustfmt` reshapes. Floor re-derived `6 → 8` with the reasoning inline: a floor is **shrink-coupled, not growth-coupled**, so it fails decision `0033`'s three-part shadow test and is not the hand-maintained list the doctrine forbids — adding a 9th category never requires touching it; only removing one would, which is exactly the event worth stopping for. **Negative-controlled four ways**, every in-place edit restored byte-exact and verified with `git diff --stat` (never `git checkout --`, per the recorded gotcha): (1) **the decisive one** — replacing `datapath` in `USER_GUIDE.md` now FAILS naming the file, and was **invisible before**; (2) rewriting the arm single-line still yields 8 ⇒ format-independent; (3) **the contrast that proves the class fix** — the *old* regex yields 8 on the single-line form and 7 on `rustfmt`'s block form, i.e. its answer depended on the layout while the new one's does not; (4) a wrong path yields 0 entries and trips the floor at 8. Driver 8/8; **no doc changed, because none needed changing.** | [docs/tasks/PARITY-EXTRACTOR-ARM-SHAPE-GAP.md](tasks/PARITY-EXTRACTOR-ARM-SHAPE-GAP.md) |
| `PARITY-EXTRACTOR-CHARSET-GAP` | `closed` | Doctrine enforcement — a gate that under-verifies | **Registered and CLOSED `2026-08-01`**, from a defect *measured* at `CAPABILITY-BREADTH-EXPANSION.4b.1` and **confirmed pre-existing**: two `ENUMERATION-PARITY` extractors captured an id with a character class narrower than their source permits, so a value outside it was **not read at all** and every doc-parity assertion about it passed **vacuously** — a new steering category named `case_qualifier` reported `ok` at all **7** fenced sites when it should have failed at all 7. Sibling of `PARITY-EXTRACTOR-ARM-SHAPE-GAP` (same file, same check) but a different class of wrongness: that one was a *format* assumption, this is a *charset* assumption about the captured value. `.1` widened both — the category capture `[a-z]+ -> [a-z0-9_]+` (now matching the **name** column on the very same row, where the asymmetry was accidental) and the adapter capture `[a-z0-9]+ -> [a-z0-9_-]+`. **Both held by a control measured fail-before / pass-after on the real source**, then restored to a zero diff: the category probe went **0 FAILs -> 7**, and the adapter probe went from **1 *wrong* failure** (its count floor reporting *"produced 4 entries (floor 5)"*, which points at the extractor rather than the two docs missing the id) to **2 correct parity failures naming `iverilog-compile`** — so that site gained *diagnosis*, not coverage. **`.2` then re-opened and properly closed it**: `.1` corrected two values and wrote prose, which is the bottom of the repair ladder and leaves nothing standing to catch the next narrowing — against this tree's own Goal (*"close it at the class level, not the instance"*). `.2` takes both skipped rungs: **(R1)** stop specifying a charset at all, capturing `"([^"]+)"`, since the quotes already delimit the value and a *widened* guess is still a guess; **(R2)** `total_or_fail`, requiring an extractor to **account for every item it walks** (a deliberately looser candidate predicate vs the extraction), so a silent skip is a hard failure. Decisive control: the exact historic scenario now reports *"walked 40 item(s) but produced 38 — it SILENTLY SKIPPED 2"* where it previously produced **7 vacuous `ok`s**; the guard also subsumes the older `PARITY-EXTRACTOR-ARM-SHAPE-GAP` class. Limit stated: it fires when an unreadable member *arrives*, not when the narrowing is *introduced* — which is the moment harm would occur. The durable rule, recorded in the script beside the arm-shape lesson: **capture the charset the SOURCE permits, not the charset its current members happen to use**, because a count floor is *shrink-coupled* and therefore blind to an id that is **born invisible** — and ids are added far more often than renamed, so the case a floor cannot see is the common one. | [docs/tasks/PARITY-EXTRACTOR-CHARSET-GAP.md](tasks/PARITY-EXTRACTOR-CHARSET-GAP.md) |
| `BOOK-TEST-COUNT-SHADOWS` | `done` | Live-doc hygiene / shadow-enumeration residue | **Registered `2026-07-31`**, found while `IR-TYPES-DECOMPOSITION.2` moved two tests out of `src/ir/types.rs` and had to read the book's claim about that file. `book/src/architecture.md` publishes a *"Current counts"* list of per-file unit-test totals; measured at `bd7dba2`, **four of five are wrong**: `types.rs` 40 vs **42**, `gen/cone.rs` 42 vs **43**, `emit/sv.rs` 17 vs **26**, `metrics.rs` 18 vs **31** (72 % understated); only `validate.rs` (26) is right. **Every error is an under-count** — the signature of prose that decays monotonically as tests are added, because nothing forces the edit. Sharpest illustration of why *correcting* the numbers is the wrong repair: `.2` moved exactly 2 tests out of `types.rs`, taking it 42 → 40 and thereby making the stale "40" **accidentally true again** — a number that can become correct by coincidence is not carrying information. All three of decision `0033` rule (a)'s tests hold (derivable by `grep -c`, growth-coupled, silent), so the mandated rung is **R1, deletion** — fixed *before* the work starts so the cheap wrong fix is not available later under pressure. Together with `PARITY-EXTRACTOR-ARM-SHAPE-GAP` these are **two independent residues of `SHADOW-ENUMERATION-SWEEP`'s `2026-07-30` closing claim** that the class was fully handled (R1/R2/R3/R4); the claim was too strong, and is repaired by new trees that measure it rather than by editing the closed one (decision `0031`: history stays raw). That is decision `0033`'s own rule (0) biting its author — *never write "currently correct" without measuring it.* **`.1` done `2026-07-31`:** all five counts **deleted** and replaced by three runnable derivations (per-file `grep -c`, whole-crate `grep -rc \| awk`, `cargo test --lib -- --list`), keeping the part that carried information — what each test cluster is *for*. The predicted embarrassment was **live at the moment of deletion**: `types.rs` claimed 40 and measured **40**, because `IR-TYPES-DECOMPOSITION.2` had walked 42 *down onto* the stale number; the other four measured 26/43/26/31 against claims of 26/42/17/18. The mandated **sweep from the EFFECT** (a numeral qualifying a countable repo noun) covered **94 live-doc files**, excluding `CHANGES.md`/`DEVELOPMENT_NOTES.md`/`docs/tasks/*.md` **by kind** — an append-only dated measurement is honest history, not a standing claim. It found **two more live shadows**, both repaired by deletion: `book/src/api-tools.md`'s *"exposes **10 tools**"* and `api-reference.md`'s *"the 10 tools: …"* + *"the 5 workflow prompts"*. Both were **measured against the running server first** (`tools/list` = 10, `prompts/list` = 5) and were **currently correct** — so they are recorded as **latent**, not as live inconsistencies, which is `PARITY-EXTRACTOR-ARM-SHAPE-GAP.1`'s lesson applied to its sibling. Equally load-bearing, the sweep **rejected three false positives** with reasons, which is what distinguishes an effect-keyed sweep from a shape-keyed one: `structured-emission.md`'s *"the six surfaces above"* is positional narrative that stays true as the set grows (test 2 fails); its *"nine surfaces"* / *"≥ 8"* mirror the real identifiers `saw_all_nine_emit_surfaces_in_one_module` / `saw_all_emit_surfaces_in_one_module`, so a 10th surface forces a rename **in code** (test 3 fails — not silent); and `CODEBASE_ANALYSIS.md`'s *"all 7 categories"* is the Phase-7 **manifest fact** categories, where 7 is right — a shape-keyed sweep would have "fixed" it into a lie. One ambiguous find was **split to `.2` rather than guessed at**: `USER_GUIDE.md`'s *"The 16 knobs documented below as config-file knobs"*, which can only be verified by reading the section it points at. **`.2` done `2026-07-31` ⇒ tree CLOSED.** Measured before editing, that count was wrong under **both** readings of its own referent: only **5** of the `2b.1` sixteen have a bullet below the sentence, **11** appear nowhere in `USER_GUIDE.md` at all, and **9** knobs are documented below — four of which (`multi_output_task_emit_prob`, `mux_if_emit_prob`, `case_mux_if_emit_prob`, `casez_mux_if_emit_prob`) shipped *after* `2b.1` and were never among the sixteen. So this was not a number that decayed but one that **never matched**: `.1` deleted counts that *became* wrong, `.2` deleted one that was wrong the day it was written, because a count is asserted about a referent and nothing checks the referent is the set the author meant. Repaired at **R1 by deletion** plus a runnable `comm -23` derivation of `Config`'s fields against `--help`'s flags. The **three-name exclusion list was measured and kept** — `Config` 92 fields minus `Overrides` 88 is exactly `{library_prob, max_nodes_per_module, use_async_reset}` + `{seed, steering}`, and `--help` confirms the three have no flag — because it enumerates an *exception*, not a derivable set. **Two errors self-caught before staging**, both by refusing to trust the artifact: the first draft's derivation wrote to `/tmp` (a `NO-BOOT-VOLUME-REFS` / decision `0031` breach in a live doc), and **running** the published command showed it prints two exception names the prose had guessed differently (`child_instances_per_module_by_depth` in, `seed` out) — the recorded *"the fixture agrees with you; the tool does not"* gotcha, caught only by execution. Its sweep split **two live registry shadows** to [`LIVE-DOC-REGISTRY-SHADOWS`](tasks/LIVE-DOC-REGISTRY-SHADOWS.md) and rejected one false positive with a reason. | [docs/tasks/BOOK-TEST-COUNT-SHADOWS.md](tasks/BOOK-TEST-COUNT-SHADOWS.md) |
| `LIVE-DOC-REGISTRY-SHADOWS` | `done` | Live-doc hygiene / shadow-enumeration residue | **Registered `2026-07-31`** by `BOOK-TEST-COUNT-SHADOWS.2`'s effect-keyed sweep, which found **three live sites naming a strict subset of an authoritative registry**, each measured against the running system rather than inferred. (a) `docs/knowledge/api-reference.md` says *"the **9 tools**"* and **lists nine** — a live `tools/list` returns **10**, and the missing one is `coverage`, so the defect is **membership**, not merely a count; the same card cites `schema_version 1.11` against a live **`1.27`**. A stale Knowledge Map card is the worst case per the recorded gotcha: it is read *instead of* re-deriving, so it misinforms rather than omits. (b) `book/src/agent-mcp.md` and (c) `book/src/api-introspection.md` each name **6 of the 8** steering categories, omitting `motifs` and `emission` — and `agent-mcp.md` calls its six *"the fixed set"*, which makes the omission a positive false claim. Five other list sites (`USER_GUIDE.md`, `book/src/knobs.md`, `book/src/algorithm.md`, `docs/AGENT_INTROSPECTION_SCHEMA.md`, `CODEBASE_ANALYSIS.md`) were measured and all name eight. **The finding that makes this worth a tree is that the gate for this exact class was green throughout.** `ENUMERATION-PARITY` pair 4 exists precisely to hold the `--steer` taxonomy in live docs, and it passes — because it checks **four declared sites** and neither offending chapter is one of them. The pair's own comment records that it was written by naming *"the six-name list copied into five live docs plus the book"*, i.e. the sites its author happened to find, and it was never re-swept from the authoritative set. That is decision [`0033`](decisions/0033-shadow-enumeration-classification.md) rule (2) — *search from the authoritative set, never from the shadow you found first* — biting the mechanism built to enforce decision `0033`, because a gate whose coverage is a hand-written list of sites inherits the very failure it exists to prevent. Repair rung fixed before work starts: the **count** `9` is **R1-delete**, the **list** is **repaired** (it is API surface a reader needs enumerated). `.1` repairs the three sites and regenerates the KM; `.2` answers whether pair 4's declared-site list can be *derived* instead of hand-maintained, and lands the answer as a decision record either way, since both outcomes bind every future pair. Docs-only ⇒ DUT byte-identical. **`.1` done** (`abf7090`) — all three sites repaired against re-measured registries, and its negative control, in failing, exposed that `covers_set` greps the whole *file* rather than the *list*. **`.2` done** — decision [`0037`](decisions/0037-enumeration-parity-declared-sites-and-list-scoped-coverage.md), both halves measured. **(i) The site list cannot be derived**: the candidate selector fails in *both* directions with no threshold in between — at `T ≤ 6` it selects append-only history (`CHANGES.md`, `DEVELOPMENT_NOTES.md`, `docs/tasks/*`) holding the *literal* historical six-name list, so the gate would be satisfiable only by a forbidden history rewrite (decision `0031`) or by an inverted hand-maintained exclusion list; at `T ≥ 7` it drops `book/src/algorithm.md` and `book/src/knobs.md` because their lists wrap across two lines, silently exempting a site on a prose-reflow accident. The list **fails `0033` test (2)** — it is *supposed* to differ from the tree — so it is **authoritative**, the fifth such hard case and the first at the enforcement layer. The selector survives as a **review-time discovery instrument** (it found `CODEBASE_ANALYSIS.md:2349` undeclared). **(ii) `covers_set` is the wrong predicate, and the hole is bigger than `.1` reported**: sweeping all **10** uses of it (rule (2) again — the authoritative set is *every use*, not the reported site) and probing each by deleting the enumeration, **3 still pass** — *both* pair-3 sites (`book/src/api-tools.md`, `book/src/agent-mcp.md`), so the downstream-allow-list pair protects **nothing** today, plus `book/src/knobs.md`. A proximity window was evaluated and **rejected on measurement** (still 2 lines at both pair-3 sites; misses a single-id omission where good explanatory prose sits beside the list). Repair: an HTML-comment **fence** carrying a set id and *no members*. **`.3` done — tree CLOSED.** 13 fences across 11 files; `covers_set` became `covers_fenced_set`, reading only inside the fence, with a **missing fence a hard failure** so the predicate cannot decay back; pair 4 declared over **7** sites. Markers are **inline** rather than on their own line, and that is load-bearing: most enumerations sit mid-paragraph, in a table row or in a list, and an own-line HTML comment is a CommonMark *block* that would split them — verified in the generated HTML that markers stay invisible and paragraphs stay intact. **Acceptance test passes**: the vacuity probe now FAILS at all 13 sites, including the 3 that previously passed with the enumeration deleted. **98 single-id controls — every id at every site, not a sample — all fail**, and they caught a defect in this leaf's own work: `.2`'s honest-limit paragraph sat *inside* `architecture.md`'s fence and named `MEMORY-ARCH` a second time, so dropping it from the registry list stayed green — the vacuity defect reproduced at fence scale by the sentence explaining vacuity. Two limits recorded as a dated **Correction** on `0037` rather than edited away: bidirectional parity is **withdrawn** (a fence enclosing prose-bearing list items is not losslessly extractable, and nothing is ever retired here anyway), and **a fence must contain the enumeration, not the discussion of it**. | [docs/tasks/LIVE-DOC-REGISTRY-SHADOWS.md](tasks/LIVE-DOC-REGISTRY-SHADOWS.md) |
| `LIVE-DOC-HYGIENE-BACKFILL` | `done` | Workflow / live-doc + task-tree hygiene | (complete — closed `2026-06-24`) Closed the doc-drift flagged across the `SEMANTIC-INTROSPECTION-EXPANSION` lane (deliberately not bundled into its `.8*`/`.9*`/`.10*` slices). **`.1`**: `book/src/api-introspection.md` was stale at schema `1.14` (predating the `.15`–`.22` bumps) — rewritten to `1.22` (envelope example + field table + stability-contract section enumerating the nine `analyze` queries + the metric-count bumps, cross-linking the canonical §7 changelog); also fixed a `1.21 → 1.22` JSON example in `book/src/api-tools.md` that `.10b.2` missed; mdBook builds. **`.2`**: backfilled the missing `.8a`/`.8b.1`/`.8b.2` Verification/Commit-Log rows (hashes `cabe696`/`5499067`/`ce86560`) + corrected the stale Status labels `.5b.2` (`pending → done`), `.9`/`.9b` (`active → done`), and the `.3` frontier-table cell (`active → done`) in `docs/tasks/SEMANTIC-INTROSPECTION-EXPANSION.md` — that file now has no leaf whose Status disagrees with its `done` Result. Docs-only ⇒ DUT byte-identical. | [docs/tasks/LIVE-DOC-HYGIENE-BACKFILL.md](tasks/LIVE-DOC-HYGIENE-BACKFILL.md) |
| `LIVE-DOC-DRIFT-FIX` | `done` | Workflow / live-doc hygiene | (complete — closed `2026-06-24`) Closed residual live-doc drift found at session bootstrap, beyond what `LIVE-DOC-HYGIENE-BACKFILL` reached. **`.1`**: `ROADMAP.md` still listed `Frontier → .19 (impl)` for the ninth structured surface (`casez_mux_if`) although `.19a`→`.19b.3` all landed (code `src/ir/casez_mux_if_emit.rs`, schema `1.22`, README current) — rewritten to record the surface delivered end-to-end + the lane at a no-active-frontier boundary; `TOOLBOX.md` stated the current introspection schema as `1.14` (→ `1.22`) and listed only 4 of the 9 delivered `analyze` derived queries (→ all nine: + `flop_dependencies`/`memory_provenance`/`fsm_provenance`/`node_drivers`/`node_readers`). **`.2`**: a dedicated mdBook deep-read found six stale `book/src/` spots — `api-reference.md` (current schema `1.14 → 1.22`; "9 tools" → "10 tools" adding `coverage`), `api-resources-prompts.md` (analysis resource 4 → 9 queries), `synthesizability.md` (stale "only pure function if ever used" → tasks/functions are behaviour-preserving emit-projections), `structured-emission.md` (intro 8 → nine surfaces), `introduction.md` ("What you'll find" + Structural Rules); `mdbook build` clean. Docs-only ⇒ DUT byte-identical. | [docs/tasks/LIVE-DOC-DRIFT-FIX.md](tasks/LIVE-DOC-DRIFT-FIX.md) |
| `MEMORY-ARCHITECTURE-DOC` | `done` | Workflow / memory architecture | (complete — `.1` standard + README pointer; `.2` ANVIL layer-C decisions; `.3` bounded `MEMORY.md`; `.4` self-check/hooks/CI/bootstrap enforcement; `.5` final focused validation clean; full cargo test intentionally out of scope per owner resource policy) | [docs/tasks/MEMORY-ARCHITECTURE-DOC.md](tasks/MEMORY-ARCHITECTURE-DOC.md) |
| `KNOWLEDGE-MAP-DOC` | `done` | Workflow / retrieval architecture | complete — `.1` project-agnostic bundle + discovery pointers; `.2` generated map + hook/CI enforcement; `.3` ANVIL decision-record retrieval keys + close. | [docs/tasks/KNOWLEDGE-MAP-DOC.md](tasks/KNOWLEDGE-MAP-DOC.md) |
| `SEQUENTIAL-IDENTITY` | `done` | NodeId as identity / full-factorization mode | complete — `.1` merges equivalent generated FSM blocks under node-id identity, surfaces `fsms_merged`, and documents the FSM-vs-memory proof boundary. | [docs/tasks/SEQUENTIAL-IDENTITY.md](tasks/SEQUENTIAL-IDENTITY.md) |
| `LIVE-DOC-IDENTITY-ALIGNMENT` | `done` | Live docs / NodeId identity status | complete — `.1` aligned stale CODEBASE identity-status prose after the FSM merge and existing hierarchy module-dedup layer. | [docs/tasks/LIVE-DOC-IDENTITY-ALIGNMENT.md](tasks/LIVE-DOC-IDENTITY-ALIGNMENT.md) |
| `LIVE-DOC-ROADMAP-ALIGNMENT` | `done` | Live docs / roadmap follow-up status | complete — `.1` aligned current roadmap/index/codebase follow-up status after `MULTI-CLOCK-CDC` and `DIFFERENTIAL-SIMULATION` closure. | [docs/tasks/LIVE-DOC-ROADMAP-ALIGNMENT.md](tasks/LIVE-DOC-ROADMAP-ALIGNMENT.md) |
| `LIVE-DOC-ROADMAP-LANE-STATUS-ALIGNMENT` | `done` | Live docs / roadmap owner-directed-lane status | complete — `.1` aligned the eight stale owner-directed-lane status statements in `ROADMAP.md` with delivered reality (structured-emission 5th `cone_function`/6th `multi_output_task` surfaces; `AGENT-MCP-EXPANSION` closed; `SIGNOFF-AUTOMATION-EXPANSION` active+`.2`; `IDENTITY-DEEPENING.3b.2b`; `DOWNSTREAM-ADAPTER-EXPANSION` sv2v+slang; `KNOB-ERGONOMICS-AND-PRESETS`; `CI-PACKAGING-DISTRIBUTION`; `CAPABILITY-BREADTH-EXPANSION` Mealy); docs-only, closed `2026-06-22` by `d3eb968`. | [docs/tasks/LIVE-DOC-ROADMAP-LANE-STATUS-ALIGNMENT.md](tasks/LIVE-DOC-ROADMAP-LANE-STATUS-ALIGNMENT.md) |
| `LIVE-DOC-TASK-TREE-INDEX-ALIGNMENT` | `done` | Live docs / task-tree index ↔ tree-file status alignment | complete — `.1` corrected the stale `LIVE-DOC-ROADMAP-LANE-STATUS-ALIGNMENT` index row (`active`→`done`) that drifted from its already-closed tree file; a full per-file `- Status:` vs index audit across all trees found no other drift. | [docs/tasks/LIVE-DOC-TASK-TREE-INDEX-ALIGNMENT.md](tasks/LIVE-DOC-TASK-TREE-INDEX-ALIGNMENT.md) |
| `LIVE-DOC-CODEBASE-ALIGNMENT` | `done` | Live docs / CODEBASE_ANALYSIS ↔ workspace alignment | complete — `.1` added the 5 omitted modules (`ir/param.rs`, `ir/aggregate.rs`, `frontend/`, `umbrella/`, `diff_sim/`) to the module map and corrected the integration-test count 3→6; surfaced by the session-bootstrap deep-dive, no other drift found. | [docs/tasks/LIVE-DOC-CODEBASE-ALIGNMENT.md](tasks/LIVE-DOC-CODEBASE-ALIGNMENT.md) |
| `HIERARCHY-DEDUP-PRUNE` | `done` | NodeId as identity / hierarchical module identity | complete — `.1` prunes modules made unreachable by opt-in hierarchy dedup merges while preserving no-merge under-instantiation and pre-existing top-unreachable modules from reachability cleanup. | [docs/tasks/HIERARCHY-DEDUP-PRUNE.md](tasks/HIERARCHY-DEDUP-PRUNE.md) |
| `MEMORY-IDENTITY-BOUNDARY` | `done` | NodeId as identity / full-factorization mode | complete — `.1` proves and documents that current inferrable memories remain state-by-instance under full-factorization passes because their stored contents are not reset-defined. | [docs/tasks/MEMORY-IDENTITY-BOUNDARY.md](tasks/MEMORY-IDENTITY-BOUNDARY.md) |
| `HIERARCHY-IDENTITY-BOUNDARY` | `done` | NodeId as identity / hierarchical module identity | complete — `.1` proves and documents that module dedup remains structural-only, not arbitrary semantic module equivalence. | [docs/tasks/HIERARCHY-IDENTITY-BOUNDARY.md](tasks/HIERARCHY-IDENTITY-BOUNDARY.md) |
| `ENDPOINT-IDENTITY-BOUNDARY` | `done` | NodeId as identity / full-factorization mode | complete — `.1` proves and documents that same-shape semantic cones over different leaf endpoints do not merge. | [docs/tasks/ENDPOINT-IDENTITY-BOUNDARY.md](tasks/ENDPOINT-IDENTITY-BOUNDARY.md) |
| `LIVE-DOC-BOOK-ALIGNMENT` | `done` | Live docs / mdBook ↔ codebase alignment | complete — `.1` corrected mdBook chapters that still labelled delivered Phase 5-9 motifs (memories, parameterization, Phase 7-9 lanes) as "future". | [docs/tasks/LIVE-DOC-BOOK-ALIGNMENT.md](tasks/LIVE-DOC-BOOK-ALIGNMENT.md) |
| `RESOURCE-SAFE-TOOLING` | `done` | Quality / workflow — resource-safe validation | complete — `.1` `scripts/ram_guard.sh` RAM watchdog; `.2` USER_GUIDE "Resource-safe runs" docs. | [docs/tasks/RESOURCE-SAFE-TOOLING.md](tasks/RESOURCE-SAFE-TOOLING.md) |
| `AGGREGATE-ARRAY-PACKING` | `done` | Phase 5b follow-on — synthesizable aggregates (packed array) | complete — `.1`–`.5` done (`AggregateKind::ArrayPacked` + emitter + `aggregate_array_prob` selection + metric + 7/7 Verilator/Yosys downstream-clean + book/docs sync); `.4b` (optional matrix CI instrumentation) `deferred`. Default-off byte-identical. | [docs/tasks/AGGREGATE-ARRAY-PACKING.md](tasks/AGGREGATE-ARRAY-PACKING.md) |
| `WORKLOAD-MEMORY-SAFETY` | `done` | Quality / signoff — resource-safe generation (ANVIL's own runtime) | complete — `.1` design; `.2` streamed directory-output manifest (peak metadata RAM O(1) in `--count`); `.3` real per-module node budget (`max_nodes_per_module`); `.4` opt-in internal RAM/RSS self-governor (`src/mem_guard.rs`, `--max-rss-mb`/`--ram-abort-pct`, between-unit `--out` checkpoint, clean exit 99); `.5` closeout (book safe-envelope narrative + deferred boundaries). All mechanisms default-off / byte-identical. | [docs/tasks/WORKLOAD-MEMORY-SAFETY.md](tasks/WORKLOAD-MEMORY-SAFETY.md) |
| `CONE-DECOMPOSITION` | `done` | Code quality / maintainability — generator core readability | complete — `.1` design + `.2`–`.7` extractions (`cone/{snapshot,semantic,primitives,terminals,flops,motifs}.rs`). `src/gen/cone.rs` 5551→2446 lines (56% reduction); root = recursion strategy, six cohesive submodules re-exported via `pub(crate) use <sub>::*`. Every leaf byte-identical (snapshots 6/6 throughout; full suite green at `.2` + `.7`). | [docs/tasks/CONE-DECOMPOSITION.md](tasks/CONE-DECOMPOSITION.md) |
| `AGENT-INTROSPECTION-MCP` | `done` | Capability — agent-drivable introspection + MCP interface | **closed `2026-06-15`** — all leaves `.1`–`.7` done. `.1` decision `0004`; `.2` `docs/AGENT_INTROSPECTION_SCHEMA.md` (versioned envelope, zero new computed truth); `.3` `src/introspect/` + default-off `--introspect` flag (DUT byte-identical); `.4` `src/mcp/` + `anvil-mcp` bin (stdio JSON-RPC; generate/introspect/dump_config + resources over a content-addressed cache); `.5` (`.5.1`/`.5.2`/`.5.3`) the controlled `validate`/`minimize` tools (`src/downstream/`: sandboxed + fixed `verilator`/`yosys`/`iverilog` allow-list + ram-guard + `anvil://audit/log`; minimize = deterministic coordinate-descent, seed fixed, budget-bounded; e2e clean vs real Verilator 5.046 + Yosys 0.64); `.6` the five agent-workflow prompts as first-class MCP prompts (`prompts/list`/`prompts/get`); `.7` the user-facing closeout — mdBook chapter `book/src/agent-mcp.md` (Reference) + USER_GUIDE section + README CLI-truth/key-paths sync (`book_examples` gate 3/3). DUT byte-identical throughout (snapshots 6/6). Optional future breadth (coverage_gaps MCP tool, HTTP transport, non-DUT lanes over MCP) does not reopen the tree. | [docs/tasks/AGENT-INTROSPECTION-MCP.md](tasks/AGENT-INTROSPECTION-MCP.md) |
| `CAPABILITY-LANE-OWNERSHIP` | `done` | Workflow / capability-lane task-tree ownership | complete — `.1` registered the three owner-directed post-phase capability lanes before implementation resumed (`2026-06-15`) | [docs/tasks/CAPABILITY-LANE-OWNERSHIP.md](tasks/CAPABILITY-LANE-OWNERSHIP.md) |
| `AGENT-MCP-EXPANSION` | `done` | Capability — agent/MCP interface breadth | complete — `.1` decision `0005`; `.2` coverage_gaps tool; `.3` (`.3a`+`.3b`) non-DUT lanes over MCP; `.4` (`.4a` framing design + `.4b` hand-rolled `--http <addr>` transport, loopback default, no new dep, DUT byte-identical); `.5` book/USER_GUIDE/README closeout. Tree CLOSED `2026-06-15` | [docs/tasks/AGENT-MCP-EXPANSION.md](tasks/AGENT-MCP-EXPANSION.md) |
| `SIGNOFF-AUTOMATION-EXPANSION` | `active` | Quality — downstream signoff automation breadth | `.1` done (decision `0006`); `.2` (= `.2a` design + `.2b` impl) **done** — first richer-knob-sweep increment delivered: new `num_operator_gates_with_duplicate_operands` metric + `ScenarioSet::SignoffKnobSweep` + `--signoff-knob-sweep-gate` + 4 `saw_*` facts (operand/mux-arm duplication, array-packed aggregate, memory×fsm interplay), banked clean at `anvil-signoff-knob-sweep-r1` (12 scenarios / 48 modules / `coverage_gaps=[]` / 48/0 Verilator + both Yosys). **No active frontier** — higher-ceiling future leaves preserved (decision `0006`); next lane per order = `IDENTITY-DEEPENING` | [docs/tasks/SIGNOFF-AUTOMATION-EXPANSION.md](tasks/SIGNOFF-AUTOMATION-EXPANSION.md) |
| `IDENTITY-DEEPENING` | `active` | NodeId as identity / full-factorization deepening | `.1` **done** (decision `0007`). `.2` **done** (`.2a` design + `.2b` impl): the opt-in `merge_bisimilar_flops` bounded bisimulation pass is live — merges mutually-recursive / swapped-feedback registers via greatest-fixpoint partition refinement over the existing 12-bit/128-node/131072-work proof, default-off / byte-identical, resetless flops excluded; new `Config`/`Module` `bisimulation_flop_merge` knob + `Metrics::bisimulation_flops_merged`; shared `finalize_flop_merge` refactor keeps the exact pass byte-identical; introspection schema MINOR-bumped 1.0→1.1; banked downstream-clean (Verilator + both Yosys + Icarus). `.3` split into `.3a` (design — **done**, decision `0008`: bounded whole-leaf-module sequential equivalence via cross-module bisimulation beside `dedup_semantic_modules`) + `.3b` (impl). `.3b` split into `.3b.1` (design-detail — **done**: combined-module materialization so inputs unify by `(PortId,width)` for free, factored `bisimulation_partition` reuse, no-bijection coinduction soundness, pre-filter + union-find grouping) + `.3b.2` (impl). `.3b.2` split into `.3b.2a` (**done**: factored the byte-identical `bisimulation_partition` helper) + `.3b.2b` (cross-module feature — **done** `2026-06-16`). `.3b.2b` split into `.3b.2b.1` (**done**: `modules_sequentially_equivalent` via combined-module materialization + `dedup_sequential_modules` pass + default-off `hierarchy_sequential_module_dedup` knob + gated wire-in + rules-first gate; DUT byte-identical), `.3b.2b.2a` (**done**: shared `group_sequentially_equivalent_modules` helper + `DesignMetrics` `sequential_module_proof_signatures` + `num_sequentially_duplicate_module_pairs` + introspection schema 1.3→1.4 + downstream-clean bank), and `.3b.2b.2b` (**done**: book §9b + hierarchy.md + USER_GUIDE knob + ROADMAP gap 2 + KM card). `.3`/`.3b`/`.3b.2`/`.3b.2b` all **done**. **No current frontier** — the deeper module-equivalence boundaries (memory / FSM / wrapper / retimed-state) are named, not-started future leaves (open-ended, none retired). | [docs/tasks/IDENTITY-DEEPENING.md](tasks/IDENTITY-DEEPENING.md) |
| `SV-VERSION-TARGETING` | `done` | Capability / breadth — version-targeted SV emission | **CLOSED `2026-06-16`** — all leaves done; down-gating + up-opting + per-version downstream acceptance axis delivered; the first version-distinctive up-opt (the IEEE 1800-2023 `union soft` overlay) ships both as a generator capability (`.3b.2a`) and a repo-owned matrix gate (`.3b.2b`, banked `anvil-sv-version-gate-upopt-r1`: 10 scenarios / 20 units / `coverage_gaps=[]` / Verilator 20/0 / Yosys 18/0); further up-opts are open-ended post-tree breadth (nothing retired). `.1` **done** (decision `0009`): opt-in `--sv-version <2012\|2017\|2023>` gate (`Config::sv_version`) — down-gating (never emit a construct newer than the target → standard-validity guarantee) + up-opting (deliberately emit a higher standard's distinctive synthesizable constructs, each proven downstream-clean in the matching tool mode), default byte-identical, rules-first, per-version downstream acceptance axis. Owner-directed highest-leverage lane (`2026-06-15`). `.2a` **done** (design detail). `.2b.1` **done**: `SvVersion {Sv2012<Sv2017<Sv2023}` enum + `Config::sv_version` + `--sv-version` CLI + versioned emitter entry points (`permits` down-gating bound) + introspection schema `1.1→1.2` + `tests/sv_version.rs`; default byte-identical. `.2b.2a` **done**: `run_verilator(_design)` `language: Option<&str>` selector (`--language 1800-20xx`; `None` = byte-identical) + `tests/sv_version_downstream.rs` (`#[ignore]`) banked clean. `.2b.2b` **done** (`2026-06-16`): repo-owned `tool_matrix --sv-version-gate` + `ScenarioSet::SvVersionSweep` (9 Interleaved scenarios = 3 targets × {comb leaf, seq leaf, recursive hierarchy design}) + per-version emit threading + matching-mode Verilator (`verilator_language_for`) + `saw_sv_version_*_targeted_acceptance` coverage facts + `MatrixReport.sv_version_gate`; banked clean `anvil-sv-version-gate-r1` (9 scenarios / 18 units / `coverage_gaps=[]` / 18/0 Verilator + both Yosys). **`.2`/`.2b`/`.2b.2` all closed.** `.3` split into `.3a` (design — **done**, decision `0010`: first up-opt = a default-off heterogeneous-width packed `union soft` (IEEE 1800-2023 §7.3.1) gated on `sv_version >= Sv2023`, struct down-gate fallback ⇒ byte-identical; the installed tools don't enforce 1800-version acceptance so the teeth are LRM + construction-time down-gating + matching-mode acceptance; Yosys/Icarus recorded no-op) + `.3b` (impl). `.3b` split into `.3b.1` (design-detail — **done**) + `.3b.2` (impl). `.3b.2` split into `.3b.2a` (**done** — the live first up-opt: `Config::soft_union_slice_prob` + `src/ir/soft_union.rs` gen-time pass + the `permits(Sv2023)`-gated `emit/sv.rs` `union soft` overlay of a proper low-bits `Slice`; default-off / byte-identical, snapshots 6/6; banked Verilator `--language 1800-2023` clean, 159 overlays / 7 seeds, 2012 down-gate proven; Yosys/Icarus recorded no-op) + `.3b.2b` (**done** `2026-06-16`: the repo-owned matrix up-opt gate — a tenth `sv2023_soft_union_upopt` `--sv-version-gate` scenario, Verilator-only via `scenario_emits_soft_union_overlay`/`verilator_only`, `ModuleReport.emitted_soft_union_overlay` emission evidence, the `saw_sv_version_2023_soft_union_upopt` fact + a `!yosys.is_empty()` honesty guard; banked clean). **Tree CLOSED.** | [docs/tasks/SV-VERSION-TARGETING.md](tasks/SV-VERSION-TARGETING.md) |
| `STRUCTURED-EMISSION-EXPANSION` | `active` | Capability / breadth — richer structured emission | **Activated `2026-06-16` by explicit owner directive.** `.1` design **done** — decision `0012`: the first richer-structured surface is a default-off, valid-by-construction combinational `function automatic` emit-projection of an existing combinational cone (the `output_support` support-leaf boundary gives its parameter list), chosen over interface/modport (weak Yosys synth support) + nested generate (bigger blast radius); opt-in `function_emit_prob` (default `0.0`) ⇒ byte-identical; downstream gate = Verilator + both Yosys modes + Icarus. `.2a` design-detail **done**; `.2b.1` live surface **done** (`function_emit_prob` knob + `Module.function_emit_gates` + `src/ir/function_emit.rs` gen-time mark + two generator call-site rolls + `to_sv_with_modules` `<wire>__f` `function automatic` decl/positional-body/call rendering + 9 lib proofs; `Slice` excluded from the first cut — `-Wall UNUSEDSIGNAL` on a full-width param, still emitted inline, nothing retired; no schema bump; default-off / DUT byte-identical, snapshots 6/6; forced sweep clean across Verilator + both Yosys + Icarus, `anvil-fe-r2/`). `.2b.2` pre-split (`2026-06-16`) → `.2b.2a` (metric + schema — **done**: `Metrics::num_emitted_combinational_functions` ⇒ introspection schema `1.7 → 1.8`; 468 lib tests / snapshots 6/6 / mdbook green) + `.2b.2b` (the `tool_matrix` gate — **done `2026-06-16`**: `--function-emit-gate` + `ScenarioSet::FunctionEmitSweep` + `build_function_emit_sweep_scenarios` [comb-only `function_emit_prob=1.0` × 3 strategies] + `ModuleReport.emitted_combinational_function` SV-text detection + `saw_combinational_function_emit` + early-return gap enforcement + 5 proofs; banked clean `anvil-function-emit-gate-r1` [3 scenarios / 12 modules / 608 emitted functions / `coverage_gaps=[]` / `12/0` Verilator + both Yosys + Icarus compile]; default-off / DUT byte-identical, snapshots 6/6) + `.2b.2c` (book/knobs/USER_GUIDE/README/KM closeout — **done `2026-06-16`**: new `How It Works` chapter `book/src/structured-emission.md` [byte-verified seed-42 before/after; single-gate rule; `Slice`/structured exclusions; duplicate-operand positional params; combinational-only] + `function_emit_prob` knob entry in `book/src/knobs.md`/`USER_GUIDE.md`/README "Current CLI truth" [config-file-only knob] + KM how-to card `combinational-function-emit`; KM 36→37 facts/272→286 keys; `mdbook build` + `check_knowledge_map` + `check_memory_architecture` + `cargo test --test book_examples` 3/3 green; docs-only / DUT byte-identical). **The first structured surface (the combinational `function automatic` emit-projection) is delivered end-to-end — `.2b.2`/`.2b`/`.2` all close.** `.3` design **done `2026-06-16`** (decision `0013`, by owner steer *"structured emission: next surface"* → `generate`): the **second** surface is a default-off, valid-by-construction **`generate for` loop** emit-projection of an existing `{N{x}}` replication (index-regular by construction), over `task` [leading future, also clean for simple comb void tasks], `interface`/`modport` [weak Yosys synth], and constant-predicate `generate if` [dead untaken branch; frontend lane already has it]; empirically grounded clean across Verilator `-Wall` + both Yosys + Icarus; the DUT emitter has no `generate`/`genvar` today, the frontend lane has `generate if`. Rules-first / default-off `generate_loop_emit_prob` (proposed) ⇒ byte-identical; downstream gate `saw_generate_loop_emit`. ****`.4a` design-detail done `2026-06-16`** — grounded decision `0013` in the real emitter (`render_gate`'s `{N{x}}` `Concat` replication predicate `sv.rs:1159` is the index-regular source; `function_emit.rs`/`soft_union.rs` gen-time annotation is the mechanism) and resolved all five points: first-cut = a `{N{x}}` **1-bit-lane** replication `Concat` (excludes function-emit marks); gen-time `src/ir/generate_loop.rs` + `Module.generate_loop_gates`; `genvar`/`generate for` block + assign-loop `continue`; `Config::generate_loop_emit_prob` config-file-only default `0.0`; `tool_matrix --generate-loop-gate`/`saw_generate_loop_emit`. **`.4b.1` live surface done `2026-06-16`** — the second richer-structured emit surface goes live: `Config::generate_loop_emit_prob` (default `0.0`, config-file-only) + `Module.generate_loop_gates` + new `src/ir/generate_loop.rs` (`annotate_generate_loop_gates`, `{N{x}}` 1-bit-lane replication candidate excluding function-emit marks) + two guarded gen-time call-site rolls (after function_emit) + `to_sv_with_modules` `generate_loop_gate`/`render_generate_loop_block` (`genvar`/`generate for` block + assign-loop suppression) + 9 lib proofs; `gi = gi + 1` increment (`gi++` not retired); default-off / DUT byte-identical (snapshots 6/6, lib 477); forced sweep clean across Verilator `--lint-only` (`-Wall` Δ=0) + both Yosys + Icarus (`anvil-gl-r1/`, 5 seeds). **`.4b.2a` metric + schema bump done `2026-06-16`** — `Metrics::num_emitted_generate_loops` (`= m.generate_loop_gates.len()`) surfaced in introspection `module_metrics` ⇒ `SCHEMA_VERSION` `1.8→1.9` (the metric bumps; the `.4b.1` knob rode the version); bumped all current-output schema refs (9 test assertions + schema doc + README + USER_GUIDE + 5 book example JSONs + the CODEBASE_ANALYSIS envelope line; historical landing attributions left intact); lib proof; default-off / DUT byte-identical (snapshots 6/6, lib 478); end-to-end introspect default `0` / forced `50`. **`.4`/`.4b`/`.4b.2` are `active` containers; **`.4b.2b` gate done `2026-06-16`** — the repo-owned `tool_matrix --generate-loop-gate` (`ScenarioSet::GenerateLoopSweep` + `build_generate_loop_sweep_scenarios` [comb-only `generate_loop_emit_prob=1.0` × 3 strategies] + `ModuleReport.emitted_generate_loop` + `saw_generate_loop_emit` + early-return gap enforcement + 5 proofs) banked clean `anvil-generate-loop-gate-r1` (3 scenarios / 12 modules / 8 emitting a loop / `coverage_gaps=[]` / `12/0` Verilator + both Yosys + Icarus); README/USER_GUIDE/CODEBASE_ANALYSIS gate entries. `.4b.2` closes. **`.4`/`.4b` are `active` containers; **`.4b.3` user-facing closeout done `2026-06-16`** — `book/src/structured-emission.md` gains a `## The second surface: a generate for loop` section (byte-verified seed-12 before/after; the `{N{x}}` 1-bit-lane rule; wider-lane exclusion; `gi = gi + 1`) + the `generate_loop_emit_prob` knob entry in `book/src/knobs.md` / `USER_GUIDE.md` / README + the KM how-to card `generate-loop-emit` (KM 38→39 facts / 296→309 keys); `mdbook build` + `check_knowledge_map` + `check_memory_architecture` + `book_examples` 3/3 green. **`.4b.3` / `.4b` / `.4` all close — the second structured surface (the `generate for` loop emit-projection) is delivered end-to-end.** **`.5` design-leaf done `2026-06-16`** (decision `0014`, autonomously selected at the no-frontier boundary per the owner *"pick any tree and roll"* directive): the **THIRD** structured surface is a default-off, valid-by-construction combinational **`task automatic`** emit-projection of a single combinational gate (the decision `0012` single-gate parallel, but a procedural `task` with an `output` arg called from `always_comb`), over nested `generate` + `interface`/`modport`; empirically grounded clean across Verilator `-Wall` + both Yosys + Icarus (both the direct-output and the output-var passthrough forms); rules-first / default-off `task_emit_prob` / `saw_combinational_task_emit` gate. **`.6a` design-detail done `2026-06-16`** — grounded decision `0014` in the real emitter (the `to_sv_with_modules` function-decl + generate-block sections as the template; **`render_gate_function_body` reused verbatim** as the task body) and resolved all five points: the output-var + passthrough-`assign` integration; gen-time `src/ir/task_emit.rs` + `Module.task_emit_gates` (the function-emit predicate plus exclusion of the sibling projections, run after generate_loop); the `task automatic` decl + `always_comb` call + assign-RHS swap; `Config::task_emit_prob` config-file-only default `0.0`; `num_emitted_combinational_tasks` metric (schema `1.9→1.10`) + `tool_matrix --task-emit-gate` / `saw_combinational_task_emit`. **`.6`/`.6b` are `active` containers; **`.6b.1` live surface done `2026-06-16`** — `Config::task_emit_prob` (default `0.0`, config-file-only) + `Module.task_emit_gates` + new `src/ir/task_emit.rs` (`annotate_task_emit_gates`, the function-emit candidate predicate plus exclusion of the three sibling projections) + two guarded gen-time call-site rolls (after generate_loop) + the `to_sv_with_modules` `task_emit_gate` accessor + `render_gate_task_decl` (body via the reused `render_gate_function_body`) + `render_gate_task_call` (the `logic <wire>__tv` var + the `always_comb` call) + the gate-assign-loop passthrough + 11 lib proofs; output-var + passthrough integration; no schema bump (the `.6b.2` metric bumps `1.9→1.10`); default-off / DUT byte-identical (snapshots 6/6, lib 489); forced `task_emit_prob=1.0` sweep clean across Verilator `--lint-only` (`-Wall` Δ=0) + both Yosys + Icarus (`anvil-te-r1/`, 5 seeds). **`.6b.3` user-docs closeout done `2026-06-16`** — the THIRD structured surface (the combinational `task automatic`, decision `0014`) is delivered end-to-end; `.6b.3` / `.6b` / `.6` all close. Docs-only: a `## The third surface: a combinational task automatic` section in `book/src/structured-emission.md` (byte-verified seed-1 before/after) + the `task_emit_prob` knob entry in `book/src/knobs.md` / `USER_GUIDE.md` / README + the KM how-to card `combinational-task-emit` (KM 40→41 facts / 318→331 keys); `mdbook build` + `check_knowledge_map` + `check_memory_architecture` + `book_examples` 3/3 green. Full surface: `.6a` design + `.6b.1` live (`task_emit_prob` + `src/ir/task_emit.rs` + emitter) + `.6b.2a` metric/schema-`1.10` + `.6b.2b` the `tool_matrix --task-emit-gate` (banked `anvil-task-emit-gate-r1`, 12/12 tasks, `12/0` all tools). **`.7` design-leaf done `2026-06-17`** (decision `0015`, autonomously selected at the no-frontier boundary per `feedback_pick_and_roll_at_no_frontier`): the **FOURTH** structured surface is a default-off, valid-by-construction **wider-lane `generate for` part-select** — a behaviour-preserving broadening of the second surface from the 1-bit lane to `LW >= 1` (a `{N{x}}` replication whose lane is `LW` bits renders `assign <wire>[gi*LW +: LW] = <x>;`; `LW==1` stays the byte-identical `[gi]` form). Chosen over `interface`/`modport` (**empirically disqualified** this session: Icarus syntax-fails the modport port + both Yosys modes warn on the implicit interface-member decl) and nested/multi-level `generate` (clean but bigger blast radius + no routine by-construction 2D source); a fresh probe (Verilator 5.046 `-Wall` + Yosys 0.64 both modes + Icarus 13.0) accepts the wider-lane part-select warning-clean + iverilog-sim-proven `== {N{x}}`. **Reuses** the existing `generate_loop_emit_prob` knob + `num_emitted_generate_loops` metric — no new knob / no new metric / no schema bump. Split `.7` (design done) + `.8` (impl, pre-split `.8a` design-detail + `.8b` impl) + future `.9+` (nested/multi-level `generate`, `interface`/`modport`, richer tasks). **`.8b` impl done `2026-06-17` — the FOURTH structured surface (the wider-lane `generate for` part-select) is delivered end-to-end; the lane returns to no-current-frontier (open-ended).** Two surgical edits: `src/ir/generate_loop.rs` `gate_qualifies` relaxed to `LW >= 1` / `width == N*LW`, and `src/emit/sv.rs` `render_generate_loop_block` branches `LW==1` (verbatim `[gi]`, byte-identical) vs `LW>1` (`[gi*LW +: LW]`), with `generate_loop_gate`'s defensive re-check mirrored. 4 lib proofs + book §"The fourth surface" (byte-verified seed-74 before/after) + knobs/USER_GUIDE/README/CODEBASE_ANALYSIS/KM closeout. **Reuses** `generate_loop_emit_prob` + `num_emitted_generate_loops` — no new knob / no new metric / no schema bump. `cargo test --lib` 493 + snapshots 6/6 byte-identical; a per-seed ON-vs-OFF downstream sweep (8 seeds) emits 9 wider-lane part-selects with Verilator `-Wall` Δ=0 + Yosys both + Icarus, and `--generate-loop-gate` stays regression-clean (12/0). `.8a` (prior) resolved the design-detail + proved corpus-liveness (20 multi-bit-lane replications / 300-module sweep). FOUR structured surfaces delivered end-to-end (`function automatic` `.1`+`.2`, `generate for` loop `.3`+`.4`, `task automatic` `.5`+`.6`, wider-lane part-select `.7`+`.8`). **`.9` design-leaf done `2026-06-17`** (decision `0016`, autonomously selected at the no-frontier boundary per `feedback_pick_and_roll_at_no_frontier`): picked the **FIFTH** structured surface — a default-off, valid-by-construction **multi-gate-cone `function automatic`** (deepen the first surface from a single gate to a whole combinational cone: params = the `output_support` support leaves, body = topo-ordered function-local `logic` temps for the interior gates, return = root; behaviour-identical to the inline per-gate chain). Chosen on the by-construction-source axis (decision `0015`): any cone with `>= 2` interior gates qualifies (pervasive), vs the multi-output task's policy-laden co-supported-sink source + nested generate's absent 2D source. Fresh probe (`anvil-se9-probe/`, Verilator 5.046 `-Wall` + Yosys 0.64 both modes + Icarus 13.0): zero `%Warning` across all tools + iverilog-sim-proven `==` the inline cone (4000 vec); the multi-output `task` is also clean+sim-equiv (deferred runner-up); nested `generate` clean-but-no-source; `interface`/`modport` stays disqualified (`0015`). Its **own** opt-in `cone_function_emit_prob` knob (the shipped single-gate `function_emit_prob` surface stays byte-identical; reusing it rejected) + a `num_emitted_cone_functions` metric (schema `1.10 → 1.11` at impl) + a `--cone-function-gate` / `saw_cone_function_emit` gate. Split `.9` (design done) + `.10` (impl, pre-split `.10a` design-detail + `.10b` impl) + future `.11+` (multi-output task, nested/multi-level `generate`, `interface`/`modport`). **`.10a` design-detail done `2026-06-17`** (no source): grounded decision `0016` in the real `function_emit.rs` (`gate_qualifies`), `analyze.rs` (`build_cone`/`visit` — the post-order walk + support-leaf boundary), `sv.rs` (`render_gate_function_decl`/`_body`/`_call`), `gen/mod.rs` (run cone-function last), `config.rs` (the knob list); resolved all five open questions — separate `cone_function_emit_prob` knob; absorb only admissible, non-sibling-marked, **single-use-within-cone** interior gates (else boundary param; `Constant` folds inline); post-order topo body + module-wire-name locals + ascending-NodeId params; run last + `Module.cone_function_gates: BTreeMap<NodeId, Vec<NodeId>>` + emitter interior-suppression; `num_emitted_cone_functions` (schema `1.10 → 1.11`) + `--cone-function-gate` / `saw_cone_function_emit`. **`.10b.1` live surface done `2026-06-17`** (first source change): `Config::cone_function_emit_prob` (separate from `function_emit_prob`) + `Module.cone_function_gates: BTreeMap<NodeId, Vec<NodeId>>` + new `src/ir/cone_function_emit.rs` (use-count + single-use post-order cone-walk + greedy selection + interior reservation, run last) + two `gen/mod.rs` rolls + `src/emit/sv.rs` cone-decl section + interior-suppression (net-decl **and** gate-assign loops) + 4 render helpers + 8 lib proofs. No metric / no schema bump (those at `.10b.2`). Default-off / DUT byte-identical (`cargo test --lib` 493→501; snapshots 6/6); forced sweep (8 seeds) emits 18 cone functions, Verilator `-Wall` Δ=0 vs OFF + Yosys both + Icarus clean (`anvil-cf-sweep/`). `.10b` split → `.10b.1` (done) + `.10b.2` (done) + `.10b.3` (done). **`.10b.3` done `2026-06-17`** — the user-facing docs closeout (a `## The fifth surface: a multi-gate-cone function automatic` section in `book/src/structured-emission.md` [byte-verified seed-4 before/after] + the `cone_function_emit_prob` knob entry in `book/src/knobs.md` / `USER_GUIDE.md` / README + the KM how-to card `multi-gate-cone-function-emit` [KM 43→44 facts / 351→364 keys]; `mdbook build` + `check_knowledge_map` + `check_memory_architecture` + `book_examples` 3/3 green; docs-only / DUT byte-identical); **the FIFTH structured surface (the multi-gate-cone `function automatic`, decision `0016`) is delivered end-to-end** (`.9` design + `.10a` design-detail + `.10b.1` live + `.10b.2` metric+gate + `.10b.3` docs). Prior: `.10b.2` done — the cone-function metric (`Metrics::num_emitted_cone_functions`, introspection schema `1.10 → 1.11`) + the repo-owned `tool_matrix --cone-function-gate`, banked clean `anvil-cone-function-gate-r1` (3 scenarios / 12 modules / 12 emitting a cone function / 148 cone functions / `coverage_gaps=[]` / `12/0` Verilator + both Yosys + Icarus); `cargo test --lib` 502 / `--bin tool_matrix` 73 / snapshots 6/6 byte-identical. **`.11` design done `2026-06-22`** (decision `0025`, autonomously selected at the no-frontier boundary per `feedback_pick_and_roll_at_no_frontier`): picked the **SIXTH** structured surface — a default-off, valid-by-construction **multi-output combinational `task automatic`** (generalize the single-gate `task` from one `output` to several: a co-supported group of qualifying combinational gates co-emitted into ONE `task automatic` whose deduplicated input formals carry their shared support, called once from `always_comb` into per-member output vars; each member's net driven by a passthrough `assign`). First cut = a **pair** (`k=2`) sharing a **non-constant** operand; **soundness rule** = mutual fan-in independence (no member in another's transitive fan-in, else the shared task closes a combinational cycle — `UNOPTFLAT`; cheap bounded backward DFS over the operand-topological `NodeId` invariant). Reuses the cone-function dedup render primitives (`cone_function_params`/`cone_operand_ref`/`render_cone_gate_expr`) + the single-gate task candidate predicate (`feedback_full_factorization`). Own `multi_output_task_emit_prob` knob + `--multi-output-task-emit-prob` flag (single-gate surface stays byte-identical; reusing `task_emit_prob` rejected) + `num_emitted_multi_output_tasks` metric (schema `1.13 → 1.14` at impl) + `--multi-output-task-gate` / `saw_multi_output_task_emit` (`__mt(` detection). Fresh probe (`anvil-se-motask-probe/`): a 2-output combinational task is Verilator `-Wall` 2012/2017/2023 + Yosys both modes + Icarus clean and iverilog-sim-equiv (5000 vectors). The recorded deferred runner-up (decision `0016` `.9` probe). Split `.11` (done) + `.12` (impl, pre-split `.12a`/`.12b`) + future `.13+` (wider `k>2` task groups, nested/multi-level generate, interface/modport). **`.12a` design-detail done `2026-06-22`** (`DEVELOPMENT_NOTES.md`: pinned the candidate predicate, the `multi_output_task_groups` BTreeMap carrier, the one-roll-per-leader pairing, the `in_fanin` bounded-DFS soundness check, the dedup-formal rendering reusing the cone primitives, the ordering/exclusion, and the knob/metric/gate split; `.12b` pre-split `.12b.1`/`.12b.2`/`.12b.3`). **`.12b.1` live surface done `2026-06-22`** — `multi_output_task_emit_prob` knob + `--multi-output-task-emit-prob` flag + `Module.multi_output_task_groups` + new `src/ir/multi_output_task_emit.rs` (pairing + the `in_fanin` soundness DFS) + two `gen/mod.rs` rolls + `cone_function` sibling exclusion + `src/emit/sv.rs` decl/call section (reusing `render_cone_gate_expr` with an empty interior_set) + per-gate passthrough + 11 lib proofs; default-off / DUT byte-identical (lib 599, snapshots 6/6); forced sweep `anvil-mo-sweep/` emits `__mt(` on 4/5 seeds, Verilator `-Wall` Δ=0 vs OFF (2012/2017/2023) + Yosys both + Icarus clean + exhaustively sim-equiv to the inline reference. **`.12b.2a` metric + schema done `2026-06-22`** — `Metrics::num_emitted_multi_output_tasks` ⇒ introspection schema `1.13 → 1.14` (all current-output refs bumped + the drifted CODEBASE envelope brought current; book JSONs deferred to `.12b.3`); `cargo test --lib` 600 + snapshots 6/6; `--introspect` default 0 / forced 1 at schema 1.14. **`.12b.2b` repo-owned gate done `2026-06-22`** — `tool_matrix --multi-output-task-gate` + `ScenarioSet::MultiOutputTaskSweep` + focus config (high `terminal_reuse_prob` + shallow `max_depth`) + `ModuleReport.emitted_multi_output_task` (`__mt(`) + `saw_multi_output_task_emit` + gap arm + 5 proofs + fixtures + README/USER_GUIDE/CODEBASE entries; banked clean `anvil-multi-output-task-gate-r1` (3 scenarios / 12 modules / 6 emitting / `coverage_gaps=[]` / `12/0` Verilator + both Yosys + Icarus); `cargo test --bin tool_matrix` 84 + snapshots 6/6. **`.12b.3` user docs done `2026-06-22`** — `book/src/structured-emission.md` sixth-surface section (byte-verified seed-3 before/after) + `multi_output_task_emit_prob` knob entry in `book/src/knobs.md`/`USER_GUIDE.md`/README + KM card `multi-output-task-emit` (62 facts) + the deferred book schema-JSON `1.13→1.14` refresh; `mdbook`/`check_knowledge_map`/`check_memory_architecture`/`book_examples` 3/3 green. **The sixth structured surface (decision `0025`) is delivered end-to-end; SIX surfaces total.** **`.13a` design-detail done `2026-06-22`** — the first *deepening* of the sixth surface (widen the multi-output `task automatic` group from the pair `k=2` to a bounded `k>2`, the recorded `.13` follow-up of decision `0025`): a `DEVELOPMENT_NOTES.md` entry confirming the **emitter is already k-agnostic** (so the only `.13b` source change is `annotate_multi_output_task_groups`) + pinning the greedy group-extension (connected co-support + generalized mutual fan-in independence + a `const MAX_MULTI_OUTPUT_TASK_GROUP_MEMBERS=8` cap), the no-new-knob/metric/schema decision, and the byte-identical/RNG argument. **`.13b` live widening done `2026-06-22`** — the whole change is in `src/ir/multi_output_task_emit.rs`: `const MAX_MULTI_OUTPUT_TASK_GROUP_MEMBERS = 8` + two helpers (`connected_co_support` / `independent_of_all`) + the partner loop rewritten "find-first-and-break" → greedy "admit every eligible ungrouped higher-`NodeId` candidate up to the cap" (a group forms iff `>= 1` partner ⇒ `k=2` is the exact subset). **No emitter / config / metric / schema / knob change** (the emitter was already k-agnostic). 5 new k>2 lib proofs + all 10 pair proofs green (15 total); doc comments + `CODEBASE_ANALYSIS.md` brought current. Default-off / DUT byte-identical (`cargo test --lib` 600→605, snapshots 6/6). Gate re-bank `anvil-mo-k3-gate-r1` `coverage_gaps=[]` / `saw_multi_output_task_emit=true` / `12/0` Verilator + both Yosys + Icarus with a **k=3 group** present; forced sweep seed 22 emits a genuine k=3 task `shr_0__mt(o0,o1,o2,…)`, Verilator `-Wall` Δ=0 vs OFF + iverilog sim-equiv over 20000 vectors. **`.13c` user docs done `2026-06-22`** — `book/src/structured-emission.md` sixth-surface intro reframed (pair → group `k>=2`, up to 8) + a `### Wider groups (k > 2)` subsection with a real seed-22 three-member task + the connected-co-support / all-member-independence rules + the cap; "What gets wrapped"/"How anvil proves it" generalized; the `multi_output_task_emit_prob` knob entry in `book/src/knobs.md`/`USER_GUIDE.md`/README; KM card `multi-output-task-emit` refreshed (4 new answers, body pair → k>=2, bank → `anvil-mo-k3-gate-r1`; KM 589→593 keys); `mdbook`/`check_knowledge_map`/`check_memory_architecture`/`book_examples` 3/3 green. **The first deepening of the sixth surface (wider `k>2` groups) is delivered end-to-end — `.13c`/`.13` close.** **`.14` design done `2026-06-22`** — decision `0027`: picked the **SEVENTH** structured surface, a default-off, valid-by-construction **procedural `always_comb` `if`/`else`** emit-projection of a `Mux` gate (the 2:1 selection it renders today as the `(sel)?(a):(b)` ternary), written into a per-gate `<wire>__cv` output var + a passthrough `assign` — the **first procedural-conditional construct** in the lane (the six delivered surfaces are function/task/generate projections; the `Mux` is a continuous-assign ternary; `CaseMux`/`CasezMux` are `case`/`casez`); chosen over nested-multi-level `generate` (no by-construction source — operand-uniqueness CSE shares the inner `{N{x}}` so the single-level loop already fires on `{M{y}}`) + `interface`/`modport` (empirically disqualified since `.7`); own `mux_if_emit_prob` knob + `num_emitted_mux_if_blocks` metric (schema `1.14 → 1.15` at impl) + `--mux-if-gate`/`saw_mux_if_emit` (`__cv` detection); fresh probe (`anvil-ifelse-probe.*`) Verilator `-Wall` 2012/2017/2023 + both Yosys (no warnings) + Icarus clean + iverilog sim-equiv 20000 vectors; no source change ⇒ DUT byte-identical. **`.15a` impl design-detail done `2026-06-22`** — a `DEVELOPMENT_NOTES.md` entry grounding decision `0027` in the real `task_emit.rs` / `sv.rs` (`node_ref` `:1242`, the task decl/call `:444`, the gate-assign-loop passthrough `:578`, the procedural-block loop `:620`) / `gen/mod.rs` (the six-pass roll chain `:95`/`:330`) / `config.rs` / `types.rs`, pinning all seven impl points (the `GateOp::Mux` predicate excluding the six sibling marks; `Module.mux_if_gates: BTreeSet<NodeId>`; the emitter `always_comb if/else` `<name>__cv` block + the gate-assign-loop passthrough; run `annotate_mux_if_gates` **last** ⇒ no other pass changes; the knob/flag; the metric schema `1.14→1.15`; the `--mux-if-gate`/`saw_mux_if_emit` gate; the byte-identical/RNG argument); pre-split `.15` → `.15b` (impl, itself `.15b.1` live / `.15b.2` metric+gate / `.15b.3` docs). No source change. **`.15b.1` live surface done `2026-06-22`** — the procedural `always_comb` `if`/`else` projection of a 2:1 `Mux` goes live behind the opt-in `mux_if_emit_prob` knob: new `src/ir/mux_if_emit.rs` (`annotate_mux_if_gates`, the `GateOp::Mux` predicate excluding all six sibling marks, run **last**; 10 lib proofs) + `Module.mux_if_gates: BTreeSet<NodeId>` + `ir/mod.rs` registration + the `mux_if_emit_prob` knob + `--mux-if-emit-prob` flag + two guarded `gen/mod.rs` rolls (after `cone_function`, single + design) + the `emit/sv.rs` procedural-block section (`logic [w-1:0] <name>__cv;` + `always_comb if/else`, operand refs via `node_ref`) + the gate-assign-loop `<name>__cv` passthrough; `CODEBASE_ANALYSIS.md` + `DEVELOPMENT_NOTES.md` updated. No metric/schema bump (the `.15b.2` metric bumps `1.14→1.15`; the knob rides the version). Default-off / DUT byte-identical (`cargo test --lib` 605→615, snapshots 6/6). Forced `mux_if_emit_prob=1.0` sweep (`anvil-muxif-genproof.*`, 5 seeds): every plain mux (121–195/seed) → `__cv`, Verilator `-Wall` Δ=0 vs OFF + both Yosys (no warnings) + Icarus clean; comb-only ON-vs-OFF iverilog sim-equiv 20000 vectors. **`.15b.2` metric + gate done `2026-06-22`** — `Metrics::num_emitted_mux_if_blocks` (`= m.mux_if_gates.len()`, `#[serde(default)]`) + the `metrics_count_emitted_mux_if_blocks` lib proof ⇒ introspection `SCHEMA_VERSION` `1.14 → 1.15` (bumped all current-output refs: `introspect/mod.rs` const + doc-comment + 3 assertions, 8 `mcp/mod.rs` assertions, `docs/AGENT_INTROSPECTION_SCHEMA.md` §7 current-version + a `1.14→1.15` changelog entry, README ×2 + USER_GUIDE ×1 + `CODEBASE_ANALYSIS.md` envelope refs; historical `num_emitted_multi_output_tasks @ 1.14` attributions intact; book example JSONs deferred to `.15b.3`) + the repo-owned `tool_matrix --mux-if-gate` (`ScenarioSet::MuxIfSweep` + `build_mux_if_sweep_scenarios`/`mux_if_focus_config` [comb-only node-id+egraph, `mux_if_emit_prob=1.0`, `comb_mux_prob=0.9` + `comb_mux_encoding_prob=1.0` forcing the encoded chained-ternary path that builds plain `GateOp::Mux` gates] × 3 strategies + `ModuleReport.emitted_mux_if` [`__cv` detection] + `CoverageSummary.saw_mux_if_emit` + merge + `MatrixReport.mux_if_gate` + run-plan units/fail-on-gap + mutual-exclusion + select/build dispatch + summarize block + early-return gap arm + slug arms + `test_cli` default + 5 cargo-portable proofs + 8 fixture extensions), templated on `--multi-output-task-gate`. `cargo test --lib` 615→616 / `--bin tool_matrix` 84→89 / snapshots 6/6 byte-identical; banked clean `anvil-mux-if-gate-r1` (3 scenarios / 12 modules / `coverage_gaps=[]` / `saw_mux_if_emit=true` / 12/12 emit `__cv` / 215 blocks / Verilator 12/0 + both Yosys 12/0 + Icarus 12/0 / exit 0). Default-off / DUT byte-identical. Frontier → `.15b.3`. **`.15b.3` user docs done `2026-06-22`** — docs-only closeout (book seventh-surface section with a byte-verified seed-1 before/after + the `mux_if_emit_prob` / `--mux-if-emit-prob` / `--mux-if-gate` entries in `book/src/knobs.md` + `USER_GUIDE.md` + README + the deferred book example-JSON `schema_version` `1.14 → 1.15` refresh [`api-tools.md` ×3, `agent-mcp.md` ×1] + the `docs/knowledge/mux-if-emit.md` KM card + regenerated `KNOWLEDGE_MAP.md` + the `ROADMAP.md` lane status); `mdbook build` + `check_knowledge_map` + `check_memory_architecture` green; `cargo test --test book_examples` 3/3 (new bash block `book-test: skip`); docs-only ⇒ DUT byte-identical. **`.15b` / `.15` close — the SEVENTH structured surface (the procedural `always_comb` `if`/`else` projection of a `Mux`, decision `0027`) is delivered end-to-end; SEVEN structured surfaces total.** **`.16` design done `2026-06-22`** — decision `0028`: picked the **EIGHTH** structured surface, a default-off, valid-by-construction procedural `always_comb` `if`/`else if` **priority chain** emit-projection of a `CaseMux` gate (the N-way selection it renders today as the parallel `case (sel) … default` statement) — the recorded decision-`0027` follow-up. Distinct from the seventh surface (the N-way `CaseMux` chain vs the single 2:1 `Mux` conditional; `GateOp::CaseMux` vs `GateOp::Mux`) and the existing `case` render (sequential-priority `if`/`else if` vs parallel-match `case`). Simpler than the seventh surface: a `CaseMux` is already an `always_comb`-written `logic` var ⇒ **no** `__cv` passthrough (only the body swaps `case…endcase` → `if…else`). Candidate = a dynamic-selector `CaseMux` (`render_static_structured_gate` `None`); the `CasezMux` masked chain is the follow-up. Own `case_mux_if_emit_prob` knob + `num_emitted_case_mux_if_chains` metric (schema `1.15 → 1.16` at impl) + `--case-mux-if-gate`/`saw_case_mux_if_emit` (metric-keyed, no new text token). Fresh probe (`scratchpad/probe8/`): Verilator `-Wall` 2012/2017/2023 + both Yosys (32 cells, no warnings) + Icarus clean + iverilog sim-equiv to the parallel `case` over 20000 vectors + an exhaustive selector sweep. Stale parent `.15`/`.15b` flipped `active`→`done`. **`.17a` impl design-detail done `2026-06-22`** — a `DEVELOPMENT_NOTES.md` entry grounding decision `0028` in the real `mux_if_emit.rs` template + `render_static_structured_gate`'s `CaseMux` static-collapse test + the structured-case emitter loop + the roll chain + config/types touch points, pinning all seven impl points (the dynamic-selector `CaseMux` predicate, the in-place `case`→`if`/`else if` body branch with no `__cv` passthrough, the carrier/knob/metric/metric-keyed-gate/RNG). **`.17b.1` LIVE source change done `2026-06-22`** — the eighth surface goes live behind the opt-in `case_mux_if_emit_prob` knob: new `src/ir/case_mux_if_emit.rs` (`annotate_case_mux_if_gates`, the dynamic-selector `CaseMux` predicate excluding constant selectors + `CasezMux` + sibling marks, run last; 9 lib proofs) + `Module.case_mux_if_gates` + `--case-mux-if-emit-prob` flag + two `gen/mod.rs` rolls (after `mux_if`) + the `emit/sv.rs` structured-case body branch (`case…endcase` → `if`/`else if`, parallel `case` suppressed for a marked gate). No metric/schema bump (rides the version; `.17b.2`). `cargo test --lib` 616→625 (9 new proofs) / `--bin tool_matrix` 89 unaffected / snapshots 6/6 byte-identical; forced `case_mux_if_emit_prob=1.0` sweep (seed 2: 7 `CaseMux` → 7 chains) Verilator `-Wall` Δ=0 vs OFF (2012/2017/2023) + both Yosys + Icarus clean + generated-RTL ON-vs-OFF sim-equiv 20000 vectors. **`.17b.2a` metric+schema done `2026-06-23`** — `Metrics::num_emitted_case_mux_if_chains` (`= m.case_mux_if_gates.len()`) + a lib proof ⇒ introspection `SCHEMA_VERSION` `1.15 → 1.16` (all current-output refs; historical `num_emitted_mux_if_blocks @ 1.15` intact; book JSONs deferred to `.17b.3`); exact metric (static selectors excluded); `cargo test --lib` 625→626 / snapshots 6/6 byte-identical; live `--introspect` `1.16` + metric `0` default / `53` forced. **`.17b.2b` gate done `2026-06-23`** — repo-owned `tool_matrix --case-mux-if-gate` (`ScenarioSet::CaseMuxIfSweep` + a `case_mux_prob`-biased focus config × 3 strategies + the **METRIC-KEYED** `emitted_case_mux_if`/`saw_case_mux_if_emit`, no new text token; templated on `--mux-if-gate`, ~30 touch points; focus config sets `comb_mux_prob = 0.0` so the earlier-rolling comb-mux block never starves the `case`-mux roll); `cargo test --bin tool_matrix` 89→94, snapshots 6/6; banked clean `anvil-case-mux-if-gate-r1` (3 scenarios / 12 modules / 12 emitting a chain / 83 chains / `coverage_gaps = []` / `saw_case_mux_if_emit = true` / 12/0 Verilator + both Yosys + Icarus). Split `.16` (design) + `.17` (impl: `.17a` design-detail done + `.17b` `.17b.1` live done / `.17b.2` pre-split `.17b.2a` metric+schema done / `.17b.2b` gate done / `.17b.3` docs done — book eighth-surface section + byte-verified seed-1 example + `case_mux_if_emit_prob` knob entries in knobs/USER_GUIDE/README + the deferred book example-JSON `schema_version` `1.15→1.16` + seed-42 `run_id` refresh + a `case-mux-if-emit` KM card; `mdbook build`/`book_examples` 3/3/`check_knowledge_map` clean) ⇒ **`.17b`/`.17` close, EIGHT structured surfaces delivered end-to-end**. **`.18` design done `2026-06-23`** — decision `0029`: picked the **NINTH** structured surface, a default-off, valid-by-construction procedural `always_comb` `if`/`else if` **masked** priority chain `(sel & care_mask) == value_masked` emit-projection of a wildcard `CasezMux` gate (the N-way wildcard selection it renders today as the parallel `casez (sel) … default` statement), writing the gate's existing `always_comb`-written `logic` var (no `__cv` passthrough, the eighth-surface precedent); generalizes the eighth surface's bare-equality `CaseMux` chain (decision `0028`) to the wildcard `CasezMux`, reusing the established `care_mask = (~wildcard_mask) & sel_mask` idiom from `metrics.rs`/`compact.rs`. Fresh probe (`scratchpad/probe`, grounded in a real ANVIL `casez_mux_0` block): the concise `sel ==? pattern` form **DISQUALIFIED** — Yosys `0.64` rejects `==?` (`syntax error`) in BOTH repo modes — so the lowered masked-AND form ships; it is Verilator `5.046` `-Wall` 2012/2017/2023 + both Yosys modes (0 warnings, `check` passes) + Icarus clean and iverilog `vvp` EXHAUSTIVELY sim-equivalent to the `casez` (128/128 disjoint + 128/128 a hand-built overlapping-priority probe, 0 mismatches). Generator facts: `build_casez_patterns` uses `wildcard_bits=1` ⇒ arms non-overlapping, all-wildcard arm impossible ⇒ no constant-true condition. Own `casez_mux_if_emit_prob` knob + `num_emitted_casez_mux_if_chains` metric (schema `1.16→1.17` at impl) + metric-keyed `--casez-mux-if-gate`/`saw_casez_mux_if_emit`. DOCS-ONLY ⇒ DUT byte-identical. **`.19a` impl design-detail done `2026-06-23`** — a `DEVELOPMENT_NOTES.md` entry grounding decision `0029` in the real code (the `case_mux_if_emit.rs` template; the `emit/sv.rs` `CasezMux` arm `:724` + `render_casez_pattern` `:2171` + `constant_value`/`bitmask`/`casez_pattern_matches` + the `:769` trailing-default suppression; `build_casez_patterns` `wildcard_bits=1`; the `metrics.rs`/`compact.rs` `care_mask` idiom), pinning all `.19b` impl points (dynamic-selector `CasezMux` predicate; `casez_mux_if_gates` carrier; the in-place masked `(sel & care) == val` `if`/`else if` emitter branch, no `__cv`, + the trailing-default suppression tweak; run last; knob/flag; metric + schema `1.16→1.17`; metric-keyed `--casez-mux-if-gate` + a focus config zeroing both `comb_mux_prob` and `case_mux_prob`; byte-identical/RNG) + the `.19b` proof plan. **`.19b.1` live source change done `2026-06-23`** — the ninth surface goes live behind the opt-in `casez_mux_if_emit_prob` knob: new `src/ir/casez_mux_if_emit.rs` (`annotate_casez_mux_if_gates`, the dynamic-selector `GateOp::CasezMux` predicate, run last; 9 lib proofs) + `Module.casez_mux_if_gates` + `ir/mod.rs` reg + the knob/`--casez-mux-if-emit-prob` flag + two `gen/mod.rs` rolls (after `case_mux_if`) + the `emit/sv.rs` `CasezMux` masked-chain body branch (`(sel & SW'h{care}) == SW'h{val}` via the existing `constant_value`/`bitmask` helpers, no `__cv`) + the trailing-default suppression tweak. `cargo test --lib` 635 (9 new), snapshots 6/6 byte-identical, clippy/fmt clean; forced sweep (6 seeds) Verilator `-Wall` Δ=0 (2012/2017/2023) + both Yosys + Icarus clean + ON-vs-OFF sim-equiv (seed 1 8/8 + seed 4 128/128). No schema bump at `.19b.1`. **`.19b.2a` metric + schema done `2026-06-23`** — `Metrics::num_emitted_casez_mux_if_chains` (`= m.casez_mux_if_gates.len()`, exact) ⇒ introspection schema `1.16 → 1.17` (all current-output refs; per-metric landing attributions + book JSONs intact/deferred); `cargo test --lib` 636, snapshots 6/6 byte-identical; live `--introspect` `1.17` + `0` default / `1` forced. **`.19b.2b` gate done `2026-06-23`** — repo-owned `tool_matrix --casez-mux-if-gate` (`ScenarioSet::CasezMuxIfSweep` + a `casez_mux_prob`-biased `casez_mux_if_focus_config` × 3 strategies + the **METRIC-KEYED** `emitted_casez_mux_if`/`saw_casez_mux_if_emit`, no new text token; templated 1:1 on `--case-mux-if-gate`, ~30 touch points; the focus config zeros **BOTH** `comb_mux_prob` AND `case_mux_prob` since both roll before `casez_mux` in `cone.rs` and short-circuit it — the eighth surface's single-zero generalized); a co-located 1-line blank-line fix resolved a pre-existing rustfmt-1.9.0 fmt drift in the `.19b.2a` `metrics.rs` test; `cargo test --bin tool_matrix` +5 proofs / `--lib casez_mux_if` 10/10 / snapshots 6/6 byte-identical / clippy+fmt clean; banked clean `anvil-casez-mux-if-gate-r1` (3 scenarios / 12 modules / 12 emitting a chain / 108 chains / `coverage_gaps = []` / `saw_casez_mux_if_emit = true` / 12/0 Verilator + both Yosys + Icarus). `.19b.2` closes. **`.19b.3` user docs done `2026-06-23`** — DOCS-ONLY ⇒ DUT byte-identical: a `book/src/structured-emission.md` ninth-surface section (byte-verified seed-1 before/after — `casez (slice_0)` block vs masked `if ((slice_0 & 2'h2) == …)` chain) + chapter-intro surface list; the 4 deferred book example-JSON `schema_version` `1.16 → 1.17` (`agent-mcp.md` + `api-tools.md` ×3); `casez_mux_if_emit_prob` entries in `book/src/knobs.md` / `USER_GUIDE.md` / README + a `--casez-mux-if-gate` matrix-gate entry; a `docs/knowledge/casez-mux-if-emit.md` KM card, `KNOWLEDGE_MAP.md` regenerated (69 → 70 facts / 672 keys); `mdbook build` + `book_examples` 3/3 + `check_knowledge_map` clean. **`.19b.3` / `.19b` / `.19` close ⇒ the NINTH structured surface is delivered end-to-end; NINE surfaces total.** Tree stays `active` at a **no-frontier boundary**; future `.20+` (nested/multi-level `generate`, `interface`/`modport`) each picked on its own merits with a fresh probe + decision. Default-off ⇒ DUT byte-identical. None retired | [docs/tasks/STRUCTURED-EMISSION-EXPANSION.md](tasks/STRUCTURED-EMISSION-EXPANSION.md) |
| `SEMANTIC-INTROSPECTION-EXPANSION` | `active` | Capability — deeper introspection surface | **Activated `2026-06-16` by explicit owner directive** (deep semantic introspection first-class + everything MCP-queryable via a top-notch API). `.1` design **done** (decision `0011`): a first-class, versioned, MCP-queryable, **SCHEMA-DERIVED derived-relation** API (a `DerivedAnalysis` introspection section, schema `1.3`, + a pure MCP `analyze` tool) answering *what depends on what* by pure IR-graph traversal — relations, **not** behaviour (the `0004` no-shadow-simulator / structure-first boundary is the permanent ceiling). First query = the output **support cone**. `.2` split into `.2a` (design-detail — **done**: the `DerivedAnalysis`/`SupportCone` shape in a pure `src/introspect/analyze.rs`, `output_support` kind + name-`target`, default-introspect-stays-lean + cone-stops-at-instance-boundary, schema `1.2→1.3`) + `.2b` (impl). `.2b` split into `.2b.1` (**done**: the pure `src/introspect/analyze.rs` support-cone analysis core) + `.2b.2` (**done**: schema `1.2→1.3` + the `DerivedAnalysisDocument` + the pure MCP `analyze` tool + dispatch/`tools/list`/`anvil://artifact/<run_id>/analysis/<query>` resource + book/USER_GUIDE/schema-doc + KM; unknown query/target ⇒ `-32602`; DUT byte-identical, snapshots 6/6). **`.2`/`.2b` done — the first query (output support cone) is delivered end-to-end.** Two queries delivered (`output_support` `.1`/`.2` + `input_reach` `.3`, schema `1.5`, e2e `anvil-mcp` clean). **`.4` (`flop_reset_provenance`, per-flop reset/data provenance) done** (`.4a`/`.4b.1`/`.4b.2`): the MCP `analyze` tool now answers `query=flop_reset_provenance` (a `FlopProvenance` per flop: reset kind/value, zero-vs-hold default, mux kind/arms, has_d) by projecting `Module.flops`; a third `flop_provenance` vec keeps prior docs byte-identical; schema `1.5 → 1.6`; e2e `anvil-mcp` clean. **all four named query kinds from decision `0011` delivered** (`output_support` `.1`/`.2`, `input_reach` `.3`, `flop_reset_provenance` `.4`, `module_reachability` `.5`), schema `1.7`, DUT byte-identical. `.5` closed `2026-06-16` (`.5a` design + `.5b.1` pure core + `.5b.2` surface): the MCP `analyze` tool answers `query=module_reachability` (which modules in a design are reachable from `design.top` via the instance graph) — `cargo test --lib` 458/0/2, snapshots 6/6, book_examples 3/3, e2e `anvil-mcp` smoke clean. **`.6` done `2026-06-23`** — a **fifth** derived query, `flop_dependencies` (the register→register dependency graph; the first beyond decision `0011`'s four named kinds, under the "open-ended breadth" clause), delivered end-to-end (`.6a` design + `.6b.1` pure core + `.6b.2` surface): `QUERY_FLOP_DEPENDENCIES` + `FlopDependencies { flop, depends_on_flops, driven_flops, self_dependent }` fifth parallel vec reusing `build_cone`; registry + `run_analyze` dispatch + schema `1.17 → 1.18` + `analyze_schema` enum + book/USER_GUIDE/schema-doc/README/KM; `cargo test --lib` 644/0/2; snapshots 6/6; book_examples 3/3; e2e `anvil-mcp` smoke (seed 7 → schema 1.18, 32 flops; unknown → -32602). **`.7` (`memory_provenance`) active `2026-06-23`** — a **sixth** derived query (per-inferrable-memory port provenance: each `Memory`'s structural shape + the support cone of each of its `raddr`/`waddr`/`wdata`/`we` ports, reusing `build_cone`; the second beyond decision `0011`'s four named kinds; **the first query to open the documented opaque-`MemRead`-leaf boundary**). `.7a` design-detail **done** (no source): a sixth `memory_provenance: Vec<MemoryProvenance>` vec reusing `SupportCone` per port — prior five documents byte-identical; `"mem:<id>"` addressing; schema `1.18 → 1.19`. Pre-split `.7b` → `.7b.1` (pure core) + `.7b.2` (surface). delivered end-to-end (`.7a` design + `.7b.1` pure core + `.7b.2` surface): `QUERY_MEMORY_PROVENANCE` + `MemoryProvenance { mem, addr_width, data_width, kind, single_port, +4 per-port `SupportCone`s }` sixth parallel vec reusing `build_cone`; registry + `run_analyze` dispatch (`module`/`design_memory_provenance`) + schema `1.18 → 1.19` + `analyze_schema` enum (the `"mem:<id>"` target) + book/USER_GUIDE/schema-doc/README/KM; `cargo test --lib` 653/0/2; snapshots 6/6; book_examples 3/3; e2e `anvil-mcp` smoke (seed 7 `memory_prob 1.0` → schema 1.19, 1 `simple_dual_port` memory; unknown `mem:99999` → -32602). **`.8` (`fsm_provenance`) active `2026-06-23`** — a **seventh** derived query (per-generated-encoding-FSM provenance: each `Fsm`'s structural shape — `num_states`/`encoding`/`state_width`/`sel_width`/`out_width`/`is_mealy` — + the support cone of its one generated input cone `sel`, reusing `build_cone`; the third beyond decision `0011`'s four named kinds; **the second query to open a documented opaque-leaf boundary**, the `Node::FsmOut` sibling of `.7`'s `MemRead`; the table *values* deliberately out of scope). `.8a` design-detail **done** (no source): a seventh `fsm_provenance: Vec<FsmProvenance>` vec reusing `SupportCone` for the one `sel` cone — prior six documents byte-identical; `"fsm:<id>"` addressing; schema `1.19 → 1.20`. delivered end-to-end (`.8a` design + `.8b.1` pure core + `.8b.2` surface): `QUERY_FSM_PROVENANCE` + `FsmProvenance { fsm, num_states, encoding, state_width, sel_width, out_width, is_mealy, sel_support }` seventh parallel vec reusing `build_cone` over `Fsm::sel`; registry + `run_analyze` dispatch (`module`/`design_fsm_provenance`) + schema `1.19 → 1.20` + `analyze_schema` enum (the `"fsm:<id>"` target) + book/USER_GUIDE/schema-doc/README/KM; `cargo test --lib` 663/0/2; snapshots 6/6; book_examples 3/3; e2e `anvil-mcp` smoke (seed 7 `fsm_prob 1.0` → schema 1.20, 1 gray 3-state Moore FSM with a real `sel` cone; unknown `fsm:99999` → -32602). **`.9` (`node_drivers`) active `2026-06-23`** — an **eighth** derived query (per-node immediate 1-hop driver adjacency + each node's `GateOp` — the atomic node-level primitive complementing the transitive `output_support` cone; the **fourth** beyond decision `0011`'s four named kinds; its dual `node_readers` a natural future sibling). `.9a` design-detail **done** (no source): an eighth `node_drivers: Vec<NodeDrivers>` vec + a `NodeRef` operand struct — prior seven documents byte-identical; `"node:<id>"` addressing (None ⇒ all nodes ascending; a leaf node known-but-empty; out-of-range ⇒ `-32602`); a single one-hop pass over `m.nodes` (no transitive walk); schema `1.20 → 1.21`. Pre-split `.9b` → `.9b.1` (pure core) + `.9b.2` (surface). delivered end-to-end (`.9a` design + `.9b.1` pure core + `.9b.2` surface): `QUERY_NODE_DRIVERS` + `NodeDrivers { node, kind, op, width, drivers: Vec<NodeRef> }` + `NodeRef { node, kind, name }` eighth parallel vec + `module_node_drivers`/`design_node_drivers` (a single one-hop pass over `m.nodes`); registry + `run_analyze` dispatch (`module`/`design_node_drivers`) + schema `1.20 → 1.21` + `analyze_schema` enum (the `"node:<id>"` target) + book/USER_GUIDE/schema-doc/README/KM; `cargo test --lib` 671/0/2; snapshots 6/6; book_examples 3/3; e2e `anvil-mcp` smoke (seed 7 → schema 1.21, 1674 nodes / 1438 gate nodes with ops; unknown `node:999999` → -32602). **`.10` (`node_readers`) active `2026-06-24`** — a **ninth** derived query (per-node immediate 1-hop **reader** adjacency — the **exact transpose of `node_drivers` `.9`**, the operand-relation inverse; the node-level analog of `input_reach` ↔ `output_support`, with the provable duality `B ∈ node_drivers(A) ⇔ A ∈ node_readers(B)`; the **fifth** beyond decision `0011`'s four named kinds). `.10a` design-detail **done** (no source): a ninth `node_readers: Vec<NodeReaders>` vec reusing the existing `NodeRef` — prior eight documents byte-identical; transpose the operand relation (readers sorted+deduped ascending, unlike `node_drivers`' operand order; output-port/flop-D drives out of scope); `"node:<id>"` addressing (None ⇒ all nodes ascending; a node with no readers known-but-empty; out-of-range ⇒ `-32602`); schema `1.21 → 1.22`. delivered end-to-end (`.10a` design + `.10b.1` pure core + `.10b.2` surface): `QUERY_NODE_READERS` + `NodeReaders { node, kind, op, width, readers: Vec<NodeRef> }` (reusing `NodeRef`) + the ninth `node_readers` vec + `module_node_readers`/`design_node_readers` (the `node_readers_with` driver transposes each `Gate`'s operands via a `BTreeMap<u32,BTreeSet<u32>>` reader index; readers sorted+deduped ascending; output-port/flop-D drives out of scope ⇒ `B ∈ drivers(A) ⇔ A ∈ readers(B)`); registry + `run_analyze` dispatch (`module`/`design_node_readers`) + schema `1.21 → 1.22` + `analyze_schema` enum (the `"node:<id>"` target) + book/USER_GUIDE/schema-doc/README/KM; `cargo test --lib` 678/0/2; snapshots 6/6; book_examples 3/3; e2e `anvil-mcp` smoke (seed 7 → schema 1.22, 1674 nodes / 1653 with ≥1 reader, node 0 read by gates `[43, 705]`; unknown `node:999999` → -32602). **`.11` (`instance_provenance`) active `2026-06-24`** — a **tenth** derived query (for each child instance in a design's top, the support cone of each child output port built **inside the child module's graph**; the **third opaque-leaf boundary-opener** — opens `Node::InstanceOutput`, the sibling of `.7`'s `MemRead` + `.8`'s `FsmOut` — and the **first and only derived query that crosses the module boundary**, hence **Design-only** since a bare `Module` lacks the child-definition table; the **sixth** beyond decision `0011`'s four named kinds). `.11a` design-detail **done** (no source): a tenth `instance_provenance: Vec<InstanceProvenance>` vec + `InstanceProvenance { instance, module, role, output_support: Vec<SupportCone> }` reusing `SupportCone` — prior nine documents byte-identical; descend into the child (reuse `build_cone` unchanged with the child as its walked module); instance-**name** addressing; `design_*` real / `module_*` degenerate no-child-defs; schema `1.22 → 1.23`. delivered end-to-end (`.11a` design + `.11b.1` pure core + `.11b.2` surface): `QUERY_INSTANCE_PROVENANCE` + `InstanceProvenance { instance, module, role, output_support: Vec<SupportCone> }` reusing `SupportCone` + the tenth `instance_provenance` vec + `module_instance_provenance`/`design_instance_provenance` (descend into the child — `build_cone` per child output port inside the child, reusing `build_cone` unchanged); registry + `run_analyze` dispatch (`module`/`design_instance_provenance`) + schema `1.22 → 1.23` + `analyze_schema` enum (a new instance-**name** target) + book/USER_GUIDE/schema-doc/README/KM; `cargo test --lib` 690/0/2; snapshots 6/6; book_examples 3/3; e2e `anvil-mcp` smoke (seed 42 hierarchy → schema 1.23, 2 instances; `u_0`/`mod_42_0000`/`planned_child`, cone `u_0.o_0` → `support_inputs ["i_1"]` + `support_flops [0]` across the module boundary; unknown instance → -32602). **`.12` (`instance_input_bindings`) done `2026-06-24`** — an **eleventh** derived query (the **parent-side dual of `instance_provenance` `.11`**: for each child instance in a design's top, the parent node (`NodeRef`) driving each child **input** port, read from `Instance.inputs: Vec<(PortId, NodeId)>`; `.11` descends into the child for outputs, `.12` ascends to the parent for inputs; chaining `.12 → .11` traces a parent signal across the module boundary; the **seventh** beyond decision `0011`'s four named kinds — the exact future extension `.11a` named). delivered end-to-end (`.12a` design + `.12b.1` pure core + `.12b.2` surface): `QUERY_INSTANCE_INPUT_BINDINGS` + `InstanceInputBindings { instance, module, role, input_bindings }` + `InstanceInputBinding { port, driver: NodeRef }` reusing `NodeRef` + the eleventh `instance_input_bindings` vec + `module_`/`design_instance_input_bindings` (a **pure read** of `Instance.inputs`; parent driver via `node_ref_of(top, ..)`; child def names the port, fallback `"port<id>"`; the module variant **NON-degenerate** — the contrast with `.11`); registry + `run_analyze` dispatch + schema `1.23 → 1.24` + `analyze_schema` enum + book/USER_GUIDE/schema-doc/README/TOOLBOX/KM; `cargo test --lib` 700/0/2; snapshots 6/6; book_examples 3/3; e2e `anvil-mcp` smoke (seed 42 hierarchy → schema 1.24, 2 instances, 10 bindings; `u_0` input `clk` ← driver `{node:0, primary_input, "clk"}`; unknown instance → -32602) — which found-and-corrected a control-port doc bug (clk/rst_n ARE in `Instance.inputs`; code already correct). **No active frontier** — `.13` (`longest_path`, the TWELFTH derived query) delivered end-to-end (`.13a` design + `.13b.1` pure core + `.13b.2` surface): for a target, one representative LONGEST combinational fan-in path — the ordered interior-`Gate` chain realizing `output_support`'s scalar `cone_depth`, terminating at a `NodeRef` leaf; the witness for `cone_depth` + transitive complement to `node_drivers`; structural gate-depth NOT timing; provably `longest_path(t).depth == output_support(t).cone_depth`; schema `1.25`. Twelve derived queries delivered (schema `1.25`). **`.14` (`node_reach`) active `2026-06-24`** — a **thirteenth** derived query (for a node addressed `"node:<id>"`, the **transitive combinational fan-OUT** of that node: the boundary sinks it reaches — `reaches_outputs` output ports + `reaches_flops` flop `D` cones + `fanout_targets`; the **forward-transitive corner** completing the node-addressed driver/reader × 1-hop/transitive 2×2 matrix — `node_drivers` `.9`/`node_readers` `.10`/`longest_path` `.13`/`node_reach` `.14` — the **transitive complement to `node_readers`** mirroring `longest_path`'s complement to `node_drivers`; the node-addressed generalization of `input_reach` `.3` that **subsumes** the per-FSM/per-memory reach candidate; provably `node_reach("node:<PrimaryInput-of-i>").{reaches_outputs,reaches_flops} == input_reach(i).{…}`; sinks output ports + flop `D`s only, symmetric with `input_reach`). `.14a` design-detail **done** (no source): a thirteenth `node_reach: Vec<NodeReach>` vec + `NodeReach { node, kind, op, width, reaches_outputs, reaches_flops, fanout_targets }` (node-family header field-for-field with `NodeReaders` + `input_reach` payload field-for-field with `ReachResult`; no new leaf type — twelve prior documents byte-identical); transpose operands → reader index, forward-closure BFS, classify sinks (output driver ∈ closure / flop `D` ∈ closure; register boundary automatic); `"node:<id>"` addressing; real-in-both module-vs-design; schema `1.25 → 1.26`. delivered end-to-end (`.14a` design + `.14b.1` pure core + `.14b.2` surface): `QUERY_NODE_REACH` + `NodeReach { node, kind, op, width, reaches_outputs, reaches_flops, fanout_targets }` + the thirteenth `node_reach` vec + `module_node_reach`/`design_node_reach` + the `node_reach_with` driver (transpose operands → reader index, forward-closure BFS, classify sinks; register boundary automatic — a flop is not a `Gate` ⇒ no reader edge); registry + `run_analyze` dispatch (`module`/`design_node_reach`) + schema `1.25 → 1.26` + `analyze_schema` enum + book/USER_GUIDE/schema-doc/README/TOOLBOX/KM; `cargo test --lib` 720/0/2; snapshots 6/6; book_examples 3/3; e2e `anvil-mcp` smoke (seed 7 → schema 1.26, 1674 node_reach entries, fanout_targets consistent, a data input reaching 30 flop D-cones but 0 outputs [register boundary]; unknown `node:999999` → -32602) + a live `node_reach == input_reach` cross-check (0 mismatches). **`.15` (`reach_path`) done `2026-06-24`** — a **fourteenth** derived query and the **forward-transitive witness**: for a start node `"node:<id>"`, one representative LONGEST combinational fan-OUT path — the ordered interior-`Gate` chain (each a `PathStep`) from the node forward to a boundary SINK (an output port / `"flop:<id>"`); the **forward complement to `longest_path` `.13`** (the backward fan-in witness) + the **path-witness for `node_reach` `.14`** (the forward fan-out *set* — `node_reach` reports *which* sinks, `reach_path` the longest gate-chain *to* one); single-endpoint `"node:<id>"` (the `node_reach` namespace ⇒ no MCP signature change); `sink` in the `output_support`/`longest_path` target namespace (chains into `longest_path`/`output_support`); reuses `PathStep` + a new `ReachPath { node, depth, path, sink }` payload; provably `reach_path(n).sink.is_some() ⟺ node_reach(n).fanout_targets>0`, `sink ∈ node_reach`'s set, `depth == path.len()`; structural gate-depth NOT timing; schema `1.26 → 1.27` at impl. `.15a` design-detail **done** (no source): a fourteenth `reach_path: Vec<ReachPath>` vec + `ReachPath { node, depth, path: Vec<PathStep>, sink: Option<String> }` (reusing `PathStep`; the forward mirror of `LongestPath` with `target → node`, `leaf:NodeRef → sink:String`; thirteen prior documents byte-identical); transpose operands → reader index, sink-aware `gate_reach_height` memo, greedy forward descent ties→smallest reader id, `pick_sink` = smallest output-port name else `"flop:<id>"`; `"node:<id>"` addressing; real-in-both module-vs-design; schema `1.26 → 1.27`. delivered end-to-end (`.15a` design + `.15b.1` pure core + `.15b.2` surface): `QUERY_REACH_PATH` + `ReachPath { node, depth, path: Vec<PathStep>, sink: Option<String> }` (reusing `PathStep`) + the fourteenth `reach_path` vec + `module_reach_path`/`design_reach_path` + the `reach_path_with` driver + `build_reach_path` (greedy forward descent along the max-`gate_reach_height` reader, ties → smallest reader id) + `gate_reach_height`/`drives_sink`/`pick_sink`; registry + `run_analyze` dispatch (`module`/`design_reach_path`) + schema `1.26 → 1.27` + `analyze_schema` enum + book/USER_GUIDE/schema-doc/README/TOOLBOX/KM; `cargo test --lib` 730/0/2; snapshots 6/6; book_examples 3/3; e2e `anvil-mcp` smoke (seed 7 → schema 1.27, 1674 reach_path entries, `depth == path.len()` for all, node 0 → a 66-gate chain to sink `flop:1`; unknown `node:999999` → -32602) + a live `reach_path.sink ⟺ node_reach.fanout` cross-check (0 mismatches). **No active frontier** — fourteen derived queries delivered (schema `1.27`); further kinds (recursive multi-level instance descent; instance-input reach sinks; a two-endpoint witness-path) remain open-ended breadth; none retired. No new numbered decision (decision `0011` governs; the `.3a`–`.14a` precedent) | [docs/tasks/SEMANTIC-INTROSPECTION-EXPANSION.md](tasks/SEMANTIC-INTROSPECTION-EXPANSION.md) |
| `LOCAL-REFERENCE-CACHE` | `active` | Workflow / tooling — local LRM grounding | `.1` **done `2026-06-16`** — gitignore `.cache/` + land the owner-provided IEEE 1800-2017/2023 SystemVerilog LRM Markdown (untracked) under `.cache/local-references/sv/{2017,2023}/` with a provenance README; recorded as the `reference_sv_lrm_local_cache` auto-memory so future sessions grep the LRM before legality claims. No code / no RTL change. No active frontier (future reference caches land as new `.N` leaves). | [docs/tasks/LOCAL-REFERENCE-CACHE.md](tasks/LOCAL-REFERENCE-CACHE.md) |
| `USABILITY-LANE-OWNERSHIP` | `active` | Workflow — task-tree ownership of the owner-directed usability/capability lanes | `.1` **done `2026-06-17`** — registered the seven owner-directed lanes (below) + decision [`0017`](decisions/0017-api-first-everything-mcp-accessible.md) (API-first: every meaningful feature MCP-accessible/controllable/steerable/queryable; deep semantic introspection first-class) before any implementation, mirroring the `CAPABILITY-LANE-OWNERSHIP` registration precedent. Docs/workflow only / DUT byte-identical. | [docs/tasks/USABILITY-LANE-OWNERSHIP.md](tasks/USABILITY-LANE-OWNERSHIP.md) |
| `BUG-HUNT-ORCHESTRATION` | `done` | Usability — turnkey bug-finder (north star, idea 1) | **`.1` design ADR done `2026-06-17`** (decision [`0018`](decisions/0018-bug-hunt-orchestration-loop.md)): the turnkey MCP-driven fuzz → detect (reject/warning/mismatch) → auto-minimize → reproducer-bundle loop is a **thin orchestrator** — one `hunt::run(&HuntRequest)->HuntReport` in `src/hunt/` that BOTH the `hunt` MCP tool and the first `anvil hunt` CLI subcommand shim over (decision `0017`), composing the existing `downstream::validate`/`minimize` (warning already folds into `ok=false`) + optional **extracted** diff-sim (`diff_sim::run_agreement`) + content-addressed `introspect`, adding no detector and no minimizer of its own. Bundle = a directory `<root>/<run_id>/` (repro.sv/knobs.json/introspection.json/manifest.json/tool-logs/hunt-verdict.json/repro.sh). Sandbox path caller-set, never agent-supplied (decision `0004`); default-off / DUT byte-identical. Pre-split `.2` → `.2a` (diff-sim extract) → `.2b` (`src/hunt/` core) → `.2c` (MCP tool) → `.2d` (CLI) → `.2e` (real-tool gate + docs). **`.2a` done** (`src/diff_sim/mod.rs` now owns the run+compare pipeline + `run_agreement`; `tool_matrix` wraps it; byte-identical). **`.2b` → `.2b.1` (loop core, done; + seed-threading fix) + `.2b.2` (→ `.2b.2a` cross-sim fold, done + `.2b.2b` bundle).** `src/hunt/mod.rs` + `pub mod hunt`: `hunt::run(&HuntRequest) -> HuntReport` composing `downstream::validate`/`minimize` over a deterministic seed sweep; **`.2b.2a` `2026-06-17`** folds optional cross-sim mismatch detection (`HuntRequest.diff_sim` → `diff_sim::run_agreement` on each validate-clean artifact → a `cross_sim_mismatch` finding) and extracts the shared `downstream::generate_dut_artifact` (validate byte-identical). SCHEMA-DERIVED report (decision `0017`); cargo-portable proofs; default-off / DUT byte-identical (snapshots 6/6). **`.2b.2b` `2026-06-17`** adds the reproducer-bundle emitter — `HuntRequest.bundle_root: Option<PathBuf>` (caller-set) makes each finding write a self-contained directory `<bundle_root>/<run_id>/` (`repro.sv`/`knobs.json`/`introspection.json`/`hunt-verdict.json`/`tool-logs`/`repro.sh`) via the shared `generate_dut_artifact` + new `downstream::introspect_dut_artifact`; `HuntFailure.bundle` (`HuntBundle`) carries the path + `anvil://` URIs; prefers the minimized reproducer. **`.2b`/`.2b.2` close** — the `src/hunt/` engine is complete (hunt:: 11/11; snapshots 6/6). **`.2c` `2026-06-17`** adds the controlled MCP `hunt` tool (`run_hunt` shims `hunt::run`; `cache_hunt_failures` populates the artifact cache for each finding's `run_id` via `downstream::introspect_dut_artifact` so `anvil://artifact/<run_id>/{sv,introspection}` resolve; one top-level `hunt` audit record; shared `parse_max_oracle_calls`/`parse_hunt_seeds`/`parse_bool_arg`; `book/src/agent-mcp.md` tool list/table; no introspection schema bump). `bundle_root=None` for MCP (the agent reads artifacts from the cache). The loop is now MCP-invocable + queryable (decision `0017`). mcp:: 66/0; default `anvil` build / DUT byte-identical (snapshots 6/6). **`.2d` `2026-06-17`** adds the `anvil hunt` CLI subcommand (ANVIL's **first** subcommand) — `Cli` gains an optional `#[command(subcommand)] command: Option<Commands>` so the flat `anvil --seed N …` path stays byte-identical (`command==None`); `anvil hunt …` shims `hunt::run` via `run_hunt_command`/`build_hunt_request`, `--out` ⇒ the on-disk reproducer bundle (the MCP path stays cache-only); `AcceptanceTool` gains `clap::ValueEnum`; USER_GUIDE/README/book synced; 5 anvil-bin proofs (anvil-bin 12/0) + a real-tool smoke (`--seeds 3 --tools verilator` ⇒ `n_failures=0`); snapshots 6/6 + book_examples. **`.2e` `2026-06-17` CLOSES the tree** — `tests/hunt_e2e.rs` (2 `#[ignore]` tool-gated proofs vs real Verilator: a clean `anvil hunt` sweep + the byte-identical reproducer recipe), the book "bug-hunting loop end to end" rewrite (turnkey `hunt` CLI+MCP), the `bug-hunt-cli` KM card (47 facts), ROADMAP lane 1 DONE. **Tree complete:** the `hunt::run` engine + both invocation surfaces (`anvil hunt` CLI + MCP `hunt` tool) + the reproducer bundle + the real-tool gate all delivered; default `anvil` build / DUT byte-identical. | [docs/tasks/BUG-HUNT-ORCHESTRATION.md](tasks/BUG-HUNT-ORCHESTRATION.md) |
| `ACCEPTANCE-DIVERGENCE-HUNTING` | `done` | Usability — acceptance-divergence bug-finder (north star, idea 2) | **DONE `2026-06-17` — tree closed (all `.1`..`.2f` done; `.2c` + `.2` + root closed).** **`.1` design ADR done `2026-06-17`** (decision [`0019`](decisions/0019-acceptance-divergence-hunting.md)): acceptance divergence is a **first-class, default-off, SCHEMA-DERIVED detector** with **one shared home** — `src/divergence/`'s `divergence::run(seed,cfg,&DivergenceOptions)->DivergenceReport` composing the existing `downstream::run_verilator`/`run_yosys`/`run_iverilog_compile` primitives (all enabled tools/modes to completion, no fold) + a shared `downstream::tool_verdict` classifier extracted from `hunt::run` (a per-tool **accept/warn/reject** projection of one `ToolInvocation` — no second classifier). A divergence = "not all labelled-tool verdicts equal" (`accept_reject` \| `accept_warn` \| `warn_reject` \| `version_mismatch`), so `--yosys-mode both` can itself diverge. **Reused by three surfaces, one detector** (resolves the "hunt vs matrix" open question to **both**): a `hunt::run` axis (`HuntRequest.divergence` → an `acceptance_divergence` finding), a `tool_matrix` column (`ModuleReport`/`DesignReport.divergence` + the opportunistic `saw_acceptance_divergence` fact + the `classify_diff_sim_axis` subset reuse), and a controlled MCP `divergence` tool (cache + audit) + a CLI shim (decision `0017`). Complements `--diff-sim` (cross-*sim* trace agreement) with the orthogonal *acceptance* axis. Reproducers reuse `write_bundle` / the matrix `.sv`+log retention (no new format); `saw_acceptance_divergence` is opportunistic (never a required gate — all-agree is the valid-by-construction steady state); tool-version-vs-version is the later `.2e` (caller supplies binaries/labels, kind stays allow-listed). Sandbox caller-set (decision `0004`); default-off / DUT byte-identical. Pre-split `.2` → `.2a` (classifier extract) → `.2b` (`src/divergence/` core) → `.2c` (hunt fold + matrix column) → `.2d` (MCP tool + CLI) → `.2e` (version axis) → `.2f` (real-tool gate + docs). **`.2a` done `2026-06-17`** — extracted the shared `pub fn downstream::tool_verdict(&ToolInvocation) -> ToolVerdict` (`ToolVerdict{Accept,Warn,Reject}`, snake_case serde) from `hunt::run`'s inline detection so the divergence detector and the hunt share **one** accept/warn/reject classifier (no second classifier); `hunt::classify_detection` derives from it byte-identically; +1 `downstream` proof; `cargo test --lib` 522/0, snapshots 6/6 byte-identical, clippy/fmt green; default-off / DUT byte-identical. **`.2b` done `2026-06-17`** — the `src/divergence/` library core (`pub mod divergence`): `DivergenceOptions{validate:ValidateOptions}` (the `MinimizeOptions` wrap), `ToolDecision`/`Divergence`/`DivergenceReport`, and `divergence::run` that **reuses** the one `downstream::validate` orchestration + the shared `tool_verdict` (no forked sandbox loop / no second classifier) and `classify_divergences` (one `Divergence` per present pair-class `accept_reject`/`accept_warn`/`warn_reject`, deterministic; Yosys `both` ⇒ two labels so without-abc-vs-with-abc itself diverges); 7 cargo-portable proofs; `cargo test --lib` 529/0, snapshots 6/6 byte-identical, clippy/fmt green; default-off / DUT byte-identical. **`.2c` pre-split → `.2c.1` (hunt fold) + `.2c.2` (matrix column); `.2c.1` done `2026-06-17`** — extracted `divergence::classify_report(&ValidateReport)` (the pure projection half of `run`; byte-identical) so `hunt::run` classifies the tools `validate` already ran without re-validating; `HuntRequest.divergence` + `HuntFailure.divergence`; a finding where one tool accepted what another rejected/warned is refined to `detection="acceptance_divergence"` + the report attached (not minimized — the `cross_sim_mismatch` rationale); MCP/CLI set `divergence:false` (arg-wiring=`.2d`); +2 proofs; `cargo test --lib` 531/0, snapshots 6/6 + book_examples 3/3, clippy/fmt green; default-off / DUT byte-identical. **`.2c.2` done `2026-06-17`** — the `tool_matrix --divergence` column (the second surface; closes `.2c`): `ModuleReport`/`DesignReport.divergence: Option<DivergenceReport>` populated by `unit_divergence`, which assembles the verilator/yosys/iverilog `ToolInvocation`s the matrix **already ran** into a `ValidateReport` and classifies it through the **same** `divergence::classify_report` (no second classifier) — a pure projection, so **no** extra tool spawn and (unlike `--diff-sim`) **no** tool-clean precondition; gated by a `.divergence-subset` sentinel reusing `select_diff_sim_subset`/`classify_diff_sim_axis` (membership factored into `scenario_in_named_subset`); the **opportunistic** `saw_acceptance_divergence` fact is **never** a coverage gate (`compute_coverage_gaps` untouched — all-agree is the steady state); +4 proofs; `cargo test --bin tool_matrix` 75/0, full lib 531/0, snapshots 6/6 + book_examples 3/3, clippy/fmt green; real-tool smoke `anvil-divergence-col-smoke` 17/0 (`divergence_enabled`, subset 2 axes, `diverged=false`, fact `false`); default-off / DUT byte-identical. **`.2d` done `2026-06-17`** — the third surface (decision `0017` API-completeness): a controlled MCP `divergence` tool (`mcp::run_divergence`, a single-`(seed,cfg)` shim over `divergence::run` with the same `tools`/`yosys_mode` input as `validate`, caching the divergent `run_id` so `anvil://artifact/<run_id>/{sv,introspection}` resolve + a `divergence` audit record) added to `tools_list`/dispatch; the `hunt` tool's `divergence` axis arg (flipped the `.2c.1` placeholder); the `anvil hunt --divergence` CLI shim. `book/src/agent-mcp.md` tool list/table updated; no introspection schema bump (no new resource type). +4 MCP proofs + CLI parse proofs; `cargo test --lib mcp::tests` 55/0, full lib 535/0, `cargo test --bin anvil` 12/0, snapshots 6/6 + book_examples 3/3, `mdbook build book` clean, clippy/fmt green; real-tool CLI sanity (`anvil hunt --divergence`) `n_clean=4 n_failures=0`; default-off / DUT byte-identical. **`.2e` done `2026-06-17`** — the tool-version-vs-version axis as a library surface, composing the *one* hardened orchestration (no forked invocation set): `downstream::{ToolInvocation.version (serde-absent on default paths ⇒ byte-identical reports/checkpoints), tool_version (best-effort --version, axis-only), ToolSpec {kind allow-listed, binary caller-supplied, label}, the extracted shared DutSandbox/prepare_dut_sandbox (validate refactored onto it, byte-identical), validate_tool_specs (reuses prepare_dut_sandbox + the vetted run_* primitives + MemGuard; one relabeled+version-stamped invocation per spec; single Yosys mode for the axis)}` + `divergence::{DivergenceOptions.tool_specs (empty ⇒ same-version path; non-empty ⇒ version axis), ToolDecision.version, classify_version_mismatch (a version_mismatch over the SAME tool_verdict — a distinct relation, not a second classifier), assemble_report factor, run dispatch}`; the MCP `divergence` literal gets `tool_specs: vec![]` (axis library-only at `.2e`; a caller-supplied-binary MCP/CLI surface is a distinct trust boundary, decided at `.2f`). +8 proofs; `cargo test --lib` 543/0 (535→543) + `tool_matrix` 75/0 + `anvil` 12/0; snapshots 6/6 byte-identical; clippy/fmt green; real-tool `validate`-refactor regression `anvil hunt --divergence` `n_clean=4 n_failures=0`; default-off / DUT byte-identical. **`.2f` done `2026-06-17` — CLOSES the tree** — the real-tool e2e gate `tests/divergence_e2e.rs` (structured like `tests/hunt_e2e.rs`): a **portable** `#[test]` asserting an injected accept/reject `ValidateReport` ⇒ one `accept_reject` divergence + a queryable serde round-trip, all via the **public** API (`anvil::downstream::{ValidateReport,ToolInvocation}` + `anvil::divergence::classify_report`); plus two `#[ignore]` tool-gated proofs — an all-agree real-tool 3-seed sweep ⇒ `diverged=false` across Verilator 5.046 + both Yosys modes (3 labelled verdicts, distinct `run_id`s, JSON round-trip) and `anvil hunt --divergence` inert on a clean sweep (no `acceptance_divergence` finding). **Version-axis surfacing decision:** `DivergenceOptions.tool_specs` stays **library-only** — *not* exposed over MCP/CLI, because an allow-listed *kind* + a caller-supplied *binary path* is a strictly larger trust surface than the fixed-binary controlled tools (decision `0004`); safe exposure needs an operator-configured version-binary registry (never an agent-supplied path), recorded as **future breadth** in decision `0019` + DEVELOPMENT_NOTES; nothing retired. Docs closeout: `book/src/synthesizability.md` "Acceptance divergence across tools" + `book/src/agent-mcp.md` version-axis note + USER_GUIDE `--divergence` flag + README hunt bullet + MCP controlled-tools list + `docs/knowledge/acceptance-divergence.md` KM card (48→49) + ROADMAP lane 2 delivered. `cargo test --test divergence_e2e` portable 1/0 + `--ignored` 2/0; snapshots 6/6 byte-identical; book_examples 3/3; mdbook clean; clippy/fmt green; check_memory_architecture + KM gen/check OK. Default-off / DUT byte-identical. **Tree CLOSED.** | [docs/tasks/ACCEPTANCE-DIVERGENCE-HUNTING.md](tasks/ACCEPTANCE-DIVERGENCE-HUNTING.md) |
| `DOWNSTREAM-ADAPTER-EXPANSION` | `active` | Usability / breadth — more downstream tool reach (north star, idea 3) | **Registered `2026-06-17`** (owner-directed). A generic, API-selectable downstream-adapter interface so new tools (slang / sv2v / Surelog-UHDM / commercial wrappers) plug in as acceptance columns with little new core code, reusing the hardened `src/downstream` allow-list + the `tool_matrix` column model. Binds decision `0017`. **`.1` design ADR done `2026-06-17`** (decision [`0020`](decisions/0020-downstream-adapter-interface.md)): a **closed, compile-time `Adapter` registry** over the one `run_tool` runner + the one `tool_verdict` classifier (no second runner/classifier; the trait carries only argv + the warning predicate + an optional SCHEMA-DERIVED `extract_facts` hook); built-ins re-expressed byte-identically (`AcceptanceTool` not retired; the `verilator`/`yosys-<mode>`/`iverilog-compile` labels are a hard byte-identical constraint). Because the verdict is unchanged, every added column becomes a new comparable verdict in `divergence::run` + a new selectable tool in `hunt`/`validate` **for free**. First adapter = **`sv2v`** (`.2b`, minimal accept/reject transpile column); second = **`slang`** (`.2c`, the JSON-AST fact hook). Live-toolchain probe: `slang`/`sv2v`/`surelog` all **absent** ⇒ first cuts ship structural + friendly absent-tool no-op + an `#[ignore]` real-tool gate. API-completeness (`0017`): adapters selectable via the existing `tools` arg + queryable via the existing reports + a new SCHEMA-DERIVED **adapter-catalog** projection; CLI a shim; allow-list/sandbox/RAM-guard/audit (`0004`) + the `0019.2f` caller-supplied-binary library-only boundary preserved. `.2` pre-split → `.2a` (registry refactor + catalog) / `.2b` (sv2v) / `.2c` (slang); `.2d+` (surelog/UHDM, commercial-wrapper) future. **`.2a` split + `.2a.1` done `2026-06-17`** — `src/downstream/mod.rs` gains the closed `Adapter` registry core: `pub trait Adapter: Sync { id, binary, run(&AdapterRunCx)->Result<Vec<ToolInvocation>> }` + `AdapterRunCx`/`AdapterTarget{Module,Design}` + three built-in unit-struct adapters delegating **verbatim** to the existing `run_*` primitives + `static ADAPTER_REGISTRY`/`pub fn adapters()` + `AcceptanceTool::adapter()`, with `validate` routed through `tool.adapter().run(&cx)` (byte-identical: same labels/argv/order, Yosys `Both` still 2 rows, mem-guard once per tool). `.2a` split into `.2a.1` (downstream core) / `.2a.2` (adapter-catalog discoverability) / `.2a.3` (route `validate_tool_specs` + the `tool_matrix` columns through the registry). **`.2a.2` done `2026-06-17`** — the new MCP resource `anvil://catalog/adapters`: `downstream` gains a defaulted `Adapter::supports_facts()` + `AdapterInfo{id,binary,present,supports_facts}` + `pub fn adapter_catalog()` (a SCHEMA-DERIVED projection of `adapters()` with a live `tool_version()` PATH probe for `present`), advertised in `resources_list` + served by `resources_read`, so an agent discovers which downstream tools exist + which are installed over the API alone (decision `0017`); book synced (`api-resources-prompts.md` + `agent-mcp.md`); no introspection schema bump. Gate green: lib 546/0 (+1 catalog proof), snapshots 6/6 byte-identical, clippy/fmt, mdbook, book_examples 3/3. **`.2a.3` done `2026-06-18` (completes `.2a`)** — routed the last two downstream callers through the registry: `downstream::run_tool_spec` (the `validate_tool_specs` version axis) dispatches via `spec.kind.adapter().run(&cx)` (the Yosys `Both`→single collapse + the relabel/version-stamp preserved), and `tool_matrix`'s `run_module_tools`/`run_design_tools` dispatch each fixed `verilator`/`yosys`/`iverilog_compile` column through `AcceptanceTool::*.adapter().run(&cx)` (one `AdapterTarget` + a per-column `run_column` closure; skip flags / `verilator_only` no-op / `--language` selector / Yosys-mode row count preserved); the six now-unused `run_*` primitive imports dropped from `tool_matrix.rs` (primitives stay live behind the adapters). Byte-identical (fixed columns + banked reports + `--resume` + snapshots 6/6 untouched), so adding `sv2v` (`.2b`) is now a registry entry + a column field + a routing line. Gate green: lib 547/0 (+1 routing proof), snapshots 6/6, tool_matrix 75/0, anvil+pipeline+divergence_e2e exit 0, clippy/fmt, mdbook, check_memory_architecture + KM. **`.2b` split + `.2b.1` done `2026-06-18`** — the first new adapter, `sv2v`: `src/downstream/mod.rs` gains `AcceptanceTool::Sv2v` (`from_name`/`binary` `"sv2v"`/`adapter()`) + `run_sv2v` (`sv2v <file>`) + `run_sv2v_design` (`sv2v --top=<top> <files…>`) transpile accept/reject primitives (no fact hook) + an `Sv2vAdapter` (`supports_facts=false`) + a 4th `ADAPTER_REGISTRY` entry + a `first_tool_warning` `"sv2v"` arm; `src/mcp/mod.rs` adds `"sv2v"` to the four `tools` enum schemas + the `validate` description + the `parse_validate_tools` allow-list, so `sv2v` is selectable via the `tools` arg and discoverable in `adapter_catalog()` (`anvil://catalog/adapters` now 4 entries, `present=false` until installed — the friendly no-op); book synced (`agent-mcp.md`/`api-tools.md`/`api-resources-prompts.md`). Additive / DUT byte-identical (snapshots 6/6, no generator change). `.2b` split into `.2b.1` (downstream + MCP) / `.2b.2` (the byte-identical-sensitive `tool_matrix` column + the `#[ignore]` real-tool gate + book/USER_GUIDE/README/KM). Gate green: lib 549/0 (+2 net proofs), snapshots 6/6, clippy/fmt, mdbook, KM. **`.2b.2` done `2026-06-18` (closes `.2b`)** — the `tool_matrix --sv2v` transpile-acceptance column (byte-identical-sensitive), mirroring `--iverilog-compile`: `Cli.sv2v`/`sv2v_bin` + `ModuleReport`/`DesignReport.sv2v` (serde `skip_serializing_if`) dispatched via `AcceptanceTool::Sv2v.adapter().run(&cx)` + checkpoint field + `--resume` guard + the `ToolSummary.sv2v_passed/failed` tally (in `any_failed`) + console line + `MatrixReport.sv2v_enabled` + `unit_divergence` inclusion, all gated on a `tool_version` presence probe so a requested-but-absent `sv2v` records no column and never bails (the friendly no-op, verified by a real `--sv2v` smoke: exit 0, `sv2v 0/0`, 0 invocations); `tests/sv2v_e2e.rs` `#[ignore]` real-tool gate; README/USER_GUIDE/`synthesizability.md` + a `docs/knowledge/sv2v-adapter.md` KM card (52 facts). Default-off ⇒ banked reports + `--resume` + snapshots 6/6 byte-identical. Gate green: tool_matrix 76/0 (+1 CLI proof), snapshots 6/6, sv2v_e2e portable 1/0, lib 549/0, clippy/fmt, mdbook, KM. **`.2b` (sv2v) complete.** **`.2c` split + `.2c.1` done `2026-06-21`** — the second new adapter, `slang`, and the `Adapter` trait's **first `extract_facts` fact hook**: `src/downstream/mod.rs` gains a defaulted `Adapter::extract_facts(&AdapterRunCx,&ToolInvocation)->Option<AdapterFacts>` (built-ins + `sv2v` keep the `None` default ⇒ byte-identical), the `AdapterFacts`/`AdapterPortFact`/`AdapterInstanceFact` projections (port `ty` serde-renamed `type`), `AdapterTarget::stem()`, `AcceptanceTool::Slang` + `run_slang`/`run_slang_design` (`slang <sv> -q --ast-json <stem>.slang.json`; `--top <top>` design) + a `SlangAdapter` (`supports_facts=>true`; `extract_facts` reads the side file → the pure `parse_slang_ast_facts` over slang's **verified** `--ast-json` schema: top/ports/instances) + a 5th `ADAPTER_REGISTRY` entry + a `first_tool_warning` `"slang"` arm; `src/mcp/mod.rs` adds `"slang"` to the four `tools` enums + the `parse_validate_tools` allow-list, so `slang` is selectable + discoverable (`adapter_catalog()` now 5 entries, `slang` the first `supports_facts=true`, `present=false` locally — the friendly no-op). slang absent on this host ⇒ schema + argv verified against slang's published docs; the portable proof runs the parser against a faithful synthetic fixture, the `#[ignore]` real-tool gate is `.2c.2`. Book API pages + README/USER_GUIDE allow-list synced. Additive / DUT byte-identical (snapshots 6/6, no generator change). `.2c` split into `.2c.1` (downstream + the fact hook + MCP) / `.2c.2` (the `tool_matrix` slang surface). Gate green: lib 553/0 (+4 net), downstream:: 30/0 + mcp:: 73/0, snapshots 6/6, clippy/fmt, mdbook, mem-arch + KM. **`.2c.2` split + `.2c.2a` done `2026-06-21`** — the `tool_matrix --slang` elaboration-acceptance column (a faithful `--sv2v` mirror): `Cli.slang`/`slang_bin`, `ModuleReport`/`DesignReport.slang` (serde `skip_serializing_if`) via `AcceptanceTool::Slang.adapter().run(&cx)` (the `ModuleToolColumns` tuple 4→5), checkpoint + `--resume` guard, `ToolSummary.slang_passed/failed` + console line + `MatrixReport.slang_enabled` + `unit_divergence` inclusion — gated on a `tool_version` presence probe for the friendly no-op (real `--slang` smoke: exit 0, `slang 0/0`, 0 invocations); `tests/slang_e2e.rs` portable proof + `#[ignore]` real-tool accept gate; README/USER_GUIDE/`synthesizability.md` + a `docs/knowledge/slang-adapter.md` KM card (52→53). `.2c.2` split into `.2c.2a` (the byte-identical-sensitive column) / `.2c.2b` (the novel live `extract_facts` fact-surfacing). **`.2c.2b` done `2026-06-21` — closes `.2c.2`, `.2c`, and `.2` (the implementation)** — the slang column's `--ast-json` projected via `extract_facts` into `ModuleReport`/`DesignReport.slang_facts: Option<AdapterFacts>` (serde skip ⇒ byte-identical when off; `ModuleToolColumns` 5→6; the slang column builds its `AdapterRunCx` explicitly so the cx feeds the hook); the `tests/slang_e2e.rs` `#[ignore]` gate now asserts real-slang facts (top + ≥1 port). Scope refinement: the planned `saw_slang_facts` coverage fact was DROPPED (a plain bool would change every report's `CoverageSummary`; the sv2v column added none; the requirement is met by the off-the-wire report field) — replaced by a serde-skip proof. Gate green: tool_matrix 78/0, slang_e2e portable 1/0, snapshots 6/6, clippy/fmt, mdbook, KM (53 facts); no-op smoke ⇒ 0 `slang`/`slang_facts`/`saw_slang_facts` keys. **The `.2` implementation is complete; the tree stays `active` with NO current frontier** — `surelog`/UHDM + a generic commercial-wrapper adapter are open-ended `.2d+` future picks (nothing retired; the `STRUCTURED-EMISSION-EXPANSION` precedent). Default-off / DUT byte-identical. | [docs/tasks/DOWNSTREAM-ADAPTER-EXPANSION.md](tasks/DOWNSTREAM-ADAPTER-EXPANSION.md) |
| `KNOB-ERGONOMICS-AND-PRESETS` | `active` | Usability — knob ergonomics + presets + full API steerability (north star, idea 4) | **Registered `2026-06-17`** (owner-directed). Promote high-value config-file-only knobs to CLI flags + a curated `--profile` preset registry (arithmetic-heavy / deep-hierarchy / structured-emission-max / sv2023-upopts), AND (per decision `0017`) guarantee every knob is MCP-steerable with the knob catalog + preset registry API-queryable. `.1` **done `2026-06-18`** (decision [`0021`](decisions/0021-knob-ergonomics-presets-and-queryable-catalog.md)): audit (86 `Config` fields → 67 CLI-reachable, 19 config-file-only), promote 16/19 to `Option` CLI flags (keep 3 config-only), 4 declarative presets, `default→config→profile→explicit→seed` resolution (byte-stable), SCHEMA-DERIVED queryable knob catalog + `anvil://catalog/presets`. `.2a` **done `2026-06-18`** (impl design-detail; `.2b` re-split into `.2b.1`/`.2b.2`/`.2b.3`). `.2b.1` **done `2026-06-18`** (first **code** slice: `presets()` + shared `resolve_config` + `--profile` + 16 promoted `Option` CLI flags + `ConfigError::UnknownProfile`; default DUT byte-identical, full `cargo test` green). `.2b.3` **done `2026-06-18`** (user-facing docs for that surface — README/USER_GUIDE/book knobs+structured-emission+sequential + KM card; mdbook clean; book back in sync). `.2b.2a` **done `2026-06-18`** (`anvil://catalog/presets` + the MCP `profile` input). `.2b.2b` **done `2026-06-18`** (the SCHEMA-DERIVED per-knob `knob_catalog()` + `anvil://catalog/knob-schema`; completeness-gated; raw `anvil://catalog/knobs` kept; full `cargo test` green). **Lane API complete — no current frontier** (CLI-flag promotion + `--profile` presets + shared resolver + user-facing docs + MCP preset API + per-knob catalog all delivered; tree stays `active`, open-ended; further presets/knobs/metadata are optional `.N` picks). | [docs/tasks/KNOB-ERGONOMICS-AND-PRESETS.md](tasks/KNOB-ERGONOMICS-AND-PRESETS.md) |
| `CI-PACKAGING-DISTRIBUTION` | `active` | Usability — drop-in CI packaging (north star, idea 5) | **Registered `2026-06-17`** (owner-directed). Prebuilt per-platform release binaries + a drop-in GitHub Action so a downstream-tool maintainer can continuously fuzz their parser/elaborator against ANVIL, driven through the same API surface (decision `0017`). Soft dep on `BUG-HUNT-ORCHESTRATION` (the engine it wraps — **already shipped**). `.1` **done `2026-06-18`** (decision [`0022`](decisions/0022-ci-packaging-prebuilt-binaries-and-github-action.md)): a hand-rolled `v*`-tag release matrix (`anvil`+`anvil-mcp` tarballs + checksums; `cargo-dist` rejected) + a composite GitHub Action (not container) wrapping `anvil hunt` with inputs mapped 1:1 onto the CLI/MCP controls; user-installed tools (no vendoring); version-pin + reproducible. **`.2a` done `2026-06-21`**: `.github/workflows/release.yml` — the hand-rolled `v*`-tag 5-target build matrix (`x86_64`/`aarch64` linux-gnu, `x86_64`/`aarch64` darwin, `x86_64` windows-msvc) → `--release --locked` `anvil`+`anvil-mcp` archives (`.tar.gz`/`.zip`) + an aggregated `SHA256SUMS`, published via the runner-builtin `gh` CLI (no third-party release dep; `cargo-dist` rejected). **`.2b` done `2026-06-21`**: the drop-in composite Action — root `action.yml` + `scripts/anvil_hunt_action.sh` entrypoint + a presence-gated self-test workflow, wrapping `anvil hunt` (decision `0017`/`0018`) with hunt-flag inputs mapped 1:1 + Action-level plumbing (`anvil-version`/`anvil-bin`/`artifact-name`/`fail-on-finding`); proven by a real local entrypoint smoke (findings=0 on clean DUT output). **`.2c` done `2026-06-21`** (closes `.2`): README/USER_GUIDE/`book/src/recipes.md` "Use ANVIL in your CI" + the `ci-github-action` KM card (56→57) + the decision-`0022` "implemented" status note. **`.2` implementation complete — tree stays `active` with no current frontier** (more targets / a Marketplace listing / an MCP-driven variant are optional `.N`). CI-infra / DUT byte-identical. | [docs/tasks/CI-PACKAGING-DISTRIBUTION.md](tasks/CI-PACKAGING-DISTRIBUTION.md) |
| `COVERAGE-STEERED-GENERATION` | `done` | Usability / effectiveness — coverage-steered generation (north star, idea 6) | **CLOSED `2026-06-22`** — every acceptance criterion met (`.1`+`.2a`+`.2b`+`.2c.1`+`.2c.2`). **Registered `2026-06-17`** (owner-directed). Bias generation toward under-exercised constructs at **construction time** (the `roll_knob` decision sites) — **never** generate-then-filter (`feedback_rules_first_generation`) — with an API-settable coverage target + an API-queryable achieved-coverage readout (decision `0017`); byte-stable per `(seed, knobs, steering-config)`. **`.1` done `2026-06-21`** (decision [`0023`](decisions/0023-coverage-steered-generation.md)): the steering primitive = a deterministic per-category probability-prior **multiplier** on `prob` at the `roll_knob` site (one `gen_bool` draw preserved) — rules-first (a prior, not a filter) + byte-stable per `(seed, knobs, steering-config)`, byte-identical when unset; target = a `SteeringConfig`; readout = SCHEMA-DERIVED from `knob_roll_attempts`/`fires` + histograms; feedback = an outer measure→derive→re-steer loop; API per decision `0017`. **`.2a` done `2026-06-21`** (first **code** slice): `SteeringConfig` (`per_knob`/`per_category` emphasis weights) + `KnobId::category()` + the `roll_knob` prior multiplier (`effective_prob = clamp01(prob * weight)`, one `gen_bool` draw preserved) + `ConfigError::SteeringWeight`; three proofs green (byte-identical default via snapshots 6/6; measurable distribution shift; no-filter architectural) + full cargo gate; rules-first / DUT byte-identical when unset. **`.2b` done `2026-06-21`** (second **code** slice, the READ half): `src/introspect/coverage.rs` (`CoverageReadout` = per-knob + per-category empirical fire rates + the gate/operand/depth histograms; `module_coverage`/`design_coverage`) + `KnobId::all()`/`category_of_name()`, surfaced as the `IntrospectionPayload::coverage_readout` section (schema `1.11→1.12`) **and** the standalone `CoverageDocument` returned by the new pure MCP `coverage` tool — one projection feeding both (no drift); `fire_rate` is integer parts-per-million for byte-stable determinism (a raw f64 division diverged by 1 ULP across evaluation contexts; caught by the pre-existing exact-equality round-trip test, fixed without weakening it); SCHEMA-DERIVED / DUT `.sv` byte-identical; full cargo gate green. **`.2c` pre-split → `.2c.1` (code) + `.2c.2` (docs/close); `.2c.1` done `2026-06-21`** (third **code** slice, the steering-OUT half): the pure `derive_steering_from_coverage(&CoverageReadout, &DeriveParams) -> SteeringConfig` helper (decision `0023` §4 — per-category `weight = clamp(target_share / max(observed, eps), 0, max_weight)`, milli-quantized for byte-stability, rules-first: no generation/filter) + `SteeringConfig::set_weight` (one classifier reusing the `KnobId` taxonomy) + `pub validate` + `Overrides.steer` + the repeatable `--steer <key>=<weight>` CLI shim (preset-then-explicit in `resolve_config`); 9 proofs + CLI smoke (steered≠unsteered, neutral=unsteered, bad-key error); unsteered default DUT byte-identical; full cargo gate green. **`.2c.2` done `2026-06-22`** (docs/close): the steering-lane book section (`algorithm.md` mechanism + `agent-mcp.md` measure→derive→re-steer loop + the `coverage` tool + the `coverage_readout` in `--introspect`) + the schema-`1.11→1.12` book-example refresh across `api-tools`/`api-introspection`/`api-reference` + `knobs.md` cross-ref + USER_GUIDE (`--steer` row + recipe) + decision `0023` enriched as the KM card (4 shipped-surface answer keys, status → delivered); mdbook clean; `book_examples` 3/3 (runnable `--steer` example). Docs-only / DUT byte-identical. **`.2c` + `.2` + the tree CLOSED** — optional open-ended follow-ups (in-generator adaptive schedule; routing raw `gen_bool` sites through `roll_knob`) are future `.N` leaves that do not reopen the closed scope. **REOPENED `2026-07-30` with a new `.3` node** (the `.1`/`.2` scope stays closed and is not revisited — the Phase-4 closure pattern). **`.3a` done `2026-07-30`** (decision [`0034`](decisions/0034-one-steering-aware-knob-roll-primitive.md)): a **measured silent no-op** in the shipped steering surface — `src/gen/hierarchy.rs:883-938` defines **seven roll primitives of its own** that record the same `m.knob_rolls` telemetry as `roll_knob` while omitting `SteeringConfig::effective_prob`, so **6 of the 22 `KnobId`s never reach the prior** and the whole documented `hierarchy` steering category biases nothing (`HierarchyParentFlopProb` is *half*-steered: its two `cfg.flop_prob`-swap cone scopes obey the weight, its dedicated helper does not). Measured at `ff506e1`: a 9× per-knob **or** per-category steer leaves the recorded fire counts **bit-identical** (`hierarchy_child_input_cone_prob` stays `0/5` where `clamp01(0.3 × 9) = 1.0` demands `5/5`); `--steer hierarchy=8.0` vs `=0.01` — an 800× spread — emits **byte-identical** SV; positive control `--steer state=8.0` does change output. Silent by every surface (`--steer` accepts, validates, stores and echoes the weight). Root cause: the `.1` survey enumerated roll sites by the **shape** it knew (`roll_knob(` call sites) instead of from the **authoritative set** (`knob_rolls.record(` — 8 sites in 2 files), decision `0033` rule (2) recurring; the helpers predate the steering core by two months. **`.3b` done `2026-07-30`** (the fix): new `src/ir/knob_roll.rs` holds the one `roll_knob_into(&mut KnobRollCounters, &SteeringConfig, rng, knob, prob)` primitive with `KnobRollCounters::record` **private to it**, so a second primitive is a **compile error** (`error[E0624]`, repair rung **R2**; a registered doctrine was rejected — the type system enforces it for free); `Generator::roll_knob` is the crate-wide shim, `cone::roll_knob` an alias (37 call sites untouched), and the seven `roll_hierarchy_*` helpers are **deleted** (seven one-line `KnobId` wrappers are themselves a shadow) in favour of seven inline `g.roll_knob(m, KnobId::…, prob)` calls. `--steer hierarchy` now works: `child_input_cone` goes `0/5 → 5/5`, exactly `clamp01(0.3 × 9)`. Unsteered byte-identical (3 pre-fix hashes + snapshots 6/6); both guards negative-controlled **both** ways; full cargo gate green. `.3b` also **corrected `.3a`'s root cause** via a dated Correction section in decision `0034`: the fork was **visibility** (a module-private `fn` in `gen::cone`, invisible to its sibling), not a borrow conflict — and the honest reachability split was **15 fully steered / 6 not at all / 1 partial**, not 16/6. **`.3c` done `2026-07-30`** (docs/close): `book/src/algorithm.md` restated as the one-primitive invariant plus a new **"Why the guard is a compile error"** subsection (guard the *effect*, not the wrapper; a private helper carrying a cross-cutting invariant is an invitation to fork it); `book/src/knobs.md` gains a **runnable** `0/5 → 5/5` example and the "identical rather than close" diagnostic; `USER_GUIDE.md` states which knobs a steer reaches and flags that `--steer hierarchy` was inert before `2026-07-30`; `README.md` corrected net-neutral; decision `0034` enriched as the KM card (status → delivered, `reverify` added). mdbook clean; `book_examples` 3/3 (**65** runnable blocks). **`.3` CLOSED.** **`.4a` done `2026-07-30`** (decision [`0035`](decisions/0035-steering-width-motif-and-emission-knobs.md)): steering's *width* — measured, `Config` carries **41** validated probability knobs, **22** have a `KnobId`, and of the remaining 19 **16 are in scope** (7 module-level **motif** rolls + 9 per-gate **emit-projection** rolls) while **3 are excluded by kind** (`operand_duplication_rate`/`mux_arm_duplication_rate` are dedup thresholds with no roll to prior-multiply; `library_prob` has no reader in `src/`). Their symptom is *loud* (`unknown steer key`), so this is a **feature gap, not a defect** — the real cost is that the outer measure→derive→re-steer loop is blind to the whole structured-emission family. Decided: two **new** categories `motifs` + `emission` (the existing six keep exact membership, so every working steering config keeps its meaning), a `&SteeringConfig` parameter on the nine `annotate_*` passes, the `> 0.0` guard pinned **load-bearing for reproducibility** (with its honest consequence: a default-off knob records `attempts = 0`, so the readout cannot tell *off* from *never reached*), and **no schema bump** with the rule stated (bump for a new payload key or query kind, not for more map entries). Also flagged, not decided: a hard-coded `gen_bool(0.5)` at `src/gen/module.rs:143` with no knob at all, and `KnobId::all()` reaching **38** hand-maintained entries needing an R2 guard. **Recon on `.4b.1` then corrected `.4a` twice (dated Correction on decision `0035`):** (1) **3 of the 7 motif rolls have no `Module` to record into** — `width_parameterization`/`memory`/`fsm` roll at `src/gen/module.rs:385/401/414` and each *returns a differently-built module* at `390/404/417`, while that function's first `Module` binding is at line `432`; the roll **chooses which module to construct**, so `.4b.1` needs a `Generator::pending_knob_rolls` buffer drained inside `ir::knob_roll`, not mechanical routing; (2) **the `.3b` R2 guard is incomplete** — privatising `record` left `attempts`/`fires` `pub`, so a prior-skipping primitive that writes the maps directly **compiles clean** (measured, `cargo check` exit `0`). **`.5` done `2026-07-30`**: the counter fields are private behind read-only `attempts()`/`fires()` accessors, so the direct-field bypass — which compiled **clean** before `.5` — now fails with `error[E0616]`, and with `.3b`'s `E0624` on `record` **both** syntactic writes to the protected state are compile errors (negative-controlled both ways; no generation path touched ⇒ byte-identical). **`.4b.1` done `2026-07-30`**: the 7 module-level motif knobs are steerable **and** measurable — 7 new `KnobId`s + the `motifs` category, with a `Generator::pending_knob_rolls` buffer for the 3 that roll *before any `Module` exists*, drained at **one** point outside every branch (the first cut drained only inside the firing branches, which made every reported `fire_rate` a constant `1.000`; caught by printing raw counts, fixed structurally, negative-controlled). `--memory-prob 0.5 --introspect` goes from an **empty** readout — the symptom that opened the session — to `memory_prob 1/1`; a `motifs` steer moves the achieved rate `0.267 → 0.833 / 0.033`. **17 configurations byte-identical** vs a `HEAD` binary built in an isolated worktree. `KnobId::all()`'s guard (exhaustive `index()` ⇒ `E0004`, plus an ordering test) ships with its **residual gap documented**: a *tail* truncation is not caught, and the length assertion drafted to catch it **could not fail**, so it was deleted rather than shipped — the R1 fix (macro-derive the list) is registered as `.6`. **`.4b.2` done `2026-07-30`**: the 9 emit-projection knobs gain a `KnobId` and the **`emission`** category — nine `annotate_*` passes take a `&SteeringConfig`, ~99 call sites updated, and each pass's per-gate roll now goes through the one primitive. `--profile structured-emission-max --introspect` went from reporting **no** emission knob to all eight non-version-gated surfaces with their own per-gate counts, **every achieved rate ≈ `0.25`** — the value decision `0032` chose by external measurement, now observable from the artifact itself. Category steers `0.397 → 0.069`; **16 configurations byte-identical**. With it, **steering's width is delivered**: all 38 `KnobId`s are steered at every roll site and visible in the readout, so decision `0017`'s API-completeness gate holds for every capability that actually rolls. **`.4c` done `2026-07-30`, CLOSING `.4`**: all six live-doc copies of the category taxonomy updated 6 → 8; `algorithm.md` gains the `motifs`-vs-`emission` distinction (once per **module** vs once per candidate **gate**); `USER_GUIDE.md` states the gap is closed for every knob that rolls and names the 3 exclusions-by-kind with the **real** error text; `structured-emission.md` gains "Measuring the surfaces: per-gate fire rates" with the measured table and two cautions (steering the *category* up moves the lane toward saturation — steer individual under-represented surfaces for diversity; `soft_union` is in the category but version-gated). A **fourth `ENUMERATION-PARITY` pair** binds those five doc sites to `KnobId::category`'s match arms (count-floored, negative-controlled both ways) — a stale copy is worse than an omission here, because `--steer` *errors* on an unknown key, so a user reading a short list never learns the category exists and the feature ships invisible. `DOCTRINE_ENFORCEMENT.md`'s "Three today" count was **deleted rather than incremented** (decision `0033` repairs a count beside a list by deletion). **`.4` CLOSED — steering is complete in both dimensions**: reach (`.3`+`.5`) and width (`.4`). **`.6` done `2026-07-31` — TREE CLOSED.** The last node retired the `KnobId` list rather than guarding it harder (rung **R1**): one `knob_ids!` table expands to the enum + `all()` + `name()` + `category()`, so each of the 38 variant names went from **5** occurrences in `src/ir/knob_id.rs` to **exactly 1**, the file dropped 483 → 317 lines, and `.4b.1`'s `index()` + `all_is_complete_and_ordered` were **deleted with the list they guarded** (lib tests 752 → 751 — one retired, nothing else lost). The reason is the durable part: `.4b.1` documented a gap its guard could not catch — a **tail** truncation of `all()` — and *neither* form of the obvious patch can work, since a derived count shrinks with the list and cannot fail while a hand-written one is the second copy decision `0033` forbids as a repair. **When a guard's residual gap can only be closed by adding another hand-written list, the guard is at the wrong rung.** Negative-controlled decisively: at `HEAD`, dropping the last `all()` entry leaves the build *and* the guard test **green** (reproduced live); now the same omission means deleting the row, which deletes the variant (`error[E0599]`). **A prediction this leaf made and then disproved, kept on the record:** leaving `ENUMERATION-PARITY`'s extractor on `pub fn category` was expected to read **zero** categories and trip the floor loudly. Measured, it reads the correct **8** — worse than zero — because its range terminator stopped existing (`    };` vs `    }`), so the range **over-ran 162 lines** into the table, where a `"[a-z]+"` scan skips knob *names* only by the coincidence that every one contains `_`. One probe knob named `"probe"` makes it emit a **phantom** category and the gate starts crying wolf — the failure this repo notes gets a gate deleted. Hence: *a `sed` line-range whose terminator stops existing does not fail; it runs on and returns something plausible.* Also repaired by **deletion** (rung R1): the book's `KnobId` snippet had rotted to **21 of 38** variants and its crate-layout diagram still attributed `KnobId`/`KnobRollCounters` to `types.rs`, two moves out of date; `book/src/knobs.md` named the pre-`IR-TYPES-DECOMPOSITION.2` path plus 18 of the 38 knob names as *"the full list"*. All three now point at the table instead of copying it. **23 valid comparisons across 22 configurations byte-identical** vs a `HEAD` release binary in an isolated worktree — after **2 earlier ones were discarded as vacuous**: both sides hashed `e3b0c44298fc`, the SHA-256 of the **empty string**, because `--hierarchy-depth 1` errors without `--num-leaf-modules`. The harness now refuses to score an empty or failed run. **Steering is complete in all three dimensions** — reach (`.3`+`.5`), width (`.4`), and maintainability (`.6`). The deferred decision-`0023` follow-up (the **in-generator adaptive schedule**) stays a future `.7` and blocks nothing. | [docs/tasks/COVERAGE-STEERED-GENERATION.md](tasks/COVERAGE-STEERED-GENERATION.md) |
| `CAPABILITY-BREADTH-EXPANSION` | `active` | Capability / breadth — high value-per-effort RTL surface additions (north star) | **Registered `2026-06-17`** (owner-directed). Two strands: (`.1`) more SV-2017/2023 up-opts (enum/typedef, packed multidim arrays, more 2023 constructs as proven default-off up-opts continuing the `union soft` / `SV-VERSION-TARGETING` pattern) and (`.2`) Mealy FSM outputs (extending the Phase-6 Moore-only `Fsm`). Each default-off, proven downstream-clean, API-selectable + introspectable (decision `0017`). **`.2a` done `2026-06-22`** (decision [`0024`](decisions/0024-mealy-fsm-outputs.md)): Mealy FSM output design ADR — a default-off combinational decode of `(state_q, sel)` (a per-`(state, sel_value)` table mirroring `transitions`, rendered as the proven nested `case (state_q)` → `case (sel)` form driving the **opaque** `FsmOut` leaf; only its decode reads the input-dependent `sel` cone, state stays Moore-clocked); new `fsm_mealy_prob` knob + `num_mealy_fsm_modules` metric (schema `1.12 → 1.13`) + `saw_mealy_fsm_design` gate + `--fsm-mealy-prob`/MCP; grounded in a fresh all-tool probe (verilator -Wall 1800-2012/2017/2023 + yosys both + iverilog all ACCEPT warning-clean). `.2` split into `.2a` (design, done) + `.2b` (impl, proposed). **`.2` advanced ahead of frontier-ordered `.1`**: the `.2a` probe re-confirmed (per decision `0010`) that the named `.1` candidates (enum/typedef, packed multidim arrays) are accepted at every Verilator `--language` mode + Yosys + Icarus ⇒ not version-distinctive (no down-gating teeth); `.1` stays `pending`, nothing retired. **`.2b.1` done `2026-06-22`** (Mealy **mechanism**: the default-off `fsm_mealy_prob`/`--fsm-mealy-prob` knob + `Fsm.mealy_outputs: Option<Vec<Vec<u128>>>` + the emitter nested `case(state_q)->case(sel)` Mealy decode + validate + the `merge_equivalent_fsms` Mealy exclusion + 2 lib tests; `FsmOut` unchanged opaque; full `cargo test` green, snapshots 6/6 Moore byte-identical, all-tool-clean [Verilator -Wall 2012/2017/2023 + both Yosys + Icarus]). `.2b` split into `.2b.1` (done) + `.2b.2` (introspection + gate) + `.2b.3` (docs). **`.2b.2a` done `2026-06-22`**: the `num_mealy_fsm_modules` `DesignMetrics` metric + introspection schema bump **`1.12 -> 1.13`** (queryable per decision `0017`); default-off byte-identical; full `cargo test` green, snapshots 6/6, live `--introspect` reports `num_mealy_fsm_modules` at schema `1.13`. `.2b.2` split into `.2b.2a` (done) + `.2b.2b` (the `saw_mealy_fsm_design` `tool_matrix` gate). **`.2b.2b` done `2026-06-22`**: the repo-owned Mealy gate in `src/bin/tool_matrix.rs` — `saw_mealy_fsm_design` coverage fact + the focused `phase6_mealy_fsm` scenario (`fsm_mealy_prob=1.0`) + detection/merge/`Phase4Hierarchy` gap + a non-vacuity test (scenario count 222→225, gate designs 888→900), mirroring the `phase6_fsm`/`phase6_inferrable_memory` motif gates; default-off DUT byte-identical (snapshots 6/6); proven downstream-clean by a focused harness-faithful run of the exact `phase6_mealy_fsm` scenario (Verilator `--top-module` + Yosys both modes + Icarus `-g2012 -s top`), with the `--phase4-hierarchy-gate` run enforcing `coverage_gaps=[]` incl. `saw_mealy_fsm_design` (gap-enforcement + non-vacuity unit-proven; the 224 non-Mealy scenarios unchanged from the r87 / `phase6-fsm-p1` banks). `.2b.2` done. **`.2b.3` done `2026-06-22`** (docs-only): `book/src/sequential.md` "FSM outputs: Moore vs Mealy" + a byte-verified runnable example (passing in `tests/book_examples`), `knobs.md` `fsm_mealy_prob` (entry + metric-table row + corrected stale "Mealy not emitted today" line), `USER_GUIDE`/`README` `--fsm-mealy-prob`, a new KM card `docs/knowledge/fsm-mealy-outputs.md` (cross-linked to decision `0024`), and the deferred introspection schema **`1.12 → 1.13`** book-example refresh (`api-tools`/`agent-mcp`/`api-introspection`/`api-reference`; provenance kept at `1.12`); mdbook build clean, KM regen+check green. **`.2b` done, and the whole Mealy strand `.2` is done.** **`.3` done `2026-08-01`** (decision [`0044`](decisions/0044-capability-breadth-unique-priority-case-qualifiers.md)): a **third strand** — the default-off `unique` / `priority` **case qualifiers** on `CaseMux`/`CasezMux`, valid-by-construction (the emitter's always-present `default:` arm ⇒ FULL; the generator's sequential arm indices ⇒ PARALLEL), measured over 50,761 emitted blocks and runtime-proven with `verilator --assert`. `.1` (SV up-opt breadth) stays **deferred-not-retired** with its question intact — `.3` was opened *beside* it, not as a reframing. **`.4a` done `2026-08-01`** (docs-only): the impl design-detail in `DEVELOPMENT_NOTES.md` resolving all five of `0044`'s Open Questions — the `Module.case_qualifiers` map carrier + the enum in a new `src/ir/case_qualifier.rs`, the `emission` `KnobId` category, the metric names @ schema `1.27 → 1.28`, the *roll `unique` first and skip the `priority` roll on a hit* precedence (so each knob's recorded fires equal its metric exactly), and **no** tenth/eleventh axis on decision `0032`'s interaction gate (a qualifier replaces no rendering; the co-occurrence lands inside `--case-qualifier-gate` instead, so this pass's one **non-vacuous** exclusion is not left unexercised end-to-end). `.4` split into `.4a` (done) + `.4b` (impl: `.4b.1` live / `.4b.2` metric+gate / `.4b.3` docs). **`.4b.1` done `2026-08-01`** — **the surface is live**: `src/ir/case_qualifier.rs` + `Module.case_qualifiers` + the two default-off knobs/flags + two `KnobId` rows in a **new `qualifiers`** steering category (`.4a` had said `emission`; **corrected mid-leaf** when a `tests/pipeline.rs` guard that pins `emission` to the nine *projections* failed — a qualifier is not a projection, and it claims only the gates those projections declined, so pooling them would average a self-cancelling mixture) + the tenth guarded roll in both `gen/mod.rs` paths + the emitter prefix on the two *unprojected* branches; snapshots 6/6 byte-identical, lib 749 → 764, each knob at `1.0` qualifying **30 of 30** eligible statements, ON byte-identical to OFF once the token is stripped, Verilator/Yosys(both modes)/Icarus clean with **identical** synthesized cell counts 8/8 and `unique` reproducing decision `0044`'s 30 Icarus `sorry:` lines with 0 `warning:`. Its FULL/PARALLEL property test was put through `DOCTRINE_ENFORCEMENT.md` §9 on the real source (delete the `default:` arm, widen the casez mask — each reddens it), so the `.3` corpus measurement has a durable home that provably looks. **`.4b.2a` done `2026-08-01`** — the surface is **queryable**: `Metrics::num_emitted_unique_cases` / `num_emitted_priority_cases` (one filter each over `Module::case_qualifiers`, `#[serde(default)]`) at introspection schema **`1.27 → 1.28`**, propagated across the const + its doc-comment history, the 3 `introspect` + 18 `mcp` assertions, the `docs/AGENT_INTROSPECTION_SCHEMA.md` contract (three current-version statements + a `1.27 → 1.28` changelog entry), the `TOOLBOX`/`USER_GUIDE` envelope references and the book's 19 example-JSON version strings. **Two** metrics rather than one because `.4b.2b` must pick a **per-qualifier tool plan** — Icarus prints `sorry:` for every `unique` block and is silent for `priority` — so a combined count would force the gate to re-derive the split from the config. Exact (the pass excludes statically-collapsed *and* chain-projected gates) and knob-aligned (each metric == that knob's `fires`; the two sum to `case_qualifiers.len()`, pinned by a new proof). Default-off DUT byte-identical (snapshots 6/6). The bump was treated as a **classification** job, not a find-and-replace: current-value references moved, while per-feature attributions, prior changelog entries and one knowledge-card anecdote — all statements about the past — did not; one card's evidence pointer was reworded from `SCHEMA_VERSION = 1.27` to name the const, the same lesson `docs/knowledge/api-reference.md` had already recorded. **`.4b.2` split into `.4b.2a` (done) + `.4b.2b` (the gate)**, mirroring both sibling surfaces and this tree's own Mealy strand. **`.4b.2b` done `2026-08-01`** — **the gate**: `tool_matrix --case-qualifier-gate` + `ScenarioSet::CaseQualifierSweep`, the first gate whose subject is an **assertion** rather than a rendering. 13 scenarios (`{unique, priority}` × `{case_mux, casez_mux}` × 3 strategies, plus one **co-occurrence** scenario with the chain knobs at `0.25`), three independently-required facts (`saw_unique_case_qualifier` / `saw_priority_case_qualifier` / `saw_case_qualifier_beside_if_chain`), metric-keyed detection off the `.4b.2a` metrics, and **per-qualifier tool plans** through a new `scenario_emits_unique_case_qualifier` predicate threaded as a *second* boolean beside `verilator_only` — gating the Icarus column alone, because `verilator_only` would also drop both Yosys modes and Yosys is this surface's strongest column. Banked clean at [`anvil-case-qualifier-gate-r1`](evidence/anvil-case-qualifier-gate-r1.md): 13 scenarios / 52 modules / **359** qualified statements / `coverage_gaps = []` / Verilator 52/0 / Yosys 52/0 both modes / Icarus **28/0**, where 28 is exactly the seven non-`unique` scenarios × 4 — the per-qualifier plan visible in the bank, not asserted. The thirteenth scenario is the only place the pass's **non-vacuous** exclusion fires end-to-end. Two findings from building it: the non-vacuity probe was reading **one** module where the gate runs **four** (which is how the co-occurrence fact's 2-of-4 rate, and hence the load-bearing unit floor, surfaced — *a vacuity probe that does not reproduce the run plan is testing a different experiment*), and a comment justifying metric-keying by a vocabulary collision was **probed false and retracted** before the commit. `.4b.2` done. **`.4b.3` done `2026-08-01`** (docs-only): `book/src/structured-emission.md` *"A different kind of thing: case qualifiers"* — deliberately **not** "the tenth surface", and placed **after** "Combining the surfaces" so the projection-vs-decoration boundary shows in the table of contents rather than only in the prose; `book/src/knobs.md` "Case-qualifier knobs" as its own heading (the knobs are not `_emit_prob`, not in the `emission` steering category, and not in the `structured_emission` catalog group — filing them under structured emission would contradict three mechanisms at once); `USER_GUIDE` prose entry + two CLI-table rows; the new KM card `docs/knowledge/case-qualifiers-unique-priority.md` with a **run** `reverify`; and the digest `commit` backfill (`c1cc6a1ff1f9` → `955e387`, with a note recording why a gate banked by its own introducing commit cannot know its hash). **One drafted number was caught by re-measuring**: the section quoted `.4b.1`'s *"8 modules / 30 blocks"* beside a **different** command that actually yields **176** — corrected, and the strip-the-token `diff` re-run at that shape. The rule recorded: *a measured number belongs to the exact invocation that produced it*. `mdbook build` clean, `tests/book_examples` 4/4, KM 113 → 114 facts. **`.4b`, `.4` and the whole case-qualifier strand are CLOSED**; the tree stays `active` with only `.1` (SV up-opt breadth) **deferred-not-retired**. | [docs/tasks/CAPABILITY-BREADTH-EXPANSION.md](tasks/CAPABILITY-BREADTH-EXPANSION.md) |
| `BOOK-API-REFERENCE` | `done` | Docs / book — comprehensive API reference (owner-directed) | **DONE `2026-06-17` (tree closed).** Owner directive: thoroughly document ANVIL's API in the mdBook, top-notch / industry-standard. Delivered as one pure-docs commit — four new Reference pages under the `agent-mcp.md` tutorial: `api-reference.md` (JSON-RPC protocol + transports + the `-32700`/`-32601`/`-32602`-vs-`isError` error model + content-addressing + the `protocolVersion`/`schema_version` versioning contract), `api-tools.md` (the 9 tools with input-schema tables + result shapes + errors + examples), `api-resources-prompts.md` (the `anvil://…` resources + the 5 prompts), `api-introspection.md` (the `--introspect` envelope + the 4 `analyze` query schemas + the stability contract); tutorial cross-links the reference; KM card `api-reference` (49→50). Schemas faithful to `src/mcp/mod.rs` + `docs/AGENT_INTROSPECTION_SCHEMA.md`; mdbook clean; book_examples 3/3; pure-docs / DUT byte-identical. Registered `2026-06-17` (owner directive: thoroughly document ANVIL's API in the mdBook, top-notch / industry-standard). A formal, JSON-Schema-faithful **API Reference** for the agent/automation API (the `anvil-mcp` JSON-RPC server — lifecycle methods, 9 tools, resources, 5 prompts, the `-32700`/`-32601`/`-32602` vs tool-level `isError` error model, stdio/HTTP transports; the `--introspect` document schema `1.11`; the four `analyze` query schemas; the `protocolVersion`/`schema_version` versioning contract), alongside the existing `agent-mcp.md` tutorial (progressive disclosure). Source of truth = `src/mcp/mod.rs` + `docs/AGENT_INTROSPECTION_SCHEMA.md`; pure-docs / DUT byte-identical; no new ADR (governed by `0017`/`0004`/`0011`). Pre-split `.1` (scaffold + protocol/overview) → `.2` (tools) → `.3` (resources + prompts) → `.4` (introspection + analyze + closeout). Frontier `.1`. | [docs/tasks/BOOK-API-REFERENCE.md](tasks/BOOK-API-REFERENCE.md) |
| `EVIDENCE-BANK-DURABILITY` | `done` | Quality / evidence architecture (cross-cutting) | **OPENED `2026-07-25`** from a session-bootstrap observation: every banked closure artifact this repo cites lives under **volatile `/tmp`** and is **absent from the machine** (`ls -d anvil-*` → 0 dirs; `target/tmp` → 0 bytes), while **77 distinct `anvil-*` paths** are cited across README/ROADMAP/USER_GUIDE/CODEBASE_ANALYSIS/TOOLBOX/book/tasks — including every phase-closing artifact (`-phase1-real-r21`, `-phase2-share-r1`, `-phase3-structured-r4`, `-phase4-hierarchy-r87`, `-phase5-p1`, `-phase5b-p1`, `-phase6-p1`, `-phase6-fsm-p1`, `-phase7-yosys-p1`, `-phase8-yosys-p1`) and every structured-surface gate bank. Not a claim that any closure was wrong — the gates stay re-runnable oracles (`DOCTRINE_ENFORCEMENT.md` §3) — but the *cited artifact* is unreachable, so re-verification means re-derivation (the archaeology the Knowledge Map exists to remove), and it is a partial regression against `LIVE-DOC-PATH-HYGIENE.1` (which fixed repo paths, not `/tmp` evidence paths). `.2` `done` (`2026-07-29`): decision [`0030`](decisions/0030-durable-closure-evidence-citations.md) — the ONE mechanism is the **committed per-bank digest** under `docs/evidence/` (embedding the exact re-runnable command + report SHA-256); pre-0030 `/tmp` citations demoted to labelled historical breadcrumbs; audit = 77 raw strings / 73 canonical banks ((a) 10 phase-closing, (b) 15 surface/sweep gates, (c) 47 focused smokes/probes, (d) 1 illustrative). **`.3` `done` `2026-07-30`** — the `EVIDENCE-CITATIONS` doctrine is live (driver 5/5 → **6/6**). The leaf had to re-decide `0030`'s recognition rule: its `/tmp/anvil-*` discriminator no longer exists (`VOLUME-DATA-LOCALITY.5` stripped every prefix), and the surviving bare `anvil-<name>` shape is shared with binaries (`anvil-mcp`), dirs (`anvil-sandbox`), Action inputs and prose — so the check **classifies** (digest-backed \| grandfathered \| not-evidence) and **fails closed**, rather than guessing which prose tokens are claims. §1 is pinned by entry count + membership SHA-256 because its membership is a historical fact (unpinned, "just grandfather it" bypasses the doctrine in one line); §2 grows under review. Ships `docs/evidence/{README,INVENTORY}.md` + `scripts/evidence_digest.sh` + `scripts/check_evidence_citations.sh` + the registry row, recorded as a dated amendment to `0030` (original text untouched). 4 negative controls incl. the escape hatch; classification cross-check **10/15/47 == the ADR's own audit**, reached independently. `.4` narrowed to a pointer-per-doc now that the inventory carries the breadcrumb record; `.5` promoted from deferred to the end-to-end proof. **`.5` `done`** — first real digest banked (`anvil-case-mux-if-gate-r2`: 12 modules, `coverage_gaps=[]`, 12/0 Verilator + both Yosys + Icarus), taken before `.4` as the end-to-end proof; it exposed two real tool bugs (a BRE interval inside `grep -E` making the deriver report "2907 gap(s)" for an empty array; a path-style digest citation the check rejected) — both fixed, `--self-test` added. **`.4` `done`** — one breadcrumb pointer per affected live doc + book chapter, one note in this file covering `docs/tasks/*` (layer-B history); `docs/knowledge/*` left alone (their `reverify:` one-liners are a stronger path). **TREE CLOSED `2026-07-30`** — then **REOPENED the same day** by the *second* real digest (`EMIT-SURFACE-INTERACTION-GATE.3`), which exposed two defects the first bank structurally could not: **(A)** `scripts/evidence_digest.sh` reconstructs the re-runnable `--command` from a **hardcoded** list of fourteen `*_gate` report fields, so a gate added later yields a command with **no gate flag at all** — it would run the *default* scenario set and "re-verify" entirely different numbers, silent and plausible and wrong (the new gate escaped only because `--command` was passed explicitly); **(B)** `commit:` is `HEAD` at derivation time, i.e. the **parent** of the banking commit, so for a gate introduced by that same commit the digest's own re-verification instruction names a commit where the flag does not exist — hit exactly, and corrected by hand in `anvil-emit-surface-interaction-r1` (`d73b1545a577` → `401d72de3425`). Both share one root: **the deriver guesses at facts the report and the commit already know** — this tree's own *classify, never guess* rule, one layer down, in the tool that produces the artifact the check validates. **`.6` `done` — TREE RE-CLOSED `2026-07-30`:** both defects repaired by **classification**, the rule this tree already applies one layer up. `true_gates_in` reads every `"*_gate": true` key straight from the report and the caller classifies: exactly one ⇒ use it; more than one ⇒ die (mutually exclusive, so malformed); zero ⇒ legitimate *only* when `scenario_set == "default"`, otherwise die naming the contradiction. `--commit <sha>` added, and a **dirty** working tree now triggers a loud warning that `commit:` names the *parent* of the banking commit — converting the silent wrong value into a visible one. Five live negative controls (the real report now reconstructs `--emit-surface-interaction-gate` **automatically**; flag-flipped ⇒ dies; two flags ⇒ dies; `default` set with no flag ⇒ still succeeds; `--commit` regenerates the hand-corrected pointer with no warning) plus a **meta-control**: sabotaging the shared extractor with this repo's own BRE-interval-inside-ERE mistake turns `--self-test` red, restoring turns it green. The four new self-test cases call the **same** extractor the real path calls — a self-test holding its own copy of the pattern would be defect A one level up. No phase label moves. | [docs/tasks/EVIDENCE-BANK-DURABILITY.md](tasks/EVIDENCE-BANK-DURABILITY.md) |
| `BOOK-LANE-COVERAGE` | `done` | Docs / book-sync (cross-cutting) | **OPENED `2026-07-29`** on an owner-relayed **PGEN** report: the mdBook documents the DUT lane thoroughly but has **no chapter for the microdesign or frontend lanes** — two of three artifact lanes are invisible on the review surface (book mentions exist only in `introduction.md`, `faq.md`, and the MCP/API reference). `.1` `done` (register + audit + KM card `pgen-first-contact-parser-gap` — consumer-reported: ANVIL output exposed a missing `case`/`endcase` production in PGEN's parser within minutes of first contact). **CLOSED `2026-07-29`**: `.2` microdesign chapter + a new "Artifact Lanes" SUMMARY part; `.3` frontend chapter + SUMMARY row + cross-links from `introduction.md`/`faq.md`/`microdesign-lane.md` — every pre-existing book mention of a non-DUT lane now routes to a real chapter. Both chapters paste REAL captured output and were cross-checked against `src/`+`tests/`; `mdbook build` exit 0 + `cargo test --test book_examples` 3/3 per leaf. Sibling code fix `LANE-OUT-FILENAME` landed between `.1` and `.2`. No phase label moves. | [docs/tasks/BOOK-LANE-COVERAGE.md](tasks/BOOK-LANE-COVERAGE.md) |
| `LANE-OUT-FILENAME` | `done` | Quality / non-DUT lane CLI correctness | **OPENED + CLOSED `2026-07-29`** from the `BOOK-LANE-COVERAGE.1` grounding runs: `--artifact frontend --out DIR` wrote **`child_0.sv`/`child_0.json`** (the first `module` declaration in the SV text via `src/main.rs` `parse_top_name`) instead of the documented top `acc_<seed>.sv` — the frontend emitter declares child stubs before the top. Fix (`.1`): `LaneArtifact` gains a builder-truth `top` field populated by all three lanes (DUT `design.top`, microdesign `top_name(seed)` single-sourced with `emit_sv`/`emit_manifest`, frontend `unit.top.name`); `run_non_dut_lane` uses it; `parse_top_name` deleted; SV/manifest bytes unchanged (filenames only). **`.1` `done`:** REJECT→PASS (`child_0.sv` → `acc_0.sv`), new lib proof `lane_artifact_top_is_builder_truth`, full gate green (`cargo test` exit 0 under `ram_guard`; snapshots 6/6 byte-identical). No frontier. | [docs/tasks/LANE-OUT-FILENAME.md](tasks/LANE-OUT-FILENAME.md) |
| `VOLUME-DATA-LOCALITY` | `done` | Quality / data locality + path portability (cross-cutting) | **OPENED `2026-07-29`** on an explicit owner directive (`2026-07-29`: repo-root-relative paths + §13 same-volume data locality) after all projects moved to a 4TB SSD. Measured: repo volume `/dev/disk5s1` vs `/tmp`+`/private/tmp` on `/dev/disk3s1s1` ⇒ **every OS-temp write is off-volume**. Audit of 15 `std::env::temp_dir()` sites: **(a) 1 PRODUCTION** — `ValidateOptions::default().sandbox_root` in `src/downstream/mod.rs`, so every `validate`/`minimize`/`hunt`/`divergence` run (CLI + MCP) writes generated SV + tool output off-volume; **(b) 1 debug dump** — `src/ir/compact.rs` hardcodes `anvil-bisim-merged.sv` (also a §12 breach, cited by a KM `reverify`); **(c) 13 test-fixture/temp-workspace sites** (in §13 scope); **(d)** CLI-parse string literals that write nothing (not violations). Frontier → `.2` (shared runtime-derived sandbox-root resolver + rewire the production default; never `temp_dir()`), then `.3` (test fixtures), `.4` (ADR `0031` + amend `0030`, whose rejected option (iii) is now partly MANDATED), `.5` (docs/book). **Also surfaced, NOT actioned (owner decision):** the pre-move checkout `~/Documents/github/anvil` still exists — 1.1G off-volume, HEAD a strict ancestor of ours, identical caches ⇒ no unique work. No phase label moves. | [docs/tasks/VOLUME-DATA-LOCALITY.md](tasks/VOLUME-DATA-LOCALITY.md) |
| `CARGO-TMPDIR-SWEEP-REGRESSION` | `done` | Quality / live-doc + doctrine-check precision | **OPENED `2026-07-29`** from a session-bootstrap deep-dive: 10 live-doc paths point at `target.cache/anvil-sandbox/<name>`, a directory that has never existed. Root cause pinned with `git show 4bb1822`: `VOLUME-DATA-LOCALITY.5`'s Rule B rewrote the substring `/tmp/` → `.cache/anvil-sandbox/` **unanchored**, so it also fired inside `target/tmp/<name>` — Cargo's `CARGO_TARGET_TMPDIR`, which lives inside the checkout and is therefore already on the project volume. `NO-BOOT-VOLUME-REFS` shared the same imprecision, so it could neither catch the damage (the mangled paths no longer contain `/tmp/`) nor accept the repair (which does) — hence `.1` before `.2`. **`.1` `done`:** `BANNED_RE` anchored to a path start (`(^\|[^A-Za-z0-9_.-])(/tmp/\|/private/tmp\|/var/folders)`; `~` excluded from the continuation set so `~/tmp/…` still counts), proven by 9 negative controls in both directions + an old-vs-new offender-set diff showing the change narrows false positives only; driver 5/5. **`.2` `done` — tree CLOSED `2026-07-29`:** all 10 citations restored across 8 files, each byte-identical to its pre-sweep text at `4bb1822` (8/8 token-set MATCH — an inversion of the sweep, not a re-guess); the three user-facing spots also name `CARGO_TARGET_TMPDIR`; residue census → 0 citations (6 defect-describing occurrences retained on purpose, the policy-document principle); `mdbook build` exit 0 + `book_examples` 3/3 + driver 5/5. No phase label moves; docs + doctrine script only ⇒ DUT byte-identical. Durable output beyond the ten strings: the `DEVELOPMENT_NOTES.md` rule that a gate written from a sweep's own search string inherits that sweep's blind spot. | [docs/tasks/CARGO-TMPDIR-SWEEP-REGRESSION.md](tasks/CARGO-TMPDIR-SWEEP-REGRESSION.md) |
| `EMIT-SURFACE-INTERACTION-GATE` | `done` | Quality / signoff (steering gaps 1 + 3) | **OPENED `2026-07-30`**, agent-picked under the owner's standing autonomy directive. Measured: all 8 `*_emit_prob` focus configs in `src/bin/tool_matrix.rs` set exactly **one** surface to `1.0`, so the nine emit projections' **mutual-exclusion predicates always see an empty sibling set** — the single property holding nine overlapping projections apart has no end-to-end downstream gate; and `--profile structured-emission-max` sets **4 of 9** knobs (it predates surfaces 6–9, so its name is false). Not a claim any surface is wrong — each is individually banked clean — but a module carrying several at once is exactly the interaction-heavy legal RTL the north star wants. `.1` design ADR (naming `cone_function` interior *absorption* vs the others' per-gate marking as the one genuinely risky pair) → `.2` honest preset + a drift test → `.3` the combined gate + a `docs/evidence/` digest. **`.1` `done` `2026-07-30`** — decision [`0032`](decisions/0032-emit-surface-interaction-gate.md). It strengthened the opening observation by measurement: the preset does not merely cover 4 of 9, it **emits 1 of 9** (796 combinational functions, every other surface exactly `0`) — because at `prob = 1.0` the second pass in the fixed order claims every gate the later passes' candidate sets overlap. All-eight-at-`1.0` collapses the same way (3 live surfaces, 12/12 modules). The fix is an **intermediate** probability: all eight at `0.25` puts **8 distinct surfaces in 20 of 24 modules**, clean 24/24 on Verilator + both Yosys modes + Icarus with zero warnings, floor 6/8/7 across the three construction strategies; `0.25` maximises the least-represented surface (max-min over seeds 1–5). Decided: a 3 + 1 scenario sweep; co-occurrence proven **metric-keyed** from a derived `distinct_emit_surfaces` over the nine `ModuleReport.emitted_*` booleans that already exist (no new token, the `0028` precedent); `soft_union` as a separate Verilator-only 2023 scenario so the universal three keep their Yosys + Icarus columns; the preset redefined as maximal surface *diversity*. Five interaction risks reasoned from source — four predicted-and-measured clean, one **latent trap** split out as new leaf `.4`: `cone_function_emit::compute_use_counts` omits `Memory` ports and `Fsm.sel`, so an absorbed gate could have its wire deleted from under a memory block; unreachable today only because `build_memory_leaf`/`build_fsm_block` build gate-free modules. **`.2` `done` `2026-07-30`** — the preset is honest: `structured-emission-max` now sets all eight non-version-gated `*_emit_prob` to the named `STRUCTURED_EMISSION_MAX_PROB = 0.25` plus `comb_mux_prob`/`case_mux_prob`/`casez_mux_prob` to `0.35` (three of the eight surfaces project a `Mux`/`CaseMux`/`CasezMux` and would starve on shape at the defaults — the `deep-hierarchy` precedent). Measured **8/8 surfaces emitted on seeds 1–5** (was 1/8), and a 12-module corpus clean on all four tool columns with **every** module carrying all eight. The anti-drift test is **derived, not enumerated**: it reads the required knob set from `knob_catalog()`'s `structured_emission` group (itself already gated to cover every `Config` field) minus the version-gated `soft_union_slice_prob`, so a tenth surface joins that group by virtue of its `*_emit_prob` name and must join the preset or CI is red — closing the exact decay mode that let surfaces 6–9 go missing. Negative-controlled in both directions (omitted knob; knob at `1.0` instead of the shared value). Docs corrected everywhere the old claim was pinned, including the KM card's `reverify` one-liner, plus a new `book/src/structured-emission.md` "Combining the surfaces" section with three runnable examples. **`.3` `done` `2026-07-30`** — the gate exists: `tool_matrix --emit-surface-interaction-gate`, five scenarios (three comb-only DUTs one per construction strategy with all eight surfaces at the shared probability; one **saturation** scenario at `1.0` where every later pass must skip against a *full* sibling set; one Verilator-only IEEE 1800-2023 scenario adding the ninth, kept separate so the other four keep Yosys + Icarus). Co-occurrence is proven **by projection, not by a new token**: `distinct_emit_surfaces` counts the nine `emitted_*` booleans `ModuleReport` already carries, so no new metric is minted and the `--resume` checkpoint payload is unwidened. Facts `saw_multi_surface_emit_interaction` (≥2), `saw_all_emit_surfaces_in_one_module` (≥8), and the **Verilator-only** `saw_all_nine_emit_surfaces_in_one_module` (requiring Yosys there would make it unreachable — Yosys rejects `union soft`), plus a recorded-not-gated `max_distinct_emit_surfaces`. **Clean on the first run**: `coverage_gaps = []`, Verilator `20/0`, both Yosys modes `16/0`, Icarus `16/0`. The measured shape matched the ADR's source-derived prediction exactly — `8` surfaces per module in the three universal scenarios, `9` in the 2023 scenario, and **exactly 3** under saturation, those three being `function_emit` + `case_mux_if` + `casez_mux_if`: the pass that runs first plus the two whose targets it cannot claim. Digest banked at [`docs/evidence/anvil-emit-surface-interaction-r1.md`](evidence/anvil-emit-surface-interaction-r1.md) — decision `0030`'s second customer, as `.1` intended. **`.4` `done` — TREE CLOSED `2026-07-30`:** the absorption consumer census is complete. `cone_function_emit::compute_use_counts` now counts `memories[..].{we,waddr,wdata,raddr}` and `fsms[..].sel`, and its doc comment carries a **table of every `Module` field that can hold a `NodeId`** so a new field forces a decision rather than being silently omitted — the omission that created the trap. Three tests (memory `wdata`, FSM `sel`, and the gate-only no-op complement), both new ones **negative-controlled**: removing the census rows makes them fail, restoring them makes them pass. The byte-identical claim is **measured, not argued** — 30 modules generated at `--cone-function-emit-prob 1.0 --memory-prob 0.3 --fsm-prob 0.3` pre- and post-fix are `diff -r` identical. Deliberately **not** extracted into a shared `ir::use_counts` helper (the `.1` open question): one absorbing pass exists today, so extraction would add an indirection with a single caller; the recorded trigger is a *second* declaration-suppressing pass. No phase label moves; every surface stays default-off and the no-`--profile` path is untouched ⇒ DUT byte-identical. |  [docs/tasks/EMIT-SURFACE-INTERACTION-GATE.md](tasks/EMIT-SURFACE-INTERACTION-GATE.md) |
| `SHADOW-ENUMERATION-SWEEP` | `closed` (`2026-07-30`) | Quality / defect-class elimination (cross-cutting) | **OPENED `2026-07-30` on owner directive** — *"the only way to have any defect handled is to have it task-tree tracked"* — after the same finding was first parked in `MEMORY.md`, which `MEMORY_ARCHITECTURE.md` §71 defines as **overwrite-only with a hard size cap**: a finding stored only there is queued for deletion by the next state refresh, not retained. Owns the **class**, not the instances: three same-shape bugs landed in one session, each in a different subsystem — the `structured-emission-max` knob list (shadowing `knob_catalog()`'s `structured_emission` group; the preset set 4 knobs and **emitted 1**), `compute_use_counts`'s consumer census (shadowing `Module`'s `NodeId`-bearing fields; absorption became unsound-in-waiting), and `evidence_digest.sh`'s 14-entry gate list (shadowing the report's own `"*_gate": true` keys; emitted a **flagless** re-verification command). In every case the authoritative set was already reachable and the list was a second copy that nothing failed on when it fell behind. Two further sites measured while registering: **(i)** `src/bin/tool_matrix.rs` enumerates the gate flags in **seven production sites**, of which five are silent on omission — and the severe one is the `fail_on_coverage_gap` or-chain, because `:1490` reads `if plan.fail_on_coverage_gap && !coverage_gaps.is_empty()`, so a gate missing from it **runs, reports gaps, and exits 0** — a gate that cannot fail, whose output would be banked as clean closure evidence; **(ii)** `merge_coverage` hand-merges **149/149** `CoverageSummary` fields — complete today, guarded by nothing, one omission away from a coverage fact that never unions across scenarios. Neither is a live bug; the defect is that correctness rests on diligence, and this session holds three proofs that diligence is insufficient. Explicit non-goal: **authoritative** enumerations stay hand-written (the `DOCTRINES` registry, `presets()`) and the deliberate allow-lists (`check_no_boot_volume_refs.sh`, the frozen `EVIDENCE-CITATIONS` §1 pin) are load-bearing and must not be "improved" into derivation. `.1` register + audit (done) → `.2` design ADR (done) → `.3`–`.7` one fix per site. **`.2` `done` `2026-07-30`** — decision [`0033`](decisions/0033-shadow-enumeration-classification.md). The rule is a **conjunction of three tests**, not "L duplicates S": a list is a shadow iff it is **(1) derivable** (another artifact in the repo already enumerates the same membership by ordinary program/script means), **(2) growth-coupled** (that set grows and every growth *requires* a matching entry), and **(3) silent** (the omission produces no compile error, no failing test, no runtime error). Test (2) is the load-bearing one and is written in precisely to *reject* the load-bearing allow-lists: `check_no_boot_volume_refs.sh`'s allow-list and the frozen `EVIDENCE-CITATIONS` §1 pin both satisfy (1) and are authoritative anyway, because the gap between the list and the set **is the content of the rule** — dropping test (2) would license deleting decision `0031`'s history-stays-raw guarantee and decision `0030`'s un-growable grandfather list. Test (1) rejects `presets()` and `DOCTRINES` (no such set exists). Repairs form a four-rung ladder in strict preference order — **R1** derive (the list stops existing) → **R2** make it loud (a compile error; what already protects the `Cli` fields, the `ScenarioSet` `match`, and `main.rs`'s exhaustive `cli_overrides` literal) → **R3** guard with a test that *derives* the expectation (models already in-repo: `knob_catalog_classifies_every_field`, and the decision-`0032` preset drift test) → **R4** a registered doctrine check, the only rung available where there is neither a compiler nor `cargo test` — bound by two constraints: a repair may not introduce a new hand-maintained list, and every guard is negative-controlled in both directions. **Mechanizability, answered honestly: no as *discovery*.** Test (1) is a semantic relation between two sets — nothing syntactic separates `presets()` from a shadow — so a detector would have to already know the pairing, which is the exact judgement the rule encodes; its only failure modes are *miss* (false confidence, this tree's own defect at the meta level) and *cry wolf* (and a gate that cries wolf gets deleted, taking its real coverage with it). Yes as *holding classified pairs*, split by language: Rust pairs get an in-crate `#[test]` (already gated by `cargo test` per `COMMIT.md` + CI — a shell doctrine would be a second mechanism for one job), docs/script pairs get **one** `ENUMERATION-PARITY` check over a declared pairs table that is itself authoritative under rule (a), so the mechanism does not recurse. Severity tiers **S3** (a gate reports success it did not earn — a *false green*, banked as decision-`0030` evidence and cited in live docs) / **S2** (a user-requested behaviour silently does not happen) / **S1** (a contract or report understates reality — fail-safe in direction). The 20-site audit **corrects `.1` twice, from measurement**: `merge_coverage` drops from #2 to #3 because all 149 merges are monotone (`135 × \|=` + `13 × .extend()` + `1 × .max()`, zero assignments ⇒ an omission can only *under*-report) and **134 of the 149** fields are read by `compute_coverage_gaps`, whose every gap has the positive-polarity shape `if !coverage.saw_x {…}` ⇒ a forgotten merge yields a *spurious* gap ⇒ under any gate the run **bails loudly**; the genuinely silent surface is **15** fields, not 149. Taking its place at #2 is the tree's own unaudited open question, now measured: `Config::apply_cli_overrides` is **87 fields / 87 read / 87 written**, complete and guarded by nothing, and its omission makes a knob's CLI flag *and every `--profile` preset that sets it* silently inert — the exact shape of the decision-`0032` bug, since presets apply through the same applier. Three sites `.1` never reached are now tracked: the four hardcoded adapter-id JSON-schema `enum`s in `src/mcp/mod.rs` shadowing `ADAPTER_REGISTRY` (a sixth adapter usable but unadvertised to agents — decision `0017`), `DOCTRINE_ENFORCEMENT.md` §10's table shadowing the `DOCTRINES` array (a documented but unregistered doctrine is the §11 "trust me" anti-pattern), and `book/src/SUMMARY.md` shadowing `book/src/*.md` (an unlinked chapter is written and **never rendered**, and the book is the owner's only window). Tree grows to seven leaves; execution order `.3` → `.5` → `.4` → `.6` → `.7`, carried by the tree's `Current Frontier` table because leaf **numbers** stay in creation order (a leaf id is the durable commit↔tree join key). **`.3` `done` `2026-07-30`** — the S3 site is gone, closed by **R1** rather than merely guarded. `static GATES: &[GateSpec]` declares a gate exactly once (`flag`, `enabled: fn(&Cli) -> bool`, `scenario_set`, and a `UnitFloor::{TotalAtLeast, PerScenario}` — the two unit shapes modelled, not flattened, per the `PHASE4-HIERARCHY` lesson that a stale *total* budget silently weakens a gate when its scenario count grows), and one `enabled_gates` iterator feeds both consumers: the 15-arm run-plan `if`-chain, the 15-arm scenario-set `if`-chain, the 15-term exclusivity sum, and the retyped 15-flag `bail!` prose all collapse into it, while the S3 line itself becomes **derived, not enumerated** — `cli.fail_on_coverage_gap \|\| gate.is_some()`. The **R2** form decision `0033` left open (derive it from the compiler-enforced `ScenarioSet`: `scenario_set != Default`) was evaluated against the code and **rejected because it would have disarmed the Phase 1 gate** — `--phase1-gate` maps to `ScenarioSet::Default`, raising the corpus size over the *built-in* set rather than selecting a dedicated one, and its old selector arm was an *absence* rather than a line; the tempting form would have introduced the very S3 defect into the largest gate in the file while looking like the more principled version. Four derived guards, none a new hand-written list: `every_cli_gate_flag_is_registered` (expected set derived from **clap's own `Cli` metadata** — the `knob_catalog_classifies_every_field` pattern, plus a non-vacuity assert so a future clap change fails loudly instead of measuring nothing), distinct-scenario-set (catching the copy-paste row, and pinning `--phase1-gate` as the only gate allowed on `Default`), a per-gate arms-the-`bail!`-and-raises-units invariant that turns each flag on **through `Cli::try_parse_from`** (a flag→field `match` was drafted and deleted on realising it would be a *seventh* copy of the same set inside its own guard; parsing also proves each `flag` string is a real CLI flag, which a `&mut` switch could never check), and a `MatrixReport` gate-field check keyed off a flag→field derivation (`MatrixReport` gained `Default` **solely** so that test needs no literal — a literal would be a fresh shadow of the struct). Negative-controlled in both directions, and the S3 defect **reproduced on purpose**: with the fifteenth row deleted, a temporary probe measured `fail_on_coverage_gap = false, modules_per_scenario = 1` for `--emit-surface-interaction-gate` — a gate that runs 1 unit/scenario and cannot fail — while the new guard named the exact missing flag; a copy-paste row was caught by the distinctness guard; source restored byte-identical after each. Harness binary only ⇒ **DUT byte-identical**, `tests/snapshots.rs` untouched; no CLI flag, report field, gate behaviour or message text changed (the derived exclusivity message renders the identical string). **`.5` `done` `2026-07-30`** — the **S2** site, and the tree's own unanswered opening question. `Config::apply_cli_overrides` is 87 hand-written `if let Some(v) = o.x { self.x = v; }` lines shadowing `Overrides`; forget one and the knob's `--flag` **and every `--profile` preset that sets it** go silently inert, because presets apply through the same applier (`src/config.rs:2465`) — the exact shape of the decision-`0032` bug. Closed by **R3 on an R2-protected fixture**, and the pairing is the reusable lesson: a round-trip guard needs a fully-populated `Overrides`, and hand-writing one would be a *fresh shadow of the very struct under test*, so `every_override_set()` is an **exhaustive literal with no `..Default::default()`** — adding a field is an `E0063` error there (demonstrated), which means it reads like a hand-written list but fails decision `0033`'s *silence* test and is therefore not a shadow at all. The expectation is derived from the `Overrides ∩ Config` serde-key intersection, so a new knob joins it by existing. **Two legs, because a set-difference guard proves only half the property:** a *leaky* applier line that writes its own knob and a neighbour's leaves the whole-struct test green (every knob still moves), and only the single-knob leg catches it — measured, not assumed (NC-D: whole-struct green, single-knob red with `left: ["max_depth","max_width"]`). Two anti-decay pins so the guard cannot rot into a tautology: the derivation must still measure something (`expected.len() > 50`), and the **complement** is pinned — exactly four `Config` knobs are not CLI-overridable (`library_prob`, `max_nodes_per_module`, `use_async_reset` — the documented config-file-only three, decision `0021` — plus `seed`, stamped last by `resolve_config`), so a knob quietly dropping out of `Overrides` fails loudly instead of shrinking the expectation. Measured en route: `Overrides` has **88** fields, not the 87 decision `0033` recorded — the 88th is `steer`, the one override that is not a `Config` field (a repeatable list folded into `Config.steering` by the resolver), now pinned by name. Five negative controls, source restored byte-identical after each; `cargo test --lib` 742/0 (was 740), snapshots 6/6. Diff confined to `src/config.rs`'s `#[cfg(test)]` module — **no production line changed** ⇒ DUT byte-identical. **`.4` `done` `2026-07-30`** — the last Rust-side site, closed by reusing the `.5` pattern verbatim: an **exhaustive** 149-field `CoverageSummary` fixture (`E0063`-maintained, demonstrated), leg 1 comparing serde projections after a full merge, leg 2 feeding one fact at a time, and the same anti-decay assert. **NC-C is the payoff and the reason the two-leg shape is now a rule rather than an observation:** cross-wiring `dst.saw_flop_merge \|= src.saw_semantic_gate_merge` left **leg 1 green** — with every source fact set both fields end `true` either way — and only leg 2 fired. That is the second independent site (after `.5`'s config applier) where a whole-struct round trip proved blind to a *leaky or cross-wired* line, so it is recorded as a rule: *a set-difference guard proves nothing was dropped; it cannot prove nothing extra was picked up.* It bites hardest exactly here, where 149 near-identical merge lines share 60-character name prefixes. `CoverageSummary` gained `Deserialize` + `#[serde(default)]` **solely** so leg 2 can build a single-fact source without a 149-entry literal — inert for the run path, zero bytes changed in the emitted report, the same discipline as `.3`'s `MatrixReport: Default`. Census re-derived rather than trusted: 149 = 135 `bool` + 13 `BTreeSet<String>` + 1 `usize`, with one line-wrapped `bool` that a naive per-line regex reports as 148 — the extractor was repaired rather than the more plausible number chosen. `cargo test --bin tool_matrix` 113/0 (was 111); snapshots byte-identical; four negative controls, source restored byte-identical after each. Harness binary only ⇒ DUT byte-identical. **`.6` `done` `2026-07-30`** — the MCP adapter-id allow-list, and **the first leaf in this tree to fix a list that was already wrong rather than one still correct**. Decision `0033`'s audit recorded four hardcoded schema `enum` literals; re-measured against the registry rather than against the known *shape*, there were **seven** copies — the four `tools` JSON-schema `enum`s (`validate`/`divergence`/`minimize`/`hunt`), the unknown-tool error in `parse_validate_tools`, `validate`'s tool **description**, and the server **`instructions`** — and two of them had **already fallen behind**: the `instructions` named three adapters (`verilator / yosys / iverilog`) and `validate`'s description four (missing `slang`), against a registry of five. ANVIL's own API had been telling agents it accepts fewer downstream tools than it accepts since `sv2v` and `slang` landed, and nothing anywhere failed. **Why the audit missed it:** `.2` swept for the *shape* of the instance it had found (a JSON `enum` array), which silently redefines the class as *structured* copies; sweeping for the *content* (any non-test line naming ≥ 2 registry ids) finds all seven, and the three extras were exactly the rotted ones. Prose is in scope from now on — under decision `0017` a tool description **is** the contract, and it satisfies rule (a)'s three tests (derivable ∧ growth-coupled ∧ silent) exactly as a schema does. Closed by **R1** at every site: one `registered_adapter_ids()` projects `downstream::adapters()` and the schema `enum`s, the error message, and both prose strings render from it, so the copies stop existing. Three derived guards, none a new list — and the schema guard **walks the emitted `tools/list`** rather than checking the four sites by name, because a guard listing the known sites would be a fifth copy of the growing set sitting inside the test written to abolish copies of it (a `>= 4` floor keeps the walk from passing vacuously). One candidate site audited and **deliberately not fixed**: `AcceptanceTool::from_name`'s `_ => None` catch-all is already covered by `adapter_registry_lists_the_originals_then_new_adapters`, which loops every registry id through it — a second guard would be a second mechanism for one job (`feedback_full_factorization`), recorded so the next sweep does not duplicate it. **NC-E is the acceptance criterion demonstrated rather than argued:** a sixth `ProbeAdapter` added to `ADAPTER_REGISTRY` **and nothing else** propagated to all four schemas, both prose sites and the error message with zero edits under `src/mcp/`, while the three derived guards stayed green because they derive too; the registry's own landing pin was separately confirmed to fail on the sixth entry, so the registry is not silently growable either. Also negative-controlled by replaying the live defect (the pre-fix three-adapter prose ⇒ *"missing `sv2v`"*) and a hand-retyped four-entry `hunt` `enum` (⇒ names the offending tool), source restored byte-identical after each. `src/mcp/mod.rs` only ⇒ **DUT byte-identical**, `tests/snapshots.rs` untouched. Frontier **`.7`**, the last leaf; **every code-side silent shadow in the audit is now closed**, leaving only the docs/script pairs — which `.6` grows by one: `book/src/api-tools.md`'s hand-written `tools` `enum`, now the only adapter-id copy left outside the registry. **`.7` `done` `2026-07-30` — the last leaf; the tree CLOSES.** The docs/script pairs are the only sites in the audit with **neither a compiler nor `cargo test`**, so they are the only ones needing rung **R4**: one registered `ENUMERATION-PARITY` doctrine (`scripts/check_enumeration_parity.sh`, `DOCTRINE_ENFORCEMENT.md` §10 row, the **7th** entry in `DOCTRINES`) over a declared `PAIRS` table — **four** pair groups, not the two `.2` planned: §10's own table ↔ the `DOCTRINES` registry (exact, both directions), the four live-registry documentation sites ↔ the same registry (coverage), `book/src/SUMMARY.md` ↔ `book/src/*.md` (exact, both directions — mdBook renders **only** what `SUMMARY.md` links, so an unlinked chapter is written and never rendered), and the two book chapters documenting the downstream-tool allow-list ↔ the adapter registry (coverage, the pair `.6` left behind). **Its first run found THREE simultaneously-stale published copies of the registry it was written to guard:** `book/src/architecture.md` ("The live registry:"), the `docs/knowledge/doctrine-enforcement.md` fact card ("The live registry is **four** doctrines"), and `CODEBASE_ANALYSIS.md` ("`PASS` × **4** doctrines") each named **4** doctrines while **6** were enforced — `NO-BOOT-VOLUME-REFS` and `EVIDENCE-CITATIONS` had both shipped without any of the three being updated; `README.md` was correct at 6 and would have gone stale on this very commit. Severity is not uniform and that is the point: the book is the owner's only window into the project, a Knowledge Map card is what a future agent reads **instead of** re-deriving (so a stale one *misinforms* rather than omits), and `CODEBASE_ANALYSIS.md` is required by `COMMIT.md` to reflect the code as it now is. Four design decisions recorded: the coverage pairs **name** their sites rather than discovering them (decision `0033` (c) already ruled out a shadow *detector*, and "any chapter naming two adapter ids" also matches an ordinary `--tools verilator,yosys` example — a gate that cries wolf gets deleted); the `PAIRS` table is **authoritative, not a shadow** (no set enumerates *"which lists shadow which sets"* ⇒ rule (a) test (1) fails ⇒ no recursion) and lives **inside the script** for the same reason `DOCTRINES` lives inside its driver — a data file would need a schema, a parser and an existence meta-check, three mechanisms for one table read by one script (`feedback_full_factorization`), which answers the tree's last open question; **every extraction is count-floored**, because a silently-empty extractor would make the gate pass *vacuously* — the exact failure this tree exists to remove, and one `EVIDENCE-BANK-DURABILITY.5` already demonstrated (NC-F: *"the extractor is broken, not the enumeration; do NOT lower the floor"*); and two redundant **counts** were **deleted rather than gated** (`README.md`'s "Live registry (6)", the fact card's "is four doctrines"), because a number beside the list it counts is a second copy of the same set with none of the list's value — "gate the count too" is the tempting answer and it keeps a shadow alive. The doctrine also **gates its own registration**: pair 1 read `7 entries` the moment `ENUMERATION-PARITY` joined `DOCTRINES`, so the check would have failed this leaf had the §10 row been forgotten. Seven negative controls, both directions on every exact pair, source restored byte-identical after each; `check_doctrines.sh` 7/7 PASS, `mdbook build` clean, Knowledge Map regenerated + in sync (85 facts / 850 question keys), snapshots 6/6. **No `src/` change at all ⇒ DUT byte-identical by construction.** **Tree closed:** across seven leaves the class is now held by the full repair ladder rather than by diligence — **R1** derive (the `tool_matrix` `GATES` table, the MCP allow-list), **R2** compile error (the two exhaustive fixtures), **R3** derived `#[test]` (the config applier, the coverage merger), **R4** registered doctrine (the docs/script pairs). The finding that outlived the tree: it opened believing these lists were *"one omission away"* from being bugs, and measurement found **five** omissions that had already happened — two in the MCP API's self-description, three in the published doctrine registry — none of which anything had noticed, all of them in **prose**, the copy nobody greps when adding an entry. No phase label moves; every leaf was DUT byte-identical. | [docs/tasks/SHADOW-ENUMERATION-SWEEP.md](tasks/SHADOW-ENUMERATION-SWEEP.md) |

## Directory Layout

```text
docs/TASK_TREE.md
docs/TASK_TREE_README.md
docs/tasks/
  TEMPLATE.md
  <TREE>.md
```

`docs/TASK_TREE.md` is the workflow and active-tree index.
Each top-level task owns one file in `docs/tasks/`.
`docs/tasks/TEMPLATE.md` is copied when creating a new top-level tree.

## Definitions

- Task tree: the recursive decomposition of one top-level task.
- Node: one item in that tree.
- Container node: a node with children. It is not directly executable.
- Leaf node: a node with no children. It is the only unit PNT may implement.
- Current frontier: the ordered set of leaf nodes that are eligible to be
  picked next.
- Slice: one completed leaf task plus its tests, docs, live-doc updates, and
  commit workflow.
- Evidence: the validation output, changed-doc summary, and git commit subject
  that prove a leaf was completed.

## ID Rules

Each task tree has a stable top-level ID.

```text
<TREE>
<TREE>.1
<TREE>.1.1
<TREE>.1.1.1
```

Rules:

- `<TREE>` uses uppercase letters, digits, and hyphens.
- Child IDs append dot-separated positive integers.
- IDs are permanent once published.
- Never renumber closed nodes.
- If a new ordering is needed, add new IDs and mark old nodes `superseded` or
  `deferred` with a reason.
- A commit that completes a task-tree leaf must identify the leaf ID in the
  commit subject or in the first body line.

## Status Vocabulary

Use only these statuses.

| Status | Meaning |
| --- | --- |
| `proposed` | Captured but not yet accepted into the active tree. |
| `active` | The top-level tree is open, or a container has unfinished children. |
| `pending` | Ready to be selected once it reaches the current frontier. |
| `in_progress` | Currently being implemented in the worktree. |
| `blocked` | Cannot proceed without a named blocker and unblock condition. |
| `done` | Completed, validated, documented, and committed. |
| `deferred` | Deliberately postponed with an explicit consequence. |
| `superseded` | Replaced by another node, with the replacement ID named. |

## Required Task File Sections

Every top-level task file must contain:

- Metadata: tree ID, status, roadmap lane, created date, last updated date.
- Goal: the user-visible or project-visible outcome.
- Non-goals: what this tree deliberately does not try to solve.
- Acceptance criteria: concrete conditions that close the top-level task.
- Task tree: all known nodes, with status and short result intent.
- Current frontier: ordered leaf nodes that PNT may select next.
- Decisions: accepted technical decisions and their rationale.
- Open questions: unresolved questions that do not block the whole tree yet.
- Blockers: blockers with unblock conditions.
- Verification log: checks run for completed leaves.
- Commit log: leaf IDs mapped to completion commit subjects.
- Changelog: dated edits to the tree itself.

## Node Rules

Every node must be one of these two shapes.

Container node:

```text
- ID: <TREE>.<n>
  Status: active
  Goal: ...
  Children: <TREE>.<n>.1, <TREE>.<n>.2
```

Leaf node:

```text
- ID: <TREE>.<n>
  Status: pending
  Goal: ...
  Acceptance: ...
  Verification: pending
  Commit: pending
```

A node with children must not be marked `done` until every child is `done`,
`deferred`, or `superseded`, and every non-`done` child has a recorded reason.

## Current Frontier Rules

The current frontier is the only list PNT uses when selecting work from a task
tree.

Rules:

- The frontier contains only leaf nodes.
- The frontier is ordered by intended priority.
- A container never appears in the frontier.
- A blocked node stays out of the frontier until unblocked.
- When a leaf is split, remove that leaf from the frontier, mark it `active`,
  add children, and place the first executable child or children in the
  frontier.
- When a leaf completes, remove it from the frontier and add the next eligible
  leaf or leaves.

## PNT Selection Rules

When PNT is asked to continue and at least one active task tree exists:

1. Read `docs/TASK_TREE.md`.
2. Read the active task file named in the `Active Task Trees` table.
3. Pick the first eligible leaf in that file's `Current Frontier`.
4. Implement only that leaf.
5. If the leaf is too broad, split it before implementation and commit the
   tree update as the leaf's honest outcome.
6. Run the required validation for the leaf.
7. Update the task file, live docs, and roadmap if status changed.
8. Run the full commit workflow before selecting another leaf.

If several active trees exist, choose the first active tree in the table unless
the user names another tree or the roadmap status names a different immediate
lane.

When the user asks for PNT and **no** active task tree is appropriate (the
work is a linear `rN` coverage extension), continue on the `rN` convention —
do not invent a task tree just to satisfy this section.

## Splitting Rules

Split a node when any of these are true:

- It cannot be completed to signoff quality in one slice.
- It mixes design, implementation, diagnostics, tests, and docs in ways that
  can be reviewed independently.
- It hides an unresolved policy choice behind implementation wording.
- It would require touching unrelated ownership areas in one commit.
- It discovers a lower-level dependency that should be solved first.

Do not split merely to create vague placeholders. Every child must have a
clear goal and a way to verify completion.

## Completion Rules

A leaf is complete only when all of the following are true:

- Implementation or documentation work for that leaf is finished.
- Focused checks passed, and broader checks ran when warranted (see
  `COMMIT.md` for the full pre-commit checklist).
- The owning task file records the result, validation, and commit subject.
- `CHANGES.md`, `MEMORY.md`, and the other live docs listed in `COMMIT.md`
  are updated when the leaf changes project state.
- The commit workflow in `COMMIT.md` has completed.
- `git_message_brief.txt` has been cleared after commit.

Commit hashes are intentionally not required inside the same task-file update:
the final hash cannot be known until after the commit exists. The stable
join key is the leaf ID in the commit subject or first body line. Later status
refreshes may backfill hashes if useful.

## Blocker Rules

A blocked node must record:

- the exact blocker,
- why it blocks the node,
- the unblock condition,
- and the next task that should run instead, if any.

Do not leave a node as `blocked` only because it is large or unclear. Large or
unclear work should be split until a real blocker is visible.

## Relationship To Live Docs

The task tree is the detailed execution ledger.

- `ROADMAP.md` remains the canonical high-level phase status.
- `MEMORY.md` remains the recovery/handoff continuity log.
- `CHANGES.md` remains the chronological technical history.
- `DEVELOPMENT_NOTES.md` remains design rationale.
- `CODEBASE_ANALYSIS.md` remains the live workspace analysis.
- `USER_GUIDE.md` remains user-facing CLI/workflow reference.
- The mdBook (`book/src/*.md`) remains user-facing product/algorithm
  documentation.

Do not duplicate the whole task tree into those files. Link to the task tree
and summarize only the part that changes live project state. ANVIL's
`rN`-named slices stay recorded in `CHANGES.md` and `MEMORY.md` as before —
task-tree adoption does not change how `rN` slices land.

## Commit Workflow Tie-In

When a commit completes a task-tree leaf, `COMMIT.md`'s checklist still
applies in full. The only additional rule is:

- The commit subject or first body line must include the leaf ID
  (e.g., `HIERARCHY-AWARE-IDENTITY.1`).
- The owning `docs/tasks/<TREE>.md` file must be updated in the same commit
  with the leaf's new status, verification log entry, and commit-log entry.

For commits that are **not** task-tree-managed (linear `rN` slices, isolated
doc edits, workflow tweaks), no leaf ID is required.

## Copying This Workflow To Another Project

The detailed project-adoption checklist lives in
[docs/TASK_TREE_README.md](TASK_TREE_README.md).
