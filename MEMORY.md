# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `STRUCTURED-EMISSION-EXPANSION.19a` **CasezMux masked priority-chain impl design-detail** commit — **DOCS-ONLY** (no `src/`/`tests/`/`examples/`/build touched) ⇒ **DUT byte-identical** + exempt from the code-scoped doctrine checks. A `DEVELOPMENT_NOTES.md` entry grounding decision `0029` (the NINTH surface) in the real code and pinning every `.19b` impl point (dynamic-selector `CasezMux` predicate; `Module.casez_mux_if_gates`; the in-place `emit/sv.rs` masked-chain branch `(sel & SW'h{care}) == SW'h{val}` + trailing `else W'h0`, **no `__cv`**, + the `:769` trailing-default suppression tweak, reusing the existing `constant_value`/`bitmask` helpers + the `metrics.rs`/`compact.rs` `care_mask = !wildcard_mask & sel_mask` idiom; run last after `case_mux_if`; the `casez_mux_if_emit_prob` knob+flag; the `num_emitted_casez_mux_if_chains` metric schema `1.16→1.17`; the metric-keyed `--casez-mux-if-gate` + a focus config zeroing BOTH `comb_mux_prob` and `case_mux_prob`; byte-identical/RNG) + the `.19b` proof plan. Records the empirical form selection (`sel ==? pattern` DISQUALIFIED — Yosys rejects `==?`; the masked-AND form ships). Prior: `6aeecf6` (`.18`). Push cadence: `origin/main` at `4564533` → **5 commits ahead** after this ⇒ no push (threshold 30, `feedback_push_cadence`).
- active_work_unit: **`STRUCTURED-EMISSION-EXPANSION` — NINTH surface designed (`.18`+`.19a` done); frontier → `.19b.1` (the live source change).** Lane stays `active`. EIGHT surfaces delivered end-to-end; the ninth (the wildcard `CasezMux` → masked `if`/`else if` priority chain, decision `0029`) is fully design-detailed against the real code; the live impl is next.
- next_action: **PNT — implement `STRUCTURED-EMISSION-EXPANSION.19b.1` (the live source change)** exactly per the `.19a` `DEVELOPMENT_NOTES.md` design-detail (no deviations). New `src/ir/casez_mux_if_emit.rs` (`annotate_casez_mux_if_gates`, the dynamic-selector `GateOp::CasezMux` predicate `operands[0]` not `Node::Constant` + `len >= 4`, excluding `CaseMux`/`case_mux_if`/all sibling marks + `param_env`, run last; ~9 lib proofs mirroring `case_mux_if_emit.rs`) + `Module.casez_mux_if_gates: BTreeSet<NodeId>` (`types.rs` after `:552`) + `ir/mod.rs` `pub mod casez_mux_if_emit;` + `Config::casez_mux_if_emit_prob` (six `config.rs` touch points) + `--casez-mux-if-emit-prob` flag (`main.rs` `:554`+`:1175`) + two guarded `gen/mod.rs` rolls after `case_mux_if` (`:170`/`:429`) + the `emit/sv.rs` `GateOp::CasezMux` arm (`:724`) masked-chain branch on `casez_mux_if_gates.contains(&idx)` + the `:769` trailing-default suppression tweak. No metric/schema bump at `.19b.1` (that is `.19b.2`: schema `1.16→1.17`). Validate: `cargo check`/`clippy -D warnings`/`fmt`; `cargo test --lib` (new proofs) + `--test snapshots` 6/6 byte-identical (default-off); forced `casez_mux_if_emit_prob=1.0` sweep Verilator `-Wall` Δ=0 + both Yosys + Icarus + ON-vs-OFF sim-equiv. Then `.19b.2` (metric+gate) / `.19b.3` (docs). Monitor RAM on heavy builds (`scripts/ram_guard.sh --threshold 90 -- <cmd>`). (Tracked-lower: the `agent-mcp.md` `analyze`-example schema-`1.11` drift.)
- handoff: repo handoff-ready after this commit — `scripts/check_doctrines.sh` green (4 doctrines); `git diff --check` clean; DOCS-ONLY ⇒ DUT byte-identical; decision `0029` + the `.19a` design-detail + probe evidence durable. origin/main at `4564533` (the `.17b.1` push); 5 commits ahead after this (`.17b.2a`+`.17b.2b`+`.17b.3`+`.18`+`.19a`). Co-staged: `DEVELOPMENT_NOTES.md` + `docs/tasks/STRUCTURED-EMISSION-EXPANSION.md` + `docs/TASK_TREE.md` + CHANGES + MEMORY.
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); one runner + one classifier not two (`feedback_full_factorization`); every capability opt-in + MCP-invocable + queryable + CLI-as-shim (decision `0017`); design the API for agents not humans (`feedback_api_for_agents_not_humans`); the book is the user-facing surface and must not drift (`feedback_book_doctrine`); decision/KM fact per durable capability or boundary. Doctrine-enforcement (decision `0026`): every doctrine = a deterministic check in `scripts/check_doctrines.sh`; code-scoped checks are scope-aware (non-code commits exempt).
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
