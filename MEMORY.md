# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `DOWNSTREAM-ADAPTER-EXPANSION.1` commit — the design ADR (decision `0020`) for the owner-directed usability lane 3 (north-star idea 3: more downstream tool reach). **Docs-only (no `src/` ⇒ DUT byte-identical; `tests/snapshots.rs` untouched).** Decision `0020`: ANVIL's downstream reach becomes pluggable through a **closed, compile-time `Adapter` registry** over the one `run_tool` runner + the one `tool_verdict` classifier (no second runner/classifier; the trait carries only `module_argv`/`design_argv` + a `warning` predicate + an optional SCHEMA-DERIVED `extract_facts` hook). Built-ins re-expressed byte-identically (`AcceptanceTool` not retired; the `verilator`/`yosys-<mode>`/`iverilog-compile` labels+argv are a hard byte-identical constraint for banked reports + `--resume`). Verdict unchanged ⇒ every added column is a new comparable verdict in `divergence::run` + a new selectable tool in `hunt`/`validate` **for free**. First adapter = **`sv2v`** (`.2b`, minimal accept/reject transpile column); second = **`slang`** (`.2c`, JSON-AST fact hook). Live-toolchain probe: `slang`/`sv2v`/`surelog` all **absent** (only verilator 5.046/yosys 0.64/iverilog 13.0) ⇒ first cuts ship structural + friendly absent-tool no-op + `#[ignore]` real-tool gate. API-completeness (`0017`): adapters selectable via the existing `tools` arg + queryable via the existing reports + a new SCHEMA-DERIVED adapter-catalog projection; CLI a shim. `.2` pre-split → `.2a` (registry refactor + catalog) / `.2b` (sv2v) / `.2c` (slang). Files: `docs/decisions/0020-*` (new) + INDEX + the tree + `docs/TASK_TREE.md` + DEVELOPMENT_NOTES + CHANGES + MEMORY + regen `KNOWLEDGE_MAP.md`. Prior: `6a441f7`=`BOOK-API-REFERENCE.1` (tree closed); `5d3c430`=`ACCEPTANCE-DIVERGENCE-HUNTING.2f`; `7142fd7` (**== origin/main**, pushed). Push cadence: `origin/main` at `7142fd7` → **6 commits ahead** after this (`.2c.2`,`.2d`,`.2e`,`.2f`,`BOOK-API-REFERENCE.1`,`DOWNSTREAM-ADAPTER-EXPANSION.1`); push at ~30 (`feedback_push_cadence`).
- active_work_unit: **`DOWNSTREAM-ADAPTER-EXPANSION`** — `.1` design ADR done; frontier now **`.2a`** (the closed `Adapter` registry refactor + the adapter-catalog query, built-ins re-expressed byte-identically). This is the next PNT pick (it IS a code leaf — owned by the tree). Other open lanes per `docs/TASK_TREE.md`: `KNOB-ERGONOMICS-AND-PRESETS.1`, `CI-PACKAGING-DISTRIBUTION.1`, `COVERAGE-STEERED-GENERATION.1`, `CAPABILITY-BREADTH-EXPANSION.1/.2` (all design-first ADRs, no code yet) + no-frontier capability trees (`STRUCTURED-EMISSION-EXPANSION`, `SEMANTIC-INTROSPECTION-EXPANSION`+`AGENT-MCP-EXPANSION`, `IDENTITY-DEEPENING`, `SIGNOFF-AUTOMATION-EXPANSION`).
- next_action: **PNT — `DOWNSTREAM-ADAPTER-EXPANSION.2a`** (the registry refactor + adapter-catalog query per decision `0020`; pure refactor, built-in labels/argv byte-identical, banked `tool_matrix` reports + `--resume` unchanged, snapshots 6/6). This is a real code leaf ⇒ full `cargo` gate applies. Or take an owner steer if one is given.
- handoff: repo handoff-ready — code state green as of `6a441f7` (`cargo test --lib` 543/0, snapshots 6/6, `tool_matrix` 75/0, `anvil` 12/0, `divergence_e2e` portable 1/0 + ignored 2/0; introspection schema `1.11`); this commit is docs-only so that state is unchanged; `mdbook build` clean; `check_memory_architecture` + KM gen/check green (**KM 51 facts** after `0020` folds in). Working tree clean after this commit.
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); one runner + one classifier not two (`feedback_full_factorization`); every capability opt-in + MCP-invocable + queryable + CLI-as-shim (decision `0017`); design the API for agents not humans (`feedback_api_for_agents_not_humans`); the book is the user-facing surface and must not drift (`feedback_book_doctrine`); decision/KM fact per durable capability or boundary.
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
