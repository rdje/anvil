# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (OVERWRITE this block; do not append history — that is git + the task trees)
- latest_commit: **`LIVE-DOC-REGISTRY-SHADOWS.3`** (pending) — **that tree is CLOSED**. Prior: `e37cec3`+`d4dd326` (`CHANGES-ENTRY-PLACEMENT.1`), `e873a6e`+`087ca7b` (`.2`), `abf7090`+`a73e5f9` (`.1`), `715019b`+`d9b9be4` (`BOOK-TEST-COUNT-SHADOWS.2` — **CLOSED that tree**), `1a6f276` (`.1`), `f335926`+`48cbc0a` (`COVERAGE-STEERED-GENERATION.6` — **CLOSED that tree**), `a461b4b`+`a195978` (`IR-TYPES-DECOMPOSITION.2`, resume-pointer refresh), `472b8da`+`2ee8500` (`PARITY-EXTRACTOR-ARM-SHAPE-GAP.1`), `218277d`+`30e731b` (`IR-TYPES-DECOMPOSITION.2`/`.1`, `KnobId` extracted), `bd7dba2`+`5775766`+`1287b8d`+`d6cca64`+`55b84d2`+`b50ff9e` (`README-POLICY-ADOPTION`, tree CLOSED, `README-GROWTH` = the 8th doctrine), `1b85589`+`fcb9e58` (`CSG.4c`), `4f41ba9`+`4a15ecd` (`.4b.2`), `6ab30f5`+`af8bd9c` (`.4b.1`), `ed3ed29`+`2d447c3` (`.5`), `d86475f`+`393c5e6`+`cc817d6`+`040ebc3` (`.4a`) — decisions `0034`/`0035`/`0036`/`0037`. Session history: `git log --oneline` (do not re-narrate it here).
- active_work_unit: **none mid-flight.** `LIVE-DOC-REGISTRY-SHADOWS.3` **CLOSED that tree**: `covers_set` (whole-file grep) became `covers_fenced_set`, reading only between inline `<!--enum:ID-->` markers, with a **missing fence a HARD FAILURE** so it cannot decay back; **13 fences / 11 files**; pair 4 now **7** sites (was 4), pair 3 and 1b fenced too. **ACCEPTANCE TEST PASSES** — the vacuity probe (*delete the enumeration, re-run*) now **fails at all 13**, including the 3 that passed with it deleted: **both pair-3 sites** (that pair had protected **nothing** its whole life) and `knobs.md`. **98 single-id controls — every id at every site, NOT a sample — all fail**, plus 2 empty-fence, 1 positive. **THE CONTROL CAUGHT THIS LEAF'S OWN DEFECT (1 wrong pass of 32):** `.2`'s honest-limit paragraph sat **inside** `architecture.md`'s fence and named `MEMORY-ARCH` a 2nd time, so dropping it from the registry list stayed green — **the vacuity bug reproduced at fence scale, inside its own fix, by the sentence explaining it**. Rule: **a fence marks the enumeration, not the discussion of it.** A sampled control would have MISSED it (7 of 8 ids at that site fail correctly) — the set was **partitioned**, `0034`'s lesson again. **Two more earned facts:** markers must be **INLINE** — an own-line HTML comment is a CommonMark *block* that splits the paragraph/table/list, i.e. it would silently change the rendered book (verified by reading the generated HTML, not by reasoning); and `0037`'s **bidirectional parity is WITHDRAWN** — 4 fences must enclose prose-bearing list items, so harvesting their ids would cry wolf; **exact parity needs both sides losslessly extractable, and readable docs are not.** Both landed as a dated **Correction** on `0037`, not an edit. Before it, `CHANGES-ENTRY-PLACEMENT` was registered (`.1` audited): this file's **two newest entries sit at its BOTTOM**, below the April *"Initial scaffold"*, in the convention retired in June — `CODE-CHANGE-EVIDENCE` checks *presence* in the staged list, never *position*, and is scope-aware so docs-only commits are exempt: **`0037`'s file-vs-region vacuity one layer up.** Still `active`: **`CHANGES-ENTRY-PLACEMENT`** (`.2`/`.3`/`.4`) and **`IR-TYPES-DECOMPOSITION`** (`.3`/`.4`, untouched).
- next_action: **`CHANGES-ENTRY-PLACEMENT.2`** — decide, and record, whether relocating a *misplaced but landed* `CHANGES.md` entry is permitted under `0031`'s append-only rule (correction of **position** vs rewrite of **content**), choosing between relocate / in-place forward pointer / re-publish at top citing the original; the decision must state what it does **not** license, so it cannot later be cited to justify a general sweep. Then `.3` applies it with byte-level proof the bodies are unchanged, and `.4` asks whether placement warrants a mechanism at all (applying `0033`'s three-part test **first** — *"a placement check cries wolf"* is a valid recordable outcome; note the trap that both offending commits were docs-only, so a code-scoped check would not have fired). Then **`IR-TYPES-DECOMPOSITION.3`** (the ~1830-line interning engine + ~38 tests — the big one; `.4` re-measures and may close). **Every commit subject must name a LEAF** (`TREE.N`) — the `commit-msg` hook rejects a bare tree name.
- in_flight_uncommitted: none once `.3` commits. **Docs + one check script**, so: `check_doctrines.sh` **8/8** after `git add`; `check_enumeration_parity.sh` green on all **15** pair checks; **113 controls** (13 vacuity + 98 single-id + 2 empty-fence) all failing correctly and 1 positive passing; duplicate-id audit clean across 13 fences; `mdbook build` clean **and the generated HTML inspected** (markers invisible, paragraphs/tables intact); `check_knowledge_map.sh` in sync (90 facts / 909 keys). `src/` untouched ⇒ DUT byte-identical by construction, so the `cargo test` / snapshot / byte-identity banks stand from `1a6f276` unchanged and were not re-run. `git_message_brief.txt` empty.
- blockers: none.

## Standing directives (owner-set; violating these is worse than not shipping)
- **A DEFECT IS ONLY HANDLED IF A TASK-TREE OWNS IT** (`2026-07-30`, owner-set). *"If you just record in MEMORY.md it will be lost there."* Correct, and mechanical: layer A is **overwrite-only with a hard cap** (`MEMORY_ARCHITECTURE.md` §71), so a finding parked here is queued for deletion by the next state refresh. Surfacing a finding is step one; **opening the tree is step two, in the same turn**. Never leave a defect in the resume pointer, a commit message, or prose alone.
- **DECIDE, DON'T ASK** (`2026-07-30`, owner-set). *"You are an elite coder and you know the roadmap and the objectives of ANVIL, so take SOTA and signoff-level decisions whenever you can."* Pick the next work unit, narrow or supersede a decision record, choose the mechanism — autonomously, and record the reasoning where it survives. Escalate only when proceeding under any assumption would be unsafe or would make the work useless if wrong. Surfacing a finding is still mandatory; turning it into a **question** is not.
- **NEVER REWRITE HISTORY** (`2026-07-29`, absolute, decision `0031`). No `rebase` / `--amend` of landed commits / `reset --hard` / `filter-branch` / force-push. `CHANGES.md` + `DEVELOPMENT_NOTES.md` are **never** retro-edited — append only; their pre-`0031` `/tmp` references stay raw. Owner: *"Keep it raw, keep honest, so that people can follow the whole history."* A swept history is a dishonest history.
- **THE SSD IS THE ONLY PROJECT VOLUME** (decision `0031`). ANVIL stores nothing on, and no live doc/code points at, `/tmp` · `/private/tmp` · `$TMPDIR` (macOS `/var/folders/…`, which a `"/tmp"` grep MISSES). Repo = `/dev/disk5s1`; boot = `/dev/disk3s1s1`. All ANVIL scratch resolves through `crate::paths::sandbox_root()` → `.cache/anvil-sandbox/`. Enforced by `NO-BOOT-VOLUME-REFS`; its **allow-list is load-bearing** (policy docs must name what they forbid; history must stay raw) — never "tighten" it into a history rewrite.
- **SHARED MEANS SHARED — use it, never duplicate.** `~/.cargo`, `~/.rustup`, `/opt/homebrew` tools stay in place; `CARGO_HOME`/`RUSTUP_HOME` at defaults. Owner: *"if something is shared, use the shared information."* Forking a shared cache costs disk twice and creates a second source of truth.
- **`~/Documents/github` is owner-owned** — the pre-move checkouts. No agent deletes or migrates it; the owner removes it themselves. Excluded from every audit.
- Harness runtime files (`/private/tmp/claude-501/…`, `~/.claude/…`) belong to the harness and cannot be moved from the repo — never *depend* on them; redirect real output to `.cache/scratch/`.

## Operating gotchas (earned the hard way — do not relearn)
- **Never clear `.cache/anvil-sandbox` while a test run is in flight.** It is the live sandbox root now, so clearing it deletes running tests' working dirs *and* their stdout/stderr capture files — surfacing as a `book_examples` failure with **empty** output. Clear before or after, never during. A failure with no captured output at all ⇒ suspect the harness's scratch before the code.
- **Never mass-rewrite strings across docs whose *subject* is that string.** A blanket `/tmp` sweep once turned decision `0030`'s own `reverify` into the meaningless `ls -d anvil-*`. Always allow-list the policy/history documents first.
- **`git checkout -- <file>` during a negative-control sweep discards unrelated edits to that file.** It silently cost a README citation that a commit message then claimed; `CHANGES.md` is append-only so the fix is to make the claim true in the next leaf, never to rewrite it. Re-read `git diff --cached --name-only` before writing commit prose. **Same class:** never baseline a metric against a file you have already edited — the README-policy tree was first registered with `1773 lines / 122,920 B`, measured *after* this session's own 2-line README edit (true HEAD: `1771 / 122,767`). Baseline from `git show HEAD:<file>`, not the worktree. **Same class, deleting docs (`0036`/`.2`):** (i) grep the **doctrine checks** for the file first — they encode invisible content requirements (`README.md` was a declared `ENUMERATION-PARITY` pair-4 site, so cutting one `--steer` bullet inside a 1141-line deletion would have failed the hook; the fix is to **drop the site**, decision `0033` R1, never to re-add a list "to keep the gate green" — that list grows one line per future category, which is how the file reached 1771); (ii) grep **`src/`** too — a doc comment can name where a fact is *cited* (two `tool_matrix.rs` comments said "cited in `README.md`"), and no docs-side check sees that lie; (iii) to prove nothing was lost, sweep **every** token in the deleted range (879 here), not a hand-picked phrase list — a working sweep's residue is made only of **composites of covered parts**, and an orphaned fact stands out in a set of 32 where it would hide in 879; (iv) a **line cap and a byte cap are not redundant** — the survivors were `10,297 B / 141 lines`, so the projected file was *over* the byte cap while at 74 % of the line cap (prose density: 118 B/line for a numbered list vs 57 for a path list). Trim to the cap; never raise the cap to fit the trim. **Same class, a negative control that PASSES on the first try (`CSG.6`):** three did, and all three were too weak to fail — a `datapath` → `datapathXX` mask that `covers_set` still **substring**-matches; a reshaped row for a category that has three rows; and a prediction never actually run. Check that a control *can* fail before trusting that it did. **And two hashes to recognise on sight:** `e3b0c44298fc…` is the SHA-256 of the **empty string** — if both sides of a byte-identity comparison "match" at it, they compared *nothing* (a run errored with stderr suppressed); and a comparison that hashes only stdout silently ignores `--metrics`/`--trace`, which write to **stderr**.
- **Under mutual exclusion with a fixed pass order, probability is PRIORITY, not intensity.** All nine `*_emit_prob` at `1.0` ⇒ `function_emit` (2nd) claims every admissible gate and 5 later surfaces emit ZERO — measured: `--profile structured-emission-max` sets 4 surfaces and emits **1**. Max *coverage* and max *diversity* are opposed; ANVIL wants diversity ⇒ calibrate by **max-min** (`0.25` maximises the least-represented surface). A tenth surface joins at the shared intermediate value, never at `1.0`. Decision `0032`.
- **Auditing a defect class — three rules, all earned by `SHADOW-ENUMERATION-SWEEP`.** **(0) Never write "currently correct" without MEASURING it, and treat a COUNT beside a list as a shadow OF that list.** The tree opened on *"nothing here is a live bug; correctness merely rests on diligence."* Measured, **five** omissions had already happened: the MCP `instructions` named 3 adapters and `validate`'s description 4 (vs 5 registered); `book/src/architecture.md` + the `doctrine-enforcement` KM card + `CODEBASE_ANALYSIS.md` each named 4 doctrines (vs 6). All five were **prose** — the copy nobody greps when adding an entry. A stale KM card is worst: it is read *instead of* re-deriving, so it misinforms rather than omits. Redundant counts (`"Live registry (6)"`, `"is four doctrines"`) are R1-by-**deletion**, never "gate the count too" — that keeps the shadow alive and spends a mechanism on it forever. **(1) Rank by what one omission CORRUPTS, never by list length, and check the CONSUMER's polarity before ranking the producer.** `merge_coverage` (149 entries) looked worst until measured: all merges monotone (`135 |=` + `13 extend` + `1 max`) ⇒ under-report only, and 134/149 fields are read as `if !saw_x` ⇒ a forgotten merge yields a *spurious* gap ⇒ the gate **bails loudly**. Silent surface = 15, fail-safe. The real S3 was a 15-term `||` chain: omit a gate from `fail_on_coverage_gap` and `:1490`'s `bail!` is disarmed ⇒ a gate that **cannot fail**, banked as `0030` evidence. **(2) Search from the AUTHORITATIVE SET, never from the shadow you found first.** `.2` swept for the *shape* of the known instance (a JSON `enum` array) ⇒ 4 MCP adapter-id copies; `.6` swept for the *content* (any line naming ≥ 2 registry ids) ⇒ **7**, and the 3 extra were the only ones that had **already rotted** (the server `instructions` named 3 adapters, `validate`'s description 4, vs a registry of 5). A shape-keyed search silently narrows the class to *structured* copies; prose passes all three tests too, and under decision `0017` a tool description **is** the contract. Corollary: a guard that lists the known sites is one more copy of the set — walk the real output instead. Decision `0033` (also: a list is a shadow only if derivable **∧ growth-coupled ∧** silent — the middle test is what keeps the two load-bearing allow-lists hand-written). **Rule (2) again, 2nd lane (`0034`):** before claiming *"all X funnel through one place"*, search the **effect** (the write/record/insert — here `knob_rolls.record(`, 8 sites/2 files) not the funnel (`roll_knob(`, 1 file, false premise) — a 2nd funnel is DEFINED by producing the same effect a different way. And a proof sampling ONE member of a set cannot detect the set is **partitioned** (`.2a` proved steering on a `cone.rs` knob; all 7 `hierarchy.rs` knobs were unsteered for 2 months).
- **The fixture agrees with you; the tool does not.** Three independent instances now (`DIFFERENTIAL-SIMULATION.3b.2` two-space `input  logic`; `PHASE-7 .2c.2b.1` `rem_euclid` vs `%`; `EVIDENCE-BANK-DURABILITY.5` a deriver reporting *2907 gaps* for an empty array). Run a new tool against **real** output before trusting it — a hand-made fixture is shaped by the same assumption as the parser.
- **Never parse a FORMATTER's output for a semantic set.** `ENUMERATION-PARITY`'s steering extractor read **7 of 8** categories for its whole life: `grep -oE '=> "[a-z]+"'` assumed a one-line match arm and **`rustfmt` blocked the one arm whose pattern got long enough to wrap**, so `=>` and the string landed on different lines. Measured across all six extractors: the five parsing structure a *tool* controls (bash array, Markdown column, directory listing, link syntax) are exact; the only one parsing `rustfmt` output was the only one wrong — and it degrades exactly when a category **grows**. Three corollaries: **(a) a count floor catches "matched nothing", not "matched most"** (floor 6, extractor 7) — when an extraction can be off by one, the floor must be the real count, which is safe because a floor is **shrink-coupled, not growth-coupled** and so fails `0033`'s three-part shadow test; **(b) a one-directional `covers_set` turns a missing extraction into a SILENT EXEMPTION** (an id never extracted is checked nowhere), so bias extractors toward over-matching and strip only provable noise — over-matching cries wolf and gets noticed, under-matching is invisible; **(c) measure whether the guarded thing actually drifted, separately from whether the guard works** — all four doc sites did name `datapath`, so this was a latent hole, not a live inconsistency, and saying otherwise would have been as wrong as missing it. **`grep -E` uses ERE: the interval is `{0,1}`, NOT `\{0,1\}`.** `sed` is BRE and wants the backslashes. This repo's scripts mix both dialects; the escaped form inside `grep -E` matches a literal brace and silently never fires. An extractor must die on a missing field, never fall through to something plausible.
- **A doctrine check must classify, never guess.** `EVIDENCE-CITATIONS` requires every `anvil-<name>` token to be in one of three buckets and fails closed on an unknown one; a heuristic would either miss a real uncited bank or cry wolf on prose, and a gate that cries wolf gets deleted. Its grandfathered list is pinned by count+SHA **because its membership is a historical fact** — unpinned, "just grandfather it" bypasses the doctrine in one line.
- **`cmd | tail` DESTROYS `cmd`'s exit status** (bash returns the last stage's). Bit twice on `2026-07-30`: `cargo test 2>&1 | tail -30` "passed" while proving nothing, and `git commit -F brief && truncate -s 0 brief` ran the `truncate` after the hook REJECTED the commit — emptying the message file. Redirect to a log and echo `$?` on its own line; never chain `&&` cleanup off a piped command.
- **Run `scripts/check_doctrines.sh` AFTER `git add`.** `git grep`/`git ls-files` see tracked content only, so a new file's contents are invisible to every structural check until staged. Three self-catches in two days were all this blind spot.
- **Anchor a path-prefix rewrite to a path start.** The same sweep, applied unanchored, also fired *inside* `target/tmp/…` (Cargo's `CARGO_TARGET_TMPDIR`, on-volume) and minted 10 paths to a directory that never existed. A wrong path that looks plausible survives review. And a gate built from the sweep's own search string shares its blind spot — write the check from the property ("a boot-volume path is absolute"), not the string.
- `verilator -Wall` on a fixed-filename dump always fails `DECLFILENAME` — pass `-Wno-DECLFILENAME`; it is a filename artifact, not a defect.

## Lane invariants (all lanes)
- Rules-first / no generate-then-filter (valid-by-construction); default-off + byte-identical wherever output could change; `tests/snapshots.rs` untouched by default.
- **No retirement** (`feedback_never_retire_strategies`); one runner + one classifier, never two (`feedback_full_factorization`).
- Every capability opt-in + MCP-invocable + queryable, CLI-as-shim (decision `0017`); design the API for agents, not humans (`feedback_api_for_agents_not_humans`).
- SEMANTIC-INTROSPECTION stays SCHEMA-DERIVED / no shadow simulator (decisions `0004`/`0011`).
- The book is the user-facing surface and must not drift (`feedback_book_doctrine`); a decision record or KM fact per durable capability or boundary.
- Doctrine-enforcement (decision `0026`): every doctrine is a deterministic check in `scripts/check_doctrines.sh`; code-scoped checks are scope-aware (non-code commits exempt).

## Validation policy
- Heavy builds run under `scripts/ram_guard.sh --threshold 90 -- <cmd>` (note the `--` separator); stop above 90% RAM and record it as an environment stop (decision `0003`).
- Workflow/memory/retrieval doc leaves may use focused functional checks; a full `cargo test` is not required for them per owner instruction.
