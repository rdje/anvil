# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `BUG-HUNT-ORCHESTRATION.2b.2a` commit (**code, `src/hunt/` + `src/downstream/`**) — folded the optional **cross-simulator** axis into `hunt::run`: `HuntRequest.diff_sim` runs `diff_sim::run_agreement` on each validate-clean artifact; a trace disagreement is a `cross_sim_mismatch` finding (`HuntFailure.diff_sim` carries the `DiffSimReport`; not minimized). Extracted the shared `downstream::generate_dut_artifact(cfg) -> (kind, top, sv)` (validate byte-identical). DUT byte-identical (snapshots 6/6). Prior: `5ca0006` = `.2b.1` fix (seed-threading); `f4fc9c8` = `.2b.1` (hunt lib core); `8a4fbab` = `.2a`; `b8c1e8b` = `.1` (decision `0018`); `59f248c` = `USABILITY-LANE-OWNERSHIP.1` (decision `0017`); … `b90bdff` = `.5` (**pushed** — `origin/main` at `b90bdff`). Working tree clean. Push cadence: `origin/main` at `b90bdff` → **20 commits ahead**; push at ~30 (`feedback_push_cadence`).
- active_work_unit: **`BUG-HUNT-ORCHESTRATION`** — `.1` design ADR (decision `0018`) **done**; `.2a` (diff-sim extract) **done**; `.2b.1` (loop core + seed-fix) **done**; `.2b.2` pre-split → `.2b.2a` (cross-sim fold + the shared `generate_dut_artifact` extract) **done** + `.2b.2b` (frontier). Frontier **`.2b.2b`** = the **reproducer-bundle emitter**: `HuntRequest.bundle_root: Option<PathBuf>`; on each finding write a directory `<bundle_root>/<run_id>/` with `repro.sv` (regenerated via `generate_dut_artifact`, or the minimized config's SV), `knobs.json` (the effective/minimized `Config`), `introspection.json`, `tool-logs/`, `hunt-verdict.json` (the `HuntFailure`), and a one-command `repro.sh`; `HuntFailure` gains a bundle ref. Then `.2c` (MCP `hunt` tool) → `.2d` (`anvil hunt` CLI, ANVIL's first subcommand + byte-identical guard) → `.2e` (real-tool gate + book/USER_GUIDE/README/KM). Design invariants: thin orchestrator (no new detector/minimizer), bundle = directory, sandbox caller-set (decision `0004`), every report field SCHEMA-DERIVED (decision `0017`). Six other lanes still `active` design-first (`ACCEPTANCE-DIVERGENCE-HUNTING`, `DOWNSTREAM-ADAPTER-EXPANSION`, `KNOB-ERGONOMICS-AND-PRESETS`, `CI-PACKAGING-DISTRIBUTION`, `COVERAGE-STEERED-GENERATION`, `CAPABILITY-BREADTH-EXPANSION`); plus no-frontier `STRUCTURED-EMISSION-EXPANSION` (5 surfaces; 6th = future `.11`), `SEMANTIC-INTROSPECTION-EXPANSION` + `AGENT-MCP-EXPANSION`, `IDENTITY-DEEPENING`, `SIGNOFF-AUTOMATION-EXPANSION`, `LOCAL-REFERENCE-CACHE`. Index: `docs/TASK_TREE.md`.
- next_action: **PNT — `BUG-HUNT-ORCHESTRATION.2b.2b`** (the reproducer-bundle directory emitter; `HuntFailure` gains the bundle path + `anvil://` resource refs). Touches `src/` ⇒ full `COMMIT.md` cargo gate + decision-`0017` gate. Reuse `generate_dut_artifact` for `repro.sv` + the existing `introspect` document builder for `introspection.json`; `repro.sh` = `anvil --seed <s> --config knobs.json > repro.sv` + the captured failing `argv`. Deferred 6th structured surface (multi-output `task`) = a future `STRUCTURED-EMISSION-EXPANSION.11` decision.
- handoff: repo handoff-ready — `.2b.2a` all gates green: `cargo check/clippy/fmt` clean, `cargo test` green (lib **512** = 505 + 7 hunt proofs / downstream 20/0 / tool_matrix 71+1 ignored / book_examples 3/3 / snapshots **6/6 byte-identical**; introspection schema `1.11`). `check_memory_architecture` + KM gen/check green (KM 46 facts). Working tree clean.
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); every capability opt-in; the hunt adds **no** new generator path / no detector / no minimizer (it composes the existing surfaces); decision/KM fact per durable capability or boundary.
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
