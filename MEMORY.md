# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `IDENTITY-DEEPENING.2a` commit (hash backfills next update; prior: `43e2a2d` = `IDENTITY-DEEPENING.1`; `5de8e91` = `SIGNOFF-AUTOMATION-EXPANSION.2b`). Working tree clean after this commit.
- active_work_unit: **`IDENTITY-DEEPENING`** (Lane 1, **`active`**). Lanes 2/3 at handoff/closed. `.1` (decision `0007`) **done**; `.2` split → `.2a` (design-detail) **done**. Frontier leaf: **`.2b`** (`pending`, **CODE**). `.3` (module-level sequential equivalence) `proposed`/future. Index: `docs/TASK_TREE.md`.
- next_action: **do `IDENTITY-DEEPENING.2b`** — implement `merge_bisimilar_flops` in `src/ir/compact.rs` per the `.2a` grounded design (see `DEVELOPMENT_NOTES.md` 2026-06-15 `.2a` entry for the full algorithm). Steps: (1) extract the post-`old_to_canonical_old` tail of `merge_equivalent_flops` into a shared `finalize_flop_merge(m, old_to_canonical_old, removed)` helper (keep exact pass byte-identical); (2) add `merge_bisimilar_flops`: bucket by `(width,reset_kind,reset_val,clock_domain)`, greatest-fixpoint partition refinement keyed on a QUOTIENT D-signature (existing `cone_proof` with `FlopQ{flop}` → `class_rep(flop)`), **fresh memos each refinement iteration** (NodeId-keyed memos assume fixed endpoints — stale = unsound), bucket cap `N_bisim_flops=64`, then `finalize_flop_merge`; (3) `Config::bisimulation_flop_merge: bool=false` threaded onto `Module` beside `identity_mode`, also requiring node-id/e-graph; (4) `Module::bisimulation_flops_merged`→`Metrics::bisimulation_flops_merged`; (5) order it AFTER `merge_equivalent_flops`, BEFORE FSM merge/compaction in `generate_leaf_module`; (6) rules-first gate test (`D_f=Q_g`,`D_g=Q_f`: exact removes 0, bisim removes 1) + knob-off snapshot 6/6 + downstream-clean bank (Verilator + both Yosys). Run fmt/check/clippy + focused tests. Continue PNT (no self-pause — `feedback_no_self_pause_until_trees_closed`).
- soundness (recorded, `0007`): reset base case + bisimulation step ⇒ corresponding Qs equal for all time (coinduction). Generalizes the exact self-hold + same-endpoint classes; excludes retimed state (not bisimilar), reset/domain/width mismatch, memory-state (`memory-identity-boundary`), over-budget cones (→ conservative split). BMC rejected (unsound). Whole-module reachable-product → `.3`.
- lane invariants: proof-not-resemblance bar; rules-first / no generate-then-filter; bounded by support/node/work budget (12-bit / 128-node / 131072-work) with structural fallback; **no retirement** of any landed merge class; default-off / byte-identical where a merge could change RTL; `--identity-mode relaxed` stays the real off-switch; single identity source stays `src/ir/compact.rs`. A Knowledge Map card captures each new identity fact/boundary (`.2b` adds one).
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current. **`.2b` is the next slice and is a substantial code change into the 4477-line identity engine — a fresh session is reasonable if focus is needed; the design is fully pinned so `.2b` is code-only.**
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 88` for heavy builds; full `cargo test` not required for docs/design leaves per owner resource policy (monitor RAM; stop above 90%, >95% reboots). For `.2b` (code) run fmt/check/clippy + focused tests + snapshot byte-identical guard. Push cadence: `origin/main` at `381ec01`; **16 commits ahead after this commit — under the ~30 threshold, no push yet** (`feedback_push_cadence`).

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
