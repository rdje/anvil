# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `LIVE-DOC-HYGIENE-BACKFILL.2` **backfill SEMANTIC-INTROSPECTION task-tree logs** commit — a **docs-only** change (task-tree files only), task-tree-owned by `.2`; **DUT byte-identical** (no `src/`). Closes the `LIVE-DOC-HYGIENE-BACKFILL` tree. In `docs/tasks/SEMANTIC-INTROSPECTION-EXPANSION.md`: backfilled the `.8a`/`.8b.1`/`.8b.2` (`fsm_provenance`) rows into both the Verification Log + Commit Log — reconstructed from each leaf's recorded Result/Verification/Commit + the real hashes (`cabe696`/`5499067`/`ce86560`), marked "(backfilled by …)"; corrected the stale Status labels `.5b.2` (`pending → done`), `.9` + `.9b` (`active → done`), and the `.3` frontier-table cell (`active → done`) — the file now has no leaf whose Status disagrees with its `done` Result (only the tree Metadata + root node stay legitimately `active`). Validation: `bash scripts/check_doctrines.sh` green (docs commit ⇒ code-scoped checks exempt); no `src/` ⇒ cargo unaffected. Prior: `4092782` (`.1`). Push cadence: `origin/main` at `4564533` → **26 commits ahead** after this ⇒ no push (threshold 30, `feedback_push_cadence`).
- active_work_unit: **None at a frontier — back at a PNT decision point.** `LIVE-DOC-HYGIENE-BACKFILL` is **closed** (`.1` book schema chapter `1.14 → 1.22` + `.2` task-tree log backfill). `SEMANTIC-INTROSPECTION-EXPANSION` stays `active` at a NO-FRONTIER boundary (NINE derived queries delivered end-to-end at schema `1.22`: the four named kinds + `flop_dependencies` `.6` + `memory_provenance` `.7` + `fsm_provenance` `.8` + `node_drivers` `.9` + `node_readers` `.10`). All flagged doc-hygiene debt is now cleared.
- next_action: **PNT at a no-frontier boundary (`feedback_pick_and_roll_at_no_frontier`) — self-select the next lane.** Candidates: stay in `SEMANTIC-INTROSPECTION-EXPANSION` (a per-FSM/per-memory **reach** — the fan-out of an FSM's `sel` / a memory's ports, the cone-level analog; or per-child-module provenance — recurse the gate-graph queries through child instances; each needs a new `.Na` design-detail leaf + a fresh derivation), or switch lanes — `STRUCTURED-EMISSION-EXPANSION` (a genuinely new emitted construct + a fresh `tool_matrix` probe + a decision record), `IDENTITY-DEEPENING` (deeper module-equivalence / factorization rungs), `SIGNOFF-AUTOMATION-EXPANSION` (new downstream tool columns / implicit-knob sweep axes). Doctrine: **task-tree-own the chosen leaf BEFORE any code**. Monitor RAM (`scripts/ram_guard.sh --threshold 90 -- <cmd>`).
- handoff: **repo handoff-ready after this commit** — `scripts/check_doctrines.sh` green (4 doctrines); `git diff --check` clean; DUT byte-identical (docs-only). The book's schema chapter is in sync (`1.22`) and the `SEMANTIC-INTROSPECTION-EXPANSION` task file is internally self-consistent (no stale Status, complete leaf↔commit logs). All flagged doc-hygiene debt cleared. A long, productive session (`.10a`/`.10b.1`/`.10b.2` delivering the ninth query end-to-end, then the `LIVE-DOC-HYGIENE-BACKFILL` tree, after a full bootstrap read) ⇒ a fresh session is reasonable before picking the next lane. origin/main at `4564533`; 26 commits ahead after this (push threshold 30 ⇒ still no push). Co-staged: `docs/tasks/SEMANTIC-INTROSPECTION-EXPANSION.md` + `docs/tasks/LIVE-DOC-HYGIENE-BACKFILL.md` + `docs/TASK_TREE.md` + CHANGES + MEMORY.
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); one runner + one classifier not two (`feedback_full_factorization`); every capability opt-in + MCP-invocable + queryable + CLI-as-shim (decision `0017`); design the API for agents not humans (`feedback_api_for_agents_not_humans`); SEMANTIC-INTROSPECTION stays SCHEMA-DERIVED / no-shadow-simulator (decisions `0004`/`0011`); the book is the user-facing surface and must not drift (`feedback_book_doctrine`); decision/KM fact per durable capability or boundary. Doctrine-enforcement (decision `0026`): every doctrine = a deterministic check in `scripts/check_doctrines.sh`; code-scoped checks are scope-aware (non-code commits exempt).
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
