# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `SEMANTIC-INTROSPECTION-EXPANSION.2b.2` commit (hash backfills next update; prior: `cc2b5bc` = `.2b.1`; `63d2622` = `.2a`; `a31bf94` = `.1`). Working tree clean after this commit. Push cadence: `origin/main` at `63d2622` → **2 commits ahead after this commit; push at ~30** (`feedback_push_cadence`).
- active_work_unit: **`SV-VERSION-TARGETING`** (**`active`**) is the primary remaining frontier. First up-opt SHIPS (`.3b.2a` done — live `union soft` overlay). Frontier: **`.3b.2b`** (the repo-owned matrix up-opt gate). The other lane, **`SEMANTIC-INTROSPECTION-EXPANSION`**, has its **first-query milestone delivered** (`.1`+`.2` done — the output support cone end-to-end: `src/introspect/analyze.rs` + the pure MCP `analyze` tool + schema `1.3`); tree stays `active` with **no active frontier** (future query kinds `input_reach`/`flop_reset_provenance`/`module_reachability` are open-ended `.3+`). `STRUCTURED-EMISSION-EXPANSION` `proposed`. `IDENTITY-DEEPENING.3b.2b` parked-but-fully-designed. Index: `docs/TASK_TREE.md`.
- next_action: continue PNT (no self-pause) → **`SV-VERSION-TARGETING.3b.2b`** — the repo-owned matrix up-opt gate (CODE, in `src/bin/tool_matrix.rs`): a scenario that fires `soft_union_slice_prob` under a `2023` target + a `saw_sv_version_2023_soft_union_upopt` coverage fact proving the live `union soft` overlay is emitted and Verilator-`--language 1800-2023`-accepted (Yosys/Icarus recorded no-op, not a failure). Read `docs/tasks/SV-VERSION-TARGETING.md` `.3b.2b` + the existing `--sv-version-gate`/`ScenarioSet::SvVersionSweep` in `src/bin/tool_matrix.rs` first; rules-first; bank a clean report. Matrix-only ⇒ DUT `.sv` byte-identical (snapshots untouched).
- `.2b.2` delivered (CODE+DOCS, DUT byte-identical): schema `1.2→1.3` + `DerivedAnalysisDocument`/`derived_analysis_document` (`src/introspect/mod.rs`) + the pure MCP `analyze` tool (`src/mcp/mod.rs` `run_analyze`: DUT-only; unknown query/target ⇒ `-32602`; cached in `CachedArtifact.analyses`; served `anvil://artifact/<run_id>/analysis/<query>`); schema-doc §6.7 + book + USER_GUIDE + KM fact. `cargo test --lib` 427/0/2; snapshots 6/6; clippy/fmt clean; mdbook+book_examples 3/3; KM in sync; `anvil-mcp` stdio e2e smoke clean. `.2b.1` (prior): the pure analysis core. `.3b.2a` (prior, SV lane): live `union soft` up-opt.
- **PARKED (not abandoned): `IDENTITY-DEEPENING`** (`active`, frontier `.3b.2b`) — cross-module whole-module sequential equivalence, **fully designed** (decision `0008` + `.3b.1` detail + `.3b.2a` `bisimulation_partition` helper landed `edae1b0`). Resume from `docs/tasks/IDENTITY-DEEPENING.md` `.3b.2b`. Owner redirected priority to `SV-VERSION-TARGETING`.
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); every capability is an opt-in knob; decision/KM fact per durable capability or boundary.
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`. The next code leaf (`SV-VERSION-TARGETING.3b.2b`) is matrix-only (`src/bin/tool_matrix.rs`) ⇒ snapshots stay 6/6 byte-identical; bank a clean `tool_matrix_report.json` with the new `saw_sv_version_2023_soft_union_upopt` fact (Verilator matching-mode; Yosys/Icarus no-op).

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
