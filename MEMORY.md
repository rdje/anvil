# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `STRUCTURED-EMISSION-EXPANSION.14` **pick procedural if/else mux surface + decision 0027** commit — docs-only / no source / **DUT byte-identical**. PNT at the structured-emission no-frontier boundary (`feedback_pick_and_roll_at_no_frontier`): picked the **SEVENTH** structured surface — a default-off, valid-by-construction **procedural `always_comb` `if`/`else`** emit-projection of a `Mux` gate (the 2:1 selection it renders today as the `(sel)?(a):(b)` ternary), written into a per-gate `<wire>__cv` output var + a passthrough `assign` (the decision-`0014` single-gate-task mechanism, but a bare `always_comb if/else`). The **first procedural-conditional construct** in the lane. Chosen over nested generate (no by-construction source — CSE shares the inner `{N{x}}` so the single-level loop already fires on `{M{y}}`) + interface/modport (empirically disqualified). Fresh probe (`/tmp/anvil-ifelse-probe.*`): Verilator `-Wall` 2012/2017/2023 + both Yosys (no warnings) + Icarus clean + iverilog sim-equiv to the ternary over 20000 vectors. Own `mux_if_emit_prob` knob + `num_emitted_mux_if_blocks` metric (schema `1.14 → 1.15` at impl) + `--mux-if-gate`/`saw_mux_if_emit` (`__cv` detection). Decision `0027` + INDEX row + tree leaf `.14` + `docs/TASK_TREE.md` row + `ROADMAP.md` lane #2 + `CHANGES.md`. Prior: `d3eb968`=`LIVE-DOC-ROADMAP-LANE-STATUS-ALIGNMENT.1`. Push cadence: `origin/main` at `605ec44` → **22 commits ahead** after this ⇒ no push (threshold 30, `feedback_push_cadence`).
- active_work_unit: **`STRUCTURED-EMISSION-EXPANSION` — frontier `.15`** (implement the seventh surface; `.14` design `done` by this commit). `.15` pre-split → `.15a` (design-detail: ground in `src/ir/task_emit.rs` + `to_sv_with_modules` Mux-render + the gate source; pin pass ordering, the `Module.mux_if_gates: BTreeSet<NodeId>` carrier, the emitter integration, the knob/metric/gate names) + `.15b` (impl, itself `.15b.1` live / `.15b.2` metric `num_emitted_mux_if_blocks` @ schema `1.14 → 1.15` + `--mux-if-gate` / `.15b.3` user docs). Other `active` lanes (`IDENTITY-DEEPENING`, `SIGNOFF-AUTOMATION-EXPANSION`, `SEMANTIC-INTROSPECTION-EXPANSION`, `DOWNSTREAM-ADAPTER-EXPANSION`, `KNOB-ERGONOMICS-AND-PRESETS`, `CI-PACKAGING-DISTRIBUTION`, `CAPABILITY-BREADTH-EXPANSION`, `LOCAL-REFERENCE-CACHE`) at no-frontier boundaries.
- next_action: **PNT → `STRUCTURED-EMISSION-EXPANSION.15a`** (the seventh-surface impl design-detail, no source: ground decision `0027` in the real `task_emit.rs` / `to_sv_with_modules` Mux-render / `gen/mod.rs` roll chain / `config.rs`, resolving the `.15a` open questions). Then `.15b.1` live surface (`mux_if_emit_prob` knob + `src/ir/mux_if_emit.rs` gen-time mark + the `always_comb if/else` + `<wire>__cv` + passthrough emitter rendering + lib proofs + forced-sweep downstream proof). Default-off / DUT byte-identical throughout.
- handoff: repo handoff-ready after this commit — `scripts/check_doctrines.sh` green (4 doctrines); `git diff --check` clean; docs-only so `cargo`/snapshots untouched ⇒ DUT byte-identical, no `src/`. Decision `0027` + INDEX + tree file + `docs/TASK_TREE.md` + `ROADMAP.md` + `CHANGES.md` + `MEMORY.md` + regenerated `KNOWLEDGE_MAP.md` co-staged.
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); one runner + one classifier not two (`feedback_full_factorization`); every capability opt-in + MCP-invocable + queryable + CLI-as-shim (decision `0017`); design the API for agents not humans (`feedback_api_for_agents_not_humans`); the book is the user-facing surface and must not drift (`feedback_book_doctrine`); decision/KM fact per durable capability or boundary. Doctrine-enforcement (decision `0026`): every doctrine = a deterministic check in `scripts/check_doctrines.sh`; code-scoped checks are scope-aware (non-code commits exempt).
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
