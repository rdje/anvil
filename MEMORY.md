# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `SEMANTIC-INTROSPECTION-EXPANSION.11b.1` **pure instance_provenance core** commit — a **code** change scoped to `src/introspect/analyze.rs` (+ `CODEBASE_ANALYSIS.md`), task-tree-owned by `.11b.1`; **DUT byte-identical** (no IR/generator change; `analyze.rs` not wired to any emit path; `tests/snapshots.rs` untouched). Implements the `.11a` design: `QUERY_INSTANCE_PROVENANCE` + `InstanceProvenance { instance, module, role, output_support: Vec<SupportCone> }` (reusing `SupportCone`) + the tenth `instance_provenance` `skip_serializing_if` vec (the 18 existing `DerivedAnalysis` literals gained `instance_provenance: Vec::new()`) + `design_instance_provenance` (descend into the child — `build_cone` per child output port inside the child, reusing `build_cone` unchanged) + `module_instance_provenance` (degenerate no-child-defs) + the `instance_provenance_analysis`/`instance_role_str` helpers. **NOT** in `supported_query_kinds()` yet (joins `run_analyze` dispatch in `.11b.2`). 10 in-crate proofs (incl. the cross-boundary descent proof: child output cone reports the CHILD's input "a"). Validation: `cargo test --lib introspect::analyze` 68/0; `cargo test --lib` 688/0/2; snapshots 6/6 byte-identical; clippy `-D warnings` + fmt clean. Prior: `2637185` (`.11a`). Push cadence: `origin/main` at `4564533` → **30 commits ahead** after this ⇒ **push due** (threshold 30, `feedback_push_cadence`) — push after this commit lands.
- active_work_unit: **`SEMANTIC-INTROSPECTION-EXPANSION.11` (`instance_provenance`) — in flight; frontier leaf `.11b.2` (MCP surface + schema `1.22 → 1.23`), `pending`.** `.11a` design + `.11b.1` pure core landed. `STRUCTURED-EMISSION-EXPANSION` stays `active` at a NO-FRONTIER boundary. Roadmap/codebase/mdBook aligned.
- next_action: **Land `SEMANTIC-INTROSPECTION-EXPANSION.11b.2` — wire the `instance_provenance` MCP surface (closes `.11b`/`.11`).** (1) `src/introspect/analyze.rs`: add `QUERY_INSTANCE_PROVENANCE` to `supported_query_kinds()`. (2) `src/mcp/mod.rs` `run_analyze`: branch by kind (`module`/`design_instance_provenance`) in BOTH the design + module arms + the empty-result guard checks `analysis.instance_provenance.is_empty()`; add `"instance_provenance"` to the `analyze_schema` `query` enum + the `target` description (a new instance-**name** target form) + the `analyze` tool description + the server `instructions`; 2 new mcp proofs (`analyze_returns_instance_provenance_and_caches_it` + `analyze_instance_provenance_unknown_target_is_invalid_params`). (3) `src/introspect/mod.rs`: `SCHEMA_VERSION 1.22 → 1.23` + the doc comment + the `"1.22" → "1.23"` test assertions (3 introspect + N mcp). (4) Docs: schema-doc §6.7 (tenth `instance_provenance` payload + `InstanceProvenance` + "one of ten parallel result vecs") + `1.22 → 1.23` changelog + "defines 1.23"/§7/checklist; book `agent-mcp` (analyze row + a worked example + the resource line + every JSON envelope `1.22 → 1.23`) + `api-tools`; USER_GUIDE + README (`--introspect` schema `1.23` + the tenth query); a new KM card `semantic-introspection-instance-provenance` + cross-links (regen KNOWLEDGE_MAP); ROADMAP lane status. (5) e2e `anvil-mcp` stdio smoke on a hierarchy config (a design seed → schema `1.23`, an instance with real child output cones; unknown instance ⇒ `-32602`). Validation: `cargo test --lib` + snapshots 6/6 + `mdbook build` + `book_examples` 3/3 + `check_doctrines` + KM in sync. Doctrine: `.11b.2` already owns the `src/` edit; SCHEMA-DERIVED/default-off/DUT byte-identical; monitor RAM.
- handoff: **repo handoff-ready.** `scripts/check_doctrines.sh` green; tree clean; DUT byte-identical. `SEMANTIC-INTROSPECTION-EXPANSION.11a` (design) + `.11b.1` (pure core) landed; the next leaf `.11b.2` is fully designed (see next_action — mirror the `.10b.2` cadence: registry + `run_analyze` dispatch in one commit + schema `1.22 → 1.23` + all docs + e2e smoke). origin/main at `4564533`; **30 commits ahead** after this ⇒ **push due** (threshold 30). A fresh session should `git push` then land `.11b.2` per COMMIT.md.
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); one runner + one classifier not two (`feedback_full_factorization`); every capability opt-in + MCP-invocable + queryable + CLI-as-shim (decision `0017`); design the API for agents not humans (`feedback_api_for_agents_not_humans`); SEMANTIC-INTROSPECTION stays SCHEMA-DERIVED / no-shadow-simulator (decisions `0004`/`0011`); the book is the user-facing surface and must not drift (`feedback_book_doctrine`); decision/KM fact per durable capability or boundary. Doctrine-enforcement (decision `0026`): every doctrine = a deterministic check in `scripts/check_doctrines.sh`; code-scoped checks are scope-aware (non-code commits exempt).
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
