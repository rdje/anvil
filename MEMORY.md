# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `SV-VERSION-TARGETING.3b.2a` commit (hash backfills next update; prior: `eca5d8b` = `.3b.1`; `154b2a7` = `.3a`; `ef3d94e` = `.2b.2b`). Working tree clean after this commit.
- active_work_unit: **`SV-VERSION-TARGETING`** (owner-directed capability lane, **`active`**, opened `2026-06-15`). `.1`+`.2`+`.3a`+`.3b.1` done. **First up-opt now SHIPS:** `.3b` → `.3b.1` (design-detail done) + `.3b.2` → **`.3b.2a` (done — live `union soft` overlay)** + `.3b.2b` (matrix gate, `proposed`). Frontier leaf: **`.3b.2b`**. Sibling lanes `proposed`: `STRUCTURED-EMISSION-EXPANSION`, **`SEMANTIC-INTROSPECTION-EXPANSION`** (owner directive `2026-06-16`: make deep semantic introspection first-class + MCP-queryable via a top-notch API — see below). Index: `docs/TASK_TREE.md`.
- next_action: continue PNT (no self-pause — `feedback_no_self_pause_until_trees_closed`) → implement **`.3b.2b`**: a repo-owned `tool_matrix --sv-version-gate` **up-opt scenario** (`soft_union_slice_prob = 1.0` + `sv_version 2023` + a slice-heavy config: `gate_struct_weight 10`, `min_width 4`, `max_width 16`) + a new `saw_sv_version_2023_soft_union_upopt` coverage fact that requires **Verilator `--language 1800-2023` acceptance** and records Yosys/Icarus as a **no-op** (the existing `.2b.2b` `saw_sv_version_*_targeted_acceptance` facts require Yosys-clean, which `union soft` cannot satisfy → the union scenario needs its OWN fact + lighting path in `compute_coverage_gaps`). Bank clean. All matrix code is in `src/bin/tool_matrix.rs`. THEN, per owner directive `2026-06-16`, activate **`SEMANTIC-INTROSPECTION-EXPANSION`** (make deep semantic introspection first-class + fully MCP-queryable through a clean top-notch API; `SCHEMA-DERIVED`, extends `AGENT-INTROSPECTION-MCP`/`AGENT-MCP-EXPANSION`).
- `.3b.2a` delivered (CODE, default-off byte-identical): `Config::soft_union_slice_prob` (serde-only, default 0.0, validated) + new emitter-surface `Module.soft_union_slice_gates` (BTreeSet<NodeId>, not hashed into identity; `Module` has no serde ⇒ zero snapshot impact) + `src/ir/soft_union.rs` gen-time pass (rolled in `generate_module`+`generate_design` like `aggregate_prob`; skips `param_env`) + `emit/sv.rs::soft_union_slice_overlay` (the `union soft` var decl + `u.w=src`/`gate=u.n` assigns, gated on `permits(Sv2023)`; down-gates below). Proofs: 8 lib `soft_union` tests (incl. emit divergence/down-gate) + byte-identity corpus green; banked `tests/sv_version_downstream.rs` = 159 overlays / 7 seeds Verilator `--language 1800-2023` clean + 2012 down-gate. snapshots 6/6, clippy/fmt/lib 412/0, mdbook+book_examples 3/3. Lockstep docs: book knobs+SV-version, USER_GUIDE, README, ROADMAP, CODEBASE_ANALYSIS, KM fact `sv-version-soft-union-upopt`.
- **PARKED (not abandoned): `IDENTITY-DEEPENING`** (`active`, frontier `.3b.2b`) — cross-module whole-module sequential equivalence, **fully designed** (decision `0008` + `.3b.1` detail + `.3b.2a` `bisimulation_partition` helper landed `edae1b0`). Resume from `docs/tasks/IDENTITY-DEEPENING.md` `.3b.2b`. Owner redirected priority to `SV-VERSION-TARGETING`.
- lane invariants (both lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); every capability is an opt-in knob; decision/KM fact per durable capability or boundary.
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`. The next code leaf (`.3b.2b`) is matrix-only (`src/bin/tool_matrix.rs`) ⇒ run `cargo test --bin tool_matrix` + the new gate banked; default matrix byte-identical. Heavy `tests/pipeline.rs` re-run once when `tool_matrix` changes. Push cadence: `origin/main` at `381ec01`; **28 commits ahead after this commit — push after 30 accumulate** (`feedback_push_cadence`).

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
