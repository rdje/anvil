# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `DOWNSTREAM-ADAPTER-EXPANSION.2c.1` commit — the `slang` downstream adapter + the `Adapter` trait's **first `extract_facts` JSON-AST hook** + MCP selectability/discoverability (decision `0020`); the additive half of the `.2c` split. **`src/downstream/mod.rs` + `src/mcp/mod.rs` + book API pages + README/USER_GUIDE allow-list; additive / default-off / DUT byte-identical (`tests/snapshots.rs` 6/6 untouched, no generator change).** Defaulted `Adapter::extract_facts(&AdapterRunCx,&ToolInvocation)->Option<AdapterFacts>` (built-ins + sv2v keep `None` ⇒ byte-identical) + `AdapterFacts`/`AdapterPortFact`/`AdapterInstanceFact` (port `ty`→serde `type`) + `AdapterTarget::stem()`; `AcceptanceTool::Slang` + `run_slang`/`run_slang_design` (`slang <sv> -q --ast-json <stem>.slang.json`; `--top <top>` design) + `SlangAdapter` (`supports_facts=>true`; `extract_facts` reads the side file → pure `parse_slang_ast_facts` over slang's **verified** `--ast-json` schema: top/ports/instances) + 5th registry entry + `first_tool_warning "slang"` arm; mcp 4 `tools` enums + `parse_validate_tools` allow-list → 5 (adapter_catalog 5 entries, slang first `supports_facts=true`, `present=false` locally). slang absent ⇒ schema+argv verified vs published docs; portable proof = parser vs synthetic fixture; `#[ignore]` real-tool gate is `.2c.2`. Gate: lib **553/0** (+4 net), downstream:: 30/0 + mcp:: 73/0, snapshots **6/6**, clippy/fmt clean, mdbook clean, mem-arch + KM green (52 facts), RAM-guarded. Prior: `a3c70b9`=`DAE.2b.2`; `af81e16`=`DAE.2b.1`; `7142fd7` (**== origin/main**, pushed). Push cadence: `origin/main` at `7142fd7` → **12 commits ahead** after this; push at ~30 (`feedback_push_cadence`).
- active_work_unit: **`DOWNSTREAM-ADAPTER-EXPANSION`** — `.1` ADR + `.2a` + `.2b` (sv2v) done; `.2c` split into `.2c.1` (slang adapter + the extract_facts hook + MCP — **done**) / `.2c.2`; frontier now **`.2c.2`** (the `tool_matrix --slang` column mirroring `--sv2v` + live report fact-surfacing of `extract_facts` + the `#[ignore]` real-tool gate `tests/slang_e2e.rs` + a user-facing chapter note + USER_GUIDE/README CLI surface + a KM card); `.2d+` (surelog/UHDM, commercial-wrapper) future. Other open lanes per `docs/TASK_TREE.md`: `KNOB-ERGONOMICS-AND-PRESETS.1`, `CI-PACKAGING-DISTRIBUTION.1`, `COVERAGE-STEERED-GENERATION.1`, `CAPABILITY-BREADTH-EXPANSION.1/.2` (design-first ADRs) + no-frontier capability trees (`STRUCTURED-EMISSION-EXPANSION`, `SEMANTIC-INTROSPECTION-EXPANSION`+`AGENT-MCP-EXPANSION`, `IDENTITY-DEEPENING`, `SIGNOFF-AUTOMATION-EXPANSION`).
- next_action: **PNT — `DOWNSTREAM-ADAPTER-EXPANSION.2c.2`** (refine acceptance at pick, mirroring `.2b.2`): `tool_matrix --slang`/`--slang-bin` column dispatched via `AcceptanceTool::Slang.adapter().run(&cx)` (`ModuleReport`/`DesignReport.slang: Option<ToolInvocation>` serde skip_serializing_if; checkpoint + `--resume` guard; tally + console line + `MatrixReport.slang_enabled`; `unit_divergence` inclusion) gated on a `tool_version` presence probe (friendly no-op); surface the `extract_facts` `AdapterFacts` in the slang column/report where the AST file is produced; `tests/slang_e2e.rs` portable public-API proof + `#[ignore]` real-tool gate; book (a structured/agent-mcp note) + USER_GUIDE + README CLI bullet + a `docs/knowledge/slang-adapter.md` KM card. Default-off ⇒ banked reports + `--resume` + snapshots 6/6 byte-identical.
- handoff: repo handoff-ready — code state green after this commit (`cargo test --lib` **553/0**, snapshots 6/6, downstream:: 30/0 + mcp:: 73/0 focused; introspection schema `1.11`); `mdbook build` clean; `check_memory_architecture` + KM gen/check green (**KM 52 facts**). Working tree clean after this commit.
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); one runner + one classifier not two (`feedback_full_factorization`); every capability opt-in + MCP-invocable + queryable + CLI-as-shim (decision `0017`); design the API for agents not humans (`feedback_api_for_agents_not_humans`); the book is the user-facing surface and must not drift (`feedback_book_doctrine`); decision/KM fact per durable capability or boundary.
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
