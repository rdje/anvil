# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `STRUCTURED-EMISSION-EXPANSION.4a` commit (design-detail leaf — grounds decision `0013`'s `generate for` loop surface in the real emitter + the gen-time-annotation precedent + splits `.4` into `.4a` done + `.4b` impl; **docs-only / no source change**). Prior: `c57d5b4` = `.3` (decision `0013`); `d6a8517` = `.2b.2c` (function-emit user docs). Working tree clean after this commit. Push cadence: `origin/main` at `63d2622` → **25 commits ahead after this commit; push at ~30** (`feedback_push_cadence`).
- active_work_unit: **`STRUCTURED-EMISSION-EXPANSION` tree `active`; `.4` is an `active` container; frontier = `.4b`** (implement the `generate for` loop surface, decision `0013`, grounded by `.4a`; pre-split `.4b.1` live / `.4b.2` gate+metric / `.4b.3` closeout at pick time if too broad). The first surface (combinational `function automatic`, `.1`+`.2`) is delivered end-to-end. Future surfaces (`task` [leading], nested/multi-level `generate`, `interface`/`modport`) are `.5+`, each its own decision. Index: `docs/TASK_TREE.md`.
- `.4a` delivered (docs-only design-detail): grounded decision `0013` in the real code and resolved all five points. The index-regular source already exists — `render_gate`'s `Concat` arm (`src/emit/sv.rs:1159`) detects `{N{x}}` via `operands.len() >= 2 && all-same-NodeId`. First cut = a `{N{x}}` replication `Concat` with a **1-bit lane** (`W == N` ⇒ `assign <wire>[gi] = <x>` byte-faithful; the one-hot `{W{sel}}` idiom), excluding `m.function_emit_gates` (run after function-emit). Gen-time `src/ir/generate_loop.rs annotate_generate_loop_gates` + `Module.generate_loop_gates`. Emit: a `generate_loop_gate` accessor + `genvar <wire>__gi; generate for (…;<gi>++) begin : <wire>__gen assign <wire>[<gi>] = <x>; end endgenerate` after the function-decl section + assign-loop `continue` suppression. `Config::generate_loop_emit_prob` config-file-only default `0.0` byte-identical; `num_emitted_generate_loops` metric in `.4b` bumps schema `1.8→1.9`. Gate = `tool_matrix --generate-loop-gate`/`saw_generate_loop_emit` (full Verilator + both Yosys). **Gate-shape risk** for `.4b`: the corpus must emit `{N{x}}` 1-bit replications (the one-hot mux-mask idiom) for the loops to fire.
- next_action: **PNT — execute `STRUCTURED-EMISSION-EXPANSION.4b`** (the live `generate for` loop surface, **first source change in this lane since `.2b.1`**): `Config::generate_loop_emit_prob` + `Module.generate_loop_gates` + new `src/ir/generate_loop.rs` (`annotate_generate_loop_gates` + the 1-bit-lane `{N{x}}` candidate predicate excluding function-emit marks + lib proofs) + two guarded gen-time call-site rolls (after function-emit) + `to_sv_with_modules` `generate_loop_gate` accessor + `render_generate_loop_block` + generate-block section + assign-loop inline-replication suppression + a forced `generate_loop_emit_prob=1.0` Verilator+both-Yosys+Icarus spot-check; then the `tool_matrix --generate-loop-gate` gate + `num_emitted_generate_loops` metric (schema `1.8→1.9`) + book/USER_GUIDE/KM. Default-off / DUT byte-identical (snapshots 6/6). Pre-split `.4b.1`/`.4b.2`/`.4b.3` if too broad for one signoff-quality commit. Doctrine: NO code change without a task-tree leaf owning it first — `.4b` owns it.
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); every capability is an opt-in knob; decision/KM fact per durable capability or boundary.
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
