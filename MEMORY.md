# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `STRUCTURED-EMISSION-EXPANSION.16` **pick the EIGHTH structured surface (CaseMux → if/else-if priority chain) + decision 0028** commit — **DOCS-ONLY / design leaf** (no `src/` touched) ⇒ **DUT byte-identical**. At the no-frontier boundary (`feedback_pick_and_roll_at_no_frontier`) the eighth structured surface is picked + designed: a default-off, valid-by-construction procedural `always_comb` `if`/`else if` **priority chain** projection of a `CaseMux` gate (the N-way selection it renders today as the parallel `case (sel) … default`), the recorded decision-`0027` follow-up. Distinct from the seventh surface (N-way chain vs single 2:1 `Mux` conditional) + the `case` render (sequential-priority vs parallel-match); **simpler** (a `CaseMux` is already an `always_comb`-written `logic` var ⇒ no `__cv` passthrough — only the body swaps `case…endcase` → `if…else`). Own `case_mux_if_emit_prob` knob + `num_emitted_case_mux_if_chains` metric (schema `1.15 → 1.16` at impl) + `--case-mux-if-gate`/`saw_case_mux_if_emit` (metric-keyed, no new text token); `CasezMux` masked chain = follow-up. Fresh probe (`scratchpad/probe8/`): Verilator `-Wall` 2012/2017/2023 + both Yosys (32 cells, no warnings) + Icarus clean + iverilog sim-equiv to the parallel `case` over 20000 vectors + exhaustive sweep (`EQUIV OK`). Decision `0028` + INDEX row + regenerated `KNOWLEDGE_MAP.md` (67 facts/628 keys) + tree split `.16`/`.17`/`.18+` + the stale parent `.15`/`.15b` flipped `active`→`done` + ROADMAP lane #2 + `docs/TASK_TREE.md` row. `check_doctrines` 4/4 PASS + `check_knowledge_map` + `check_memory_architecture` + `mdbook build` green; `git diff --check` clean. Prior: this `LIVE-DOC-TASK-TREE-INDEX-ALIGNMENT.1` commit. Push cadence: `origin/main` at `605ec44` → **28 commits ahead** after this ⇒ no push (threshold 30, `feedback_push_cadence`).
- active_work_unit: **`STRUCTURED-EMISSION-EXPANSION` — frontier `.17`** (impl of the eighth surface: `case_mux_if_emit_prob` knob + a `src/ir/case_mux_if_emit.rs` gen-time annotation + the `emit/sv.rs` structured-case `always_comb` body branched `case…endcase`→`if…else if` + the `num_emitted_case_mux_if_chains` metric @ schema `1.15 → 1.16` + `tool_matrix --case-mux-if-gate`/`saw_case_mux_if_emit` + book/USER_GUIDE/KM; pre-split `.17a` design-detail + `.17b` impl). SEVEN surfaces delivered (`.1`–`.15`); the EIGHTH is picked (`.16`). All other `active` lanes at no-frontier boundaries.
- next_action: **PNT into `STRUCTURED-EMISSION-EXPANSION.17a`** (the eighth-surface impl design-detail, no source): ground decision `0028` in a fresh read of `src/ir/mux_if_emit.rs` (the annotation-pass template), `src/emit/sv.rs` (the structured-case `always_comb` block ~`688`–`751`: branch the `case…endcase` body to the `if`/`else if` chain over the same `node_ref` operand refs + the same `default` literal), `src/gen/mod.rs` (the roll chain — run `annotate_case_mux_if_gates` after the seven existing passes), `src/config.rs`, `src/ir/types.rs`. Pin: the `Module.case_mux_if_gates: BTreeSet<NodeId>` carrier; the dynamic-selector candidate predicate (`render_static_structured_gate` returns `None`); the in-place body branch; the `case_mux_if_emit_prob` knob + `--case-mux-if-emit-prob` flag; the `num_emitted_case_mux_if_chains` metric (schema `1.15 → 1.16`); the `--case-mux-if-gate`/`saw_case_mux_if_emit` gate (metric-keyed) + a `case_mux_prob`-biased focus config; the byte-identical/RNG argument. Then `.17b` (impl, pre-split `.17b.1` live / `.17b.2` metric+gate / `.17b.3` docs). Monitor RAM on heavy builds (`scripts/ram_guard.sh --threshold 90 -- <cmd>`).
- handoff: repo handoff-ready after this commit — `scripts/check_doctrines.sh` green (4 doctrines); `git diff --check` clean; docs-only ⇒ DUT byte-identical; the prior `.15b.2`/`.15b.3` code+doc state holds (`cargo test --lib` 616 / `--bin tool_matrix` 89 / snapshots 6/6 / `--mux-if-gate` banked `/tmp/anvil-mux-if-gate-r1`; `mdbook build` + `check_knowledge_map` + `book_examples` 3/3 green). Co-staged: `docs/decisions/0028-…-case-mux-priority-chain.md` (new) + `docs/decisions/INDEX.md` + `KNOWLEDGE_MAP.md` (regenerated) + `docs/tasks/STRUCTURED-EMISSION-EXPANSION.md` + `docs/TASK_TREE.md` + `ROADMAP.md` + `CHANGES.md` + `MEMORY.md`.
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); one runner + one classifier not two (`feedback_full_factorization`); every capability opt-in + MCP-invocable + queryable + CLI-as-shim (decision `0017`); design the API for agents not humans (`feedback_api_for_agents_not_humans`); the book is the user-facing surface and must not drift (`feedback_book_doctrine`); decision/KM fact per durable capability or boundary. Doctrine-enforcement (decision `0026`): every doctrine = a deterministic check in `scripts/check_doctrines.sh`; code-scoped checks are scope-aware (non-code commits exempt).
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
