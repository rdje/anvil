# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `STRUCTURED-EMISSION-EXPANSION.6a` commit (design-detail leaf — grounds decision `0014`'s combinational `task automatic` surface in the real emitter + splits `.6` into `.6a` done + `.6b` impl; **docs-only / no source change**). Prior: `b90bdff` = `.5` (decision `0014`, **pushed** — `origin/main` is at `b90bdff`); `909c82a` = `.4b.3`. Working tree clean after this commit. Push cadence: `origin/main` at `b90bdff` → **1 commit ahead after this commit; push at ~30** (`feedback_push_cadence`).
- active_work_unit: **`STRUCTURED-EMISSION-EXPANSION` tree `active`; `.6` is an `active` container; frontier = `.6b`** (implement the `task automatic` surface, decision `0014`, grounded by `.6a`; pre-split `.6b.1` live / `.6b.2` metric+gate / `.6b.3` docs per the `.4b` precedent). `.5` (decision `0014`) + `.6a` (design-detail) done. The FIRST (combinational `function automatic`, `.1`+`.2`) and SECOND (`generate for` loop, `.3`+`.4`) surfaces are delivered end-to-end. Future surfaces (nested/multi-level `generate`, `interface`/`modport`, richer tasks) are `.7+`, each its own decision. Index: `docs/TASK_TREE.md`.
- `.6a` delivered (docs-only design-detail): grounded decision `0014` in the real code, resolved all five points. The emitter already has the section template (`to_sv_with_modules` function-decl + generate-block sections + the per-gate assign-loop `continue`), and **`render_gate_function_body` is reused verbatim** as the task body. (1) Integration = output-var + passthrough-`assign` (keep `<wire>` a net, add `logic <wire>__tv`, `always_comb <wire>__t(<wire>__tv, …)`, swap the gate's assign RHS to `<wire>__tv`; `<wire>`-as-var rejected first cut). (2) Gen-time `src/ir/task_emit.rs annotate_task_emit_gates` + `Module.task_emit_gates`, the function-emit predicate plus exclusion of `function_emit_gates`/`generate_loop_gates`/`soft_union_slice_gates`, run after generate_loop. (3) `task automatic <wire>__t(output …, input …)` decl + `always_comb` call + assign-RHS swap. (4) `Config::task_emit_prob` config-file-only default `0.0`. (5) `num_emitted_combinational_tasks` metric (schema `1.9→1.10`) + `tool_matrix --task-emit-gate`/`saw_combinational_task_emit`.
- `.5` (prior) delivered (decision `0014`): the third surface = a combinational `task automatic` (the decision `0012` single-gate parallel, but a procedural `task` called from `always_comb`). Empirically grounded clean across Verilator `-Wall` + both Yosys + Icarus (both integration forms). Rules-first / default-off / mutually exclusive with the sibling projections / combinational only / structured + `Slice` excluded.
- next_action: **PNT — execute `STRUCTURED-EMISSION-EXPANSION.6b`** (the live `task automatic` surface — first source change since `.4b.1`; pre-split `.6b.1` live / `.6b.2` metric+gate / `.6b.3` docs per the `.4b` precedent, implement `.6b.1` first): `Config::task_emit_prob` (default `0.0`) + `default_task_emit_prob()` + Default + `0.0..=1.0` validation; `Module.task_emit_gates: BTreeSet<NodeId>`; new `src/ir/task_emit.rs` (`annotate_task_emit_gates` + the function-emit candidate predicate **plus** exclusion of `function_emit_gates`/`generate_loop_gates`/`soft_union_slice_gates`; `param_env` skipped; lib proofs) + `src/ir/mod.rs` registration; two guarded gen-time call-site rolls in `generate_module` + `generate_design` **after** the generate_loop roll; `src/emit/sv.rs` `task_emit_gate` accessor + a `task automatic <wire>__t(output logic[W-1:0] o, input …); o = <render_gate_function_body(op,widths)>; endtask` decl section + `logic <wire>__tv;` + `always_comb <wire>__t(<wire>__tv, <operand refs>);` + the assign-RHS swap to `<wire>__tv`; a forced `task_emit_prob=1.0` Verilator+both-Yosys+Icarus spot-check (bank `/tmp/anvil-te-r1/`). Then `.6b.2` (metric `num_emitted_combinational_tasks` + schema `1.9→1.10` + `tool_matrix --task-emit-gate` gate) + `.6b.3` (book/knobs/USER_GUIDE/README/KM closeout). Default-off / DUT byte-identical (snapshots 6/6). Doctrine: NO code change without a task-tree leaf owning it first — `.6b` owns it. NOTE: heavy multi-commit implementation — a fresh session is reasonable here (repo handoff-ready: tree clean, pushed through `.5`, self-checks green).
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); every capability is an opt-in knob; decision/KM fact per durable capability or boundary.
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
