# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `CAPABILITY-BREADTH-EXPANSION.2b.2b` **Mealy FSM `tool_matrix` gate** commit — **default-off ⇒ DUT byte-identical**. `src/bin/tool_matrix.rs` gains the `saw_mealy_fsm_design` coverage fact + the focused `phase6_mealy_fsm` scenario (= `phase6_fsm` + `fsm_mealy_prob=1.0`, the input-dependent `(state_q, sel)` decode) folded into the `Phase4Hierarchy` set with detection/merge/gap + a `phase6_mealy_fsm_scenario_is_non_vacuous` test, mirroring the `phase6_fsm`/`phase6_inferrable_memory` motif gates (scenario count 222→225 = one tuple ×3 strategies; gate designs 888→900). Proven downstream-clean by a focused harness-faithful run of the exact `phase6_mealy_fsm` scenario (Verilator `--top-module` + Yosys both modes + Icarus); the `--phase4-hierarchy-gate` run enforces `coverage_gaps=[]` incl. `saw_mealy_fsm_design` (gap-enforcement + non-vacuity unit-proven; 224 non-Mealy scenarios unchanged from the r87/`phase6-fsm-p1` banks; the full bank is a long-running regression run separately). Prior: `c8b0562`=`…2b.2a` metric+schema `1.13`; `dc7df68`=`…2b.1` mechanism; `10f53ad`=`…2a` ADR. Push cadence: `origin/main` at `605ec44` → **4 commits ahead** after this ⇒ no push (threshold 30, `feedback_push_cadence`).
- active_work_unit: **`CAPABILITY-BREADTH-EXPANSION.2b` active** — `.2b.1` (mechanism) + `.2b.2` (`.2b.2a` metric/schema + `.2b.2b` gate) **done**; only `.2b.3` (user-facing docs) remains to close `.2b`. `.1` (next SV up-opt) deferred-not-retired (the `.2a` probe: enum/typedef + packed multidim arrays NOT version-distinctive per decision `0010`).
- next_action: **PNT — pick `CAPABILITY-BREADTH-EXPANSION.2b.3`** (the Mealy user-facing docs): `book/src/sequential.md` (Moore vs Mealy + a byte-verified example) + `book/src/knobs.md` (`fsm_mealy_prob`) + `USER_GUIDE.md` (the `--fsm-mealy-prob` row) + README "Current CLI truth" + a KM how-to card; **incl. the deferred schema-`1.12→1.13` book-example refresh** in `api-tools.md`/`agent-mcp.md`/`api-introspection.md` (the coverage-steered-lane precedent). `mdbook build` clean. Re-check `docs/TASK_TREE.md` before picking.
- handoff: repo handoff-ready after this commit — `cargo check --all-targets` ✓, `cargo test` green (tool_matrix bin 79/0 incl. the new non-vacuous test, lib 589/0, snapshots 6/6 byte-identical), clippy `--all-targets -D warnings` + fmt clean; focused harness-faithful downstream proof (seed 7, the exact `phase6_mealy_fsm` shape) ACCEPT warning-clean across Verilator `--top-module` + Yosys both modes + Icarus `-g2012 -s top`; gate wiring + non-vacuity proven by the tool_matrix unit tests; the `--phase4-hierarchy-gate` run enforces `coverage_gaps=[]` incl. `saw_mealy_fsm_design` (gap-enforcement + non-vacuity unit-proven; 224 non-Mealy scenarios unchanged from r87/`phase6-fsm-p1`); `check_memory_architecture` + KM gen/check green.
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); one runner + one classifier not two (`feedback_full_factorization`); every capability opt-in + MCP-invocable + queryable + CLI-as-shim (decision `0017`); design the API for agents not humans (`feedback_api_for_agents_not_humans`); the book is the user-facing surface and must not drift (`feedback_book_doctrine`); decision/KM fact per durable capability or boundary.
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
