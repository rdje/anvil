# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `LIVE-DOC-HYGIENE-BACKFILL.1` **book schema chapter 1.14 → 1.22** commit — a **docs-only** change (book + a new task tree), task-tree-owned by `.1`; **DUT byte-identical** (no `src/`). `book/src/api-introspection.md` was **stale at schema `1.14`** (predating the `.15`–`.22` bumps); rewritten to `1.22` — the envelope JSON example + the field-table "currently" row + the stability-contract section, with the MINOR-bump narrative enumerating the **nine** `analyze` query kinds + the metric-count bumps and cross-linking the canonical §7 changelog in `docs/AGENT_INTROSPECTION_SCHEMA.md`. Sweep caught + fixed a stale `"schema_version": "1.21"` `output_support` example at `book/src/api-tools.md:165` (missed by `.10b.2`). Post-fix `book/src` sweep: no remaining non-`1.22` schema example / "current schema" claim. New tree `LIVE-DOC-HYGIENE-BACKFILL` registered (`.1` done, `.2` pending). Validation: `mdbook build book` clean; `bash scripts/check_doctrines.sh` green (docs commit ⇒ code-scoped checks exempt). Prior: `e492f3e` (`.10b.2`). Push cadence: `origin/main` at `4564533` → **25 commits ahead** after this ⇒ no push (threshold 30, `feedback_push_cadence`).
- active_work_unit: **`LIVE-DOC-HYGIENE-BACKFILL` — closing flagged doc-drift; active frontier `.2`.** `.1` (book schema chapter `1.14 → 1.22`) done. `.2` = task-tree log integrity in `docs/tasks/SEMANTIC-INTROSPECTION-EXPANSION.md`: backfill the missing `.8a`/`.8b.1`/`.8b.2` Verification-Log + Commit-Log rows + correct the stale `.5b.2` (`pending → done`) + `.9` (`active → done`) Status labels. `SEMANTIC-INTROSPECTION-EXPANSION` itself stays `active` at a no-frontier boundary (NINE derived queries delivered, schema `1.22`).
- next_action: **Implement `LIVE-DOC-HYGIENE-BACKFILL.2`** — in `docs/tasks/SEMANTIC-INTROSPECTION-EXPANSION.md` only: (1) add the `.8a`/`.8b.1`/`.8b.2` rows to the Verification Log + Commit Log (reconstruct each from its leaf's recorded Result/Verification/Commit fields + the CHANGES.md `.8*` entries — they ARE present in the leaf bodies + Frontier table, just missing from the two logs); (2) flip the `.5b.2` leaf Status `pending → done` and the `.9` leaf Status `active → done` (their Result/Verification/Commit already say done). Then mark `.2` done, close the `LIVE-DOC-HYGIENE-BACKFILL` tree, CHANGES + MEMORY, commit. Docs-only / DUT byte-identical. After that, PNT the next lane (a per-FSM/per-memory reach or per-child-module provenance in SEMANTIC-INTROSPECTION; or switch to STRUCTURED-EMISSION / IDENTITY-DEEPENING / SIGNOFF-AUTOMATION). Monitor RAM (`scripts/ram_guard.sh --threshold 90 -- <cmd>`).
- handoff: **repo handoff-ready after this commit** — `scripts/check_doctrines.sh` green (4 doctrines); `git diff --check` clean; DUT byte-identical (docs-only). The book's schema chapter is back in sync (`1.22`, nine `analyze` queries). Remaining flagged hygiene (owned by `.2`, next): the `.8a`/`.8b.1`/`.8b.2` log rows + the `.5b.2`/`.9` stale Status labels in `SEMANTIC-INTROSPECTION-EXPANSION.md`. origin/main at `4564533`; 25 commits ahead after this. Co-staged: `book/src/api-introspection.md` + `book/src/api-tools.md` + `docs/tasks/LIVE-DOC-HYGIENE-BACKFILL.md` + `docs/TASK_TREE.md` + CHANGES + MEMORY.
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); one runner + one classifier not two (`feedback_full_factorization`); every capability opt-in + MCP-invocable + queryable + CLI-as-shim (decision `0017`); design the API for agents not humans (`feedback_api_for_agents_not_humans`); SEMANTIC-INTROSPECTION stays SCHEMA-DERIVED / no-shadow-simulator (decisions `0004`/`0011`); the book is the user-facing surface and must not drift (`feedback_book_doctrine`); decision/KM fact per durable capability or boundary. Doctrine-enforcement (decision `0026`): every doctrine = a deterministic check in `scripts/check_doctrines.sh`; code-scoped checks are scope-aware (non-code commits exempt).
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
