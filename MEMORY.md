# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `CAPABILITY-BREADTH-EXPANSION.2b.3` **Mealy user-facing docs** commit (docs-only) — **DUT byte-identical** (no code). `book/src/sequential.md` "FSM outputs: Moore vs Mealy" + a byte-verified runnable example (`cargo run --release -- --seed 3 --fsm-prob 1.0 --fsm-mealy-prob 1.0 …`; passing in `tests/book_examples`; the emitted module is Verilator -Wall 2012/2017/2023 + Yosys both + Icarus clean); `knobs.md` `fsm_mealy_prob` (entry + metric-table row + corrected stale "Mealy not emitted today" line); `USER_GUIDE`/`README` `--fsm-mealy-prob`; new KM card `docs/knowledge/fsm-mealy-outputs.md` (working reverify, cross-linked to decision `0024`); the deferred introspection schema `1.12 → 1.13` book-example refresh (`api-tools`/`agent-mcp`/`api-introspection`/`api-reference`; coverage_readout provenance kept at `1.12`). **`.2b` done, and the whole Mealy strand `.2` is done.** Prior: `b8ee1ce`=`…2b.2b` gate; `c8b0562`=`…2b.2a` metric+schema `1.13`; `dc7df68`=`…2b.1` mechanism. Push cadence: `origin/main` at `605ec44` → **5 commits ahead** after this ⇒ no push (threshold 30, `feedback_push_cadence`).
- active_work_unit: **`CAPABILITY-BREADTH-EXPANSION` tree `active`, but the Mealy strand `.2` is fully done** (`.2a` ADR + `.2b.1` mechanism + `.2b.2a` metric/schema + `.2b.2b` gate + `.2b.3` docs). The only remaining child is **`.1` (SV up-opt breadth)**, which is **deferred-not-retired**: the `.2a` probe found the named candidates (enum/typedef, packed multidim arrays) accepted at every Verilator `--language` mode + Yosys + Icarus ⇒ not version-distinctive (decision `0010`); the genuinely-2023 clean space with the installed tools is thin (essentially `union soft`, shipped).
- next_action: **PNT — the `CAPABILITY-BREADTH-EXPANSION` tree's only open child `.1` is deferred** (no genuinely-version-distinctive up-opt with the installed tools). Per `feedback_pick_and_roll_at_no_frontier`, pick the next lane: re-scan `docs/TASK_TREE.md` for another `active` tree with a live frontier (e.g. the usability/identity-deepening/structured-emission lanes) and PNT into it; or, if `.1` is to be advanced, it needs a fresh genuinely-2023 construct or a rescope to `union soft` breadth (design-first ADR before any code).
- handoff: repo handoff-ready after this commit — docs-only slice; `mdbook build` clean; `tests/book_examples::every_runnable_book_bash_block_succeeds` green (exercises the new Mealy example); KM regen + check green (60 facts); `check_memory_architecture` green; code unchanged since `b8ee1ce` (lib 589/0, tool_matrix 79/0, snapshots 6/6 from that commit still hold).
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); one runner + one classifier not two (`feedback_full_factorization`); every capability opt-in + MCP-invocable + queryable + CLI-as-shim (decision `0017`); design the API for agents not humans (`feedback_api_for_agents_not_humans`); the book is the user-facing surface and must not drift (`feedback_book_doctrine`); decision/KM fact per durable capability or boundary.
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
