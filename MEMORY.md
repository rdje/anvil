# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `SEMANTIC-INTROSPECTION-EXPANSION.14b.1` **node_reach pure core** commit — a **code** change, task-tree-owned by `.14b.1`; **DUT byte-identical** (no IR/generator change; `analyze.rs` read-mostly, not wired to any emit path; `tests/snapshots.rs` untouched). The pure core of the **thirteenth** derived `analyze` query, `node_reach` (the **transitive combinational fan-OUT** of a node addressed `"node:<id>"`: `reaches_outputs` output ports + `reaches_flops` flop `D` cones + `fanout_targets`) — **NOT yet wired** to the MCP surface (that is `.14b.2`). Added `QUERY_NODE_REACH` + `NodeReach` (node-family header + `ReachResult` payload; no new leaf type) + the thirteenth `DerivedAnalysis.node_reach` field + `module_node_reach`/`design_node_reach` + the `node_reach_with` driver (transpose operands → reader index, forward-closure BFS, classify sinks; register boundary automatic — a flop is not a `Gate` ⇒ no reader edge) + the `node_reach_analysis` constructor; 21 literals gained `node_reach: Vec::new()`. `supported_query_kinds()` unchanged. 8 in-crate proofs (incl. the load-bearing `node_reach ↔ input_reach` consistency proof + register-boundary + instance-input-only boundary). CODEBASE_ANALYSIS analyze.rs block amended (thirteen query kinds, node_reach marked not-yet-wired). Validation: `cargo test --lib introspect::analyze` 92/0; `cargo test --lib` 718/0/2 (+8); snapshots 6/6 byte-identical; clippy `-D warnings` + fmt + `cargo check --lib` clean. Prior: `a675263` (`.14a`). Push cadence: `origin/main` at `a767415`; **9 commits ahead** after this ⇒ no push (threshold 30).
- active_work_unit: **`SEMANTIC-INTROSPECTION-EXPANSION.14b.2` (the `node_reach` MCP surface + schema `1.25 → 1.26`)** — the next leaf. `.14a` design + `.14b.1` pure core done. `STRUCTURED-EMISSION-EXPANSION` also `active` at a NO-FRONTIER boundary (nine structured surfaces).
- next_action: **Implement `.14b.2`** (wire `node_reach` to the surface): add `QUERY_NODE_REACH` to `analyze::supported_query_kinds()` AND branch `run_analyze` by kind (`module`/`design_node_reach`) in BOTH arms in the same commit; update the empty-result → `-32602` guard to check `analysis.node_reach` for this kind; bump `SCHEMA_VERSION 1.25 → 1.26` in `src/introspect/mod.rs` + the doc comment + the `"1.25"` schema_version test-assertion bumps (≈18: ~3 introspect + ~15 mcp); add `node_reach` to the `analyze_schema` query enum + refresh the tool/instructions descriptions (the `"node:<id>"` target form, paired with `node_readers`/`input_reach`); add 2 mcp proofs (`analyze_returns_node_reach_and_caches_it` + `analyze_node_reach_unknown_target_is_invalid_params`); docs — schema-doc §6.7 (thirteenth `node_reach` payload + the File/Producer rows + "one of thirteen parallel result vecs") + the `1.25 → 1.26` changelog + the version statements; book `agent-mcp` (analyze row + a `node_reach` worked example + the resource line + every JSON envelope `1.25 → 1.26`) + `api-tools`/`api-introspection`/`api-reference`/`api-resources-prompts`; USER_GUIDE + README + TOOLBOX (thirteen kinds + schema 1.26); a new KM card `semantic-introspection-node-reach` (+ cross-link; regenerate `KNOWLEDGE_MAP`); CODEBASE_ANALYSIS (analyze.rs block "now wired" + schema-history `1.25 → 1.26`); ROADMAP lane status (`.14` done). Run the e2e `anvil-mcp` stdio smoke (a seed → schema 1.26, a node's reach with the output/flop sinks; unknown target → `-32602`) — it catches spec-vs-reality bugs. Doctrine: SCHEMA-DERIVED/default-off/DUT byte-identical; monitor RAM (`scripts/ram_guard.sh --threshold 90 -- <cmd>`).
- handoff: **repo handoff-ready (mid-slice, `.14b.1` committed).** `scripts/check_doctrines.sh` green (4 doctrines); tree clean; DUT byte-identical. `node_reach` design + pure core done; `.14b.2` surface pending. origin/main at `a767415`; **9 commits ahead** after this; threshold 30 ⇒ no push. A fresh session should implement `.14b.2` (see next_action).
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); one runner + one classifier not two (`feedback_full_factorization`); every capability opt-in + MCP-invocable + queryable + CLI-as-shim (decision `0017`); design the API for agents not humans (`feedback_api_for_agents_not_humans`); SEMANTIC-INTROSPECTION stays SCHEMA-DERIVED / no-shadow-simulator (decisions `0004`/`0011`); the book is the user-facing surface and must not drift (`feedback_book_doctrine`); decision/KM fact per durable capability or boundary. Doctrine-enforcement (decision `0026`): every doctrine = a deterministic check in `scripts/check_doctrines.sh`; code-scoped checks are scope-aware (non-code commits exempt).
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
