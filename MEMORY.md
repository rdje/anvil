# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `ACCEPTANCE-DIVERGENCE-HUNTING.2f` commit (**closes the tree** — the real-tool e2e gate + version-axis surfacing decision + docs closeout). Only code/test artifact = `tests/divergence_e2e.rs` (portable `#[test]` injecting a synthetic accept/reject `ValidateReport` ⇒ `accept_reject` via the PUBLIC API; +2 `#[ignore]` tool-gated proofs: `anvil::divergence::run` all-agree 3-seed sweep ⇒ `diverged=false` across Verilator 5.046 + both Yosys modes; `anvil hunt --divergence` inert on a clean sweep). **Version-axis surfacing decision:** `DivergenceOptions.tool_specs` stays **library-only** — NOT exposed over MCP/CLI (an allow-listed kind + caller-supplied binary path is a larger trust surface than the fixed-binary tools, decision `0004`; safe exposure needs an operator-configured registry — future breadth in decision `0019` + DEVELOPMENT_NOTES; nothing retired). Pure-docs closeout: `synthesizability.md` "Acceptance divergence across tools", `agent-mcp.md` version-axis note, USER_GUIDE `--divergence` flag, README hunt bullet + MCP controlled-tools list, `docs/knowledge/acceptance-divergence.md` (KM 48→49), ROADMAP lane 2 delivered, tree + `.2` + root closed. `cargo test --test divergence_e2e` portable 1/0 + `--ignored` 2/0; snapshots 6/6 byte-identical; book_examples 3/3; mdbook clean; clippy/fmt green (under `ram_guard --threshold 90`); check_memory_architecture + KM gen/check OK. No schema bump (stays `1.11`). Default-off / DUT byte-identical. Prior: `0836655` = `.2e`; `4d17fc2` = `.2d`; `2fa2cc3` = `.2c.2`; `7142fd7` = `.2c.1` (**== origin/main**, pushed). Push cadence: `origin/main` at `7142fd7` → **4 commits ahead** after this (`.2c.2`,`.2d`,`.2e`,`.2f`); push at ~30 (`feedback_push_cadence`).
- active_work_unit: **NEW owner directive (`2026-06-17`): "Ensure ANVIL's API is accurately, thoroughly documented in the mdBook, using the most top-notch, industry-recommended way to describe APIs."** `ACCEPTANCE-DIVERGENCE-HUNTING` is **CLOSED** (all `.1`..`.2f` done; lane 2 delivered). Next = open a task tree for the API-documentation directive (pure-mdBook docs are task-tree-exempt, but the owner wants every activity tracked) covering the full agent/automation API: the MCP server (tools/resources/prompts) with per-tool JSON Schemas + request/response examples + JSON-RPC error codes, the `--introspect` document + schema `1.11` (`docs/AGENT_INTROSPECTION_SCHEMA.md`), the `analyze` query catalog, CLI + knobs, versioning/stability. Source of truth = `src/mcp/mod.rs` schemas. Other lanes still design-first `.1`: `DOWNSTREAM-ADAPTER-EXPANSION`, `KNOB-ERGONOMICS-AND-PRESETS`, `CI-PACKAGING-DISTRIBUTION`, `COVERAGE-STEERED-GENERATION`, `CAPABILITY-BREADTH-EXPANSION`. Index: `docs/TASK_TREE.md`.
- next_action: **Address the new API-documentation directive.** Audit the live API surface (MCP tools/resources/prompts in `src/mcp/mod.rs`, the introspection schema, `analyze` queries, CLI/knobs), then add a top-notch, industry-standard **API Reference** to the mdBook (every method: input/output JSON Schema + examples + error codes + stability/versioning). Register a tracking tree first; commit per `COMMIT.md`.
- handoff: repo handoff-ready — code state green (`cargo test --lib` 543/0, snapshots 6/6, `tool_matrix` 75/0, `anvil` 12/0, `divergence_e2e` portable 1/0 + ignored 2/0; introspection schema `1.11`); `check_memory_architecture` + KM gen/check green (**KM 49 facts**). Working tree clean after the `.2f` commit.
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); one classifier not two (`feedback_full_factorization`); every capability opt-in + MCP-invocable + queryable + CLI-as-shim (decision `0017`); design the API for agents not humans (`feedback_api_for_agents_not_humans`); the book is the user-facing surface and must not drift (`feedback_book_doctrine`); decision/KM fact per durable capability or boundary.
- in_flight_uncommitted: none after the `.2f` commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
