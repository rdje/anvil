# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `SEMANTIC-INTROSPECTION-EXPANSION.7a` **memory_provenance impl design-detail** commit — a **docs/design-only** change (no `src/`), task-tree-owned by `.7a`; **DUT byte-identical**. Opens `.7`, the **sixth** derived `analyze` query, `memory_provenance` (per-inferrable-memory port provenance) — the **second beyond decision `0011`'s four named kinds** and **the first query to open the documented opaque-`MemRead`-leaf boundary**. The `.7a` DEVELOPMENT_NOTES design-detail entry resolves Q1–Q5 grounded in the real `Memory`/`MemKind` IR + `analyze.rs`: a SIXTH parallel `memory_provenance: Vec<MemoryProvenance>` vec (reusing the existing `SupportCone` per `raddr`/`waddr`/`wdata`/`we` port — the five prior documents stay byte-identical), `build_cone`-per-port derivation, `"mem:<id>"` addressing, top-module module-vs-design (fmt closure needed), schema `1.18 → 1.19`. No new numbered decision (decision `0011` governs; the `.3a`/`.4a`/`.5a`/`.6a` precedent). Pre-split `.7b` → `.7b.1` (pure core) + `.7b.2` (surface). Validation: docs-only ⇒ `scripts/check_doctrines.sh` green (code-scoped checks exempt); DUT byte-identical. Prior: `fd8aa7c` (`.6b.2`). Push cadence: `origin/main` at `4564533` → **13 commits ahead** after this ⇒ no push (threshold 30, `feedback_push_cadence`).
- active_work_unit: **`SEMANTIC-INTROSPECTION-EXPANSION` — `.7` `memory_provenance` is the active frontier (the sixth derived query).** Five queries delivered end-to-end at schema `1.18` (`output_support`/`input_reach`/`flop_reset_provenance`/`module_reachability`/`flop_dependencies`); `.7a` design landed ⇒ frontier `.7b.1`.
- next_action: **Implement `.7b.1` — the pure `memory_provenance` core in `src/introspect/analyze.rs`.** Add `QUERY_MEMORY_PROVENANCE = "memory_provenance"`, the `MemoryProvenance` struct (reusing `SupportCone` per port), the sixth `memory_provenance: Vec<MemoryProvenance>` field on `DerivedAnalysis` (`#[serde(default, skip_serializing_if = "Vec::is_empty")]`), and `module_memory_provenance`/`design_memory_provenance` (build a cone per `raddr`/`waddr`/`wdata`/`we` via the existing `build_cone`; ascending mem id; `"mem:<id>"` target). Add `memory_provenance: Vec::new()` to the 10 existing `DerivedAnalysis` literals + `crate::ir::{Memory, MemKind}` import. **Do NOT** add to `supported_query_kinds()` yet (joins with the `run_analyze` dispatch in `.7b.2`). Lib proofs (per-port exact cones incl. flop-Q + instance-output + the SinglePort shared-address case; `"mem:<id>"` target + unknown ⇒ none; memoryless ⇒ empty; serialization omits the other five vecs; determinism; design top-module). Then `.7b.2` (surface: registry + dispatch + schema `1.18 → 1.19` + `analyze_schema` enum + schema-doc/book/USER_GUIDE/README/KM + e2e smoke). Monitor RAM (`scripts/ram_guard.sh --threshold 90 -- <cmd>`).
- handoff: **repo handoff-ready after this commit** — `scripts/check_doctrines.sh` green (4 doctrines); `git diff --check` clean; DUT byte-identical. `.7` is task-tree-owned (`.7a` done, `.7b.1`/`.7b.2` registered); ready for the `.7b.1` code change. origin/main at `4564533`; 13 commits ahead after this. Co-staged: `DEVELOPMENT_NOTES.md` + `docs/tasks/SEMANTIC-INTROSPECTION-EXPANSION.md` + `docs/TASK_TREE.md` + `CHANGES.md` + `MEMORY.md`.
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); one runner + one classifier not two (`feedback_full_factorization`); every capability opt-in + MCP-invocable + queryable + CLI-as-shim (decision `0017`); design the API for agents not humans (`feedback_api_for_agents_not_humans`); SEMANTIC-INTROSPECTION stays SCHEMA-DERIVED / no-shadow-simulator (decisions `0004`/`0011`); the book is the user-facing surface and must not drift (`feedback_book_doctrine`); decision/KM fact per durable capability or boundary. Doctrine-enforcement (decision `0026`): every doctrine = a deterministic check in `scripts/check_doctrines.sh`; code-scoped checks are scope-aware (non-code commits exempt).
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
