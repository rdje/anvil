# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `SEMANTIC-INTROSPECTION-EXPANSION.10a` **node_readers impl design-detail** commit — a **docs/design-only** change (no `src/`), task-tree-owned by `.10a`; **DUT byte-identical**. Opens `.10` ⇒ the **ninth** derived `analyze` query, `node_readers` (per-node immediate 1-hop **reader** adjacency — the **exact transpose of `node_drivers` `.9`**, the operand-relation inverse; the node-level analog of `input_reach` ↔ `output_support`, provable duality `B ∈ node_drivers(A) ⇔ A ∈ node_readers(B)`; the fifth beyond decision `0011`'s four named kinds). `DEVELOPMENT_NOTES.md` design entry resolves Q1–Q5 (Q1 a ninth `node_readers: Vec<NodeReaders>` vec reusing the existing `NodeRef`; Q2 transpose the operand relation — `BTreeMap<u32,BTreeSet<u32>>` reader index → `node_ref_of`, readers sorted+deduped ascending, output-port/flop-D drives out of scope; Q3 `"node:<id>"` addressing; Q4 top-module module-vs-design; Q5 schema `1.21 → 1.22`); pre-split `.10b` → `.10b.1` (pure core) + `.10b.2` (surface). No new numbered decision (`.3a`–`.9a` precedent; decision `0011` governs). Validation: `bash scripts/check_doctrines.sh` green (docs/design commit ⇒ code-scoped checks exempt); no `src/` ⇒ cargo unaffected. Prior: `d409498` (`.9b.2`). Push cadence: `origin/main` at `4564533` → **22 commits ahead** after this ⇒ no push (threshold 30, `feedback_push_cadence`).
- active_work_unit: **`SEMANTIC-INTROSPECTION-EXPANSION` — `.10` `node_readers` OPEN; active frontier `.10b.1`.** Eight derived queries delivered end-to-end (schema `1.21`): `output_support`/`input_reach`/`flop_reset_provenance`/`module_reachability` (decision `0011`'s four) + `flop_dependencies` (`.6`) + `memory_provenance` (`.7`) + `fsm_provenance` (`.8`) + `node_drivers` (`.9`). `.10a` (design) landed; `.10` = `node_readers`, the immediate fan-OUT dual of `node_drivers` (operand-relation transpose), is the ninth — the fifth beyond the four named kinds. Nothing retired.
- next_action: **Implement `.10b.1` — the pure `node_readers` core** in `src/introspect/analyze.rs`: `QUERY_NODE_READERS = "node_readers"` + `NodeReaders { node, kind, op: Option<String>, width, readers: Vec<NodeRef> }` (reuse `NodeRef`) + the ninth `DerivedAnalysis.node_readers` `skip_serializing_if` vec + `module_node_readers`/`design_node_readers` + the `node_readers_with` driver (one pass building a `BTreeMap<u32,BTreeSet<u32>>` reader index by transposing each `Gate`'s operands; resolve each reader via `node_ref_of`; readers ascending node id). Add `node_readers: Vec::new()` to the ~16 existing `DerivedAnalysis` literals (15 `node_drivers: Vec::new(),` sites + the populated `node_drivers,` return). Do NOT add to `supported_query_kinds()` yet (joins with dispatch in `.10b.2`). Lib proofs incl. the **transpose proof** `B ∈ module_node_drivers(A).drivers ⇔ A ∈ module_node_readers(B).readers`. Then `.10b.2` (surface: registry + dispatch + schema `1.21 → 1.22` + analyze_schema enum + schema-doc/book/USER_GUIDE/README/KM + e2e smoke). Monitor RAM (`scripts/ram_guard.sh --threshold 90 -- <cmd>`).
- handoff: **repo handoff-ready after this commit** — `scripts/check_doctrines.sh` green (4 doctrines); `git diff --check` clean; DUT byte-identical (docs/design-only, no `src/`); `.10a` design landed (DEVELOPMENT_NOTES Q1–Q5 + tree registered + frontier `.10b.1`). Pre-existing doc-hygiene gaps (not bundled, per no-bundling): the `.8a`/`.8b.1`/`.8b.2` rows are missing from this tree's Verification Log + Commit Log; the `.5b.2` leaf Status reads `pending` though done — a dedicated docs-hygiene backfill leaf can close both. origin/main at `4564533`; 22 commits ahead after this. Co-staged: `DEVELOPMENT_NOTES.md` + `docs/tasks/SEMANTIC-INTROSPECTION-EXPANSION.md` + `docs/TASK_TREE.md` + CHANGES + MEMORY.
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); one runner + one classifier not two (`feedback_full_factorization`); every capability opt-in + MCP-invocable + queryable + CLI-as-shim (decision `0017`); design the API for agents not humans (`feedback_api_for_agents_not_humans`); SEMANTIC-INTROSPECTION stays SCHEMA-DERIVED / no-shadow-simulator (decisions `0004`/`0011`); the book is the user-facing surface and must not drift (`feedback_book_doctrine`); decision/KM fact per durable capability or boundary. Doctrine-enforcement (decision `0026`): every doctrine = a deterministic check in `scripts/check_doctrines.sh`; code-scoped checks are scope-aware (non-code commits exempt).
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
