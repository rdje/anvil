# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `STRUCTURED-EMISSION-EXPANSION.17a` **eighth-surface (CaseMux priority chain) impl design-detail** commit — **DOCS-ONLY / design-detail leaf** (no `src/` touched) ⇒ **DUT byte-identical**. Grounds decision `0028` in a fresh read of HEAD (the `src/ir/mux_if_emit.rs` template, `render_static_structured_gate`'s `CaseMux` static-collapse test `constant_value(operands[0]).is_some()` at `sv.rs:2077`, the structured-case `always_comb` loop `:667`–`:753`, the seven-pass `gen/mod.rs` roll chain, the `config.rs`/`main.rs`/`types.rs` touch points) and pins all seven `.17b` impl points in `DEVELOPMENT_NOTES.md`. The pass mirrors `mux_if_emit.rs` but is **simpler**: a `CaseMux` is already an `always_comb`-written `logic` var ⇒ **no** `__cv` passthrough — only the `always_comb` body swaps `case…endcase` → `if…else if`. Candidate = a **dynamic-selector** `CaseMux` (`operands[0]` not a `Node::Constant`; `CasezMux` + sibling marks + `param_env` excluded); `Module.case_mux_if_gates: BTreeSet<NodeId>`; in-place body branch reusing the same operand refs + `W'h0` default; metric `num_emitted_case_mux_if_chains` exact (static excluded); the gate is **metric-keyed** (no new text token). Tree `.17`/`.17a`/`.17b` registered (`.17a` done); `docs/TASK_TREE.md` row tail updated. `check_doctrines` 4/4 PASS + `check_knowledge_map` + `check_memory_architecture` + `mdbook build` green; `git diff --check` clean. Prior: this `STRUCTURED-EMISSION-EXPANSION.16` commit. Push cadence: `origin/main` at `605ec44` → **29 commits ahead** after this ⇒ **push imminent** (threshold 30, `feedback_push_cadence` — the next commit triggers a push).
- active_work_unit: **`STRUCTURED-EMISSION-EXPANSION` — frontier `.17b.1`** (the LIVE source change for the eighth surface). `.16` (decision `0028`) + `.17a` (impl design-detail) done; `.17b` impl pending (pre-split `.17b.1` live / `.17b.2` metric+gate / `.17b.3` docs). SEVEN surfaces delivered (`.1`–`.15`); the EIGHTH is picked + designed. All other `active` lanes at no-frontier boundaries.
- next_action: **PNT into `STRUCTURED-EMISSION-EXPANSION.17b.1`** (the live source change — the FIRST `src/` edit this session, so the task-tree-ownership doctrine's owning leaf is `.17b.1`, already registered). Per the `.17a` design: new `src/ir/case_mux_if_emit.rs` (mirror `mux_if_emit.rs`: `gate_qualifies` = a `GateOp::CaseMux` with `operands[0]` not a `Node::Constant`, excluding sibling marks; `annotate_case_mux_if_gates` collect-then-`gen_bool`-roll; `param_env` skip; ~8 lib proofs incl. the static-selector-excluded + end-to-end emit proofs) + `Module.case_mux_if_gates: BTreeSet<NodeId>` (`types.rs`) + `ir/mod.rs` registration + `Config::case_mux_if_emit_prob` (six `config.rs` touch points) + `--case-mux-if-emit-prob` flag (`main.rs`) + two guarded `gen/mod.rs` rolls after `mux_if` (`:160`/`:409`) + the `emit/sv.rs` structured-case body branch (the `CaseMux` arm `:690` branches on `case_mux_if_gates.contains(&idx)` → `if (sel == SW'dk) name = data_k; … else name = W'h0;`). NO metric/schema bump (rides the version; `.17b.2` bumps `1.15 → 1.16`). Validate: `cargo check/clippy(-D warnings)/fmt` + `cargo test --lib` (new proofs) + `cargo test --test snapshots` 6/6 byte-identical (default-off) + a forced `case_mux_if_emit_prob=1.0` ON-vs-OFF generated-RTL sweep (Verilator `-Wall` Δ=0 2012/2017/2023 + both Yosys + Icarus + iverilog sim-equiv 20000 vectors). Monitor RAM on heavy builds (`scripts/ram_guard.sh --threshold 90 -- <cmd>`).
- handoff: repo handoff-ready after this commit — `scripts/check_doctrines.sh` green (4 doctrines); `git diff --check` clean; docs-only ⇒ DUT byte-identical; the prior `.15b.2`/`.15b.3` code state holds (`cargo test --lib` 616 / `--bin tool_matrix` 89 / snapshots 6/6 / `--mux-if-gate` banked `/tmp/anvil-mux-if-gate-r1`; `mdbook build` + `check_knowledge_map` + `book_examples` 3/3 green). The `.17a` design-detail fully specifies `.17b.1` against real line refs. Co-staged: `DEVELOPMENT_NOTES.md` + `docs/tasks/STRUCTURED-EMISSION-EXPANSION.md` + `docs/TASK_TREE.md` + `CHANGES.md` + `MEMORY.md`.
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); one runner + one classifier not two (`feedback_full_factorization`); every capability opt-in + MCP-invocable + queryable + CLI-as-shim (decision `0017`); design the API for agents not humans (`feedback_api_for_agents_not_humans`); the book is the user-facing surface and must not drift (`feedback_book_doctrine`); decision/KM fact per durable capability or boundary. Doctrine-enforcement (decision `0026`): every doctrine = a deterministic check in `scripts/check_doctrines.sh`; code-scoped checks are scope-aware (non-code commits exempt).
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
