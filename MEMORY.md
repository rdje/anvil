# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `STRUCTURED-EMISSION-EXPANSION.6b.2a` commit (the `num_emitted_combinational_tasks` metric + introspection schema `1.9 → 1.10`). Prior: `55761f9` = `.6b.1` (the live `task automatic` surface); `7b6a500` = `.6a`; `b90bdff` = `.5` (decision `0014`, **pushed** — `origin/main` is at `b90bdff`). Working tree clean after this commit. Push cadence: `origin/main` at `b90bdff` → **3 commits ahead after this commit (`.6a` `7b6a500`, `.6b.1` `55761f9`, this `.6b.2a`); push at ~30** (`feedback_push_cadence`).
- active_work_unit: **`STRUCTURED-EMISSION-EXPANSION` tree `active`; `.6`/`.6b`/`.6b.2` `active` containers; frontier = `.6b.2b`** (the repo-owned `tool_matrix --task-emit-gate` downstream gate). `.6b.2` split into `.6b.2a` (metric + schema `1.10` — **done**) + `.6b.2b` (gate). `.6b.1`+`.6a`+`.5` done. The FIRST (combinational `function automatic`, `.1`+`.2`) and SECOND (`generate for` loop, `.3`+`.4`) surfaces are delivered end-to-end; the THIRD (`task automatic`) live surface + metric are done, gate + docs remain. Future surfaces (nested/multi-level `generate`, `interface`/`modport`, richer tasks) are `.7+`. Index: `docs/TASK_TREE.md`.
- `.6b.2a` delivered (metric + schema bump): `Metrics::num_emitted_combinational_tasks` (`= m.task_emit_gates.len()`, `#[serde(default)]`) in `src/metrics.rs` `compute()` + a lib proof; surfaced in introspection `module_metrics` ⇒ `SCHEMA_VERSION` `1.9 → 1.10` (`src/introspect/mod.rs` const + doc comment + 2 assertions; `src/mcp/mod.rs` 7 assertions). MINOR is an integer ⇒ `1.9 → 1.10` (ten), not a decimal (recorded in the doc comment + schema changelog). Bumped all current-output schema refs (schema doc 3 version lines + `1.9→1.10` changelog; README `--introspect`+`analyze`; USER_GUIDE `--sv-version --introspect` row; 5 `book/src/agent-mcp.md` JSONs; `CODEBASE_ANALYSIS` envelope); historical landing attributions left intact. Default-off / DUT byte-identical (snapshots 6/6; lib 490); end-to-end `--introspect` default `0` / forced `task_emit_prob=1.0` (seed 42) `39`.
- `.6b.1` (prior) delivered (live emitter change): `Config::task_emit_prob` (default `0.0`, config-file-only) + `Module.task_emit_gates` + new `src/ir/task_emit.rs` (`annotate_task_emit_gates` = the function-emit candidate predicate **plus** exclusion of the three sibling projections; `param_env` skipped) + two guarded gen-time rolls after generate_loop + `src/emit/sv.rs` `task_emit_gate` accessor + a task section (`render_gate_task_decl` — body **reuses `render_gate_function_body` verbatim**; `render_gate_task_call` — `logic <wire>__tv;` + `always_comb <wire>__t(<wire>__tv, refs);`) + the gate-assign-loop passthrough `assign <wire> = <wire>__tv;` + 11 lib proofs. Output-var + passthrough form. Forced `task_emit_prob=1.0` sweep clean across Verilator `--lint-only` (`-Wall` Δ=0) + both Yosys + Icarus (`/tmp/anvil-te-r1/`).
- next_action: **PNT — execute `STRUCTURED-EMISSION-EXPANSION.6b.2b`** (the repo-owned `tool_matrix --task-emit-gate` gate, templated on `--function-emit-gate`/`--generate-loop-gate`): in `src/bin/tool_matrix.rs` add `--task-emit-gate` CLI flag + `ScenarioSet::TaskEmitSweep` + `MatrixReport.task_emit_gate` (wire into `select_scenario_set` [mutually exclusive], `derive_run_plan` [4 units/scenario floor + `fail_on_coverage_gap`], `build_scenarios`, `scenario_set_slug` "task-emit-sweep", `artifact_kind_slug` "module") + `build_task_emit_sweep_scenarios`/`task_emit_focus_config` (the `generate_loop_focus_config`-shaped comb-only single-module DUT — node-id + e-graph, `flop_prob=0.0` — with `task_emit_prob=1.0` × all three construction strategies = 3 scenarios) + `ModuleReport.emitted_combinational_task` (`#[serde(default)]`, set in `materialize_prepared_module` from `prepared.sv_text.contains("task automatic")`) + `CoverageSummary.saw_combinational_task_emit` (lit in `summarize_coverage` when an emitted-task module is Verilator-success AND has a non-empty clean Yosys vec — a task is universally synthesizable, full plan) + merge in `merge_coverage` + an early-return arm in `compute_coverage_gaps` + 5 proofs + thread the new field through the 6 ModuleReport fixtures + the `test_cli` default. Bank clean `/tmp/anvil-task-emit-gate-r1` (`--task-emit-gate --yosys-mode both --iverilog-compile`). No schema bump (harness-only). Then `.6b.3` (book/knobs/USER_GUIDE/README/KM closeout). Default-off / DUT byte-identical (snapshots 6/6). Doctrine: NO code change without a task-tree leaf owning it first — `.6b.2b` owns it.
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); every capability is an opt-in knob; decision/KM fact per durable capability or boundary.
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
