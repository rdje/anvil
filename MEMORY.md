# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `KNOB-ERGONOMICS-AND-PRESETS.2b.2b` commit — the **SCHEMA-DERIVED per-knob catalog** (decision `0021`/`0017`), **completing the lane's planned API**. **Code — `src/config.rs` + `src/mcp/mod.rs` + book + KM card; DUT byte-identical (snapshots 6/6).** `KnobInfo` + `knob_catalog()` project every `Config` field (names+defaults from `serde_json::to_value(Config::default())`, `cli_flag` from the `Overrides` serde key set ∪ {seed}, `group` via `knob_group` prefix-classifier, `validation` best-effort); the new `anvil://catalog/knob-schema` MCP resource (raw `anvil://catalog/knobs` kept); a **completeness test** (`knob_catalog_classifies_every_field`: every field covered, no `"other"` group, cli/config partition — caught min/max_hierarchy_depth during dev). Book `agent-mcp.md`/`api-resources-prompts.md` + KM card extended. Gate: lib **563→566** (+2 tests), snapshots 6/6, clippy -D warnings + fmt clean, mdbook clean, full `cargo test` green. Prior: `455223e`=`.2b.2a` (presets resource + MCP profile); `c44b3a5`=`.2b.3` (docs); `c59bf39`=`.2b.1` (16 CLI flags). Push cadence: `origin/main` at `7142fd7` → **20 commits ahead** after this; **push at ~30** (`feedback_push_cadence`).
- active_work_unit: **`KNOB-ERGONOMICS-AND-PRESETS` — lane API COMPLETE, no current frontier.** All done: `.1` (ADR/decision `0021`), `.2a` (design-detail), `.2b.1` (16 CLI flags + `--profile` + shared `resolve_config`), `.2b.3` (user-facing docs), `.2b.2a` (presets resource + MCP `profile` input), `.2b.2b` (per-knob catalog + `anvil://catalog/knob-schema`). Tree stays `active` (open-ended): more presets / promoted knobs / richer catalog metadata are optional `.N` picks (nothing retired). **Next PNT pick is a NEW lane** (pick-and-roll, `feedback_pick_and_roll_at_no_frontier`).
- next_action: **PNT — pick the next lane.** Candidate design-first ADR `.1` leaves (docs-only, low-risk): `CI-PACKAGING-DISTRIBUTION.1` (prebuilt release binaries + a drop-in GitHub Action wrapping `anvil hunt` — synergistic, soft-dep on the hunt engine), `COVERAGE-STEERED-GENERATION.1` (bias generation toward under-exercised constructs at construction time — rules-first, never generate-then-filter), or `CAPABILITY-BREADTH-EXPANSION.1`/`.2` (more SV-2017/2023 up-opts / Mealy FSM outputs). Each binds decision `0017` (API-first). Refine acceptance at pick.
- handoff: repo fully handoff-ready & **in sync** after this commit — full `cargo test` green, snapshots 6/6, clippy/fmt clean, mdbook clean; `check_memory_architecture` + KM gen/check green (**KM 55 facts**); introspection schema `1.11`. No pending doc-drift; KNOB-ERGONOMICS lane API fully delivered + documented.
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); one runner + one classifier not two (`feedback_full_factorization`); every capability opt-in + MCP-invocable + queryable + CLI-as-shim (decision `0017`); design the API for agents not humans (`feedback_api_for_agents_not_humans`); the book is the user-facing surface and must not drift (`feedback_book_doctrine`); decision/KM fact per durable capability or boundary.
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
