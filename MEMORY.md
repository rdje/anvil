# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `STRUCTURED-EMISSION-EXPANSION.5` commit (design/decision leaf — decision `0014`: the THIRD structured surface is a default-off, valid-by-construction combinational `task automatic` emit-projection of a single combinational gate; **docs-only / no source change**; autonomously selected at a no-frontier boundary per owner *"pick any tree and roll"*). Prior: `909c82a` = `.4b.3` (generate-for user docs, closed the second surface); `2129035` = `.4b.2b`. Working tree clean after this commit. Push cadence: `origin/main` at `63d2622` → **30 commits ahead after this commit → PUSH NOW** (`feedback_push_cadence`); then continue.
- active_work_unit: **`STRUCTURED-EMISSION-EXPANSION` tree `active`; `.5` (design/decision leaf) done + `.6` (impl) is the frontier** — sibling leaves under the top tree (the `.1`/`.2`, `.3`/`.4` pattern). `.6` implements the `task automatic` surface (decision `0014`; pre-split `.6a` design-detail + `.6b` impl, `.6b` may further split `.6b.1`/`.6b.2`/`.6b.3` per the `.4b` precedent). The FIRST (combinational `function automatic`, `.1`+`.2`) and SECOND (`generate for` loop, `.3`+`.4`) surfaces are delivered end-to-end. Future surfaces (nested/multi-level `generate`, `interface`/`modport`, richer tasks) are `.7+`, each its own decision. Index: `docs/TASK_TREE.md`.
- `.5` delivered (decision `0014`, docs-only): the third surface = a combinational `task automatic` — the decision `0012` single-gate parallel, but a *procedural* `task` with an `output` arg called from `always_comb`. For `<wire> = op(o0,o1,…)`: `task automatic <wire>__t(output …, input …); o = a0 op a1 …; endtask` + (minimal blast radius) `logic <wire>__tv; always_comb <wire>__t(<wire>__tv, …); assign <wire> = <wire>__tv;`. Empirically grounded clean across Verilator `-Wall` + both Yosys + Icarus (BOTH the direct-output and the output-var passthrough forms) — confirms decision `0013`'s narrowed task caution. Rules-first / default-off `task_emit_prob` (proposed) ⇒ byte-identical; mutually exclusive with the sibling projections; combinational only; structured selectors + `Slice` excluded; gate `saw_combinational_task_emit`.
- next_action: **FIRST push** (30 commits ahead, cadence due — `feedback_push_cadence`; this is my call per `feedback_pick_and_roll_at_no_frontier`). **Then PNT — execute `STRUCTURED-EMISSION-EXPANSION.6a`** (the design-detail leaf for the `task` surface, **docs-only, no source**): ground decision `0014` in the real `src/emit/sv.rs` `to_sv_with_modules` (the function-decl / generate-block sections + the per-gate assign loop) + the `function_emit.rs`/`generate_loop.rs` gen-time-annotation precedent + `src/config.rs`/`src/gen/mod.rs`. Pin: (1) the net-vs-var integration (output-var + passthrough-`assign` leading vs `<wire>`-as-`always_comb`-var) + one-`always_comb`-per-call vs shared; (2) gen-time annotation (`Module.task_emit_gates`) + the candidate predicate (ordinary combinational `Gate`, ≥1 operand, excluding the sibling projections + structured selectors + `Slice`); (3) the `task automatic` decl + positional-param body (reuse the `render_gate_function_body` positional counterpart) + the `always_comb` call rendering + the assign-loop suppression; (4) the `task_emit_prob` knob (default `0.0` byte-identical); (5) the `saw_combinational_task_emit` gate + a `num_emitted_combinational_tasks` metric (schema `1.9→1.10`). Then `.6b` implements (live surface + gate + docs, likely `.6b.1`/`.6b.2`/`.6b.3`). Default-off / DUT byte-identical. Doctrine: NO code change without a task-tree leaf owning it first — `.6`/`.6a`/`.6b` own it.
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); every capability is an opt-in knob; decision/KM fact per durable capability or boundary.
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
