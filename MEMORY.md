# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `SV-VERSION-TARGETING.2b.2b` commit (hash backfills next update; prior: `e3c2f4b` = `.2b.2a`; `c9895ad` = `.2b.1`; `a18d51d` = `.2a`). Working tree clean after this commit.
- active_work_unit: **`SV-VERSION-TARGETING`** (owner-directed capability lane, **`active`**, opened `2026-06-15`). `.1` + `.2a` + `.2b.1` + `.2b.2a` + **`.2b.2b` done** ⇒ **`.2b.2`, `.2b`, and `.2` all closed**. Frontier leaf: **`.3`** (`proposed`, first version-distinctive up-opted construct, design-first). Two sibling lanes registered `proposed`: `STRUCTURED-EMISSION-EXPANSION`, `SEMANTIC-INTROSPECTION-EXPANSION`. Index: `docs/TASK_TREE.md`.
- next_action: continue PNT (no self-pause — `feedback_no_self_pause_until_trees_closed`). Within `SV-VERSION-TARGETING` the only remaining frontier is **`.3`** — a **design-first** leaf: pick the first version-distinctive *synthesizable* construct introduced by IEEE 1800-2017 or -2023 that a downstream tool accepts in its version mode (e.g. survey Verilator 5.046 / Yosys 0.64 / Icarus support), write a decision/design leaf (which construct, why synthesizable + tool-accepted, the `sv_version >= that_standard` construction-time gate), then implement gated + downstream-clean in the matching mode + default-off byte-identical. Alternatively resume the parked `IDENTITY-DEEPENING.3b.2b` (cross-module sequential equivalence, fully designed) per the table order if owner re-prioritizes. **The matrix emit-threading is in place**, so an up-opted construct's gate scenarios already run Verilator in the matching `--language` mode.
- `.2b.2b` delivered (CODE in `src/bin/tool_matrix.rs`; default matrix byte-identical): `--sv-version-gate` → `ScenarioSet::SvVersionSweep` (9 Interleaved scenarios = 3 targets × {comb leaf, seq leaf, recursive hierarchy design}); per-version emit via `to_sv_versioned`/`to_sv_in_design_versioned`; `verilator_language_for` runs Verilator `--language 1800-20xx` only under the gate; `saw_sv_version_*_targeted_acceptance` facts (lit by `light_sv_version_acceptance` when Verilator ran-and-succeeded + clean Yosys) enforced by an early-return arm in `compute_coverage_gaps`; `MatrixReport.sv_version_gate`; 6 new proofs. Banked clean: `/tmp/anvil-sv-version-gate-r1` (9 scenarios / 18 units / `coverage_gaps=[]` / 18/0 Verilator + both Yosys; per-scenario Verilator argv carries the matching `--language`).
- **PARKED (not abandoned): `IDENTITY-DEEPENING`** (`active`, frontier `.3b.2b`) — cross-module whole-module sequential equivalence, **fully designed** (decision `0008` + `.3b.1` detail + `.3b.2a` `bisimulation_partition` helper landed `edae1b0`). Resume from `docs/tasks/IDENTITY-DEEPENING.md` `.3b.2b` (combined-module `modules_bisimilar` + `dedup_sequential_modules` + `hierarchy_sequential_module_dedup` knob + metric + gate). Owner redirected priority to `SV-VERSION-TARGETING`.
- lane invariants (both lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); every capability is an opt-in knob; decision/KM fact per durable capability or boundary.
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`. The next code leaf (`.3` impl, when reached) touches the emitter/generator ⇒ run snapshots 6/6 + `tests/sv_version` + the relevant gate; `.3`'s design leaf is docs-only (self-checks only). Push cadence: `origin/main` at `381ec01`; **25 commits ahead after this commit — push after 30 accumulate** (`feedback_push_cadence`).

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
