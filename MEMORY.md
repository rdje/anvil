# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `STRUCTURED-EMISSION-EXPANSION.2b.2c` commit (the user-facing closeout — new `book/src/structured-emission.md` "How It Works" chapter + `function_emit_prob` knob entry in `book/src/knobs.md`/`USER_GUIDE.md`/README "Current CLI truth" [config-file-only knob, no CLI flag] + KM how-to card `combinational-function-emit`; **docs-only / DUT byte-identical**). Prior: `3c63f96` = `LOCAL-REFERENCE-CACHE.1` (gitignore `.cache/`); `873803b` = `.2b.2b`. Working tree clean after this commit. Push cadence: `origin/main` at `63d2622` → **23 commits ahead after this commit; push at ~30** (`feedback_push_cadence`).
- active_work_unit: **`STRUCTURED-EMISSION-EXPANSION` tree `active`, NO current frontier.** `.2b.2c` closes `.2b.2`/`.2b`/`.2`: the first structured surface (the combinational `function automatic` emit-projection) is delivered end-to-end (`.1` decision `0012` → `.2b.1` live surface → `.2b.2a` metric+schema `1.8` → `.2b.2b` `tool_matrix --function-emit-gate` gate → `.2b.2c` docs). Future surfaces (`task`/nested `generate`/`interface`/`modport`) are `.3+`, each its own design/decision leaf when picked. Index: `docs/TASK_TREE.md`.
- `.2b.2c` delivered (docs-only): `book/src/structured-emission.md` (+ SUMMARY link) teaches the emit-projection concept (the `soft_union`/aggregate precedent), a **byte-verified seed-42 before/after** (`function_emit_prob` 0.0→1.0 = the `add_0__f` decl + rewriting only that gate's assign to a call; rest byte-identical), the single-gate rule, the `Slice`/structured exclusions (full-width `Slice` param ⇒ `-Wall UNUSEDSIGNAL`; nothing retired), duplicate-operand positional params, combinational-only. Knob docs in `book/src/knobs.md` (`### Structured emission`) + `USER_GUIDE.md` + README. KM card `combinational-function-emit` (37 facts / 286 keys; validated `reverify`). NOTE: `function_emit_prob` is a **config-file-only knob (no CLI flag)** — verified against `src/config.rs`/`src/main.rs`; the resume pointer's earlier loose "`anvil --function-emit-prob`" phrasing was NOT copied.
- next_action: **PNT — pick the next roadmap-aligned activity.** `STRUCTURED-EMISSION-EXPANSION` has no current frontier; the next step in *that* lane is a `.3` design/decision leaf for the next structured surface (`task` is the natural next candidate per decision `0012`), OR PNT moves to another `active` lane: `SIGNOFF-AUTOMATION-EXPANSION` (no frontier — higher-ceiling future leaves), `IDENTITY-DEEPENING` (no frontier — deeper module-equivalence boundaries), `SEMANTIC-INTROSPECTION-EXPANSION` (no frontier — open-ended query breadth), `LOCAL-REFERENCE-CACHE` (no frontier). Each requires a design/decision leaf first per the task-tree-before-code doctrine. Doctrine reminder: NO code change without a task-tree leaf owning it first.
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); every capability is an opt-in knob; decision/KM fact per durable capability or boundary.
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
