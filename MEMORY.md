# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `SEMANTIC-INTROSPECTION-EXPANSION.1` commit (hash backfills next update; prior: `59e09c4` = `SV-VERSION-TARGETING.3b.2a`; `eca5d8b` = `.3b.1`; `154b2a7` = `.3a`). Working tree clean after this commit.
- active_work_unit: **TWO active lanes.** (A) **`SEMANTIC-INTROSPECTION-EXPANSION`** (**`active`**, owner-prioritized `2026-06-16`: deep semantic introspection first-class + everything MCP-queryable via a top-notch API). `.1` **done** (decision `0011`). Frontier: **`.2`** (impl). (B) **`SV-VERSION-TARGETING`** (**`active`**): first up-opt SHIPS (`.3b.2a` done — live `union soft` overlay). Frontier: **`.3b.2b`** (matrix up-opt gate). `STRUCTURED-EMISSION-EXPANSION` still `proposed`. `IDENTITY-DEEPENING.3b.2b` parked-but-fully-designed. Index: `docs/TASK_TREE.md`.
- next_action: continue PNT (no self-pause) → **owner-prioritized: `SEMANTIC-INTROSPECTION-EXPANSION.2`** — implement the first derived query, the **output support cone** (per decision `0011`): a PURE post-hoc analysis (BFS/DFS over the existing `Module.nodes` operands + `drives` + flop D-cones + the design instance graph) computing, per output (and flop D-cone), the transitive fan-in support set (primary inputs / flop Qs / child-instance outputs) + cone size/depth; a new `DerivedAnalysis` introspection payload section (schema MINOR **1.2→1.3** + schema-doc + test-assertion bumps); a new **pure** MCP `analyze` tool (`{seed, lane, config, query:<kind>, target?}`, cached, big cones as `ResourceRef`). **SCHEMA-DERIVED** (no new oracle / no shadow simulator — the `0004` boundary), default-off / DUT byte-identical (snapshots 6/6). Pre-split `.2` → `.2a` (design-detail: the `DerivedAnalysis` struct + `query`-kind enum + `target` addressing + design-vs-module cone semantics) + `.2b` (impl) if broad. Then book(`agent-mcp.md`)/USER_GUIDE/schema-doc + KM fact. PARALLEL SV-version frontier `.3b.2b` (matrix `--sv-version-gate` up-opt scenario + `saw_sv_version_2023_soft_union_upopt` fact, Verilator-only, Yosys/Icarus no-op; all in `src/bin/tool_matrix.rs`).
- `.1` delivered (DOCS-ONLY): decision `0011` activated the lane (owner directive) — a first-class, versioned, MCP-queryable, **SCHEMA-DERIVED derived-RELATION** API (`DerivedAnalysis` schema `1.3` + pure MCP `analyze` tool) answering *what depends on what* by pure IR-graph traversal; **relations, not behaviour** (no shadow simulator). First query = output support cone. Existing surface mapped: `src/introspect/mod.rs` (Config/Metrics/DesignMetrics serde projection), `src/mcp/mod.rs` (generate/introspect/dump_config/coverage_gaps/validate/minimize), `src/metrics.rs` (counts/distributions but NO relations). `.3b.2a` (prior): live `union soft` up-opt (`soft_union_slice_prob` + `src/ir/soft_union.rs` + `emit/sv.rs` overlay; banked Verilator 1800-2023 clean; snapshots 6/6).
- **PARKED (not abandoned): `IDENTITY-DEEPENING`** (`active`, frontier `.3b.2b`) — cross-module whole-module sequential equivalence, **fully designed** (decision `0008` + `.3b.1` detail + `.3b.2a` `bisimulation_partition` helper landed `edae1b0`). Resume from `docs/tasks/IDENTITY-DEEPENING.md` `.3b.2b`. Owner redirected priority to `SV-VERSION-TARGETING`.
- lane invariants (both lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); every capability is an opt-in knob; decision/KM fact per durable capability or boundary.
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`. The next code leaf (`SEMANTIC-INTROSPECTION-EXPANSION.2`) touches `src/introspect/`+`src/mcp/`+`docs/AGENT_INTROSPECTION_SCHEMA.md` ⇒ run snapshots 6/6 (DUT byte-identical) + introspect/mcp lib tests + schema-version assertions. `.3b.2b` (parallel) is matrix-only. Push cadence: `origin/main` at `381ec01`; **29 commits ahead after this commit — push after 30 accumulate** (`feedback_push_cadence`), so push after the next commit.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
