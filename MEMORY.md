# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `SEMANTIC-INTROSPECTION-EXPANSION.12a` **instance_input_bindings impl design-detail** commit — a **docs/design-only** change (no `src/`), task-tree-owned by `.12a`; **DUT byte-identical**. **Opens `.12` `instance_input_bindings`** — the **eleventh** derived `analyze` query and the **parent-side dual of `instance_provenance` `.11`** (the exact future extension `.11a` named): for each child instance in a design's top, the parent node (`NodeRef`) driving each child **input** port, read from `Instance.inputs: Vec<(PortId, NodeId)>`. `.11` descends INTO the child for outputs; `.12` ascends to the PARENT for inputs; chaining `.12 → .11` traces a parent signal across the module boundary. DEVELOPMENT_NOTES design-detail entry resolves Q1–Q5 (Q1 an eleventh `instance_input_bindings: Vec<InstanceInputBindings>` vec + `InstanceInputBindings`/`InstanceInputBinding { port, driver: NodeRef }` reusing `NodeRef`; Q2 a pure read of `Instance.inputs` — driver = `node_ref_of(top, ..)`, child def names the port; Q3 instance-name addressing; Q4 a NON-degenerate module variant — the contrast with `.11`; Q5 schema `1.23 → 1.24`). Pre-split `.12b` → `.12b.1` (pure core) + `.12b.2` (surface). No new numbered decision (`.3a`–`.11a` precedent; decision `0011` governs). Validation: docs-only ⇒ `cargo` unaffected; `check_doctrines.sh` green (code-scoped checks exempt for a non-code commit; MEMORY-ARCH + KM pass). Prior: `fc790e1` (`.11b.2`). Push cadence: `origin/main` at `a767415`; **2 commits ahead** after this (`fc790e1` + this) ⇒ no push (threshold 30).
- active_work_unit: **`SEMANTIC-INTROSPECTION-EXPANSION.12` (`instance_input_bindings`)** → frontier leaf **`.12b.1`** (the pure core in `src/introspect/analyze.rs`). `.12a` (design-detail) done. The lane is `active`; ten prior derived queries delivered @ schema `1.23`.
- next_action: **Implement `.12b.1` — the pure `instance_input_bindings` core** in `src/introspect/analyze.rs`: `QUERY_INSTANCE_INPUT_BINDINGS = "instance_input_bindings"`, the `InstanceInputBindings { instance, module, role, input_bindings }` + `InstanceInputBinding { port, driver: NodeRef }` structs, the eleventh `instance_input_bindings` `#[serde(default, skip_serializing_if)]` vec on `DerivedAnalysis`, and `module_instance_input_bindings`/`design_instance_input_bindings` (driver = `node_ref_of(top, parent_node_id, top_fmt)`; child def names the port, fallback `"port<id>"`; instances sorted by name; bindings ascending child PortId). Add `instance_input_bindings: Vec::new()` to the existing `DerivedAnalysis` literals. **Do NOT add to `supported_query_kinds()` yet** (registry + `run_analyze` dispatch land together in `.12b.2`). Lib-tested only (incl. the cross-boundary binding proof + the non-degenerate module-variant proof); snapshots 6/6 byte-identical. Then `.12b.2` (surface: registry + dispatch + schema `1.23 → 1.24` + analyze_schema enum + schema-doc/book/USER_GUIDE/README/KM + e2e smoke). Monitor RAM (`scripts/ram_guard.sh --threshold 90 -- <cmd>`).
- handoff: **repo handoff-ready (mid-lane, `.12a` design done, frontier `.12b.1`).** `scripts/check_doctrines.sh` green; tree clean; DUT byte-identical. `.12` (`instance_input_bindings`) opened end-to-end design; next session implements `.12b.1` (pure core) then `.12b.2` (surface). origin/main at `a767415`; **2 commits ahead** after this; threshold 30 ⇒ no push.
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); one runner + one classifier not two (`feedback_full_factorization`); every capability opt-in + MCP-invocable + queryable + CLI-as-shim (decision `0017`); design the API for agents not humans (`feedback_api_for_agents_not_humans`); SEMANTIC-INTROSPECTION stays SCHEMA-DERIVED / no-shadow-simulator (decisions `0004`/`0011`); the book is the user-facing surface and must not drift (`feedback_book_doctrine`); decision/KM fact per durable capability or boundary. Doctrine-enforcement (decision `0026`): every doctrine = a deterministic check in `scripts/check_doctrines.sh`; code-scoped checks are scope-aware (non-code commits exempt).
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
