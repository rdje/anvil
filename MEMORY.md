# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `DOWNSTREAM-ADAPTER-EXPANSION.2a.1` commit — the first **code** leaf of decision `0020` (closed `Adapter` registry). **`src/downstream/mod.rs` only; default-off / DUT byte-identical (`tests/snapshots.rs` 6/6 untouched).** Landed: `pub trait Adapter: Sync { id, binary, run(&AdapterRunCx)->Result<Vec<ToolInvocation>> }` + `AdapterRunCx{binary,out_dir,target,yosys_mode,language}` + `AdapterTarget{Module,Design}`(Copy) + 3 built-in unit-struct adapters (`Verilator`/`Yosys`/`Icarus`) whose `run` **delegates verbatim** to the existing `run_verilator`/`run_yosys`/`run_iverilog_compile`(+`_design`) ⇒ byte-identical (Yosys `Both` still 2 rows) + `static ADAPTER_REGISTRY`/`pub fn adapters()` (the `Adapter: Sync` supertrait makes the static registry valid — the E0515 fix) + `AcceptanceTool::adapter()` (enum stays canonical built-in identity, not retired). `downstream::validate` refactored to dispatch via `tool.adapter().run(&cx)` (same labels/argv/order, `adapter.binary()==tool.binary()`, mem-guard once per tool). +2 lib proofs. `extract_facts` (slang) = `.2c`; `validate_tool_specs`+`tool_matrix` routing = `.2a.3`; adapter-catalog query = `.2a.2`. Gate: lib **545/0** (+2), snapshots **6/6**, tool_matrix **75/0**, anvil **12/0**, clippy/fmt clean, RAM-guarded. Files: `src/downstream/mod.rs` + the tree + `docs/TASK_TREE.md` + `ROADMAP.md` + `CODEBASE_ANALYSIS.md` + DEVELOPMENT_NOTES + CHANGES + MEMORY. Prior: `412e5ff`=`DOWNSTREAM-ADAPTER-EXPANSION.1` (decision `0020` ADR); `6a441f7`=`BOOK-API-REFERENCE.1`; `7142fd7` (**== origin/main**, pushed). Push cadence: `origin/main` at `7142fd7` → **7 commits ahead** after this (`.2c.2`,`.2d`,`.2e`,`.2f`,`BOOK-API-REFERENCE.1`,`DAE.1`,`DAE.2a.1`); push at ~30 (`feedback_push_cadence`).
- active_work_unit: **`DOWNSTREAM-ADAPTER-EXPANSION`** — `.1` ADR + `.2a.1` (registry core) done; frontier now **`.2a.2`** (the SCHEMA-DERIVED adapter-catalog discoverability query/resource over MCP — `{id,binary,present,supports_facts}` — decision `0017`), then `.2a.3` (route `validate_tool_specs`+`tool_matrix` columns through the registry, byte-identical), then `.2b` (sv2v), `.2c` (slang). Other open lanes per `docs/TASK_TREE.md`: `KNOB-ERGONOMICS-AND-PRESETS.1`, `CI-PACKAGING-DISTRIBUTION.1`, `COVERAGE-STEERED-GENERATION.1`, `CAPABILITY-BREADTH-EXPANSION.1/.2` (design-first ADRs) + no-frontier capability trees (`STRUCTURED-EMISSION-EXPANSION`, `SEMANTIC-INTROSPECTION-EXPANSION`+`AGENT-MCP-EXPANSION`, `IDENTITY-DEEPENING`, `SIGNOFF-AUTOMATION-EXPANSION`).
- next_action: **PNT — `DOWNSTREAM-ADAPTER-EXPANSION.2a.2`** (the adapter-catalog discoverability surface: project `adapters()` as `{id,binary,present (PATH --version probe),supports_facts}` over an MCP resource/query so an agent discovers installed tools over the API alone — decision `0017`). Touches `src/mcp` (+ maybe `src/introspect` + book). Real code leaf ⇒ full `cargo` gate. Or take an owner steer if one is given.
- handoff: repo handoff-ready — code state green after this commit (`cargo test --lib` **545/0**, snapshots 6/6, `tool_matrix` 75/0, `anvil` 12/0, `divergence_e2e` portable 1/0 + ignored 2/0; introspection schema `1.11`); `mdbook build` clean; `check_memory_architecture` + KM gen/check green (**KM 51 facts**). Working tree clean after this commit.
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); one runner + one classifier not two (`feedback_full_factorization`); every capability opt-in + MCP-invocable + queryable + CLI-as-shim (decision `0017`); design the API for agents not humans (`feedback_api_for_agents_not_humans`); the book is the user-facing surface and must not drift (`feedback_book_doctrine`); decision/KM fact per durable capability or boundary.
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
