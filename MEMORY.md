# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `SEMANTIC-INTROSPECTION-EXPANSION.2b.1` commit (hash backfills next update; prior: `63d2622` = `.2a`; `a31bf94` = `.1`; `59e09c4` = `SV-VERSION-TARGETING.3b.2a`). Working tree clean after this commit. Push cadence: `origin/main` was at `63d2622` (= prior HEAD; the earlier "30 ahead" push already landed) → **1 commit ahead after this commit; push at ~30** (`feedback_push_cadence`).
- active_work_unit: **TWO active lanes.** (A) **`SEMANTIC-INTROSPECTION-EXPANSION`** (**`active`**, owner-prioritized `2026-06-16`). `.1` (decision `0011`) + `.2a` (design-detail) + **`.2b.1`** (the pure `src/introspect/analyze.rs` support-cone analysis core, DUT byte-identical) **done**. Frontier: **`.2b.2`** (surface wiring). (B) **`SV-VERSION-TARGETING`** (**`active`**): first up-opt SHIPS (`.3b.2a` done — live `union soft` overlay). Frontier: **`.3b.2b`** (matrix up-opt gate). `STRUCTURED-EMISSION-EXPANSION` `proposed`. `IDENTITY-DEEPENING.3b.2b` parked-but-fully-designed. Index: `docs/TASK_TREE.md`.
- next_action: continue PNT (no self-pause) → **owner-prioritized: `SEMANTIC-INTROSPECTION-EXPANSION.2b.2`** — wire the `.2b.1` analysis to the surface (CODE): schema `SCHEMA_VERSION` **`1.2 → 1.3`** (the `src/introspect/mod.rs` const + the ~5 `"1.2"` test-assertion bumps + a `docs/AGENT_INTROSPECTION_SCHEMA.md` §4/§7 section + changelog) + a `DerivedAnalysisDocument` envelope (reuse `RequestEcho`/`run_id` + an `analysis: DerivedAnalysis` payload) + the **pure** MCP `analyze` tool in `src/mcp/mod.rs` (dispatch + `tools/list` entry + the `anvil://artifact/<run_id>/analysis/<query>` resource), `query` validated against `analyze::supported_query_kinds()` and unknown query/target ⇒ `-32602` (the `prompts/get` precedent); cached like the other pure tools; route a `design` artifact through `design_support_cones`, a `module` through `module_support_cones`. Then book(`agent-mcp.md`)/USER_GUIDE/schema-doc + a KM fact. **DUT `.sv` byte-identical** (introspect/MCP not in snapshots) ⇒ run snapshots 6/6 + introspect/mcp lib tests + the schema-version assertions. PARALLEL SV-version frontier `.3b.2b` (matrix `--sv-version-gate` up-opt scenario + `saw_sv_version_2023_soft_union_upopt` fact, Verilator-only, Yosys/Icarus no-op; all in `src/bin/tool_matrix.rs`).
- `.2b.1` delivered (CODE, DUT byte-identical): `src/introspect/analyze.rs` — `DerivedAnalysis`/`SupportCone` + `module_support_cones`/`design_support_cones` (memoized **combinational** fan-in DFS; FlopQ a register-boundary support leaf with `"flop:<id>"` D-cone target; instance-boundary stop; opaque MemRead/FsmOut termination; unknown target ⇒ no cone). 9 in-crate proofs; `cargo test --lib` 421/0/2; snapshots 6/6; clippy/fmt clean. `.2a` (prior): resolved `0011`'s 3 questions + the impl shape. `.3b.2a` (prior): live `union soft` up-opt.
- **PARKED (not abandoned): `IDENTITY-DEEPENING`** (`active`, frontier `.3b.2b`) — cross-module whole-module sequential equivalence, **fully designed** (decision `0008` + `.3b.1` detail + `.3b.2a` `bisimulation_partition` helper landed `edae1b0`). Resume from `docs/tasks/IDENTITY-DEEPENING.md` `.3b.2b`. Owner redirected priority to the two active lanes above.
- lane invariants (both lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); every capability is an opt-in knob; decision/KM fact per durable capability or boundary.
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`. The next code leaf (`.2b.2`) touches `src/introspect/`+`src/mcp/`+`docs/AGENT_INTROSPECTION_SCHEMA.md` ⇒ run snapshots 6/6 (DUT byte-identical) + introspect/mcp lib tests + schema-version assertions.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
