# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `DOWNSTREAM-ADAPTER-EXPANSION.2b.1` commit — the first new downstream adapter, `sv2v` (decision `0020`). **`src/downstream/mod.rs` + `src/mcp/mod.rs` + book; additive / DUT byte-identical (`tests/snapshots.rs` 6/6 untouched; no generator change; no introspection SCHEMA_VERSION bump).** `downstream`: `AcceptanceTool::Sv2v` (`from_name`/`binary` `"sv2v"`/`adapter()`) + `run_sv2v` (`sv2v <file>`) + `run_sv2v_design` (`sv2v --top=<top> <files…>`) transpile accept/reject primitives (no fact hook) + an `Sv2vAdapter` (`supports_facts=false`) + a 4th `ADAPTER_REGISTRY` entry + a `first_tool_warning` `"sv2v"` arm. `mcp`: `"sv2v"` in the four `tools` enum schemas + the `validate` description + the `parse_validate_tools` allow-list, so `sv2v` is selectable via `tools` + in `adapter_catalog()` (`anvil://catalog/adapters` now 4 entries; `present=false` until installed — friendly no-op). Book synced (`agent-mcp.md`/`api-tools.md`/`api-resources-prompts.md`). +2 net lib proofs (catalog-projects-every-adapter; mcp parse_validate_tools-accepts-sv2v); registry/warning/routing/catalog proofs extended to 4. Gate: lib **549/0**, snapshots **6/6**, clippy/fmt clean, mdbook clean, check_memory_architecture + KM green, RAM-guarded. Files: `src/downstream/mod.rs` + `src/mcp/mod.rs` + 3 book chapters + the tree + `docs/TASK_TREE.md` + `CODEBASE_ANALYSIS.md` + CHANGES + DEVELOPMENT_NOTES + MEMORY. Prior: `9980cee`=`DAE.2a.3` (orchestrator routing); `f1388af`=`DAE.2a.2` (catalog); `7142fd7` (**== origin/main**, pushed). Push cadence: `origin/main` at `7142fd7` → **10 commits ahead** after this (`.2c.2`,`.2d`,`.2e`,`.2f`,`BOOK-API-REFERENCE.1`,`DAE.1`,`DAE.2a.1`,`DAE.2a.2`,`DAE.2a.3`,`DAE.2b.1`); push at ~30 (`feedback_push_cadence`).
- active_work_unit: **`DOWNSTREAM-ADAPTER-EXPANSION`** — `.1` ADR + `.2a` (all done) + `.2b.1` (sv2v downstream adapter + MCP) done; `.2b` split into `.2b.1`/`.2b.2`; frontier now **`.2b.2`** (the byte-identical-sensitive `tool_matrix` `sv2v` column mirroring `--iverilog-compile` + the `#[ignore]` real-tool gate + book/USER_GUIDE/README/KM), then `.2c` (slang + the `extract_facts` JSON-AST hook); `.2d+` (surelog/UHDM, commercial-wrapper) future. Other open lanes per `docs/TASK_TREE.md`: `KNOB-ERGONOMICS-AND-PRESETS.1`, `CI-PACKAGING-DISTRIBUTION.1`, `COVERAGE-STEERED-GENERATION.1`, `CAPABILITY-BREADTH-EXPANSION.1/.2` (design-first ADRs) + no-frontier capability trees (`STRUCTURED-EMISSION-EXPANSION`, `SEMANTIC-INTROSPECTION-EXPANSION`+`AGENT-MCP-EXPANSION`, `IDENTITY-DEEPENING`, `SIGNOFF-AUTOMATION-EXPANSION`).
- next_action: **PNT — `DOWNSTREAM-ADAPTER-EXPANSION.2b.2`** (the `tool_matrix` `sv2v` acceptance column, mirroring the `--iverilog-compile` precedent: a `--sv2v` opt-in flag + `sv2v_bin` + `ModuleReport`/`DesignReport.sv2v: Option<ToolInvocation>` [serde skip_serializing_if] routed through the registry, `ModuleCheckpoint`/`DesignCheckpoint` field + `--resume` guard, a `ToolSummary.sv2v_passed/failed` tally + console/JSON line + `MatrixReport.sv2v_enabled`, `unit_divergence` inclusion, an opportunistic `saw_sv2v_*` fact [never a gate], the friendly absent-tool no-op; an `#[ignore]` real-tool gate `tests/sv2v_e2e.rs`; book [`synthesizability.md`/`architecture.md`/`faq.md`] + USER_GUIDE + README CLI surface + a KM card). Default-off ⇒ banked reports + `--resume` + snapshots 6/6 byte-identical. The iverilog-column 19-touchpoint map is the template. Refine `.2b.2` acceptance at pick. Or take an owner steer.
- handoff: repo handoff-ready — code state green after this commit (`cargo test --lib` **549/0**, snapshots 6/6, `tool_matrix` 75/0, anvil+pipeline+divergence_e2e exit 0; introspection schema `1.11`); `mdbook build` clean; `check_memory_architecture` + KM gen/check green (**KM 51 facts**). Working tree clean after this commit.
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); one runner + one classifier not two (`feedback_full_factorization`); every capability opt-in + MCP-invocable + queryable + CLI-as-shim (decision `0017`); design the API for agents not humans (`feedback_api_for_agents_not_humans`); the book is the user-facing surface and must not drift (`feedback_book_doctrine`); decision/KM fact per durable capability or boundary.
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
