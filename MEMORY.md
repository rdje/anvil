# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `STRUCTURED-EMISSION-EXPANSION.12b.2b` **repo-owned tool_matrix --multi-output-task-gate** commit — harness-only, **default-off / DUT byte-identical** (generator unchanged). `src/bin/tool_matrix.rs`: `--multi-output-task-gate` + `ScenarioSet::MultiOutputTaskSweep` + `build_multi_output_task_sweep_scenarios`/`multi_output_task_focus_config` (comb-only `multi_output_task_emit_prob=1.0` × 3 strategies; node-id+egraph; high `terminal_reuse_prob=0.6` + shallow `max_depth=2` + `min_outputs≥2` so co-supported fan-in-independent pairs exist) + `ModuleReport.emitted_multi_output_task` (`__mt(` detection) + `saw_multi_output_task_emit` + `MatrixReport.multi_output_task_gate` + merge + early-return gap arm + units floor + mutual-exclusion + 5 proofs + 8 ModuleReport + 1 Cli fixtures + README/USER_GUIDE/CODEBASE gate entries. **Banked clean** `/tmp/anvil-multi-output-task-gate-r1` (3 scenarios / 12 modules / 6 emitting / `coverage_gaps=[]` / `saw_multi_output_task_emit=true` / `12/0` Verilator + both Yosys + Icarus). `cargo test --bin tool_matrix` 84 + snapshots 6/6. Prior: `21bc84c`=`…12b.2a` metric/schema; `cae38ce`=`…12b.1` live; `46669ec`=`…12a`; `c31fbb0`=`…11`. Push cadence: `origin/main` at `605ec44` → **10 commits ahead** after this ⇒ no push (threshold 30, `feedback_push_cadence`).
- active_work_unit: **`STRUCTURED-EMISSION-EXPANSION` tree `active`, frontier `.12b.3`** (the user-facing docs closeout; decision `0025`; `.11` design + `.12a` design-detail + `.12b.1` live + `.12b.2a` metric/schema + `.12b.2b` repo-owned gate done). Five prior structured surfaces delivered end-to-end (function `.1`+`.2`, generate-for `.3`+`.4`, task `.5`+`.6`, wide-lane part-select `.7`+`.8`, cone-function `.9`+`.10`).
- next_action: **PNT — `STRUCTURED-EMISSION-EXPANSION.12b.3`** (the user-facing docs closeout, docs-only): a `## The sixth surface: a multi-output task automatic` section in `book/src/structured-emission.md` (BYTE-VERIFIED before/after vs the release binary; the co-supported-pair rule; the soundness rule; the `__mt`/`__mtv` form; the metric @ schema 1.14 + the `--multi-output-task-gate`; a skip-sentinelled repro) + the `multi_output_task_emit_prob` knob entry in `book/src/knobs.md` / `USER_GUIDE.md` / README "Current CLI truth" + a KM how-to card `docs/knowledge/multi-output-task-emit.md` (cross-linked to decision `0025`, with a working reverify) + **the deferred `book/src/*` example-JSON `schema_version` `1.13 → 1.14` refresh** (api-tools/agent-mcp/api-introspection/api-reference; the historical `num_mealy_fsm_modules @ 1.13` refs in sequential.md/knobs.md stay). `mdbook build` + `check_knowledge_map` + `check_memory_architecture` + `cargo test --test book_examples` green. Closes `.12b.3`/`.12b`/`.12` — the sixth surface delivered end-to-end; the lane returns to no current frontier (open-ended; future surfaces `.13+`).
- handoff: repo handoff-ready after this commit — `cargo check/clippy/fmt` + `--bin tool_matrix` (84) + `--lib` (600) + snapshots 6/6 green; `--multi-output-task-gate` banked clean; `--introspect` schema 1.14; `check_memory_architecture` + KM check green.
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); one runner + one classifier not two (`feedback_full_factorization`); every capability opt-in + MCP-invocable + queryable + CLI-as-shim (decision `0017`); design the API for agents not humans (`feedback_api_for_agents_not_humans`); the book is the user-facing surface and must not drift (`feedback_book_doctrine`); decision/KM fact per durable capability or boundary.
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
