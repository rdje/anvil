# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `SEMANTIC-INTROSPECTION-EXPANSION.11b.2` **instance_provenance MCP surface + schema 1.23** commit — a **code + docs** change, task-tree-owned by `.11b.2`; **DUT byte-identical** (no IR/generator change; `analyze`/`introspect` read-mostly default-off; `tests/snapshots.rs` untouched). **Closes `.11b`/`.11` — `instance_provenance` delivered end-to-end** (the **tenth** derived `analyze` query; the MCP tool now answers ten). `QUERY_INSTANCE_PROVENANCE` added to `supported_query_kinds()`; `run_analyze` branches by kind (`module`/`design_instance_provenance`) in both arms + the vec-aware `-32602` guard; `analyze_schema` enum + descriptions; `SCHEMA_VERSION 1.22 → 1.23` (16 assertion bumps + 2 new mcp proofs); schema-doc §6.7/changelog + book(agent-mcp/api-tools/api-introspection/api-reference/api-resources-prompts)/USER_GUIDE/README + a KM card (`KNOWLEDGE_MAP` 75 → 76 facts) + CODEBASE_ANALYSIS + ROADMAP. Validation: `cargo test --lib` 690/0/2; snapshots 6/6; clippy `-D warnings` + fmt + `mdbook build` + `book_examples` 3/3 + `check_doctrines` + KM green. E2e `anvil-mcp` smoke (seed 42 hierarchy → schema `1.23`, design top `mod_42_0002`, 2 instances; `u_0`/`mod_42_0000`/`planned_child`, cone `u_0.o_0` → `support_inputs ["i_1"]` + `support_flops [0]` = the CHILD's input+flop across the module boundary; unknown instance ⇒ `-32602`). Prior: `a767415` (`.11b.1`). Push cadence: pushed at `a767415` (30-flush); `origin/main` at `a767415` → **1 commit ahead** after this ⇒ no push (threshold 30).
- active_work_unit: **None at a frontier — back at a PNT decision point.** `SEMANTIC-INTROSPECTION-EXPANSION.11` (`instance_provenance`) **closed** (`.11a`/`.11b.1`/`.11b.2`); the lane returns to a NO-FRONTIER boundary (stays `active`). `STRUCTURED-EMISSION-EXPANSION` also `active` at a NO-FRONTIER boundary (nine structured surfaces). Roadmap/codebase/mdBook aligned (ten derived analyze queries @ schema `1.23`; nine structured surfaces).
- next_action: **PNT — pick the next lane/slice.** Candidates at the no-frontier boundary: (a) an **eleventh** `SEMANTIC-INTROSPECTION-EXPANSION` derived query — natural siblings: a per-FSM/per-memory **reach** (assessed redundant with `input_reach` last time — re-check), `instance_provenance` follow-ups (input-binding chaining: map a child-input cone leaf back to the parent node via `Instance.inputs`; or **recursive multi-level descent** through grand-children), or a per-flop/per-output **path** query. (b) `STRUCTURED-EMISSION-EXPANSION` tenth construct (nested `generate` / `interface`/`modport` — both currently blocked/disqualified; needs a fresh decision + downstream probe). (c) `IDENTITY-DEEPENING` (open-ended whole-module sequential-equivalence boundaries). (d) `SIGNOFF-AUTOMATION-EXPANSION` (more knob axes promoted to first-class matrix gates). Doctrine: **task-tree-own the new leaf BEFORE any `src/` edit**; SCHEMA-DERIVED/default-off/DUT byte-identical; mirror the `.Na` design-detail → `.Nb.1` pure core → `.Nb.2` surface cadence; monitor RAM (`scripts/ram_guard.sh --threshold 90 -- <cmd>`).
- handoff: **repo handoff-ready (clean no-frontier boundary, lane closed).** `scripts/check_doctrines.sh` green (4 doctrines); tree clean; DUT byte-identical. `SEMANTIC-INTROSPECTION-EXPANSION.11` closed end-to-end (ten derived analyze queries @ schema `1.23`). origin/main at `a767415`; **1 commit ahead** after this (pushed the 30-flush at `a767415`; threshold 30 ⇒ no push). A fresh session should PNT a new leaf (see next_action), task-tree-owning it first.
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); one runner + one classifier not two (`feedback_full_factorization`); every capability opt-in + MCP-invocable + queryable + CLI-as-shim (decision `0017`); design the API for agents not humans (`feedback_api_for_agents_not_humans`); SEMANTIC-INTROSPECTION stays SCHEMA-DERIVED / no-shadow-simulator (decisions `0004`/`0011`); the book is the user-facing surface and must not drift (`feedback_book_doctrine`); decision/KM fact per durable capability or boundary. Doctrine-enforcement (decision `0026`): every doctrine = a deterministic check in `scripts/check_doctrines.sh`; code-scoped checks are scope-aware (non-code commits exempt).
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
