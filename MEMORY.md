# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `STRUCTURED-EMISSION-EXPANSION.12b.2a` **multi-output task metric + introspection schema 1.13 → 1.14** commit — **default-off / DUT byte-identical** (a post-hoc `Metrics` field changes no RTL). `Metrics::num_emitted_multi_output_tasks` (= `m.multi_output_task_groups.len()`, `#[serde(default)]`) in `metrics::compute()` + lib proof; surfaced in introspection `module_metrics` ⇒ `SCHEMA_VERSION` `1.13 → 1.14` (doc comment + const + 3 introspect + 8 mcp `schema_version` assertions + `docs/AGENT_INTROSPECTION_SCHEMA.md` `1.13→1.14` changelog/defines/checklist + README + USER_GUIDE current refs + the CODEBASE envelope line brought current from a drifted "1.12"; **book/src example JSONs deferred to `.12b.3`** per the Mealy precedent; historical attributions intact). lib 600, snapshots 6/6; `--introspect` default 0 / forced 1 at schema 1.14. Prior: `cae38ce`=`…12b.1` live surface; `46669ec`=`…12a`; `c31fbb0`=`…11` (decision `0025`). Push cadence: `origin/main` at `605ec44` → **9 commits ahead** after this ⇒ no push (threshold 30, `feedback_push_cadence`).
- active_work_unit: **`STRUCTURED-EMISSION-EXPANSION` tree `active`, frontier `.12b.2b`** (the repo-owned `tool_matrix --multi-output-task-gate`; decision `0025`; `.11` design + `.12a` design-detail + `.12b.1` live + `.12b.2a` metric/schema done; `.12b.3` docs after). Five prior structured surfaces delivered end-to-end (function `.1`+`.2`, generate-for `.3`+`.4`, task `.5`+`.6`, wide-lane part-select `.7`+`.8`, cone-function `.9`+`.10`).
- next_action: **PNT — `STRUCTURED-EMISSION-EXPANSION.12b.2b`** (the repo-owned gate, templated on `--cone-function-gate`): `src/bin/tool_matrix.rs` `--multi-output-task-gate` flag + `ScenarioSet::MultiOutputTaskSweep` + `build_multi_output_task_sweep_scenarios`/focus config (comb-only `multi_output_task_emit_prob=1.0` × 3 construction strategies; `terminal_reuse_prob` high + shallow `max_depth` + `min_outputs≥2` so co-supported independent pairs exist) + `ModuleReport.emitted_multi_output_task` (`__mt(` SV-text detection, distinct from `__t(`/`__cf(`) + `CoverageSummary.saw_multi_output_task_emit` + `MatrixReport.multi_output_task_gate` + early-return gap arm + units floor + proofs + ModuleReport/Cli fixture updates + `test_cli` default + README/USER_GUIDE/CODEBASE gate entries; bank it clean (Verilator + both Yosys + Icarus). Then `.12b.3` (book/USER_GUIDE/README/KM docs + the deferred book schema-JSON refresh). Default-off / DUT byte-identical.
- handoff: repo handoff-ready after this commit — `cargo check/clippy/fmt/test --lib` (600) + snapshots 6/6 all green; `--introspect` schema 1.14; `check_memory_architecture` + KM check green.
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); one runner + one classifier not two (`feedback_full_factorization`); every capability opt-in + MCP-invocable + queryable + CLI-as-shim (decision `0017`); design the API for agents not humans (`feedback_api_for_agents_not_humans`); the book is the user-facing surface and must not drift (`feedback_book_doctrine`); decision/KM fact per durable capability or boundary.
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
