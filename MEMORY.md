# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `LIVE-DOC-DRIFT-FIX.1` **ROADMAP ninth-surface frontier + TOOLBOX schema/analyze currency** commit — a **docs-only** change, task-tree-owned by a new `LIVE-DOC-DRIFT-FIX` tree (single leaf, closed); **DUT byte-identical** (no `src/`). Closes residual live-doc drift found at session bootstrap that `LIVE-DOC-HYGIENE-BACKFILL` did not reach: `ROADMAP.md` still listed `Frontier → .19 (impl)` for the ninth structured surface (`casez_mux_if`) although `.19a`→`.19b.3` all landed (code `src/ir/casez_mux_if_emit.rs`, schema `1.22`, README current) → rewritten to "Nine structured surfaces delivered end-to-end + no-active-frontier"; `TOOLBOX.md` stated the current introspection schema as `1.14` (→ `1.22`) and listed only 4 of the 9 delivered `analyze` queries (→ all nine). Verified `README`/`USER_GUIDE`/`AGENT_INTROSPECTION_SCHEMA`/mdBook already current. Validation: `bash scripts/check_doctrines.sh` green (docs commit ⇒ code-scoped checks exempt); no `src/` ⇒ cargo unaffected (`cargo check --all-targets` green at bootstrap baseline). Prior: `a5afa3b` (`LIVE-DOC-HYGIENE-BACKFILL.2`). Push cadence: `origin/main` at `4564533` → **27 commits ahead** after this ⇒ no push (threshold 30, `feedback_push_cadence`).
- active_work_unit: **None at a frontier — back at a PNT decision point.** `LIVE-DOC-DRIFT-FIX` is **closed** (`.1` ROADMAP + TOOLBOX drift). `SEMANTIC-INTROSPECTION-EXPANSION` + `STRUCTURED-EMISSION-EXPANSION` both stay `active` at NO-FRONTIER boundaries (nine derived analyze queries @ schema `1.22`; nine structured surfaces delivered). All flagged + bootstrap-found doc-hygiene debt now cleared; roadmap/codebase/mdBook realigned.
- next_action: **PNT at a no-frontier boundary (`feedback_pick_and_roll_at_no_frontier`) — self-select the next lane.** Candidates: `SEMANTIC-INTROSPECTION-EXPANSION` (a tenth derived query — a per-FSM/per-memory **reach** [fan-out dual of `fsm_provenance`/`memory_provenance`], or per-child-module provenance; each needs a new `.Na` design-detail leaf + fresh derivation), `STRUCTURED-EMISSION-EXPANSION` (a tenth emitted construct — nested/multi-level `generate` or `interface`/`modport` [the latter empirically disqualified]; needs a fresh decision + empirical probe + `tool_matrix` gate), `IDENTITY-DEEPENING` (deeper module-equivalence / factorization rungs), `SIGNOFF-AUTOMATION-EXPANSION` (new downstream tool columns / implicit-knob sweep axes). Doctrine: **task-tree-own the chosen leaf BEFORE any code**. Monitor RAM (`scripts/ram_guard.sh --threshold 90 -- <cmd>`).
- handoff: **repo handoff-ready after this commit** — `scripts/check_doctrines.sh` green (4 doctrines); `git diff --check` clean; DUT byte-identical (docs-only). ROADMAP/codebase/mdBook realigned (no remaining schema/surface-count/frontier drift in the audited neighborhood). origin/main at `4564533`; 27 commits ahead after this (push threshold 30 ⇒ still no push). Co-staged: `ROADMAP.md` + `TOOLBOX.md` + `docs/tasks/LIVE-DOC-DRIFT-FIX.md` + `docs/TASK_TREE.md` + CHANGES + MEMORY.
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); one runner + one classifier not two (`feedback_full_factorization`); every capability opt-in + MCP-invocable + queryable + CLI-as-shim (decision `0017`); design the API for agents not humans (`feedback_api_for_agents_not_humans`); SEMANTIC-INTROSPECTION stays SCHEMA-DERIVED / no-shadow-simulator (decisions `0004`/`0011`); the book is the user-facing surface and must not drift (`feedback_book_doctrine`); decision/KM fact per durable capability or boundary. Doctrine-enforcement (decision `0026`): every doctrine = a deterministic check in `scripts/check_doctrines.sh`; code-scoped checks are scope-aware (non-code commits exempt).
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
