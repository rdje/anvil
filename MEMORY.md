# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `STRUCTURED-EMISSION-EXPANSION.4b.2b` commit (the repo-owned `tool_matrix --generate-loop-gate`; harness-only / DUT byte-identical; closes `.4b.2`). Prior: `678b682` = `.4b.2a` (metric + schema `1.9`); `2bf8121` = `.4b.1` (live surface). Working tree clean after this commit. Push cadence: `origin/main` at `63d2622` → **28 commits ahead after this commit; push at ~30** (`feedback_push_cadence`).
- active_work_unit: **`STRUCTURED-EMISSION-EXPANSION` tree `active`; `.4`/`.4b` are `active` containers; frontier = `.4b.3`** (the user-facing closeout: extend `book/src/structured-emission.md` with the `generate for` loop surface + the `generate_loop_emit_prob` knob entry in `book/src/knobs.md` / `USER_GUIDE.md` / README + a KM how-to card). `.4b.2` (`.4b.2a` metric + `.4b.2b` gate) is done. The first surface (combinational `function automatic`, `.1`+`.2`) is delivered end-to-end. Future surfaces (`task` [leading], nested/multi-level `generate`, `interface`/`modport`) are `.5+`, each its own decision. Index: `docs/TASK_TREE.md`.
- `.4b.2b` delivered (the gate): `src/bin/tool_matrix.rs` gains `--generate-loop-gate` + `ScenarioSet::GenerateLoopSweep` + `build_generate_loop_sweep_scenarios`/`generate_loop_focus_config` (comb-only `generate_loop_emit_prob=1.0` × 3 strategies) + `MatrixReport.generate_loop_gate` + `ModuleReport.emitted_generate_loop` (from `sv_text.contains("generate")`) + `CoverageSummary.saw_generate_loop_emit` (Verilator + Yosys full plan) + merge + early-return gap arm + 5 proofs + 6 fixture updates. README/USER_GUIDE/CODEBASE_ANALYSIS gate entries. No schema bump (harness-only); snapshots 6/6 byte-identical; `cargo test --bin tool_matrix` 63. **Banked clean** `/tmp/anvil-generate-loop-gate-r1`: 3 scenarios / 12 modules / 8 emitting a loop / `coverage_gaps=[]` / `saw_generate_loop_emit=true` / `12/0` Verilator + both Yosys + Icarus.
- `.4b.1`/`.4b.2a` (prior) delivered: the live `generate for` surface (`generate_loop_emit_prob` config-file knob + `Module.generate_loop_gates` + `src/ir/generate_loop.rs` + the emitter rendering, `gi = gi + 1`) and the `num_emitted_generate_loops` metric + introspection schema `1.8→1.9`. Forced live-surface sweep clean (`/tmp/anvil-gl-r1/`, 5 seeds, `-Wall` Δ=0).
- next_action: **PNT — execute `STRUCTURED-EMISSION-EXPANSION.4b.3`** (the user-facing closeout, **docs-only**): extend the `How It Works` book chapter `book/src/structured-emission.md` with the `generate for` loop surface (a byte-verified before/after; the `{N{x}}` 1-bit-lane rule = `W==N`; the wider-lane part-select exclusion; the `gi = gi + 1` form; the metric + the `tool_matrix --generate-loop-gate` proof) + the `generate_loop_emit_prob` knob entry in `book/src/knobs.md` (the `### Structured emission` subsection beside `function_emit_prob`) + `USER_GUIDE.md` + the README "Current CLI truth" (a config-file-only knob bullet, like `function_emit_prob`) + a Knowledge Map how-to card if a durable how-to is warranted (decision `0013` already carries `answers:`; the `combinational-function-emit` card is the template). Then `.4b.3` closes `.4b`/`.4` and the `generate for` surface is delivered end-to-end; the lane returns to no-active-frontier (open-ended `.5+`). Self-checks: `mdbook build` + `check_knowledge_map` + `check_memory_architecture` + `cargo test --test book_examples` 3/3. Default-off / DUT byte-identical. Doctrine: pure-docs leaf (exempt from the code task-tree rule, but `.4b.3` owns it anyway).
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); every capability is an opt-in knob; decision/KM fact per durable capability or boundary.
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
