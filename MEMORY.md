# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `COVERAGE-STEERED-GENERATION.2c.1` **outer-loop derive + `--steer`** commit — the third **code** slice of the steering lane (the steering-OUT half). `src/introspect/coverage.rs`: `derive_steering_from_coverage(&CoverageReadout, &DeriveParams) -> SteeringConfig` (decision `0023` §4 derive step — per-category `weight = clamp(target_share/max(observed,eps), 0, max_weight)`, milli-quantized for byte-stability, neutral weights omitted) + `DeriveParams`. **Pure read→config function — no generation, no filter (feedback in the orchestration, `feedback_rules_first_generation`).** `src/config.rs`: `SteeringConfig::set_weight` (knob name→per_knob / category→per_category / else `ConfigError::UnknownSteerKey`, reusing `KnobId::category_of_name`+`all` — one classifier), `validate()` made `pub`, `Overrides.steer` (`skip_serializing_if`), `resolve_config` applies preset-then-explicit steer before `validate`. `src/main.rs`: repeatable `--steer <key>=<weight>` flag + `parse_steer_arg` + `cli_overrides` mapping. `README.md`: factual `--steer` CLI-truth bullet. **Unsteered default DUT byte-identical** (no `--steer` ⇒ empty steering ⇒ `.2a` short-circuit). Proven end-to-end: `--steer state=8` ≠ unsteered SV; `--steer state=1.0` (neutral) byte-identical; `--steer bogus=2` errors naming the 6 categories. Full cargo gate green (check/test [snapshots 6/6 + 9 new steer/derive tests]/clippy -D warnings/fmt) + CLI smoke. Prior: `5151891`=`…2b` readout; `2530bfd`=`…2a` core; `02a960d`=`…1` notes; `8f14705`=`…1` ADR. Push cadence: `origin/main` at `7142fd7` → **29 commits ahead** after this; **push at ~30** (`feedback_push_cadence`) — **next commit hits the threshold; push after `.2c.2`.**
- active_work_unit: **`COVERAGE-STEERED-GENERATION` — `.1`+`.2a`+`.2b`+`.2c.1` DONE; frontier = `.2c.2`** (`.2c` pre-split into `.2c.1` code [done] + `.2c.2` docs/close). `.2c.2` = book steering subsection (`algorithm.md`) + `agent-mcp.md` (coverage/steering + the schema-`1.12` example refresh) + USER_GUIDE (the `--steer` shim + the measure→derive→re-steer recipe) + a KM card; then mark `.2c` + `.2` done. `CI-PACKAGING-DISTRIBUTION` stays `active` with no frontier.
- next_action: **PNT — continue.** Advance to `COVERAGE-STEERED-GENERATION.2c.2` (the steering-lane DOCS + close — book/`agent-mcp.md`/USER_GUIDE/KM card + the deferred schema-`1.12` book example refresh; mark `.2c`+`.2` done). Mostly docs ⇒ focused checks + `mdbook build` + KM regen/check (no full cargo gate needed unless code changes); per owner validation policy. **After `.2c.2` commit the count hits ~30 ⇒ PUSH** (`feedback_push_cadence`).
- handoff: repo fully handoff-ready & **in sync** after this commit — full `cargo test` green; snapshots 6/6; `check_memory_architecture` + KM gen/check green (**KM 58 facts**, unchanged — KM card for steering lands at `.2c.2`); mdbook clean; introspection schema `1.12`. Pending doc-drift (tracked, owner-sanctioned, all → `.2c.2` per decision `0023` pre-split): book/USER_GUIDE/KM for the steering lane (the prior multiplier, `SteeringConfig`, the `coverage` readout + MCP tool, `--steer`, the derive helper + outer loop) **and** the book introspection-example refresh (`agent-mcp.md`/`api-tools.md`/`api-reference.md` still show `schema_version "1.11"` + no `coverage_readout` — refreshed holistically at `.2c.2`). `README.md` is current (factual `--steer` + schema `1.12` + `coverage` tool already landed); the authoritative contract doc `docs/AGENT_INTROSPECTION_SCHEMA.md` is at `1.12`.
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); one runner + one classifier not two (`feedback_full_factorization`); every capability opt-in + MCP-invocable + queryable + CLI-as-shim (decision `0017`); design the API for agents not humans (`feedback_api_for_agents_not_humans`); the book is the user-facing surface and must not drift (`feedback_book_doctrine`); decision/KM fact per durable capability or boundary.
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
