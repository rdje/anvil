# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `CI-PACKAGING-DISTRIBUTION.1` commit — the **design ADR** (decision `0022`) for the drop-in CI packaging lane (owner idea 5; binds `0017`; wraps the shipped `anvil hunt`/`0018` + `divergence`/`0019`; consumes `--profile`/`0021`). **Docs-only — decision record + INDEX + tree + `docs/TASK_TREE.md` + `DEVELOPMENT_NOTES.md` + KM (55→56); no `src/`/CI change, DUT byte-identical.** Pinned: (1) a **hand-rolled** `v*`-tag `release.yml` matrix (5 targets → `anvil`+`anvil-mcp` tarballs + `SHA256SUMS`; `cargo-dist` rejected per the dependency-averse ethos); (2) a **composite** GitHub Action (not container) wrapping `anvil hunt`, inputs mapped 1:1 onto the CLI/MCP controls; (3) CLI-shim-over-API invocation (no Action-only path); (4) version-pin + per-bundle `repro.sh`/`knobs.json` reproducibility; (5) user-installed tools (no vendoring). `.2` pre-split `.2a`(release.yml)/`.2b`(action.yml+self-test)/`.2c`(docs). Gate: mem-arch + KM gen/check green (**KM 56**). Prior: `d731087`=`KNOB-ERGONOMICS…2b.2b` (closed that lane's API); `455223e`=`.2b.2a`. Push cadence: `origin/main` at `7142fd7` → **21 commits ahead** after this; **push at ~30** (`feedback_push_cadence`).
- active_work_unit: **`CI-PACKAGING-DISTRIBUTION` — `.1` (design ADR) DONE; frontier = `.2a`** (the `.github/workflows/release.yml` hand-rolled `v*`-tag build matrix → `anvil`+`anvil-mcp` tarballs + `SHA256SUMS`; CI-infra, task-tree-owned) → then `.2b` (composite `action.yml` + entrypoint wrapping `anvil hunt` + artifact upload + exit-on-finding + a self-test job) → `.2c` (README/USER_GUIDE "Use ANVIL in your CI" + KM card). The `KNOB-ERGONOMICS-AND-PRESETS` lane API stays complete (no frontier). Tree stays `active`.
- next_action: **PNT — continue.** Either advance to `CI-PACKAGING-DISTRIBUTION.2a` (the release workflow — real CI infra, task-tree-owned) **or** pick another design-first ADR `.1` (docs-only): `COVERAGE-STEERED-GENERATION.1` (construction-time coverage steering — rules-first, never generate-then-filter) or `CAPABILITY-BREADTH-EXPANSION.1`/`.2` (more SV up-opts / Mealy FSM). Pick-and-roll (`feedback_pick_and_roll_at_no_frontier`); refine acceptance at pick.
- handoff: repo fully handoff-ready & **in sync** after this commit — `cargo check` green (untouched by this docs-only slice); full `cargo test` was green at `d731087`; snapshots 6/6; `check_memory_architecture` + KM gen/check green (**KM 56 facts**); mdbook clean; introspection schema `1.11`. No pending doc-drift.
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); one runner + one classifier not two (`feedback_full_factorization`); every capability opt-in + MCP-invocable + queryable + CLI-as-shim (decision `0017`); design the API for agents not humans (`feedback_api_for_agents_not_humans`); the book is the user-facing surface and must not drift (`feedback_book_doctrine`); decision/KM fact per durable capability or boundary.
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
