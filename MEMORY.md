# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `SEMANTIC-INTROSPECTION-EXPANSION.3` handoff commit (registers the `input_reach` `.3`/`.3a`/`.3b` nodes + the API-audience decision; hash backfills next update; prior: `fcddc1c` = `IDENTITY-DEEPENING.3b.2b.2b`; `06bc2e1` = `.3b.2b.2a`; `314664c` = `.3b.2b.1`). Working tree clean after this commit. Push cadence: `origin/main` at `63d2622` → **7 commits ahead after this commit; push at ~30** (`feedback_push_cadence`).
- active_work_unit: **`IDENTITY-DEEPENING.3b.2b` (cross-module whole-leaf-module sequential equivalence) sub-tree CLOSED** (`.3`/`.3b`/`.3b.2`/`.3b.2b` all done; `IDENTITY-DEEPENING` lane stays `active`, deeper memory/FSM/wrapper/retimed boundaries are named open-ended future leaves, none retired). Now switched to **`SEMANTIC-INTROSPECTION-EXPANSION.3` (`input_reach`)** per owner directive; `.3a` design-detail is the frontier (not yet started). Index: `docs/TASK_TREE.md`.
- `.3b.2b.2b` delivered (DOCS-ONLY, no behaviour change): book `factorization.md` §9b (whole-module sequential equivalence: combined-module materialization + §8b partition reuse + per-output-cone equality + coinduction + exclusions + worked delay-line example) + the "full factorization still means" list + the empirical-counters metric pair; `hierarchy.md` third module-identity layer; USER_GUIDE `hierarchy_sequential_module_dedup` config-knob bullet; ROADMAP gap 2 + capability-lanes "delivered" status; new KM card `docs/knowledge/sequential-module-dedup.md` (regenerated `KNOWLEDGE_MAP.md`, 32 facts). `mdbook build` clean; `cargo test --test book_examples` 3/3 (84.7s); KM + mem-arch self-checks clean. DUT byte-identical (no source change).
- next_action: **owner chose the next lane (2026-06-16): `SEMANTIC-INTROSPECTION-EXPANSION.3` = `input_reach`** (the dual fan-OUT of the delivered `output_support` cone). Registered `.3` (container) + `.3a` (design-detail, **frontier**) + `.3b` (impl) in `docs/tasks/SEMANTIC-INTROSPECTION-EXPANSION.md`. **Resume by executing `.3a`** (a no-source design-detail leaf): ground `input_reach` in `src/introspect/analyze.rs` (already read) — pin the result shape + whether `DerivedAnalysis.results` reuses `Vec<SupportCone>` or grows a second shape (drives a schema MINOR bump `1.4→1.5` vs reuse), the derivation (invert per-output/flop-D support via the existing `module_support_cones` builder vs forward consumers BFS — pure, no IR/generator change), `target` addressing (input port name / `flop:<id>` / instance output; unknown ⇒ `-32602`), and the schema decision; write the DEVELOPMENT_NOTES design-detail entry; then `.3b` impl. **API-audience steering (owner, 2026-06-16, `feedback_api_for_agents_not_humans`):** this API is for AI agents, not humans — design for machine-friendly *completeness*/structure/speed (full reach sets, all-targets, explicit ids), not human-terse digests, within the unchanged SCHEMA-DERIVED ceiling. **Fresh session OK — repo is handoff-ready** (clean tree, all gates green; this is a clean lane boundary). Resume from `docs/TASK_TREE.md` → `SEMANTIC-INTROSPECTION-EXPANSION`.
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); every capability is an opt-in knob; decision/KM fact per durable capability or boundary.
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
