# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `ACCEPTANCE-DIVERGENCE-HUNTING.2a` commit (**first code leaf of lane 2; pure byte-identical refactor** — `src/downstream/mod.rs` + `src/hunt/mod.rs` + `CODEBASE_ANALYSIS` + the tree + `docs/TASK_TREE.md` + DEVELOPMENT_NOTES + ROADMAP + CHANGES + MEMORY) — **extracted the shared `pub fn downstream::tool_verdict(&ToolInvocation) -> ToolVerdict{Accept,Warn,Reject}`** (snake_case serde) from `hunt::run`'s inline detection, so the upcoming `divergence::run` (`.2b`) and the hunt share **one** accept/warn/reject classifier (no second classifier; `feedback_full_factorization`). `hunt::classify_detection` derives from it byte-identically (`Warn`⇒"warning", else⇒"reject"; `Accept` unreachable on a failing tool). +1 `downstream` proof; the `hunt` `classify_detection` proof unchanged. `cargo test --lib` 522/0 (downstream:: 21/0, hunt:: 11/11); snapshots 6/6 byte-identical; clippy/fmt clean. Default-off / DUT byte-identical. Prior: `e7a3de8` = `.1` design ADR (decision `0019`); `d2ddbbf` = `BUG-HUNT-ORCHESTRATION.2e` (tree closed); … `b90bdff` (**pushed** — `origin/main` at `b90bdff`). Working tree clean after this commit. Push cadence: `origin/main` at `b90bdff` → **26 commits ahead** after this; push at ~30 (`feedback_push_cadence`) — close, push after the next 4.
- active_work_unit: **`ACCEPTANCE-DIVERGENCE-HUNTING`** — `.1` design ADR **done** (decision `0019`); `.2a` shared `downstream::tool_verdict` classifier extract **done** (byte-identical hunt); frontier **`.2b`** (the `src/divergence/` library core). Other owner-directed lanes still design-first `.1`: `DOWNSTREAM-ADAPTER-EXPANSION`, `KNOB-ERGONOMICS-AND-PRESETS`, `CI-PACKAGING-DISTRIBUTION`, `COVERAGE-STEERED-GENERATION`, `CAPABILITY-BREADTH-EXPANSION`. Plus no-frontier `STRUCTURED-EMISSION-EXPANSION` (5 surfaces; 6th = future `.11`), `SEMANTIC-INTROSPECTION-EXPANSION` + `AGENT-MCP-EXPANSION`, `IDENTITY-DEEPENING`, `SIGNOFF-AUTOMATION-EXPANSION`, `LOCAL-REFERENCE-CACHE`. Index: `docs/TASK_TREE.md`.
- next_action: **PNT — implement `ACCEPTANCE-DIVERGENCE-HUNTING.2b`** (the `src/divergence/` library core): new `src/divergence/mod.rs` + `pub mod divergence` — `ToolVerdict` (reuse) / `Divergence{kind,tools}` / `DivergenceReport{run_id,lane,kind,top,sandbox,verdicts:[ToolVerdict-projection],diverged,divergences,declined}` / `DivergenceOptions{tools,yosys_mode,mem_limits,sandbox_root,keep_sandbox}` + `divergence::run(seed,cfg,&DivergenceOptions)->Result<DivergenceReport>` composing `downstream::generate_dut_artifact` + `run_verilator`/`run_yosys`/`run_iverilog_compile` (every enabled tool/mode to completion — no fold, no short-circuit) + `downstream::tool_verdict` + the multi-tool same-version divergence classifier (`accept_reject`|`accept_warn`|`warn_reject`). Cargo-portable proofs (synthetic accept/reject `ToolInvocation` set ⇒ `accept_reject`; no-tools ⇒ friendly no-op). No CLI/MCP/version axis yet (`.2c`/`.2d`/`.2e`). Default-off / DUT byte-identical (snapshots 6/6). `.2b` is task-tree-owned (registered in `e7a3de8`).
- handoff: repo handoff-ready — code state green (`cargo test --lib` 522/0, introspection schema `1.11`); `check_memory_architecture` + KM gen/check green (**KM 48 facts**, no new card for `.2a`). Working tree clean after commit.
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); one classifier not two (`feedback_full_factorization`); every capability opt-in + MCP-invocable + queryable + CLI-as-shim (decision `0017`); the divergence detector adds **no** generator path / no oracle (it composes the existing surfaces); decision/KM fact per durable capability or boundary.
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
