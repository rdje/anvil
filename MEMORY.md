# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `SEMANTIC-INTROSPECTION-EXPANSION.7b.2` **memory_provenance MCP surface + schema 1.19** commit — a **code change** (`src/mcp/mod.rs` + `src/introspect/{analyze,mod}.rs` + docs/book/KM), task-tree-owned by `.7b.2`; **DUT byte-identical** (`tests/snapshots.rs` untouched). Closes `.7b`/`.7` ⇒ the **sixth** derived `analyze` query, `memory_provenance` (per-inferrable-memory port provenance — **the first to open the documented opaque-`MemRead`-leaf boundary**), is **delivered end-to-end**: registry + `run_analyze` dispatch (`module`/`design_memory_provenance`) in one commit + the vec-aware `-32602` guard + `SCHEMA_VERSION 1.18 → 1.19` (12 assertion bumps + 2 new MCP proofs) + the `analyze_schema` enum (the `"mem:<id>"` target) + schema-doc §6.7/changelog + book(agent-mcp/api-tools — also fixed the pre-existing missing `flop_dependencies` api-tools entry)/USER_GUIDE/README + a KM card (`KNOWLEDGE_MAP` 71 → 72 facts) + an e2e `anvil-mcp` stdio smoke (seed 7 `memory_prob 1.0` → schema `1.19`, 1 `simple_dual_port` memory with real port cones; unknown `mem:99999` → `-32602`). Validation: `cargo test --lib` 653/0/2; snapshots 6/6; clippy `-D warnings` + fmt + `mdbook build` + `book_examples` 3/3 + `check_doctrines` green. Prior: `305b5a5` (`.7b.1`). Push cadence: `origin/main` at `4564533` → **15 commits ahead** after this ⇒ no push (threshold 30, `feedback_push_cadence`).
- active_work_unit: **`SEMANTIC-INTROSPECTION-EXPANSION` — SIX derived queries delivered end-to-end (schema `1.19`); back at a NO-FRONTIER boundary (stays `active`).** `output_support`/`input_reach`/`flop_reset_provenance`/`module_reachability` (decision `0011`'s four) + `flop_dependencies` (`.6`) + `memory_provenance` (`.7`, the second beyond the four named kinds, the first to open the opaque-`MemRead` boundary). Nothing retired; further derived-query kinds are open-ended breadth (FSM analog `fsm_provenance` is the recorded next sibling; a per-memory reach is a further follow-up).
- next_action: **PNT at a no-frontier boundary (`feedback_pick_and_roll_at_no_frontier`) — self-select the next lane.** All active owner-directed lanes are open-ended: `SEMANTIC-INTROSPECTION-EXPANSION` (6 queries delivered; candidate next: `fsm_provenance` — the `sel` cone + encoding/state-count/Mealy facts behind the opaque `FsmOut` leaf, the direct sibling of `memory_provenance`; or a node-level immediate-fanin `node_drivers` query), `STRUCTURED-EMISSION-EXPANSION` (9 surfaces; `.20+` nested/multi-level `generate` has no by-construction source + `interface`/`modport` empirically disqualified — needs a genuinely new construct + fresh probe + decision), `IDENTITY-DEEPENING` (deeper module-equivalence: memory/FSM/wrapper/retimed-state), `SIGNOFF-AUTOMATION-EXPANSION` (new tool columns / implicit-knob axes). Doctrine: **task-tree-own the chosen leaf BEFORE any code** (a `.Na` design-detail leaf + `docs/TASK_TREE.md` row first; per-query lanes follow the `.Na`/`.Nb.1`/`.Nb.2` cadence, no new numbered decision when an existing decision governs). Monitor RAM (`scripts/ram_guard.sh --threshold 90 -- <cmd>`).
- handoff: **repo handoff-ready after this commit** — `scripts/check_doctrines.sh` green (4 doctrines); `git diff --check` clean; DUT byte-identical (snapshots 6/6); the sixth derived query is delivered end-to-end (live + schema-versioned + MCP-dispatched + cached + documented + KM-carded + e2e-smoked). A long, productive session (3 slices: `.7a` + `.7b.1` + `.7b.2`, after a full bootstrap read) ⇒ a fresh session is reasonable before picking the next lane. origin/main at `4564533`; 15 commits ahead after this. Co-staged: `src/introspect/analyze.rs` + `src/introspect/mod.rs` + `src/mcp/mod.rs` + `docs/AGENT_INTROSPECTION_SCHEMA.md` + `book/src/agent-mcp.md` + `book/src/api-tools.md` + `USER_GUIDE.md` + README + `docs/knowledge/semantic-introspection-memory-provenance.md` + `docs/knowledge/semantic-introspection-analyze-tool.md` + `KNOWLEDGE_MAP.md` + `CODEBASE_ANALYSIS.md` + `ROADMAP.md` + `docs/tasks/SEMANTIC-INTROSPECTION-EXPANSION.md` + `docs/TASK_TREE.md` + CHANGES + MEMORY.
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); one runner + one classifier not two (`feedback_full_factorization`); every capability opt-in + MCP-invocable + queryable + CLI-as-shim (decision `0017`); design the API for agents not humans (`feedback_api_for_agents_not_humans`); SEMANTIC-INTROSPECTION stays SCHEMA-DERIVED / no-shadow-simulator (decisions `0004`/`0011`); the book is the user-facing surface and must not drift (`feedback_book_doctrine`); decision/KM fact per durable capability or boundary. Doctrine-enforcement (decision `0026`): every doctrine = a deterministic check in `scripts/check_doctrines.sh`; code-scoped checks are scope-aware (non-code commits exempt).
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
