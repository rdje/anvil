# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this **`BOOK-LANE-COVERAGE.1`** (register) commit — **docs-only** ⇒ **DUT byte-identical**. Registered the owner-relayed **PGEN** report: the mdBook has **no microdesign/frontend lane chapters** (mentions only in introduction/faq/API-reference); coverage-shaped book drift, confirmed by session audit. Recorded KM card `pgen-first-contact-parser-gap` (consumer-reported: ANVIL output exposed a missing `case`/`endcase` production in PGEN's `grammars/rtl_frontend.ebnf` within minutes). Live lane runs (microdesign seed 7, frontend seed 0) exposed a REAL bug: **frontend `--out` writes `child_0.sv` not the documented `acc_0.sv`** (`parse_top_name` in `src/main.rs` takes the FIRST `module` decl; frontend emits child stubs before the top) — owned by sibling tree `LANE-OUT-FILENAME`. Prior: `98e31e1` (`EVIDENCE-BANK-DURABILITY.2`, decision `0030`: committed per-bank digests under `docs/evidence/`; pre-0030 `/tmp` citations = labelled breadcrumbs). Push cadence: 2 ahead of `origin/main`; 30-commit threshold.
- active_work_unit: **`BOOK-LANE-COVERAGE`** (`active`; `.1` `done`) + sibling fix **`LANE-OUT-FILENAME`** (next: register+fix in one task-tree-managed commit — add `pub top: String` to `LaneArtifact` (all 3 umbrella lanes populate it from builder truth: DUT `design.top.clone()`, microdesign new `top_name(seed)` helper = `mc_<seed>` also used by `emit_sv`/`emit_manifest`, frontend `unit.top.name`), replace `parse_top_name` use in `src/main.rs` `run_non_dut_lane` with `artifact.top` + delete the fn, add lane-top unit tests + `--out` filename proof). Then `.2` microdesign chapter (+ new "Artifact Lanes" SUMMARY part between Motif Catalogue and Reference), `.3` frontend chapter + introduction/faq cross-links. Real outputs banked in scratchpad this session. NOTE: every book ```bash block is executed by `tests/book_examples.rs` (use `cargo run --release --`; no external tools without the skip sentinel). After this unit: `EVIDENCE-BANK-DURABILITY.3` (mechanize 0030), `.4` (breadcrumb sweep; `/tmp/anvil-validate-3f1c…` is illustrative — do not label), `.5` deferred; also `CAPABILITY-BREADTH-EXPANSION.1` (`pending` ADR).
- next_action: land `LANE-OUT-FILENAME.1` (code fix above; full `cargo test` under `ram_guard --threshold 90`, clippy/fmt, spot-verify `acc_0.sv`/`mc_7.sv` out filenames), then `BOOK-LANE-COVERAGE.2`/`.3` chapters (`mdbook build book` + `cargo test --test book_examples` per leaf), then resume `EVIDENCE-BANK-DURABILITY.3`.
- handoff: **repo handoff-ready.** `check_doctrines.sh` green (4/4); tree clean; docs-only commit. A fresh session resumes at `LANE-OUT-FILENAME.1` (see next_action).
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); one runner + one classifier not two (`feedback_full_factorization`); every capability opt-in + MCP-invocable + queryable + CLI-as-shim (decision `0017`); design the API for agents not humans (`feedback_api_for_agents_not_humans`); SEMANTIC-INTROSPECTION stays SCHEMA-DERIVED / no-shadow-simulator (decisions `0004`/`0011`); the book is the user-facing surface and must not drift (`feedback_book_doctrine`); decision/KM fact per durable capability or boundary. Doctrine-enforcement (decision `0026`): every doctrine = a deterministic check in `scripts/check_doctrines.sh`; code-scoped checks are scope-aware (non-code commits exempt).
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
