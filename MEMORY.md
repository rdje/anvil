# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `STRUCTURED-EMISSION-EXPANSION.15b.2` **seventh-surface metric (schema 1.15) + tool_matrix --mux-if-gate** commit — **CODE change** (metric + repo-owned downstream gate) / default-off ⇒ **DUT byte-identical** (the metric is RTL-invisible). Metric: `Metrics::num_emitted_mux_if_blocks` (`= m.mux_if_gates.len()`, `#[serde(default)]`) in `src/metrics.rs` + the `metrics_count_emitted_mux_if_blocks` lib proof ⇒ introspection `SCHEMA_VERSION` `1.14 → 1.15` (bumped all current-output refs: `introspect/mod.rs` const + doc-comment + 3 assertions, 8 `mcp/mod.rs` assertions, `docs/AGENT_INTROSPECTION_SCHEMA.md` §7 ×3 + a `1.14→1.15` changelog entry, README ×2 + USER_GUIDE ×1 + `CODEBASE_ANALYSIS.md` envelope; `num_emitted_multi_output_tasks @ 1.14` historical attributions intact; book example JSONs deferred to `.15b.3`). Gate: `tool_matrix --mux-if-gate` (`ScenarioSet::MuxIfSweep` + `build_mux_if_sweep_scenarios`/`mux_if_focus_config` [comb-only, `mux_if_emit_prob=1.0`, `comb_mux_prob=0.9` + `comb_mux_encoding_prob=1.0` forcing the encoded chained-ternary path that builds plain `GateOp::Mux` gates] × 3 strategies + `ModuleReport.emitted_mux_if` [`__cv` detection] + `saw_mux_if_emit` + merge + `MatrixReport.mux_if_gate` + run-plan/mutex/dispatch/summarize/gap-arm/slug wiring + `test_cli` default + 5 proofs + 8 fixtures), templated on `--multi-output-task-gate`. `cargo check`/`clippy`/`fmt` clean; `cargo test --lib` **616** (615 + 1); `--bin tool_matrix` **89** (84 + 5); snapshots 6/6 byte-identical; banked clean `/tmp/anvil-mux-if-gate-r1` (3 scenarios / 12 modules / `coverage_gaps=[]` / `saw_mux_if_emit=true` / 12/12 emit `__cv` / 215 blocks / Verilator 12/0 + both Yosys 12/0 + Icarus 12/0 / exit 0). Prior: `5d2fdaa`=`…15b.1`. Push cadence: `origin/main` at `605ec44` → **25 commits ahead** after this ⇒ no push (threshold 30, `feedback_push_cadence`).
- active_work_unit: **`STRUCTURED-EMISSION-EXPANSION` — frontier `.15b.3`** (the user docs; `.14` design + `.15a` design-detail + `.15b.1` live surface + `.15b.2` metric+gate all `done`). `.15b.3` = the user-facing closeout for the seventh surface: a `book/src/structured-emission.md` "seventh surface" section (byte-verified before/after example), the `mux_if_emit_prob` / `--mux-if-emit-prob` / `--mux-if-gate` entries in `book/src/knobs.md` + `USER_GUIDE.md` + README "Current CLI truth", the **deferred** book example-JSON schema-version bumps (`1.14 → 1.15`), and the KM how-to card (`0027` already carries `answers:`). Docs-only ⇒ DUT byte-identical. Other `active` lanes at no-frontier boundaries.
- next_action: **PNT → `STRUCTURED-EMISSION-EXPANSION.15b.3`** (user docs: book seventh-surface section + a byte-verified example + the `mux_if_emit_prob`/`--mux-if-emit-prob`/`--mux-if-gate` knob+flag+gate entries in book/knobs + USER_GUIDE + README + the deferred book example-JSON `1.14→1.15` bumps + a KM card). Docs-only / DUT byte-identical; gates = `mdbook build book` + `check_knowledge_map` + `check_memory_architecture` + `cargo test --test book_examples` 3/3. The owning leaf `.15b.3` is already pre-split. After `.15b.3` the seventh surface is delivered end-to-end and the lane returns to a no-frontier boundary (PNT-select the eighth surface / next lane). Monitor RAM on heavy builds (`scripts/ram_guard.sh --threshold 90 -- <cmd>`).
- handoff: repo handoff-ready after this commit — `scripts/check_doctrines.sh` green (4 doctrines); `git diff --check` clean; `cargo check/clippy/fmt` + `cargo test --lib` 616 + `--bin tool_matrix` 89 + snapshots 6/6 green; `--mux-if-gate` banked clean `/tmp/anvil-mux-if-gate-r1`; default-off ⇒ DUT byte-identical. Co-staged: `src/metrics.rs` + `src/introspect/mod.rs` + `src/mcp/mod.rs` + `src/bin/tool_matrix.rs` + `docs/AGENT_INTROSPECTION_SCHEMA.md` + README + USER_GUIDE + `CODEBASE_ANALYSIS.md` + `DEVELOPMENT_NOTES.md` + tree file + `docs/TASK_TREE.md` + `CHANGES.md` + `MEMORY.md`. (KM unchanged — `0027` already carries `answers:`; book user docs deferred to `.15b.3`.)
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); one runner + one classifier not two (`feedback_full_factorization`); every capability opt-in + MCP-invocable + queryable + CLI-as-shim (decision `0017`); design the API for agents not humans (`feedback_api_for_agents_not_humans`); the book is the user-facing surface and must not drift (`feedback_book_doctrine`); decision/KM fact per durable capability or boundary. Doctrine-enforcement (decision `0026`): every doctrine = a deterministic check in `scripts/check_doctrines.sh`; code-scoped checks are scope-aware (non-code commits exempt).
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
