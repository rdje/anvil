# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `ACCEPTANCE-DIVERGENCE-HUNTING.2c.1` commit (**the `hunt::run` fold** — `src/divergence/mod.rs` (new `classify_report`) + `src/hunt/mod.rs` (`HuntRequest.divergence` + `HuntFailure.divergence` + the finding-path fold) + `src/mcp/mod.rs` + `src/main.rs` (`divergence:false` shims) + `CODEBASE_ANALYSIS` + the tree + `docs/TASK_TREE.md` + DEVELOPMENT_NOTES + ROADMAP + CHANGES + MEMORY). Extracted `divergence::classify_report(&ValidateReport)` (pure projection half of `run`; byte-identical) so `hunt::run` classifies the tools `validate` ALREADY ran on a finding — no re-validation. On the finding path (`!report.ok`), a `req.divergence` result where one tool accepted what another rejected/warned refines `detection="acceptance_divergence"` + attaches the report; NOT minimized (the `validate` oracle can't preserve a cross-tool disagreement, the `cross_sim_mismatch` rationale). MCP/CLI set `divergence:false` (arg-wiring=`.2d`, byte-identical). +2 proofs. `cargo test --lib` 531/0 (divergence:: 8/8, hunt:: 12/12); snapshots 6/6 byte-identical + book_examples 3/3; clippy/fmt clean. Default-off / DUT byte-identical. Prior: `4028537` = `.2b` (`src/divergence/` core); `dd6c9eb` = `.2a`; `e7a3de8` = `.1` (decision `0019`); `d2ddbbf` = `BUG-HUNT-ORCHESTRATION.2e`; … `b90bdff` (**pushed** — `origin/main` at `b90bdff`). Working tree clean after this commit. Push cadence: `origin/main` at `b90bdff` → **28 commits ahead** after this; push at ~30 (`feedback_push_cadence`) — **push after the next ~2** (after `.2c.2`/`.2d`).
- active_work_unit: **`ACCEPTANCE-DIVERGENCE-HUNTING`** — `.1` ADR (`0019`) + `.2a` (`tool_verdict`) + `.2b` (`src/divergence/` core) + `.2c.1` (the `hunt::run` fold) all **done**; frontier **`.2c.2`** (the `tool_matrix` divergence column). Other owner-directed lanes still design-first `.1`: `DOWNSTREAM-ADAPTER-EXPANSION`, `KNOB-ERGONOMICS-AND-PRESETS`, `CI-PACKAGING-DISTRIBUTION`, `COVERAGE-STEERED-GENERATION`, `CAPABILITY-BREADTH-EXPANSION`. Plus no-frontier `STRUCTURED-EMISSION-EXPANSION`, `SEMANTIC-INTROSPECTION-EXPANSION`+`AGENT-MCP-EXPANSION`, `IDENTITY-DEEPENING`, `SIGNOFF-AUTOMATION-EXPANSION`, `LOCAL-REFERENCE-CACHE`. Index: `docs/TASK_TREE.md`.
- next_action: **PNT — implement `ACCEPTANCE-DIVERGENCE-HUNTING.2c.2`** (the `tool_matrix` divergence column): a `--divergence` opt-in flag + `ModuleReport`/`DesignReport.divergence: Option<DivergenceReport>` (the `diff_sim: Option<DiffSimReport>` column precedent) populated via `divergence::classify_report` over the per-unit `ToolInvocation`s the matrix already ran + the **opportunistic** `saw_acceptance_divergence` coverage fact (recorded when seen — **NEVER a required gate**; all-agree is the valid-by-construction steady state) + reuse `classify_diff_sim_axis` for the representative subset. Cargo-portable proofs; snapshots 6/6 byte-identical. `.2c.2` is task-tree-owned. **Watch `tool_matrix` build weight under ram_guard** (it's a ~5k-line binary). Then `.2d` (MCP `divergence` tool + CLI `--divergence`), `.2e` (version axis), `.2f` (real-tool gate + book/USER_GUIDE/README/KM closeout).
- handoff: repo handoff-ready — code state green (`cargo test --lib` 531/0, introspection schema `1.11`); `check_memory_architecture` + KM gen/check green (**KM 48 facts**, no new card for `.2c.1`). Working tree clean after commit.
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); one classifier not two (`feedback_full_factorization`); every capability opt-in + MCP-invocable + queryable + CLI-as-shim (decision `0017`); the divergence detector adds **no** generator path / no oracle (it composes the existing surfaces); decision/KM fact per durable capability or boundary.
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
