# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `STRUCTURED-EMISSION-EXPANSION.2a` commit (combinational `function automatic` impl design-detail; **DOCS-ONLY**, no source change; DUT byte-identical; hash backfills next update; prior: `095e471` = `.1`; `d7a7eef` = `.5b.2`). Working tree clean after this commit. Push cadence: `origin/main` at `63d2622` → **18 commits ahead after this commit; push at ~30** (`feedback_push_cadence`).
- active_work_unit: **`STRUCTURED-EMISSION-EXPANSION` tree `active` (owner-directed `2026-06-16`); frontier = `.2b.1`.** `.1` design (decision `0012`) + `.2a` design-detail done. The first richer-structured surface = a default-off, valid-by-construction combinational `function automatic` emit-projection; `.2a` pinned the **first concrete cut**: a single-gate "operand function" via a gen-time `annotate_function_emit_gates` pass marking `Module.function_emit_gates: BTreeSet<NodeId>` (the `soft_union.rs` precedent), rendered as a `<wire>__f` `function automatic` decl + positional-param body + call. `.2` pre-split → `.2b.1` (live surface, frontier) + `.2b.2` (downstream gate + closeout). Index: `docs/TASK_TREE.md`. (Prior lane `SEMANTIC-INTROSPECTION-EXPANSION` `active`/no-frontier — all 4 query kinds delivered at schema `1.7`.)
- `.2a` delivered (docs-only, design of record in DEVELOPMENT_NOTES): grounded in `src/emit/sv.rs` (`to_sv_with_modules` + `build_names`/`node_ref`/`render_gate`/`param_width_decl_w`) + `src/ir/soft_union.rs` (`Module.soft_union_slice_gates` + `annotate_soft_union_slices`). First cut = single-gate function over direct operands (minimal cone ⇒ zero sharing hazard; multi-level cone body = follow-up). Gen-time annotate → `Module.function_emit_gates`; `function automatic logic[W-1:0] <wire>__f(positional params)` + body = op over param names (positional render variant, handles dup operands) + `assign <wire> = <wire>__f(operand refs)`. `Config::function_emit_prob` default `0.0` byte-identical. Gate = Verilator + both Yosys + Icarus + `saw_combinational_function_emit` + `num_emitted_combinational_functions`.
- next_action: **PNT — execute `STRUCTURED-EMISSION-EXPANSION.2b.1`** (the live first-cut surface — **real emitter change**): add `Config::function_emit_prob: f64` (default `0.0`, `default_function_emit_prob()` serde default) beside `aggregate_prob`/`soft_union_slice_prob`; add `Module.function_emit_gates: BTreeSet<NodeId>` (default empty; emitter-surface annotation only — leave flat IR / validators / CSE / `canonical_module_signature` untouched, like `soft_union_slice_gates`); new `src/ir/function_emit.rs` `annotate_function_emit_gates(m, rng, prob)` (collect non-structured [not CaseMux/CasezMux/ForFold], non-`soft_union`-marked `Gate` candidates with ≥1 operand, roll `gen_bool(prob)`, mark); wire the gen-time call-site roll guarded on `prob > 0.0` (beside the `soft_union`/`aggregate` rolls in the generator); in `to_sv_with_modules` emit a `function automatic` decl section (before the gate `assign`s) for marked gates + replace the marked gate's `assign` with the `<wire>__f(operand refs)` call (positional-param body via a `render_gate` positional variant). Lib proofs: a marked gate emits a behaviour-preserving function + call; default-off byte-identical; the mark leaves CSE/`canonical_module_signature` untouched. `cargo test --lib` + snapshots 6/6 byte-identical (default-off); clippy `-D warnings` + fmt; a forced `function_emit_prob=1.0` sample lints clean under Verilator. Then `.2b.2` (the repo-owned gate + metric + coverage fact + book/USER_GUIDE/KM). Design of record: the `.2a` DEVELOPMENT_NOTES entry. **`.2b.1` is a focus-intensive emitter slice — a fresh session is reasonable to start it.** Doctrine: rules-first, default-off/byte-identical, snapshot-acceptance protocol if output legitimately changes (it must NOT, default-off), no code without a task-tree leaf owning it.
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); every capability is an opt-in knob; decision/KM fact per durable capability or boundary.
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
