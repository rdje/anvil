# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `DOWNSTREAM-ADAPTER-EXPANSION.2c.2a` commit — the `tool_matrix --slang` elaboration-acceptance column (decision `0020`), a faithful `--sv2v` mirror; the byte-identical-sensitive half of the `.2c.2` split. **`src/bin/tool_matrix.rs` + `tests/slang_e2e.rs` + docs/KM; default-off / banked reports + `--resume` + snapshots 6/6 byte-identical when `--slang` off, no generator change.** `Cli.slang`/`slang_bin` + `ModuleReport`/`DesignReport.slang` (serde skip_serializing_if) via `AcceptanceTool::Slang.adapter().run(&cx)` (`ModuleToolColumns` 4→5) + checkpoint + `--resume` guard + `ToolSummary.slang_passed/failed` (in `any_failed`) + console line + `MatrixReport.slang_enabled` + `unit_divergence` inclusion + `test_cli()` fields. **Friendly no-op: gated on a `tool_version` presence probe** (real smoke: `--skip-verilator --skip-yosys --slang` over 17 modules ⇒ exit 0, `slang 0/0`, 0 invocations). `tests/slang_e2e.rs` = portable public-API proof (`supports_facts=true`) + `#[ignore]` real-tool accept gate. Docs: README/USER_GUIDE/`synthesizability.md` + a `docs/knowledge/slang-adapter.md` KM card (52→**53**). Gate: tool_matrix **77/0** (+1 CLI proof), slang_e2e portable 1/0, snapshots **6/6**, book_examples 3/3, lib unaffected (bin-only), clippy/fmt clean, mdbook clean, mem-arch + KM green. Prior: `d9f251e`=`DAE.2c.1`; `a3c70b9`=`DAE.2b.2`; `7142fd7` (**== origin/main**, pushed). Push cadence: `origin/main` at `7142fd7` → **13 commits ahead** after this; push at ~30 (`feedback_push_cadence`).
- active_work_unit: **`DOWNSTREAM-ADAPTER-EXPANSION`** — `.1` ADR + `.2a` + `.2b` (sv2v) + `.2c.1` (slang adapter + extract_facts hook + MCP) done; `.2c.2` split into `.2c.2a` (the `tool_matrix --slang` column — **done**) / `.2c.2b`; frontier now **`.2c.2b`** (the live `extract_facts` fact-surfacing: `ModuleReport`/`DesignReport.slang_facts: Option<AdapterFacts>` serde-skip, populated from the slang column's `--ast-json` side file + a `saw_slang_facts` coverage fact + the `slang_e2e` `#[ignore]` gate extended to assert facts + the book "fact-bearing" note + the KM card updated); `.2d+` (surelog/UHDM, commercial-wrapper) future. Other open lanes per `docs/TASK_TREE.md`: `KNOB-ERGONOMICS-AND-PRESETS.1`, `CI-PACKAGING-DISTRIBUTION.1`, `COVERAGE-STEERED-GENERATION.1`, `CAPABILITY-BREADTH-EXPANSION.1/.2` (design-first ADRs) + no-frontier capability trees (`STRUCTURED-EMISSION-EXPANSION`, `SEMANTIC-INTROSPECTION-EXPANSION`+`AGENT-MCP-EXPANSION`, `IDENTITY-DEEPENING`, `SIGNOFF-AUTOMATION-EXPANSION`).
- next_action: **PNT — `DOWNSTREAM-ADAPTER-EXPANSION.2c.2b`** (refine acceptance at pick): add `ModuleReport`/`DesignReport.slang_facts: Option<AdapterFacts>` (`#[serde(default, skip_serializing_if = "Option::is_none")]` ⇒ byte-identical when off), populate it by calling `AcceptanceTool::Slang.adapter().extract_facts(&cx, &inv)` right after the slang column runs in `run_module_tools`/`run_design_tools` (the `--ast-json` side file is in `scenario_dir`); an opportunistic `saw_slang_facts` coverage fact (never a required gate); extend the `tests/slang_e2e.rs` `#[ignore]` gate to assert `facts.top`/`ports`/`instances` when slang is present; the book "fact-bearing adapter" note + the KM card updated. Default-off ⇒ byte-identical.
- handoff: repo handoff-ready — code state green after this commit (`cargo test --bin tool_matrix` **77/0**, slang_e2e portable 1/0, snapshots 6/6, book_examples 3/3, `cargo test --lib` 553/0; introspection schema `1.11`); `mdbook build` clean; `check_memory_architecture` + KM gen/check green (**KM 53 facts**). Working tree clean after this commit.
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); one runner + one classifier not two (`feedback_full_factorization`); every capability opt-in + MCP-invocable + queryable + CLI-as-shim (decision `0017`); design the API for agents not humans (`feedback_api_for_agents_not_humans`); the book is the user-facing surface and must not drift (`feedback_book_doctrine`); decision/KM fact per durable capability or boundary.
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
