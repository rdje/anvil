# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `SEMANTIC-INTROSPECTION-EXPANSION.15a` **reach_path design-detail** commit — a **docs/design-only** change (no `src/`), task-tree-owned by `.15a`; **DUT byte-identical**. **Opens `.15` `reach_path`** — the **fourteenth** derived `analyze` query and the **forward-transitive witness**: for a start node `"node:<id>"`, one representative LONGEST combinational fan-OUT path (ordered interior-`Gate` chain) to a boundary SINK (output port / `"flop:<id>"`); the **forward complement to `longest_path` `.13`** + the **path-witness for `node_reach` `.14`**; single-endpoint (no MCP signature change); `sink` in the `output_support`/`longest_path` target namespace; reuses `PathStep` + a new `ReachPath { node, depth, path, sink }`; provably `reach_path(n).sink.is_some() ⟺ node_reach(n).fanout_targets>0`, `depth == path.len()`; structural gate-depth NOT timing; schema `1.26 → 1.27` at impl. `DEVELOPMENT_NOTES` design-detail entry resolves Q1–Q5; task tree `.15`/`.15a`/`.15b`/`.15b.1`/`.15b.2` added; CHANGES/MEMORY/TASK_TREE updated. Validation: docs/design only ⇒ no `cargo` impact; `check_doctrines` green (code-scoped checks exempt; MEMORY-ARCH + KM pass). Prior: `7803713` (`.14b.2`). Push cadence: `origin/main` at `a767415`; **11 commits ahead** after this ⇒ no push (threshold 30).
- active_work_unit: **`SEMANTIC-INTROSPECTION-EXPANSION.15b.1`** (pure `reach_path` core in `analyze.rs`). `.15a` design landed; `.15` (`reach_path`) OPEN. **Thirteen** derived analyze queries delivered @ schema `1.26`; `.15` is the fourteenth (schema → `1.27` at `.15b.2`). Roadmap/codebase/mdBook aligned.
- next_action: **Implement `.15b.1` — the pure `reach_path` core** in `src/introspect/analyze.rs`: `QUERY_REACH_PATH = "reach_path"`, `ReachPath { node: u32, depth: usize, path: Vec<PathStep>, sink: Option<String> }` (reuse `PathStep`), the fourteenth `reach_path: Vec<ReachPath>` `skip_serializing_if` vec on `DerivedAnalysis` (every existing literal gains `reach_path: Vec::new()`), `module_reach_path`/`design_reach_path` + the shared `reach_path_with` driver (transpose operands → reader index [the `node_reach_with` pass]; the sink-aware `gate_reach_height(g) = if cont>0 {1+cont} else if drives_sink(g) {1} else {0}` memo; the greedy forward descent ties→smallest reader id; `pick_sink(n)` = smallest output-port name driven by `n` else `"flop:<id>"` of smallest flop whose `D==n`; register boundary automatic) + the `reach_path_analysis` constructor. **NOT** in `supported_query_kinds()` yet (joins with `run_analyze` dispatch in `.15b.2`). Lib proofs: the `sink.is_some() ⟺ node_reach.fanout_targets>0` cross-check + `sink ∈ node_reach`'s set + the register-boundary + leaf-start/direct-sink + deepest-chain (`depth==path.len()`) + deterministic tie-break + reaches-nothing-known-but-empty + None-all + unknown-none + serialization-omits-the-other-13 + determinism. Then `.15b.2` surface (registry + dispatch + schema `1.26 → 1.27` + `analyze_schema` enum + schema-doc/book/USER_GUIDE/README/TOOLBOX/KM + e2e smoke). Doctrine: SCHEMA-DERIVED/default-off/DUT byte-identical; mirror the `.Nb.1` pure core → `.Nb.2` surface cadence; run the e2e `anvil-mcp` smoke; monitor RAM (`scripts/ram_guard.sh --threshold 90 -- <cmd>`).
- handoff: **repo handoff-ready (`.15a` design landed; frontier `.15b.1`).** `scripts/check_doctrines.sh` green (4 doctrines); tree clean; DUT byte-identical. `.15a` opens `reach_path` (the fourteenth derived query); design fully pinned in `DEVELOPMENT_NOTES`. origin/main at `a767415`; **11 commits ahead** after this; threshold 30 ⇒ no push. A fresh session resumes at `.15b.1` (see next_action).
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); one runner + one classifier not two (`feedback_full_factorization`); every capability opt-in + MCP-invocable + queryable + CLI-as-shim (decision `0017`); design the API for agents not humans (`feedback_api_for_agents_not_humans`); SEMANTIC-INTROSPECTION stays SCHEMA-DERIVED / no-shadow-simulator (decisions `0004`/`0011`); the book is the user-facing surface and must not drift (`feedback_book_doctrine`); decision/KM fact per durable capability or boundary. Doctrine-enforcement (decision `0026`): every doctrine = a deterministic check in `scripts/check_doctrines.sh`; code-scoped checks are scope-aware (non-code commits exempt).
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
