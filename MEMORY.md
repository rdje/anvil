# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `BOOK-API-REFERENCE` commit (**owner directive: thoroughly document ANVIL's API in the mdBook, top-notch / industry-standard**). Pure-mdBook docs (no `src/` ⇒ DUT byte-identical; `tests/snapshots.rs` untouched). Four new Reference pages under the `agent-mcp.md` tutorial (progressive disclosure): `api-reference.md` (JSON-RPC 2.0 envelope + stdio/HTTP transports + the 8 lifecycle methods + the two-layer error model [`-32700`/`-32601`/`-32602` protocol vs tool-level `isError`] + content-addressing + the `protocolVersion 2024-11-05`/`schema_version 1.11` versioning + `SCHEMA-DERIVED` invariant), `api-tools.md` (the 9 tools — generate/introspect/dump_config/analyze/coverage_gaps [pure] + validate/minimize/hunt/divergence [controlled] — each: param table + result shape + errors + request→response example), `api-resources-prompts.md` (the `anvil://…` resources + the 5 prompts), `api-introspection.md` (the `--introspect` envelope + the 4 `analyze` query schemas + the stability contract). Schemas derived verbatim from `src/mcp/mod.rs` (`tools_list`/`resources_list`/`prompts`) + `docs/AGENT_INTROSPECTION_SCHEMA.md`. + SUMMARY entries + agent-mcp cross-link + `docs/knowledge/api-reference.md` KM card (KM 49→50). `mdbook build` clean; `cargo test --test book_examples` 3/3 (fixed two missing `book-test:skip` sentinels). `BOOK-API-REFERENCE` tree closed. Prior: `5d3c430` = `ACCEPTANCE-DIVERGENCE-HUNTING.2f` (lane 2 closed); `0836655` = `.2e`; `7142fd7` (**== origin/main**, pushed). Push cadence: `origin/main` at `7142fd7` → **5 commits ahead** after this (`.2c.2`,`.2d`,`.2e`,`.2f`,`BOOK-API-REFERENCE`); push at ~30 (`feedback_push_cadence`).
- active_work_unit: **none active** — `BOOK-API-REFERENCE` done (owner API-doc directive satisfied); `ACCEPTANCE-DIVERGENCE-HUNTING` closed (lane 2 delivered). Next via PNT = the next owner-directed design-first lane per `docs/TASK_TREE.md` order: `DOWNSTREAM-ADAPTER-EXPANSION.1` (design ADR), then `KNOB-ERGONOMICS-AND-PRESETS`, `CI-PACKAGING-DISTRIBUTION`, `COVERAGE-STEERED-GENERATION`, `CAPABILITY-BREADTH-EXPANSION`. Plus no-frontier capability trees (`STRUCTURED-EMISSION-EXPANSION`, `SEMANTIC-INTROSPECTION-EXPANSION`+`AGENT-MCP-EXPANSION`, `IDENTITY-DEEPENING`, `SIGNOFF-AUTOMATION-EXPANSION`). Index: `docs/TASK_TREE.md`.
- next_action: **PNT — `DOWNSTREAM-ADAPTER-EXPANSION.1`** (the next design-first lane: a generic, API-selectable downstream-adapter interface + new acceptance columns, binding decision `0017`) — design ADR first, no code before the tree leaf owns it. Or take an owner steer if one is given.
- handoff: repo handoff-ready — code state green (`cargo test --lib` 543/0, snapshots 6/6, `tool_matrix` 75/0, `anvil` 12/0, `divergence_e2e` portable 1/0 + ignored 2/0; introspection schema `1.11`); `mdbook build` clean + `book_examples` 3/3; `check_memory_architecture` + KM gen/check green (**KM 50 facts**). Working tree clean after this commit.
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); one classifier not two (`feedback_full_factorization`); every capability opt-in + MCP-invocable + queryable + CLI-as-shim (decision `0017`); design the API for agents not humans (`feedback_api_for_agents_not_humans`); the book is the user-facing surface and must not drift (`feedback_book_doctrine`); decision/KM fact per durable capability or boundary.
- in_flight_uncommitted: none after the `.2f` commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
