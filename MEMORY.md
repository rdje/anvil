# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `KNOB-ERGONOMICS-AND-PRESETS.1` commit — the **design ADR** (decision `0021`) for the knob-ergonomics usability lane (owner idea 4; binds the decision-`0017` API-completeness gate). **Docs-only — decision record + INDEX + tree + `docs/TASK_TREE.md` + `DEVELOPMENT_NOTES.md` + KM; no `src/` change, DUT byte-identical, snapshots not run (no generator change).** Verified audit (programmatic `Config` vs `Overrides` diff): **86 `Config` fields → 67 CLI-reachable (66 via `Overrides` + `seed`), 19 genuinely config-file-only**; MCP already takes a full `Config` (every knob steerable). Pinned: (1) promote **16/19** config-only knobs to `Option<T>` CLI flags (keep `library_prob`/`use_async_reset`/`max_nodes_per_module`); (2) declarative `--profile` registry (`arithmetic-heavy`/`deep-hierarchy`/`structured-emission-max`/`sv2023-upopts`, enumerable data not closures); (3) resolution `default→--config→--profile→explicit→--seed` (explicit beats preset; byte-stable; no `--profile` ⇒ byte-identical); (4) SCHEMA-DERIVED rich knob catalog (`Config::default()` serde + completeness-gated metadata table) + `anvil://catalog/presets` + MCP `profile` input (raw `anvil://catalog/knobs` kept; nothing retired). `.2` pre-split `.2a`(design-detail)/`.2b`(impl). Gate: mem-arch + KM gen/check green (**KM 54**). Prior: `2f17147`=`DAE.2c.2b` (closed DAE.2); `4865a3c`=`DAE.2c.2a`; `7142fd7` (**== origin/main**, pushed). Push cadence: `origin/main` at `7142fd7` → **15 commits ahead** after this; push at ~30 (`feedback_push_cadence`).
- active_work_unit: **`KNOB-ERGONOMICS-AND-PRESETS` — `.1` (design ADR) DONE; frontier = `.2a`** (impl design-detail: the `PartialConfig`/preset carrier type, the metadata-table + completeness-test contract, the resolver signature, the `Option`-override threading) → then `.2b` (impl). The tree stays `active`. (`DOWNSTREAM-ADAPTER-EXPANSION.2` remains complete with no frontier; `surelog`/UHDM = `DAE.2d+`.)
- next_action: **PNT — continue the loop.** Either advance this lane to `KNOB-ERGONOMICS-AND-PRESETS.2a` (impl design-detail; the natural next step, but `.2b` is real code ⇒ honors task-tree-owns-code doctrine) **or** pick another owner-directed design-first ADR leaf — `CI-PACKAGING-DISTRIBUTION.1`, `COVERAGE-STEERED-GENERATION.1`, `CAPABILITY-BREADTH-EXPANSION.1/.2` (all docs-only `.1` leaves). Pick-and-roll per `feedback_pick_and_roll_at_no_frontier`; refine the picked leaf's acceptance at pick.
- handoff: repo handoff-ready — `cargo check --all-targets` green (session bootstrap; untouched by this docs-only slice); `check_memory_architecture` + KM gen/check green (**KM 54 facts**); introspection schema `1.11`. Working tree clean after this commit.
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); one runner + one classifier not two (`feedback_full_factorization`); every capability opt-in + MCP-invocable + queryable + CLI-as-shim (decision `0017`); design the API for agents not humans (`feedback_api_for_agents_not_humans`); the book is the user-facing surface and must not drift (`feedback_book_doctrine`); decision/KM fact per durable capability or boundary.
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
