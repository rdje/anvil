# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `STRUCTURED-EMISSION-EXPANSION.8a` commit (design-detail leaf — resolves decision `0015`'s open questions against the real `generate_loop.rs`/`sv.rs` + proves the wider-lane surface is live in real generation; **docs-only / no `src/` / DUT byte-identical**). Prior: `3ec4f66` = `.7` (decision `0015`); `cf25642` = `LIVE-DOC-CODEBASE-ALIGNMENT.2`; `7163d72` = `.6b.3`; … `b90bdff` = `.5` (**pushed** — `origin/main` is at `b90bdff`). Working tree clean after this commit. Push cadence: `origin/main` at `b90bdff` → **8 commits ahead (`.6a`,`.6b.1`,`.6b.2a`,`.6b.2b`,`.6b.3`,`LIVE-DOC-CODEBASE-ALIGNMENT.2`,`.7`,this `.8a`); push at ~30** (`feedback_push_cadence`).
- active_work_unit: **`STRUCTURED-EMISSION-EXPANSION` tree `active`, frontier = `.8b`**. Three structured surfaces shipped end-to-end (`function automatic` `.1`+`.2`, `generate for` loop `.3`+`.4`, `task automatic` `.5`+`.6`); the FOURTH (wider-lane `generate for` part-select, decision `0015`) is design-done (`.7`) + design-detail-done (`.8a`) and ready to implement (`.8b` — the first source change of this surface). Index: `docs/TASK_TREE.md`.
- next_action: **PNT → `STRUCTURED-EMISSION-EXPANSION.8b`** (impl — FIRST source change): (1) `src/ir/generate_loop.rs::gate_qualifies` — replace `lane.width() != 1 || *width != operands.len()` with `lw = lane.width(); lw == 0 || *width != operands.len()*lw` (any `LW >= 1`, `width == N*LW`; keep the `function_emit`/`soft_union` + all-same-operand/`N>=2` exclusions); (2) `src/emit/sv.rs::generate_loop_gate` — mirror the relaxed defensive re-check; (3) `src/emit/sv.rs::render_generate_loop_block` — compute `lw = m.nodes[lane as usize].width()`, branch `lw == 1` (keep verbatim `assign <name>[<gi>] = <x>;`) vs `lw > 1` (`assign <name>[<gi>*lw +: lw] = <x>;`); (4) lib proofs (wider-lane marks + emit shape `[gi*LW +: LW]` + `LW==1` still `[gi]` byte-identity guard + identity/node-count untouched); (5) confirm `tool_matrix --generate-loop-gate` bank stays clean + projects wider-lane corpus replications; (6) replace the "wider lane stays inline" caveat in `book/src/structured-emission.md` + USER_GUIDE. Full validation: `cargo check/clippy -D warnings/fmt` + `cargo test --lib` + `cargo test --test snapshots` 6/6 byte-identical (default-off) + a forced `generate_loop_emit_prob=1.0` downstream sweep (Verilator `-Wall` + both Yosys + Icarus). **NO new knob / NO new metric / NO schema bump.** Doctrine: NO code change without a task-tree leaf owning it first (`.8b` owns it).
- handoff: repo handoff-ready — `cargo check --all-targets` clean; tree clean. `.8a` pinned the exact two surgical edits + the byte-identity contract; corpus-liveness proven (20 multi-bit-lane replications / 300-module sweep). `.8b` is the first source change of the fourth surface; after `.8b`, consider pushing (will be ~9 ahead — still under the ~30 cadence).
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); every capability is an opt-in knob; decision/KM fact per durable capability or boundary.
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
