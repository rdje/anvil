# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `SEMANTIC-INTROSPECTION-EXPANSION.14a` **node_reach design-detail (docs-only)** commit — a **docs/design-only** change (no `src/`), task-tree-owned by `.14a`; **DUT byte-identical**. Pins the **thirteenth** derived `analyze` query, `node_reach`, BEFORE any code (task-tree-ownership doctrine). `node_reach` = the **transitive combinational fan-OUT** of a node addressed `"node:<id>"` (boundary sinks: `reaches_outputs` output ports + `reaches_flops` flop `D` cones + `fanout_targets`) — the **forward-transitive corner** completing the node-addressed driver/reader × 1-hop/transitive 2×2 matrix (`node_drivers` `.9`/`node_readers` `.10`/`longest_path` `.13`/`node_reach` `.14`), the **transitive complement to `node_readers`**, the node-addressed generalization of `input_reach` `.3` that **subsumes** the per-FSM/per-memory reach candidate (nothing retired). Provable: `node_reach("node:<PrimaryInput-of-i>").{reaches_outputs,reaches_flops} == input_reach(i).{…}`. Created `.14`/`.14a`/`.14b`/`.14b.1`/`.14b.2` leaves + `.14a` Decisions entry + `DEVELOPMENT_NOTES` design-detail (Q1–Q5) + Current-Frontier block + `docs/TASK_TREE.md` row + `CHANGES.md` (backfilled `.13b.2` hash `b2fa0cf`). Validation: `check_doctrines.sh` green (docs/design ⇒ code-scoped checks exempt). Prior: `b2fa0cf` (`.13b.2`). Push cadence: `origin/main` at `a767415`; **8 commits ahead** after this ⇒ no push (threshold 30).
- active_work_unit: **`SEMANTIC-INTROSPECTION-EXPANSION.14b.1` (the `node_reach` pure core)** — the next leaf. `.14a` design-detail done. Schema `1.25 → 1.26` lands at `.14b.2`. `STRUCTURED-EMISSION-EXPANSION` also `active` at a NO-FRONTIER boundary (nine structured surfaces).
- next_action: **Implement `.14b.1`** (the pure `node_reach` core in `src/introspect/analyze.rs`): `QUERY_NODE_REACH = "node_reach"` + `NodeReach { node, kind, op: Option<String>, width, reaches_outputs: Vec<String>, reaches_flops: Vec<u32>, fanout_targets: usize }` + the thirteenth `node_reach: Vec<NodeReach>` field (`#[serde(default, skip_serializing_if)]`) + `module_node_reach`/`design_node_reach` + the shared `node_reach_with` (transpose operands → reader index [the `node_readers_with` pass], forward-closure BFS from the target node, classify sinks: output ports whose driver ∈ closure via `driver_of_port`/`m.outputs` + flops whose `D` ∈ closure via `m.flops`). Add `node_reach: Vec::new()` to the ~21 existing `DerivedAnalysis` literals (script the insert after each `longest_path: Vec::new(),` / `longest_path,` line). NOT in `supported_query_kinds()` yet (joins with dispatch in `.14b.2`). Lib proofs: the load-bearing `node_reach ↔ input_reach` consistency proof + register-boundary + sink-classification (output / flop-`D` / dead / instance-input-only) + transitive grandchild + None ⇒ one per node + unknown ⇒ none + serialization omits the other 12 vecs + determinism. Then `.14b.2` (surface). Doctrine: SCHEMA-DERIVED/default-off/DUT byte-identical; run the e2e `anvil-mcp` smoke at `.14b.2` (it catches spec-vs-reality bugs); monitor RAM (`scripts/ram_guard.sh --threshold 90 -- <cmd>`).
- handoff: **repo handoff-ready (mid-slice, `.14a` committed).** `scripts/check_doctrines.sh` green (4 doctrines); tree clean; DUT byte-identical. `SEMANTIC-INTROSPECTION-EXPANSION.14` (`node_reach`) design pinned end-to-end; `.14b.1`/`.14b.2` pending. origin/main at `a767415`; **8 commits ahead** after this; threshold 30 ⇒ no push. A fresh session should implement `.14b.1` (see next_action).
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); one runner + one classifier not two (`feedback_full_factorization`); every capability opt-in + MCP-invocable + queryable + CLI-as-shim (decision `0017`); design the API for agents not humans (`feedback_api_for_agents_not_humans`); SEMANTIC-INTROSPECTION stays SCHEMA-DERIVED / no-shadow-simulator (decisions `0004`/`0011`); the book is the user-facing surface and must not drift (`feedback_book_doctrine`); decision/KM fact per durable capability or boundary. Doctrine-enforcement (decision `0026`): every doctrine = a deterministic check in `scripts/check_doctrines.sh`; code-scoped checks are scope-aware (non-code commits exempt).
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
