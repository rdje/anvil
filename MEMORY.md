# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `SEMANTIC-INTROSPECTION-EXPANSION.2a` commit (hash backfills next update; prior: `a31bf94` = `.1`; `59e09c4` = `SV-VERSION-TARGETING.3b.2a`; `eca5d8b` = `.3b.1`). Working tree clean after this commit. **PUSH NOW (30 ahead — `feedback_push_cadence`).**
- active_work_unit: **TWO active lanes.** (A) **`SEMANTIC-INTROSPECTION-EXPANSION`** (**`active`**, owner-prioritized `2026-06-16`). `.1` (decision `0011`) + `.2a` (design-detail) **done**. Frontier: **`.2b`** (impl). (B) **`SV-VERSION-TARGETING`** (**`active`**): first up-opt SHIPS (`.3b.2a` done — live `union soft` overlay). Frontier: **`.3b.2b`** (matrix up-opt gate). `STRUCTURED-EMISSION-EXPANSION` `proposed`. `IDENTITY-DEEPENING.3b.2b` parked-but-fully-designed. Index: `docs/TASK_TREE.md`.
- next_action: continue PNT (no self-pause) → **owner-prioritized: `SEMANTIC-INTROSPECTION-EXPANSION.2b`** — implement the `.2a` design (CODE): new pure `src/introspect/analyze.rs` with `DerivedAnalysis { query, results: Vec<SupportCone> }` + `SupportCone { target, support_inputs[], support_flops[], support_instance_outputs[], cone_nodes, cone_depth }` (serde+Default, sorted Vecs ⇒ deterministic); `module_support_cones(m, target: Option<&str>)` + design variant = memoized DFS over `Module.nodes` operands + `drives` + flop D-cones (child-instance outputs are LEAVES; NO IR field / NO generator change). Default `introspect` payload UNCHANGED (lean) — cone reached only via a new **pure** MCP `analyze` tool returning a standalone `DerivedAnalysisDocument` (envelope reuse, schema **1.2→1.3** = `SCHEMA_VERSION` bump + schema-doc + ~5 `"1.2"` test-assertion bumps, the `.2b.1` procedure); `output_support` query-kind; name-`target` (absent ⇒ all outputs); unknown query/target ⇒ `-32602`; big cones inline first-cut. **DUT `.sv` byte-identical** (introspect not in snapshots; run snapshots 6/6 + introspect/mcp lib tests). Pre-split `.2b` → `.2b.1` (analyze module + types, lib-tested) + `.2b.2` (MCP tool + schema + book/USER_GUIDE/schema-doc + KM) if broad. PARALLEL SV-version frontier `.3b.2b` (matrix `--sv-version-gate` up-opt scenario + `saw_sv_version_2023_soft_union_upopt` fact, Verilator-only, Yosys/Icarus no-op; all in `src/bin/tool_matrix.rs`).
- `.2a` delivered (DOCS-ONLY, no source change): resolved `0011`'s 3 open questions + fixed the `.2b` impl shape (above) in `DEVELOPMENT_NOTES.md` + tree split. `.1` (prior): decision `0011` activated the lane. `.3b.2a` (prior): live `union soft` up-opt (`soft_union_slice_prob` + `src/ir/soft_union.rs` + `emit/sv.rs` overlay; banked Verilator 1800-2023 clean; snapshots 6/6).
- **PARKED (not abandoned): `IDENTITY-DEEPENING`** (`active`, frontier `.3b.2b`) — cross-module whole-module sequential equivalence, **fully designed** (decision `0008` + `.3b.1` detail + `.3b.2a` `bisimulation_partition` helper landed `edae1b0`). Resume from `docs/tasks/IDENTITY-DEEPENING.md` `.3b.2b`. Owner redirected priority to `SV-VERSION-TARGETING`.
- lane invariants (both lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); every capability is an opt-in knob; decision/KM fact per durable capability or boundary.
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`. The next code leaf (`SEMANTIC-INTROSPECTION-EXPANSION.2`) touches `src/introspect/`+`src/mcp/`+`docs/AGENT_INTROSPECTION_SCHEMA.md` ⇒ run snapshots 6/6 (DUT byte-identical) + introspect/mcp lib tests + schema-version assertions. `.3b.2b` (parallel) is matrix-only. Push cadence: `origin/main` at `381ec01`; **30 commits ahead after this commit — PUSH NOW** (`feedback_push_cadence`: push every 30).

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
