# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `COVERAGE-STEERED-GENERATION.2a` **steering-core** commit — the first **code** slice of the steering lane. `KnobId::category()` (exhaustive 21-variant taxonomy: state/selectors/datapath/terminals/sharing/hierarchy) in `src/ir/types.rs`; `SteeringConfig` (`per_knob`/`per_category` emphasis-weight `BTreeMap`s + `weight()`/`effective_prob()`/`is_empty()`/`validate()`) + `Config.steering` (first `skip_serializing_if` ⇒ empty omitted ⇒ `--dump-config`/`--introspect` byte-identical when unset) + `ConfigError::SteeringWeight` in `src/config.rs`; the `roll_knob` prior multiplier (`effective_prob = clamp01(prob*weight)`, one `gen_bool` draw preserved) in `src/gen/cone.rs`. **Rules-first / DUT byte-identical when unset.** Three proofs green: snapshots 6/6 (default unchanged); `steering_shifts_achieved_construct_distribution` (flop_prob fire-rate rises >0.1 when `state` up-weighted 4×); `neutral_steering_weight_is_byte_identical_to_unsteered` (explicit 1.0 weight = identical SV, 16 seeds) + 6 config unit tests. **Full cargo gate green** (check/test/clippy -D warnings/fmt). Implements decision `0023`. Prior: `02a960d`=`…1` handoff notes; `8f14705`=`…1` design ADR (decision 0023). Push cadence: `origin/main` at `7142fd7` → **27 commits ahead** after this; **push at ~30** (`feedback_push_cadence`) — close (~3 more).
- active_work_unit: **`COVERAGE-STEERED-GENERATION` — `.1` + `.2a` DONE; frontier = `.2b`** (the SCHEMA-DERIVED achieved-coverage readout: project `knob_roll_attempts`/`fires` + gate/operand/depth histograms into `--introspect` [schema MINOR bump] + an MCP coverage query, decision `0017`; byte-identical elsewhere). Then `.2c` (outer measure→derive→re-steer helper + `--steer` CLI shim + book/USER_GUIDE/KM; close `.2`). `CI-PACKAGING-DISTRIBUTION` stays `active` with no frontier.
- next_action: **PNT — continue.** Advance to `COVERAGE-STEERED-GENERATION.2b` (the achieved-coverage readout — **code**; task-tree-owned). SCHEMA-DERIVED projection only (decision `0011` precedent — zero new computed truth); bump the `--introspect` schema MINOR (currently `1.11`); add the MCP coverage query (decision `0017`); guarantee byte-identical-elsewhere. Code slice ⇒ run the full COMMIT.md gate; watch RAM per `0003-resource-safe-validation` (`scripts/ram_guard.sh --threshold 90 -- <cmd>`).
- handoff: repo fully handoff-ready & **in sync** after this commit — full `cargo test` green; snapshots 6/6; `check_memory_architecture` + KM gen/check green (**KM 58 facts**, unchanged — KM card for steering lands at `.2c`); mdbook clean; introspection schema still `1.11` (bumps at `.2b`). No pending doc-drift (book/USER_GUIDE for steering deferred to `.2c` per decision `0023` pre-split).
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); one runner + one classifier not two (`feedback_full_factorization`); every capability opt-in + MCP-invocable + queryable + CLI-as-shim (decision `0017`); design the API for agents not humans (`feedback_api_for_agents_not_humans`); the book is the user-facing surface and must not drift (`feedback_book_doctrine`); decision/KM fact per durable capability or boundary.
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
