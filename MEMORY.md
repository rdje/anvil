# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `STRUCTURED-EMISSION-EXPANSION.10a` commit (design-detail leaf — grounded decision `0016` in the real `function_emit.rs`/`analyze.rs`/`sv.rs`/`gen/mod.rs`/`config.rs`, resolved its five open questions; **docs-only, no source change, DUT byte-identical**; frontier → `.10b`). Prior: `46a377e` = `.9` (decision `0016`); `afb5718` = `.8b`; `ab73083` = `.8a`; `3ec4f66` = `.7` (decision `0015`); … `b90bdff` = `.5` (**pushed** — `origin/main` is at `b90bdff`). Working tree clean after this commit. Push cadence: `origin/main` at `b90bdff` → **11 commits ahead (`.6a`,`.6b.1`,`.6b.2a`,`.6b.2b`,`.6b.3`,`LIVE-DOC-CODEBASE-ALIGNMENT.2`,`.7`,`.8a`,`.8b`,`.9`,this `.10a`); push at ~30** (`feedback_push_cadence`).
- active_work_unit: **`STRUCTURED-EMISSION-EXPANSION` tree `active`, frontier = `.10b`**. **FOUR** structured surfaces delivered end-to-end: `function automatic` (`.1`+`.2`), `generate for` loop (`.3`+`.4`), `task automatic` (`.5`+`.6`), wider-lane `generate for` part-select (`.7`+`.8`, decision `0015`). The **FIFTH** surface (the **multi-gate-cone `function automatic`**, decision `0016`) is in progress: `.9` design + `.10a` design-detail both done; `.10b` impl pending (the actual source change). Future surfaces (multi-output tasks / nested-multi-level `generate` / `interface`-`modport`) are `.11+`. Index: `docs/TASK_TREE.md`.
- next_action: **PNT — implement frontier leaf `STRUCTURED-EMISSION-EXPANSION.10b`** (the FIRST source change of the fifth surface; `.10b` owns it): new `src/ir/cone_function_emit.rs` (`annotate_cone_function_gates` — root selection + the post-order cone-walk + single-use-within-cone absorption + sibling-mark/reservation mutual exclusion) + `Config::cone_function_emit_prob` (default `0.0`, config-file-only, separate from `function_emit_prob`) + `Module.cone_function_gates: BTreeMap<NodeId, Vec<NodeId>>` + `src/gen/mod.rs` two guarded call-site rolls (run LAST, after `task_emit`) + `src/emit/sv.rs` `render_cone_function_decl` (support-leaf params + topo-ordered function-local temps + return root) + the call + absorbed-interior inline-suppression + lib proofs + `Metrics::num_emitted_cone_functions` (schema `1.10 → 1.11`) + `tool_matrix --cone-function-gate`/`saw_cone_function_emit` + book/USER_GUIDE/KM. Default-off / DUT byte-identical (snapshots untouched; the single-gate `function_emit` surface untouched). **Pre-split `.10b.1` live / `.10b.2` metric+gate / `.10b.3` docs** (the `.6b`/`.4b` precedent) — `.10b` is a substantial source slice. Full validation: `cargo check/clippy(-D warnings)/fmt` + `cargo test --lib` + `cargo test --test snapshots` 6/6 + forced-knob downstream sweep (Verilator `-Wall` + both Yosys + Icarus). Doctrine: the design-detail (`.10a`) is locked; `.10b` is task-tree-owned.
- handoff: repo handoff-ready — `cargo check --all-targets` clean + `cargo test --lib` 493 + snapshots 6/6 per the `.8b` bank (this `.10a` leaf touched no source); tree clean; all self-checks green; KM at 43 facts / 351 keys. The fifth surface is *designed end-to-end* (`.9` + `.10a`); `.10b` is the implementation slice (a good fresh-session boundary if needed — the `.10a` DEVELOPMENT_NOTES entry fully specifies it).
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); every capability is an opt-in knob (the fifth surface gets its OWN `cone_function_emit_prob` so the single-gate surface stays byte-identical); decision/KM fact per durable capability or boundary.
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
