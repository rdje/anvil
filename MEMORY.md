# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `SEMANTIC-INTROSPECTION-EXPANSION.6a` **flop_dependencies impl design-detail** commit — a **DOCS/DESIGN-ONLY change** (no `src/`/`tests/`), task-tree-owned by `.6a`; **DUT byte-identical**. Opens `.6` ⇒ a **fifth** derived `analyze` query, `flop_dependencies` (the register→register dependency graph — per flop: `depends_on_flops` = its D-cone `support_flops`, `driven_flops` = the transpose, `self_dependent` = self-feedback). The register-level analog of `module_reachability`, reusing the existing support/reach machinery. A `DEVELOPMENT_NOTES.md` design-detail entry pins the 5 points (a FIFTH `skip_serializing_if`-omitted parallel vec ⇒ prior 4 documents byte-identical; reuse the support/reach machinery in one inversion pass; `"flop:<id>"` addressing; top-module like `flop_reset_provenance`; schema `1.17 → 1.18`) grounded in a fresh read of `src/introspect/analyze.rs`. No new numbered decision (the `.3a`/`.4a`/`.5a` per-query precedent; decision `0011` governs). `scripts/check_doctrines.sh` green (docs commit ⇒ code-scoped checks exempt). Prior: `715209f` (`.19b.3`). Push cadence: `origin/main` at `4564533` → **10 commits ahead** after this ⇒ no push (threshold 30, `feedback_push_cadence`).
- active_work_unit: **`SEMANTIC-INTROSPECTION-EXPANSION` — a fifth derived query (`flop_dependencies`) is in progress; the lane has an ACTIVE frontier (`stays active`).** Four named queries from decision `0011` delivered (`output_support`/`input_reach`/`flop_reset_provenance`/`module_reachability`, schema `1.7`). `.6a` design landed (this commit); frontier = `.6b.1` (pure core) then `.6b.2` (surface, schema `1.17 → 1.18`). The fifth query is the first beyond `0011`'s four named kinds, under the lane's "open-ended breadth" clause; nothing retired.
- next_action: **PNT — implement `SEMANTIC-INTROSPECTION-EXPANSION.6b.1` (the pure `flop_dependencies` core).** In `src/introspect/analyze.rs`: add `QUERY_FLOP_DEPENDENCIES = "flop_dependencies"`, the `FlopDependencies { flop, depends_on_flops, driven_flops, self_dependent }` struct, the fifth `flop_dependencies: Vec<FlopDependencies>` field on `DerivedAnalysis` (`#[serde(default, skip_serializing_if = "Vec::is_empty")]`) + `flop_dependencies: Vec::new()` on the existing literals, and `module_flop_dependencies`/`design_flop_dependencies` (reuse the support/reach machinery: predecessors = D-cone `support_flops`, successors = the transpose, `self_dependent` = `flop ∈ depends_on_flops`). Do NOT add to `supported_query_kinds()` yet (joins with `run_analyze` dispatch in `.6b.2`). Lib proofs (pipeline predecessors/successors + counter `self_dependent` + dual-consistency transpose + `"flop:<id>"` target + `None` ⇒ all flops ascending + flopless ⇒ empty + unknown ⇒ none + determinism + serialization omits the other 4 vecs); `cargo test --lib` + snapshots 6/6 + clippy `-D warnings` + fmt; amend `CODEBASE_ANALYSIS.md` `analyze.rs` block. **`.6a` already owns it** (task-tree doctrine satisfied). Monitor RAM (`scripts/ram_guard.sh --threshold 90 -- <cmd>`). (Tracked-lower: the `agent-mcp.md` `analyze`-example schema-`1.11` drift.)
- handoff: **repo handoff-ready after this commit** — `scripts/check_doctrines.sh` green (4 doctrines); `git diff --check` clean; DOCS/DESIGN-ONLY ⇒ DUT byte-identical; `.6a` design-detail establishes task-tree ownership for the fifth-query impl. origin/main at `4564533`; 10 commits ahead after this. Co-staged: `DEVELOPMENT_NOTES.md` + `docs/tasks/SEMANTIC-INTROSPECTION-EXPANSION.md` + `docs/TASK_TREE.md` + CHANGES + MEMORY.
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); one runner + one classifier not two (`feedback_full_factorization`); every capability opt-in + MCP-invocable + queryable + CLI-as-shim (decision `0017`); design the API for agents not humans (`feedback_api_for_agents_not_humans`); SEMANTIC-INTROSPECTION stays SCHEMA-DERIVED / no-shadow-simulator (decisions `0004`/`0011`); the book is the user-facing surface and must not drift (`feedback_book_doctrine`); decision/KM fact per durable capability or boundary. Doctrine-enforcement (decision `0026`): every doctrine = a deterministic check in `scripts/check_doctrines.sh`; code-scoped checks are scope-aware (non-code commits exempt).
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
