# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `SEMANTIC-INTROSPECTION-EXPANSION.12b.1` **pure instance_input_bindings core** commit — a **code + docs** change, task-tree-owned by `.12b.1`; **DUT byte-identical** (`analyze` read-mostly post-hoc, not wired to any emit path; `tests/snapshots.rs` untouched). The pure core of the **eleventh** derived `analyze` query, `instance_input_bindings` (the **parent-side dual of `instance_provenance` `.11`**) — **not** yet in the MCP registry (joins with `run_analyze` dispatch + schema bump in `.12b.2`). `src/introspect/analyze.rs`: `QUERY_INSTANCE_INPUT_BINDINGS` + `InstanceInputBindings { instance, module, role, input_bindings }` + `InstanceInputBinding { port, driver: NodeRef }` (reusing `NodeRef`) + the eleventh `DerivedAnalysis.instance_input_bindings` `skip_serializing_if` vec + `module_`/`design_instance_input_bindings` + the `instance_input_bindings_analysis` constructor; driver = `node_ref_of(top, parent_node_id, top_fmt)` (parent-side; sibling instance output → `"<sibling>.<port>"`); child def names the port (fallback `"port<id>"`); the module variant **NON-degenerate** (the contrast with `.11`). 19 literals gained `instance_input_bindings: Vec::new()`. 8 in-crate proofs (cross-boundary binding + non-degenerate module variant + ordering + known-but-empty + target/None/unknown + serialization-omits-ten + absent-top + determinism). CODEBASE_ANALYSIS analyze.rs block → eleven query kinds. Validation: `cargo test --lib introspect::analyze` 76/0; `cargo test --lib` 698/0/2; snapshots 6/6 byte-identical; clippy `-D warnings` + fmt clean. Prior: `1933d2a` (`.12a`). Push cadence: `origin/main` at `a767415`; **3 commits ahead** after this (`fc790e1`+`1933d2a`+this) ⇒ no push (threshold 30).
- active_work_unit: **`SEMANTIC-INTROSPECTION-EXPANSION.12` (`instance_input_bindings`)** → frontier leaf **`.12b.2`** (the MCP/introspect surface). `.12a` (design) + `.12b.1` (pure core) done. The lane is `active`; ten prior derived queries delivered @ schema `1.23`.
- next_action: **Implement `.12b.2` — wire `instance_input_bindings` to the surface.** Add `QUERY_INSTANCE_INPUT_BINDINGS` to `analyze::supported_query_kinds()` AND branch `run_analyze` by kind (`module`/`design_instance_input_bindings`) in `src/mcp/mod.rs` in the **same commit**; update the empty-result → `-32602` guard to check `analysis.instance_input_bindings`; bump `SCHEMA_VERSION 1.23 → 1.24` in `src/introspect/mod.rs` (+ the `"1.23"` test-assertion updates, ~3 introspect + ~13 mcp); add `"instance_input_bindings"` to the `analyze_schema` query enum + refresh the tool/instructions descriptions; 2 new mcp proofs (returns+caches on a hierarchy config; unknown target ⇒ `-32602`). Docs: schema-doc §6.7 + `1.23 → 1.24` changelog + the row; book `agent-mcp` (analyze row + worked example + JSON envelopes `1.23 → 1.24`) + `api-tools`/`api-introspection`/`api-reference`/`api-resources-prompts`; USER_GUIDE + README; a KM card (`semantic-introspection-instance-input-bindings`, cross-link from `semantic-introspection-instance-provenance`; regenerate `KNOWLEDGE_MAP` 76 → 77). e2e `anvil-mcp` stdio smoke (a hierarchy seed → schema `1.24`, an instance with real parent-driver bindings; unknown instance ⇒ `-32602`). Monitor RAM (`scripts/ram_guard.sh --threshold 90 -- <cmd>`).
- handoff: **repo handoff-ready (mid-lane, `.12a`+`.12b.1` done, frontier `.12b.2`).** `scripts/check_doctrines.sh` green; tree clean; DUT byte-identical. `.12b.1` landed the pure core (lib-proven, not in the registry); next implements `.12b.2` (surface). origin/main at `a767415`; **3 commits ahead** after this; threshold 30 ⇒ no push.
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); one runner + one classifier not two (`feedback_full_factorization`); every capability opt-in + MCP-invocable + queryable + CLI-as-shim (decision `0017`); design the API for agents not humans (`feedback_api_for_agents_not_humans`); SEMANTIC-INTROSPECTION stays SCHEMA-DERIVED / no-shadow-simulator (decisions `0004`/`0011`); the book is the user-facing surface and must not drift (`feedback_book_doctrine`); decision/KM fact per durable capability or boundary. Doctrine-enforcement (decision `0026`): every doctrine = a deterministic check in `scripts/check_doctrines.sh`; code-scoped checks are scope-aware (non-code commits exempt).
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
