# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this **`LANE-OUT-FILENAME.1`** (code) commit — filenames only ⇒ **no SV/manifest byte change in any lane**, `tests/snapshots.rs` **6/6 byte-identical**, DUT untouched. `LaneArtifact` gains builder-truth `pub top` (DUT `design.top`, microdesign new `top_name(seed)` single-sourcing `emit_sv`/`emit_manifest`, frontend `unit.top.name`); `run_non_dut_lane` stems `--out` from it; text-scraping `parse_top_name` **deleted**. REJECT→PASS: frontend `--out` wrote `child_0.sv` (first `module` decl — frontend emits child stubs before the top) vs documented `acc_0.sv`; now correct. Gate: check/clippy(-D warnings)/fmt clean; `cargo test` under `ram_guard --threshold 90` **exit 0** (pipeline 125/0, snapshots 6/6); `--lib umbrella` 9/9 incl. new `lane_artifact_top_is_builder_truth`. Prior: `5223452` (`BOOK-LANE-COVERAGE.1`), `98e31e1` (`EVIDENCE-BANK-DURABILITY.2` / decision `0030`). Push cadence: 3 ahead of `origin/main`; 30-commit threshold.
- active_work_unit: **`BOOK-LANE-COVERAGE`** (`active`; `.1` `done`, blocker cleared) → frontier **`.2`** (microdesign-lane chapter + a new "Artifact Lanes" `SUMMARY.md` part between Motif Catalogue and Reference), then **`.3`** (frontend-lane chapter + `introduction.md`/`faq.md` cross-links). **Both chapters are already drafted** from live seed-7 / seed-0 runs — drafts at `<scratchpad>/microdesign-lane.md` + `frontend-lane.md`; if the scratchpad is gone, regenerate by running the two lanes and re-drafting (structure: why the lane exists vs DUT acceptance-only → generating one artifact (real pasted SV) → the expected-facts manifest → `--out` → reproducibility → how it's kept honest (parity tests) → MCP route). NOTE: every book ```bash block is EXECUTED by `tests/book_examples.rs` — use `cargo run --release --`, no external tools without the skip sentinel. After: `EVIDENCE-BANK-DURABILITY.3` (mechanize 0030), `.4` (breadcrumb sweep; `/tmp/anvil-validate-3f1c…` is illustrative — do not label), `.5` deferred; also `CAPABILITY-BREADTH-EXPANSION.1` (`pending` ADR). `LANE-OUT-FILENAME` is `done` (no frontier).
- next_action: **`BOOK-LANE-COVERAGE.2`** — add `book/src/microdesign-lane.md` + the "Artifact Lanes" `SUMMARY.md` part; verify with `mdbook build book` + `cargo test --test book_examples`; land via `COMMIT.md`. Then `.3`, then `EVIDENCE-BANK-DURABILITY.3`.
- handoff: **repo handoff-ready.** `check_doctrines.sh` green (4/4); tree clean; full suite exit 0. A fresh session resumes at `BOOK-LANE-COVERAGE.2` (see next_action).
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); one runner + one classifier not two (`feedback_full_factorization`); every capability opt-in + MCP-invocable + queryable + CLI-as-shim (decision `0017`); design the API for agents not humans (`feedback_api_for_agents_not_humans`); SEMANTIC-INTROSPECTION stays SCHEMA-DERIVED / no-shadow-simulator (decisions `0004`/`0011`); the book is the user-facing surface and must not drift (`feedback_book_doctrine`); decision/KM fact per durable capability or boundary. Doctrine-enforcement (decision `0026`): every doctrine = a deterministic check in `scripts/check_doctrines.sh`; code-scoped checks are scope-aware (non-code commits exempt).
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
