# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `SEMANTIC-INTROSPECTION-EXPANSION.8a` **fsm_provenance impl design-detail** commit — a **docs/design-only change** (DEVELOPMENT_NOTES + task tree + live-doc index; **no `src/` touched**) ⇒ **DUT byte-identical**. Opens `.8` — the **seventh** derived `analyze` query, `fsm_provenance` (per-generated-encoding-FSM provenance: each `Fsm`'s structural shape — `num_states`/`encoding`/`state_width`/`sel_width`/`out_width`/`is_mealy` — + the support cone of its one generated input cone `sel`, reusing `build_cone`; the **direct sibling of `memory_provenance`** and the **second query to open a documented opaque-leaf boundary**, the `Node::FsmOut` analog of `.7`'s `MemRead`; the transition/output table *values* deliberately out of scope per the `0004`/`0011` no-behavioural-oracle non-goal). DEVELOPMENT_NOTES design-detail entry resolves Q1–Q5 grounded in the real `Fsm`/`FsmEncoding` IR; pre-split `.8b` → `.8b.1` (pure core) + `.8b.2` (surface, schema `1.19 → 1.20`). No new numbered decision (decision `0011` governs; `.3a`–`.7a` precedent). Validation: `scripts/check_doctrines.sh` green (docs/design ⇒ code-scoped checks exempt); `git diff --check` clean. Prior: `fd0852a` (`.7b.2`). Push cadence: `origin/main` at `4564533` → **16 commits ahead** after this ⇒ no push (threshold 30, `feedback_push_cadence`).
- active_work_unit: **`SEMANTIC-INTROSPECTION-EXPANSION` — SIX derived queries delivered end-to-end (schema `1.19`); the SEVENTH (`fsm_provenance` `.8`) is in progress, frontier `.8b.1`.** Delivered: `output_support`/`input_reach`/`flop_reset_provenance`/`module_reachability` (decision `0011`'s four) + `flop_dependencies` (`.6`) + `memory_provenance` (`.7`). `.8a` design landed; next is `.8b.1` (pure core) then `.8b.2` (surface). Nothing retired; further kinds (a per-FSM reach; per-child-module provenance) remain open-ended breadth.
- next_action: **PNT — implement `SEMANTIC-INTROSPECTION-EXPANSION.8b.1`** (the pure `fsm_provenance` core in `src/introspect/analyze.rs`): `QUERY_FSM_PROVENANCE = "fsm_provenance"` + `FsmProvenance { fsm, num_states, encoding, state_width, sel_width, out_width, is_mealy, sel_support: SupportCone }` + the seventh `fsm_provenance: Vec<FsmProvenance>` field (`#[serde(default, skip_serializing_if = "Vec::is_empty")]`) + `module_fsm_provenance`/`design_fsm_provenance` + a `fsm_provenance_with` driver (`build_cone` over `Fsm::sel`; `FsmEncoding` → stable string + `state_width`; ascending fsm id; `"fsm:<id>"` target). Fill `fsm_provenance: Vec::new()` into the existing `DerivedAnalysis` literals (+ the `crate::ir::{Fsm, FsmEncoding}` import). **Do NOT** add to `supported_query_kinds()` yet (joins with the `run_analyze` dispatch in `.8b.2`). Lib-tested only; DUT byte-identical. Then `.8b.2` (surface: registry + dispatch + schema `1.19 → 1.20` + analyze_schema enum (`"fsm:<id>"`) + schema-doc/book/USER_GUIDE/README/KM + e2e smoke). Doctrine: task-tree already owns both leaves. Monitor RAM (`scripts/ram_guard.sh --threshold 90 -- <cmd>`).
- handoff: **repo handoff-ready after this commit** — `scripts/check_doctrines.sh` green (4 doctrines); `git diff --check` clean; DUT byte-identical; `.8a` design leaf delivered (tree-owned `.8b.1`/`.8b.2` queued). origin/main at `4564533`; 16 commits ahead after this. Co-staged: `DEVELOPMENT_NOTES.md` + `docs/tasks/SEMANTIC-INTROSPECTION-EXPANSION.md` + `docs/TASK_TREE.md` + CHANGES + MEMORY.
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); one runner + one classifier not two (`feedback_full_factorization`); every capability opt-in + MCP-invocable + queryable + CLI-as-shim (decision `0017`); design the API for agents not humans (`feedback_api_for_agents_not_humans`); SEMANTIC-INTROSPECTION stays SCHEMA-DERIVED / no-shadow-simulator (decisions `0004`/`0011`); the book is the user-facing surface and must not drift (`feedback_book_doctrine`); decision/KM fact per durable capability or boundary. Doctrine-enforcement (decision `0026`): every doctrine = a deterministic check in `scripts/check_doctrines.sh`; code-scoped checks are scope-aware (non-code commits exempt).
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
