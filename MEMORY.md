# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `STRUCTURED-EMISSION-EXPANSION.15b.3` **seventh-surface user docs (book/knobs/USER_GUIDE/README/KM)** commit — **DOCS-ONLY** (no `src/` touched) ⇒ **DUT byte-identical**. **Delivers the seventh structured surface end-to-end — `.15b` / `.15` close; SEVEN structured surfaces total.** `book/src/structured-emission.md` "## The seventh surface: a procedural `if`/`else`" section (byte-verified seed-1 before/after: two muxes → two `<wire>__cv` `always_comb` `if`/`else` blocks + passthroughs) + `mux_if_emit_prob` entry in `book/src/knobs.md` + `USER_GUIDE.md` + README + the `--mux-if-gate` entry in USER_GUIDE + README + the deferred book example-JSON `schema_version` `1.14→1.15` refresh (`api-tools.md` ×3, `agent-mcp.md` ×1; `1.11` analyze/coverage examples = pre-existing drift, out of scope) + `docs/knowledge/mux-if-emit.md` KM card + regenerated `KNOWLEDGE_MAP.md` + the `ROADMAP.md` lane status. `mdbook build` rc=0; `check_knowledge_map` (map in sync, card indexed) + `check_memory_architecture` green; `cargo test --test book_examples` **3/3** (new "Reproducing it" bash block `book-test: skip`). Docs-only ⇒ `cargo test --lib`/snapshots unaffected. Prior: `2fc1a16`=`…15b.2`. Push cadence: `origin/main` at `605ec44` → **26 commits ahead** after this ⇒ no push (threshold 30, `feedback_push_cadence`).
- active_work_unit: **`STRUCTURED-EMISSION-EXPANSION` — no current frontier** (the lane stays `active` at a no-frontier boundary; the SEVENTH structured surface — the procedural `always_comb` `if`/`else` projection of a `Mux`, decision `0027` — is delivered end-to-end: `.14` design + `.15a` design-detail + `.15b.1` live + `.15b.2` metric@1.15+gate + `.15b.3` user docs all `done`; `.15b` / `.15` close). SEVEN structured surfaces delivered (function `.1`/`.2`, generate-loop `.3`/`.4`, task `.5`/`.6`, wider-lane part-select `.7`/`.8`, cone-function `.9`/`.10`, multi-output-task `.11`/`.12`/`.13`, procedural if/else `.14`/`.15`). All other `active` lanes also at no-frontier boundaries.
- next_action: **PNT at the no-frontier boundary** (`feedback_pick_and_roll_at_no_frontier`): self-select the next structured-emission surface — `.16` design+decision for one of the candidate `.16+` surfaces (N-way `CaseMux` → `if`/`else if` priority chain [the recorded `0027` follow-up, the strongest next candidate — a genuinely-distinct priority-chain construct], nested/multi-level `generate` [no routine by-construction source], `interface`/`modport` [empirically disqualified since `.7`]) — OR pick another `active` lane. The chosen surface needs a fresh empirical tool-acceptance + sim-equiv probe (Verilator `-Wall` 2012/2017/2023 + both Yosys + Icarus + iverilog sim-equiv) BEFORE authoring the decision, then a `.16` design leaf (no source). Monitor RAM on heavy builds (`scripts/ram_guard.sh --threshold 90 -- <cmd>`).
- handoff: repo handoff-ready after this commit — `scripts/check_doctrines.sh` green (4 doctrines); `git diff --check` clean; docs-only ⇒ DUT byte-identical; `mdbook build` + `check_knowledge_map` + `check_memory_architecture` + `book_examples` 3/3 green; the prior `.15b.2` code state holds (`cargo test --lib` 616 / `--bin tool_matrix` 89 / snapshots 6/6 / `--mux-if-gate` banked `/tmp/anvil-mux-if-gate-r1`). Co-staged: `book/src/structured-emission.md` + `book/src/knobs.md` + `book/src/api-tools.md` + `book/src/agent-mcp.md` + USER_GUIDE + README + ROADMAP + `docs/knowledge/mux-if-emit.md` (new) + `KNOWLEDGE_MAP.md` (regenerated) + tree file + `docs/TASK_TREE.md` + `CHANGES.md` + `MEMORY.md`.
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); one runner + one classifier not two (`feedback_full_factorization`); every capability opt-in + MCP-invocable + queryable + CLI-as-shim (decision `0017`); design the API for agents not humans (`feedback_api_for_agents_not_humans`); the book is the user-facing surface and must not drift (`feedback_book_doctrine`); decision/KM fact per durable capability or boundary. Doctrine-enforcement (decision `0026`): every doctrine = a deterministic check in `scripts/check_doctrines.sh`; code-scoped checks are scope-aware (non-code commits exempt).
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
