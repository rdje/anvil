# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `AGENT-MCP-EXPANSION.4a` commit (hash backfills next update; prior: `bc70aee` = owner `.4` HTTP decision handoff; `54ccb25` = `.3b`; `bfab27e` = `.3a`; `edd716d` = `.2`; `6af0690` = `.1`). Working tree clean after this commit.
- active_work_unit: **`AGENT-MCP-EXPANSION`** (Lane 2, `active`). `.1`/`.2`/`.3`(`.3a`+`.3b`) **done**; `.4` split into **`.4a` (design, done — HTTP framing pinned)** + **`.4b` (impl, `pending`)**. Frontier now **`.4b`**, then **`.5`** (closeout: book/USER_GUIDE/README sync). Lane order **`2 → 3 → 1`**: `AGENT-MCP-EXPANSION` → `SIGNOFF-AUTOMATION-EXPANSION` (`proposed`) → `IDENTITY-DEEPENING` (`proposed`). Index: `docs/TASK_TREE.md`.
- next_action: do **`AGENT-MCP-EXPANSION.4b`** (code) — implement the hand-rolled HTTP transport per the `.4a` design. Add `src/mcp/http.rs` (`read_http_request` over `BufRead` / `write_http_response` over `Write` / `handle_http_connection` / `serve_http(SocketAddr)`) re-exported from `src/mcp/mod.rs`; hand-parse one optional `--http <addr>` in `src/bin/anvil_mcp.rs` (bare port ⇒ `127.0.0.1:port` loopback default; full `IP:port` honored + non-loopback stderr warning) and dispatch to `serve_http`, else keep the stdio loop. Same `McpServer::handle_line` dispatcher; `POST`-only; `Content-Length` framing; status map `200`/`204`/`400`/`405`/`411`/`413`; one request per connection (`Connection: close`); single-threaded one-`McpServer` accept loop with a read timeout; `MAX_BODY_BYTES = 16 MiB`. Tests: in-memory framing units + real-socket round-trip on `127.0.0.1:0`. NO new Cargo dependency. Validate fmt/check/clippy + `cargo test --lib mcp::` + `cargo test --test snapshots` (byte-identical). Then `.5` closeout syncs `book/src/agent-mcp.md` + `USER_GUIDE.md` + `README.md` for the whole expanded surface (coverage_gaps + non-DUT lanes + HTTP). Full design: `docs/tasks/AGENT-MCP-EXPANSION.md` `.4a` + DEVELOPMENT_NOTES.md "Hand-rolled HTTP transport design".
- lane invariants (0004/0005): MCP is a thin read-mostly adapter beside the deterministic core; schema derived (serde projection), invariant SCHEMA-DERIVED; `validate`/`minimize` run external tools **only via the hardened `downstream` invocations** (fixed allow-list, fixed binary names, sandboxed temp dir, no arbitrary shell/path), no second source of truth; AI agent never a signoff oracle; `minimize` searches the **input** `(seed, knobs)` space (never mutates/repairs RTL); default `--artifact dut` byte-identical; reuse `tool_matrix`/`downstream`/`diff_sim`/`metrics`/`mem_guard`. HTTP transport drives the SAME `McpServer::handle_line`; loopback-only default; per-call guardrails unchanged. Coverage-gap source = bin-private `CoverageSummary`/`compute_coverage_gaps` (`src/bin/tool_matrix.rs:286,6552`); recorded in `MatrixReport.coverage_gaps` (`:489`).
- in_flight_uncommitted: none after this commit. Repo is handoff-ready: tree clean, gates green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 88` for heavy builds; full `cargo test` not required per owner resource policy (monitor RAM; stop above 90%, >95% reboots). For code leaves run fmt/check/clippy + focused `mcp::`/`introspect::` tests + snapshot byte-identical guard. Push cadence: `origin/main` is at `381ec01` (`.5.3`); **9 commits ahead after this commit (`.6`,`.7`,`CAPABILITY-LANE-OWNERSHIP.1`,`AGENT-MCP-EXPANSION.1`,`.2`,`.3a`,`.3b`,`.4`-handoff,`.4a`) — under the ~30 threshold, no push yet** (`feedback_push_cadence`).

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
