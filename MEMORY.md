# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `KNOB-ERGONOMICS-AND-PRESETS.2b.2a` commit — the **MCP preset API** (decision `0021`/`0017`). **Code — `src/config.rs` + `src/mcp/mod.rs` + book API docs; DUT byte-identical (snapshots 6/6, no generator change).** `Overrides` gains `Serialize`; new `presets_catalog()` (SCHEMA-DERIVED projection of `presets()` with unset overrides filtered); new `anvil://catalog/presets` MCP resource (listed+read); the MCP `profile` input added to `knob_schema`/`generate_schema` and `config_from_args` routes through the **shared** `crate::config::resolve_config(base, profile, &Overrides::default(), seed)` (config=full base → preset → seed; unknown ⇒ tool error). Book: `agent-mcp.md` + `api-resources-prompts.md` + `api-tools.md` (the resource + the `profile` arg). Gate: lib **558→563** (+1 config + 4 MCP tests), snapshots 6/6, clippy/fmt clean, mdbook clean, full `cargo test` green. Prior: `c44b3a5`=`.2b.3` (docs); `c59bf39`=`.2b.1` (16 CLI flags + `--profile` + `resolve_config`). Push cadence: `origin/main` at `7142fd7` → **19 commits ahead** after this; **push at ~30** (`feedback_push_cadence`).
- active_work_unit: **`KNOB-ERGONOMICS-AND-PRESETS` — `.1`+`.2a`+`.2b.1`+`.2b.3`+`.2b.2a` DONE; frontier = `.2b.2b`** (the SCHEMA-DERIVED per-knob `knob_catalog()`: `KnobInfo {name,group,ty,default,validation,cli_flag,config_only}` — names+defaults from `serde_json::to_value(Config::default())`, `cli_flag`/`config_only` from the `Overrides` serde key set ∪ {seed}, `group` via a curated prefix-classifier, `validation` best-effort; a completeness test that no field is unclassified; the new `anvil://catalog/knob-schema` MCP resource [keep raw `anvil://catalog/knobs`]; fold `agent-mcp.md`/`api-*` docs inline). Tree stays `active`; book fully in sync.
- next_action: **PNT — pick `KNOB-ERGONOMICS-AND-PRESETS.2b.2b`** (code: the per-knob catalog + `anvil://catalog/knob-schema`). SCHEMA-DERIVED only (project Config + Overrides serde + a small metadata table; no recomputed truth — decision `0017` ceiling). Mirror `downstream::adapter_catalog()`. Full COMMIT.md workflow incl. snapshots 6/6 + the completeness test; RAM-guard heavy builds (decision `0003`). After `.2b.2b`, the lane has no frontier (open-ended).
- handoff: repo fully handoff-ready & **in sync** after this commit — full `cargo test` green, snapshots 6/6, clippy/fmt clean, mdbook clean; `check_memory_architecture` + KM gen/check green (**KM 55 facts**); introspection schema `1.11`. No pending doc-drift.
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); one runner + one classifier not two (`feedback_full_factorization`); every capability opt-in + MCP-invocable + queryable + CLI-as-shim (decision `0017`); design the API for agents not humans (`feedback_api_for_agents_not_humans`); the book is the user-facing surface and must not drift (`feedback_book_doctrine`); decision/KM fact per durable capability or boundary.
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
