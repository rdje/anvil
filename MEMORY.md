# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `ACCEPTANCE-DIVERGENCE-HUNTING.1` commit (**docs-only** — `docs/decisions/0019-…` + INDEX + the tree + `docs/TASK_TREE.md` + DEVELOPMENT_NOTES + ROADMAP + CHANGES + MEMORY + regenerated `KNOWLEDGE_MAP.md`) — the **design ADR (decision `0019`) opening usability lane 2 (`ACCEPTANCE-DIVERGENCE-HUNTING`).** Pins acceptance divergence as a **first-class, default-off, SCHEMA-DERIVED detector** with **one shared home** — `src/divergence/`'s `divergence::run` composing the existing `downstream::run_verilator`/`run_yosys`/`run_iverilog_compile` + a `downstream::tool_verdict` accept/warn/reject classifier **extracted** from `hunt::run` (no second classifier) — reused by **three surfaces** (a `hunt::run` axis, a `tool_matrix` column, a controlled MCP `divergence` tool + CLI shim; decision `0017`). Reproducers reuse `write_bundle`/matrix retention; version-vs-version deferred to `.2e`; `saw_acceptance_divergence` opportunistic (all-agree = valid-by-construction steady state). Pre-split `.2` → `.2a`..`.2f`. DUT byte-identical (no `src/`). Prior: `d2ddbbf` = `BUG-HUNT-ORCHESTRATION.2e` (tree closed); `6172041` = `.2d` (CLI); `b2e0228` = `.2c` (MCP tool); … `b90bdff` (**pushed** — `origin/main` at `b90bdff`). Working tree clean after this commit. Push cadence: `origin/main` at `b90bdff` → **25 commits ahead** after this; push at ~30 (`feedback_push_cadence`).
- active_work_unit: **`ACCEPTANCE-DIVERGENCE-HUNTING`** — `.1` design ADR **done** (decision `0019`); frontier **`.2a`** (extract the shared `downstream::tool_verdict` accept/warn/reject classifier from `hunt::run`, byte-identical hunt). Other owner-directed lanes still design-first `.1`: `DOWNSTREAM-ADAPTER-EXPANSION`, `KNOB-ERGONOMICS-AND-PRESETS`, `CI-PACKAGING-DISTRIBUTION`, `COVERAGE-STEERED-GENERATION`, `CAPABILITY-BREADTH-EXPANSION`. Plus no-frontier `STRUCTURED-EMISSION-EXPANSION` (5 surfaces; 6th = future `.11`), `SEMANTIC-INTROSPECTION-EXPANSION` + `AGENT-MCP-EXPANSION`, `IDENTITY-DEEPENING`, `SIGNOFF-AUTOMATION-EXPANSION`, `LOCAL-REFERENCE-CACHE`. Index: `docs/TASK_TREE.md`.
- next_action: **PNT — implement `ACCEPTANCE-DIVERGENCE-HUNTING.2a`** (first code leaf of lane 2): extract the inline accept/warn/reject classifier from `hunt::run` into a shared `downstream::tool_verdict(&ToolInvocation) -> ToolVerdict` (+ a `ToolVerdict{Accept,Warn,Reject}` serde enum); `hunt::run`'s `HuntFailure.detection` derives from it (reject/warning unchanged on the wire); cargo-portable proofs; `hunt::` tests unchanged; snapshots 6/6 byte-identical. This IS a code change — `.2a` (already task-tree-owned) owns it.
- handoff: repo handoff-ready — `.1` docs-only ⇒ code state = green `BUG-HUNT-ORCHESTRATION.2e` baseline (introspection schema `1.11`). `check_memory_architecture` + KM gen/check green (**KM 48 facts**). Working tree clean after commit.
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); one classifier not two (`feedback_full_factorization`); every capability opt-in + MCP-invocable + queryable + CLI-as-shim (decision `0017`); the divergence detector adds **no** generator path / no oracle (it composes the existing surfaces); decision/KM fact per durable capability or boundary.
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
