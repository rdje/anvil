# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `STRUCTURED-EMISSION-EXPANSION.17b.2a` **case-mux-if metric + introspection schema 1.16** commit — **CODE change** (`src/` touched), default-off ⇒ **DUT byte-identical** (the metric is a post-hoc RTL-invisible count; the schema bump is introspection-only). `Metrics::num_emitted_case_mux_if_chains` (`= m.case_mux_if_gates.len()`, `#[serde(default)]`) + a lib proof ⇒ introspection `SCHEMA_VERSION` `1.15 → 1.16` across all current-output refs (`introspect/mod.rs` const + doc-comment + 3 assertions, 8 `mcp/mod.rs` assertions, `docs/AGENT_INTROSPECTION_SCHEMA.md` current-version ×3 + a new `1.15→1.16` changelog entry, README ×2 + USER_GUIDE ×1 + CODEBASE envelope; historical `num_emitted_mux_if_blocks @ 1.15` landing attributions intact; book example JSONs deferred to `.17b.3`). Metric **exact** (static-selector `CaseMux` excluded ⇒ one chain per counted gate). `cargo check/clippy(-D warnings)/fmt` clean; `cargo test --lib` 625→**626** (1 new proof; all schema assertions at `1.16`); snapshots **6/6 byte-identical**. Live `--introspect`: schema `1.16` + metric `0` default (seed 42) / `53` forced (seed 2). `.17b.2` pre-split → `.17b.2a` (this) + `.17b.2b` (gate). Prior: this `STRUCTURED-EMISSION-EXPANSION.17b.1` commit. Push cadence: `origin/main` at `4564533` → **1 commit ahead** after this ⇒ no push (threshold 30, `feedback_push_cadence`).
- active_work_unit: **`STRUCTURED-EMISSION-EXPANSION` — frontier `.17b.2b`** (the repo-owned `tool_matrix --case-mux-if-gate`). `.16` (decision `0028`) + `.17a` (design-detail) + `.17b.1` (live source) + `.17b.2a` (metric+schema `1.16`) done; `.17b.2b` (gate) / `.17b.3` (docs) pending. SEVEN surfaces delivered (`.1`–`.15`); the EIGHTH is live + metered. All other `active` lanes at no-frontier boundaries.
- next_action: **PNT into `STRUCTURED-EMISSION-EXPANSION.17b.2b`** (the repo-owned `tool_matrix --case-mux-if-gate`, templated on `--mux-if-gate`). In `src/bin/tool_matrix.rs` (mirror the ~30 `MuxIfSweep` touch points): `CASE_MUX_IF_SWEEP_MIN_UNITS_PER_SCENARIO` const + the `--case-mux-if-gate` CLI flag + `ScenarioSet::CaseMuxIfSweep` + `build_case_mux_if_sweep_scenarios` + `case_mux_if_focus_config` (comb-only node-id+egraph, `case_mux_if_emit_prob=1.0`, **`case_mux_prob` biased high** — a `CaseMux` selector is a generated cone [dynamic by default], so no encoding-path trap like `--mux-if-gate` had) + `ModuleReport.emitted_case_mux_if` set from **`module_metrics.num_emitted_case_mux_if_chains > 0`** (METRIC-KEYED — the chain has no new text token, unlike `__cv`/`__mt(`; verify how the per-module `Metrics` flow into `ModuleReport` near `sv_text.contains("__cv")` at ~`tool_matrix.rs:5525`) + `CoverageSummary.saw_case_mux_if_emit` + merge + `MatrixReport.case_mux_if_gate` + the run-plan units/fail-on-gap + mutual-exclusion count/msg + select/build dispatch + summarize block + early-return gap arm + slug arm + `test_cli` default + ~5 proofs + the `ModuleReport`/`Cli` fixture extensions. Bank clean (`cargo run --bin tool_matrix -- --case-mux-if-gate --yosys-mode both --iverilog-compile --out /tmp/anvil-case-mux-if-gate-r1`): `coverage_gaps=[]`, `saw_case_mux_if_emit=true`, N/0 Verilator + both Yosys + Icarus. Then `.17b.3` (user docs). Monitor RAM on heavy builds (`scripts/ram_guard.sh --threshold 90 -- <cmd>`).
- handoff: repo handoff-ready after this commit — `scripts/check_doctrines.sh` green (4 doctrines); `git diff --check` clean; default-off ⇒ DUT byte-identical; the eighth surface is live + metered + fully validated (`cargo test --lib` 626 / `--bin tool_matrix` 89 / snapshots 6/6 / live `--introspect` schema `1.16` + metric 0/53). origin/main at `4564533` (the `.17b.1` push); 1 commit ahead after this. Co-staged: `src/metrics.rs` + `src/introspect/mod.rs` + `src/mcp/mod.rs` + `docs/AGENT_INTROSPECTION_SCHEMA.md` + README + USER_GUIDE + CODEBASE + `docs/tasks/STRUCTURED-EMISSION-EXPANSION.md` + `docs/TASK_TREE.md` + ROADMAP + CHANGES + MEMORY.
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); one runner + one classifier not two (`feedback_full_factorization`); every capability opt-in + MCP-invocable + queryable + CLI-as-shim (decision `0017`); design the API for agents not humans (`feedback_api_for_agents_not_humans`); the book is the user-facing surface and must not drift (`feedback_book_doctrine`); decision/KM fact per durable capability or boundary. Doctrine-enforcement (decision `0026`): every doctrine = a deterministic check in `scripts/check_doctrines.sh`; code-scoped checks are scope-aware (non-code commits exempt).
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
