# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `STRUCTURED-EMISSION-EXPANSION.6b.2b` commit (the repo-owned `tool_matrix --task-emit-gate` downstream gate). Prior: `5f6822b` = `.6b.2a` (metric + schema `1.10`); `55761f9` = `.6b.1` (live `task automatic` surface); `7b6a500` = `.6a`; `b90bdff` = `.5` (**pushed** — `origin/main` is at `b90bdff`). Working tree clean after this commit. Push cadence: `origin/main` at `b90bdff` → **4 commits ahead after this commit (`.6a` `7b6a500`, `.6b.1` `55761f9`, `.6b.2a` `5f6822b`, this `.6b.2b`); push at ~30** (`feedback_push_cadence`).
- active_work_unit: **`STRUCTURED-EMISSION-EXPANSION` tree `active`; `.6`/`.6b` `active` containers; frontier = `.6b.3`** (the user-facing closeout). `.6b.2` is **done** (`.6b.2a` metric + schema `1.10` + `.6b.2b` the repo-owned `tool_matrix --task-emit-gate`, banked clean). `.6b.1`+`.6a`+`.5` done. The FIRST (combinational `function automatic`, `.1`+`.2`) and SECOND (`generate for` loop, `.3`+`.4`) surfaces are delivered end-to-end; the THIRD (`task automatic`) live surface + metric + repo-owned gate are done — only the docs closeout (`.6b.3`) remains. Future surfaces (nested/multi-level `generate`, `interface`/`modport`, richer tasks) are `.7+`. Index: `docs/TASK_TREE.md`.
- `.6b.2b` delivered (repo-owned gate, harness-only): `src/bin/tool_matrix.rs` `--task-emit-gate` + `ScenarioSet::TaskEmitSweep` + `build_task_emit_sweep_scenarios`/`task_emit_focus_config` (comb-only `task_emit_prob=1.0` × 3 strategies) + `ModuleReport.emitted_combinational_task` (`"task automatic"` SV-text detection) + `saw_combinational_task_emit` (Verilator-success + clean Yosys, full plan) + `MatrixReport.task_emit_gate` + merge + early-return gap arm + 5 proofs + 6 fixtures + `test_cli` default; README/USER_GUIDE/CODEBASE_ANALYSIS gate entries. Banked clean `/tmp/anvil-task-emit-gate-r1` (3 scenarios / 12 modules / 12 emitting a task / `coverage_gaps=[]` / `12/0` Verilator + both Yosys + Icarus). No schema bump; `cargo test --bin tool_matrix` 68; snapshots 6/6.
- `.6b.2a` (prior) delivered: `Metrics::num_emitted_combinational_tasks` (`= m.task_emit_gates.len()`) surfaced in introspection ⇒ schema `1.9 → 1.10` (MINOR is an integer ⇒ ten); all current-output schema refs bumped; lib 490; end-to-end `--introspect` default `0` / forced `39`. `.6b.1` delivered: the live `task automatic` emitter surface (`task_emit_prob` knob + `Module.task_emit_gates` + `src/ir/task_emit.rs` + emitter rendering; output-var + passthrough form; forced sweep clean across all tools).
- next_action: **PNT — execute `STRUCTURED-EMISSION-EXPANSION.6b.3`** (the user-facing closeout, docs-only / DUT byte-identical): extend `book/src/structured-emission.md` with a `## The third surface: a combinational task automatic` section (byte-verify a seed before/after — `task_emit_prob` 0.0→1.0 adds the `task automatic <wire>__t(...)` decl + `logic <wire>__tv;` + `always_comb` call + rewrites the gate's assign to the passthrough; the single-gate rule; the output-var passthrough form; the `function_emit` parallel; the `num_emitted_combinational_tasks` metric @ schema `1.10` + the `tool_matrix --task-emit-gate` proof; a skip-sentinelled repro bash block) + chapter intro update; add the `task_emit_prob` knob entry to `book/src/knobs.md` (the `### Structured emission` subsection), `USER_GUIDE.md` (after the `generate_loop_emit_prob` bullet), and the README "Current CLI truth" (a config-file-only knob bullet); add a KM how-to card `docs/knowledge/combinational-task-emit.md` (how-to keys distinct from decision `0014`'s conceptual keys + a validated reverify command) and regenerate the KM. `mdbook build book` + `check_knowledge_map` + `check_memory_architecture` + `cargo test --test book_examples` 3/3. Closes `.6b.3` / `.6b` / `.6` — the third structured surface delivered end-to-end. Doctrine: docs-only leaf (no source) — the standard `COMMIT.md` checklist without the task-tree code rules, but still record the leaf id.
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); every capability is an opt-in knob; decision/KM fact per durable capability or boundary.
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
