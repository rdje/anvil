# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `CI-PACKAGING-DISTRIBUTION.2b` commit — the **second impl slice** of decision `0022`: the drop-in **composite GitHub Action** wrapping `anvil hunt`. New: root `action.yml` (`uses: <owner>/anvil@<tag>`; composite, not container) + `scripts/anvil_hunt_action.sh` entrypoint + `.github/workflows/action-selftest.yml`. The entrypoint resolves the binary (`anvil-bin` escape hatch, else downloads the pinned release tarball — target from `RUNNER_OS`/`RUNNER_ARCH`, version=`GITHUB_ACTION_REF`, repo=`GITHUB_ACTION_REPOSITORY` ⇒ pin-by-ref), runs `anvil hunt` with hunt-flag inputs mapped 1:1 + Action-level plumbing (`anvil-version`/`anvil-bin`/`artifact-name`/`fail-on-finding`), and parses `HuntReport.summary.n_failures` (python3; `hunt` always exits 0 ⇒ the Action decides red/green). Self-test is presence-gated (absent tool = a spawn-failure finding, not a no-op ⇒ skip clean). **CI-infra only — no `src/`/`tests/`; DUT byte-identical.** Validated: **REAL local entrypoint smoke** (anvil-bin + verilator+yosys, 3 seeds → exit 0, findings=0, valid-JSON report, outputs wired) + `bash -n` + structural YAML lint + mem-arch + KM(56) green. Prior: `9527fe2`=`CI-PACKAGING…2a` (release.yml); `51d97d9`=`CI-PACKAGING…1` (ADR 0022). Push cadence: `origin/main` at `7142fd7` → **23 commits ahead** after this; **push at ~30** (`feedback_push_cadence`).
- active_work_unit: **`CI-PACKAGING-DISTRIBUTION` — `.1` (ADR) + `.2a` (release.yml) + `.2b` (composite Action) DONE; frontier = `.2c`** (docs + close: README/USER_GUIDE "Use ANVIL in your CI" — a copy-paste `uses:` snippet + the `action.yml` input table — + a KM card; closes `.2`). The tree then stays `active` with no frontier (more targets / a Marketplace listing / an MCP-driven variant are optional `.N`).
- next_action: **PNT — continue.** Advance to `CI-PACKAGING-DISTRIBUTION.2c` (docs + close — pure-docs/mdBook + a KM card; not a code change). Then the tree has no frontier ⇒ pick-and-roll (`feedback_pick_and_roll_at_no_frontier`) to a design-first ADR `.1`: `COVERAGE-STEERED-GENERATION.1` (construction-time coverage steering — rules-first, never generate-then-filter) or `CAPABILITY-BREADTH-EXPANSION.1`/`.2` (more SV up-opts / Mealy FSM); refine acceptance at pick.
- handoff: repo fully handoff-ready & **in sync** after this commit — `cargo check`/`cargo test` unaffected (no Rust touched; full `cargo test` was green at `51d97d9`); snapshots 6/6; `check_memory_architecture` + KM gen/check green (**KM 56 facts**); mdbook clean; introspection schema `1.11`. No pending doc-drift.
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); one runner + one classifier not two (`feedback_full_factorization`); every capability opt-in + MCP-invocable + queryable + CLI-as-shim (decision `0017`); design the API for agents not humans (`feedback_api_for_agents_not_humans`); the book is the user-facing surface and must not drift (`feedback_book_doctrine`); decision/KM fact per durable capability or boundary.
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
