# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `COVERAGE-STEERED-GENERATION.2c.2` **steering-lane docs + close `.2`** commit — **DOCS-ONLY** (no `src` change ⇒ DUT byte-identical). `book/src/algorithm.md` (the `roll_knob` prior-multiplier mechanism section), `agent-mcp.md` (the measure→derive→re-steer loop + the `coverage` tool + `coverage_readout` in `--introspect`), `api-tools.md`/`api-introspection.md`/`api-reference.md` (the `coverage` tool + the schema-`1.11→1.12` example refresh — the owner-deferred drift), `knobs.md` (roll-rate→steering cross-ref); `USER_GUIDE.md` (`--steer` row + the "Coverage steering" recipe). KM card = decision `0023` enriched (4 shipped-surface answer keys + status → **delivered**); `KNOWLEDGE_MAP.md` regen **58 facts / 519 question keys** (+4), check green. `ROADMAP.md` lane 6 marked **DONE**. Verified: `mdbook build` clean; `cargo test --test book_examples` 3/3 (the runnable `--steer` book example builds release + executes, exit 0). **This closes `.2c`, `.2`, and the whole `COVERAGE-STEERED-GENERATION` tree.** Prior: `12416c1`=`…2c.1` derive+`--steer`; `5151891`=`…2b` readout; `2530bfd`=`…2a` core. Push cadence: `origin/main` at `7142fd7` → **30 commits ahead** after this ⇒ **PUSH NOW** (`feedback_push_cadence`).
- active_work_unit: **`COVERAGE-STEERED-GENERATION` is `done` (`2026-06-22`, tree CLOSED)** — every acceptance criterion met (`.2a` mechanism / `.2b` readout+MCP query / `.2c.1` derive+`--steer` / `.2c.2` docs). Open-ended follow-ups (in-generator adaptive schedule; routing raw `gen_bool` sites through `roll_knob`) are optional future `.N` leaves, not blockers; nothing retired. **No active steering frontier.**
- next_action: **PUSH** (30 commits ahead ⇒ threshold hit), then **PNT — pick the next lane.** Remaining `active` lanes with concrete frontiers: `DOWNSTREAM-ADAPTER-EXPANSION` (frontier `.2a.3` = route `validate_tool_specs` + the `tool_matrix` columns through the `Adapter` registry byte-identically; then `.2b` sv2v / `.2c` slang). Others (`KNOB-ERGONOMICS-AND-PRESETS`, `CAPABILITY-BREADTH-EXPANSION`, `CI-PACKAGING-DISTRIBUTION`) are open-ended; self-select per `feedback_pick_and_roll_at_no_frontier`. Re-check `docs/TASK_TREE.md` active table before picking.
- handoff: repo fully handoff-ready & **in sync** after this commit — `mdbook` clean; `book_examples` 3/3; `check_memory_architecture` + KM gen/check green (**58 facts / 519 keys**); introspection schema `1.12`; **no pending doc-drift** (the steering-lane book/USER_GUIDE/KM + the schema-`1.12` example refresh all landed this slice; `README.md` + `docs/AGENT_INTROSPECTION_SCHEMA.md` already at `1.12`). Last full `cargo test` green at `.2c.1` (`12416c1`); this docs-only slice changes no `src`, so the code gate is unchanged.
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); one runner + one classifier not two (`feedback_full_factorization`); every capability opt-in + MCP-invocable + queryable + CLI-as-shim (decision `0017`); design the API for agents not humans (`feedback_api_for_agents_not_humans`); the book is the user-facing surface and must not drift (`feedback_book_doctrine`); decision/KM fact per durable capability or boundary.
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
