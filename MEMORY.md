# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `STRUCTURED-EMISSION-EXPANSION.4b.3` commit (the user-facing closeout — book chapter + knob docs + KM card; docs-only / DUT byte-identical; closes `.4b.3`/`.4b`/`.4` — the `generate for` loop surface delivered end-to-end). Prior: `2129035` = `.4b.2b` (the tool_matrix gate); `678b682` = `.4b.2a` (metric + schema `1.9`). Working tree clean after this commit. Push cadence: `origin/main` at `63d2622` → **29 commits ahead after this commit; push at ~30 — push imminent next slice** (`feedback_push_cadence`).
- active_work_unit: **`STRUCTURED-EMISSION-EXPANSION` tree `active` with NO current frontier** (an open-ended capability lane). BOTH delivered surfaces are complete end-to-end: the FIRST (combinational `function automatic`, `.1`+`.2`) and the SECOND (`generate for` loop of a `{N{x}}` 1-bit-lane replication, `.3`+`.4`, decision `0013`). Future surfaces (`task` [leading], nested/multi-level `generate`, `interface`/`modport`) are `.5+`, each its own decision when picked. Index: `docs/TASK_TREE.md`.
- `.4b.3` delivered (docs-only closeout): `book/src/structured-emission.md` `## The second surface: a generate for loop` (byte-verified seed-12 before/after; the `{N{x}}` 1-bit-lane rule; wider-lane exclusion; `function_emit` mutual exclusion; `gi = gi + 1`; metric + gate) + `generate_loop_emit_prob` knob entry in `book/src/knobs.md` / `USER_GUIDE.md` / README + KM card `generate-loop-emit` (KM 38→39 facts / 296→309 keys). mdbook + check_knowledge_map + check_memory_architecture + book_examples 3/3 green. Book example byte-verified downstream-clean (Verilator `-Wall` + both Yosys + Icarus).
- `.4`/`.4b` (closed): the `generate for` loop surface = `.4a` (design-detail) + `.4b.1` (live: `generate_loop_emit_prob` knob + `Module.generate_loop_gates` + `src/ir/generate_loop.rs` + the emitter rendering) + `.4b.2a` (metric + schema `1.9`) + `.4b.2b` (the `tool_matrix --generate-loop-gate`, banked `/tmp/anvil-generate-loop-gate-r1`) + `.4b.3` (docs). Default-off / DUT byte-identical throughout (snapshots 6/6). Nothing retired.
- next_action: **PNT — pick the next lane.** `STRUCTURED-EMISSION-EXPANSION` has no current frontier (open-ended). Per the active-tree table (`docs/TASK_TREE.md`), the remaining `active` trees are `SIGNOFF-AUTOMATION-EXPANSION` (no active frontier — higher-ceiling future leaves preserved per decision `0006`), `IDENTITY-DEEPENING` (no current frontier — deeper module-equivalence boundaries named/not-started), `SEMANTIC-INTROSPECTION-EXPANSION` (no active frontier — further derived-query kinds open-ended), and `LOCAL-REFERENCE-CACHE` (no active frontier). All current lanes are at no-active-frontier, so the next PNT step is an **owner-steer decision**: either open a new structured-emission `.5` surface (`task` is the leading candidate — decision `0013` recorded a simple combinational void `task` is clean on this toolchain), deepen one of the open-ended lanes, or take owner direction. **Also due now: push** (~30-commit cadence — 29 ahead). Consider a fresh session if context is degrading (repo is handoff-ready: tree clean, self-checks green, resume pointer current).
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); every capability is an opt-in knob; decision/KM fact per durable capability or boundary.
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
