# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `SEMANTIC-INTROSPECTION-EXPANSION.12b.2` **instance_input_bindings MCP surface + schema 1.24** commit — a **code + docs** change, task-tree-owned by `.12b.2`; **DUT byte-identical** (no IR/generator change; `analyze`/`introspect` read-mostly default-off; `tests/snapshots.rs` untouched). **Closes `.12b`/`.12` — `instance_input_bindings` delivered end-to-end** (the **eleventh** derived `analyze` query, the **parent-side dual of `instance_provenance` `.11`**; the MCP tool now answers eleven). `QUERY_INSTANCE_INPUT_BINDINGS` added to `supported_query_kinds()`; `run_analyze` branches by kind (`module`/`design_instance_input_bindings`) in both arms + the vec-aware `-32602` guard; `analyze_schema` enum + descriptions; `SCHEMA_VERSION 1.23 → 1.24` (17 assertion bumps + 2 new mcp proofs); schema-doc §6.7/changelog + book(agent-mcp/api-tools/api-introspection/api-reference/api-resources-prompts)/USER_GUIDE/README/TOOLBOX + a KM card (`KNOWLEDGE_MAP` 76 → 77 facts) + CODEBASE_ANALYSIS + ROADMAP. **The e2e `anvil-mcp` smoke found-and-corrected a control-port doc bug**: a clocked child's `clk`/`rst_n` ARE bound in `Instance.inputs` (the `.12a` design assumed otherwise) — the code already reported them; only the prose was fixed (analyze.rs doc + DEVELOPMENT_NOTES + book + KM). Validation: `cargo test --lib` 700/0/2; snapshots 6/6; clippy `-D warnings` + fmt + `mdbook build` + `book_examples` 3/3 + `check_doctrines` + KM green. E2e smoke (seed 42 hierarchy → schema `1.24`, design top `mod_42_0002`, 2 instances, 10 bindings; `u_0` input `clk` ← driver `{node:0, primary_input, "clk"}`; unknown instance ⇒ `-32602`). Prior: `ef16fba` (`.12b.1`). Push cadence: `origin/main` at `a767415`; **4 commits ahead** after this (`fc790e1`+`1933d2a`+`ef16fba`+this) ⇒ no push (threshold 30).
- active_work_unit: **None at a frontier — back at a PNT decision point.** `SEMANTIC-INTROSPECTION-EXPANSION.12` (`instance_input_bindings`) **closed** (`.12a`/`.12b.1`/`.12b.2`); the lane returns to a NO-FRONTIER boundary (stays `active`). Eleven derived analyze queries delivered @ schema `1.24`. Roadmap/codebase/mdBook aligned. `STRUCTURED-EMISSION-EXPANSION` also `active` at a NO-FRONTIER boundary (nine structured surfaces).
- next_action: **PNT — pick the next lane/slice.** Candidates at the no-frontier boundary: (a) a **twelfth** `SEMANTIC-INTROSPECTION-EXPANSION` derived query — natural siblings now: **recursive multi-level instance descent** (`instance_provenance`/`instance_input_bindings` through grand-children), or a per-FSM/per-memory **reach** (re-check redundancy vs `input_reach`), or a **path/witness** query (a representative path source→target, genuinely new). (b) `STRUCTURED-EMISSION-EXPANSION` tenth construct (nested `generate` / `interface`/`modport` — currently blocked/disqualified; needs a fresh decision + downstream probe). (c) `IDENTITY-DEEPENING` (open-ended whole-module sequential-equivalence). (d) `SIGNOFF-AUTOMATION-EXPANSION` (more knob axes as first-class matrix gates). Doctrine: **task-tree-own the new leaf BEFORE any `src/` edit**; SCHEMA-DERIVED/default-off/DUT byte-identical; mirror the `.Na` design-detail → `.Nb.1` pure core → `.Nb.2` surface cadence; run the e2e `anvil-mcp` smoke (it catches spec-vs-reality bugs); monitor RAM (`scripts/ram_guard.sh --threshold 90 -- <cmd>`).
- handoff: **repo handoff-ready (clean no-frontier boundary, lane closed).** `scripts/check_doctrines.sh` green (4 doctrines); tree clean; DUT byte-identical. `SEMANTIC-INTROSPECTION-EXPANSION.12` closed end-to-end (eleven derived analyze queries @ schema `1.24`). origin/main at `a767415`; **4 commits ahead** after this; threshold 30 ⇒ no push. A fresh session should PNT a new leaf (see next_action), task-tree-owning it first.
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); one runner + one classifier not two (`feedback_full_factorization`); every capability opt-in + MCP-invocable + queryable + CLI-as-shim (decision `0017`); design the API for agents not humans (`feedback_api_for_agents_not_humans`); SEMANTIC-INTROSPECTION stays SCHEMA-DERIVED / no-shadow-simulator (decisions `0004`/`0011`); the book is the user-facing surface and must not drift (`feedback_book_doctrine`); decision/KM fact per durable capability or boundary. Doctrine-enforcement (decision `0026`): every doctrine = a deterministic check in `scripts/check_doctrines.sh`; code-scoped checks are scope-aware (non-code commits exempt).
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
