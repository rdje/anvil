# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `STRUCTURED-EMISSION-EXPANSION.19b.3` **ninth-surface user docs** commit — a **DOCS-ONLY change** (no `src/`/`tests/`), task-tree-owned by `.19b.3`; **DUT byte-identical**. Closes `.19b`/`.19` ⇒ the NINTH structured surface (the wildcard `CasezMux` masked `if`/`else if` priority chain, decision `0029`) is **delivered end-to-end**. A `book/src/structured-emission.md` ninth-surface section (byte-verified seed-1 before/after: `casez (slice_0)` block vs masked `if ((slice_0 & 2'h2) == …)` chain) + chapter-intro surface list; the 4 deferred book example-JSON `schema_version` `1.16 → 1.17` (`agent-mcp.md` ×1 + `api-tools.md` ×3); `casez_mux_if_emit_prob` entries in `book/src/knobs.md` / `USER_GUIDE.md` / README + a `--casez-mux-if-gate` matrix-gate entry; a `docs/knowledge/casez-mux-if-emit.md` KM card (`KNOWLEDGE_MAP.md` regenerated 69 → 70 facts / 672 keys). `mdbook build` clean + `book_examples` 3/3 + `check_knowledge_map` OK; the before/after byte-matches real emitter output (rebuilt the stale `--bin anvil` first). Prior: `925c103` (`.19b.2b`). Push cadence: `origin/main` at `4564533` → **9 commits ahead** after this ⇒ no push (threshold 30, `feedback_push_cadence`).
- active_work_unit: **`STRUCTURED-EMISSION-EXPANSION` — NINE surfaces delivered end-to-end; the lane is at a NO-FRONTIER boundary (stays `active`).** The ninth surface (decision `0029`) is now live + metered (introspection schema `1.17`) + gated (`tool_matrix --casez-mux-if-gate`, banked clean) + documented (book/knobs/USER_GUIDE/README/KM). Future surfaces — nested/multi-level `generate`, `interface`/`modport` — are the recorded `.20+` candidates, each picked on its own merits with a fresh probe + decision.
- next_action: **PNT at a no-frontier boundary (`feedback_pick_and_roll_at_no_frontier`) — self-select the next lane.** All active owner-directed lanes are open-ended with no current frontier: `STRUCTURED-EMISSION-EXPANSION` (future `.20+` surfaces — pick nested/multi-level `generate` or re-probe `interface`/`modport`, each needs a fresh Verilator/Yosys/Icarus probe + a new `docs/decisions/00NN` + a `.20`/`.21` design leaf BEFORE any code), `SEMANTIC-INTROSPECTION-EXPANSION` (all 4 derived queries delivered; further query kinds are open-ended), `IDENTITY-DEEPENING` (deeper module-equivalence boundaries: memory/FSM/wrapper/retimed-state), `SIGNOFF-AUTOMATION-EXPANSION` (remaining implicit-knob axes / new acceptance columns). Doctrine: **task-tree-own the chosen leaf BEFORE any code change** (create the `docs/tasks/*` leaf + `docs/TASK_TREE.md` row + decision first). Monitor RAM (`scripts/ram_guard.sh --threshold 90 -- <cmd>`). (Tracked-lower: the `agent-mcp.md` `analyze`-example schema-`1.11` drift.)
- handoff: **repo handoff-ready after this commit** — `scripts/check_doctrines.sh` green (4 doctrines); `git diff --check` clean; DOCS-ONLY ⇒ DUT byte-identical; the ninth surface is delivered end-to-end (live + metered + gated + documented). A natural milestone + long session ⇒ a fresh session is recommended before picking the next lane. origin/main at `4564533`; 9 commits ahead after this. Co-staged: `book/src/structured-emission.md` + `book/src/knobs.md` + `book/src/agent-mcp.md` + `book/src/api-tools.md` + `USER_GUIDE.md` + README + `docs/knowledge/casez-mux-if-emit.md` + `KNOWLEDGE_MAP.md` + `docs/tasks/STRUCTURED-EMISSION-EXPANSION.md` + `docs/TASK_TREE.md` + CHANGES + MEMORY.
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); one runner + one classifier not two (`feedback_full_factorization`); every capability opt-in + MCP-invocable + queryable + CLI-as-shim (decision `0017`); design the API for agents not humans (`feedback_api_for_agents_not_humans`); the book is the user-facing surface and must not drift (`feedback_book_doctrine`); decision/KM fact per durable capability or boundary. Doctrine-enforcement (decision `0026`): every doctrine = a deterministic check in `scripts/check_doctrines.sh`; code-scoped checks are scope-aware (non-code commits exempt).
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
