# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `SEMANTIC-INTROSPECTION-EXPANSION.9a` **node_drivers impl design-detail** commit — a **docs/design-only change** (`DEVELOPMENT_NOTES.md` design entry + task-tree files + live-doc logs), **no `src/`** ⇒ **DUT byte-identical**. Opens `.9`, the **eighth** derived `analyze` query, `node_drivers` (per-node **immediate 1-hop driver adjacency** + each node's `GateOp` — the atomic node-level primitive complementing the transitive `output_support` cone; the **fourth** beyond decision `0011`'s four named kinds; its dual `node_readers` a natural future sibling). The PNT self-selected next-lane at the lane's no-frontier boundary (`feedback_pick_and_roll_at_no_frontier`). DEVELOPMENT_NOTES "`node_drivers` impl design-detail — `.9a`" resolves Q1–Q5 grounded in the real `Node`/`GateOp` IR + `analyze.rs`: an EIGHTH `node_drivers: Vec<NodeDrivers>` parallel vec (prior seven documents byte-identical) + `NodeDrivers { node, kind, op: Option<String>, width, drivers: Vec<NodeRef> }` + `NodeRef { node, kind, name }`; a single one-hop pass over `m.nodes` (no transitive walk); `"node:<id>"` addressing (None ⇒ all nodes ascending; leaf ⇒ known-but-empty; out-of-range ⇒ `-32602`); top-module module-vs-design (fmt closure); schema `1.20 → 1.21`; pre-split `.9b` → `.9b.1`/`.9b.2`. No new numbered decision (the `.3a`–`.8a` precedent; decision `0011` governs). Validation: `scripts/check_doctrines.sh` green (code-scoped checks scope-exempt for a docs commit); no `src/` ⇒ cargo unaffected; DUT byte-identical. Prior: `ce86560` (`.8b.2`). Push cadence: `origin/main` at `4564533` → **19 commits ahead** after this ⇒ no push (threshold 30, `feedback_push_cadence`).
- active_work_unit: **`SEMANTIC-INTROSPECTION-EXPANSION` — `.9` `node_drivers` (the EIGHTH derived query) opened; `.9a` design done, frontier `.9b.1`.** Seven queries already delivered end-to-end (schema `1.20`): `output_support`/`input_reach`/`flop_reset_provenance`/`module_reachability` (decision `0011`'s four) + `flop_dependencies` (`.6`) + `memory_provenance` (`.7`) + `fsm_provenance` (`.8`). `node_drivers` (`.9`) is the fourth beyond the four named kinds — the node-level immediate-fanin adjacency + `GateOp`, complementing the transitive cone. Nothing retired; `node_readers` (the dual), per-FSM/per-memory reach, per-child-module provenance remain open-ended breadth.
- next_action: **continue the PNT loop on `.9b.1`** — implement the pure `node_drivers` core in `src/introspect/analyze.rs` per the `.9a` design (the `.9b.1` leaf is already task-tree-owned): `QUERY_NODE_DRIVERS = "node_drivers"` + `NodeDrivers`/`NodeRef` structs + the eighth `node_drivers: Vec<NodeDrivers>` `skip_serializing_if` field (the existing `DerivedAnalysis` literals gain `node_drivers: Vec::new()`) + `module_node_drivers`/`design_node_drivers` (a single pass over `m.nodes` reading one level of operands; classify node + operand; resolve operand handles; ascending node id; operand order preserved); GateOp → stable string; **NOT** added to `supported_query_kinds()` yet (registry + `run_analyze` dispatch land together in `.9b.2`); lib proofs; snapshots 6/6 byte-identical. Then `.9b.2` (surface: registry + dispatch + schema `1.20 → 1.21` + `analyze_schema` enum + schema-doc/book/USER_GUIDE/README/KM + e2e `anvil-mcp` smoke). Monitor RAM (`scripts/ram_guard.sh --threshold 90 -- <cmd>`).
- handoff: **repo handoff-ready after this commit** — `scripts/check_doctrines.sh` green (4 doctrines); `git diff --check` clean; DUT byte-identical (docs/design only, no `src/`); `.9` opened with `.9a` design landed and `.9b.1`/`.9b.2` task-tree-owned. Note: a pre-existing prior-session gap — the `.8a`/`.8b.1`/`.8b.2` rows are missing from this tree's Verification Log + Commit Log (the Changelog has only `.8b.2`); a dedicated docs-hygiene backfill can close it (not bundled into `.9a` to keep the leaf↔commit mapping clean). origin/main at `4564533`; 19 commits ahead after this. Co-staged: `DEVELOPMENT_NOTES.md` + `docs/tasks/SEMANTIC-INTROSPECTION-EXPANSION.md` + `docs/TASK_TREE.md` + `CHANGES.md` + `MEMORY.md`.
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); one runner + one classifier not two (`feedback_full_factorization`); every capability opt-in + MCP-invocable + queryable + CLI-as-shim (decision `0017`); design the API for agents not humans (`feedback_api_for_agents_not_humans`); SEMANTIC-INTROSPECTION stays SCHEMA-DERIVED / no-shadow-simulator (decisions `0004`/`0011`); the book is the user-facing surface and must not drift (`feedback_book_doctrine`); decision/KM fact per durable capability or boundary. Doctrine-enforcement (decision `0026`): every doctrine = a deterministic check in `scripts/check_doctrines.sh`; code-scoped checks are scope-aware (non-code commits exempt).
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
