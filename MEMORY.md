# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `SEMANTIC-INTROSPECTION-EXPANSION.10b.2` **node_readers MCP surface + schema 1.22** commit — a **code change** (`src/mcp/mod.rs` + `src/introspect/{analyze,mod}.rs` + docs/book/KM), task-tree-owned by `.10b.2`; **DUT byte-identical** (`tests/snapshots.rs` untouched). Closes `.10b`/`.10` ⇒ the **ninth** derived `analyze` query, `node_readers` (the **exact transpose of `node_drivers` `.9`** — per-node immediate 1-hop reader adjacency), is **delivered end-to-end**: registry + `run_analyze` dispatch (`module`/`design_node_readers`) in one commit + the vec-aware `-32602` guard + `SCHEMA_VERSION 1.21 → 1.22` (15 assertion bumps + 2 new MCP proofs) + the `analyze_schema` enum + schema-doc §6.7/changelog + book(agent-mcp/api-tools)/USER_GUIDE/README + a KM card (`KNOWLEDGE_MAP` 74 → 75 facts / 734 keys) + an e2e `anvil-mcp` stdio smoke (seed 7 → schema `1.22`, 1674 nodes / 1653 with ≥1 reader, node 0 read by gates `[43, 705]`; unknown `node:999999` → `-32602`). Validation: `cargo test --lib` 678/0/2; snapshots 6/6; clippy `-D warnings` + fmt + `mdbook build` + `book_examples` 3/3 + `check_doctrines` green. Prior: `a2b5f98` (`.10b.1`). Push cadence: `origin/main` at `4564533` → **24 commits ahead** after this ⇒ no push (threshold 30, `feedback_push_cadence`).
- active_work_unit: **`SEMANTIC-INTROSPECTION-EXPANSION` — NINE derived queries delivered end-to-end (schema `1.22`); back at a NO-FRONTIER boundary (stays `active`).** The four named kinds (`output_support`/`input_reach`/`flop_reset_provenance`/`module_reachability`) + `flop_dependencies` (`.6`) + `memory_provenance` (`.7`) + `fsm_provenance` (`.8`) + `node_drivers` (`.9`) + `node_readers` (`.10`, the exact transpose of `node_drivers` — the node-level fan-out dual, `B ∈ node_drivers(A) ⇔ A ∈ node_readers(B)`; the fifth beyond the four named kinds). Nothing retired; further derived-query kinds are open-ended breadth (a per-FSM/per-memory reach; per-child-module provenance).
- next_action: **PNT at a no-frontier boundary (`feedback_pick_and_roll_at_no_frontier`) — self-select the next lane.** Candidate continuations: stay in `SEMANTIC-INTROSPECTION-EXPANSION` (a per-FSM/per-memory reach; per-child-module provenance — each needs a new `.Na` design-detail leaf + a fresh derivation), or switch lanes — `STRUCTURED-EMISSION-EXPANSION` (needs a genuinely new construct + fresh probe + decision), `IDENTITY-DEEPENING` (deeper module-equivalence), `SIGNOFF-AUTOMATION-EXPANSION` (new tool columns / implicit-knob axes). **OR pick the flagged docs-hygiene backfill** (a docs-only leaf): close the `.8a`/`.8b.1`/`.8b.2` missing Verification/Commit-Log rows, the `.5b.2` + `.9` stale `active`/`pending` Status labels, and the `docs/api-introspection.md` schema `1.14 → 1.22` drift. Doctrine: **task-tree-own the chosen leaf BEFORE any code**. Monitor RAM (`scripts/ram_guard.sh --threshold 90 -- <cmd>`).
- handoff: **repo handoff-ready after this commit** — `scripts/check_doctrines.sh` green (4 doctrines); `git diff --check` clean; DUT byte-identical (snapshots 6/6; lib 678/0/2); the ninth derived query is delivered end-to-end (live + schema-versioned 1.22 + MCP-dispatched + cached + documented + KM-carded + e2e-smoked). A long, productive session (`.10a` + `.10b.1` + `.10b.2`, after a full bootstrap read) ⇒ a fresh session is reasonable before picking the next lane. Pre-existing doc-hygiene gaps (not bundled, per no-bundling): the `.8a`/`.8b.1`/`.8b.2` rows are missing from this tree's Verification Log + Commit Log; the `.5b.2` + `.9` leaf Status read stale (`pending`/`active` though done); `docs/api-introspection.md` is stale at schema `1.14` — a dedicated docs-hygiene backfill leaf can close all. origin/main at `4564533`; 24 commits ahead after this. Co-staged: `src/introspect/{analyze,mod}.rs` + `src/mcp/mod.rs` + `docs/AGENT_INTROSPECTION_SCHEMA.md` + `book/src/{agent-mcp,api-tools}.md` + `USER_GUIDE.md` + README + 3 KM cards + `KNOWLEDGE_MAP.md` + `CODEBASE_ANALYSIS.md` + `ROADMAP.md` + `docs/tasks/SEMANTIC-INTROSPECTION-EXPANSION.md` + `docs/TASK_TREE.md` + CHANGES + MEMORY.
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); one runner + one classifier not two (`feedback_full_factorization`); every capability opt-in + MCP-invocable + queryable + CLI-as-shim (decision `0017`); design the API for agents not humans (`feedback_api_for_agents_not_humans`); SEMANTIC-INTROSPECTION stays SCHEMA-DERIVED / no-shadow-simulator (decisions `0004`/`0011`); the book is the user-facing surface and must not drift (`feedback_book_doctrine`); decision/KM fact per durable capability or boundary. Doctrine-enforcement (decision `0026`): every doctrine = a deterministic check in `scripts/check_doctrines.sh`; code-scoped checks are scope-aware (non-code commits exempt).
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
