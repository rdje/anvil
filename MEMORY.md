# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `SV-VERSION-TARGETING.3a` commit (hash backfills next update; prior: `ef3d94e` = `.2b.2b`; `e3c2f4b` = `.2b.2a`; `c9895ad` = `.2b.1`). Working tree clean after this commit.
- active_work_unit: **`SV-VERSION-TARGETING`** (owner-directed capability lane, **`active`**, opened `2026-06-15`). `.1`+`.2a`+`.2b.1`+`.2b.2a`+`.2b.2b` done ⇒ `.2` closed. **`.3` split into `.3a` (design — done, decision `0010`) + `.3b` (impl, `proposed`).** Frontier leaf: **`.3b`**. Two sibling lanes registered `proposed`: `STRUCTURED-EMISSION-EXPANSION`, `SEMANTIC-INTROSPECTION-EXPANSION`. Index: `docs/TASK_TREE.md`.
- next_action: continue PNT (no self-pause — `feedback_no_self_pause_until_trees_closed`) → implement **`.3b`**: a **default-off heterogeneous-width packed `union soft` (IEEE 1800-2023 §7.3.1)** aggregate projection gated on `sv_version >= Sv2023` (sibling of `AggregateKind::StructPacked`/`ArrayPacked` in `src/ir/aggregate.rs`; emit at `render_aggregate_typedef` in `src/emit/sv.rs`); `< 2023` down-gate fallback = the existing packed `struct` projection ⇒ **default byte-identical** (`tests/snapshots.rs` 6/6 untouched). Prove Verilator `--language 1800-2023` matching-mode-clean (`--binary` builds it; probe gave `y=a5`); **Yosys/Icarus reject the syntax ⇒ recorded no-op** (existing `.2b.2b` facts require Yosys-clean, so add a dedicated `saw_sv_version_2023_soft_union_upopt` fact + matrix up-opt scenario). Update `tests/sv_version.rs` (now DIVERGES at 2023 when the knob fires) + `tests/sv_version_downstream.rs` + book(knobs/sv-version)/USER_GUIDE/README/ROADMAP + KM. Pre-split `.3b` → `.3b.1` (design-detail: projection shape — internal overlay vs port-fold) + `.3b.2` (impl) if broad. Alternatively resume parked `IDENTITY-DEEPENING.3b.2b` per table order if owner re-prioritizes.
- `.3a` delivered (DOCS-ONLY, no source change): decision `0010` names the first up-opt = heterogeneous-width packed `union soft` (1800-2023 §7.3.1). **Empirical probe finding (Verilator 5.046 / Yosys 0.64 / Icarus 13.0): the installed tools do NOT enforce 1800-version acceptance** (Verilator accepts + reserves keywords identically across all `--language` modes; Yosys/Icarus have no 1800 selector) ⇒ the up-opt's teeth are LRM correctness + construction-time down-gating + matching-mode acceptance, NOT tool-side rejection. Real teeth confirmed: a non-soft heterogeneous packed union is rejected by all three (Verilator cites the standard). `DEVELOPMENT_NOTES.md` design-detail + tree split + `KNOWLEDGE_MAP.md` regen.
- **PARKED (not abandoned): `IDENTITY-DEEPENING`** (`active`, frontier `.3b.2b`) — cross-module whole-module sequential equivalence, **fully designed** (decision `0008` + `.3b.1` detail + `.3b.2a` `bisimulation_partition` helper landed `edae1b0`). Resume from `docs/tasks/IDENTITY-DEEPENING.md` `.3b.2b`. Owner redirected priority to `SV-VERSION-TARGETING`.
- lane invariants (both lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); every capability is an opt-in knob; decision/KM fact per durable capability or boundary.
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`. `.3a` is docs-only (self-checks only). The next code leaf (`.3b` impl) touches `src/ir/aggregate.rs`+`src/emit/sv.rs`+`src/config.rs`+`src/bin/tool_matrix.rs` ⇒ run snapshots 6/6 + `tests/sv_version` + `tests/sv_version_downstream` (`#[ignore]`) + the matrix up-opt gate. Push cadence: `origin/main` at `381ec01`; **26 commits ahead after this commit — push after 30 accumulate** (`feedback_push_cadence`).

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
