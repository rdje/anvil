# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `STRUCTURED-EMISSION-EXPANSION.17b.2b` **repo-owned `tool_matrix --case-mux-if-gate`** commit — **CODE change** (`src/bin/tool_matrix.rs` only — a test/CI harness binary; the DUT generator is untouched), default-off ⇒ **DUT byte-identical**. The eighth surface's downstream gate, templated 1:1 on `--mux-if-gate` (~30 touch points): `CASE_MUX_IF_SWEEP_MIN_UNITS_PER_SCENARIO` + `--case-mux-if-gate` flag + `ScenarioSet::CaseMuxIfSweep` + `build_case_mux_if_sweep_scenarios`/`case_mux_if_focus_config` + **METRIC-KEYED** `ModuleReport.emitted_case_mux_if` (`= prepared.metrics.num_emitted_case_mux_if_chains > 0`, NO new text token — first matrix surface keyed off a `Metrics` field, not `sv_text`) + `CoverageSummary.saw_case_mux_if_emit` + merge + `MatrixReport.case_mux_if_gate` + run-plan/dispatch/mutual-exclusion/summarize/gap-arm/3 exhaustive match arms/slug arms/`test_cli` default + 5 proofs + 8 fixtures. Focus config diverges in ONE way: `comb_mux_prob = 0.0` + `case_mux_prob = 0.9` (comb-mux rolls before case-mux in `cone.rs` and would starve it; no encoding-path steering — a `CaseMux` selector is a dynamic cone by construction). `cargo check/clippy(-D warnings)/fmt` clean (one `doc_lazy_continuation` fixed — wrapped doc line began with `+`); `cargo test --bin tool_matrix` 89→**94** (5 new proofs); snapshots **6/6 byte-identical**. Banked clean `/tmp/anvil-case-mux-if-gate-r1` (3 scenarios / 12 modules / 12 emitting a chain / 83 chains / `coverage_gaps=[]` / `saw_case_mux_if_emit=true` / 12/0 Verilator + both Yosys + Icarus). Prior: this `STRUCTURED-EMISSION-EXPANSION.17b.2a` commit. Push cadence: `origin/main` at `4564533` → **2 commits ahead** after this ⇒ no push (threshold 30, `feedback_push_cadence`).
- active_work_unit: **`STRUCTURED-EMISSION-EXPANSION` — frontier `.17b.3`** (the eighth surface's user docs). `.16` (decision `0028`) + `.17a` (design-detail) + `.17b.1` (live source) + `.17b.2a` (metric+schema `1.16`) + `.17b.2b` (gate) done; `.17b.3` (docs) pending → on its completion `.17b`/`.17` close, EIGHTH surface delivered end-to-end. SEVEN surfaces delivered (`.1`–`.15`); the EIGHTH is live + metered + gated. All other `active` lanes at no-frontier boundaries.
- next_action: **PNT into `STRUCTURED-EMISSION-EXPANSION.17b.3`** (user docs for the eighth surface — docs-only ⇒ DUT byte-identical, NOT task-tree-code-gated but still task-owned). Add the `case_mux_if_emit_prob` / `--case-mux-if-emit-prob` knob entry to `book/src/knobs.md` + a `book/src/structured-emission.md` "## The eighth surface: a procedural `if`/`else if` priority chain" section (byte-verified before/after example: a dynamic `CaseMux` parallel `case` → the `if (sel==…) … else if … else …` chain, no `__cv` passthrough since it's already an `always_comb` var) + the `case_mux_if_emit_prob` row in USER_GUIDE + README "Current CLI truth" + the deferred book example-JSON `schema_version` `1.15 → 1.16` refresh (the `.17b.2a` deferral — `api-tools.md`/`agent-mcp.md`) + a `docs/knowledge/case-mux-if-emit.md` KM card (regenerate `KNOWLEDGE_MAP.md`). Then `cargo test --test book_examples` + `mdbook build book` + `check_knowledge_map`. On completion flip `.17b`/`.17`/`.15`-style parents per the closed-parent convention. Monitor RAM on heavy builds (`scripts/ram_guard.sh --threshold 90 -- <cmd>`).
- handoff: repo handoff-ready after this commit — `scripts/check_doctrines.sh` green (4 doctrines); `git diff --check` clean; default-off ⇒ DUT byte-identical; the eighth surface is live + metered + gated + fully validated (`cargo test --bin tool_matrix` 94 / snapshots 6/6 / banked gate `/tmp/anvil-case-mux-if-gate-r1` clean). origin/main at `4564533` (the `.17b.1` push); 2 commits ahead after this. Co-staged: `src/bin/tool_matrix.rs` + README + USER_GUIDE + CODEBASE + ROADMAP + DEVELOPMENT_NOTES + `docs/tasks/STRUCTURED-EMISSION-EXPANSION.md` + `docs/TASK_TREE.md` + CHANGES + MEMORY.
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); one runner + one classifier not two (`feedback_full_factorization`); every capability opt-in + MCP-invocable + queryable + CLI-as-shim (decision `0017`); design the API for agents not humans (`feedback_api_for_agents_not_humans`); the book is the user-facing surface and must not drift (`feedback_book_doctrine`); decision/KM fact per durable capability or boundary. Doctrine-enforcement (decision `0026`): every doctrine = a deterministic check in `scripts/check_doctrines.sh`; code-scoped checks are scope-aware (non-code commits exempt).
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
