# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `SEMANTIC-INTROSPECTION-EXPANSION.11a` **instance_provenance impl design-detail** commit — a **docs/design-only** change (DEVELOPMENT_NOTES + task-tree files), task-tree-owned by `.11a`; **DUT byte-identical** (no `src/`; no runnable bash blocks changed ⇒ `tests/book_examples.rs` unaffected). Opens `.11`, the **tenth** derived `analyze` query, `instance_provenance` — the **third opaque-leaf boundary-opener** (opens `Node::InstanceOutput`, the sibling of `.7`'s `MemRead` + `.8`'s `FsmOut`) and the **first derived query that crosses the module boundary** (hence **Design-only**: descend into the child `Module` and `build_cone` per child output port inside the child, reusing `build_cone` unchanged). `.11a` design-detail resolves Q1–Q5 (a TENTH `instance_provenance: Vec<InstanceProvenance>` vec reusing `SupportCone`; descend-into-child derivation; instance-**name** addressing; Design-only module-vs-design; schema `1.22 → 1.23`) + the `.11b` pre-split. Validation: `bash scripts/check_doctrines.sh` green (docs commit ⇒ code-scoped checks exempt). Prior: `44848a7` (`LIVE-DOC-DRIFT-FIX.2`). Push cadence: `origin/main` at `4564533` → **29 commits ahead** after this ⇒ no push yet (threshold 30, `feedback_push_cadence`; push after `.11b.1` hits 30).
- active_work_unit: **`SEMANTIC-INTROSPECTION-EXPANSION.11` (`instance_provenance`) — in flight; frontier leaf `.11b.1` (pure core), `pending`.** `.11a` design landed. `STRUCTURED-EMISSION-EXPANSION` stays `active` at a NO-FRONTIER boundary (nine structured surfaces). Roadmap/codebase/mdBook aligned.
- next_action: **Land `SEMANTIC-INTROSPECTION-EXPANSION.11b.1` — the pure `instance_provenance` core in `src/introspect/analyze.rs`.** Add `QUERY_INSTANCE_PROVENANCE = "instance_provenance"` + `InstanceProvenance { instance, module, role, output_support: Vec<SupportCone> }` (reusing `SupportCone`) + the tenth `instance_provenance: Vec<InstanceProvenance>` field on `DerivedAnalysis` (`#[serde(default, skip_serializing_if = "Vec::is_empty")]`) + `module_instance_provenance` (degenerate no-child-defs: instances with empty cones) + `design_instance_provenance` (descend into the child — for each top instance look up its child `Module` in `design.modules`, `build_cone` per child output port inside the child with `format_instance_leaf_design`, instances sorted by name). Add `instance_provenance: Vec::new()` to every existing `DerivedAnalysis` literal (a guarded python insert per the `.10b.1` precedent). **Do NOT add to `supported_query_kinds()` yet** (registry + `run_analyze` dispatch land together in `.11b.2`). ~10 lib proofs incl. the **cross-boundary descent proof** (a hand-built design: top instantiates child `u0` of module `c` whose output `o = a` (its input); `design_instance_provenance` reports instance `u0`, module `c`, role `planned_child`, one cone `target "u0.o"` with `support_inputs ["a"]` — the CHILD's input). `cargo test --lib` + snapshots 6/6 + clippy `-D warnings` + fmt clean; DUT byte-identical. Then `.11b.2` (surface: registry + `run_analyze` dispatch + empty-guard + `SCHEMA_VERSION 1.22 → 1.23` + assertion bumps + `analyze_schema` enum + schema-doc §6.7/changelog + book(agent-mcp/api-tools)/USER_GUIDE/README/KM + e2e `anvil-mcp` hierarchy smoke). Doctrine: **task-tree leaf `.11b.1` already owns the `src/` edit** (created in `.11a`); SCHEMA-DERIVED/default-off/DUT byte-identical; monitor RAM (`scripts/ram_guard.sh --threshold 90 -- <cmd>`).
- handoff: **repo handoff-ready.** `scripts/check_doctrines.sh` green (4 doctrines); tree clean; DUT byte-identical (this `.11a` slice was docs/design-only). `SEMANTIC-INTROSPECTION-EXPANSION.11a` landed (design-detail); the next leaf `.11b.1` is fully designed (see next_action — mirror the `.10b.1` cadence: pure core in `analyze.rs`, the tenth vec + fill-ins, the cross-boundary descent proof, NOT yet in `supported_query_kinds()`). origin/main at `4564533`; **29 commits ahead** after this (~1 commit from the 30 push threshold). A fresh session should land `.11b.1` then `.11b.2`, each per COMMIT.md.
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); one runner + one classifier not two (`feedback_full_factorization`); every capability opt-in + MCP-invocable + queryable + CLI-as-shim (decision `0017`); design the API for agents not humans (`feedback_api_for_agents_not_humans`); SEMANTIC-INTROSPECTION stays SCHEMA-DERIVED / no-shadow-simulator (decisions `0004`/`0011`); the book is the user-facing surface and must not drift (`feedback_book_doctrine`); decision/KM fact per durable capability or boundary. Doctrine-enforcement (decision `0026`): every doctrine = a deterministic check in `scripts/check_doctrines.sh`; code-scoped checks are scope-aware (non-code commits exempt).
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
