# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `STRUCTURED-EMISSION-EXPANSION.4b.1` commit (live surface — the `generate for` loop emit-projection goes live: `generate_loop_emit_prob` knob + `Module.generate_loop_gates` + new `src/ir/generate_loop.rs` + the `to_sv_with_modules` rendering + 9 lib proofs; default-off / DUT byte-identical). Prior: `0f821d7` = `.4a` (design-detail); `c57d5b4` = `.3` (decision `0013`). Working tree clean after this commit. Push cadence: `origin/main` at `63d2622` → **26 commits ahead after this commit; push at ~30** (`feedback_push_cadence`).
- active_work_unit: **`STRUCTURED-EMISSION-EXPANSION` tree `active`; `.4` + `.4b` are `active` containers; frontier = `.4b.2`** (the repo-owned `tool_matrix --generate-loop-gate` gate + the `num_emitted_generate_loops` metric, schema `1.8→1.9` + the `saw_generate_loop_emit` coverage fact), then `.4b.3` (book/USER_GUIDE/README/KM closeout). The first surface (combinational `function automatic`, `.1`+`.2`) is delivered end-to-end. Future surfaces (`task` [leading], nested/multi-level `generate`, `interface`/`modport`) are `.5+`, each its own decision. Index: `docs/TASK_TREE.md`.
- `.4b.1` delivered (live surface): `Config::generate_loop_emit_prob` (default `0.0`, config-file-only) + `Module.generate_loop_gates` + new `src/ir/generate_loop.rs annotate_generate_loop_gates` (candidate = a `{N{x}}` `Concat` — `>= 2` same-NodeId operands — with a **1-bit lane** so `W == N`; excludes `function_emit_gates`/`soft_union_slice_gates`; `param_env` skipped) + two guarded gen-time call-site rolls (after function-emit) + `src/emit/sv.rs` `generate_loop_gate` accessor + `render_generate_loop_block` + generate-block section + assign-loop `continue` suppressing the inline `{N{x}}`. Increment `gi = gi + 1` (portable; `gi++` not retired). 9 lib proofs. No schema bump (default-off prob-knob; `.4b.2` metric bumps `1.8→1.9`). Default-off / DUT byte-identical (snapshots 6/6; lib 477). Forced `generate_loop_emit_prob=1.0` sweep (5 seeds, `/tmp/anvil-gl-r1/`, 62–168 loops): Verilator `--lint-only` 5/5 / 0 warnings, **`-Wall` ON-vs-OFF Δ=0**, Yosys both modes 5/5, Icarus 5/5. The 1-bit `{W{sel}}` broadcast (one-hot mux-mask idiom) fires on 27/30 default seeds.
- next_action: **PNT — execute `STRUCTURED-EMISSION-EXPANSION.4b.2`** (the repo-owned downstream gate + metric, templated on `--function-emit-gate`): a `Metrics::num_emitted_generate_loops` (`= m.generate_loop_gates.len()`) surfaced in introspection ⇒ schema MINOR bump `1.8→1.9` (the `.2b.2a` precedent — bump `SCHEMA_VERSION` + the schema_version test assertions + schema doc + README/USER_GUIDE/book current-output refs) + a `saw_generate_loop_emit` coverage fact + `tool_matrix --generate-loop-gate` + `ScenarioSet::GenerateLoopSweep` + `build_generate_loop_sweep_scenarios` (a replication-rich comb-only DUT forcing `generate_loop_emit_prob=1.0` × 3 strategies — must actually emit `{N{x}}` 1-bit replications; forced-sweep evidence already at `/tmp/anvil-gl-r1/`) + `ModuleReport.emitted_generate_loop` SV-text detection + `saw_*` merge + early-return gap enforcement + proofs + the ModuleReport fixture updates. Bank clean (Verilator + both Yosys + Icarus). Pre-split `.4b.2a` (metric+schema) + `.4b.2b` (gate) if too broad (the `.2b.2` precedent). Default-off / DUT byte-identical. Then `.4b.3` docs closeout. Doctrine: NO code change without a task-tree leaf owning it first — `.4b.2` owns it.
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); every capability is an opt-in knob; decision/KM fact per durable capability or boundary.
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
