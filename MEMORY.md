# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `STRUCTURED-EMISSION-EXPANSION.12b.3` **multi-output task user docs (sixth surface end-to-end)** commit — docs-only, **DUT byte-identical** (no `src/`). `book/src/structured-emission.md` `## The sixth surface` section (byte-verified seed-3 before/after — shared `i_2` is the single formal `a1` feeding both outputs, the co-supported sink) + `multi_output_task_emit_prob` knob entry in `book/src/knobs.md` / `USER_GUIDE.md` / README + new KM card `docs/knowledge/multi-output-task-emit.md` (working reverify, cross-linked to decision `0025`) + the deferred `book/src/{api-tools,agent-mcp,api-introspection,api-reference}.md` example-JSON `schema_version` `1.13 → 1.14` refresh (historical `num_mealy_fsm_modules @ 1.13` refs left intact). KM 61→62 facts / 565 keys. **`.12b.3` / `.12b` / `.12` all close — the SIXTH structured surface (decision `0025`) is delivered end-to-end.** Prior: `92ed8d8`=`…12b.2b` gate; `21bc84c`=`…12b.2a` metric/schema; `cae38ce`=`…12b.1` live; `46669ec`=`…12a`; `c31fbb0`=`…11`. Push cadence: `origin/main` at `605ec44` → **11 commits ahead** after this ⇒ no push (threshold 30, `feedback_push_cadence`).
- active_work_unit: **`STRUCTURED-EMISSION-EXPANSION` tree `active`, NO current frontier** (open-ended). SIX structured surfaces delivered end-to-end: function `.1`+`.2`, generate-for `.3`+`.4`, task `.5`+`.6`, wide-lane part-select `.7`+`.8`, cone-function `.9`+`.10`, multi-output task `.11`+`.12` (decision `0025`, banked clean `/tmp/anvil-multi-output-task-gate-r1`, introspection schema `1.14`). Future surfaces (wider `k>2` task groups / nested-multi-level `generate` / `interface`-`modport`) are `.13+`, each its own decision when picked.
- next_action: **PNT — at a no-frontier boundary** (per `feedback_pick_and_roll_at_no_frontier`, self-select; don't surface a steer question). The just-finished STRUCTURED-EMISSION lane is open-ended; pick the next move: either (a) a NEW structured surface `.13+` for STRUCTURED-EMISSION (wider `k>2` co-supported task groups is the natural next — generalize the pair to a group; nested/multi-level `generate` and `interface`/`modport` remain deferred per decisions `0015`/`0016`), or (b) re-scan `docs/TASK_TREE.md` for another `active` lane with a live frontier and PNT into it. Any new surface = a design-first ADR (decision `0026`) before code, empirically grounded with the installed tools (verilator 5.046 / yosys 0.64 / iverilog / mdbook; sv2v + slang absent).
- handoff: repo handoff-ready after this commit — `cargo check/clippy/fmt` + `--bin tool_matrix` (84) + `--lib` (600) + snapshots 6/6 + `book_examples` 3/3 green; `--multi-output-task-gate` banked clean; `--introspect` schema 1.14; `mdbook build` + `check_memory_architecture` + KM check (62 facts) green.
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); one runner + one classifier not two (`feedback_full_factorization`); every capability opt-in + MCP-invocable + queryable + CLI-as-shim (decision `0017`); design the API for agents not humans (`feedback_api_for_agents_not_humans`); the book is the user-facing surface and must not drift (`feedback_book_doctrine`); decision/KM fact per durable capability or boundary.
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
