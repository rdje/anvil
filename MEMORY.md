# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `STRUCTURED-EMISSION-EXPANSION.10b.3` commit (the fifth surface's user-facing docs closeout — book §"The fifth surface: a multi-gate-cone function automatic" [byte-verified seed-4 before/after] + `cone_function_emit_prob` knob entry in `book/src/knobs.md`/`USER_GUIDE.md`/README + KM card `multi-gate-cone-function-emit` [KM 43→44 facts / 351→364 keys]; **docs-only / DUT byte-identical; `mdbook build` + KM + memory-arch + `book_examples` 3/3 green**; the FIFTH structured surface is delivered end-to-end; lane → no current frontier). Prior: `015a09b` = `.10b.2`; `081583d` = `.10b.1`; `2d3a0ca` = `.10a`; `46a377e` = `.9` (decision `0016`); `afb5718` = `.8b`; … `b90bdff` = `.5` (**pushed** — `origin/main` is at `b90bdff`). Working tree clean after this commit. Push cadence: `origin/main` at `b90bdff` → **14 commits ahead (`.6a`,`.6b.1`,`.6b.2a`,`.6b.2b`,`.6b.3`,`LIVE-DOC-CODEBASE-ALIGNMENT.2`,`.7`,`.8a`,`.8b`,`.9`,`.10a`,`.10b.1`,`.10b.2`,this `.10b.3`); push at ~30** (`feedback_push_cadence`).
- active_work_unit: **`STRUCTURED-EMISSION-EXPANSION` tree `active`, NO current frontier** (open-ended lane). **FIVE** structured surfaces delivered end-to-end: `function automatic` (`.1`+`.2`), `generate for` loop (`.3`+`.4`), `task automatic` (`.5`+`.6`), wider-lane `generate for` part-select (`.7`+`.8`, decision `0015`), and the **multi-gate-cone `function automatic`** (`.9`+`.10`, decision `0016`; introspection schema `1.11`). Future surfaces (multi-output tasks / nested-multi-level `generate` / `interface`-`modport`) are `.11+`. **All other active trees are also at no-frontier boundaries** (`SIGNOFF-AUTOMATION-EXPANSION`, `IDENTITY-DEEPENING`, `SEMANTIC-INTROSPECTION-EXPANSION`, `LOCAL-REFERENCE-CACHE`). Index: `docs/TASK_TREE.md`.
- next_action: **PNT at a no-frontier boundary** (`feedback_pick_and_roll_at_no_frontier`): self-select the next lane and roll — most natural is the **SIXTH structured surface** as a new `.11` design/decision leaf (the recorded decision-`0016` runner-up = a **multi-output combinational `task`** [clean + sim-equiv in the `/tmp/anvil-se9-probe/` probe; its source is co-supported-sink gate groups — needs a selection policy], over nested/multi-level `generate` [no routine by-construction 2D source] and the still-disqualified `interface`/`modport`). Open `.11` with a fresh empirical probe (Verilator `-Wall` + both Yosys + Icarus + iverilog sim-equiv), write decision `0017`, then pre-split `.11`/`.12`. Doctrine: a new ADR + task-tree leaf BEFORE any code; default-off / byte-identical; its own knob; downstream gate; no retirement.
- handoff: repo handoff-ready — `cargo check --lib --bin tool_matrix`/`clippy -D warnings`/`fmt --check` clean; full `cargo test` all green (lib 502, tool_matrix 73, book_examples 3/3, integration 123, snapshots 6/6 byte-identical); the fifth surface is downstream-clean end-to-end (`/tmp/anvil-cone-function-gate-r1`, 148 cone functions, `coverage_gaps=[]`, `12/0` Verilator + both Yosys + Icarus); end-to-end `--introspect` schema `1.11` (default `0` / forced `14`); tree clean; all self-checks green; KM at 44 facts / 364 keys. The fifth structured surface ships end-to-end (`.9`+`.10a`+`.10b.1`+`.10b.2`+`.10b.3`); the lane is at a clean no-frontier boundary ready for the sixth surface.
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); every capability is an opt-in knob (the fifth surface gets its OWN `cone_function_emit_prob` so the single-gate surface stays byte-identical); decision/KM fact per durable capability or boundary.
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
