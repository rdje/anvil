# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `STRUCTURED-EMISSION-EXPANSION.15b.1` **procedural if/else mux emit-projection (live surface)** commit — **CODE change** (first `src/` change of the seventh-surface lane) / default-off ⇒ **DUT byte-identical**. The procedural `always_comb` `if`/`else` projection of a 2:1 `Mux` (decision `0027`) goes live behind the opt-in `mux_if_emit_prob` knob: new `src/ir/mux_if_emit.rs` (`annotate_mux_if_gates` — the `GateOp::Mux` predicate excluding all six sibling marks, run **last**; 10 lib proofs) + `Module.mux_if_gates: BTreeSet<NodeId>` + `ir/mod.rs` registration + `Config::mux_if_emit_prob` knob + `--mux-if-emit-prob` flag + two guarded `gen/mod.rs` rolls (after `cone_function`, single + design) + the `emit/sv.rs` procedural-block section (`logic [w-1:0] <name>__cv;` + `always_comb if/else`, operand refs via `node_ref`) + the gate-assign-loop `<name>__cv` passthrough. Implemented exactly per `.15a` (no deviations); `CODEBASE_ANALYSIS.md` + `DEVELOPMENT_NOTES.md` updated. No metric/schema bump (rides the version; `.15b.2` bumps `1.14→1.15`). `cargo check`/`clippy`/`fmt` clean; `cargo test --lib` 615 (605 + 10 new); snapshots 6/6 byte-identical; forced sweep `/tmp/anvil-muxif-genproof.*` (5 seeds): every mux → `__cv`, Verilator `-Wall` Δ=0 + both Yosys + Icarus clean + ON-vs-OFF sim-equiv 20000 vectors. Prior: `0084ceb`=`…15a`. Push cadence: `origin/main` at `605ec44` → **24 commits ahead** after this ⇒ no push (threshold 30, `feedback_push_cadence`).
- active_work_unit: **`STRUCTURED-EMISSION-EXPANSION` — frontier `.15b.2`** (the metric + repo-owned gate; `.14` design + `.15a` design-detail + `.15b.1` live surface all `done`). `.15b.2` = `Metrics::num_emitted_mux_if_blocks` (`= m.mux_if_gates.len()`, `#[serde(default)]`) in `metrics::compute()` + a lib proof ⇒ introspection `SCHEMA_VERSION` `1.14 → 1.15` (bump all current-output refs: introspect + mcp assertions + `docs/AGENT_INTROSPECTION_SCHEMA.md` + README + USER_GUIDE + CODEBASE envelope; historical attributions intact; book example JSONs deferred to `.15b.3` per the `.12b.2a`/`.12b.3` precedent) + the repo-owned `tool_matrix --mux-if-gate` (`ScenarioSet::MuxIfSweep` + a Mux-biased focus config × the three strategies + `ModuleReport.emitted_mux_if` [`__cv` detection] + `saw_mux_if_emit` + `MatrixReport.mux_if_gate` + the gap arm + units floor + 5 proofs + fixtures), banked clean. Templated on `--task-emit-gate`. Then `.15b.3` (user docs). Other `active` lanes at no-frontier boundaries.
- next_action: **PNT → `STRUCTURED-EMISSION-EXPANSION.15b.2`** (the metric `num_emitted_mux_if_blocks` @ schema `1.14 → 1.15` + the `tool_matrix --mux-if-gate` gate + `saw_mux_if_emit`, templated on `--task-emit-gate` / `--cone-function-gate`; Mux-biased focus config — raise `comb_mux_prob`, comb-only). Code change — owning leaf `.15b.2` already pre-split. Full `cargo` gates + `cargo test --bin tool_matrix` + a banked clean `--mux-if-gate` report (`coverage_gaps=[]`, `saw_mux_if_emit=true`, `N/0` Verilator + both Yosys + Icarus). Monitor RAM on heavy builds (`scripts/ram_guard.sh --threshold 90 -- <cmd>`).
- handoff: repo handoff-ready after this commit — `scripts/check_doctrines.sh` green (4 doctrines); `git diff --check` clean; `cargo check/clippy/fmt/test --lib` + snapshots 6/6 green; default-off ⇒ DUT byte-identical. `src/ir/mux_if_emit.rs` (new) + 6 `src/` edits + `CODEBASE_ANALYSIS.md` + `DEVELOPMENT_NOTES.md` + tree file + `docs/TASK_TREE.md` + `ROADMAP.md` + `CHANGES.md` + `MEMORY.md` co-staged. (KM unchanged — `0027` already carries `answers:`; README/USER_GUIDE/book deferred to `.15b.3`.)
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); one runner + one classifier not two (`feedback_full_factorization`); every capability opt-in + MCP-invocable + queryable + CLI-as-shim (decision `0017`); design the API for agents not humans (`feedback_api_for_agents_not_humans`); the book is the user-facing surface and must not drift (`feedback_book_doctrine`); decision/KM fact per durable capability or boundary. Doctrine-enforcement (decision `0026`): every doctrine = a deterministic check in `scripts/check_doctrines.sh`; code-scoped checks are scope-aware (non-code commits exempt).
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
