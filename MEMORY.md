# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `SEMANTIC-INTROSPECTION-EXPANSION.9b.1` **pure node_drivers core** commit — a **code change** (`src/introspect/analyze.rs` + `CODEBASE_ANALYSIS.md` + task-tree/live-doc logs), task-tree-owned by `.9b.1`; **DUT byte-identical** (`tests/snapshots.rs` untouched; no IR/generator change, not wired to any emit path). Implements the pure core of `.9`'s eighth derived `analyze` query, `node_drivers` (per-node immediate 1-hop driver adjacency + each node's `GateOp`): `QUERY_NODE_DRIVERS` + `NodeDrivers { node, kind, op: Option<String>, width, drivers: Vec<NodeRef> }` + `NodeRef { node, kind, name }` + the EIGHTH `DerivedAnalysis.node_drivers` `skip_serializing_if` vec (the seven prior documents byte-identical) + `module_node_drivers`/`design_node_drivers` + the `node_drivers_with` driver (a single one-hop pass over `m.nodes`, no transitive walk/DFS) + the `node_kind_str`/`gate_op_str`/`node_ref_of` helpers (GateOp → stable string; operand handles resolved; operand order preserved; `"node:<id>"` addressing — leaf ⇒ known-but-empty, out-of-range ⇒ `-32602` at the MCP layer). The 14 existing `DerivedAnalysis` literals gained `node_drivers: Vec::new()` (precise awk insert) + the `crate::ir::GateOp` import. `supported_query_kinds()` **unchanged** (node_drivers joins it with the `run_analyze` dispatch in `.9b.2`, the `.3b.1`/…/`.8b.1` precedent). 6 new in-crate proofs. Validation: `cargo test --lib introspect::analyze` 53/0; `cargo test --lib` 669/0/2; snapshots 6/6 byte-identical; clippy `-D warnings` + fmt clean. Prior: `9d2d046` (`.9a`). Push cadence: `origin/main` at `4564533` → **20 commits ahead** after this ⇒ no push (threshold 30, `feedback_push_cadence`).
- active_work_unit: **`SEMANTIC-INTROSPECTION-EXPANSION` — `.9` `node_drivers` (the EIGHTH derived query): `.9a` design + `.9b.1` pure core done; frontier `.9b.2` (surface).** Seven queries already delivered end-to-end (schema `1.20`): `output_support`/`input_reach`/`flop_reset_provenance`/`module_reachability` (decision `0011`'s four) + `flop_dependencies` (`.6`) + `memory_provenance` (`.7`) + `fsm_provenance` (`.8`). `node_drivers` (`.9`) is the fourth beyond the four named kinds — the node-level immediate-fanin adjacency + `GateOp`, complementing the transitive cone; the pure core is live + lib-tested but not yet surface-reachable. Nothing retired; `node_readers` (the dual), per-FSM/per-memory reach, per-child-module provenance remain open-ended breadth.
- next_action: **continue the PNT loop on `.9b.2`** — wire `node_drivers` to the surface (the `.9b.2` leaf is already task-tree-owned): add `node_drivers` to `analyze::supported_query_kinds()` AND branch `run_analyze` by query kind (`module`/`design_node_drivers`) **in the same commit** + the vec-aware `-32602` guard checking `analysis.node_drivers`; bump `SCHEMA_VERSION 1.20 → 1.21` (+ the `"1.20"` test-assertion updates); add `node_drivers` to the `analyze_schema` enum + the `"node:<id>"` target description + tool/instructions text; schema-doc §6.7 + a `1.20 → 1.21` changelog + the row; book(`agent-mcp` row + worked example + the JSON envelopes `1.20 → 1.21`) + `api-tools`; USER_GUIDE + README; a new KM card `semantic-introspection-node-drivers` (+ cross-link from `semantic-introspection-analyze-tool`; regenerate `KNOWLEDGE_MAP.md`); an `anvil-mcp` stdio e2e smoke (a seed → schema 1.21, a gate node with real operands; unknown `node:999999` → `-32602`). Default-off / DUT byte-identical. Monitor RAM (`scripts/ram_guard.sh --threshold 90 -- <cmd>`).
- handoff: **repo handoff-ready after this commit** — `scripts/check_doctrines.sh` green (4 doctrines); `git diff --check` clean; DUT byte-identical (snapshots 6/6; lib 669/0/2); `.9` at `.9b.1` done, `.9b.2` task-tree-owned + the frontier. Note: a pre-existing prior-session gap — the `.8a`/`.8b.1`/`.8b.2` rows are missing from this tree's Verification Log + Commit Log (the Changelog has only `.8b.2`); a dedicated docs-hygiene backfill can close it. origin/main at `4564533`; 20 commits ahead after this. Co-staged: `src/introspect/analyze.rs` + `CODEBASE_ANALYSIS.md` + `docs/tasks/SEMANTIC-INTROSPECTION-EXPANSION.md` + `docs/TASK_TREE.md` + `CHANGES.md` + `MEMORY.md`.
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); one runner + one classifier not two (`feedback_full_factorization`); every capability opt-in + MCP-invocable + queryable + CLI-as-shim (decision `0017`); design the API for agents not humans (`feedback_api_for_agents_not_humans`); SEMANTIC-INTROSPECTION stays SCHEMA-DERIVED / no-shadow-simulator (decisions `0004`/`0011`); the book is the user-facing surface and must not drift (`feedback_book_doctrine`); decision/KM fact per durable capability or boundary. Doctrine-enforcement (decision `0026`): every doctrine = a deterministic check in `scripts/check_doctrines.sh`; code-scoped checks are scope-aware (non-code commits exempt).
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
