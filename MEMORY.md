# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `BUG-HUNT-ORCHESTRATION.2b.1` commit (**code: new default-off library module**) — `src/hunt/mod.rs` + `pub mod hunt`: the thin-orchestrator engine `hunt::run(&HuntRequest) -> HuntReport` composing `downstream::validate`/`minimize` over a deterministic seed sweep with reject/warning detection; SCHEMA-DERIVED report; 5 cargo-portable proofs. DUT byte-identical (snapshots 6/6; wired into no generate/emit path). Prior: `8a4fbab` = `.2a` (diff-sim extract); `b8c1e8b` = `.1` (decision `0018`, docs-only); `59f248c` = `USABILITY-LANE-OWNERSHIP.1` (decision `0017`); … `b90bdff` = `.5` (**pushed** — `origin/main` at `b90bdff`). Working tree clean. Push cadence: `origin/main` at `b90bdff` → **18 commits ahead**; push at ~30 (`feedback_push_cadence`).
- active_work_unit: **`BUG-HUNT-ORCHESTRATION`** — `.1` design ADR (decision `0018`) **done**; `.2a` (diff-sim extract) **done**; `.2b` pre-split → `.2b.1` (loop core + types, reject/warning detection) **done** + `.2b.2` (frontier). Frontier **`.2b.2`** = fold cross-sim mismatch detection (`diff_sim::run_agreement`, from `.2a`) into `hunt::run` as the `"cross_sim_mismatch"` detection axis + add the **reproducer-bundle emitter** (write a directory `<bundle_root>/<run_id>/` with `repro.sv`/`knobs.json`/`introspection.json`/`manifest.json`(non-DUT)/`tool-logs/`/`hunt-verdict.json`/`repro.sh`); `HuntFailure` gains the bundle ref. Then `.2c` (MCP `hunt` tool) → `.2d` (`anvil hunt` CLI, ANVIL's first subcommand + byte-identical guard) → `.2e` (real-tool gate + book/USER_GUIDE/README/KM). Design invariants: thin orchestrator (no new detector/minimizer), bundle = directory, sandbox caller-set (decision `0004`), every report field SCHEMA-DERIVED (decision `0017`). Six other lanes still `active` design-first (`ACCEPTANCE-DIVERGENCE-HUNTING`, `DOWNSTREAM-ADAPTER-EXPANSION`, `KNOB-ERGONOMICS-AND-PRESETS`, `CI-PACKAGING-DISTRIBUTION`, `COVERAGE-STEERED-GENERATION`, `CAPABILITY-BREADTH-EXPANSION`); plus no-frontier `STRUCTURED-EMISSION-EXPANSION` (5 surfaces; 6th = future `.11`), `SEMANTIC-INTROSPECTION-EXPANSION` + `AGENT-MCP-EXPANSION`, `IDENTITY-DEEPENING`, `SIGNOFF-AUTOMATION-EXPANSION`, `LOCAL-REFERENCE-CACHE`. Index: `docs/TASK_TREE.md`.
- next_action: **PNT — `BUG-HUNT-ORCHESTRATION.2b.2`** (fold `diff_sim::run_agreement` cross-sim detection into `hunt::run` + add the reproducer-bundle directory emitter; `HuntFailure` carries the bundle ref + `detection="cross_sim_mismatch"`). Touches `src/` ⇒ full `COMMIT.md` cargo gate + decision-`0017` gate. The bundle needs the regenerated DUT SV (`Generator`+`emit::to_sv`, same as `validate` internally) for `repro.sv` + the diff-sim call. Deferred 6th structured surface (multi-output `task`) = a future `STRUCTURED-EMISSION-EXPANSION.11` decision.
- handoff: repo handoff-ready — `.2b.1` all gates green: `cargo check/clippy/fmt` clean, `cargo test` green (lib **510** = 505+5 hunt proofs / tool_matrix 71+1 ignored / book_examples 3/3 / snapshots **6/6 byte-identical**; introspection schema `1.11`). `check_memory_architecture` + KM gen/check green (KM 46 facts). Working tree clean.
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); every capability opt-in; the hunt adds **no** new generator path / no detector / no minimizer (it composes the existing surfaces); decision/KM fact per durable capability or boundary.
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
