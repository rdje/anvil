# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `SEMANTIC-INTROSPECTION-EXPANSION.8b.1` **pure fsm_provenance core** commit — a **code change** (`src/introspect/analyze.rs` only), task-tree-owned by `.8b.1`; **DUT byte-identical** (`tests/snapshots.rs` untouched; no IR/generator change; not wired to any emit path). Lands the pure core of the **seventh** derived `analyze` query, `fsm_provenance`: `QUERY_FSM_PROVENANCE` + `FsmProvenance { fsm, num_states, encoding ("binary"/"one_hot"/"gray"), state_width (= FsmEncoding::state_width), sel_width, out_width, is_mealy (= Fsm::is_mealy()), sel_support (a reused SupportCone, target "fsm:<id>.sel") }` + the SEVENTH `DerivedAnalysis.fsm_provenance` `skip_serializing_if` vec + `module_fsm_provenance`/`design_fsm_provenance` + the `fsm_provenance_with` driver (`build_cone` over `Fsm::sel`; ascending fsm id; `"fsm:<id>"` target). The 12 existing `DerivedAnalysis` literals gained `fsm_provenance: Vec::new()` (+ the `crate::ir::{Fsm, FsmEncoding}` import); the opaque-`FsmOut`-leaf module doc note updated. `supported_query_kinds()` unchanged (joins with the `run_analyze` dispatch in `.8b.2`). 8 new in-crate proofs. Validation: `cargo test --lib introspect::analyze` 47/0; `cargo test --lib` 661/0/2; snapshots 6/6; clippy `-D warnings` + fmt + `cargo check --lib` clean. Prior: `cabe696` (`.8a`). Push cadence: `origin/main` at `4564533` → **17 commits ahead** after this ⇒ no push (threshold 30, `feedback_push_cadence`).
- active_work_unit: **`SEMANTIC-INTROSPECTION-EXPANSION` — SIX derived queries delivered end-to-end (schema `1.19`); the SEVENTH (`fsm_provenance` `.8`) is in progress, frontier `.8b.2`.** Delivered: `output_support`/`input_reach`/`flop_reset_provenance`/`module_reachability` (decision `0011`'s four) + `flop_dependencies` (`.6`) + `memory_provenance` (`.7`). `.8a` design + `.8b.1` pure core landed; next is `.8b.2` (surface: schema `1.19 → 1.20`). Nothing retired; further kinds (a per-FSM reach; per-child-module provenance) remain open-ended breadth.
- next_action: **PNT — implement `SEMANTIC-INTROSPECTION-EXPANSION.8b.2`** (the `fsm_provenance` surface): add `"fsm_provenance"` to `analyze::supported_query_kinds()` **and** branch `run_analyze` (`src/mcp/mod.rs`) by query kind (`module`/`design_fsm_provenance`) in the same commit; update the empty-result → `-32602` guard to check `analysis.fsm_provenance`; bump `SCHEMA_VERSION 1.19 → 1.20` (`src/introspect/mod.rs`) + the `"1.19"` test-assertion bumps + 2 new MCP proofs (`analyze_returns_fsm_provenance_and_caches_it` + `analyze_fsm_provenance_unknown_target_is_invalid_params`); add the kind to the `analyze_schema` `query` enum + the `target` description (the `"fsm:<id>"` form) + the tool/instructions text; schema-doc §6.7 + a `1.19 → 1.20` changelog + the row; book `agent-mcp` (analyze row + worked example + the JSON envelopes `1.19 → 1.20`) + `api-tools`; USER_GUIDE + README; a KM card (`KNOWLEDGE_MAP` 72 → 73 facts); an e2e `anvil-mcp` stdio smoke (`fsm_prob 1.0` → schema `1.20`, an FSM with a real `sel` cone; unknown `fsm:99999` → `-32602`). Doctrine: task-tree already owns `.8b.2`. Monitor RAM (`scripts/ram_guard.sh --threshold 90 -- <cmd>`).
- handoff: **repo handoff-ready after this commit** — `scripts/check_doctrines.sh` green (4 doctrines); `git diff --check` clean; DUT byte-identical (snapshots 6/6; lib 661/0/2); `.8b.1` pure core delivered (tree-owned `.8b.2` is the frontier). origin/main at `4564533`; 17 commits ahead after this. Co-staged: `src/introspect/analyze.rs` + `CODEBASE_ANALYSIS.md` + `docs/tasks/SEMANTIC-INTROSPECTION-EXPANSION.md` + `docs/TASK_TREE.md` + CHANGES + MEMORY.
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); one runner + one classifier not two (`feedback_full_factorization`); every capability opt-in + MCP-invocable + queryable + CLI-as-shim (decision `0017`); design the API for agents not humans (`feedback_api_for_agents_not_humans`); SEMANTIC-INTROSPECTION stays SCHEMA-DERIVED / no-shadow-simulator (decisions `0004`/`0011`); the book is the user-facing surface and must not drift (`feedback_book_doctrine`); decision/KM fact per durable capability or boundary. Doctrine-enforcement (decision `0026`): every doctrine = a deterministic check in `scripts/check_doctrines.sh`; code-scoped checks are scope-aware (non-code commits exempt).
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
