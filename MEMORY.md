# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `ACCEPTANCE-DIVERGENCE-HUNTING.2e` commit (**the tool-version-vs-version axis** — `src/downstream/mod.rs` + `src/divergence/mod.rs` + `src/mcp/mod.rs` + the tree + `docs/TASK_TREE.md` + DEVELOPMENT_NOTES + CODEBASE_ANALYSIS + CHANGES + MEMORY). The version axis of decision `0019` as a **library surface** composing the *one* hardened orchestration (no forked invocation set): (1) `downstream`: `ToolInvocation.version: Option<String>` (serde-absent on default paths ⇒ banked reports + `--resume` byte-identical); `tool_version(binary)` (best-effort `--version`, axis-only); `ToolSpec {kind allow-listed, binary caller-supplied, label}`; extracted shared `DutSandbox`/`prepare_dut_sandbox` (`validate` refactored onto it, byte-identical); `validate_tool_specs` (reuses `prepare_dut_sandbox` + the vetted `run_*` + `MemGuard`; one relabeled+version-stamped invocation per spec; single Yosys mode for the axis). (2) `divergence`: `DivergenceOptions.tool_specs` (empty ⇒ same-version path; non-empty ⇒ version axis), `ToolDecision.version`, `classify_version_mismatch` (a `version_mismatch` over the SAME `tool_verdict` — distinct relation, not a second classifier), `assemble_report` factor, `run` dispatch. (3) `mcp`: the `DivergenceOptions` literal gets `tool_specs: vec![]` (axis library-only at `.2e`; the caller-supplied-binary MCP/CLI surface is a distinct trust boundary, decided at `.2f`). +8 proofs. `cargo test --lib` 543/0 (535→543); snapshots 6/6 byte-identical; `cargo test --bin tool_matrix` 75/0; `cargo test --bin anvil` 12/0; clippy/fmt clean (under `ram_guard --threshold 90`). Real-tool `validate`-refactor regression (`anvil hunt --seed 1 --seeds 4 --tools verilator,yosys --divergence`): `n_clean=4 n_failures=0`. No introspection schema bump (stays `1.11`). Default-off / DUT byte-identical. Prior: `4d17fc2` = `.2d`; `2fa2cc3` = `.2c.2`; `7142fd7` = `.2c.1` (**== origin/main**, pushed). Working tree clean after this commit. Push cadence: `origin/main` at `7142fd7` → **3 commits ahead** after this (`.2c.2`, `.2d`, `.2e`); push at ~30 (`feedback_push_cadence`).
- active_work_unit: **`ACCEPTANCE-DIVERGENCE-HUNTING`** — `.1` ADR (`0019`) + `.2a` (`tool_verdict`) + `.2b` (`src/divergence/` core) + `.2c.1` (hunt fold) + `.2c.2` (`tool_matrix` column) + `.2d` (MCP tool + CLI shim) + `.2e` (the tool-version-vs-version axis) all **done**; `.2c` closed; frontier **`.2f`** (the real-tool e2e gate + book/USER_GUIDE/README/KM closeout + the version-axis surfacing decision — closes the tree). Other owner-directed lanes still design-first `.1`: `DOWNSTREAM-ADAPTER-EXPANSION`, `KNOB-ERGONOMICS-AND-PRESETS`, `CI-PACKAGING-DISTRIBUTION`, `COVERAGE-STEERED-GENERATION`, `CAPABILITY-BREADTH-EXPANSION`. Plus no-frontier `STRUCTURED-EMISSION-EXPANSION`, `SEMANTIC-INTROSPECTION-EXPANSION`+`AGENT-MCP-EXPANSION`, `IDENTITY-DEEPENING`, `SIGNOFF-AUTOMATION-EXPANSION`, `LOCAL-REFERENCE-CACHE`. Index: `docs/TASK_TREE.md`.
- next_action: **PNT — implement `ACCEPTANCE-DIVERGENCE-HUNTING.2f`** (closes the tree): a `#[ignore]` tool-gated `tests/divergence_e2e.rs` proving an all-agree real-tool run records `diverged=false` + the report is queryable (and a synthetic-injected accept/reject pair classifies `accept_reject`); decide + (if approved) wire the `.2e` `tool_specs` version axis over MCP/CLI given the caller-supplied-binary trust boundary, else record it as future breadth; the book (`synthesizability.md` + `agent-mcp.md`) + USER_GUIDE + README closeout; a KM how-to card (`acceptance-divergence` query + the version axis); mark ROADMAP lane 2 delivered; close `.2` + the root. `.2f` is task-tree-owned. Then PNT the next active lane (`DOWNSTREAM-ADAPTER-EXPANSION.1` design ADR per table order).
- handoff: repo handoff-ready — code state green (`cargo test --lib` 543/0, snapshots 6/6, `tool_matrix` 75/0, `anvil` 12/0; introspection schema `1.11` unchanged); `check_memory_architecture` + KM gen/check green (**KM 48 facts**, no new card for `.2e` — the KM how-to card lands at `.2f` closeout). Working tree clean after commit.
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); one classifier not two (`feedback_full_factorization`); every capability opt-in + MCP-invocable + queryable + CLI-as-shim (decision `0017`); the divergence detector adds **no** generator path / no oracle (it composes the existing surfaces); decision/KM fact per durable capability or boundary.
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
