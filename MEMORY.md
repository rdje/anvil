# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `STRUCTURED-EMISSION-EXPANSION.13a` **wider (k>2) multi-output task groups impl design-detail** commit — docs-only / no source / **DUT byte-identical**. First *deepening* of the sixth structured surface (the recorded `.13` follow-up of decision `0025`): a `DEVELOPMENT_NOTES.md` design-detail entry confirming the **emitter is already k-agnostic** (so the only `.13b` source change is `src/ir/multi_output_task_emit.rs::annotate_multi_output_task_groups`) + pinning the greedy group-extension (connected co-support + generalized mutual fan-in independence + `const MAX_MULTI_OUTPUT_TASK_GROUP_MEMBERS=8`), the **no-new-knob/metric/schema** decision, and the byte-identical/RNG argument; tree (`.13`/`.13a` done/`.13b`/`.13c`) + `docs/TASK_TREE.md` row + CHANGES updated. Prior: `2e2eb72`=`DOCTRINE-ENFORCEMENT-ADOPTION.6`. Push cadence: `origin/main` at `605ec44` → **18 commits ahead** after this ⇒ no push (threshold 30, `feedback_push_cadence`).
- active_work_unit: **`STRUCTURED-EMISSION-EXPANSION` — frontier `.13b`** (the live impl of the k>2 widening). `.13a` design-detail done. `.13b` = widen `annotate_multi_output_task_groups` to greedily collect all eligible partners (connected co-support + generalized fan-in independence) up to the const cap; new k>2 lib proofs + keep all pair proofs green; default-off byte-identical (snapshots 6/6); forced k≥3 sweep downstream-clean; re-bank `--multi-output-task-gate`. NO emitter/knob/metric/schema change. Then `.13c` (user docs).
- next_action: **Implement `STRUCTURED-EMISSION-EXPANSION.13b`** per the `.13a` design (`DEVELOPMENT_NOTES.md` top entry). Single source file: `src/ir/multi_output_task_emit.rs`. After `.13b` lands (commit per `COMMIT.md` with the leaf id), continue the PNT loop to `.13c` (docs), then re-scan `docs/TASK_TREE.md` for the next active lane. A latent out-of-scope follow-up remains: `book/src/agent-mcp.md` untagged ``` fences fail `mdbook test` under mdbook 0.5.2 (CI pins 0.4.40, green) — a `LIVE-DOC-BOOK-ALIGNMENT`-style fix if/when prioritized.
- handoff: repo handoff-ready after this commit — `scripts/check_doctrines.sh` green (4 doctrines, 6 bootstrap pointers; 64 facts / 589 keys); `mdbook build` clean; no `src/` touched ⇒ `cargo test` + snapshots unaffected (`cargo check --all-targets` clean at session start).
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); one runner + one classifier not two (`feedback_full_factorization`); every capability opt-in + MCP-invocable + queryable + CLI-as-shim (decision `0017`); design the API for agents not humans (`feedback_api_for_agents_not_humans`); the book is the user-facing surface and must not drift (`feedback_book_doctrine`); decision/KM fact per durable capability or boundary. Doctrine-enforcement (decision `0026`): every doctrine = a deterministic check in `scripts/check_doctrines.sh`; code-scoped checks are scope-aware (non-code commits exempt).
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
