# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `SEMANTIC-INTROSPECTION-EXPANSION.13a` **longest_path impl design-detail** commit — **docs/design-only** (no `src/`), task-tree-owned by `.13a`; **DUT byte-identical**. **Opens `.13` — the TWELFTH derived `analyze` query, `longest_path`**: for a target (output port / `"flop:<id>"`), one representative LONGEST combinational fan-in path — the ordered chain of interior `Gate` nodes (each with its `GateOp`) realizing `output_support`'s scalar `cone_depth`, terminating at a boundary leaf (`NodeRef`). The WITNESS for `cone_depth` + the TRANSITIVE COMPLEMENT to `node_drivers` (`.9`); structural gate-depth NOT timing (named `longest_path` not `critical_path`); provable `longest_path(t).depth == output_support(t).cone_depth`; single-endpoint (the exact `output_support` addressing ⇒ NO MCP signature change); real in both module + design variants; the eighth beyond decision `0011`'s four named kinds; schema `1.24 → 1.25` at impl. DEVELOPMENT_NOTES `.13a` entry resolves Q1–Q5 (TWELFTH `longest_path: Vec<LongestPath>` vec + `LongestPath { target, depth, path: Vec<PathStep>, leaf: Option<NodeRef> }` + minimal `PathStep { node, op, width }` reusing `NodeRef`; two pure passes — `node_depth` memo + greedy max-child-depth descent, ties → smallest operand id). Backfilled `.12a`/`.12b.1`/`.12b.2` hashes (`1933d2a`/`ef16fba`/`ea28272`). Validation: no `src/` ⇒ `cargo` unaffected; `check_doctrines.sh` green. Prior: `ea28272` (`.12b.2`). Push cadence: `origin/main` at `a767415`; **5 commits ahead** after this ⇒ no push (threshold 30).
- active_work_unit: `SEMANTIC-INTROSPECTION-EXPANSION.13` (`longest_path`) **open** — `.13a` design done; **active frontier → `.13b.1`** (pure core). Eleven prior derived analyze queries delivered @ schema `1.24`; this twelfth lands at `1.25`. Roadmap/codebase/mdBook aligned.
- next_action: **implement `.13b.1` — the pure `longest_path` core** in `src/introspect/analyze.rs`: `QUERY_LONGEST_PATH = "longest_path"` + `LongestPath`/`PathStep` structs (reuse `NodeRef` for the leaf) + the twelfth `longest_path: Vec<LongestPath>` `skip_serializing_if` vec on `DerivedAnalysis` + `module_longest_path`/`design_longest_path` (resolve_target → root; `node_depth` memo = the `visit` recurrence; greedy max-child-depth descent, ties → smallest operand id; leaf via `node_ref_of`; op via `gate_op_str`) + `longest_path: Vec::new()` fill-ins across the existing literals. NOT in `supported_query_kinds()` yet (joins with `run_analyze` dispatch in `.13b.2`). Lib proofs: the `depth == cone_depth` consistency proof, the deterministic tie-break proof, leaf-classification (input / flop-Q / instance-output / undriven-empty), ordered root→leaf, serialization-omits-the-other-eleven-vecs, determinism. `cargo test --lib` + snapshots 6/6 byte-identical; clippy `-D warnings` + fmt. Then `.13b.2` (surface: registry + dispatch + schema `1.24 → 1.25` + analyze_schema enum + schema-doc/book/USER_GUIDE/README/TOOLBOX/KM + e2e `anvil-mcp` smoke). Doctrine: SCHEMA-DERIVED/default-off/DUT byte-identical; monitor RAM (`scripts/ram_guard.sh --threshold 90 -- <cmd>`).
- handoff: **repo handoff-ready (active frontier `.13b.1`).** `scripts/check_doctrines.sh` green (4 doctrines); tree clean; DUT byte-identical. `.13a` design-detail landed; `src/` untouched. origin/main at `a767415`; **5 commits ahead** after this; threshold 30 ⇒ no push. A fresh session should implement `.13b.1` (see next_action).
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); one runner + one classifier not two (`feedback_full_factorization`); every capability opt-in + MCP-invocable + queryable + CLI-as-shim (decision `0017`); design the API for agents not humans (`feedback_api_for_agents_not_humans`); SEMANTIC-INTROSPECTION stays SCHEMA-DERIVED / no-shadow-simulator (decisions `0004`/`0011`); the book is the user-facing surface and must not drift (`feedback_book_doctrine`); decision/KM fact per durable capability or boundary. Doctrine-enforcement (decision `0026`): every doctrine = a deterministic check in `scripts/check_doctrines.sh`; code-scoped checks are scope-aware (non-code commits exempt).
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
