# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `STRUCTURED-EMISSION-EXPANSION.17b.3` **eighth-surface user docs** commit — **DOCS-ONLY** (no `src/`/`tests/`/`examples/`/build touched) ⇒ **DUT byte-identical** + exempt from the code-scoped doctrine checks. **`.17b`/`.17` close ⇒ the EIGHTH structured surface (the `CaseMux` `if`/`else if` priority chain, decision `0028`) is delivered end-to-end.** `book/src/structured-emission.md` eighth-surface section (byte-verified seed-1 before/after, no `__cv`) + chapter-intro list + `case_mux_if_emit_prob` entries in `book/knobs.md`/USER_GUIDE/README + the deferred book example-JSON `schema_version` `1.15 → 1.16` (api-tools ×3 + agent-mcp ×1) + the stale seed-42 `run_id` `3f1cad… → ee39c1e3df8192dd` (FNV over `lane+seed+knobs_json`, which gained the field at `.17b.1`; verified live: seed 42 → `ee39c1e3df8192dd`/`1.16`/80383 bytes) + a `docs/knowledge/case-mux-if-emit.md` KM card (`KNOWLEDGE_MAP.md` regenerated). `mdbook build` clean; `cargo test --test book_examples` **3/3**; `check_knowledge_map` OK; card reverify CLEAN. Prior: this `STRUCTURED-EMISSION-EXPANSION.17b.2b` commit. Push cadence: `origin/main` at `4564533` → **3 commits ahead** after this ⇒ no push (threshold 30, `feedback_push_cadence`).
- active_work_unit: **`STRUCTURED-EMISSION-EXPANSION` — at a no-frontier boundary** (EIGHT surfaces delivered end-to-end; `.16`+`.17a`+`.17b.1`+`.17b.2a`+`.17b.2b`+`.17b.3` all done; `.17b`/`.17` closed). Lane stays `active`; the next surface (`CasezMux` masked chain / nested-or-multi-level `generate` / `interface`-`modport` [disqualified since `.7`]) is PNT-selected at the next no-frontier boundary per `feedback_pick_and_roll_at_no_frontier`, each its own design+decision leaf. ALL `active` lanes are at no-frontier boundaries.
- next_action: **PNT — self-select the next lane at the no-frontier boundary** (`feedback_pick_and_roll_at_no_frontier` — don't surface a steer question). Two tracked options: (a) **pick the NINTH structured surface** in `STRUCTURED-EMISSION-EXPANSION` — most-grounded candidate = the `CasezMux` masked priority chain (`if ((sel & mask) == pattern) …`, the recorded `.18+` follow-up; needs a fresh empirical probe + a decision before any source), the only remaining-open lane with a clear next step; OR (b) **a dedicated docs pass** to fix the observed pre-existing drift: `book/src/agent-mcp.md`'s `analyze`-tool example JSONs are frozen at `schema_version 1.11` (stale across the `.12`/`.13`/`.15`/`.17` bumps; README's analyze schema is already `1.16`) — verify the example payload shapes against live `analyze` output before bumping. Recommend (a) (capability breadth, the north star) unless the drift in (b) is judged higher-priority. Monitor RAM on heavy builds (`scripts/ram_guard.sh --threshold 90 -- <cmd>`).
- handoff: repo handoff-ready after this commit — `scripts/check_doctrines.sh` green (4 doctrines); `git diff --check` clean; DOCS-ONLY ⇒ DUT byte-identical; the eighth surface is delivered end-to-end + fully validated (`mdbook build` clean / `book_examples` 3/3 / `check_knowledge_map` OK / banked gate `/tmp/anvil-case-mux-if-gate-r1` clean from `.17b.2b`). origin/main at `4564533` (the `.17b.1` push); 3 commits ahead after this (`.17b.2a`+`.17b.2b`+`.17b.3`). Co-staged: book ×4 + README + USER_GUIDE + ROADMAP + `docs/knowledge/case-mux-if-emit.md` + `KNOWLEDGE_MAP.md` + `docs/tasks/STRUCTURED-EMISSION-EXPANSION.md` + `docs/TASK_TREE.md` + CHANGES + MEMORY.
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); one runner + one classifier not two (`feedback_full_factorization`); every capability opt-in + MCP-invocable + queryable + CLI-as-shim (decision `0017`); design the API for agents not humans (`feedback_api_for_agents_not_humans`); the book is the user-facing surface and must not drift (`feedback_book_doctrine`); decision/KM fact per durable capability or boundary. Doctrine-enforcement (decision `0026`): every doctrine = a deterministic check in `scripts/check_doctrines.sh`; code-scoped checks are scope-aware (non-code commits exempt).
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
