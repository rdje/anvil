# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `ACCEPTANCE-DIVERGENCE-HUNTING.2b` commit (**the `src/divergence/` library core** — new `src/divergence/mod.rs` + `pub mod divergence` in `lib.rs` + `CODEBASE_ANALYSIS` module-map entry + the tree + `docs/TASK_TREE.md` + DEVELOPMENT_NOTES + ROADMAP + CHANGES + MEMORY) — `DivergenceOptions{validate:ValidateOptions}` (the `MinimizeOptions` wrap) + `ToolDecision{tool,verdict:ToolVerdict,exit_code,first_message}` + `Divergence{kind,tools}` + `DivergenceReport` + `divergence::run(seed,cfg,&DivergenceOptions)->DivergenceReport`. `run` **reuses** the one `downstream::validate` orchestration (runs every tool/mode to completion, no short-circuit) and projects `report.tools` via the shared `downstream::tool_verdict` (no forked loop / no second classifier), then `classify_divergences` ⇒ one `Divergence` per present pair-class (`accept_reject`/`accept_warn`/`warn_reject`, deterministic; Yosys `both` ⇒ two labels so without-abc-vs-with-abc itself diverges). 7 cargo-portable proofs. `cargo test --lib` 529/0 (divergence:: 7/7); snapshots 6/6 byte-identical; clippy/fmt clean. Default-off / DUT byte-identical. Prior: `dd6c9eb` = `.2a` (shared `tool_verdict`); `e7a3de8` = `.1` design ADR (decision `0019`); `d2ddbbf` = `BUG-HUNT-ORCHESTRATION.2e` (closed); … `b90bdff` (**pushed** — `origin/main` at `b90bdff`). Working tree clean after this commit. Push cadence: `origin/main` at `b90bdff` → **27 commits ahead** after this; push at ~30 (`feedback_push_cadence`) — push after the next ~3.
- active_work_unit: **`ACCEPTANCE-DIVERGENCE-HUNTING`** — `.1` ADR (decision `0019`) + `.2a` (shared `tool_verdict`) + `.2b` (`src/divergence/` core) all **done**; frontier **`.2c`** (fold the detector into `hunt::run` as `HuntRequest.divergence` → an `acceptance_divergence` finding + `HuntFailure.divergence`, AND add the `tool_matrix` column + the opportunistic `saw_acceptance_divergence` fact). Other owner-directed lanes still design-first `.1`: `DOWNSTREAM-ADAPTER-EXPANSION`, `KNOB-ERGONOMICS-AND-PRESETS`, `CI-PACKAGING-DISTRIBUTION`, `COVERAGE-STEERED-GENERATION`, `CAPABILITY-BREADTH-EXPANSION`. Plus no-frontier `STRUCTURED-EMISSION-EXPANSION`, `SEMANTIC-INTROSPECTION-EXPANSION`+`AGENT-MCP-EXPANSION`, `IDENTITY-DEEPENING`, `SIGNOFF-AUTOMATION-EXPANSION`, `LOCAL-REFERENCE-CACHE`. Index: `docs/TASK_TREE.md`.
- next_action: **PNT — implement `ACCEPTANCE-DIVERGENCE-HUNTING.2c`** (fold + matrix column): (1) `HuntRequest.divergence: bool` → `hunt::run` calls `divergence::run` on each swept artifact; a `diverged==true` result is a finding (`detection="acceptance_divergence"`, `HuntFailure.divergence: Option<DivergenceReport>`, no minimize by default — the `validate` oracle can't reproduce a *disagreement*, like `cross_sim_mismatch`); (2) the `tool_matrix` column — a `--divergence` opt-in, `ModuleReport`/`DesignReport.divergence: Option<DivergenceReport>`, the **opportunistic** `saw_acceptance_divergence` fact (NEVER a required gate — all-agree is the valid-by-construction steady state), reuse `classify_diff_sim_axis` for the subset. Cargo-portable proofs; snapshots 6/6 + book_examples unchanged. `.2c` is task-tree-owned (registered in `e7a3de8`). Watch `tool_matrix` build weight under ram_guard.
- handoff: repo handoff-ready — code state green (`cargo test --lib` 529/0, introspection schema `1.11`); `check_memory_architecture` + KM gen/check green (**KM 48 facts**, no new card for `.2b`). Working tree clean after commit.
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); one classifier not two (`feedback_full_factorization`); every capability opt-in + MCP-invocable + queryable + CLI-as-shim (decision `0017`); the divergence detector adds **no** generator path / no oracle (it composes the existing surfaces); decision/KM fact per durable capability or boundary.
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
