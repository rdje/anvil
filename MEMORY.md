# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `LIVE-DOC-TASK-TREE-INDEX-ALIGNMENT.1` **align task-tree index status** commit — **DOCS-ONLY / live-doc** (no `src/` touched) ⇒ **DUT byte-identical**. A session-bootstrap audit cross-checked every `docs/tasks/*.md` `- Status:` against its `docs/TASK_TREE.md` index row and found exactly one drift: the `LIVE-DOC-ROADMAP-LANE-STATUS-ALIGNMENT` index row read `active` while its tree file was already `done` (closed by `d3eb968`). Fix: corrected that row `active`→`done` with a closure frontier cell + added the new owning tree's own `done` row + new `docs/tasks/LIVE-DOC-TASK-TREE-INDEX-ALIGNMENT.md`. Restores full index ↔ tree-file alignment (no drift). `scripts/check_doctrines.sh` 4/4 PASS (code-scoped exempt — docs commit); `git diff --check` clean. Prior: `470699e`=`STRUCTURED-EMISSION-EXPANSION.15b.3`. Push cadence: `origin/main` at `605ec44` → **27 commits ahead** after this ⇒ no push (threshold 30, `feedback_push_cadence`).
- active_work_unit: **no open frontier** — `LIVE-DOC-TASK-TREE-INDEX-ALIGNMENT` closed by this commit. `STRUCTURED-EMISSION-EXPANSION` stays `active` at its no-frontier boundary (SEVEN structured surfaces delivered: function `.1`/`.2`, generate-loop `.3`/`.4`, task `.5`/`.6`, wider-lane part-select `.7`/`.8`, cone-function `.9`/`.10`, multi-output-task `.11`/`.12`/`.13`, procedural if/else `.14`/`.15`). All other `active` lanes also at no-frontier boundaries.
- next_action: **PNT at the no-frontier boundary** (`feedback_pick_and_roll_at_no_frontier`): self-select the next structured-emission surface — `.16` design+decision for one of the candidate `.16+` surfaces (N-way `CaseMux` → `if`/`else if` priority chain [the recorded `0027` follow-up, strongest next candidate — a genuinely-distinct priority-chain construct], nested/multi-level `generate` [no routine by-construction source], `interface`/`modport` [empirically disqualified since `.7`]) — OR pick another `active` lane. The chosen surface needs a fresh empirical tool-acceptance + sim-equiv probe (Verilator `-Wall` 2012/2017/2023 + both Yosys + Icarus + iverilog sim-equiv; all present locally except slang/sv2v) BEFORE authoring the decision, then a `.16` design leaf (no source). Monitor RAM on heavy builds (`scripts/ram_guard.sh --threshold 90 -- <cmd>`).
- handoff: repo handoff-ready after this commit — `scripts/check_doctrines.sh` green (4 doctrines); `git diff --check` clean; docs-only ⇒ DUT byte-identical; the prior `.15b.2`/`.15b.3` code+doc state holds (`cargo test --lib` 616 / `--bin tool_matrix` 89 / snapshots 6/6 / `--mux-if-gate` banked `/tmp/anvil-mux-if-gate-r1`; `mdbook build` + `check_knowledge_map` + `book_examples` 3/3 green). Co-staged: `docs/TASK_TREE.md` + `docs/tasks/LIVE-DOC-TASK-TREE-INDEX-ALIGNMENT.md` (new) + `CHANGES.md` + `MEMORY.md`.
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); one runner + one classifier not two (`feedback_full_factorization`); every capability opt-in + MCP-invocable + queryable + CLI-as-shim (decision `0017`); design the API for agents not humans (`feedback_api_for_agents_not_humans`); the book is the user-facing surface and must not drift (`feedback_book_doctrine`); decision/KM fact per durable capability or boundary. Doctrine-enforcement (decision `0026`): every doctrine = a deterministic check in `scripts/check_doctrines.sh`; code-scoped checks are scope-aware (non-code commits exempt).
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
