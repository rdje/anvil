# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `AGENT-MCP-EXPANSION.4b` commit (hash backfills next update; prior: `6101c15` = `.4a`; `bc70aee` = owner `.4` HTTP decision handoff; `54ccb25` = `.3b`; `edd716d` = `.2`; `6af0690` = `.1`). Working tree clean after this commit.
- active_work_unit: **`AGENT-MCP-EXPANSION`** (Lane 2, `active`). `.1`/`.2`/`.3`(`.3a`+`.3b`)/`.4`(`.4a` design + `.4b` impl) **done**. Frontier now **`.5`** (closeout, `pending`): sync `book/src/agent-mcp.md` + `USER_GUIDE.md` + `README.md` to the whole expanded MCP surface, then **close the tree**. Lane order **`2 → 3 → 1`**: `AGENT-MCP-EXPANSION` → `SIGNOFF-AUTOMATION-EXPANSION` (`proposed`) → `IDENTITY-DEEPENING` (`proposed`). Index: `docs/TASK_TREE.md`.
- next_action: do **`AGENT-MCP-EXPANSION.5`** (closeout, user-facing docs — exempt from task-tree code rules but still full `COMMIT.md`). Sync three user surfaces to everything landed since `AGENT-INTROSPECTION-MCP.7`: (1) the **`coverage_gaps`** pure MCP tool (`.2`); (2) the **non-DUT lanes** (`microdesign`/`frontend`) over MCP generate/introspect + the `anvil://artifact/<run_id>/manifest` resource (`.3b`); (3) the opt-in **`--http <addr>`** HTTP transport (`.4b`, loopback default, same `handle_line`, no new dep). Edit `book/src/agent-mcp.md` (tools table + resources + a transport subsection), `USER_GUIDE.md` "Agent introspection and the MCP server", and `README.md` "Current CLI truth" (the `anvil-mcp` bullet). Run `mdbook build book` + `cargo test --test book_examples` (3/3; mark any non-runnable HTTP/long-lived-server bash block with the `<!-- book-test: skip -->` sentinel). Then close the tree (`AGENT-MCP-EXPANSION` → `done` in `docs/TASK_TREE.md`) and PNT into Lane 3 `SIGNOFF-AUTOMATION-EXPANSION` (promote `proposed`→`active`, start `.1` design).
- lane invariants (0004/0005): MCP is a thin read-mostly adapter beside the deterministic core; schema derived (serde projection), invariant SCHEMA-DERIVED; `validate`/`minimize` run external tools **only via the hardened `downstream` invocations** (fixed allow-list, fixed binary names, sandboxed temp dir, no arbitrary shell/path), no second source of truth; AI agent never a signoff oracle; `minimize` searches the **input** `(seed, knobs)` space (never mutates/repairs RTL); default `--artifact dut` byte-identical; reuse `tool_matrix`/`downstream`/`diff_sim`/`metrics`/`mem_guard`. HTTP transport (`.4b`) drives the SAME `McpServer::handle_line`; loopback default; per-call guardrails unchanged; NO new Cargo dependency. Coverage-gap source = bin-private `CoverageSummary`/`compute_coverage_gaps` (`src/bin/tool_matrix.rs:286,6552`); recorded in `MatrixReport.coverage_gaps` (`:489`).
- in_flight_uncommitted: none after this commit. Repo is handoff-ready: tree clean, gates green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 88` for heavy builds; full `cargo test` not required per owner resource policy (monitor RAM; stop above 90%, >95% reboots). For code leaves run fmt/check/clippy + focused `mcp::`/`introspect::` tests + snapshot byte-identical guard. Push cadence: `origin/main` is at `381ec01` (`.5.3`); **10 commits ahead after this commit (`.6`,`.7`,`CAPABILITY-LANE-OWNERSHIP.1`,`AGENT-MCP-EXPANSION.1`,`.2`,`.3a`,`.3b`,`.4`-handoff,`.4a`,`.4b`) — under the ~30 threshold, no push yet** (`feedback_push_cadence`).

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
