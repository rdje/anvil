# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `ACCEPTANCE-DIVERGENCE-HUNTING.2d` commit (**the MCP `divergence` tool + CLI shim** — `src/mcp/mod.rs` + `src/main.rs` + `book/src/agent-mcp.md` + `CODEBASE_ANALYSIS` + the tree + `docs/TASK_TREE.md` + DEVELOPMENT_NOTES + ROADMAP + CHANGES + MEMORY). The third surface of the one-shared-detector design (decision `0019`/`0017`): (1) `mcp::run_divergence` — a single-`(seed,cfg)` controlled tool shimming `divergence::run` (same input shape as `validate`: `tools`+`yosys_mode`; inherits the allow-list/sandbox/RAM-guard/audit), returns the `DivergenceReport`, caches the divergent `run_id` so `anvil://artifact/<run_id>/{sv,introspection}` resolve, appends a `divergence` audit record; added to `tools_list`+dispatch. (2) The `hunt` tool gains a `divergence: bool` arg (flipped the `.2c.1` placeholder), so the hunt-axis sweeps over MCP. (3) The `anvil hunt --divergence` CLI flag (flipped the `.2c.1` placeholder in `build_hunt_request`). +4 MCP proofs + CLI parse proofs. `cargo test --lib mcp::tests` 55/0; full `cargo test --lib` 535/0 (531→535); `cargo test --bin anvil` 12/0; snapshots 6/6 byte-identical + book_examples 3/3; `mdbook build book` clean; clippy/fmt clean. Real-tool CLI sanity (`anvil hunt --seed 1 --seeds 4 --tools verilator,yosys --divergence`): `n_clean=4 n_failures=0` (steady state). No introspection schema bump (no new resource type). Default-off / DUT byte-identical. Prior: `2fa2cc3` = `.2c.2` (the `tool_matrix` column); `7142fd7` = `.2c.1` (**== origin/main**, pushed); `4028537` = `.2b`; `e7a3de8` = `.1` (decision `0019`). Working tree clean after this commit. Push cadence: `origin/main` at `7142fd7` → **2 commits ahead** after this; push at ~30 (`feedback_push_cadence`).
- active_work_unit: **`ACCEPTANCE-DIVERGENCE-HUNTING`** — `.1` ADR (`0019`) + `.2a` (`tool_verdict`) + `.2b` (`src/divergence/` core) + `.2c.1` (the `hunt::run` fold) + `.2c.2` (the `tool_matrix` column) + `.2d` (the MCP tool + CLI shim) all **done**; `.2c` closed; frontier **`.2e`** (the tool-version-vs-version axis). Other owner-directed lanes still design-first `.1`: `DOWNSTREAM-ADAPTER-EXPANSION`, `KNOB-ERGONOMICS-AND-PRESETS`, `CI-PACKAGING-DISTRIBUTION`, `COVERAGE-STEERED-GENERATION`, `CAPABILITY-BREADTH-EXPANSION`. Plus no-frontier `STRUCTURED-EMISSION-EXPANSION`, `SEMANTIC-INTROSPECTION-EXPANSION`+`AGENT-MCP-EXPANSION`, `IDENTITY-DEEPENING`, `SIGNOFF-AUTOMATION-EXPANSION`, `LOCAL-REFERENCE-CACHE`. Index: `docs/TASK_TREE.md`.
- next_action: **PNT — implement `ACCEPTANCE-DIVERGENCE-HUNTING.2e`** (the tool-version-vs-version axis): extend `DivergenceOptions` with `tool_specs` carrying `(AcceptanceTool kind, binary, label)` so two versions of one tool kind are run + compared (kind stays allow-listed via `AcceptanceTool::from_name`; the *binary* is a caller-supplied path/PATH shim); add a `ToolInvocation` observed-`version` capture (parsed from `--version`); add the `version_mismatch` divergence kind. Portability note: caller supplies binaries/labels; ANVIL never manages installs. Cargo-portable proofs (synthetic same-kind differing verdicts ⇒ `version_mismatch`); snapshots 6/6. `.2e` is task-tree-owned. Then `.2f` (real-tool e2e gate + book/USER_GUIDE/README/KM closeout — closes the tree).
- handoff: repo handoff-ready — code state green (`cargo test --lib` 535/0, `mcp::tests` 55/0, introspection schema `1.11` unchanged); `check_memory_architecture` + KM gen/check green (**KM 48 facts**, no new card for `.2d` — the KM how-to card lands at `.2f` closeout). Working tree clean after commit.
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); one classifier not two (`feedback_full_factorization`); every capability opt-in + MCP-invocable + queryable + CLI-as-shim (decision `0017`); the divergence detector adds **no** generator path / no oracle (it composes the existing surfaces); decision/KM fact per durable capability or boundary.
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
