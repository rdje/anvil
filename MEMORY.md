# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this **`EVIDENCE-BANK-DURABILITY.2`** (ADR) commit — **docs-only** ⇒ **DUT byte-identical**, `tests/snapshots.rs` untouched. Decision **`0030`** (`docs/decisions/0030-durable-closure-evidence-citations.md`): closure/gate claims cite a **committed per-bank digest** under `docs/evidence/` (owning leaf + commit hash + exact re-runnable command + counts + per-tool pass/fail + `coverage_gaps` + report SHA-256); every pre-0030 `/tmp/anvil-*` citation (77 raw strings / 73 canonical banks: 10 phase-closing, 15 surface/sweep gates, 47 smokes/probes, 1 illustrative) is a **labelled historical breadcrumb** re-verified only by oracle re-run. Rejected: command-only (subsumed into the digest), gitignored `.cache/evidence/` (fails §2 durability), committing corpora. Validation: `check_doctrines.sh` 4/4 PASS; KM regenerated (+8 keys). Prior: `ecda0e7` (`EVIDENCE-BANK-DURABILITY.1`). Push cadence: 1 ahead of `origin/main` after the prior session's directive-push; normal 30-commit threshold applies.
- active_work_unit: **owner re-steer (`2026-07-29`)**: PGEN reports the **mdBook has no microdesign/frontend lane chapters** (two of three artifact lanes invisible on the review surface — ANVIL's fix, not PGEN's). This unit takes the frontier NEXT: register a tree (check `USABILITY-LANE-OWNERSHIP` first for fit), add the two lane chapters + SUMMARY rows, `mdbook build` clean, commit. Consumer-reported context to record durably there: ANVIL's first contact found a real unknown gap in PGEN's parser within minutes (`case`/`endcase` absent from `grammars/rtl_frontend.ebnf`). After it: **`EVIDENCE-BANK-DURABILITY.3`** (mechanize 0030: digest schema + `evidence_digest.sh` + `check_evidence_citations.sh` + registry line + grandfathered list), then `.4` (breadcrumb sweep; NOTE: `/tmp/anvil-validate-3f1c…` in the book API reference is illustrative, not evidence — do not label it), `.5` deferred. Other trees: all at NO-FRONTIER boundaries except `CAPABILITY-BREADTH-EXPANSION.1` (`pending` ADR).
- next_action: open/extend the task tree for the mdBook lane-chapter gap, write `book/src/` chapters for the microdesign + frontend lanes (grounded in `src/` lane reality + USER_GUIDE), wire `SUMMARY.md`, `mdbook build book` clean, land via `COMMIT.md`; then resume `EVIDENCE-BANK-DURABILITY.3`. Doctrine: task-tree-own before any edit; book must not drift; docs-only ⇒ DUT byte-identical; monitor RAM for heavy runs (`scripts/ram_guard.sh --threshold 90 -- <cmd>`).
- handoff: **repo handoff-ready.** `scripts/check_doctrines.sh` green (4/4); tree clean; DUT byte-identical (docs-only commit). A fresh session resumes at the mdBook-lanes unit (see next_action), then `EVIDENCE-BANK-DURABILITY.3`.
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); one runner + one classifier not two (`feedback_full_factorization`); every capability opt-in + MCP-invocable + queryable + CLI-as-shim (decision `0017`); design the API for agents not humans (`feedback_api_for_agents_not_humans`); SEMANTIC-INTROSPECTION stays SCHEMA-DERIVED / no-shadow-simulator (decisions `0004`/`0011`); the book is the user-facing surface and must not drift (`feedback_book_doctrine`); decision/KM fact per durable capability or boundary. Doctrine-enforcement (decision `0026`): every doctrine = a deterministic check in `scripts/check_doctrines.sh`; code-scoped checks are scope-aware (non-code commits exempt).
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
