# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `COVERAGE-STEERED-GENERATION.1` commit — the **design ADR** (decision `0023`) for the coverage-steered-generation lane (owner idea 6; binds `0017`; bounded by `feedback_rules_first_generation`). **Docs-only — decision record + INDEX + tree + `docs/TASK_TREE.md` + `DEVELOPMENT_NOTES.md` + KM (57→58); no `src/`/CI change, DUT byte-identical.** Pinned: (1) steering = a deterministic per-category probability-prior **multiplier** on `prob` at the single `roll_knob` site (`effective_prob = clamp01(prob * weight)`, one `gen_bool` draw preserved) — rules-first (a prior, NOT a filter; no rejection path) + byte-stable per `(seed, knobs, steering-config)`, byte-identical when unset; (2) target = a `SteeringConfig` (per-`KnobId` / category emphasis weights); (3) readout = SCHEMA-DERIVED from `knob_roll_attempts`/`fires` + histograms (zero new truth, `0011`); (4) feedback = an **outer** measure→derive→re-steer loop; (5) decision-`0017` API. Pre-split `.2a`(core+proofs)/`.2b`(readout+MCP query)/`.2c`(outer loop+docs). Gate: mem-arch + KM gen/check green (**KM 58**). Prior: `5b63bf0`=`CI-PACKAGING…2c` (closed lane); `6df2ca8`=`…2b`. Push cadence: `origin/main` at `7142fd7` → **25 commits ahead** after this; **push at ~30** (`feedback_push_cadence`).
- active_work_unit: **`COVERAGE-STEERED-GENERATION` — `.1` (design ADR, decision `0023`) DONE; frontier = `.2a`** (the steering **core**: a `SteeringConfig` type + the `weight()` lookup + the `roll_knob` prior multiplier, with three proofs — byte-identical-when-unset [snapshots 6/6], measurable distribution-shift vs unsteered on a seed sweep, and no-filter [no rejection branch; draw count per roll unchanged]). `.2a` is the first **code** slice of this lane. Then `.2b` (SCHEMA-DERIVED readout + MCP coverage query) → `.2c` (outer measure→derive→re-steer helper + book/USER_GUIDE/KM; close `.2`). `CI-PACKAGING-DISTRIBUTION` stays `active` with no frontier.
- next_action: **PNT — continue.** Advance to `COVERAGE-STEERED-GENERATION.2a` (the steering core — **code**; task-tree-owned; the `roll_knob` prior multiplier + the three proofs). This is the first code slice in a while ⇒ run the full COMMIT.md gate (`cargo check --all-targets`, `cargo test` incl. snapshots, `cargo clippy -D warnings`, `cargo fmt --check`); watch RAM per `0003-resource-safe-validation` (`scripts/ram_guard.sh --threshold 90 -- <cmd>`). Alternatively pick-and-roll to `CAPABILITY-BREADTH-EXPANSION.1`/`.2` (design ADRs).
- handoff: repo fully handoff-ready & **in sync** after this commit — `cargo check`/`cargo test` unaffected (no Rust touched; full `cargo test` was green at `51d97d9`); snapshots 6/6; `check_memory_architecture` + KM gen/check green (**KM 58 facts**); mdbook clean; introspection schema `1.11`. No pending doc-drift.
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); one runner + one classifier not two (`feedback_full_factorization`); every capability opt-in + MCP-invocable + queryable + CLI-as-shim (decision `0017`); design the API for agents not humans (`feedback_api_for_agents_not_humans`); the book is the user-facing surface and must not drift (`feedback_book_doctrine`); decision/KM fact per durable capability or boundary.
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
