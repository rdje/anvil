# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `SEMANTIC-INTROSPECTION-EXPANSION.5a` commit (`module_reachability` impl design-detail; **DOCS-ONLY**, no source change; opens `.5` = the fourth derived query; hash backfills next update; prior: `a715070` = `.4b.2`; `3ee0417` = `.4b.1`). Working tree clean after this commit. Push cadence: `origin/main` at `63d2622` → **14 commits ahead after this commit; push at ~30** (`feedback_push_cadence`).
- active_work_unit: **`SEMANTIC-INTROSPECTION-EXPANSION` tree `active`; `.5` = `module_reachability` opened; frontier = `.5b.1`.** THREE queries already delivered end-to-end: `output_support` (`.1`/`.2`) + `input_reach` (`.3`) + `flop_reset_provenance` (`.4`), schema `1.6`. `.5a` design done; `.5b` pre-split → `.5b.1` (pure core, frontier) + `.5b.2` (surface). Index: `docs/TASK_TREE.md`.
- `.5a` delivered (docs-only, design of record in DEVELOPMENT_NOTES): `module_reachability` = which modules in a `Design` are reachable from `design.top` via the instance graph. Result shape = a FOURTH parallel `module_reachability: Vec<ModuleReachability>` vec on `DerivedAnalysis` (`#[serde(default, skip_serializing_if)]` ⇒ prior 3 documents byte-identical); `ModuleReachability { module, reachable, depth: Option<usize> (skip-if-None), instantiates (sorted distinct child names), instance_count }`. Derivation = BFS from `design.top` over `Module.instances[].module` edges of a name→Module index (min-depth, output sorted by name). Target = a **module NAME** (None ⇒ all; unknown ⇒ -32602) — distinct from prior port-name/`flop:<id>` targets. Module variant = degenerate one-node (bare module rooted at itself). Schema `1.6 → 1.7`.
- next_action: **PNT — implement `SEMANTIC-INTROSPECTION-EXPANSION.5b.1`** (the pure core in `src/introspect/analyze.rs`): `QUERY_MODULE_REACHABILITY` + `ModuleReachability` struct + the fourth `module_reachability` vec on `DerivedAnalysis` + `design_module_reachability`/`module_module_reachability` builders (BFS over instance edges); the **6** existing `DerivedAnalysis` literals each gain `module_reachability: Vec::new()`; do NOT add to `supported_query_kinds()` yet (registry+dispatch land together in `.5b.2`). Lib proofs: BFS reachability over a multi-level design + min depth + instantiates/instance_count + an unreachable module (no depth) + module-name target + None⇒all-sorted + unknown⇒none + bare-module degenerate + serialization omits the other 3 vecs + determinism. `cargo test --lib` + snapshots 6/6 byte-identical; clippy -D warnings + fmt. Then `.5b.2` (surface: schema 1.7 + dispatch + docs + KM). Pattern fully established by `.3b`/`.4b` — follow it. **API-audience steering (`feedback_api_for_agents_not_humans`):** machine-friendly completeness within the SCHEMA-DERIVED ceiling. Other lanes: `SIGNOFF-AUTOMATION-EXPANSION`/`IDENTITY-DEEPENING` `active` no-frontier; `STRUCTURED-EMISSION-EXPANSION` `proposed` (owner-gated).
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); every capability is an opt-in knob; decision/KM fact per durable capability or boundary.
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
