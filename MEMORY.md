# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `KNOB-ERGONOMICS-AND-PRESETS.2b.1` commit — the **first code slice** of the knob-ergonomics lane (decision `0021`). **Code — `src/config.rs` + `src/main.rs`; default DUT output byte-identical (`tests/snapshots.rs` 6/6 untouched).** Promotes the **16** config-file-only knobs to `Option` CLI flags (12 prob/u32 + the 4 dedup/identity bools as `SetTrue` → `Some(true)` via `.then_some(true)`); adds the `Preset` struct + `presets()` registry (4 curated presets, single source of truth) + `preset_overrides`/`preset_names`; the **shared** `resolve_config(base, profile, overrides, seed)` (`default|--config → --profile → explicit → seed → validate`, the one resolver CLI+MCP share); `ConfigError::UnknownProfile`; the `--profile` CLI flag wired into the DUT path. A preset IS an `Overrides` (one applier, `apply_cli_overrides`). Gate: lib **558/0** (+5 preset/resolver tests), anvil bin **14/0** (+2 CLI tests), snapshots **6/6** byte-identical, full `cargo test` green, clippy/fmt clean; `--dump-config` smoke confirms presets/explicit-beats-preset/unknown-error/bool-toggle. Prior: `4d1b8c4`=`.2a`; `e68e2d1`=`.1` (decision `0021`); `2f17147`=`DAE.2c.2b`. Push cadence: `origin/main` at `7142fd7` → **17 commits ahead** after this; push at ~30 (`feedback_push_cadence`).
- active_work_unit: **`KNOB-ERGONOMICS-AND-PRESETS` — `.1`+`.2a`+`.2b.1` DONE; frontier = `.2b.2`** (the SCHEMA-DERIVED queryable knob catalog: `knob_catalog()` + `KnobInfo` + metadata table + completeness test [`catalog names == Config::default() serde keys`] mirroring `adapter_catalog()`; `Serialize` on `Overrides`; the new `anvil://catalog/knob-schema` + `anvil://catalog/presets` resources; the MCP `generate`/`introspect`/`analyze` `profile` input routed through `resolve_config`). Then `.2b.3` = user-facing docs (book `knobs.md`+`agent-mcp.md` / USER_GUIDE / README / KM card) — **the book is currently ahead of docs by `.2b.1`+`.2b.2`; close at `.2b.3` this session** (`feedback_book_doctrine`). Tree stays `active`.
- next_action: **PNT — pick `KNOB-ERGONOMICS-AND-PRESETS.2b.2`** (code: the queryable catalog + MCP `profile` input). SCHEMA-DERIVED (project `Config::default()` serde + a metadata table; no recomputed truth). Full COMMIT.md workflow; RAM-guard heavy builds (decision `0003`).
- handoff: repo handoff-ready after this commit — full `cargo test` green, clippy/fmt clean, snapshots 6/6; `check_memory_architecture` green; KM gen/check green (**KM 54 facts**); introspection schema `1.11`. NOTE: user-facing docs for `.2b.1` (the new CLI flags + `--profile`) land at `.2b.3` — if pausing before then, do `.2b.3` first to keep the book in sync.
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); one runner + one classifier not two (`feedback_full_factorization`); every capability opt-in + MCP-invocable + queryable + CLI-as-shim (decision `0017`); design the API for agents not humans (`feedback_api_for_agents_not_humans`); the book is the user-facing surface and must not drift (`feedback_book_doctrine`); decision/KM fact per durable capability or boundary.
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
