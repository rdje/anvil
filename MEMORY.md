# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `STRUCTURED-EMISSION-EXPANSION.19b.2b` **repo-owned `tool_matrix --casez-mux-if-gate`** commit — a **CODE change** (touches `src/bin/tool_matrix.rs` + a 1-line fmt fix to `src/metrics.rs`), task-tree-owned by `.19b.2b`; **default-off ⇒ DUT byte-identical** (a new opt-in gate adds no generator output path; `tests/snapshots.rs` untouched). The full `--casez-mux-if-gate` wiring (~30 touch points, 1:1 on `--case-mux-if-gate`): `ScenarioSet::CasezMuxIfSweep` + `build_casez_mux_if_sweep_scenarios` + `casez_mux_if_focus_config` (comb-only, `casez_mux_if_emit_prob=1.0`, `casez_mux_prob=0.9`, **BOTH** `comb_mux_prob` AND `case_mux_prob` zeroed — both short-circuit `casez_mux` in `cone.rs`) × 3 strategies + **METRIC-KEYED** `ModuleReport.emitted_casez_mux_if` (`= num_emitted_casez_mux_if_chains > 0`, no text token) + `saw_casez_mux_if_emit` + merge/report/dispatch/mutual-exclusion/gap-arm/3 match arms/2 slug arms/`test_cli`/8 fixtures + 5 cargo proofs. Co-located: a blank-line fix resolved a pre-existing rustfmt-1.9.0 fmt drift in the `.19b.2a` `metrics.rs` test (HEAD failed `cargo fmt --all --check`). `cargo check/clippy(-D warnings)/fmt` clean; `--bin tool_matrix` +5 proofs + `--lib casez_mux_if` 10/10 + snapshots 6/6 byte-identical. **Banked clean `/tmp/anvil-casez-mux-if-gate-r1`**: 3 scenarios / 12 modules / 12 emitting a chain / 108 chains / `coverage_gaps = []` / `saw_casez_mux_if_emit = true` / 12/0 Verilator + both Yosys + Icarus. Prior: `3d944fd` (`.19b.2a`). Push cadence: `origin/main` at `4564533` → **8 commits ahead** after this ⇒ no push (threshold 30, `feedback_push_cadence`).
- active_work_unit: **`STRUCTURED-EMISSION-EXPANSION` — NINTH surface LIVE + metered + GATED (`.18`+`.19a`+`.19b.1`+`.19b.2a`+`.19b.2b` done); frontier → `.19b.3` (the ninth-surface user docs).** Lane stays `active`. The ninth surface (the wildcard `CasezMux` → masked `if`/`else if` priority chain, decision `0029`) is live behind `casez_mux_if_emit_prob`, surfaced at introspection schema `1.17`, and now proven downstream-clean by the repo-owned gate; only the user docs remain before `.19b`/`.19` close.
- next_action: **PNT — implement `STRUCTURED-EMISSION-EXPANSION.19b.3` (the ninth-surface user docs, DOCS-ONLY ⇒ DUT byte-identical)**: a `book/src/structured-emission.md` "## The ninth surface: a masked if/else if priority chain" section (byte-verified before/after from real emitter output) + the chapter-intro surface list + the `casez_mux_if_emit_prob` / `--casez-mux-if-emit-prob` / `--casez-mux-if-gate` entries in `book/src/knobs.md` + `USER_GUIDE.md` + README "Current CLI truth" + the deferred book example-JSON `schema_version` `1.16 → 1.17` refresh (`api-tools.md`/`agent-mcp.md`) + a `docs/knowledge/casez-mux-if-emit.md` KM card (regenerate `KNOWLEDGE_MAP.md`). Acceptance: `mdbook build book` + `cargo test --test book_examples` + `check_knowledge_map.sh` green; no `src/` touched. On completion `.19b`/`.19` close ⇒ NINTH surface delivered end-to-end; lane returns to a no-frontier boundary (future `.20+`: nested/multi-level `generate`, `interface`/`modport`). Monitor RAM (`scripts/ram_guard.sh --threshold 90 -- <cmd>`). (Tracked-lower: the `agent-mcp.md` `analyze`-example schema-`1.11` drift.)
- handoff: repo handoff-ready after this commit — `scripts/check_doctrines.sh` green (4 doctrines, code-scoped checks satisfied); `git diff --check` clean; default-off ⇒ DUT byte-identical; the ninth surface is live + metered + gated + fully validated. origin/main at `4564533`; 8 commits ahead after this. Co-staged: `src/bin/tool_matrix.rs` + `src/metrics.rs` (fmt) + `CODEBASE_ANALYSIS.md` + `DEVELOPMENT_NOTES.md` + `docs/tasks/STRUCTURED-EMISSION-EXPANSION.md` + `docs/TASK_TREE.md` + CHANGES + MEMORY.
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); one runner + one classifier not two (`feedback_full_factorization`); every capability opt-in + MCP-invocable + queryable + CLI-as-shim (decision `0017`); design the API for agents not humans (`feedback_api_for_agents_not_humans`); the book is the user-facing surface and must not drift (`feedback_book_doctrine`); decision/KM fact per durable capability or boundary. Doctrine-enforcement (decision `0026`): every doctrine = a deterministic check in `scripts/check_doctrines.sh`; code-scoped checks are scope-aware (non-code commits exempt).
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
