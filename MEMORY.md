# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `SEMANTIC-INTROSPECTION-EXPANSION.10b.1` **pure node_readers core** commit — a **code change** (`src/introspect/analyze.rs` only + `CODEBASE_ANALYSIS.md`), task-tree-owned by `.10b.1`; **DUT byte-identical** (`tests/snapshots.rs` untouched; not wired to any emit path). The pure core of the **ninth** derived `analyze` query, `node_readers` (the exact transpose of `node_drivers` `.9`): `QUERY_NODE_READERS` + `NodeReaders { node, kind, op: Option<String>, width, readers: Vec<NodeRef> }` (reusing the existing `NodeRef`) + the ninth `DerivedAnalysis.node_readers` `skip_serializing_if` vec (⇒ eight prior documents byte-identical) + `module_node_readers`/`design_node_readers` + the `node_readers_with` driver (one pass building a `BTreeMap<u32,BTreeSet<u32>>` reader index by transposing each `Gate`'s operands — sorted+deduped ascending, `x & x` once; output-port/flop-D drives out of scope ⇒ `B ∈ drivers(A) ⇔ A ∈ readers(B)`; readers resolved via the shared `node_ref_of`). 15 empty-fill literals gained `node_readers: Vec::new()` (guarded python insert). `supported_query_kinds()` unchanged (joins with dispatch in `.10b.2`). 5 new in-crate proofs (transpose both directions + empty-readers boundary + subject mirror; `x & x` dedup; `"node:<id>"` target/unknown; serialization omits the other 8 vecs; design top-module). Validation: `cargo test --lib introspect::analyze` 58/0; `cargo test --lib` 676/0/2; snapshots 6/6; clippy `-D warnings` + fmt + `check_doctrines` green. Prior: `955846e` (`.10a`). Push cadence: `origin/main` at `4564533` → **23 commits ahead** after this ⇒ no push (threshold 30, `feedback_push_cadence`).
- active_work_unit: **`SEMANTIC-INTROSPECTION-EXPANSION` — `.10` `node_readers` in implementation; active frontier `.10b.2` (the MCP surface).** Eight derived queries delivered end-to-end (schema `1.21`): the four named kinds (`output_support`/`input_reach`/`flop_reset_provenance`/`module_reachability`) + `flop_dependencies` (`.6`) + `memory_provenance` (`.7`) + `fsm_provenance` (`.8`) + `node_drivers` (`.9`). `.10a` (design) + `.10b.1` (pure core, lib-proven incl. the transpose proof) landed; `.10` = `node_readers`, the immediate fan-OUT dual of `node_drivers`, is the ninth — the fifth beyond the four named kinds. Nothing retired.
- next_action: **Implement `.10b.2` — the `node_readers` MCP surface + schema `1.21 → 1.22`.** In `src/introspect/analyze.rs`: add `QUERY_NODE_READERS` to `supported_query_kinds()`. In `src/mcp/mod.rs`: branch `run_analyze` by query kind (`module_node_readers`/`design_node_readers` in both module + design paths) **in the same commit**; the empty-result → `-32602` guard checks `analysis.node_readers`; add `node_readers` to the `analyze_schema` `query` enum + refresh the `analyze` tool description + server `instructions` (the `"node:<id>"` target form is already documented by `node_drivers`); add 2 mcp proofs (`analyze_returns_node_readers_and_caches_it` + `analyze_node_readers_unknown_target_is_invalid_params`). In `src/introspect/mod.rs`: `SCHEMA_VERSION 1.21 → 1.22` + doc comment + bump the `"1.21"` test assertions (introspect + mcp). Docs: schema-doc §6.7 (ninth `node_readers` payload + `NodeReaders`, "one of nine parallel result vecs") + `1.21 → 1.22` changelog + "defines 1.22"/§7/checklist; book `agent-mcp` (analyze row + a `node_readers` worked example + the resource line + every JSON envelope `1.21 → 1.22`) + `api-tools` (query enum/target + `1.21 → 1.22`); USER_GUIDE + README; a new KM card `semantic-introspection-node-readers` (+ cross-link); CODEBASE_ANALYSIS (mcp/schema-history blocks); ROADMAP lane. Then `mdbook build` + `book_examples` + KM regen + an `anvil-mcp` stdio e2e smoke (seed → schema `1.22`, a node with real readers; unknown `node:NNN` → `-32602`). Monitor RAM (`scripts/ram_guard.sh --threshold 90 -- <cmd>`).
- handoff: **repo handoff-ready after this commit** — `scripts/check_doctrines.sh` green (4 doctrines); `git diff --check` clean; DUT byte-identical (snapshots 6/6; lib 676/0/2); `.10b.1` pure `node_readers` core landed (lib-proven, not yet on the MCP surface). Pre-existing doc-hygiene gaps (not bundled, per no-bundling): the `.8a`/`.8b.1`/`.8b.2` rows are missing from this tree's Verification Log + Commit Log; the `.5b.2` leaf Status reads `pending` though done — a dedicated docs-hygiene backfill leaf can close both. origin/main at `4564533`; 23 commits ahead after this. Co-staged: `src/introspect/analyze.rs` + `CODEBASE_ANALYSIS.md` + `docs/tasks/SEMANTIC-INTROSPECTION-EXPANSION.md` + `docs/TASK_TREE.md` + CHANGES + MEMORY.
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); one runner + one classifier not two (`feedback_full_factorization`); every capability opt-in + MCP-invocable + queryable + CLI-as-shim (decision `0017`); design the API for agents not humans (`feedback_api_for_agents_not_humans`); SEMANTIC-INTROSPECTION stays SCHEMA-DERIVED / no-shadow-simulator (decisions `0004`/`0011`); the book is the user-facing surface and must not drift (`feedback_book_doctrine`); decision/KM fact per durable capability or boundary. Doctrine-enforcement (decision `0026`): every doctrine = a deterministic check in `scripts/check_doctrines.sh`; code-scoped checks are scope-aware (non-code commits exempt).
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
