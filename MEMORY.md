# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this **`VOLUME-DATA-LOCALITY.3`** (code) commit — all **14** test-fixture/temp-workspace `temp_dir()` sites moved onto `crate::paths::sandbox_root()`; `grep -rn "temp_dir()" src/ tests/` now matches **only `src/paths.rs`** (the OS temp dir is named in exactly one place ⇒ enforceable by inspection). `book_examples` capture files stay SIBLINGS of the script CWD (invariant preserved). KM card `bisimulation-flop-merge` reverify fixed + made honest (needs `-Wno-DECLFILENAME`: fixed dump filename can't match the module name, so bare `-Wall` always exits nonzero). **HAZARD created by this slice:** the sandbox root is now INSIDE the repo ⇒ live state during a test run; clearing it mid-run deletes running tests' CWDs *and* their capture files (I did this and got a misleading empty-output `book_examples` failure; re-run alone was green). Clear `.cache/anvil-sandbox` before/after a run, NEVER during. Prior: **`7518179`** `VOLUME-DATA-LOCALITY.2` (code) commit — **the one production off-volume default is gone.** New `src/paths.rs` = THE resolver: `sandbox_root()` → `$ANVIL_SANDBOX_ROOT` → marker-walked (`.git`/`Cargo.toml`, nearest wins) project root → CWD, each + `.cache/anvil-sandbox`; never `temp_dir()`. `ValidateOptions::default()` rewired onto it ⇒ validate/minimize/hunt/divergence, CLI+MCP, all inherit; `compact.rs` bisim dump moved off hardcoded `/tmp` and now prints its path. Design constraint: ANVIL is DISTRIBUTED, so no baked-in repo path — runtime marker-walk + CWD fallback (rejected: `env!("CARGO_MANIFEST_DIR")`, XDG/home cache, refuse-without-root). Measured: real `validate(7)` w/ Verilator+Yosys → `sandbox = <repo>/.cache/anvil-sandbox/anvil-validate-d8420426e78b2d05`, `ok=true`. 5 resolver tests; check/clippy/fmt/mdbook clean; full `cargo test` under `ram_guard` **exit 0**, snapshots **6/6**. Book+USER_GUIDE synced in-slice (the `/tmp` sandbox examples went wrong on landing). Prior: **`6d9a318`** `VOLUME-DATA-LOCALITY.1` (register+audit) — docs-only ⇒ DUT byte-identical. Owner §12/§13 directive: repo is `/dev/disk5s1`, `/tmp`+`/private/tmp` are `/dev/disk3s1s1` ⇒ **every OS-temp write is off-volume**. Audit of 15 `temp_dir()` sites: **(a) 1 PRODUCTION** — `ValidateOptions::default().sandbox_root` (`src/downstream/mod.rs:1185`) ⇒ every validate/minimize/hunt/divergence run (CLI+MCP) writes off-volume; **(b)** `src/ir/compact.rs:3167` hardcodes `/tmp/anvil-bisim-merged.sv` (KM card `bisimulation-flop-merge` cites it); **(c)** 13 test-fixture sites; **(d)** CLI-parse literals = NOT violations (don't "fix"). Session scratch already migrated to `.cache/scratch/` (copy/verify/delete, tree-hash identical, residue census clean). Prior: **`0f4d484`** `BOOK-LANE-COVERAGE.3` — **closed the `BOOK-LANE-COVERAGE` tree**: `book/src/frontend-lane.md` + the deferred `SUMMARY.md` frontend row + cross-links from `introduction.md`/`faq.md`/`microdesign-lane.md`, so every pre-existing book mention of a non-DUT lane now routes to a real chapter. Real seed-0 output pasted; parity claims cross-checked against `tests/frontend_parity.rs`. `mdbook build` exit 0; `book_examples` 3/3. Prior: **`efb5ee5`** `BOOK-LANE-COVERAGE.2` (microdesign chapter + the new "Artifact Lanes" part). Prior: **`a4c9f9e`** `LANE-OUT-FILENAME.1` (code) commit — filenames only ⇒ **no SV/manifest byte change in any lane**, `tests/snapshots.rs` **6/6 byte-identical**, DUT untouched. `LaneArtifact` gains builder-truth `pub top` (DUT `design.top`, microdesign new `top_name(seed)` single-sourcing `emit_sv`/`emit_manifest`, frontend `unit.top.name`); `run_non_dut_lane` stems `--out` from it; text-scraping `parse_top_name` **deleted**. REJECT→PASS: frontend `--out` wrote `child_0.sv` (first `module` decl — frontend emits child stubs before the top) vs documented `acc_0.sv`; now correct. Gate: check/clippy(-D warnings)/fmt clean; `cargo test` under `ram_guard --threshold 90` **exit 0** (pipeline 125/0, snapshots 6/6); `--lib umbrella` 9/9 incl. new `lane_artifact_top_is_builder_truth`. Prior: `5223452` (`BOOK-LANE-COVERAGE.1`), `98e31e1` (`EVIDENCE-BANK-DURABILITY.2` / decision `0030`). Push cadence: 3 ahead of `origin/main`; 30-commit threshold.
- active_work_unit: **`VOLUME-DATA-LOCALITY`** (`active`; `.1`+`.2`+`.3` `done`) → frontier **`.4`**: write ADR `docs/decisions/0031-*.md` (the data-locality contract + the resolver's resolution order + rejected alternatives: build-time `CARGO_MANIFEST_DIR` root / XDG-home cache / refuse-without-root) and **amend decision `0030`** — its rejected option (iii) is now partly MANDATED: a future evidence bank must be repo-derived + on-volume, while the committed digest remains the citation form (supersede-don't-mutate). Also record the old-checkout decision there, and decide whether to add `check_no_off_volume_defaults.sh` (structural: `temp_dir()` outside `src/paths.rs` ⇒ FAIL) to the `check_doctrines.sh` registry — cheap and now trivially true, so likely yes. Then `.5` (narrowed: `TOOLBOX.md` + a stale-sandbox doc sweep; book/USER_GUIDE landed in `.2`). Also open: `EVIDENCE-BANK-DURABILITY.3`/`.4`/`.5`, `CAPABILITY-BREADTH-EXPANSION.1`. **Do NOT touch** the `/tmp/x` CLI-*parse* literals — recorded non-violations.
- next_action: **`VOLUME-DATA-LOCALITY.4`** — ADR `0031` + `INDEX.md` row + KM front-matter; amend `0030`; add the doctrine check if adopted (then `check_doctrines.sh` should report 5/5). Docs/decision leaf ⇒ no heavy tool run needed. Use `.cache/scratch/` for scratch — never `/private/tmp`; and never clear `.cache/anvil-sandbox` while a test run is in flight.
- handoff: **repo handoff-ready.** `check_doctrines.sh` green (4/4); tree clean; `mdbook build` + book-examples green. Session scratch migrated off `/private/tmp` to `.cache/scratch/` (copy/verify/delete: 13 files, 25744 bytes, tree-hash `a5473f7d…` identical, source deleted, residue census clean). **Old checkout — RESOLVED, do not re-raise:** `~/Documents/github/anvil` (1.1G off-volume, HEAD a strict ancestor of ours, identical caches ⇒ no unique work) is **deliberate owner-owned data**. Owner decision `2026-07-29`: they keep all original projects under `~/Documents/github` for now and will remove that whole directory themselves later. **No agent deletes or migrates it**, and any off-volume audit / doctrine check must EXCLUDE `~/Documents/github` — finding it there is the expected state.
- **STANDING OWNER DIRECTIVE (`2026-07-29`, absolute, stated twice) — decision `0031`: the SSD is the ONLY volume this project stores on OR points at.** Never write to and never *reference* `/tmp`, `/private/tmp`, `$TMPDIR` (macOS `/var/folders/…`), `$HOME`, or any boot-volume path — prose citations count as references. Repo = `/dev/disk5s1`; all of `/tmp`+`$HOME`+`$TMPDIR` = `/dev/disk3s1s1` (boot). Sole exception: the installed toolchain (`~/.rustup` rustc/cargo, `/opt/homebrew` verilator/yosys/iverilog) — execute-only, documented in `0031`. Shared caches are POPULATED, never deleted; `~/Documents/github` is owner-owned, never touched by an agent. Harness runtime files under `/private/tmp/claude-501/` + `~/.claude/` are the harness's own and cannot be moved from the repo — so never depend on them: redirect real output to `.cache/scratch/`.
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); one runner + one classifier not two (`feedback_full_factorization`); every capability opt-in + MCP-invocable + queryable + CLI-as-shim (decision `0017`); design the API for agents not humans (`feedback_api_for_agents_not_humans`); SEMANTIC-INTROSPECTION stays SCHEMA-DERIVED / no-shadow-simulator (decisions `0004`/`0011`); the book is the user-facing surface and must not drift (`feedback_book_doctrine`); decision/KM fact per durable capability or boundary. Doctrine-enforcement (decision `0026`): every doctrine = a deterministic check in `scripts/check_doctrines.sh`; code-scoped checks are scope-aware (non-code commits exempt).
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
