# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `DOWNSTREAM-ADAPTER-EXPANSION.2b.2` commit — the `tool_matrix --sv2v` transpile-acceptance column (decision `0020`); **closes `.2b`**. **`src/bin/tool_matrix.rs` + `tests/sv2v_e2e.rs` + docs; default-off / DUT byte-identical (`tests/snapshots.rs` 6/6 untouched; banked reports + `--resume` byte-identical when off).** Mirrors `--iverilog-compile`: `Cli.sv2v`/`sv2v_bin` + `ModuleReport`/`DesignReport.sv2v` (serde skip_serializing_if) dispatched via `AcceptanceTool::Sv2v.adapter().run(&cx)` + checkpoint field + `--resume` guard + `ToolSummary.sv2v_passed/failed` (in `any_failed`) + console line + `MatrixReport.sv2v_enabled` + `unit_divergence` inclusion. **Friendly no-op: gated on a `tool_version` presence probe** so a requested-but-absent `sv2v` records no column + never bails (smoke: `--skip-verilator --skip-yosys --sv2v` ⇒ exit 0, `sv2v 0/0`, 0 invocations). `tests/sv2v_e2e.rs` = portable public-API proof + `#[ignore]` real-tool gate. `run_module_tools` 4-tuple → `ModuleToolColumns` alias (clippy type_complexity); `unit_divergence` documented `#[allow(too_many_arguments)]`. Docs: README/USER_GUIDE/`synthesizability.md` + KM card (52 facts). Gate: `tool_matrix` **76/0** (+1 CLI proof), snapshots **6/6**, sv2v_e2e portable 1/0, lib 549/0, clippy/fmt clean, mdbook clean, KM green, RAM-guarded. Prior: `af81e16`=`DAE.2b.1` (sv2v adapter + MCP); `9980cee`=`DAE.2a.3`; `7142fd7` (**== origin/main**, pushed). Push cadence: `origin/main` at `7142fd7` → **11 commits ahead** after this (…`DAE.2a.3`,`DAE.2b.1`,`DAE.2b.2`); push at ~30 (`feedback_push_cadence`).
- active_work_unit: **`DOWNSTREAM-ADAPTER-EXPANSION`** — `.1` ADR + `.2a` + `.2b` (sv2v: `.2b.1` downstream+MCP / `.2b.2` tool_matrix column) all done; frontier now **`.2c`** (slang — the richer adapter landing the optional JSON-AST `extract_facts` hook, proving the trait's SCHEMA-DERIVED fact path; absent locally ⇒ no-op + `#[ignore]` gate); `.2d+` (surelog/UHDM, commercial-wrapper) future. Other open lanes per `docs/TASK_TREE.md`: `KNOB-ERGONOMICS-AND-PRESETS.1`, `CI-PACKAGING-DISTRIBUTION.1`, `COVERAGE-STEERED-GENERATION.1`, `CAPABILITY-BREADTH-EXPANSION.1/.2` (design-first ADRs) + no-frontier capability trees (`STRUCTURED-EMISSION-EXPANSION`, `SEMANTIC-INTROSPECTION-EXPANSION`+`AGENT-MCP-EXPANSION`, `IDENTITY-DEEPENING`, `SIGNOFF-AUTOMATION-EXPANSION`).
- next_action: **PNT — `DOWNSTREAM-ADAPTER-EXPANSION.2c`** (the second new adapter, `slang`, with the optional `extract_facts` JSON-AST hook — proving the trait's richer SCHEMA-DERIVED path: accept/reject **plus** structural/elaboration facts (top/ports/instances) projected from `slang --ast-json`, never a behavioural oracle. `Adapter::supports_facts()` overrides `true` for slang. `slang` absent locally ⇒ ship structural + friendly absent-tool no-op + `#[ignore]` real-tool gate (the sv2v `.2b` precedent). Pre-split if warranted (the fact-hook plumbing vs the column). Refine `.2c` acceptance at pick. Or take an owner steer.
- handoff: repo handoff-ready — code state green after this commit (`cargo test --bin tool_matrix` **76/0**, snapshots 6/6, sv2v_e2e portable 1/0, `cargo test --lib` 549/0; introspection schema `1.11`); `mdbook build` clean; `check_memory_architecture` + KM gen/check green (**KM 52 facts**). Working tree clean after this commit.
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); one runner + one classifier not two (`feedback_full_factorization`); every capability opt-in + MCP-invocable + queryable + CLI-as-shim (decision `0017`); design the API for agents not humans (`feedback_api_for_agents_not_humans`); the book is the user-facing surface and must not drift (`feedback_book_doctrine`); decision/KM fact per durable capability or boundary.
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
