# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `STRUCTURED-EMISSION-EXPANSION.12a` **multi-output task impl design-detail** commit — **design-detail leaf, no source change, DUT byte-identical**. A `DEVELOPMENT_NOTES.md` entry grounding decision `0025` in the real source + pinning all seven `.12b` impl choices: (1) replicate the admissibility predicate; (2) `Module.multi_output_task_groups: BTreeMap<NodeId,Vec<NodeId>>` (leader→partners); (3) `src/ir/multi_output_task_emit.rs` one-roll-per-leader pairing on a shared **non-constant** operand; (4) the `in_fanin(m,target,root)` **bounded backward DFS** soundness check (operand-topological `NodeId` invariant confirmed); (5) dedup-formal rendering reusing `render_cone_gate_expr` with an **empty `interior_set`** + `__mt`/`__mtv` naming + one always_comb per group + per-member passthrough (members keep their wires); (6) run after `task_emit` before `cone_function` + extend `sibling_marked`; (7) `multi_output_task_emit_prob` knob + `--multi-output-task-emit-prob` flag + `num_emitted_multi_output_tasks` metric (schema `1.13→1.14`) + `--multi-output-task-gate` / `saw_multi_output_task_emit` + gate-shape calibration (high `terminal_reuse_prob` + shallow `max_depth`). Prior: `c31fbb0`=`STRUCTURED-EMISSION-EXPANSION.11` (decision `0025` + tree split); `df21b5c`=`CAPABILITY-BREADTH-EXPANSION.2b.3`. Push cadence: `origin/main` at `605ec44` → **7 commits ahead** after this ⇒ no push (threshold 30, `feedback_push_cadence`).
- active_work_unit: **`STRUCTURED-EMISSION-EXPANSION` tree `active`, frontier `.12b`** (impl of the sixth surface, decision `0025`; `.11` design + `.12a` design-detail done; `.12b` pre-split `.12b.1` live / `.12b.2` metric+gate / `.12b.3` docs). Five structured surfaces delivered end-to-end (function `.1`+`.2`, generate-for `.3`+`.4`, task `.5`+`.6`, wide-lane part-select `.7`+`.8`, cone-function `.9`+`.10`).
- next_action: **PNT — `STRUCTURED-EMISSION-EXPANSION.12b.1`** (the live surface, FIRST source change): add `Config::multi_output_task_emit_prob` (default 0.0, validation, dump-config) + `--multi-output-task-emit-prob` CLI flag + `Module.multi_output_task_groups` + new `src/ir/multi_output_task_emit.rs` (`annotate_multi_output_task_groups`: leader-roll pairing on a shared non-constant operand + the `in_fanin` bounded backward DFS) + `src/ir/mod.rs` registration + two guarded `gen/mod.rs` rolls (after `task_emit`, before `cone_function`) + extend `cone_function_emit::sibling_marked` + `src/emit/sv.rs` rendering (dedup-formal `__mt` decl reusing `render_cone_gate_expr` with empty interior_set + per-member `__mtv` always_comb call + passthrough assigns) + lib proofs + a forced `multi_output_task_emit_prob=1.0` Verilator/Yosys/Icarus sweep. NO metric/schema bump (knob rides the version; metric+gate at `.12b.2`). Default-off / DUT byte-identical (snapshots 6/6). Then `.12b.2`, `.12b.3`.
- handoff: repo handoff-ready after this commit — docs-only slice; `check_memory_architecture` + KM check green (`0025` indexed); `mdbook build` clean; code unchanged since `df21b5c` (lib 589/0, tool_matrix 79/0, snapshots 6/6 from that commit still hold; `cargo check --all-targets` clean at session start).
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); one runner + one classifier not two (`feedback_full_factorization`); every capability opt-in + MCP-invocable + queryable + CLI-as-shim (decision `0017`); design the API for agents not humans (`feedback_api_for_agents_not_humans`); the book is the user-facing surface and must not drift (`feedback_book_doctrine`); decision/KM fact per durable capability or boundary.
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
