# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `SEMANTIC-INTROSPECTION-EXPANSION.5b.1` commit (pure `module_reachability` core in `src/introspect/analyze.rs`; SOURCE+docs, DUT byte-identical; not yet over MCP — registry+dispatch land in `.5b.2`; hash backfills next update; prior: `d05d145` = `.5a`; `a715070` = `.4b.2`). Working tree clean after this commit. Push cadence: `origin/main` at `63d2622` → **15 commits ahead after this commit; push at ~30** (`feedback_push_cadence`).
- active_work_unit: **`SEMANTIC-INTROSPECTION-EXPANSION` tree `active`; `.5` = `module_reachability` in progress; frontier = `.5b.2` (surface).** THREE queries already delivered end-to-end: `output_support` (`.1`/`.2`) + `input_reach` (`.3`) + `flop_reset_provenance` (`.4`), schema `1.6`. `.5a` design + `.5b.1` pure core done. Index: `docs/TASK_TREE.md`.
- `.5b.1` delivered (DUT byte-identical): `src/introspect/analyze.rs` gains `QUERY_MODULE_REACHABILITY`, `ModuleReachability { module, reachable, depth: Option<usize> (skip-if-None), instantiates (sorted distinct child names), instance_count }`, the FOURTH `DerivedAnalysis.module_reachability` vec (`#[serde(default, skip_serializing_if)]` ⇒ prior 3 documents byte-identical), and `design_module_reachability` (min-depth BFS from `design.top` over `Module.instances[].module` edges; one entry per module sorted by name; absent-top ⇒ all unreachable) / `module_module_reachability` (degenerate one-node) + `reachability_of`/`distinct_instantiated` helpers. The 6 existing `DerivedAnalysis` literals gained `module_reachability: Vec::new()`. `supported_query_kinds()` unchanged (joins with dispatch in `.5b.2`). 6 new proofs. Validation: `cargo test --lib` 456/0/2; snapshots 6/6 byte-identical; clippy -D warnings + fmt clean.
- next_action: **PNT — implement `SEMANTIC-INTROSPECTION-EXPANSION.5b.2`** (the surface): add `module_reachability` to `analyze::supported_query_kinds()` AND branch `run_analyze` by kind (`design_module_reachability` for the design path / `module_module_reachability` for the module path) in the SAME commit; update the unknown-target → `-32602` guard to check `analysis.module_reachability.is_empty()` for this kind; bump `SCHEMA_VERSION` `1.6 → 1.7` in `src/introspect/mod.rs` (+ doc comment) + the `"1.6" → "1.7"` test-assertion bumps (introspect + mcp); add `"module_reachability"` to the `analyze_schema` `query` enum + the `target` description + the `analyze` tool description + the server `instructions`; schema-doc §6.7 (a fourth `module_reachability` payload + `ModuleReachability`) + the `1.6 → 1.7` changelog + "defines 1.7"/checklist; book `agent-mcp` (analyze row + worked example + the JSON examples `1.6 → 1.7`); USER_GUIDE + README (schema 1.7); a new KM card `semantic-introspection-module-reachability` (+ cross-link, regenerate KM). Add 2 MCP proofs (returns+caches on a hierarchy design; unknown module name ⇒ `-32602`). Validation: `cargo test --lib` + snapshots 6/6; clippy/fmt; mdbook + book_examples 3/3; e2e `anvil-mcp` smoke (hierarchy design → schema 1.7 reachability; unknown module ⇒ -32602). Closes `.5b`/`.5`. Pattern fully established by `.3b.2`/`.4b.2` — follow it. **API-audience steering (`feedback_api_for_agents_not_humans`):** machine-friendly completeness within the SCHEMA-DERIVED ceiling. Other lanes: `SIGNOFF-AUTOMATION-EXPANSION`/`IDENTITY-DEEPENING` `active` no-frontier; `STRUCTURED-EMISSION-EXPANSION` `proposed` (owner-gated).
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); every capability is an opt-in knob; decision/KM fact per durable capability or boundary.
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
