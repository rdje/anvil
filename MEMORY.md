# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `BUG-HUNT-ORCHESTRATION.2d` commit (**code, `src/main.rs` + `src/downstream/`** + USER_GUIDE/README/book/live-docs) — the **`anvil hunt` CLI subcommand** (ANVIL's **first** subcommand): `Cli` gains an optional `#[command(subcommand)] command: Option<Commands>` so `anvil --seed N …` parses with `command==None` ⇒ the historical generate flow runs **byte-identical**; `anvil hunt …` dispatches `run_hunt_command` → `build_hunt_request` → `hunt::run`, printing the `HuntReport` JSON. `HuntCommand` projects `HuntRequest` (`--seed`/`--seeds`/`--config`/`--tools`/`--yosys-mode`/`--no-minimize`/`--budget`/`--diff-sim`/`--out`); `--out` ⇒ `bundle_root` (the human-CLI on-disk bundle; MCP stays cache-only). `AcceptanceTool` gained `clap::ValueEnum`. Prior: `b2e0228` = `.2c` (MCP `hunt` tool); `a7b011f` = `.2b.2b`; `a5458ef` = `.2b.2a`; `5ca0006` = `.2b.1` fix; `f4fc9c8` = `.2b.1`; `8a4fbab` = `.2a`; `b8c1e8b` = `.1` (decision `0018`); … `b90bdff` = `.5` (**pushed** — `origin/main` at `b90bdff`). Working tree clean. Push cadence: `origin/main` at `b90bdff` → **23 commits ahead**; push at ~30 (`feedback_push_cadence`).
- active_work_unit: **`BUG-HUNT-ORCHESTRATION`** — `.1` ADR / `.2a` / `.2b.1` / `.2b.2a` / `.2b.2b` (engine) / `.2c` (MCP `hunt` tool) / `.2d` (`anvil hunt` CLI) all **done**. Frontier **`.2e`** (the **last** leaf; closes `.2` + the tree) = a **real-tool end-to-end gate** (`#[ignore]`, tool-gated) that runs a hunt against Verilator/Yosys with an **injected/known failure** and asserts a one-command-reproducible bundle is produced (exercises the `.2b.2b` bundle + `minimize` end-to-end), **plus** the full user-facing closeout: a `book/src/agent-mcp.md` "bug-hunting loop end to end" rewrite, a USER_GUIDE deepening, the README, and a **Knowledge Map card** (`how do I run a turnkey downstream bug hunt`). Design invariants: thin orchestrator (no new detector/minimizer), sandbox caller-set (decision `0004`), every report field SCHEMA-DERIVED (decision `0017`). Six other lanes still `active` design-first (`ACCEPTANCE-DIVERGENCE-HUNTING`, `DOWNSTREAM-ADAPTER-EXPANSION`, `KNOB-ERGONOMICS-AND-PRESETS`, `CI-PACKAGING-DISTRIBUTION`, `COVERAGE-STEERED-GENERATION`, `CAPABILITY-BREADTH-EXPANSION`); plus no-frontier `STRUCTURED-EMISSION-EXPANSION` (5 surfaces; 6th = future `.11`), `SEMANTIC-INTROSPECTION-EXPANSION` + `AGENT-MCP-EXPANSION`, `IDENTITY-DEEPENING`, `SIGNOFF-AUTOMATION-EXPANSION`, `LOCAL-REFERENCE-CACHE`. Index: `docs/TASK_TREE.md`.
- next_action: **PNT — `BUG-HUNT-ORCHESTRATION.2e`** (the tree's last leaf). Add a tool-gated `#[ignore]` e2e test (likely `tests/hunt_e2e.rs` or in `src/hunt`) that builds a `HuntRequest` with a `bundle_root` temp dir + an injected/known-failing config (or a tool that rejects), runs `hunt::run`, and asserts a finding + a complete bundle dir (`repro.sv`/`knobs.json`/`introspection.json`/`hunt-verdict.json`/`tool-logs`/`repro.sh`) + that `repro.sh` regenerates byte-identical `.sv`. Then the KM card + the book "end to end" rewrite. Touches `tests/` ⇒ full `COMMIT.md` cargo gate; DUT byte-identical (test-only + docs). Closes `.2` and the tree.
- handoff: repo handoff-ready — `.2d` all gates green: `cargo check/clippy/fmt` clean, `cargo test` green (anvil-bin **12/0** [+5 hunt CLI proofs] / mcp:: 66/0 / hunt:: 11/11 / downstream:: 20/0 / tool_matrix 71+1 ignored / book_examples 3/3 / snapshots **6/6 byte-identical**; introspection schema `1.11`). Real-tool smoke: `anvil hunt --seed 1 --seeds 3 --tools verilator` ⇒ `n_failures=0`. `check_memory_architecture` + KM gen/check green (KM 46 facts). Working tree clean.
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); every capability opt-in; the hunt adds **no** new generator path / no detector / no minimizer (it composes the existing surfaces); decision/KM fact per durable capability or boundary.
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
