# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this **`EVIDENCE-BANK-DURABILITY.1`** (register + record) commit — **docs-only** (no `src/`/`tests/`/`examples/`) ⇒ **DUT byte-identical**, `tests/snapshots.rs` untouched. Records a session-bootstrap observation: **every banked closure artifact this repo cites is gone** — `ls -d /tmp/anvil-*` → 0 dirs, `target/tmp` → 0 bytes, while **77 distinct `/tmp/anvil-*` paths** are cited across README/ROADMAP/USER_GUIDE/CODEBASE_ANALYSIS/TOOLBOX/book/tasks, incl. every phase-closing artifact + every structured-surface gate bank. **Not** a claim any closure was wrong (the gates stay re-runnable oracles, `DOCTRINE_ENFORCEMENT.md` §3) — the defect is the **evidence architecture**: the cited artifact is unreachable, so re-verification = re-derivation (the archaeology the Knowledge Map exists to remove); partial regression vs `LIVE-DOC-PATH-HYGIENE.1` (repo paths fixed, `/tmp` evidence paths not). Validation: `check_doctrines.sh` 4/4 PASS; ramp-up `cargo check --all-targets` clean + `cargo test` under `ram_guard --threshold 90`. Prior: `4bb1822` (`SEMANTIC-INTROSPECTION-EXPANSION.15b.2`, schema `1.27`, fourteen derived analyze queries). Push cadence: **pushed this session by explicit owner directive** (was 13 ahead of `a767415`; the 30-commit threshold was overridden by the directive, not retired).
- active_work_unit: **`EVIDENCE-BANK-DURABILITY`** (new, `active`; `.1` register+record `done`) → frontier leaf **`.2`** (`pending`, design-first ADR). Every other tree is at a NO-FRONTIER boundary: `SEMANTIC-INTROSPECTION-EXPANSION` (fourteen derived analyze queries @ schema `1.27`), `STRUCTURED-EMISSION-EXPANSION` (nine structured surfaces), `IDENTITY-DEEPENING`, `SIGNOFF-AUTOMATION-EXPANSION`, `DOWNSTREAM-ADAPTER-EXPANSION`, `KNOB-ERGONOMICS-AND-PRESETS`, `CI-PACKAGING-DISTRIBUTION`, `LOCAL-REFERENCE-CACHE`, `USABILITY-LANE-OWNERSHIP`; the one other `pending` leaf anywhere is `CAPABILITY-BREADTH-EXPANSION.1` (SV up-opt breadth ADR). Roadmap/codebase/mdBook verified aligned at this bootstrap (spot-check: `SCHEMA_VERSION = "1.27"` and `supported_query_kinds()` = 14 kinds in `src/introspect/`, matching the docs).
- next_action: **`EVIDENCE-BANK-DURABILITY.2`** — write the ADR (`docs/decisions/0030-*.md`) choosing ONE durable-evidence mechanism for closure citations: (i) cite the re-runnable gate command beside every claim; (ii) commit a small per-bank digest (counts + `coverage_gaps` + per-tool pass/fail + report SHA-256) under `docs/evidence/`; (iii) relocate banks to a stable non-`/tmp` path (e.g. gitignored `.cache/evidence/`); (iv) explicitly demote `/tmp` paths to labelled breadcrumbs. Record rejected alternatives + the implementation leaf shape. Docs/decision leaf ⇒ no tool run needed. Alternative if the owner re-steers: PNT one of the no-frontier lanes (a fifteenth derived query — recursive multi-level instance descent / two-endpoint `support_path` / instance-input reach sinks; or `CAPABILITY-BREADTH-EXPANSION.1`). Doctrine: **task-tree-own the leaf BEFORE any `src/` edit**; SCHEMA-DERIVED / default-off / DUT byte-identical; `.Na` design-detail → `.Nb.1` pure core → `.Nb.2` surface cadence; run the e2e `anvil-mcp` smoke (it catches spec-vs-reality bugs); monitor RAM (`scripts/ram_guard.sh --threshold 90 -- <cmd>`).
- handoff: **repo handoff-ready.** `scripts/check_doctrines.sh` green (4/4); tree clean; DUT byte-identical (docs-only commit). Pushed to `origin/main` this session by explicit owner directive. A fresh session resumes at `EVIDENCE-BANK-DURABILITY.2` (see next_action).
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); one runner + one classifier not two (`feedback_full_factorization`); every capability opt-in + MCP-invocable + queryable + CLI-as-shim (decision `0017`); design the API for agents not humans (`feedback_api_for_agents_not_humans`); SEMANTIC-INTROSPECTION stays SCHEMA-DERIVED / no-shadow-simulator (decisions `0004`/`0011`); the book is the user-facing surface and must not drift (`feedback_book_doctrine`); decision/KM fact per durable capability or boundary. Doctrine-enforcement (decision `0026`): every doctrine = a deterministic check in `scripts/check_doctrines.sh`; code-scoped checks are scope-aware (non-code commits exempt).
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
