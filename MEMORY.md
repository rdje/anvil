# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `DOWNSTREAM-ADAPTER-EXPANSION.2a.2` commit — the adapter-catalog discoverability resource (decision `0017`). **`src/downstream/mod.rs` + `src/mcp/mod.rs` + book; default-off / DUT byte-identical (`tests/snapshots.rs` 6/6 untouched; no introspection SCHEMA_VERSION bump).** Landed: a defaulted `Adapter::supports_facts()` (built-ins false; slang `.2c` overrides) + serializable `AdapterInfo{id,binary,present,supports_facts}` + `pub fn adapter_catalog()` (a SCHEMA-DERIVED projection of `adapters()` with a live `tool_version()` PATH probe for `present`); `mcp` `resources_list` advertises + `resources_read` serves the new **`anvil://catalog/adapters`** resource (`{ "adapters":[...] }`), beside `knobs`/`lanes`; book synced (`api-resources-prompts.md` table row + `agent-mcp.md` list). No new tool, no schema bump. +1 mcp proof. Gate: lib **546/0** (+1), snapshots **6/6**, clippy/fmt clean, mdbook clean, book_examples **3/3**, RAM-guarded. Files: `src/downstream/mod.rs` + `src/mcp/mod.rs` + `book/src/api-resources-prompts.md` + `book/src/agent-mcp.md` + the tree + `docs/TASK_TREE.md` + `CODEBASE_ANALYSIS.md` + CHANGES + MEMORY. Prior: `c5d0fac`=`DAE.2a.1` (registry core); `412e5ff`=`DAE.1` (decision `0020` ADR); `7142fd7` (**== origin/main**, pushed). Push cadence: `origin/main` at `7142fd7` → **8 commits ahead** after this (`.2c.2`,`.2d`,`.2e`,`.2f`,`BOOK-API-REFERENCE.1`,`DAE.1`,`DAE.2a.1`,`DAE.2a.2`); push at ~30 (`feedback_push_cadence`).
- active_work_unit: **`DOWNSTREAM-ADAPTER-EXPANSION`** — `.1` ADR + `.2a.1` (registry core) + `.2a.2` (catalog resource) done; frontier now **`.2a.3`** (route `validate_tool_specs` + the `tool_matrix` per-unit invocation through the registry, keeping the fixed `ModuleReport`/`DesignReport` columns + banked reports + `--resume` byte-identical — the bridge that makes `.2b` sv2v a near-one-line add), then `.2b` (sv2v accept/reject transpile column), `.2c` (slang + the `extract_facts` JSON-AST hook). Other open lanes per `docs/TASK_TREE.md`: `KNOB-ERGONOMICS-AND-PRESETS.1`, `CI-PACKAGING-DISTRIBUTION.1`, `COVERAGE-STEERED-GENERATION.1`, `CAPABILITY-BREADTH-EXPANSION.1/.2` (design-first ADRs) + no-frontier capability trees (`STRUCTURED-EMISSION-EXPANSION`, `SEMANTIC-INTROSPECTION-EXPANSION`+`AGENT-MCP-EXPANSION`, `IDENTITY-DEEPENING`, `SIGNOFF-AUTOMATION-EXPANSION`).
- next_action: **PNT — `DOWNSTREAM-ADAPTER-EXPANSION.2a.3`** (route `validate_tool_specs` [version axis, single-yosys-mode, caller-supplied binary] + `tool_matrix` `run_module_tools`/`run_design_tools` through the registry; the fixed `ModuleReport`/`DesignReport` `verilator`/`yosys`/`iverilog_compile` columns + banked reports + `--resume` stay byte-identical — this is the byte-identical-sensitive slice). Touches `src/downstream` + `src/bin/tool_matrix.rs`. Full `cargo` gate. Or take an owner steer if one is given.
- handoff: repo handoff-ready — code state green after this commit (`cargo test --lib` **546/0**, snapshots 6/6, `tool_matrix` 75/0, `anvil` 12/0, `divergence_e2e` portable 1/0 + ignored 2/0; introspection schema `1.11`); `mdbook build` clean + `book_examples` 3/3; `check_memory_architecture` + KM gen/check green (**KM 51 facts**). Working tree clean after this commit.
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); one runner + one classifier not two (`feedback_full_factorization`); every capability opt-in + MCP-invocable + queryable + CLI-as-shim (decision `0017`); design the API for agents not humans (`feedback_api_for_agents_not_humans`); the book is the user-facing surface and must not drift (`feedback_book_doctrine`); decision/KM fact per durable capability or boundary.
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
