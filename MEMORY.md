# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `STRUCTURED-EMISSION-EXPANSION.17b.1` **live CaseMux if/else-if priority-chain emit-projection (eighth surface)** commit — **CODE change** (`src/` touched), default-off ⇒ **DUT byte-identical**. The eighth structured surface goes LIVE behind the opt-in `case_mux_if_emit_prob` knob: new `src/ir/case_mux_if_emit.rs` (`annotate_case_mux_if_gates` — a `GateOp::CaseMux` with a non-`Constant` selector, excluding `CasezMux` + all sibling marks, run **last**; 9 lib proofs incl. the constant-selector exclusion + the end-to-end emit proof) + `Module.case_mux_if_gates: BTreeSet<NodeId>` + `ir/mod.rs` registration + `Config::case_mux_if_emit_prob` (6 `config.rs` touch points) + `--case-mux-if-emit-prob` flag (`main.rs`) + two guarded `gen/mod.rs` rolls (after `mux_if`) + the `emit/sv.rs` structured-case body branch (the `GateOp::CaseMux` arm → `if`/`else if` chain over the same operand refs + `W'h0` default when marked; parallel `case`/`endcase` suppressed via the `!contains(...)` tail guard). Implemented exactly per `.17a` (no deviations); **simpler than the seventh surface** — a `CaseMux` is already an `always_comb` `logic` var ⇒ no `__cv` passthrough. NO metric/schema bump (rides the version; `.17b.2` bumps `1.15 → 1.16`). `cargo check/clippy(-D warnings)/fmt` clean; `cargo test --lib` 616→**625** (9 new proofs); `--bin tool_matrix` **89** (unaffected); snapshots **6/6 byte-identical** (default-off). Forced `case_mux_if_emit_prob=1.0` sweep (`scratchpad/probe8/`, seed 2: 7 `CaseMux` → 7 chains / 14 `else if` / 0 residual `case`): Verilator `-Wall` **Δ=0 vs OFF** (2012/2017/2023) + both Yosys modes + Icarus clean + generated-RTL ON-vs-OFF **sim-equiv 20000 vectors** (`GEN-RTL SIM-EQUIV OK`). `CODEBASE_ANALYSIS` + `DEVELOPMENT_NOTES` + tree + `docs/TASK_TREE.md` + ROADMAP updated. Prior: this `STRUCTURED-EMISSION-EXPANSION.17a` commit. Push cadence: `origin/main` at `605ec44` → **30 commits ahead** after this ⇒ **PUSH NOW** (threshold 30, `feedback_push_cadence`).
- active_work_unit: **`STRUCTURED-EMISSION-EXPANSION` — frontier `.17b.2`** (the metric + repo-owned gate for the eighth surface). `.16` (decision `0028`) + `.17a` (design-detail) + `.17b.1` (live source) done; `.17b.2` (metric + gate) / `.17b.3` (docs) pending. SEVEN surfaces delivered (`.1`–`.15`); the EIGHTH is live (`.16`/`.17a`/`.17b.1`). All other `active` lanes at no-frontier boundaries.
- next_action: **PUSH FIRST** (`git push` — 30 commits ahead hits the cadence threshold), then **PNT into `STRUCTURED-EMISSION-EXPANSION.17b.2`** (the metric + repo-owned gate). Per the `.17a` design: add `Metrics::num_emitted_case_mux_if_chains` (`= m.case_mux_if_gates.len()`, `#[serde(default)]`) in `src/metrics.rs` + a lib proof ⇒ bump introspection `SCHEMA_VERSION` `1.15 → 1.16` across all current-output refs (`introspect/mod.rs` const + doc-comment + 3 assertions, 8 `mcp/mod.rs` assertions, `docs/AGENT_INTROSPECTION_SCHEMA.md` §7 + a `1.15→1.16` changelog entry, README + USER_GUIDE + CODEBASE envelope; historical `num_emitted_mux_if_blocks @ 1.15` attributions intact; book example JSONs deferred to `.17b.3`) + the repo-owned `tool_matrix --case-mux-if-gate` (`ScenarioSet::CaseMuxIfSweep` + a `case_mux_prob`-biased `case_mux_if_focus_config` × 3 strategies + `ModuleReport.emitted_case_mux_if = module_metrics.num_emitted_case_mux_if_chains > 0` [**metric-keyed**, NOT a text scan — the chain has no new identifier token] + `saw_case_mux_if_emit` + the gap arm + units floor + ~5 proofs + fixtures), templated on `--mux-if-gate`. Then `.17b.3` (user docs: book/knobs/USER_GUIDE/README/KM). Monitor RAM on heavy builds (`scripts/ram_guard.sh --threshold 90 -- <cmd>`).
- handoff: repo handoff-ready after this commit + push — `scripts/check_doctrines.sh` green (4 doctrines); `git diff --check` clean; default-off ⇒ DUT byte-identical; the eighth surface is live + fully validated (`cargo test --lib` 625 / `--bin tool_matrix` 89 / snapshots 6/6 / forced sweep Verilator-Yosys-Icarus clean + 20000-vector sim-equiv). Co-staged: `src/ir/case_mux_if_emit.rs` (new) + `src/ir/mod.rs` + `src/ir/types.rs` + `src/config.rs` + `src/main.rs` + `src/gen/mod.rs` + `src/emit/sv.rs` + `CODEBASE_ANALYSIS.md` + `DEVELOPMENT_NOTES.md` + `docs/tasks/STRUCTURED-EMISSION-EXPANSION.md` + `docs/TASK_TREE.md` + `ROADMAP.md` + `CHANGES.md` + `MEMORY.md`.
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); one runner + one classifier not two (`feedback_full_factorization`); every capability opt-in + MCP-invocable + queryable + CLI-as-shim (decision `0017`); design the API for agents not humans (`feedback_api_for_agents_not_humans`); the book is the user-facing surface and must not drift (`feedback_book_doctrine`); decision/KM fact per durable capability or boundary. Doctrine-enforcement (decision `0026`): every doctrine = a deterministic check in `scripts/check_doctrines.sh`; code-scoped checks are scope-aware (non-code commits exempt).
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
