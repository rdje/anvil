# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `DOWNSTREAM-ADAPTER-EXPANSION.2a.3` commit — routed the last two downstream callers through the closed `Adapter` registry, completing `.2a` (decision `0020`). **`src/downstream/mod.rs` + `src/bin/tool_matrix.rs`; default-off / DUT byte-identical (`tests/snapshots.rs` 6/6 untouched; no introspection SCHEMA_VERSION bump).** `downstream::run_tool_spec` (the `validate_tool_specs` version axis) now dispatches via `spec.kind.adapter().run(&cx)` (Yosys `Both`→single collapse + relabel/version-stamp preserved); `tool_matrix`'s `run_module_tools`/`run_design_tools` dispatch each fixed `verilator`/`yosys`/`iverilog_compile` column through `AcceptanceTool::*.adapter().run(&cx)` (one `AdapterTarget` + a per-column `run_column` closure); six now-unused `run_*` imports dropped (primitives stay live behind the adapters). So adding `sv2v` (`.2b`) is now a registry entry + a column field + a routing line. +1 lib proof (Yosys `Both`→single collapse survives routing). Gate: lib **547/0** (+1), snapshots **6/6**, `tool_matrix` 75/0, anvil+pipeline+divergence_e2e exit 0, clippy/fmt clean, mdbook clean, check_memory_architecture + KM green, RAM-guarded. Files: `src/downstream/mod.rs` + `src/bin/tool_matrix.rs` + the tree + `docs/TASK_TREE.md` + CHANGES + DEVELOPMENT_NOTES + MEMORY. Prior: `f1388af`=`DAE.2a.2` (catalog resource); `c5d0fac`=`DAE.2a.1` (registry core); `7142fd7` (**== origin/main**, pushed). Push cadence: `origin/main` at `7142fd7` → **9 commits ahead** after this (`.2c.2`,`.2d`,`.2e`,`.2f`,`BOOK-API-REFERENCE.1`,`DAE.1`,`DAE.2a.1`,`DAE.2a.2`,`DAE.2a.3`); push at ~30 (`feedback_push_cadence`).
- active_work_unit: **`DOWNSTREAM-ADAPTER-EXPANSION`** — `.1` ADR + `.2a` (`.2a.1` registry core / `.2a.2` catalog resource / `.2a.3` orchestrator routing) all done; frontier now **`.2b`** (sv2v accept/reject transpile column — a near-one-line registry add now that every downstream caller routes through the registry), then `.2c` (slang + the `extract_facts` JSON-AST hook); `.2d+` (surelog/UHDM, commercial-wrapper) future. Other open lanes per `docs/TASK_TREE.md`: `KNOB-ERGONOMICS-AND-PRESETS.1`, `CI-PACKAGING-DISTRIBUTION.1`, `COVERAGE-STEERED-GENERATION.1`, `CAPABILITY-BREADTH-EXPANSION.1/.2` (design-first ADRs) + no-frontier capability trees (`STRUCTURED-EMISSION-EXPANSION`, `SEMANTIC-INTROSPECTION-EXPANSION`+`AGENT-MCP-EXPANSION`, `IDENTITY-DEEPENING`, `SIGNOFF-AUTOMATION-EXPANSION`).
- next_action: **PNT — `DOWNSTREAM-ADAPTER-EXPANSION.2b`** (the first new adapter: `sv2v` as an accept/reject transpile column — a registered descriptor in `adapters()` + a `tools`-selectable + queryable verdict in `ValidateReport`/`DivergenceReport`/the matrix column; friendly absent-tool no-op + an `#[ignore]` real-tool gate; book/USER_GUIDE/README/KM card). `sv2v` is absent locally ⇒ ship structural + the no-op + the gate (the `hunt_e2e`/`divergence_e2e` precedent). Refine `.2b` acceptance at pick. Or take an owner steer if one is given.
- handoff: repo handoff-ready — code state green after this commit (`cargo test --lib` **547/0**, snapshots 6/6, `tool_matrix` 75/0, anvil+pipeline+divergence_e2e exit 0; introspection schema `1.11`); `mdbook build` clean; `check_memory_architecture` + KM gen/check green (**KM 51 facts**). Working tree clean after this commit.
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); one runner + one classifier not two (`feedback_full_factorization`); every capability opt-in + MCP-invocable + queryable + CLI-as-shim (decision `0017`); design the API for agents not humans (`feedback_api_for_agents_not_humans`); the book is the user-facing surface and must not drift (`feedback_book_doctrine`); decision/KM fact per durable capability or boundary.
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
