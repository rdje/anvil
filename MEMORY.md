# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `BUG-HUNT-ORCHESTRATION.2c` commit (**code, `src/mcp/`** + book/live-docs) — the **MCP `hunt` controlled tool**: `tools_list` gains `hunt_schema` + descriptor, `tools_call` a `"hunt"` arm → `run_hunt` (shim over `hunt::run`; sandbox fixed to OS temp; `bundle_root=None` — the MCP path serves reproducers from the **cache**, decision `0004`). `cache_hunt_failures` populates `self.cache` per finding `run_id` (original + minimized via `downstream::introspect_dut_artifact`) so `anvil://artifact/<run_id>/{sv,introspection}` resolve; one top-level `hunt` audit record. Shared `parse_max_oracle_calls` (reused by `minimize`, byte-identical) + `parse_hunt_seeds`/`parse_bool_arg`. No introspection schema bump. Default `anvil` build / DUT byte-identical. Prior: `a7b011f` = `.2b.2b` (reproducer-bundle emitter); `a5458ef` = `.2b.2a`; `5ca0006` = `.2b.1` fix; `f4fc9c8` = `.2b.1`; `8a4fbab` = `.2a`; `b8c1e8b` = `.1` (decision `0018`); … `b90bdff` = `.5` (**pushed** — `origin/main` at `b90bdff`). Working tree clean. Push cadence: `origin/main` at `b90bdff` → **22 commits ahead**; push at ~30 (`feedback_push_cadence`).
- active_work_unit: **`BUG-HUNT-ORCHESTRATION`** — `.1` ADR / `.2a` / `.2b.1` / `.2b.2a` / `.2b.2b` (engine) / `.2c` (MCP `hunt` tool) all **done**. Frontier **`.2d`** = the **`anvil hunt` CLI subcommand** (ANVIL's **first** subcommand) — a thin shim over `hunt::run` with `--out <dir>` directing the on-disk reproducer bundle (`bundle_root`, the human convenience; MCP stays cache-only), parsing `--lane`/`--seed`/`--seeds`/`--config`/`--tools`/`--yosys-mode`/`--no-minimize`/`--budget`/`--diff-sim`; **byte-identical default-path guard** (the flat `anvil --seed N …` generate path unperturbed ⇒ `tests/snapshots.rs` 6/6 + `book_examples` 3/3 unchanged). Then `.2e` (real-tool e2e gate + full book/USER_GUIDE/README/KM closeout, closes the tree). Design invariants: thin orchestrator (no new detector/minimizer), sandbox caller-set (decision `0004`), every report field SCHEMA-DERIVED (decision `0017`). Six other lanes still `active` design-first (`ACCEPTANCE-DIVERGENCE-HUNTING`, `DOWNSTREAM-ADAPTER-EXPANSION`, `KNOB-ERGONOMICS-AND-PRESETS`, `CI-PACKAGING-DISTRIBUTION`, `COVERAGE-STEERED-GENERATION`, `CAPABILITY-BREADTH-EXPANSION`); plus no-frontier `STRUCTURED-EMISSION-EXPANSION` (5 surfaces; 6th = future `.11`), `SEMANTIC-INTROSPECTION-EXPANSION` + `AGENT-MCP-EXPANSION`, `IDENTITY-DEEPENING`, `SIGNOFF-AUTOMATION-EXPANSION`, `LOCAL-REFERENCE-CACHE`. Index: `docs/TASK_TREE.md`.
- next_action: **PNT — `BUG-HUNT-ORCHESTRATION.2d`** (the `anvil hunt` CLI subcommand). Touches `src/main.rs` (CLI) ⇒ full `COMMIT.md` cargo gate + the **byte-identical default-path guard** (snapshots 6/6 + book_examples 3/3). Shim over `hunt::run`; `--out <dir>` ⇒ `HuntRequest.bundle_root` (the on-disk bundle is the human CLI convenience; the MCP tool stays cache-only). Keep the existing flat-flag generate invocation untouched (a subcommand, not a new global flag).
- handoff: repo handoff-ready — `.2c` all gates green: `cargo check/clippy/fmt` clean, `cargo test` green (lib `mcp::` **66/0** [+5 hunt proofs] / `hunt::` 11/11 / `downstream::` 20/0 / tool_matrix 71+1 ignored / book_examples 3/3 / snapshots **6/6 byte-identical**; introspection schema `1.11`, unchanged). `check_memory_architecture` + KM gen/check green (KM 46 facts). Working tree clean.
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); every capability opt-in; the hunt adds **no** new generator path / no detector / no minimizer (it composes the existing surfaces); decision/KM fact per durable capability or boundary.
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
