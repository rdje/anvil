# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `DOWNSTREAM-ADAPTER-EXPANSION.2c.2b` commit — the live `extract_facts` fact-surfacing in the `tool_matrix` slang report (decision `0020`); **closes `.2c.2`, `.2c`, and `.2` (the implementation)**. **`src/bin/tool_matrix.rs` + `tests/slang_e2e.rs` + docs/KM; default-off / banked reports + `--resume` + snapshots 6/6 byte-identical (incl. `CoverageSummary` unchanged).** `ModuleReport`/`DesignReport.slang_facts: Option<AdapterFacts>` (serde default + skip_serializing_if ⇒ off the wire when None); the slang column builds its `AdapterRunCx` explicitly so the same cx feeds `AcceptanceTool::Slang.adapter().extract_facts(&cx,&inv)` over the `<stem>.slang.json` side file (`ModuleToolColumns` 5→6). `tests/slang_e2e.rs` `#[ignore]` gate now keeps the sandbox + asserts real-slang facts (top + ≥1 port). **Scope refinement: the planned `saw_slang_facts` CoverageSummary fact was DROPPED** — a plain bool changes every report's CoverageSummary (verified 18× in a no-op smoke); the off-the-wire `slang_facts` field meets the requirement; sv2v added no coverage fact. Replaced by `slang_facts_serialize_only_when_present`. Docs updated "follow-up"→"surfaced under slang_facts". Gate: tool_matrix **78/0**, slang_e2e portable 1/0, snapshots **6/6**, lib unaffected (bin-only), no-op smoke ⇒ 0 slang/slang_facts/saw_slang_facts keys, clippy/fmt clean, mdbook clean, mem-arch + KM green (53). Prior: `4865a3c`=`DAE.2c.2a`; `d9f251e`=`DAE.2c.1`; `7142fd7` (**== origin/main**, pushed). Push cadence: `origin/main` at `7142fd7` → **14 commits ahead** after this; push at ~30 (`feedback_push_cadence`).
- active_work_unit: **`DOWNSTREAM-ADAPTER-EXPANSION` — `.2` implementation COMPLETE, no current frontier.** `.1` ADR + `.2a` (registry + catalog) + `.2b` (sv2v) + `.2c` (slang: `.2c.1` adapter + extract_facts hook + MCP / `.2c.2a` tool_matrix column / `.2c.2b` live fact-surfacing) all done. The tree stays `active` (open-ended) but has **no current frontier** — `surelog`/UHDM + a generic commercial-wrapper adapter are `.2d+` future picks (each its own pick; nothing retired; the `STRUCTURED-EMISSION-EXPANSION` precedent). **Next PNT pick is a NEW lane** (pick-and-roll, `feedback_pick_and_roll_at_no_frontier`). Candidate open lanes per `docs/TASK_TREE.md`: `KNOB-ERGONOMICS-AND-PRESETS.1`, `CI-PACKAGING-DISTRIBUTION.1`, `COVERAGE-STEERED-GENERATION.1`, `CAPABILITY-BREADTH-EXPANSION.1/.2` (all design-first ADRs — `.1` leaves) + the no-frontier capability trees (`STRUCTURED-EMISSION-EXPANSION`, `SEMANTIC-INTROSPECTION-EXPANSION`+`AGENT-MCP-EXPANSION`, `IDENTITY-DEEPENING`, `SIGNOFF-AUTOMATION-EXPANSION`) + a `DAE.2c` (slang) `.2d+` adapter pick.
- next_action: **PNT — pick the next lane** (no current DAE frontier). Recommended: an owner-directed usability lane's design ADR — `KNOB-ERGONOMICS-AND-PRESETS.1` (promote config-only knobs to CLI flags + a `--profile` preset registry, all MCP-steerable; decision `0017`) or `CI-PACKAGING-DISTRIBUTION.1` (a CI wrapper that fuzzes a maintainer's tool — itself a candidate adapter, synergistic with DAE). Each is a design-first ADR leaf (docs-only, no code), so it is a clean low-risk next pick. Alternatively a `DAE.2d` adapter (surelog/UHDM) once a binary is locally installable. Refine the picked leaf's acceptance at pick.
- handoff: repo handoff-ready — code state green after this commit (`cargo test --bin tool_matrix` **78/0**, slang_e2e portable 1/0, snapshots 6/6, `cargo test --lib` 553/0; introspection schema `1.11`); `mdbook build` clean; `check_memory_architecture` + KM gen/check green (**KM 53 facts**). Working tree clean after this commit.
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); one runner + one classifier not two (`feedback_full_factorization`); every capability opt-in + MCP-invocable + queryable + CLI-as-shim (decision `0017`); design the API for agents not humans (`feedback_api_for_agents_not_humans`); the book is the user-facing surface and must not drift (`feedback_book_doctrine`); decision/KM fact per durable capability or boundary.
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
