# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `KNOB-ERGONOMICS-AND-PRESETS.2a` commit — the **impl design-detail** for the knob-ergonomics lane (decision `0021`). **Docs-only — `DEVELOPMENT_NOTES.md` + tree + `docs/TASK_TREE.md` + CHANGES + MEMORY; no `src/` change, DUT byte-identical, snapshots not run.** Pins the Rust shapes: **the preset carrier IS `Overrides`** (one applier, not two; gains `Default`+`Serialize`(skip-None)); `presets()->Vec<Preset>` registry (single source of truth); **one shared `resolve_config(base, profile, overrides, seed)`** for CLI+MCP (order `default|--config → profile → explicit → seed`); `config_from_args` + MCP schemas gain a `profile` arg; the SCHEMA-DERIVED `knob_catalog()` (`KnobInfo` + metadata table) with a **completeness test** (`catalog names == Config::default() serde keys`) mirroring `adapter_catalog()`, surfaced as a new `anvil://catalog/knob-schema` (raw `anvil://catalog/knobs` kept). `.2b` re-split → `.2b.1`(registry+resolver+`--profile`+16 promoted `Option` CLI flags+byte-stable proofs) / `.2b.2`(catalog+completeness test+`knob-schema`/`presets` resources+MCP `profile` input) / `.2b.3`(docs). Gate: mem-arch green; KM unchanged (no `answers:` edit). Prior: `e68e2d1`=`.1` (decision `0021`); `2f17147`=`DAE.2c.2b`. Push cadence: `origin/main` at `7142fd7` → **16 commits ahead** after this; push at ~30 (`feedback_push_cadence`).
- active_work_unit: **`KNOB-ERGONOMICS-AND-PRESETS` — `.1`+`.2a` DONE; frontier = `.2b.1`** (first **code** slice: the `presets()` registry + `Preset`/`lookup_preset` + `Overrides` `Default`/`Serialize` + the shared `resolve_config` + `--profile` CLI flag + the 16 promoted `Option<T>` CLI flags; proofs: no `--profile` ⇒ snapshots 6/6 byte-identical, explicit flag beats preset, `(seed,profile,overrides)` byte-stable, unknown profile errors). Tree stays `active`. (`DOWNSTREAM-ADAPTER-EXPANSION.2` complete, no frontier; `surelog`/UHDM = `DAE.2d+`.)
- next_action: **PNT — pick `KNOB-ERGONOMICS-AND-PRESETS.2b.1`** (real code, task-tree-owned per doctrine). Implement per the `.2a` DEVELOPMENT_NOTES design-detail; full COMMIT.md workflow incl. `cargo check`/`test`/`clippy`/`fmt` + `tests/snapshots.rs` 6/6 (default byte-identical) + new resolver/preset proofs. RAM-guard heavy builds (`scripts/ram_guard.sh --threshold 90 -- <cmd>`, decision `0003`).
- handoff: repo handoff-ready — `cargo check --all-targets` green (bootstrap; untouched by this docs-only slice); `check_memory_architecture` green; KM gen/check green (**KM 54 facts**); introspection schema `1.11`. Working tree clean after this commit.
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); one runner + one classifier not two (`feedback_full_factorization`); every capability opt-in + MCP-invocable + queryable + CLI-as-shim (decision `0017`); design the API for agents not humans (`feedback_api_for_agents_not_humans`); the book is the user-facing surface and must not drift (`feedback_book_doctrine`); decision/KM fact per durable capability or boundary.
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
