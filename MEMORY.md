# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `SV-VERSION-TARGETING.3b.1` commit (hash backfills next update; prior: `154b2a7` = `.3a`; `ef3d94e` = `.2b.2b`; `e3c2f4b` = `.2b.2a`). Working tree clean after this commit.
- active_work_unit: **`SV-VERSION-TARGETING`** (owner-directed capability lane, **`active`**, opened `2026-06-15`). `.1`+`.2`+`.3a` done. **`.3b` split into `.3b.1` (design-detail — done) + `.3b.2` (impl, `proposed`).** Frontier leaf: **`.3b.2`**. Two sibling lanes registered `proposed`: `STRUCTURED-EMISSION-EXPANSION`, `SEMANTIC-INTROSPECTION-EXPANSION`. Index: `docs/TASK_TREE.md`.
- next_action: continue PNT (no self-pause — `feedback_no_self_pause_until_trees_closed`) → implement **`.3b.2`** (first ANVIL **code** up-opt): a default-off `Config` knob (working name **`soft_union_slice_prob`**, default `0.0`) rolled rules-first; when it fires AND `cfg.sv_version.permits(Sv2023)` AND a gate is a **proper low-bits `Slice`** (`GateOp::Slice { hi, lo: 0 }`, non-constant multi-bit source, `hi < W-1`), render it via an internal `union soft` overlay instead of `src[hi:0]`: `typedef union soft { logic[W-1:0] w; logic[hi:0] n; } u; assign u.w = src;` then drive the slice wire from `u.n` (behaviour-preserving — packed-union members are LSB-aligned, `u.n == src[hi:0]`). Emit in `src/emit/sv.rs` gate decl/assign region (NOT in `render_gate`, which stays a pure expression); `render_gate` `Slice` arm at `sv.rs:1040`. Default-off / **byte-identical** (snapshots 6/6 untouched). Update `tests/sv_version.rs` (byte-identity off + across versions; **divergence at Sv2023** when on) + `tests/sv_version_downstream.rs` (verilator `--language 1800-2023` accepts/builds; **confirm `--lint-only` warning-clean**) + a matrix up-opt scenario + `saw_sv_version_2023_soft_union_upopt` fact (Verilator-only; Yosys/Icarus recorded no-op) + book(knobs/sv-version)/USER_GUIDE/README/ROADMAP + KM fact. Alternatively resume parked `IDENTITY-DEEPENING.3b.2b` per table order if owner re-prioritizes.
- `.3b.1` delivered (DOCS-ONLY, no source change): resolved `0010`'s mechanism open question. **Rejected the `AggregateKind` union sibling** (`src/ir/aggregate.rs`'s boundary projection is sound only because packed struct/array = concatenation bit-equivalence, which a union violates — width `max`, aliased bits) **and the port-boundary fold** (changes the interface; generation-time, large blast radius; deferred breadth). Chose the internal `union soft` overlay of a low-bits `Slice` (above). `DEVELOPMENT_NOTES.md` design-detail + tree split. (`.3a`: decision `0010` named the construct + the installed-tools-don't-enforce-1800-version finding.)
- **PARKED (not abandoned): `IDENTITY-DEEPENING`** (`active`, frontier `.3b.2b`) — cross-module whole-module sequential equivalence, **fully designed** (decision `0008` + `.3b.1` detail + `.3b.2a` `bisimulation_partition` helper landed `edae1b0`). Resume from `docs/tasks/IDENTITY-DEEPENING.md` `.3b.2b`. Owner redirected priority to `SV-VERSION-TARGETING`.
- lane invariants (both lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); every capability is an opt-in knob; decision/KM fact per durable capability or boundary.
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`. `.3a`/`.3b.1` are docs-only (self-checks only). The next code leaf (`.3b.2` impl) touches `src/config.rs`+`src/emit/sv.rs` (+ maybe `src/bin/tool_matrix.rs`/`tests/sv_version*.rs`) ⇒ run snapshots 6/6 + `tests/sv_version` + `tests/sv_version_downstream` (`#[ignore]`) + clippy/fmt + the matrix up-opt gate. Push cadence: `origin/main` at `381ec01`; **27 commits ahead after this commit — push after 30 accumulate** (`feedback_push_cadence`).

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
