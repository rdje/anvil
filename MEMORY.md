# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `LIVE-DOC-DRIFT-FIX.2` **mdBook schema/tool-count/query-list/surface-count drift** commit — a **docs-only** change (mdBook + task-tree files), task-tree-owned by `.2`; **DUT byte-identical** (no `src/`; no runnable bash blocks changed ⇒ `tests/book_examples.rs` unaffected). Closes the `LIVE-DOC-DRIFT-FIX` tree. The bootstrap mdBook deep-read found six stale `book/src/` spots: `api-reference.md` (current schema `1.14 → 1.22` + MINOR-bump changelog through `node_readers`; "9 tools" → "10 tools" adding `coverage`), `api-resources-prompts.md` (analysis resource 4 → 9 queries), `synthesizability.md` (stale "only pure function if ever used" → behaviour-preserving emit-projections, cross-linked), `structured-emission.md` (intro 8 → nine surfaces), `introduction.md` (+ Structural Rules chapter). `mdbook build` clean. Validation: `bash scripts/check_doctrines.sh` green (docs commit ⇒ code-scoped checks exempt). Prior: `d4fc14e` (`.1`). Push cadence: `origin/main` at `4564533` → **28 commits ahead** after this ⇒ no push (threshold 30, `feedback_push_cadence`).
- active_work_unit: **None at a frontier — back at a PNT decision point.** `LIVE-DOC-DRIFT-FIX` is **closed** (`.1` ROADMAP + TOOLBOX, `.2` mdBook). `SEMANTIC-INTROSPECTION-EXPANSION` + `STRUCTURED-EMISSION-EXPANSION` both stay `active` at NO-FRONTIER boundaries (nine derived analyze queries @ schema `1.22`; nine structured surfaces delivered). All flagged + bootstrap-found doc-hygiene debt now cleared; roadmap/codebase/mdBook fully realigned (verified via two bootstrap deep-read passes — book + codebase; `CODEBASE_ANALYSIS.md`/`DEVELOPMENT_NOTES.md` confirmed current).
- next_action: **PNT — leading candidate is `SEMANTIC-INTROSPECTION-EXPANSION.11` (a tenth derived `analyze` query), grounded this session.** Bootstrap codebase deep-read recommends **`instance_provenance`** — the **third opaque-leaf boundary-opener** (after `memory_provenance`/`fsm_provenance`): for a child-instance output (an opaque `InstanceOutput` leaf in all nine current queries) report the child module's matching output port + that output's support cone **inside the child module's node graph**, reusing `build_cone` unchanged. **Design-only** (needs the child-definition table — a bare `Module` lacks it). Candidate (a) per-FSM/per-memory reach was assessed **redundant** with `input_reach`. Touch-points: `src/introspect/analyze.rs` (new `QUERY_INSTANCE_PROVENANCE` const + `InstanceProvenance` struct + tenth parallel vec + `instance_provenance_with` + `design_instance_provenance` + `supported_query_kinds` + every `DerivedAnalysis{}` literal gets the new field); `src/mcp/mod.rs` `run_analyze` (design arm dispatch + empty-check + `analyze_schema` enum + target desc); `src/introspect/mod.rs` `SCHEMA_VERSION` `1.22 → 1.23`; book/USER_GUIDE/AGENT_INTROSPECTION_SCHEMA/README/KM. Split per lane pattern: `.11a` design-detail (docs) → `.11b.1` pure core + ~10 unit proofs → `.11b.2` MCP surface + schema bump + e2e `anvil-mcp` smoke → `.11b.3` docs. Alternatives: `STRUCTURED-EMISSION-EXPANSION` tenth construct (nested `generate`/`interface` — both currently blocked/disqualified; needs fresh decision + probe), `IDENTITY-DEEPENING`, `SIGNOFF-AUTOMATION-EXPANSION`. Doctrine: **task-tree-own `.11a` BEFORE any `src/` edit**; default-off/SCHEMA-DERIVED/DUT byte-identical. Monitor RAM (`scripts/ram_guard.sh --threshold 90 -- <cmd>`).
- handoff: **repo handoff-ready (FRESH-SESSION boundary).** `scripts/check_doctrines.sh` green (4 doctrines); `git diff --check` clean; tree clean; DUT byte-identical (this session was docs-only). Full bootstrap complete this session (all live docs + two delegated deep-read passes: mdBook + `src/`); ALL bootstrap-found drift closed across ROADMAP/TOOLBOX (`LIVE-DOC-DRIFT-FIX.1`, `d4fc14e`) + six `book/src/` chapters (`.2`, `45a22fc`); `CODEBASE_ANALYSIS.md`/`DEVELOPMENT_NOTES.md` verified current. origin/main at `4564533`; **28 commits ahead** (push threshold 30 ⇒ still no push — ~2 commits from a push). **Next slice is fully designed (see next_action): `SEMANTIC-INTROSPECTION-EXPANSION.11a` → `instance_provenance` tenth `analyze` query.** A fresh session should land `.11a` (design-detail, docs — mirror the `.10a` cadence: DEVELOPMENT_NOTES Q-resolution + task-tree `.11`/`.11a`/`.11b` split, no new decision, decision `0011` governs) then `.11b.1`/`.11b.2`/`.11b.3` (the Rust impl + MCP surface + schema `1.22 → 1.23` + docs), each per COMMIT.md.
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); one runner + one classifier not two (`feedback_full_factorization`); every capability opt-in + MCP-invocable + queryable + CLI-as-shim (decision `0017`); design the API for agents not humans (`feedback_api_for_agents_not_humans`); SEMANTIC-INTROSPECTION stays SCHEMA-DERIVED / no-shadow-simulator (decisions `0004`/`0011`); the book is the user-facing surface and must not drift (`feedback_book_doctrine`); decision/KM fact per durable capability or boundary. Doctrine-enforcement (decision `0026`): every doctrine = a deterministic check in `scripts/check_doctrines.sh`; code-scoped checks are scope-aware (non-code commits exempt).
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
