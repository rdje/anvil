# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `IDENTITY-DEEPENING.3b.2a` commit (hash backfills next update; prior: `762cf46` = `IDENTITY-DEEPENING.3b.1`; `ce46141` = `.3a`). Working tree clean after this commit.
- active_work_unit: **`IDENTITY-DEEPENING`** (Lane 1, **`active`**). `.1`+`.2` done; `.3a` design (decision `0008`) done; `.3b.1` design-detail done; `.3b.2` split into **`.3b.2a` refactor (done)** + **`.3b.2b` cross-module feature (future)**. Frontier leaf: **`.3b.2b`**. Index: `docs/TASK_TREE.md`.
- next_action: **`IDENTITY-DEEPENING.3b.2b`** — the cross-module feature on top of the `.3b.2a` `bisimulation_partition` helper. Steps: (1) `modules_bisimilar(A,B)`: build a throwaway **combined Module** (A.nodes++B.nodes with B offset + refs remapped; A.flops⊎B.flops), call `bisimulation_partition(combined)` for the `rep_map`, verdict = interfaces match by `(PortId,width)` AND every output port `p`: `cone_proof(combined, driveA(p), rep_map) == cone_proof(combined, driveB(p)+offset, rep_map)` (NO bijection); (2) `dedup_sequential_modules` beside `dedup_semantic_modules` in `src/ir/dedup.rs` — flops-only leaf candidates (skip memory/FSM/instance/param/aggregate + any resetless flop, skip top), pre-filter by (interface, flop multiset, output count), pairwise `modules_bisimilar` + union-find, reuse the lex-survivor / `rewrite_instance_module_names` / iterate-to-fixpoint / `prune_modules_made_unreachable` tail; (3) default-off `hierarchy_sequential_module_dedup` knob (Config/Module/Design, no CLI flag — mirror `hierarchy_semantic_module_dedup`) gated node-id+e-graph; (4) `DesignMetrics::sequential_module_proof_signatures` + `num_sequentially_duplicate_module_pairs`; (5) wire into `src/gen/mod.rs` finalization after structural + combinational dedup; (6) rules-first gate (permuted/cross-wired equal-reset stateful leaves collapse 2→1, both structural+combinational dedup leave 2, downstream-clean Verilator + both Yosys) + knob-off snapshots 6/6 byte-identical; (7) book/src/factorization.md + hierarchy.md, USER_GUIDE/knobs, ROADMAP gap 2, CODEBASE_ANALYSIS + KM card. Cap `N_bisim_module_flops` (~64). Continue PNT (no self-pause — `feedback_no_self_pause_until_trees_closed`).
- `.3b.2a` delivered (code, byte-identical): factored `merge_bisimilar_flops`'s "bucket→partition→refine" core into a non-mutating `bisimulation_partition(&Module) -> Option<Vec<Vec<FlopId>>>` (None preserves the old `!has_refinable` early return); `merge_bisimilar_flops` keeps guards + collapse + `finalize_flop_merge`. snapshots 6/6 + 6 bisim tests + clippy/fmt clean. The helper is what `.3b.2b` calls on the combined module.
- lane invariants: proof-not-resemblance bar; rules-first / no generate-then-filter; bounded by support/node/work budget (12-bit / 128-node / 131072-work) with structural fallback; **no retirement** of any landed merge class; default-off / byte-identical where a merge could change RTL; `--identity-mode relaxed` stays the real off-switch; single identity source stays `src/ir/compact.rs` + `src/ir/dedup.rs`.
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 88` for heavy builds; `.3b.2a` ran `cargo build`/`fmt --check`/`clippy -D warnings`/`test --test snapshots`/`test --lib bisim` (all clean); full slow `pipeline` suite not run (byte-identical default path proven by snapshots 6/6) per `0003-resource-safe-validation`; monitor RAM, stop >90%. Push cadence: `origin/main` at `381ec01`; **20 commits ahead after this commit — under the ~30 threshold, no push yet** (`feedback_push_cadence`).

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
