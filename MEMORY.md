# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `STRUCTURED-EMISSION-EXPANSION.10b.1` commit (live surface — the FIRST source change of the fifth surface: the multi-gate-cone `function automatic` emit-projection; `cone_function_emit_prob` knob + `Module.cone_function_gates` + new `src/ir/cone_function_emit.rs` + 2 gen rolls + emitter + 8 lib proofs; **default-off / DUT byte-identical, snapshots 6/6, lib 493→501**; frontier → `.10b.2`). Prior: `2d3a0ca` = `.10a`; `46a377e` = `.9` (decision `0016`); `afb5718` = `.8b`; `3ec4f66` = `.7` (decision `0015`); … `b90bdff` = `.5` (**pushed** — `origin/main` is at `b90bdff`). Working tree clean after this commit. Push cadence: `origin/main` at `b90bdff` → **12 commits ahead (`.6a`,`.6b.1`,`.6b.2a`,`.6b.2b`,`.6b.3`,`LIVE-DOC-CODEBASE-ALIGNMENT.2`,`.7`,`.8a`,`.8b`,`.9`,`.10a`,this `.10b.1`); push at ~30** (`feedback_push_cadence`).
- active_work_unit: **`STRUCTURED-EMISSION-EXPANSION` tree `active`, frontier = `.10b.2`**. **FOUR** structured surfaces delivered end-to-end: `function automatic` (`.1`+`.2`), `generate for` loop (`.3`+`.4`), `task automatic` (`.5`+`.6`), wider-lane `generate for` part-select (`.7`+`.8`, decision `0015`). The **FIFTH** surface (the **multi-gate-cone `function automatic`**, decision `0016`) is shipping: `.9` design + `.10a` design-detail + `.10b.1` **live surface** all done; `.10b.2` (metric `num_emitted_cone_functions` + schema `1.10→1.11` + the `--cone-function-gate`) + `.10b.3` (user docs) pending. Future surfaces (multi-output tasks / nested-multi-level `generate` / `interface`-`modport`) are `.11+`. Index: `docs/TASK_TREE.md`.
- next_action: **PNT — implement frontier leaf `STRUCTURED-EMISSION-EXPANSION.10b.2`** (`.10b.2` owns it): (1) `Metrics::num_emitted_cone_functions` (= `m.cone_function_gates.len()`, `#[serde(default)]`) surfaced in introspection `module_metrics` ⇒ `SCHEMA_VERSION` `1.10 → 1.11` (bump all current-output schema refs: the schema_version test assertions + `docs/AGENT_INTROSPECTION_SCHEMA.md` changelog + README + USER_GUIDE + the 5 `book/src/agent-mcp.md` example JSONs + the `CODEBASE_ANALYSIS.md` envelope line; leave historical "landed at schema X" attributions intact) + a lib proof; (2) the repo-owned `tool_matrix --cone-function-gate` (`ScenarioSet::ConeFunctionSweep` + `build_cone_function_sweep_scenarios`/`cone_function_focus_config` [comb-only `cone_function_emit_prob=1.0` × 3 strategies] + `ModuleReport.emitted_cone_function` [`__cf(` SV-text detection] + `saw_cone_function_emit` + `MatrixReport.cone_function_gate` + early-return gap arm + 5 proofs + fixture updates + `test_cli` default) banked clean (Verilator + both Yosys + Icarus) + README/USER_GUIDE/CODEBASE_ANALYSIS gate entries. Templated on `--task-emit-gate`. Default-off / DUT byte-identical (snapshots 6/6). Then `.10b.3` = the user-facing book/knobs/USER_GUIDE/README/KM closeout. Doctrine: each is task-tree-owned.
- handoff: repo handoff-ready — `cargo check --lib`/`clippy -D warnings`/`fmt --check` clean; `cargo test --lib` 501 + snapshots 6/6 byte-identical; the fifth surface's live emitter is downstream-clean (forced sweep `/tmp/anvil-cf-sweep/`, 18 cone functions, Verilator `-Wall` Δ=0 + both Yosys + Icarus); tree clean; all self-checks green; KM at 43 facts / 351 keys. The fifth surface's **live emitter ships** (`.9`+`.10a`+`.10b.1`); `.10b.2` (metric+gate) + `.10b.3` (docs) remain — both well-specified in the task tree.
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); every capability is an opt-in knob (the fifth surface gets its OWN `cone_function_emit_prob` so the single-gate surface stays byte-identical); decision/KM fact per durable capability or boundary.
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
