# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `USABILITY-LANE-OWNERSHIP.1` commit (**docs/workflow-only registration**: 7 owner-directed lanes task-tree'd + decision `0017` API-first mandate, before any code). Created `docs/decisions/0017-api-first-everything-mcp-accessible.md` + INDEX row; 7 lane trees in `docs/tasks/` (`BUG-HUNT-ORCHESTRATION`, `ACCEPTANCE-DIVERGENCE-HUNTING`, `DOWNSTREAM-ADAPTER-EXPANSION`, `KNOB-ERGONOMICS-AND-PRESETS`, `CI-PACKAGING-DISTRIBUTION`, `COVERAGE-STEERED-GENERATION`, `CAPABILITY-BREADTH-EXPANSION`) + the `USABILITY-LANE-OWNERSHIP` tree; 8 `docs/TASK_TREE.md` rows; ROADMAP §"Owner-directed usability + capability lanes (2026-06-17)". **DUT byte-identical (no `src/`).** Prior: `6125ea5` = `.10b.3`; `015a09b` = `.10b.2`; `081583d` = `.10b.1`; `2d3a0ca` = `.10a`; `46a377e` = `.9` (decision `0016`); … `b90bdff` = `.5` (**pushed** — `origin/main` at `b90bdff`). Working tree clean. Push cadence: `origin/main` at `b90bdff` → **15 commits ahead** (`.6a`…`.10b.3` = 14, + this `USABILITY-LANE-OWNERSHIP.1`); push at ~30 (`feedback_push_cadence`).
- active_work_unit: **Seven new owner-directed lanes registered + decision `0017` (API-first mandate)**, all `active` with a **design-first `.1` ADR frontier** (no code yet). Cross-cutting mandate: every meaningful feature must be MCP-accessible/controllable/steerable/queryable; deep semantic introspection first-class; the `SCHEMA-DERIVED`/no-shadow-simulator ceiling unchanged. Also still `active` (no current frontier): `STRUCTURED-EMISSION-EXPANSION` (FIVE surfaces delivered end-to-end; sixth = a future `.11` decision), `SEMANTIC-INTROSPECTION-EXPANSION` + `AGENT-MCP-EXPANSION` (the cross-cutting API-completeness homes), `IDENTITY-DEEPENING`, `SIGNOFF-AUTOMATION-EXPANSION`, `LOCAL-REFERENCE-CACHE`. Index: `docs/TASK_TREE.md`.
- next_action: **PNT — pick a lane `.1` design ADR** (each new lane is design-first; a decision record + tree split BEFORE any code, per doctrine). Owner-recommended highest-leverage first = **`BUG-HUNT-ORCHESTRATION.1`** (the turnkey fuzz→minimize→reproducer loop; the "single biggest usability multiplier"), with `KNOB-ERGONOMICS-AND-PRESETS.1` + `DOWNSTREAM-ADAPTER-EXPANSION.1` feeding it; `ACCEPTANCE-DIVERGENCE-HUNTING.1` complements `--diff-sim`; `CAPABILITY-BREADTH-EXPANSION.1` (SV up-opts) + `.2` (Mealy FSM) continue closed lanes. EVERY lane leaf must satisfy the decision-`0017` API-completeness gate (controls MCP-settable, actions MCP-invocable, facts SCHEMA-DERIVED queryable, documented). The deferred sixth structured surface (multi-output `task`) remains a future `STRUCTURED-EMISSION-EXPANSION.11` decision.
- handoff: repo handoff-ready — this slice is docs/workflow-only (no `src/`), so `cargo check/clippy/fmt/test` are unaffected and the code state is exactly the green `.10b.3` state (lib 502 / tool_matrix 73 / book_examples 3/3 / snapshots 6/6 byte-identical; introspection schema `1.11`). `check_memory_architecture` + KM gen/check green (KM now 45 facts — the new `0017` card). Working tree clean; all 8 new trees + decision `0017` + ROADMAP/INDEX/MEMORY/CHANGES landed.
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); every capability is an opt-in knob (the fifth surface gets its OWN `cone_function_emit_prob` so the single-gate surface stays byte-identical); decision/KM fact per durable capability or boundary.
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
