# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `STRUCTURED-EMISSION-EXPANSION.15a` **procedural if/else mux impl design-detail** commit — docs-only / no source / **DUT byte-identical**. The seventh-surface (decision `0027`, procedural `always_comb` `if`/`else` projection of a `Mux`) impl design-detail: a `DEVELOPMENT_NOTES.md` entry grounding the seven impl points in the real `src/ir/task_emit.rs` (candidate-predicate + collect-then-roll template), `src/emit/sv.rs` (`node_ref` `:1242`/used `:644`; task decl/call `:444`; gate-assign-loop passthrough `:578`; procedural-block loop `:620`), `src/gen/mod.rs` (the six-pass roll chain `:95`/`:330`), `src/config.rs`, `src/ir/types.rs`. Pinned: the `GateOp::Mux` predicate excluding the union of all six sibling marks (pass runs **last** ⇒ no other pass changes); `Module.mux_if_gates: BTreeSet<NodeId>`; the emitter `always_comb if/else` `<name>__cv` block + the gate-assign-loop passthrough (keeps `<name>` a net); `mux_if_emit_prob` knob + `--mux-if-emit-prob` flag; `num_emitted_mux_if_blocks` metric (schema `1.14→1.15` at `.15b.2`); `--mux-if-gate`/`saw_mux_if_emit` (`__cv` detection); the byte-identical/RNG argument. Tree `.15`/`.15a` leaves + `docs/TASK_TREE.md` row + `ROADMAP.md` lane #2 + `CHANGES.md`. Prior: `caf3d7d`=`…14`. Push cadence: `origin/main` at `605ec44` → **23 commits ahead** after this ⇒ no push (threshold 30, `feedback_push_cadence`).
- active_work_unit: **`STRUCTURED-EMISSION-EXPANSION` — frontier `.15b.1`** (the live seventh-surface source change; `.14` design + `.15a` design-detail both `done`). `.15b.1` = first source change: `Config::mux_if_emit_prob` + `--mux-if-emit-prob` + `Module.mux_if_gates: BTreeSet<NodeId>` + new `src/ir/mux_if_emit.rs` (`annotate_mux_if_gates`, the `GateOp::Mux` predicate excluding all six sibling marks) + `src/ir/mod.rs` registration + two guarded `gen/mod.rs` rolls (run **last**, after `cone_function`) + `src/emit/sv.rs` procedural-block section (`logic [w-1:0] <name>__cv;` + `always_comb if/else`, operand refs via `node_ref`) + the gate-assign-loop `<name>__cv` passthrough + ~6 lib proofs. No metric/schema bump (the `.15b.2` metric bumps `1.14→1.15`; the knob rides the version). Then `.15b.2` (metric + `--mux-if-gate`) + `.15b.3` (user docs). Other `active` lanes at no-frontier boundaries.
- next_action: **PNT → `STRUCTURED-EMISSION-EXPANSION.15b.1`** (the live source change per the `.15a` DEVELOPMENT_NOTES plan). This is the **first `src/` change of the loop** — full `cargo check --all-targets` + `cargo clippy --all-targets -- -D warnings` + `cargo fmt --all --check` + `cargo test --lib` + `cargo test --test snapshots` (6/6 byte-identical, default-off) required, plus a forced `mux_if_emit_prob=1.0` downstream sweep (Verilator `-Wall` Δ=0 vs OFF + both Yosys + Icarus + iverilog sim-equiv to the inline ternary). The owning task-tree leaf `.15b.1` already exists (pre-split) — code change is task-tree-owned. Monitor RAM on heavy builds (`scripts/ram_guard.sh --threshold 90 -- <cmd>`).
- handoff: repo handoff-ready after this commit — `scripts/check_doctrines.sh` green (4 doctrines); `git diff --check` clean; docs-only so `cargo`/snapshots untouched ⇒ DUT byte-identical, no `src/`. `DEVELOPMENT_NOTES.md` + tree file + `docs/TASK_TREE.md` + `ROADMAP.md` + `CHANGES.md` + `MEMORY.md` co-staged. (KM unchanged — `0027` already carries `answers:`.)
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); one runner + one classifier not two (`feedback_full_factorization`); every capability opt-in + MCP-invocable + queryable + CLI-as-shim (decision `0017`); design the API for agents not humans (`feedback_api_for_agents_not_humans`); the book is the user-facing surface and must not drift (`feedback_book_doctrine`); decision/KM fact per durable capability or boundary. Doctrine-enforcement (decision `0026`): every doctrine = a deterministic check in `scripts/check_doctrines.sh`; code-scoped checks are scope-aware (non-code commits exempt).
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
