# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `STRUCTURED-EMISSION-EXPANSION.6b.1` commit (the third structured surface goes LIVE — a combinational `task automatic` emit-projection; **first source change since `.4b.1`**). Prior: `7b6a500` = `.6a` (design-detail); `b90bdff` = `.5` (decision `0014`, **pushed** — `origin/main` is at `b90bdff`); `909c82a` = `.4b.3`. Working tree clean after this commit. Push cadence: `origin/main` at `b90bdff` → **3 commits ahead after this commit (`.6a`+`.6b.1` local, plus this); push at ~30** (`feedback_push_cadence`).
- active_work_unit: **`STRUCTURED-EMISSION-EXPANSION` tree `active`; `.6`/`.6b` `active` containers; frontier = `.6b.2`** (the metric + the repo-owned downstream gate). `.6b` split into `.6b.1` (live surface — **done**) + `.6b.2` (metric+gate) + `.6b.3` (docs). `.5`+`.6a` done. The FIRST (combinational `function automatic`, `.1`+`.2`) and SECOND (`generate for` loop, `.3`+`.4`) surfaces are delivered end-to-end. Future surfaces (nested/multi-level `generate`, `interface`/`modport`, richer tasks) are `.7+`, each its own decision. Index: `docs/TASK_TREE.md`.
- `.6b.1` delivered (live emitter change): `Config::task_emit_prob` (default `0.0`, config-file-only) + `Module.task_emit_gates` + new `src/ir/task_emit.rs` (`annotate_task_emit_gates` = the function-emit candidate predicate **plus** exclusion of `function_emit_gates`/`generate_loop_gates`/`soft_union_slice_gates`; `param_env` skipped) + `src/ir/mod.rs` reg + two guarded gen-time rolls in `generate_module`+`generate_design` **after** generate_loop + `src/emit/sv.rs` `task_emit_gate` accessor + a task section (`render_gate_task_decl` — `task automatic <wire>__t(output logic[W-1:0] o, input …); o = render_gate_function_body(op,widths); endtask` — **reuses `render_gate_function_body` verbatim**; `render_gate_task_call` — `logic <wire>__tv;` + `always_comb <wire>__t(<wire>__tv, refs);`) + the gate-assign-loop passthrough `assign <wire> = <wire>__tv;` + 11 lib proofs. Output-var + passthrough form (the `.6a` first cut; `<wire>` stays a net). No schema bump (knob rides `#[serde(default)]`; the metric bumps `1.9→1.10` at `.6b.2`). Default-off / DUT byte-identical (snapshots 6/6; lib 489). Forced `task_emit_prob=1.0` sweep clean: Verilator `--lint-only` + `-Wall` Δ=0 vs OFF + both Yosys + Icarus (`/tmp/anvil-te-r1/`, 5 seeds, 4–39 tasks each).
- next_action: **PNT — execute `STRUCTURED-EMISSION-EXPANSION.6b.2`** (the metric + the repo-owned gate; pre-split per the `.4b.2` precedent, do `.6b.2a` first): `.6b.2a` = `Metrics::num_emitted_combinational_tasks` (`= m.task_emit_gates.len()`, `#[serde(default)]`) in `src/metrics.rs` `compute()` + a lib proof + introspection schema MINOR bump `1.9 → 1.10` (`SCHEMA_VERSION` in `src/introspect/mod.rs` + the `schema_version` assertions in `src/introspect/mod.rs`/`src/mcp/mod.rs` + the schema doc changelog/defines/checklist + README/USER_GUIDE current-output refs + the 5 `book/src/agent-mcp.md` example JSONs + the `CODEBASE_ANALYSIS` envelope line; leave historical landing attributions intact). Then `.6b.2b` = `tool_matrix --task-emit-gate` + `ScenarioSet::TaskEmitSweep` + `build_task_emit_sweep_scenarios`/`task_emit_focus_config` (comb-only `task_emit_prob=1.0` × 3 strategies) + `ModuleReport.emitted_combinational_task` (`sv_text.contains("task automatic")`) + `saw_combinational_task_emit` + merge + early-return gap arm + 5 proofs + the ModuleReport/test_cli fixtures (templated on `--function-emit-gate`/`--generate-loop-gate`); bank clean `/tmp/anvil-task-emit-gate-r1`. Then `.6b.3` (book/knobs/USER_GUIDE/README/KM closeout). Default-off / DUT byte-identical (snapshots 6/6). Doctrine: NO code change without a task-tree leaf owning it first — `.6b.2` owns it.
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); every capability is an opt-in knob; decision/KM fact per durable capability or boundary.
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
