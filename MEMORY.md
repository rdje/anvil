# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `STRUCTURED-EMISSION-EXPANSION.11` **pick the sixth structured surface (multi-output task) + decision 0025** commit — **design/decision leaf, no source change, DUT byte-identical**. Decision `0025` + INDEX row + full task-tree registration (`.11` done, `.12` impl pending pre-split `.12a`/`.12b`) in both `docs/tasks/STRUCTURED-EMISSION-EXPANSION.md` and `docs/TASK_TREE.md`; CHANGES top entry; KM picks up `0025`. The sixth surface = a default-off multi-output combinational `task automatic` co-emitting a **mutually-fan-in-independent pair** of qualifying gates that **share a non-constant operand** into one task with **deduplicated input formals + per-member output args**, called once from `always_comb`, each member's net driven by a passthrough `assign`; reuses the cone-function dedup render primitives + the single-gate task predicate; own `multi_output_task_emit_prob` knob; `num_emitted_multi_output_tasks` metric (schema `1.13 → 1.14` at impl); `--multi-output-task-gate` / `saw_multi_output_task_emit` (`__mt(`). Soundness rule = mutual fan-in independence (else `UNOPTFLAT`). Probe `/tmp/anvil-se-motask-probe/`: 2-output task Verilator -Wall 2012/2017/2023 + Yosys both + Icarus clean + iverilog-sim-equiv (5000 vec). Prior: `df21b5c`=`CAPABILITY-BREADTH-EXPANSION.2b.3` Mealy docs; `b8ee1ce`=`…2b.2b`; `c8b0562`=`…2b.2a`. Push cadence: `origin/main` at `605ec44` → **6 commits ahead** after this ⇒ no push (threshold 30, `feedback_push_cadence`).
- active_work_unit: **`STRUCTURED-EMISSION-EXPANSION` tree `active`, frontier `.12`** (impl of the sixth surface, decision `0025`; pre-split `.12a` design-detail / `.12b` impl). `.11` design done. Five structured surfaces delivered end-to-end (function `.1`+`.2`, generate-for `.3`+`.4`, task `.5`+`.6`, wide-lane part-select `.7`+`.8`, cone-function `.9`+`.10`).
- next_action: **PNT — `STRUCTURED-EMISSION-EXPANSION.12a`** (design-detail, no source): ground decision `0025` in the real `src/ir/task_emit.rs` + `src/ir/cone_function_emit.rs` + `src/emit/sv.rs` (`render_gate_task_decl`/`render_gate_task_call`/`cone_function_params`/`cone_operand_ref`/`render_cone_gate_expr`) + `src/gen/mod.rs` roll chain + `src/config.rs`; pin the exact mutual-fan-in-independence DFS mechanism, the `Module.multi_output_task_groups: BTreeMap<NodeId,Vec<NodeId>>` carrier, the always_comb-per-group choice, the `__mt`/`__mtv` naming, and the `.12b` impl shape (`.12b.1` live / `.12b.2` metric+gate / `.12b.3` docs). Then `.12b`.
- handoff: repo handoff-ready after this commit — docs-only slice; `check_memory_architecture` + KM regen+check green (`0025` indexed); `mdbook build` clean; code unchanged since `df21b5c` (lib 589/0, tool_matrix 79/0, snapshots 6/6 from that commit still hold; `cargo check --all-targets` clean at session start).
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); one runner + one classifier not two (`feedback_full_factorization`); every capability opt-in + MCP-invocable + queryable + CLI-as-shim (decision `0017`); design the API for agents not humans (`feedback_api_for_agents_not_humans`); the book is the user-facing surface and must not drift (`feedback_book_doctrine`); decision/KM fact per durable capability or boundary.
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
