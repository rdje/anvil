# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `STRUCTURED-EMISSION-EXPANSION.4b.2a` commit (metric + schema bump — `Metrics::num_emitted_generate_loops` + introspection schema `1.8→1.9`; default-off / DUT byte-identical, a post-hoc Metrics field changes no RTL). Prior: `2bf8121` = `.4b.1` (live surface); `0f821d7` = `.4a` (design-detail). Working tree clean after this commit. Push cadence: `origin/main` at `63d2622` → **27 commits ahead after this commit; push at ~30** (`feedback_push_cadence`).
- active_work_unit: **`STRUCTURED-EMISSION-EXPANSION` tree `active`; `.4`/`.4b`/`.4b.2` are `active` containers; frontier = `.4b.2b`** (the repo-owned `tool_matrix --generate-loop-gate` + the `saw_generate_loop_emit` coverage fact + `ModuleReport.emitted_generate_loop` detection), then `.4b.3` (book/USER_GUIDE/README/KM closeout). The first surface (combinational `function automatic`, `.1`+`.2`) is delivered end-to-end. Future surfaces (`task` [leading], nested/multi-level `generate`, `interface`/`modport`) are `.5+`, each its own decision. Index: `docs/TASK_TREE.md`.
- `.4b.2a` delivered (metric + schema bump): `Metrics::num_emitted_generate_loops` (`= m.generate_loop_gates.len()`, `#[serde(default)]`) computed in `metrics::compute()`, surfaced in introspection `module_metrics` ⇒ `SCHEMA_VERSION` `1.8→1.9` in `src/introspect/mod.rs` (the metric bumps; the `.4b.1` knob rode the version, the `1.7→1.8` `num_emitted_combinational_functions` precedent). Bumped all current-output schema refs to `1.9` (9 test assertions: 2 introspect + 7 mcp; schema doc changelog+defines+checklist; README `--introspect`/`analyze`; USER_GUIDE `--introspect`; 5 book example JSONs; CODEBASE_ANALYSIS envelope). Historical landing attributions left intact (`num_emitted_combinational_functions` @ 1.8; sv-version @ 1.2). Lib proof. Default-off / DUT byte-identical (snapshots 6/6, lib 478); end-to-end `--introspect` default `0` / forced `50`.
- `.4b.1` (prior) delivered (live surface): `Config::generate_loop_emit_prob` (config-file-only, default `0.0`) + `Module.generate_loop_gates` + `src/ir/generate_loop.rs` ({N{x}} 1-bit-lane replication candidate excluding function-emit marks) + two guarded gen-time call-site rolls + `src/emit/sv.rs` `generate_loop_gate`/`render_generate_loop_block` + assign-loop suppression. `gi = gi + 1` (portable; `gi++` not retired). Forced sweep clean (`/tmp/anvil-gl-r1/`, 5 seeds, Verilator `-Wall` Δ=0 vs OFF + both Yosys + Icarus). Surface fires on 27/30 default seeds (one-hot mux-mask idiom).
- next_action: **PNT — execute `STRUCTURED-EMISSION-EXPANSION.4b.2b`** (the repo-owned `tool_matrix` gate, templated on `--function-emit-gate` / `.2b.2b`): a `--generate-loop-gate` CLI flag + `ScenarioSet::GenerateLoopSweep` + `build_generate_loop_sweep_scenarios` (a comb-only DUT forcing `generate_loop_emit_prob=1.0` × 3 construction strategies — the config MUST emit `{N{x}}` 1-bit replications, i.e. one-hot mux-mask broadcasts; `function_emit_focus_config`/`share_heavy_comb_only_config` is the shape, verify `emitted_generate_loop=true` in the bank and broaden if needed) + `MatrixReport.generate_loop_gate` wired into `select_scenario_set`/`derive_run_plan`/`build_scenarios`/`scenario_set_slug`/`artifact_kind_slug` + `ModuleReport.emitted_generate_loop` (`#[serde(default)]`, from `prepared.sv_text.contains("generate")`) + `CoverageSummary.saw_generate_loop_emit` (lit on Verilator success AND non-empty clean Yosys — full plan, a generate-for is universally synthesizable) + merge_coverage + an early-return arm in `compute_coverage_gaps` + proofs + the ModuleReport fixture updates. Bank clean `/tmp/anvil-generate-loop-gate-r1` (Verilator + both Yosys + Icarus). Then `.4b.3` docs closeout (book/knobs/USER_GUIDE/README/KM). Default-off / DUT byte-identical. Doctrine: NO code change without a task-tree leaf owning it first — `.4b.2b` owns it.
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); every capability is an opt-in knob; decision/KM fact per durable capability or boundary.
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
