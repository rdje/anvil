# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `STRUCTURED-EMISSION-EXPANSION.18` **pick CasezMux masked if/else-if priority-chain surface (decision `0029`)** commit — **DOCS-ONLY** (no `src/`/`tests/`/`examples/`/build touched) ⇒ **DUT byte-identical** + exempt from the code-scoped doctrine checks. Picked **autonomously** at the no-frontier boundary (`feedback_pick_and_roll_at_no_frontier`) the **NINTH** structured surface: a procedural `always_comb` `if`/`else if` **masked** priority chain `(sel & care_mask) == value_masked` emit-projection of a wildcard `CasezMux` gate (generalizes the eighth surface's bare-equality `CaseMux` chain, decision `0028`, to the wildcard `CasezMux`; reuses the `care_mask = (~wildcard_mask) & sel_mask` idiom from `metrics.rs`/`compact.rs`; no `__cv` passthrough — a `CasezMux` is already an `always_comb` var). **Fresh empirical probe (`scratchpad/probe`): the `sel ==? pattern` form DISQUALIFIED — Yosys `0.64` rejects `==?` in BOTH repo modes — so the lowered masked-AND form ships; it is Verilator `-Wall` 2012/2017/2023 + both Yosys (0 warnings) + Icarus clean and iverilog `vvp` EXHAUSTIVELY sim-equiv to the `casez` (128/128 disjoint + 128/128 overlapping-priority, 0 mismatches).** Generator fact: `build_casez_patterns` uses `wildcard_bits=1` ⇒ arms non-overlapping, all-wildcard arm impossible ⇒ no constant-true condition. Decision `0029` (KM front-matter) + `INDEX.md` row + regenerated `KNOWLEDGE_MAP.md` (68→69 facts) + task-tree/ROADMAP/TASK_TREE updates (parent-node SIX→EIGHT-surfaces drift fixed, children `.1`–`.19`, `.17b.3` hash backfilled `ee34e4c`). `check_doctrines.sh` green; `check_knowledge_map.sh` OK. Prior: `ee34e4c` (`.17b.3`). Push cadence: `origin/main` at `4564533` → **4 commits ahead** after this ⇒ no push (threshold 30, `feedback_push_cadence`).
- active_work_unit: **`STRUCTURED-EMISSION-EXPANSION` — NINTH surface picked (`.18` done); frontier → `.19` (impl, pre-split `.19a`/`.19b`).** Lane stays `active`. EIGHT surfaces delivered end-to-end; the ninth (the wildcard `CasezMux` → masked `if`/`else if` priority chain, decision `0029`) is designed, impl pending at `.19`.
- next_action: **PNT — implement the ninth surface, leaf `STRUCTURED-EMISSION-EXPANSION.19a`** (impl design-detail, no source change): a `DEVELOPMENT_NOTES.md` entry grounding decision `0029` in the real `src/emit/sv.rs` `GateOp::CasezMux` structured-case arm (`:724`) + `render_casez_pattern` (`:2171`) + the `src/ir/case_mux_if_emit.rs` annotation precedent + `src/gen/cone/motifs.rs:832` (`build_casez_patterns`, `wildcard_bits=1`); pin all impl points (the dynamic-selector `CasezMux` predicate; `Module.casez_mux_if_gates`; the in-place emitter body branch computing per-arm `care`/`val` from the pattern/mask constants — `'h` hex, no `__cv`; run last; the `casez_mux_if_emit_prob` knob+flag; the `num_emitted_casez_mux_if_chains` metric schema `1.16→1.17`; the metric-keyed `--casez-mux-if-gate` + the `casez_mux_prob`-biased focus config with correct earlier-selector zeroing). Then `.19b.1` (live) / `.19b.2` (metric+gate) / `.19b.3` (docs). Monitor RAM on heavy builds (`scripts/ram_guard.sh --threshold 90 -- <cmd>`). (Tracked but lower priority: the `agent-mcp.md` `analyze`-example schema-`1.11` drift — a dedicated docs pass.)
- handoff: repo handoff-ready after this commit — `scripts/check_doctrines.sh` green (4 doctrines); `git diff --check` clean; DOCS-ONLY ⇒ DUT byte-identical; decision `0029` + probe evidence durable. origin/main at `4564533` (the `.17b.1` push); 4 commits ahead after this (`.17b.2a`+`.17b.2b`+`.17b.3`+`.18`). Co-staged: `docs/decisions/0029-…md` + `docs/decisions/INDEX.md` + `KNOWLEDGE_MAP.md` + `docs/tasks/STRUCTURED-EMISSION-EXPANSION.md` + `docs/TASK_TREE.md` + `ROADMAP.md` + CHANGES + MEMORY.
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); one runner + one classifier not two (`feedback_full_factorization`); every capability opt-in + MCP-invocable + queryable + CLI-as-shim (decision `0017`); design the API for agents not humans (`feedback_api_for_agents_not_humans`); the book is the user-facing surface and must not drift (`feedback_book_doctrine`); decision/KM fact per durable capability or boundary. Doctrine-enforcement (decision `0026`): every doctrine = a deterministic check in `scripts/check_doctrines.sh`; code-scoped checks are scope-aware (non-code commits exempt).
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
