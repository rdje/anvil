# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `STRUCTURED-EMISSION-EXPANSION.19b.1` **CasezMux masked priority-chain emit-projection (live surface)** commit — a **CODE change** (touches `src/`), task-tree-owned by `.19b`; **default-off (`casez_mux_if_emit_prob = 0.0`) ⇒ DUT byte-identical** (`tests/snapshots.rs` untouched). The NINTH structured surface goes live: new `src/ir/casez_mux_if_emit.rs` (`annotate_casez_mux_if_gates` — the dynamic-selector `GateOp::CasezMux` predicate `operands[0]` not `Node::Constant` + `len >= 4`, excluding `CaseMux`/`case_mux_if`/all sibling marks, run last; 9 lib proofs) + `Module.casez_mux_if_gates: BTreeSet<NodeId>` + `ir/mod.rs` reg + `Config::casez_mux_if_emit_prob` (6 touch points) + `--casez-mux-if-emit-prob` flag + two `gen/mod.rs` rolls (after `case_mux_if`) + the `emit/sv.rs` `GateOp::CasezMux` masked-chain body branch (per arm `care_mask = !wildcard_mask & sel_mask`, `value_masked = pattern_value & care_mask` via the existing `constant_value`/`bitmask` helpers, emitting `(sel & SW'h{care}) == SW'h{val}` + trailing `else W'h0`, **no `__cv`**) + the `:769` trailing-default suppression tweak. No metric/schema bump (`.19b.2` bumps `1.16→1.17`). `cargo check`/`clippy -D warnings`/`fmt` clean; `cargo test --lib` 626→635 (9 new); snapshots 6/6 byte-identical. Forced `casez_mux_if_emit_prob=1.0` sweep (`scratchpad/sweep19`, 6 seeds): every `casez` → a masked chain (0 residual); Verilator `-Wall` Δ=0 (2012/2017/2023) + both Yosys + Icarus clean; ON-vs-OFF sim-equiv (seed 1 8/8 + seed 4 128/128). Prior: `f266d54` (`.19a`). Push cadence: `origin/main` at `4564533` → **6 commits ahead** after this ⇒ no push (threshold 30, `feedback_push_cadence`).
- active_work_unit: **`STRUCTURED-EMISSION-EXPANSION` — NINTH surface LIVE (`.18`+`.19a`+`.19b.1` done); frontier → `.19b.2` (metric + repo-owned gate).** Lane stays `active`. EIGHT surfaces delivered end-to-end + the ninth (the wildcard `CasezMux` → masked `if`/`else if` priority chain, decision `0029`) now live behind `casez_mux_if_emit_prob`; the metric+gate and docs remain.
- next_action: **PNT — implement `STRUCTURED-EMISSION-EXPANSION.19b.2` (metric + repo-owned gate)**, mirroring the eighth surface's `.17b.2a`+`.17b.2b`. (1) `Metrics::num_emitted_casez_mux_if_chains` (`= m.casez_mux_if_gates.len()`, `#[serde(default)]`) + a lib proof ⇒ introspection `SCHEMA_VERSION` `1.16 → 1.17` (the const in `src/introspect/mod.rs:67` + all current-output assertions in `introspect`/`mcp` + the schema doc current-version + a `1.16→1.17` changelog entry + README/USER_GUIDE/CODEBASE refs; leave historical `@ 1.16` attributions intact; defer the book example JSONs to `.19b.3`). (2) `tool_matrix --casez-mux-if-gate` templated on `--case-mux-if-gate`: `ScenarioSet::CasezMuxIfSweep` + `casez_mux_if_focus_config` (comb-only, `casez_mux_if_emit_prob=1.0`, `casez_mux_prob` biased high, and **zero BOTH `comb_mux_prob` and `case_mux_prob`** — both roll before `casez_mux` in `cone.rs`) × 3 strategies + METRIC-KEYED `ModuleReport.emitted_casez_mux_if` (`= num_emitted_casez_mux_if_chains > 0`) + `CoverageSummary.saw_casez_mux_if_emit` + merge + the gap arm + ~5 proofs + fixtures; bank clean `/tmp/anvil-casez-mux-if-gate-r1`. Then `.19b.3` (docs: book ninth-surface section + knobs/USER_GUIDE/README + the book JSON `1.16→1.17` refresh + a KM card). Validate per COMMIT.md; monitor RAM (`scripts/ram_guard.sh --threshold 90 -- <cmd>`). (Tracked-lower: the `agent-mcp.md` `analyze`-example schema-`1.11` drift.)
- handoff: repo handoff-ready after this commit — `scripts/check_doctrines.sh` green (4 doctrines, code-scoped checks satisfied); `git diff --check` clean; default-off ⇒ DUT byte-identical; the ninth surface is live + fully validated. origin/main at `4564533` (the `.17b.1` push); 6 commits ahead after this (`.17b.2a`+`.17b.2b`+`.17b.3`+`.18`+`.19a`+`.19b.1`). Co-staged: `src/ir/casez_mux_if_emit.rs` + `src/ir/mod.rs` + `src/ir/types.rs` + `src/emit/sv.rs` + `src/config.rs` + `src/main.rs` + `src/gen/mod.rs` + `DEVELOPMENT_NOTES.md` + `CODEBASE_ANALYSIS.md` + `docs/tasks/STRUCTURED-EMISSION-EXPANSION.md` + `docs/TASK_TREE.md` + CHANGES + MEMORY.
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); one runner + one classifier not two (`feedback_full_factorization`); every capability opt-in + MCP-invocable + queryable + CLI-as-shim (decision `0017`); design the API for agents not humans (`feedback_api_for_agents_not_humans`); the book is the user-facing surface and must not drift (`feedback_book_doctrine`); decision/KM fact per durable capability or boundary. Doctrine-enforcement (decision `0026`): every doctrine = a deterministic check in `scripts/check_doctrines.sh`; code-scoped checks are scope-aware (non-code commits exempt).
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
