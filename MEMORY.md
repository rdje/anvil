# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `SEMANTIC-INTROSPECTION-EXPANSION.5b.2` commit (`module_reachability` MCP surface + schema `1.7`; SOURCE+docs, DUT byte-identical; closes `.5b`/`.5`; hash backfills next update; prior: `66b25c5` = `.5b.1`; `d05d145` = `.5a`). Working tree clean after this commit. Push cadence: `origin/main` at `63d2622` → **16 commits ahead after this commit; push at ~30** (`feedback_push_cadence`).
- active_work_unit: **`SEMANTIC-INTROSPECTION-EXPANSION` tree `active`, NO active frontier.** **All FOUR named query kinds from decision `0011` delivered end-to-end**: `output_support` (`.1`/`.2`) + `input_reach` (`.3`) + `flop_reset_provenance` (`.4`) + `module_reachability` (`.5` = `.5a`/`.5b.1`/`.5b.2`), introspection schema `1.7`, DUT byte-identical. Further derived-query kinds = open-ended breadth (none named, not a blocker, none retired). Index: `docs/TASK_TREE.md`.
- `.5b.2` delivered (DUT byte-identical): `analyze::supported_query_kinds()` now has all 4 kinds; `src/mcp/mod.rs` `run_analyze` branches to `module/design_module_reachability` (both design + module paths) + the `module_reachability` `-32602` guard; `analyze_schema` enum + `target`/tool/instructions text; `SCHEMA_VERSION` `1.6→1.7` + 8 `"1.6"→"1.7"` test asserts. Docs: schema-doc §6.7 (fourth payload + the stale "two parallel vecs" intro fixed to four) + `1.6→1.7` changelog; book agent-mcp (worked example, 4 JSON examples bumped, table+resource rows); USER_GUIDE + README; new KM card `semantic-introspection-module-reachability` (+ cross-link, KM regenerated to 35 facts); CODEBASE_ANALYSIS + ROADMAP. Validation: `cargo test --lib` 458/0/2 (2 new MCP proofs); snapshots 6/6 byte-identical; clippy/fmt clean; mdbook + book_examples 3/3; e2e `anvil-mcp` smoke (hierarchy seed 42 → schema 1.7, 3 modules all reachable, top depth 0; unknown module → -32602).
- next_action: **PNT decidable frontier is EXHAUSTED across all active trees — next lane is OWNER-DIRECTED.** The `SEMANTIC-INTROSPECTION-EXPANSION` named scope (decision `0011`'s four query kinds) is complete; `SIGNOFF-AUTOMATION-EXPANSION` + `IDENTITY-DEEPENING` are `active` with no named frontier (open-ended breadth only); `STRUCTURED-EMISSION-EXPANSION` is `proposed` (owner-gated — its `.1` design leaf activates only on owner direction). Options for the owner: (a) **activate `STRUCTURED-EMISSION-EXPANSION`** (richer structured synthesizable SV: function/task, interface/modport, nested generate) — register `.1` design leaf + decision record first; (b) open a new named derived-query/identity/signoff breadth leaf (needs a decision record, per lane invariants); (c) name a different lane. No code change should start without a task-tree leaf owning it + (for a new capability) a decision record. **API-audience steering (`feedback_api_for_agents_not_humans`)** still applies to any introspection work.
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); every capability is an opt-in knob; decision/KM fact per durable capability or boundary.
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
