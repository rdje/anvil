# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `BUG-HUNT-ORCHESTRATION.2b.2b` commit (**code, `src/hunt/` + `src/downstream/`**) — the **reproducer-bundle emitter**: `HuntRequest.bundle_root: Option<PathBuf>` (caller-set, decision `0004`) makes each finding write `<bundle_root>/<run_id>/` (`repro.sv` via `generate_dut_artifact`, `knobs.json`, `introspection.json` via new `downstream::introspect_dut_artifact`, `hunt-verdict.json`, `tool-logs/NOTE.txt`, one-command `repro.sh`); `HuntFailure.bundle: Option<HuntBundle>` carries the path + `anvil://` URIs; prefers the minimized reproducer. **`.2b`/`.2b.2` close** — the `src/hunt/` engine is complete. DUT byte-identical (snapshots 6/6). Prior: `a5458ef` = `.2b.2a` (cross-sim fold + `generate_dut_artifact` extract); `5ca0006` = `.2b.1` fix; `f4fc9c8` = `.2b.1`; `8a4fbab` = `.2a`; `b8c1e8b` = `.1` (decision `0018`); … `b90bdff` = `.5` (**pushed** — `origin/main` at `b90bdff`). Working tree clean. Push cadence: `origin/main` at `b90bdff` → **21 commits ahead**; push at ~30 (`feedback_push_cadence`).
- active_work_unit: **`BUG-HUNT-ORCHESTRATION`** — `.1` ADR (decision `0018`) / `.2a` (diff-sim extract) / `.2b.1` (loop core + seed-fix) / `.2b.2a` (cross-sim fold) / `.2b.2b` (reproducer bundle) all **done** ⇒ **`.2b` engine complete**. Frontier **`.2c`** = the **MCP `hunt` controlled tool** wired into `src/mcp` (input schema, `HuntReport` result, failing-run artifact-cache population so `anvil://artifact/<run_id>/{sv,introspection,manifest}` reads resolve, a top-level `hunt` audit record; introspection/MCP doc + schema note; proofs). Then `.2d` (`anvil hunt` CLI, ANVIL's first subcommand + byte-identical guard) → `.2e` (real-tool gate + book/USER_GUIDE/README/KM). Design invariants: thin orchestrator (no new detector/minimizer), bundle = directory, sandbox caller-set (decision `0004`), every report field SCHEMA-DERIVED (decision `0017`). Six other lanes still `active` design-first (`ACCEPTANCE-DIVERGENCE-HUNTING`, `DOWNSTREAM-ADAPTER-EXPANSION`, `KNOB-ERGONOMICS-AND-PRESETS`, `CI-PACKAGING-DISTRIBUTION`, `COVERAGE-STEERED-GENERATION`, `CAPABILITY-BREADTH-EXPANSION`); plus no-frontier `STRUCTURED-EMISSION-EXPANSION` (5 surfaces; 6th = future `.11`), `SEMANTIC-INTROSPECTION-EXPANSION` + `AGENT-MCP-EXPANSION`, `IDENTITY-DEEPENING`, `SIGNOFF-AUTOMATION-EXPANSION`, `LOCAL-REFERENCE-CACHE`. Index: `docs/TASK_TREE.md`.
- next_action: **PNT — `BUG-HUNT-ORCHESTRATION.2c`** (the MCP `hunt` controlled tool over `src/mcp` dispatcher). Touches `src/` ⇒ full `COMMIT.md` cargo gate + decision-`0017` API-completeness gate (invocable + queryable, CLI is a shim). Wrap `hunt::run`; reuse the existing controlled-tool sandbox/allow-list/RAM-guard/audit discipline; the MCP sandbox is fixed (never agent-supplied); populate the artifact cache for each failing `run_id`; append a top-level `hunt` audit record.
- handoff: repo handoff-ready — `.2b.2b` all gates green: `cargo check/clippy/fmt` clean, `cargo test` green (lib `hunt::` **11/11** [was 7] / `downstream::` **20/0** + 2 ignored / tool_matrix 71+1 ignored / book_examples 3/3 / snapshots **6/6 byte-identical**; introspection schema `1.11`). `check_memory_architecture` + KM gen/check green (KM 46 facts). Working tree clean.
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); every capability opt-in; the hunt adds **no** new generator path / no detector / no minimizer (it composes the existing surfaces); decision/KM fact per durable capability or boundary.
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
