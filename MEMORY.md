# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `SEMANTIC-INTROSPECTION-EXPANSION.15b.1` **pure reach_path core** commit — a **code** change, task-tree-owned by `.15b.1`; **DUT byte-identical** (no IR/generator change; `analyze` read-mostly default-off; `tests/snapshots.rs` untouched; **not wired to any emit path** — `supported_query_kinds()` unchanged ⇒ MCP `analyze` still answers thirteen until `.15b.2`). Landed the pure `reach_path` core in `src/introspect/analyze.rs`: `QUERY_REACH_PATH` + `ReachPath { node, depth, path: Vec<PathStep>, sink: Option<String> }` (reuse `PathStep`; `sink` in the `output_support`/`longest_path` target namespace) + the FOURTEENTH `DerivedAnalysis.reach_path` `skip_serializing_if` vec (22 literals gained `reach_path: Vec::new()` via script) + `module_reach_path`/`design_reach_path` + the `reach_path_with` driver + `build_reach_path` (greedy forward descent along the max-`gate_reach_height` reader, ties → smallest reader id) + the `gate_reach_height` sink-aware forward-height memo + `drives_sink`/`pick_sink` + the `reach_path_analysis` constructor. 8 in-crate proofs (the load-bearing `sink.is_some() ⟺ node_reach.fanout_targets>0` + `sink ∈ node_reach`'s set + `depth==path.len()` + register-boundary + leaf-direct-sink + longest-chain + deterministic tie-break). Validation: `cargo test --lib introspect::analyze` 100/0; `cargo test --lib` 728/0/2 (+8); snapshots 6/6 byte-identical; clippy `-D warnings` + fmt + `cargo check --lib` clean. CODEBASE_ANALYSIS amended (fourteen kinds + reach_path block, marked not-yet-wired). Prior: `e0e7b8e` (`.15a`). Push cadence: `origin/main` at `a767415`; **12 commits ahead** after this ⇒ no push (threshold 30).
- active_work_unit: **`SEMANTIC-INTROSPECTION-EXPANSION.15b.2`** (wire `reach_path` to the MCP surface + schema `1.27`). `.15a` design + `.15b.1` pure core landed; `.15` (`reach_path`) OPEN. **Thirteen** derived analyze queries currently surfaced @ schema `1.26`; `.15b.2` makes it fourteen @ `1.27`. Roadmap/codebase/mdBook aligned.
- next_action: **Implement `.15b.2` — wire `reach_path` to the surface.** In `analyze.rs`: add `QUERY_REACH_PATH` to `supported_query_kinds()`. In `src/mcp/mod.rs`: branch `run_analyze` by kind (`module`/`design_reach_path`) in BOTH the design and module arms; update the unknown-target → `-32602` guard to check `analysis.reach_path`; add `"reach_path"` to the `analyze_schema` query enum + the query/target descriptions (the `"node:<id>"` form, paired with `node_reach`/`longest_path`) + the analyze tool description + server instructions; add 2 mcp proofs (`analyze_returns_reach_path_and_caches_it`, `analyze_reach_path_unknown_target_is_invalid_params`). In `src/introspect/mod.rs`: `SCHEMA_VERSION 1.26 → 1.27` + doc comment + the `"1.26" → "1.27"` schema_version test-assertion bumps (≈19 — verify count). Docs: schema-doc §6.7 (the fourteenth `reach_path` payload + `ReachPath` + "one of fourteen parallel result vecs" + File/Producer rows) + the `1.26 → 1.27` changelog + version statements/checklist; book agent-mcp (analyze row + a `reach_path` worked example + the resource line + every JSON envelope `1.26 → 1.27`) + api-tools (query enum/target + JSON examples) + api-introspection (fourteen queries + schema 1.27) + api-reference + api-resources-prompts; USER_GUIDE + README + TOOLBOX (fourteen kinds + schema 1.27); a new KM card `semantic-introspection-reach-path` (+ a cross-link from the `node-reach`/`longest-path` cards; regenerate KNOWLEDGE_MAP). E2e `anvil-mcp` stdio smoke (a seed → schema 1.27, a node's reach_path with the gate chain + the output/flop sink; unknown `node:999999` → `-32602`; a live `reach_path.sink.is_some() ⟺ node_reach.fanout_targets>0` cross-check). Doctrine: SCHEMA-DERIVED/default-off/DUT byte-identical; run the e2e smoke (it catches spec-vs-reality bugs); monitor RAM (`scripts/ram_guard.sh --threshold 90 -- <cmd>`).
- handoff: **repo handoff-ready (`.15a` design + `.15b.1` core landed; frontier `.15b.2`).** `scripts/check_doctrines.sh` green (4 doctrines); tree clean; DUT byte-identical. `reach_path` pure core delivered (8 proofs); MCP wiring is all that remains. origin/main at `a767415`; **12 commits ahead** after this; threshold 30 ⇒ no push. A fresh session resumes at `.15b.2` (see next_action).
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); one runner + one classifier not two (`feedback_full_factorization`); every capability opt-in + MCP-invocable + queryable + CLI-as-shim (decision `0017`); design the API for agents not humans (`feedback_api_for_agents_not_humans`); SEMANTIC-INTROSPECTION stays SCHEMA-DERIVED / no-shadow-simulator (decisions `0004`/`0011`); the book is the user-facing surface and must not drift (`feedback_book_doctrine`); decision/KM fact per durable capability or boundary. Doctrine-enforcement (decision `0026`): every doctrine = a deterministic check in `scripts/check_doctrines.sh`; code-scoped checks are scope-aware (non-code commits exempt).
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
