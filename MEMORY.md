# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `STRUCTURED-EMISSION-EXPANSION.13b` **widen multi-output task groups to k>2 (live)** commit — the live `k>2` widening of the sixth structured surface (decision `0025`); default-off / **DUT byte-identical**. Whole change in `src/ir/multi_output_task_emit.rs`: `const MAX_MULTI_OUTPUT_TASK_GROUP_MEMBERS=8` + helpers `connected_co_support` / `independent_of_all` + the partner loop rewritten "find-first+break" → greedy "admit every eligible partner up to the cap" (group forms iff `>= 1` partner ⇒ `k=2` is the exact subset). **No emitter/config/metric/schema/knob change** (emitter already k-agnostic). 5 new k>2 lib proofs + 10 pair proofs green (15 total); `CODEBASE_ANALYSIS.md` + doc comments + `DEVELOPMENT_NOTES.md` (impl-time note) current. Prior: `8790528`=`…13a`. Push cadence: `origin/main` at `605ec44` → **19 commits ahead** after this ⇒ no push (threshold 30, `feedback_push_cadence`).
- active_work_unit: **`STRUCTURED-EMISSION-EXPANSION` — frontier `.13c`** (user-facing docs closeout). `.13a` (design) + `.13b` (live impl) done. `.13c` = extend `book/src/structured-emission.md` sixth-surface section to note `k>2` groups (byte-verified before/after of a 3-member group) + a note in the `multi_output_task_emit_prob` knob entries (book/USER_GUIDE/README) + update the KM card `multi-output-task-emit` + a decision `0025` cross-ref. mdbook + KM + memory-arch + book_examples green. Docs-only / DUT byte-identical. After `.13c` the lane returns to no current frontier.
- next_action: **Write `STRUCTURED-EMISSION-EXPANSION.13c`** (the user docs). Then re-scan `docs/TASK_TREE.md` for the next active lane and continue the PNT loop. Validation evidence for `.13b`: `cargo test --lib` 605 / snapshots 6/6; gate re-bank `/tmp/anvil-mo-k3-gate-r1` `coverage_gaps=[]` / `12/0` all tools, k=3 present; forced seed-22 k=3 task Verilator `-Wall` Δ=0 vs OFF + iverilog sim-equiv (20000 vectors). A latent out-of-scope follow-up remains: `book/src/agent-mcp.md` untagged ``` fences fail `mdbook test` under mdbook 0.5.2 (CI pins 0.4.40, green).
- handoff: repo handoff-ready after this commit — `scripts/check_doctrines.sh` green (4 doctrines); `cargo fmt/clippy` clean; `cargo test --lib` 605 + snapshots 6/6 byte-identical; `mdbook build` clean; the `.13b` source change is the only `src/` edit and is default-off byte-identical.
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); one runner + one classifier not two (`feedback_full_factorization`); every capability opt-in + MCP-invocable + queryable + CLI-as-shim (decision `0017`); design the API for agents not humans (`feedback_api_for_agents_not_humans`); the book is the user-facing surface and must not drift (`feedback_book_doctrine`); decision/KM fact per durable capability or boundary. Doctrine-enforcement (decision `0026`): every doctrine = a deterministic check in `scripts/check_doctrines.sh`; code-scoped checks are scope-aware (non-code commits exempt).
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
