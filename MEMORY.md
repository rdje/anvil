# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `STRUCTURED-EMISSION-EXPANSION.10b.2` commit (the fifth surface's metric + repo-owned gate: `Metrics::num_emitted_cone_functions` ⇒ introspection schema `1.10→1.11` + `tool_matrix --cone-function-gate` [`ScenarioSet::ConeFunctionSweep` + `build_cone_function_sweep_scenarios`/`cone_function_focus_config` + `ModuleReport.emitted_cone_function` [`__cf(`] + `saw_cone_function_emit` + early-return gap arm + 5 proofs + 6 fixtures]; banked clean `/tmp/anvil-cone-function-gate-r1` [3 scen / 12 mod / 148 cone funcs / `coverage_gaps=[]` / `12/0` Verilator + both Yosys + Icarus]; **default-off / DUT byte-identical, snapshots 6/6, lib 501→502, tool_matrix 68→73**; frontier → `.10b.3`). Prior: `081583d` = `.10b.1`; `2d3a0ca` = `.10a`; `46a377e` = `.9` (decision `0016`); `afb5718` = `.8b`; `3ec4f66` = `.7` (decision `0015`); … `b90bdff` = `.5` (**pushed** — `origin/main` is at `b90bdff`). Working tree clean after this commit. Push cadence: `origin/main` at `b90bdff` → **13 commits ahead (`.6a`,`.6b.1`,`.6b.2a`,`.6b.2b`,`.6b.3`,`LIVE-DOC-CODEBASE-ALIGNMENT.2`,`.7`,`.8a`,`.8b`,`.9`,`.10a`,`.10b.1`,this `.10b.2`); push at ~30** (`feedback_push_cadence`).
- active_work_unit: **`STRUCTURED-EMISSION-EXPANSION` tree `active`, frontier = `.10b.3`**. **FOUR** structured surfaces delivered end-to-end: `function automatic` (`.1`+`.2`), `generate for` loop (`.3`+`.4`), `task automatic` (`.5`+`.6`), wider-lane `generate for` part-select (`.7`+`.8`, decision `0015`). The **FIFTH** surface (the **multi-gate-cone `function automatic`**, decision `0016`) is nearly delivered: `.9` design + `.10a` design-detail + `.10b.1` **live surface** + `.10b.2` **metric + repo-owned gate** all done (introspection schema `1.11`); only `.10b.3` (user docs) remains. Future surfaces (multi-output tasks / nested-multi-level `generate` / `interface`-`modport`) are `.11+`. Index: `docs/TASK_TREE.md`.
- next_action: **PNT — implement frontier leaf `STRUCTURED-EMISSION-EXPANSION.10b.3`** (`.10b.3` owns it; docs-only / DUT byte-identical): a `## The fifth surface: a multi-gate-cone function automatic` section in `book/src/structured-emission.md` (byte-verified seed before/after — the inline per-gate cone becomes one `function automatic <root>__cf(...)` with a function-local per interior gate + the call; single-use-interior rule; separate `cone_function_emit_prob` knob vs the single-gate `function_emit_prob`; the metric @ schema `1.11` + the `--cone-function-gate`; a skip-sentinelled repro bash block) + the `cone_function_emit_prob` knob entry in `book/src/knobs.md` / `USER_GUIDE.md` / README "Current CLI truth" (config-file-only knob) + a KM how-to card `multi-gate-cone-function-emit`. Gates: `mdbook build` + `check_knowledge_map` + `check_memory_architecture` + `cargo test --test book_examples` 3/3 green. Closes `.10b.3` / `.10b` / `.10` — the fifth structured surface delivered end-to-end. Doctrine: task-tree-owned.
- handoff: repo handoff-ready — `cargo check --lib --bin tool_matrix`/`clippy -D warnings`/`fmt --check` clean; `cargo test --lib` 502 + `--bin tool_matrix` 73 + snapshots 6/6 byte-identical; the fifth surface's metric + gate are downstream-clean (`/tmp/anvil-cone-function-gate-r1`, 148 cone functions, `coverage_gaps=[]`, `12/0` Verilator + both Yosys + Icarus); end-to-end `--introspect` default `0` / forced `14` at schema `1.11`; tree clean; all self-checks green; KM at 43 facts / 351 keys. The fifth surface's emitter + metric + gate all ship (`.9`+`.10a`+`.10b.1`+`.10b.2`); only `.10b.3` (user docs) remains — well-specified in the task tree.
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); every capability is an opt-in knob (the fifth surface gets its OWN `cone_function_emit_prob` so the single-gate surface stays byte-identical); decision/KM fact per durable capability or boundary.
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
