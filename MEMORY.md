# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `SEMANTIC-INTROSPECTION-EXPANSION.7b.1` **pure memory_provenance core** commit — a **code change** (`src/introspect/analyze.rs` only), task-tree-owned by `.7b.1`; **DUT byte-identical** (`tests/snapshots.rs` untouched; no IR/generator change; not wired to any emit path). Lands the pure core of the **sixth** derived `analyze` query, `memory_provenance` (per-inferrable-memory port provenance — **the first query to open the documented opaque-`MemRead`-leaf boundary**): `QUERY_MEMORY_PROVENANCE` + `MemoryProvenance { mem, addr_width, data_width, kind, single_port, +4 per-port `SupportCone`s (raddr/waddr/wdata/we) }` + the SIXTH `DerivedAnalysis.memory_provenance` `skip_serializing_if` vec (⇒ the five prior documents byte-identical) + `module_memory_provenance`/`design_memory_provenance` + the `memory_provenance_with` driver (`build_cone` per port; `"mem:<id>"` target; ascending mem id). The 10 existing `DerivedAnalysis` literals gained `memory_provenance: Vec::new()`; `crate::ir::{MemKind, Memory}` import; opaque-`MemRead`-leaf module doc note updated. `supported_query_kinds()` **unchanged** (joins with the `run_analyze` dispatch in `.7b.2`) ⇒ not yet MCP-reachable. Validation: `cargo test --lib introspect::analyze` 39/0 (7 new proofs); `cargo test --lib` 651/0/2; snapshots 6/6; clippy `-D warnings` + fmt + `cargo check --lib` clean. Prior: `221d746` (`.7a`). Push cadence: `origin/main` at `4564533` → **14 commits ahead** after this ⇒ no push (threshold 30, `feedback_push_cadence`).
- active_work_unit: **`SEMANTIC-INTROSPECTION-EXPANSION` — `.7` `memory_provenance` (the sixth derived query); `.7a` design + `.7b.1` pure core landed; frontier `.7b.2` (the MCP surface).** Five queries delivered end-to-end at schema `1.18`; `memory_provenance` is the sixth (core landed, surface pending).
- next_action: **Implement `.7b.2` — wire `memory_provenance` to the MCP surface.** Add `"memory_provenance"` to `analyze::supported_query_kinds()` **and** branch `run_analyze` (`src/mcp/mod.rs`) by query kind (`module`/`design_memory_provenance` in both the design and module paths) in the SAME commit; update the empty-result → `-32602` guard to check `analysis.memory_provenance` for this kind. Bump `SCHEMA_VERSION 1.18 → 1.19` (`src/introspect/mod.rs`) + the `"1.18"` test-assertion bumps + 2 new MCP proofs (returns+caches; unknown `mem:<id>` ⇒ `-32602`). Add the kind to the `analyze_schema` enum + the `target` description (the `"mem:<id>"` form) + the tool/instructions text. Docs: schema-doc §6.7 (sixth payload + `MemoryProvenance`) + `1.18 → 1.19` changelog + "defines 1.19"/§7/checklist; book `agent-mcp` (analyze row + worked example + the JSON envelopes `1.18 → 1.19`) + `api-tools`; USER_GUIDE + README; a new KM card `semantic-introspection-memory-provenance` (+ cross-link from `semantic-introspection-analyze-tool`; regenerate KM). e2e `anvil-mcp` stdio smoke (`memory_prob 1.0` → schema `1.19`, a memory with real port cones; unknown `mem:<id>` ⇒ `-32602`). Monitor RAM (`scripts/ram_guard.sh --threshold 90 -- <cmd>`).
- handoff: **repo handoff-ready after this commit** — `scripts/check_doctrines.sh` green; `git diff --check` clean; DUT byte-identical (snapshots 6/6). `.7b.1` pure core is delivered + lib-tested; `.7b.2` is the only remaining `.7` leaf (surface + schema + docs/KM + e2e smoke). origin/main at `4564533`; 14 commits ahead after this. Co-staged: `src/introspect/analyze.rs` + `CODEBASE_ANALYSIS.md` + `docs/tasks/SEMANTIC-INTROSPECTION-EXPANSION.md` + `docs/TASK_TREE.md` + `CHANGES.md` + `MEMORY.md`.
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); one runner + one classifier not two (`feedback_full_factorization`); every capability opt-in + MCP-invocable + queryable + CLI-as-shim (decision `0017`); design the API for agents not humans (`feedback_api_for_agents_not_humans`); SEMANTIC-INTROSPECTION stays SCHEMA-DERIVED / no-shadow-simulator (decisions `0004`/`0011`); the book is the user-facing surface and must not drift (`feedback_book_doctrine`); decision/KM fact per durable capability or boundary. Doctrine-enforcement (decision `0026`): every doctrine = a deterministic check in `scripts/check_doctrines.sh`; code-scoped checks are scope-aware (non-code commits exempt).
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
