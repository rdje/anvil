# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `BUG-HUNT-ORCHESTRATION.1` commit (**docs-only design ADR**: decision `0018` pins the turnkey fuzz→detect→minimize→reproducer-bundle loop BEFORE any code). Wrote `docs/decisions/0018-bug-hunt-orchestration-loop.md` + INDEX row; `.1` done + pre-split `.2a`…`.2e` in the lane tree; `DEVELOPMENT_NOTES`/`docs/TASK_TREE` row refreshed. **DUT byte-identical (no `src/`).** Prior: `59f248c` = `USABILITY-LANE-OWNERSHIP.1` (decision `0017`); `6125ea5` = `.10b.3`; `015a09b` = `.10b.2`; `081583d` = `.10b.1`; … `b90bdff` = `.5` (**pushed** — `origin/main` at `b90bdff`). Working tree clean. Push cadence: `origin/main` at `b90bdff` → **16 commits ahead** (`.6a`…`.10b.3` = 14, + `USABILITY-LANE-OWNERSHIP.1` + this `BUG-HUNT-ORCHESTRATION.1`); push at ~30 (`feedback_push_cadence`).
- active_work_unit: **`BUG-HUNT-ORCHESTRATION`** — `.1` design ADR (decision `0018`) **done**; frontier **`.2a`** (pure refactor: extract the `tool_matrix` diff-sim run+compare into `diff_sim::run_agreement`, the `.3b.1` precedent), then `.2b` (`src/hunt/` core `hunt::run` + bundle emitter) → `.2c` (MCP `hunt` tool) → `.2d` (`anvil hunt` CLI, ANVIL's first subcommand + byte-identical guard) → `.2e` (real-tool gate + book/USER_GUIDE/README/KM). Design = thin orchestrator over `downstream::validate`/`minimize` (warning already = `ok=false`), bundle = a directory, sandbox caller-set (decision `0004`), every report field SCHEMA-DERIVED (decision `0017`). Six other lanes still `active` design-first (`ACCEPTANCE-DIVERGENCE-HUNTING`, `DOWNSTREAM-ADAPTER-EXPANSION`, `KNOB-ERGONOMICS-AND-PRESETS`, `CI-PACKAGING-DISTRIBUTION`, `COVERAGE-STEERED-GENERATION`, `CAPABILITY-BREADTH-EXPANSION`); plus no-frontier `STRUCTURED-EMISSION-EXPANSION` (5 surfaces; 6th = future `.11`), `SEMANTIC-INTROSPECTION-EXPANSION` + `AGENT-MCP-EXPANSION`, `IDENTITY-DEEPENING`, `SIGNOFF-AUTOMATION-EXPANSION`, `LOCAL-REFERENCE-CACHE`. Index: `docs/TASK_TREE.md`.
- next_action: **PNT — `BUG-HUNT-ORCHESTRATION.2a`** (the first impl leaf: a pure, byte-identical extraction of the diff-sim run+compare from `src/bin/tool_matrix.rs` into a reusable `diff_sim::run_agreement(...)`). NOTE: `.2a`+ touch `src/` ⇒ the full `COMMIT.md` cargo gate (check/test/clippy/fmt + snapshots) applies, and the leaf must satisfy the decision-`0017` API-completeness gate. Alternatively pick another lane `.1` design ADR (e.g. `KNOB-ERGONOMICS-AND-PRESETS.1` / `DOWNSTREAM-ADAPTER-EXPANSION.1`, which feed the hunt). Deferred 6th structured surface (multi-output `task`) = a future `STRUCTURED-EMISSION-EXPANSION.11` decision.
- handoff: repo handoff-ready — this slice is docs-only (no `src/`), so `cargo check/clippy/fmt/test` are unaffected and the code state is exactly the green `.10b.3` state (lib 502 / tool_matrix 73 / book_examples 3/3 / snapshots 6/6 byte-identical; introspection schema `1.11`). `check_memory_architecture` + KM gen/check green (KM now 46 facts — the new `0018` card). Working tree clean.
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); every capability opt-in; the hunt adds **no** new generator path / no detector / no minimizer (it composes the existing surfaces); decision/KM fact per durable capability or boundary.
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
