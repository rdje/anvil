# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `SEMANTIC-INTROSPECTION-EXPANSION.13b.1` **pure longest_path core** commit — a **code + docs** change, task-tree-owned by `.13b.1`; **DUT byte-identical** (no IR/generator change; `analyze` read-mostly default-off; not wired to any emit path; `tests/snapshots.rs` untouched). Adds the pure core of the TWELFTH derived `analyze` query, `longest_path`, in `src/introspect/analyze.rs` — **NOT** yet wired to the MCP surface (`.13b.2`). `QUERY_LONGEST_PATH` + `LongestPath { target, depth, path: Vec<PathStep>, leaf: Option<NodeRef> }` + `PathStep { node, op, width }` (every step a Gate; leaf reuses `NodeRef`) + the TWELFTH `DerivedAnalysis.longest_path` `skip_serializing_if` vec + `module_longest_path`/`design_longest_path` + the `longest_path_with` driver + `build_longest_path` (two pure passes: `node_depth` memo = the recurrence `visit` returns as `cone_depth`, then a greedy max-child-depth descent, ties → smallest operand node id ⇒ a unique byte-stable path) + `node_depth` + `longest_path_analysis`. 20 literals gained `longest_path: Vec::new()` (regex insert). `supported_query_kinds()` UNCHANGED (joins with `run_analyze` dispatch in `.13b.2`). 8 in-crate proofs incl. the load-bearing `longest_path(t).depth == output_support(t).cone_depth` consistency proof + the deterministic tie-break proof. CODEBASE_ANALYSIS amended (twelve query kinds). Validation: `cargo test --lib introspect::analyze` 84/0; `cargo test --lib` 708/0/2 (exactly +8); snapshots 6/6 byte-identical; clippy `-D warnings` + fmt clean. Prior: `fd6739d` (`.13a`). Push cadence: `origin/main` at `a767415`; **6 commits ahead** after this ⇒ no push (threshold 30).
- active_work_unit: `SEMANTIC-INTROSPECTION-EXPANSION.13` (`longest_path`) **open** — `.13a` design + `.13b.1` pure core done; **active frontier → `.13b.2`** (the MCP surface + schema `1.24 → 1.25`). Eleven prior derived analyze queries wired @ schema `1.24`; this twelfth wires at `1.25`. Roadmap/codebase/mdBook aligned.
- next_action: **implement `.13b.2` — the `longest_path` MCP surface** (the `.4b.2`–`.12b.2` precedent). In `src/introspect/analyze.rs`: add `QUERY_LONGEST_PATH` to `supported_query_kinds()`. In `src/mcp/mod.rs`: branch `run_analyze` by kind (`module`/`design_longest_path`) in BOTH the design and module arms; the unknown-target → `-32602` guard checks `analysis.longest_path`; add `"longest_path"` to the `analyze_schema` query enum + the query/target descriptions (the output-name / `"flop:<id>"` target form, paired with `output_support`) + the analyze tool description + server instructions; 2 new mcp proofs (returns + caches the longest_path doc; unknown-target → `-32602`). In `src/introspect/mod.rs`: `SCHEMA_VERSION 1.24 → 1.25` + the doc comment + the `"1.24" → "1.25"` schema_version test-assertion bumps (introspect + mcp). Docs: schema-doc §6.7 (twelfth payload + `LongestPath`/`PathStep` + "one of twelve parallel result vecs" + File/Producer rows) + the `1.24 → 1.25` changelog + "defines 1.25"/§7/checklist; book agent-mcp (analyze row + a `longest_path` worked example + the resource line + every JSON envelope `1.24 → 1.25`) + api-tools (query enum/target) + api-introspection (twelve queries + schema 1.25) + api-reference (1.25) + api-resources-prompts (twelve queries); USER_GUIDE (analyze description + schema 1.25); README (`--introspect` schema 1.25 + the twelfth query); TOOLBOX (twelve kinds + schema 1.25); new KM card `semantic-introspection-longest-path` (+ a cross-link from the output_support card; regenerate KNOWLEDGE_MAP 77 → 78 facts); CODEBASE_ANALYSIS (analyze.rs block now wired + schema-history `1.24 → 1.25`); ROADMAP lane status (.13 done). Run the e2e `anvil-mcp` stdio smoke (a seed → schema 1.25, an output's longest path with the realizing gate chain + the terminal leaf; unknown target → `-32602`) — it catches spec-vs-reality bugs. `cargo test --lib` + snapshots 6/6 + clippy + fmt + `mdbook build` + `book_examples` + `check_doctrines` + KM green. Doctrine: SCHEMA-DERIVED/default-off/DUT byte-identical; monitor RAM (`scripts/ram_guard.sh --threshold 90 -- <cmd>`).
- handoff: **repo handoff-ready (active frontier `.13b.2`).** `scripts/check_doctrines.sh` green (4 doctrines); tree clean; DUT byte-identical. `.13a` + `.13b.1` landed; the pure core is lib-proven (708/0/2) but not yet MCP-reachable. origin/main at `a767415`; **6 commits ahead** after this; threshold 30 ⇒ no push. A fresh session should implement `.13b.2` (see next_action).
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); one runner + one classifier not two (`feedback_full_factorization`); every capability opt-in + MCP-invocable + queryable + CLI-as-shim (decision `0017`); design the API for agents not humans (`feedback_api_for_agents_not_humans`); SEMANTIC-INTROSPECTION stays SCHEMA-DERIVED / no-shadow-simulator (decisions `0004`/`0011`); the book is the user-facing surface and must not drift (`feedback_book_doctrine`); decision/KM fact per durable capability or boundary. Doctrine-enforcement (decision `0026`): every doctrine = a deterministic check in `scripts/check_doctrines.sh`; code-scoped checks are scope-aware (non-code commits exempt).
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
