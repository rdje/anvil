# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `STRUCTURED-EMISSION-EXPANSION.1` commit (activate the lane + decision `0012`; **DOCS-ONLY**, no source change; DUT byte-identical; hash backfills next update; prior: `d7a7eef` = `.5b.2`; `66b25c5` = `.5b.1`). Working tree clean after this commit. Push cadence: `origin/main` at `63d2622` → **17 commits ahead after this commit; push at ~30** (`feedback_push_cadence`).
- active_work_unit: **`STRUCTURED-EMISSION-EXPANSION` tree `active` (owner-directed `2026-06-16`); frontier = `.2a`.** `.1` design done (decision `0012`): the first richer-structured surface = a default-off, valid-by-construction combinational `function automatic` emit-projection of an existing combinational cone (the `output_support` support-leaf boundary gives its parameter list), over interface/modport (weak Yosys synth) + nested generate (bigger blast radius); opt-in `function_emit_prob` default `0.0` ⇒ byte-identical; downstream gate = Verilator + both Yosys modes + Icarus (`saw_combinational_function_emit`). `.2` pre-split → `.2a` (design-detail, frontier) + `.2b` (impl). Index: `docs/TASK_TREE.md`. (Prior lane `SEMANTIC-INTROSPECTION-EXPANSION` `active`/no-frontier — all 4 query kinds delivered at schema `1.7`.)
- `.1` delivered (docs-only): decision `0012` + `INDEX.md` row; tree activated (`proposed → active`); `.2`/`.2a`/`.2b` registered; DEVELOPMENT_NOTES design-detail entry is for `.2a` (next), not `.1`. Validation: `bash scripts/check_memory_architecture.sh` + `bash knowledge-map/scripts/check_knowledge_map.sh` clean; KM regenerated (decision `0012` carries `answers:` front-matter).
- next_action: **PNT — execute `STRUCTURED-EMISSION-EXPANSION.2a`** (design-detail, **docs-only/no source**): ground the combinational `function automatic` surface in the real `src/emit/sv.rs` `to_sv_with_modules` + the `soft_union.rs` / `aggregate_layout` emit-projection precedents + `src/config.rs`. Pin (1) the rules-first cone-selection rule (which `Gate` nodes qualify; size/depth bounds); (2) gen-time annotation (the `soft_union.rs`/`aggregate_layout` precedent) vs pure emit-time pass; (3) the `function automatic` signature + body rendering (params = cone support leaves; local decls vs single return; width/`logic` typing); (4) the `function_emit_prob` knob semantics + default `0.0` byte-identical; (5) the downstream-gate scenario shape (`saw_combinational_function_emit`). Write the DEVELOPMENT_NOTES design-detail entry + pre-split `.2b` → `.2b.1`/`.2b.2` if broad. Then `.2b` (impl: real emitter change + the downstream-clean gate — **substantial, focus-intensive; a fresh session is reasonable before `.2b`**). Pattern: the design-leaf → impl flow is the same as the SEMANTIC-INTROSPECTION lane. Doctrine: rules-first (`feedback_rules_first_generation`), default-off/byte-identical (`feedback_never_retire_strategies`), no code without a task-tree leaf owning it.
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); every capability is an opt-in knob; decision/KM fact per durable capability or boundary.
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
