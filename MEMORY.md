# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `SEMANTIC-INTROSPECTION-EXPANSION.9b.2` **node_drivers MCP surface + schema 1.21** commit — a **code change** (`src/mcp/mod.rs` + `src/introspect/{analyze,mod}.rs` + docs/book/KM), task-tree-owned by `.9b.2`; **DUT byte-identical** (`tests/snapshots.rs` untouched). Closes `.9b`/`.9` ⇒ the **eighth** derived `analyze` query, `node_drivers` (per-node immediate 1-hop driver adjacency + each node's `GateOp` — the atomic node-level primitive complementing the transitive `output_support` cone, the fourth beyond decision `0011`'s four named kinds), is **delivered end-to-end**: registry + `run_analyze` dispatch (`module`/`design_node_drivers`) in one commit + the vec-aware `-32602` guard + `SCHEMA_VERSION 1.20 → 1.21` (14 assertion bumps + 2 new MCP proofs) + the `analyze_schema` enum (the `"node:<id>"` target) + schema-doc §6.7/changelog + book(agent-mcp/api-tools)/USER_GUIDE/README + a KM card (`KNOWLEDGE_MAP` 73 → 74 facts / 722 keys) + an e2e `anvil-mcp` stdio smoke (seed 7 → schema `1.21`, 1674 nodes / 1438 gate nodes with ops, node 3 = `slice` over input `i_1`; unknown `node:999999` → `-32602`). Validation: `cargo test --lib` 671/0/2; snapshots 6/6; clippy `-D warnings` + fmt + `mdbook build` + `book_examples` 3/3 + `check_doctrines` green. Prior: `1c84d76` (`.9b.1`). Push cadence: `origin/main` at `4564533` → **21 commits ahead** after this ⇒ no push (threshold 30, `feedback_push_cadence`).
- active_work_unit: **`SEMANTIC-INTROSPECTION-EXPANSION` — EIGHT derived queries delivered end-to-end (schema `1.21`); back at a NO-FRONTIER boundary (stays `active`).** `output_support`/`input_reach`/`flop_reset_provenance`/`module_reachability` (decision `0011`'s four) + `flop_dependencies` (`.6`) + `memory_provenance` (`.7`) + `fsm_provenance` (`.8`) + `node_drivers` (`.9`, the fourth beyond the four named kinds — the node-level immediate-fanin adjacency + `GateOp`, the atomic primitive complementing the transitive cone). Nothing retired; further derived-query kinds are open-ended breadth (`node_readers` the dual; a per-FSM/per-memory reach; per-child-module provenance).
- next_action: **PNT at a no-frontier boundary (`feedback_pick_and_roll_at_no_frontier`) — self-select the next lane.** Candidate continuations: stay in `SEMANTIC-INTROSPECTION-EXPANSION` (`.10` = `node_readers`, the immediate fan-OUT dual of `node_drivers` — the cheapest next, transpose the 1-hop adjacency; or a per-FSM/per-memory reach; or per-child-module provenance), or switch lanes — `STRUCTURED-EMISSION-EXPANSION` (9 surfaces; needs a genuinely new construct + fresh probe + decision), `IDENTITY-DEEPENING` (deeper module-equivalence), `SIGNOFF-AUTOMATION-EXPANSION` (new tool columns / implicit-knob axes). Doctrine: **task-tree-own the chosen leaf BEFORE any code** (a `.Na` design-detail leaf + `docs/TASK_TREE.md` row first; per-query lanes follow the `.Na`/`.Nb.1`/`.Nb.2` cadence, no new numbered decision when an existing decision governs). Monitor RAM (`scripts/ram_guard.sh --threshold 90 -- <cmd>`).
- handoff: **repo handoff-ready after this commit** — `scripts/check_doctrines.sh` green (4 doctrines); `git diff --check` clean; DUT byte-identical (snapshots 6/6; lib 671/0/2); the eighth derived query is delivered end-to-end (live + schema-versioned + MCP-dispatched + cached + documented + KM-carded + e2e-smoked). A long, productive session (`.9a` + `.9b.1` + `.9b.2`, after a full bootstrap read) ⇒ a fresh session is reasonable before picking the next lane. Note: a pre-existing prior-session gap — the `.8a`/`.8b.1`/`.8b.2` rows are missing from this tree's Verification Log + Commit Log (the Changelog has only `.8b.2`); a dedicated docs-hygiene backfill can close it (not bundled into the `.9*` slices to keep the leaf↔commit mapping clean). origin/main at `4564533`; 21 commits ahead after this. Co-staged: `src/introspect/analyze.rs` + `src/introspect/mod.rs` + `src/mcp/mod.rs` + `docs/AGENT_INTROSPECTION_SCHEMA.md` + `book/src/agent-mcp.md` + `book/src/api-tools.md` + `USER_GUIDE.md` + README + `docs/knowledge/semantic-introspection-node-drivers.md` + `docs/knowledge/semantic-introspection-analyze-tool.md` + `KNOWLEDGE_MAP.md` + `CODEBASE_ANALYSIS.md` + `ROADMAP.md` + `docs/tasks/SEMANTIC-INTROSPECTION-EXPANSION.md` + `docs/TASK_TREE.md` + CHANGES + MEMORY.
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); one runner + one classifier not two (`feedback_full_factorization`); every capability opt-in + MCP-invocable + queryable + CLI-as-shim (decision `0017`); design the API for agents not humans (`feedback_api_for_agents_not_humans`); SEMANTIC-INTROSPECTION stays SCHEMA-DERIVED / no-shadow-simulator (decisions `0004`/`0011`); the book is the user-facing surface and must not drift (`feedback_book_doctrine`); decision/KM fact per durable capability or boundary. Doctrine-enforcement (decision `0026`): every doctrine = a deterministic check in `scripts/check_doctrines.sh`; code-scoped checks are scope-aware (non-code commits exempt).
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
