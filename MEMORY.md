# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this **`BOOK-LANE-COVERAGE.3`** (book) commit — **closes the `BOOK-LANE-COVERAGE` tree**: `book/src/frontend-lane.md` + the deferred `SUMMARY.md` frontend row + cross-links from `introduction.md`/`faq.md`/`microdesign-lane.md`, so every pre-existing book mention of a non-DUT lane now routes to a real chapter. Real seed-0 output pasted; parity claims cross-checked against `tests/frontend_parity.rs`. `mdbook build` exit 0; `book_examples` 3/3. Prior: **`efb5ee5`** `BOOK-LANE-COVERAGE.2` (microdesign chapter + the new "Artifact Lanes" part). Prior: **`a4c9f9e`** `LANE-OUT-FILENAME.1` (code) commit — filenames only ⇒ **no SV/manifest byte change in any lane**, `tests/snapshots.rs` **6/6 byte-identical**, DUT untouched. `LaneArtifact` gains builder-truth `pub top` (DUT `design.top`, microdesign new `top_name(seed)` single-sourcing `emit_sv`/`emit_manifest`, frontend `unit.top.name`); `run_non_dut_lane` stems `--out` from it; text-scraping `parse_top_name` **deleted**. REJECT→PASS: frontend `--out` wrote `child_0.sv` (first `module` decl — frontend emits child stubs before the top) vs documented `acc_0.sv`; now correct. Gate: check/clippy(-D warnings)/fmt clean; `cargo test` under `ram_guard --threshold 90` **exit 0** (pipeline 125/0, snapshots 6/6); `--lib umbrella` 9/9 incl. new `lane_artifact_top_is_builder_truth`. Prior: `5223452` (`BOOK-LANE-COVERAGE.1`), `98e31e1` (`EVIDENCE-BANK-DURABILITY.2` / decision `0030`). Push cadence: 3 ahead of `origin/main`; 30-commit threshold.
- active_work_unit: **none open** — `BOOK-LANE-COVERAGE` (`done`, PGEN gap closed) and `LANE-OUT-FILENAME` (`done`) both closed this session. NEXT UNIT = **NEW owner directive (`2026-07-29`): volume / data-locality policy** (CLAUDE.md §12/§13). Projects moved to the 4TB SSD; repo is `/dev/disk5s1`, `/tmp`+`/private/tmp` are `/dev/disk3s1s1` ⇒ **off-volume**. ALL project data must be repo-root-relative and on the repo volume. Known violations to audit/fix under a new tree: (a) `src/downstream/` MCP `validate`/`minimize` sandboxes and `tests/book_examples.rs` use `std::env::temp_dir()` ⇒ off-volume by default (code change, needs a leaf first); (b) decision `0030`'s rejected option (iii) is now **partly mandated** — future banks must be repo-derived + on-volume (the committed digest stays the citation form) ⇒ amend/supersede `0030`; (c) fold in `EVIDENCE-BANK-DURABILITY.3` (mechanize 0030) + `.4` (breadcrumb sweep; `/tmp/anvil-validate-3f1c…` is illustrative — do not label). Then `.5` deferred; `CAPABILITY-BREADTH-EXPANSION.1` (`pending` ADR).
- next_action: open the volume/data-locality task tree (register BEFORE any `src/` edit), then audit + fix the `temp_dir()` call sites to a repo-root-derived on-volume path, and amend decision `0030`. Session scratch now lives at `.cache/scratch/` (gitignored, on-volume) — do NOT use `/private/tmp` for project data.
- handoff: **repo handoff-ready.** `check_doctrines.sh` green (4/4); tree clean; `mdbook build` + book-examples green. Session scratch migrated off `/private/tmp` to `.cache/scratch/` (copy/verify/delete: 13 files, 25744 bytes, tree-hash `a5473f7d…` identical, source deleted, residue census clean). **Surfaced, awaiting owner decision:** the pre-move checkout at `~/Documents/github/anvil` still exists — 1.1G off-volume (877M `target/`), HEAD `ecda0e7` is a strict ancestor of ours, identical 118-file reference cache ⇒ **no unique work**; deletion is destructive so it is NOT actioned.
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); one runner + one classifier not two (`feedback_full_factorization`); every capability opt-in + MCP-invocable + queryable + CLI-as-shim (decision `0017`); design the API for agents not humans (`feedback_api_for_agents_not_humans`); SEMANTIC-INTROSPECTION stays SCHEMA-DERIVED / no-shadow-simulator (decisions `0004`/`0011`); the book is the user-facing surface and must not drift (`feedback_book_doctrine`); decision/KM fact per durable capability or boundary. Doctrine-enforcement (decision `0026`): every doctrine = a deterministic check in `scripts/check_doctrines.sh`; code-scoped checks are scope-aware (non-code commits exempt).
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
