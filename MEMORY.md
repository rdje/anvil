# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `BUG-HUNT-ORCHESTRATION.2a` commit (**code: pure byte-identical refactor**) — extracted the `tool_matrix` per-module diff-sim run+compare into `anvil::diff_sim::run_agreement(work_dir, top, sv_text, n_vectors) -> DiffSimReport` (+ moved `DiffSimReport`/`DutPort`/`parse_dut_ports`/`emit_testbench_for_ports`); `tool_matrix`'s `run_diff_sim_for_module` is now a thin wrapper. Byte-identical: snapshots 6/6, `tool_matrix_report.json` schema unchanged. Prior: `b8c1e8b` = `BUG-HUNT-ORCHESTRATION.1` (decision `0018`, docs-only); `59f248c` = `USABILITY-LANE-OWNERSHIP.1` (decision `0017`); `6125ea5` = `.10b.3`; … `b90bdff` = `.5` (**pushed** — `origin/main` at `b90bdff`). Working tree clean. Push cadence: `origin/main` at `b90bdff` → **17 commits ahead**; push at ~30 (`feedback_push_cadence`).
- active_work_unit: **`BUG-HUNT-ORCHESTRATION`** — `.1` design ADR (decision `0018`) **done**; `.2a` (diff-sim extract) **done**; frontier **`.2b`** = the `src/hunt/` library core: `HuntRequest`/`HuntReport`/`HuntFailure` + `hunt::run(&HuntRequest) -> HuntReport` composing `downstream::validate`/`minimize` (warning already = `ok=false`) + optional `diff_sim::run_agreement` (now available from `.2a`) + the reproducer-bundle emitter (a directory `<root>/<run_id>/`); cargo-portable proofs; no CLI/MCP yet. Then `.2c` (MCP `hunt` tool) → `.2d` (`anvil hunt` CLI, ANVIL's first subcommand + byte-identical guard) → `.2e` (real-tool gate + book/USER_GUIDE/README/KM). Design invariants: thin orchestrator (no new detector/minimizer), bundle = directory, sandbox caller-set (decision `0004`), every report field SCHEMA-DERIVED (decision `0017`). Six other lanes still `active` design-first (`ACCEPTANCE-DIVERGENCE-HUNTING`, `DOWNSTREAM-ADAPTER-EXPANSION`, `KNOB-ERGONOMICS-AND-PRESETS`, `CI-PACKAGING-DISTRIBUTION`, `COVERAGE-STEERED-GENERATION`, `CAPABILITY-BREADTH-EXPANSION`); plus no-frontier `STRUCTURED-EMISSION-EXPANSION` (5 surfaces; 6th = future `.11`), `SEMANTIC-INTROSPECTION-EXPANSION` + `AGENT-MCP-EXPANSION`, `IDENTITY-DEEPENING`, `SIGNOFF-AUTOMATION-EXPANSION`, `LOCAL-REFERENCE-CACHE`. Index: `docs/TASK_TREE.md`.
- next_action: **PNT — `BUG-HUNT-ORCHESTRATION.2b`** (the `src/hunt/` library core + reproducer-bundle emitter; the engine both the MCP tool `.2c` and the CLI `.2d` shim over). Touches `src/` ⇒ full `COMMIT.md` cargo gate (check/test/clippy/fmt + snapshots) + the decision-`0017` API-completeness gate. Pre-split `.2b` when picked (types + `hunt::run` + bundle emitter + proofs). Deferred 6th structured surface (multi-output `task`) = a future `STRUCTURED-EMISSION-EXPANSION.11` decision.
- handoff: repo handoff-ready — `.2a` is a code slice, all gates green: `cargo check/clippy/fmt` clean, `cargo test` green (lib **505** / tool_matrix **71**+1 ignored / book_examples 3/3 / snapshots **6/6 byte-identical**; introspection schema `1.11`). `check_memory_architecture` + KM gen/check green (KM 46 facts). Working tree clean.
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); every capability opt-in; the hunt adds **no** new generator path / no detector / no minimizer (it composes the existing surfaces); decision/KM fact per durable capability or boundary.
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
