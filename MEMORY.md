# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `STRUCTURED-EMISSION-EXPANSION.19b.2a` **casez-mux-if metric + introspection schema 1.17** commit — a **CODE change** (touches `src/metrics.rs`/`introspect`/`mcp`), task-tree-owned by `.19b`; **default-off ⇒ DUT byte-identical** (a post-hoc `Metrics` field changes no RTL; `tests/snapshots.rs` untouched). `Metrics::num_emitted_casez_mux_if_chains` (`= m.casez_mux_if_gates.len()`, `#[serde(default)]`, **exact** — static-selector `CasezMux` excluded) + the `metrics_count_emitted_casez_mux_if_chains` lib proof ⇒ introspection `SCHEMA_VERSION` `1.16 → 1.17` across all current-output refs (`introspect` const + doc-comment history + 3 assertions; 8 `mcp` assertions; the schema doc current-version ×3 + a `1.16→1.17` changelog entry; README envelope ×2 + USER_GUIDE envelope ×1 + CODEBASE envelope); per-metric `@ 1.16` landing attributions intact, book JSONs deferred to `.19b.3`. `cargo test --lib` 635→636; clippy `-D warnings` + fmt clean; snapshots 6/6 byte-identical; live `--introspect` `1.17` + metric `0` default (seed 2) / `1` forced (seed 4). Prior: `61b8c8e` (`.19b.1`). Push cadence: `origin/main` at `4564533` → **7 commits ahead** after this ⇒ no push (threshold 30, `feedback_push_cadence`).
- active_work_unit: **`STRUCTURED-EMISSION-EXPANSION` — NINTH surface LIVE + metered (`.18`+`.19a`+`.19b.1`+`.19b.2a` done); frontier → `.19b.2b` (the repo-owned gate).** Lane stays `active`. The ninth surface (the wildcard `CasezMux` → masked `if`/`else if` priority chain, decision `0029`) is live behind `casez_mux_if_emit_prob` and surfaced at introspection schema `1.17`; the repo-owned gate + docs remain.
- next_action: **PNT — implement `STRUCTURED-EMISSION-EXPANSION.19b.2b` (the repo-owned `tool_matrix --casez-mux-if-gate`)**, templated 1:1 on `--case-mux-if-gate` (the `.17b.2b` precedent; ~30 touch points in `src/bin/tool_matrix.rs`). Add: a `--casez-mux-if-gate` CLI flag + `ScenarioSet::CasezMuxIfSweep` + `build_casez_mux_if_sweep_scenarios` + `casez_mux_if_focus_config` (comb-only node-id+egraph, `casez_mux_if_emit_prob=1.0`, `casez_mux_prob` biased high, and **zero BOTH `comb_mux_prob` AND `case_mux_prob`** — verify the `cone.rs` roll order: `comb_mux`/`case_mux` roll before `casez_mux`, so both must be zeroed or they starve the casez roll) × 3 strategies + **METRIC-KEYED** `ModuleReport.emitted_casez_mux_if` (`= prepared.metrics.num_emitted_casez_mux_if_chains > 0`, NOT a text token) + `CoverageSummary.saw_casez_mux_if_emit` + merge + `MatrixReport.casez_mux_if_gate` + run-plan/dispatch/mutual-exclusion/summarize/gap arm/3 exhaustive match arms/slug arms/`test_cli` default + ~5 cargo proofs + ModuleReport fixtures. Validate: `cargo test --bin tool_matrix` (new proofs) + snapshots 6/6; bank clean `/tmp/anvil-casez-mux-if-gate-r1` (`coverage_gaps = []` / `saw_casez_mux_if_emit` / Verilator + both Yosys + Icarus). Then `.19b.3` (docs: book ninth-surface section + knobs/USER_GUIDE/README + the book JSON `1.16→1.17` refresh + a KM card) ⇒ `.19b`/`.19` close, NINTH surface delivered end-to-end. Monitor RAM (`scripts/ram_guard.sh --threshold 90 -- <cmd>`). (Tracked-lower: the `agent-mcp.md` `analyze`-example schema-`1.11` drift.)
- handoff: repo handoff-ready after this commit — `scripts/check_doctrines.sh` green (4 doctrines, code-scoped checks satisfied); `git diff --check` clean; default-off ⇒ DUT byte-identical; the ninth surface is live + metered + fully validated. origin/main at `4564533` (the `.17b.1` push); 7 commits ahead after this. Co-staged: `src/metrics.rs` + `src/introspect/mod.rs` + `src/mcp/mod.rs` + `docs/AGENT_INTROSPECTION_SCHEMA.md` + README + USER_GUIDE + CODEBASE_ANALYSIS + `docs/tasks/STRUCTURED-EMISSION-EXPANSION.md` + `docs/TASK_TREE.md` + CHANGES + MEMORY.
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); one runner + one classifier not two (`feedback_full_factorization`); every capability opt-in + MCP-invocable + queryable + CLI-as-shim (decision `0017`); design the API for agents not humans (`feedback_api_for_agents_not_humans`); the book is the user-facing surface and must not drift (`feedback_book_doctrine`); decision/KM fact per durable capability or boundary. Doctrine-enforcement (decision `0026`): every doctrine = a deterministic check in `scripts/check_doctrines.sh`; code-scoped checks are scope-aware (non-code commits exempt).
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
