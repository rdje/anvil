# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `BUG-HUNT-ORCHESTRATION.2e` commit (**`tests/hunt_e2e.rs` + `Cargo.toml` dev-dep** + book/USER_GUIDE/README/ROADMAP/KM/live-docs) — the **real-tool e2e gate + closeout that CLOSES the `BUG-HUNT-ORCHESTRATION` tree.** Two `#[ignore]` tool-gated proofs drive the real `anvil hunt` binary against real Verilator (clean 3-seed sweep `n_failures=0` + the byte-identical reproducer recipe); honest boundary: ANVIL has no by-construction downstream failure to fabricate, the bundle dir format is unit-proven by `.2b.2b`. `serde_json` → `[dev-dependencies]`. Book "bug-hunting loop end to end" rewritten (turnkey `hunt` CLI+MCP). KM card `bug-hunt-cli` (46→47 facts). ROADMAP lane 1 DONE. Default `anvil` build / DUT byte-identical (test+docs only). Prior: `6172041` = `.2d` (CLI); `b2e0228` = `.2c` (MCP tool); `a7b011f` = `.2b.2b`; `a5458ef` = `.2b.2a`; `5ca0006`/`f4fc9c8` = `.2b.1`; `8a4fbab` = `.2a`; `b8c1e8b` = `.1` (decision `0018`); … `b90bdff` (**pushed** — `origin/main` at `b90bdff`). Working tree clean. Push cadence: `origin/main` at `b90bdff` → **24 commits ahead**; push at ~30 (`feedback_push_cadence`).
- active_work_unit: **none open — `BUG-HUNT-ORCHESTRATION` is CLOSED** (north-star usability lane 1 delivered: the `hunt::run` engine + the `anvil hunt` CLI + the MCP `hunt` tool + the reproducer bundle + the real-tool gate; decision `0018`). **Next: PNT-pick the next lane** (`feedback_pick_and_roll_at_no_frontier`). The six remaining owner-directed lanes are all `active` design-first (`.1` ADR, no code): **`ACCEPTANCE-DIVERGENCE-HUNTING`** (the natural next *detector* that plugs into the now-complete hunt engine — decision `0018` names it), `DOWNSTREAM-ADAPTER-EXPANSION` (more acceptance tools for hunt), `KNOB-ERGONOMICS-AND-PRESETS` (curated `--profile`s for hunt), `CI-PACKAGING-DISTRIBUTION` (CI wrapper around hunt), `COVERAGE-STEERED-GENERATION`, `CAPABILITY-BREADTH-EXPANSION`. Plus no-frontier `STRUCTURED-EMISSION-EXPANSION` (5 surfaces; 6th = future `.11`), `SEMANTIC-INTROSPECTION-EXPANSION` + `AGENT-MCP-EXPANSION`, `IDENTITY-DEEPENING`, `SIGNOFF-AUTOMATION-EXPANSION`, `LOCAL-REFERENCE-CACHE`. Index: `docs/TASK_TREE.md`.
- next_action: **PNT — start `ACCEPTANCE-DIVERGENCE-HUNTING` at its `.1` design ADR** (the recommended next lane: it's the "tool A accepts / tool B rejects" + version-vs-version divergence detector that plugs into the just-closed hunt engine, decision `0018`). Register the tree (`docs/tasks/ACCEPTANCE-DIVERGENCE-HUNTING.md` + a `docs/TASK_TREE.md` row) if not already, then write the design ADR (next decision id `0019`): the divergence-detection axis over `downstream::validate` across tools/versions, its `HuntReport`-style projection, MCP+CLI surfaces (decision `0017`), default-off / byte-identical. Docs-only ⇒ no cargo gate; `check_memory_architecture` + KM gen/check.
- handoff: repo handoff-ready — `.2e` all gates green: `cargo check/clippy/fmt` clean, `cargo test` green (tests/hunt_e2e **2/2 --ignored** vs real Verilator + 0 portable / anvil-bin 12/0 / mcp:: 66/0 / hunt:: 11/11 / downstream:: 20/0 / book_examples 3/3 / snapshots **6/6 byte-identical**; introspection schema `1.11`). `check_memory_architecture` + KM gen/check green (**KM 47 facts**). Working tree clean.
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); every capability opt-in; the hunt adds **no** new generator path / no detector / no minimizer (it composes the existing surfaces); decision/KM fact per durable capability or boundary.
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
