# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `ACCEPTANCE-DIVERGENCE-HUNTING.2c.2` commit (**the `tool_matrix` divergence column** — `src/bin/tool_matrix.rs` + `CODEBASE_ANALYSIS` + the tree + `docs/TASK_TREE.md` + DEVELOPMENT_NOTES + ROADMAP + CHANGES + MEMORY). Added the `--divergence` opt-in flag + `ModuleReport`/`DesignReport.divergence: Option<DivergenceReport>` (the `diff_sim` column precedent) + `unit_divergence(...)`: it gathers the verilator/yosys/iverilog `ToolInvocation`s the matrix ALREADY ran on a unit, assembles a `ValidateReport`, and classifies it through the shared `divergence::classify_report` — **no extra tool spawn**, and (unlike `--diff-sim`) **no tool-clean precondition** (a divergence is most interesting when one tool rejects what another accepts). Gated by the `.divergence-subset` sentinel reusing `select_diff_sim_subset`/`classify_diff_sim_axis`; shared membership factored into `scenario_in_named_subset`. The **opportunistic** `saw_acceptance_divergence` fact (lit by `summarize_coverage`/`summarize_design_coverage` from a `diverged` unit, unioned by `merge_coverage`) is **NEVER a required gate** (`compute_coverage_gaps` untouched — all-agree is the valid-by-construction steady state, decision `0019`). +4 cargo-portable proofs. `cargo test --bin tool_matrix` 75/0 (1 ignored); full `cargo test --lib` 531/0 (lib untouched); snapshots 6/6 byte-identical + book_examples 3/3; clippy/fmt clean. Real-tool smoke `/tmp/anvil-divergence-col-smoke` (Verilator 5.046 + Yosys 0.64 both modes): 17/0, `divergence_enabled=true`, subset=2 axes, each subset unit 3× `accept` `diverged=false`, `saw_acceptance_divergence=false` (steady state). Default-off / DUT byte-identical. Prior: `7142fd7` = `.2c.1` (the `hunt::run` fold; now **== origin/main**, pushed); `4028537` = `.2b`; `dd6c9eb` = `.2a`; `e7a3de8` = `.1` (decision `0019`). Working tree clean after this commit. Push cadence: `origin/main` at `7142fd7` → **1 commit ahead** after this; push at ~30 (`feedback_push_cadence`).
- active_work_unit: **`ACCEPTANCE-DIVERGENCE-HUNTING`** — `.1` ADR (`0019`) + `.2a` (`tool_verdict`) + `.2b` (`src/divergence/` core) + `.2c.1` (the `hunt::run` fold) + `.2c.2` (the `tool_matrix` column) all **done**; `.2c` (both children) closed; frontier **`.2d`** (the MCP `divergence` controlled tool + `anvil` CLI shim). Other owner-directed lanes still design-first `.1`: `DOWNSTREAM-ADAPTER-EXPANSION`, `KNOB-ERGONOMICS-AND-PRESETS`, `CI-PACKAGING-DISTRIBUTION`, `COVERAGE-STEERED-GENERATION`, `CAPABILITY-BREADTH-EXPANSION`. Plus no-frontier `STRUCTURED-EMISSION-EXPANSION`, `SEMANTIC-INTROSPECTION-EXPANSION`+`AGENT-MCP-EXPANSION`, `IDENTITY-DEEPENING`, `SIGNOFF-AUTOMATION-EXPANSION`, `LOCAL-REFERENCE-CACHE`. Index: `docs/TASK_TREE.md`.
- next_action: **PNT — implement `ACCEPTANCE-DIVERGENCE-HUNTING.2d`** (the MCP `divergence` controlled tool + CLI shim, decision `0017`): a controlled `divergence` tool in `src/mcp` `tools_list`/`tools_call` shimming `divergence::run` (inheriting the `validate`/`hunt` allow-list/sandbox/RAM-guard/audit), returning the `DivergenceReport`, caching each divergent `run_id` so `anvil://artifact/<run_id>/{sv,introspection}` resolve, + a top-level `divergence` audit record; the `anvil` CLI `--divergence` shim (on `anvil hunt` — flip the `divergence:false` that `.2c.1` wired in `src/main.rs`/`src/mcp`); `book/src/agent-mcp.md` tool list/table; cargo-portable proofs (decision-0017 API-completeness gate). `.2d` is task-tree-owned. Then `.2e` (version axis: `DivergenceOptions.tool_specs` + `ToolInvocation.version` + `version_mismatch`), `.2f` (real-tool e2e gate + book/USER_GUIDE/README/KM closeout — closes the tree).
- handoff: repo handoff-ready — code state green (`cargo test --bin tool_matrix` 75/0, full lib 531/0, introspection schema `1.11`); `check_memory_architecture` + KM gen/check green (**KM 48 facts**, no new card for `.2c.2` — the KM how-to card lands at `.2f` closeout). Working tree clean after commit.
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); one classifier not two (`feedback_full_factorization`); every capability opt-in + MCP-invocable + queryable + CLI-as-shim (decision `0017`); the divergence detector adds **no** generator path / no oracle (it composes the existing surfaces); decision/KM fact per durable capability or boundary.
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
