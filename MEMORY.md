# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `STRUCTURED-EMISSION-EXPANSION.2b.2b` commit (repo-owned `tool_matrix --function-emit-gate`; **DUT byte-identical** — harness-only, no generator/emitter/config-default change; hash backfills next update; prior: `503ce60` = `.2b.2a`; `15844d9` = `.2b.1`). Working tree clean after this commit. Push cadence: `origin/main` at `63d2622` → **21 commits ahead after this commit; push at ~30** (`feedback_push_cadence`).
- active_work_unit: **`STRUCTURED-EMISSION-EXPANSION` tree `active`; frontier = `.2b.2c`.** `.1`+`.2a`+`.2b.1` (live surface)+`.2b.2a` (metric+schema `1.8`)+`.2b.2b` (the `tool_matrix` gate, banked clean) done; only `.2b.2c` (user-facing closeout) remains in the `.2b.2` split. Index: `docs/TASK_TREE.md`.
- `.2b.2b` delivered: `src/bin/tool_matrix.rs` repo-owned `--function-emit-gate` — `ScenarioSet::FunctionEmitSweep` + `build_function_emit_sweep_scenarios`/`function_emit_focus_config` (one comb-only `function_emit_prob=1.0` DUT × 3 construction strategies) + `ModuleReport.emitted_combinational_function` (from `sv_text.contains("function automatic")`) + `CoverageSummary.saw_combinational_function_emit` (lit when emitted-function module is Verilator-success + clean-Yosys; the gate runs the FULL tool plan because a synthesizable function is universally accepted — unlike the Verilator-only `union soft` up-opt) + merge + early-return `compute_coverage_gaps` arm + 5 cargo-portable proofs + 6 `ModuleReport` fixture updates. Templated on `--signoff-knob-sweep-gate` (scaffolding) + the soft_union detection precedent. No schema bump (harness-only). Banked clean `/tmp/anvil-function-emit-gate-r1` (3 scenarios / 12 modules / **608 emitted functions** / `coverage_gaps=[]` / `saw_combinational_function_emit=true` / `12/0` Verilator + both Yosys + Icarus compile). `cargo test --bin tool_matrix` 58/1ign; lib 468; snapshots 6/6 byte-identical.
- next_action: **PNT — execute `STRUCTURED-EMISSION-EXPANSION.2b.2c`** (the user-facing closeout, **docs-only / byte-identical**): a book chapter/section on the combinational `function automatic` surface (under "How It Works" or "Reference", with examples) + the USER_GUIDE `function_emit_prob` **knob** entry + the README "Current CLI truth" `anvil --function-emit-prob` **knob** entry + a Knowledge Map card if a durable how-to is warranted (decision `0012` already carries `answers:`). NOTE: the `tool_matrix --function-emit-gate` *gate* entries already landed in README + USER_GUIDE with `.2b.2b`; `.2b.2c` adds the `anvil`-side `function_emit_prob` *knob* user docs + the book chapter. Acceptance: `mdbook build book` clean; `check_knowledge_map` clean; self-checks clean. After `.2b.2c`, `.2b.2`/`.2b`/`.2` all close; `STRUCTURED-EMISSION-EXPANSION` then has no active frontier (future `task`/nested-`generate`/`interface` surfaces are `.3+`, open-ended). Doctrine: pure-docs leaf (no task-tree-code rule), but still full `COMMIT.md`.
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); every capability is an opt-in knob; decision/KM fact per durable capability or boundary.
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
