# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `STRUCTURED-EMISSION-EXPANSION.13c` **wider (k>2) multi-output task user docs** commit — docs-only / no source / **DUT byte-identical**. Closes `.13c` / `.13` — the first deepening of the sixth structured surface (wider `k>2` multi-output `task automatic` groups) is delivered end-to-end. `book/src/structured-emission.md` sixth-surface intro reframed (pair → group `k>=2`, up to 8) + a `### Wider groups (k > 2)` subsection with a real seed-22 three-member task + the connected-co-support / all-member-independence rules + the cap; `multi_output_task_emit_prob` knob entry in `book/src/knobs.md`/`USER_GUIDE.md`/README; KM card `multi-output-task-emit` refreshed (KM 589→593 keys). Prior: `68aeea2`=`…13b`. Push cadence: `origin/main` at `605ec44` → **20 commits ahead** after this ⇒ no push (threshold 30, `feedback_push_cadence`).
- active_work_unit: **NO active STRUCTURED-EMISSION frontier — `.13` (the first deepening of the sixth surface) is `done`.** The `STRUCTURED-EMISSION-EXPANSION` lane stays `active` as an open-ended lane with **no current frontier**: SIX surfaces delivered end-to-end + the sixth now widened to `k>2`. Further surfaces (nested/multi-level `generate`, `interface`/`modport`) are `.14+`, each its own decision when picked. Other `active` lanes (`IDENTITY-DEEPENING`, `SIGNOFF-AUTOMATION-EXPANSION`, `SEMANTIC-INTROSPECTION-EXPANSION`, `DOWNSTREAM-ADAPTER-EXPANSION`, `KNOB-ERGONOMICS-AND-PRESETS`, `CI-PACKAGING-DISTRIBUTION`, `CAPABILITY-BREADTH-EXPANSION`, `LOCAL-REFERENCE-CACHE`) are also at no-frontier boundaries.
- next_action: **PNT at a no-frontier boundary** (`feedback_pick_and_roll_at_no_frontier` — self-select the next lane; don't surface a steer question). Natural candidates: (a) a NEW STRUCTURED-EMISSION surface `.14+` (nested/multi-level `generate` or `interface`/`modport` — both previously probed; `interface`/`modport` was empirically DISQUALIFIED on Yosys/Icarus, nested-generate lacks a by-construction 2D source — so a fresh angle is needed), or (b) re-scan `docs/TASK_TREE.md` for another `active` lane with a pickable next increment. A latent out-of-scope follow-up: `book/src/agent-mcp.md` untagged ``` fences fail `mdbook test` under mdbook 0.5.2 (CI pins 0.4.40, green) — a `LIVE-DOC-BOOK-ALIGNMENT`-style fix if/when prioritized.
- handoff: repo handoff-ready after this commit — `scripts/check_doctrines.sh` green (4 doctrines; 64 facts / 593 keys); `cargo fmt/clippy` clean; `cargo test --lib` 605 + snapshots 6/6 byte-identical; `mdbook build` + `book_examples` 3/3 green; no `src/` touched in `.13c`.
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); one runner + one classifier not two (`feedback_full_factorization`); every capability opt-in + MCP-invocable + queryable + CLI-as-shim (decision `0017`); design the API for agents not humans (`feedback_api_for_agents_not_humans`); the book is the user-facing surface and must not drift (`feedback_book_doctrine`); decision/KM fact per durable capability or boundary. Doctrine-enforcement (decision `0026`): every doctrine = a deterministic check in `scripts/check_doctrines.sh`; code-scoped checks are scope-aware (non-code commits exempt).
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
