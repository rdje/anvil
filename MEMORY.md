# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `STRUCTURED-EMISSION-EXPANSION.12b.1` **multi-output task automatic emit-projection (live surface)** commit — the FIRST source change of the sixth surface, **default-off / DUT byte-identical**. `Config::multi_output_task_emit_prob` (default 0.0, validation, dump-config) + `--multi-output-task-emit-prob` CLI flag + `Module.multi_output_task_groups: BTreeMap<NodeId,Vec<NodeId>>` (leader→partners) + new `src/ir/multi_output_task_emit.rs` (`annotate_multi_output_task_groups`: one-roll-per-leader pairing on a shared **non-constant** operand + the `in_fanin` bounded backward DFS soundness check) + `src/ir/mod.rs` reg + two `gen/mod.rs` rolls (after `task_emit`, before `cone_function`) + `cone_function_emit::sibling_marked` extension + `src/emit/sv.rs` (the `mo_task_member` set + decl/call section reusing `render_cone_gate_expr` with an **empty `interior_set`** + the per-gate passthrough; members keep their wires) + 11 lib proofs. NO metric/schema bump (knob rides the version; metric+gate at `.12b.2`). lib 589→599, snapshots 6/6 byte-identical; forced sweep `/tmp/anvil-mo-sweep/`: `__mt(` 4/5 seeds, Verilator -Wall Δ=0 vs OFF (2012/2017/2023) + Yosys both + Icarus clean, seed-7 exhaustively sim-equiv to inline ref (128 inputs). Prior: `46669ec`=`…12a` design-detail; `c31fbb0`=`…11` (decision `0025` + tree split); `df21b5c`=`…2b.3`. Push cadence: `origin/main` at `605ec44` → **8 commits ahead** after this ⇒ no push (threshold 30, `feedback_push_cadence`).
- active_work_unit: **`STRUCTURED-EMISSION-EXPANSION` tree `active`, frontier `.12b.2`** (metric `num_emitted_multi_output_tasks` + introspection schema `1.13 → 1.14` + the repo-owned `tool_matrix --multi-output-task-gate`; decision `0025`; `.11` design + `.12a` design-detail + `.12b.1` **live surface** done). Five prior structured surfaces delivered end-to-end (function `.1`+`.2`, generate-for `.3`+`.4`, task `.5`+`.6`, wide-lane part-select `.7`+`.8`, cone-function `.9`+`.10`).
- next_action: **PNT — `STRUCTURED-EMISSION-EXPANSION.12b.2`** (metric + schema + gate): add `Metrics::num_emitted_multi_output_tasks` (= `m.multi_output_task_groups.len()`, `#[serde(default)]`) in `metrics::compute()` + a lib proof; surface it in introspection `module_metrics` ⇒ bump `SCHEMA_VERSION` `1.13 → 1.14` (all current-output refs: assertions in `src/introspect/mod.rs` + `src/mcp/mod.rs` + `docs/AGENT_INTROSPECTION_SCHEMA.md` changelog + README + USER_GUIDE + 5 `book/src/agent-mcp.md` JSONs + CODEBASE envelope; historical attributions intact); add `tool_matrix --multi-output-task-gate` + `ScenarioSet::MultiOutputTaskSweep` + `build_multi_output_task_sweep_scenarios`/focus config (comb-only `multi_output_task_emit_prob=1.0` × 3 strategies, `terminal_reuse_prob` high + shallow `max_depth` + `min_outputs≥2`) + `ModuleReport.emitted_multi_output_task` (`__mt(` detection) + `saw_multi_output_task_emit` + `MatrixReport` + early-return gap arm + proofs + fixtures; bank it clean. Then `.12b.3` (docs). Default-off / DUT byte-identical.
- handoff: repo handoff-ready after this commit — `cargo check/clippy/fmt/test --lib` (599) + snapshots 6/6 all green; `check_memory_architecture` + KM check green; the sixth surface is live behind a default-off knob.
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); one runner + one classifier not two (`feedback_full_factorization`); every capability opt-in + MCP-invocable + queryable + CLI-as-shim (decision `0017`); design the API for agents not humans (`feedback_api_for_agents_not_humans`); the book is the user-facing surface and must not drift (`feedback_book_doctrine`); decision/KM fact per durable capability or boundary.
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
