# MEMORY - resume pointer (layer A; overwrite-only, keep <= 50 lines)

## How to resume
- Read `README.md`, then `MEMORY_ARCHITECTURE.md`.
- Work is task-tree tracked under `docs/tasks/`; index: `docs/TASK_TREE.md`.
- Durable cross-task facts live in `docs/decisions/`.
- Question-keyed retrieval facts are indexed in `KNOWLEDGE_MAP.md`.
- Commit completed leaves per `COMMIT.md`; include the leaf id in the subject.

## Current state (overwrite this block; do not append history)
- latest_commit: this `COVERAGE-STEERED-GENERATION.2b` **achieved-coverage-readout** commit — the second **code** slice of the steering lane (the READ half). `src/introspect/coverage.rs` (new): `CoverageReadout` (`knob_fire_rates` + `category_fire_rates` maps of `KnobCoverage{attempts,fires,fire_rate}` + gate-kind/operand/depth histograms) + `module_coverage(&Metrics)` / `design_coverage(&[Metrics])` (cross-child aggregate) — pure projection of the `Metrics` already recorded (SCHEMA-DERIVED, zero new truth, decision `0011`). `KnobId::all()` + `category_of_name()` in `src/ir/types.rs` (the single name→category inversion; `feedback_full_factorization`). New `IntrospectionPayload::coverage_readout` section (`skip_serializing_if=Option::is_none`) on DUT module/design docs + sibling `CoverageDocument`/`coverage_document()` for the new pure MCP `coverage` tool (`run_coverage` reuses the embedded readout — one projection, not two). `SCHEMA_VERSION` `1.11→1.12`; schema doc §5 row + §6.8 + changelog. **Determinism gotcha:** `fire_rate` is a round-half-up integer-ppm quotient then one exact `u64→f64/1e6` (a raw f64 division diverged by 1 ULP between the MCP build path and a recompute — caught by the pre-existing `introspect_tool_round_trips` exact-equality test, fixed WITHOUT weakening it). **SCHEMA-DERIVED / DUT `.sv` byte-identical; `--dump-config` byte-identical (no `Config` change).** Full cargo gate green (check/test [snapshots 6/6 + new coverage unit + introspect/mcp coverage tests]/clippy -D warnings/fmt). Prior: `2530bfd`=`…2a` steering core; `02a960d`=`…1` notes; `8f14705`=`…1` ADR. Push cadence: `origin/main` at `7142fd7` → **28 commits ahead** after this; **push at ~30** (`feedback_push_cadence`) — ~2 more.
- active_work_unit: **`COVERAGE-STEERED-GENERATION` — `.1` + `.2a` + `.2b` DONE; frontier = `.2c`** (the outer measure→derive→re-steer convenience `derive_steering_from_coverage` + the `--steer` CLI shim + book [`algorithm.md` steering subsection + `agent-mcp.md`] + USER_GUIDE + a KM card; close `.2`). `CI-PACKAGING-DISTRIBUTION` stays `active` with no frontier.
- next_action: **PNT — continue.** Advance to `COVERAGE-STEERED-GENERATION.2c` (the outer derive helper + `--steer` CLI shim + the owner-deferred steering-lane docs — book/USER_GUIDE/KM, incl. the new `coverage` MCP tool — closing `.2`; task-tree-owned). Code slice ⇒ full COMMIT.md gate; watch RAM per `0003-resource-safe-validation` (`scripts/ram_guard.sh --threshold 90 -- <cmd>`). Push once this brings the count to ~30.
- handoff: repo fully handoff-ready & **in sync** after this commit — full `cargo test` green; snapshots 6/6; `check_memory_architecture` + KM gen/check green (**KM 58 facts**, unchanged — KM card for steering lands at `.2c`); mdbook clean; introspection schema now `1.12`. Pending doc-drift (tracked, owner-sanctioned, all → `.2c` per decision `0023` pre-split): book/USER_GUIDE/KM for the steering lane (incl. the new `coverage` MCP tool) **and** the book introspection-example refresh (`agent-mcp.md`/`api-tools.md`/`api-reference.md` still show `schema_version "1.11"` + no `coverage_readout` field — refreshed holistically at `.2c`, not piecemeal). The authoritative contract doc `docs/AGENT_INTROSPECTION_SCHEMA.md` is already at `1.12`.
- lane invariants (all lanes): rules-first / no generate-then-filter (valid-by-construction); default-off / byte-identical where output could change; `tests/snapshots.rs` untouched by default; **no retirement** (`feedback_never_retire_strategies`); one runner + one classifier not two (`feedback_full_factorization`); every capability opt-in + MCP-invocable + queryable + CLI-as-shim (decision `0017`); design the API for agents not humans (`feedback_api_for_agents_not_humans`); the book is the user-facing surface and must not drift (`feedback_book_doctrine`); decision/KM fact per durable capability or boundary.
- in_flight_uncommitted: none after this commit. Repo handoff-ready: tree clean, self-checks green, resume pointer current.
- blockers: none. Validation policy: focused checks + `scripts/ram_guard.sh --threshold 90 -- <cmd>` for heavy builds (note the `--` separator); monitor RAM, stop >90% per `0003-resource-safe-validation`.

## Validation policy
- For workflow-doc memory/retrieval architecture leaves, use focused functional checks; full `cargo test` is not required per owner instruction.
- If a future full suite is run, monitor RAM; stop immediately above 90% RAM and record it as an environment/resource stop.
